# PR #9225: Add KEP-7066: Custom metadata labels for Kueue metrics

**Summary**: Add KEP-7066: Custom metadata labels for Kueue metrics

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9225

**Last updated**: 2026-03-06T20:08:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/metadata-kueue-metrics` → `main`
- **Created**: 2026-02-13T16:05:20Z
- **Updated**: 2026-03-06T20:08:02Z
- **Merged**: 2026-03-06T19:38:19Z
- **Closed**: 2026-03-06T19:38:19Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+418 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind documentation

#### What this PR does / why we need it:

KEP proposing a configuration option to promote Kubernetes labels from ClusterQueue and LocalQueue objects into Prometheus metric labels.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
KEP for #7066 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-13T16:05:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 18ae647e2b0139ca70a3ea7e1561603db5029e41 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a6a6bca71cbd0008ff8309 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-13T16:07:12Z

CC: @varunsyal @ecdatadog @yashnib

### Comment by [@varunsyal](https://github.com/varunsyal) — 2026-02-18T17:54:53Z

Thanks for putting this together, it looks good to me and very useful!

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-24T12:16:38Z

Hi all, checking in to see if there’s anything else needed before this PR can move forward.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-24T12:24:04Z

> Review on preemption cost https://github.com/kubernetes-sigs/kueue/pull/8551 

Thank you, checking the new version

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:36:44Z

/kind kep

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:38:11Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-03T09:36:16Z

LGTM label has been added.  <details>Git tree hash: 7e7164f051f5bf27ff37f78a72a5680ba5997d90</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T19:11:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9225#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9225#pullrequestreview-3881354313" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9225#pullrequestreview-3905506758" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-13T17:17:27Z

- **keps/7066-custom-metric-labels/kep.yaml:21** by [@kannon92](https://github.com/kannon92) — 2026-02-13T17:17:27Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.17"
```

  0.17 is getting quite busy.
  
  Could we push this to 0.18 to help alleviate pressure on maintainers?
  
  cc @tenzen-y @mimowo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-13T17:20:44Z

- **keps/7066-custom-metric-labels/README.md:87** by [@kannon92](https://github.com/kannon92) — 2026-02-13T17:20:44Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
```

  Right now though this would still be an issue if someone adds too many customLabels in the configuration. 
  
  It would be worth consulting with sig-instrumentation on what kind of limits we should enforce.
  
  cc @dgrisonnet

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-13T17:21:43Z

- **keps/7066-custom-metric-labels/README.md:254** by [@kannon92](https://github.com/kannon92) — 2026-02-13T17:21:43Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
+   the controller must delete the old series before reporting with the
+   new values. This requires tracking previous custom label values per
+   queue and detecting changes during reconciliation.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
+   `cost.center` both map to `custom_cost_center`).
+
+The `custom_` prefix ensures that any Kubernetes label key can be
+safely used without conflicting with built-in metric labels.
+
+Validation runs regardless of whether the `CustomMetricLabels` feature
+gate is enabled, so configuration errors are caught early.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation (invalid keys, duplicates after sanitization).
+- Label sanitization edge cases.
+- Metric reporting with custom labels; missing labels produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
+  only built-in labels.
+
+#### Integration Tests
+
+- ClusterQueue and LocalQueue with matching labels: custom labels appear
+  at `/metrics`.
+- Queue without any of the configured custom labels: empty string values
+  appear in metric series.
+- Label value change on a queue: stale series cleaned up, new series
+  appears.
+- Empty `customLabels`: only built-in labels (backward compatibility).
+- `CustomMetricLabels` enabled with `LocalQueueMetrics` disabled:
+  ClusterQueue metrics get custom labels, LocalQueue metrics are not
+  registered.
+
+### Graduation Criteria
+
+**Alpha (v0.17)**: Feature gate `CustomMetricLabels` (disabled by default),
+config field, ClusterQueue and LocalQueue metrics support, validation,
```

  LocalQueueMetrics is still alpha so we should consider if we want to add support for this.
  
  I believe there is still interest in getting LocalQueueMetrics to alpha but the author is no longer involved in the project.

### Review by [@dgrisonnet](https://github.com/dgrisonnet) (commented) — 2026-02-13T17:39:59Z

- **keps/7066-custom-metric-labels/README.md:87** by [@dgrisonnet](https://github.com/dgrisonnet) — 2026-02-13T17:39:59Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
```

  There will definitely be a slight additional cost when storing the custom labels, but as it was pointed later in the doc, this will not introduce new timeseries and the cardinality of the metrics will remain O(cluster queues).
  
  I wouldn't necessarily enforce any limits since it doesn't increase cardinality and users have to opt-in. We have a similar option in kube-state-metrics for labels and annotations and never had any problems with it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T11:14:48Z

- **keps/7066-custom-metric-labels/README.md:87** by [@mimowo](https://github.com/mimowo) — 2026-02-16T11:14:48Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
```

  I think no need to enforce additional limits. The "custom" labels feature is "opt-in", both enabling the feature and setting labels on CQs is in the admin's domain. 
  
  Label cardinality is more of a risk for resources which are managed by users, like Pods, Workloads, Jobs or namespaces.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T11:15:07Z

- **keps/7066-custom-metric-labels/kep.yaml:10** by [@mimowo](https://github.com/mimowo) — 2026-02-16T11:15:08Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  Feel free to add me here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T11:19:03Z

- **keps/7066-custom-metric-labels/README.md:118** by [@mimowo](https://github.com/mimowo) — 2026-02-16T11:19:03Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
```

  I would propose to have 
  
  ```golang
  CustomLabels []ControllerMetricsCustomLabel
  ```
  
  Where 
  ```golang
  type ControllerMetricsCustomLabel struct {
    Name string
  }
  ```
  This is effectively the same for now, but will give us more flexibility in the future. For example to support reading the custom labels for custom labels.

### Review by [@yashnib](https://github.com/yashnib) (commented) — 2026-02-17T07:49:56Z

- **keps/7066-custom-metric-labels/README.md:213** by [@yashnib](https://github.com/yashnib) — 2026-02-17T00:08:34Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
+   the controller must delete the old series before reporting with the
+   new values. This requires tracking previous custom label values per
+   queue and detecting changes during reconciliation.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
```

  Collision handling should be explicit: if two configured Kubernetes keys sanitize to the same Prometheus label name (e.g. cost-center, cost.center, cost/center → custom_cost_center), the controller should fail to start with a clear error listing the derived name and all conflicting keys. Anything else (warnings or “last one wins”) is too easy to miss and will silently produce wrong labels in dashboards.

- **keps/7066-custom-metric-labels/README.md:96** by [@yashnib](https://github.com/yashnib) — 2026-02-17T00:15:22Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
```

  Could you rewrite the stale-series cleanup section as a concrete step-by-step algorithm? Right now it says “track previous values and delete old series,” but it would help to spell out exactly what happens on queue update: compute old vs new custom-label tuple, if it changed then delete the old series before emitting the new one, and clarify that this applies across all queue-scoped metric vectors (CQ + LQ; core + cache + resource)

- **keps/7066-custom-metric-labels/README.md:144** by [@yashnib](https://github.com/yashnib) — 2026-02-17T01:08:57Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
```

  Please make sanitization fully deterministic. Right now it only calls out - . / → _ , but we should specify the exact mapping and validation so implementations/tests match. Suggestion: validate key is a valid K8s label key; replace any non [A-Za-z0-9_] with _ ; prepend custom_; then validate it matches Prometheus label-name regex. Include a note that collisions after sanitization are a hard startup error.

- **keps/7066-custom-metric-labels/README.md:188** by [@yashnib](https://github.com/yashnib) — 2026-02-17T01:23:52Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
```

  Nice-to-have: Add a one-liner noting that if any affected queue-scoped metrics are cumulative (counter/histogram), a label-tuple change creates a new series and values start fresh for that labelset. This avoids confusion in dashboards when labels change.

- **keps/7066-custom-metric-labels/README.md:87** by [@yashnib](https://github.com/yashnib) — 2026-02-17T06:48:20Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
```

  I agree it’s opt-in and the allowlist pattern is reasonable (similar to kube-state-metrics), but I don’t think we can say “no new timeseries” unconditionally. Prometheus series are labelset-defined, so if a configured label value changes you’ll create a new series (and the old one persists until we delete it); the KEP already mentions this under stale metrics. I’d reword the claim to “steady-state active series is bounded under assumptions (queue-keyed metrics, fixed customLabels, stable values), but value changes cause series churn and require cleanup,” and add guidance to avoid high-churn keys. Limits are optional, but the wording should not imply it’s free.

- **keps/7066-custom-metric-labels/README.md:297** by [@yashnib](https://github.com/yashnib) — 2026-02-17T07:38:05Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
+   the controller must delete the old series before reporting with the
+   new values. This requires tracking previous custom label values per
+   queue and detecting changes during reconciliation.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
+   `cost.center` both map to `custom_cost_center`).
+
+The `custom_` prefix ensures that any Kubernetes label key can be
+safely used without conflicting with built-in metric labels.
+
+Validation runs regardless of whether the `CustomMetricLabels` feature
+gate is enabled, so configuration errors are caught early.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation (invalid keys, duplicates after sanitization).
+- Label sanitization edge cases.
+- Metric reporting with custom labels; missing labels produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
```

  > Validation runs regardless of whether the `CustomMetricLabels` feature gate is enabled
  
  We validate customLabels regardless of whether CustomMetricLabels is enabled, and the test plan says customLabels is ignored when the gate is disabled. That behavior is reasonable, but it shouldn’t be silent. Please require a clear startup warning when customLabels is set but the feature gate is disabled (validated but not applied), so operators don’t assume the labels are active.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T15:11:08Z

- **keps/7066-custom-metric-labels/kep.yaml:21** by [@mimowo](https://github.com/mimowo) — 2026-02-18T15:11:08Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.17"
```

  I'm ok to try including this as alpha in 0.17, it does not seem complex

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T15:12:22Z

- **keps/7066-custom-metric-labels/kep.yaml:21** by [@mimowo](https://github.com/mimowo) — 2026-02-18T15:12:22Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.17"
```

  In any case, it is ok to continue under 0.17, if the maintainers don't have time to review on time, or the contibutor, then we can postpone to 0.18.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T10:30:24Z

- **keps/7066-custom-metric-labels/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2026-02-19T10:30:24Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
```

  Please add me and yashnib, kannon92 to reflect the current set of reviewers

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:33:19Z

- **keps/7066-custom-metric-labels/README.md:87** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:33:19Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:33:26Z

- **keps/7066-custom-metric-labels/kep.yaml:10** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:33:26Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:33:36Z

- **keps/7066-custom-metric-labels/README.md:118** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:33:36Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:35:02Z

- **keps/7066-custom-metric-labels/README.md:213** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:35:02Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
+   the controller must delete the old series before reporting with the
+   new values. This requires tracking previous custom label values per
+   queue and detecting changes during reconciliation.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
```

  Done. Updated the validation section.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:35:13Z

- **keps/7066-custom-metric-labels/README.md:144** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:35:13Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:35:25Z

- **keps/7066-custom-metric-labels/README.md:188** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:35:25Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:35:38Z

- **keps/7066-custom-metric-labels/README.md:297** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:35:38Z
```diff
@@ -0,0 +1,301 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` string list to the `metrics` config section. At
+startup, Kueue sanitizes each key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Each custom label adds a dimension to existing
+queue metric series. However, since custom label values are read from
+the queue object itself, the total number of series stays the same
+since no new combinations are introduced. Documentation will advise keeping the
+list focused and avoiding labels whose values change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old time series
+persists until explicitly deleted. The controller must track previous
+custom label values per queue and delete the old series when a change
+is detected, before reporting with the new values.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // Each key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    // +optional
+    CustomLabels []string `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - "team"
+    - "env"
+    - "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Hyphens, dots, and slashes in Kubernetes label keys are replaced with
+underscores, then the `custom_` prefix is prepended:
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: When a queue's custom label value changes,
+   the controller must delete the old series before reporting with the
+   new values. This requires tracking previous custom label values per
+   queue and detecting changes during reconciliation.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
+   `cost.center` both map to `custom_cost_center`).
+
+The `custom_` prefix ensures that any Kubernetes label key can be
+safely used without conflicting with built-in metric labels.
+
+Validation runs regardless of whether the `CustomMetricLabels` feature
+gate is enabled, so configuration errors are caught early.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation (invalid keys, duplicates after sanitization).
+- Label sanitization edge cases.
+- Metric reporting with custom labels; missing labels produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-19T10:35:45Z

- **keps/7066-custom-metric-labels/kep.yaml:8** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-19T10:35:44Z
```diff
@@ -0,0 +1,28 @@
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
+authors:
+  - "@IrvingMg"
+status: provisional
+creation-date: 2026-02-13
+reviewers:
+  - TBD
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T12:17:59Z

- **keps/7066-custom-metric-labels/kep.yaml:12** by [@mimowo](https://github.com/mimowo) — 2026-02-24T12:17:59Z
```diff
@@ -1,20 +1,15 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
 authors:
-  - "@kshalot"
+  - "@IrvingMg"
 status: provisional
-creation-date: 2026-02-04
+creation-date: 2026-02-13
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
-approvers:
   - "@mimowo"
-  - "@mwielgus"
-  - "@tenzen-y"
-
-see-also:
-  - "/keps/8729-workload-admission-constraints
-  - "/keps/8691-concurrent-admission
+  - "@kannon92"
+  - "@yashnib"
+approvers:
+  - TBD
```

  you can add me here

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-24T12:22:41Z

- **keps/7066-custom-metric-labels/kep.yaml:12** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-24T12:22:41Z
```diff
@@ -1,20 +1,15 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
 authors:
-  - "@kshalot"
+  - "@IrvingMg"
 status: provisional
-creation-date: 2026-02-04
+creation-date: 2026-02-13
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
-approvers:
   - "@mimowo"
-  - "@mwielgus"
-  - "@tenzen-y"
-
-see-also:
-  - "/keps/8729-workload-admission-constraints
-  - "/keps/8691-concurrent-admission
+  - "@kannon92"
+  - "@yashnib"
+approvers:
+  - TBD
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T12:28:02Z

- **keps/7066-custom-metric-labels/README.md:343** by [@mimowo](https://github.com/mimowo) — 2026-02-24T12:28:02Z
```diff
@@ -0,0 +1,336 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. At
+startup, Kueue sanitizes each entry's `name` key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label's value creates a
+new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old series
+persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is a Kubernetes label key whose value will be added as an extra
+    // Prometheus label on ClusterQueue and LocalQueue metrics.
+    // The key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    Name string `json:"name"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"
+    - name: "env"
+    - name: "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Each `name` entry is converted to a Prometheus label name as follows:
+
+1. Validate that the input is a valid [Kubernetes label key][k8s-labels].
+2. Replace every character that is **not** in `[A-Za-z0-9_]` with `_`.
+3. Prepend the prefix `custom_`.
+4. Verify the result matches the Prometheus label-name regex
+   `[a-zA-Z_][a-zA-Z0-9_]*` (guaranteed by construction since the
+   prefix starts with a letter).
+5. If two or more input keys produce the same derived name after steps
+   1–4, report a **fatal startup error** (see [Validation](#validation)).
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: The controller keeps an in-memory map
+   from queue name to last-reported custom label values. On every
+   queue reconciliation:
+   1. Read the configured custom label keys from the queue object to
+      get the new values.
+   2. Look up the previous values for this queue in the map.
+   3. If any value differs: remove the old series from every
+      queue-scoped metric vector (ClusterQueue and LocalQueue; core,
+      cache, and resource metrics), then report with the new values.
+   4. Update the map entry with the new values.
+
+   For cumulative metrics (counters and histograms), removing and
+   re-creating a series resets it to zero. This is expected; `rate()`
+   and `increase()` handle counter resets correctly.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
+   `cost.center` both map to `custom_cost_center`). If two or more
+   input keys collide after sanitization, the controller must emit a
+   **fatal startup error** that lists the derived Prometheus label name
+   and all conflicting input keys (e.g.,
+   `duplicate custom metric label "custom_cost_center" produced by keys: "cost-center", "cost.center"`).
+
+The `custom_` prefix ensures that any Kubernetes label key can be
+safely used without conflicting with built-in metric labels.
+
+Validation runs regardless of whether the `CustomMetricLabels` feature
+gate is enabled, so configuration errors are caught early. When
+`customLabels` is configured but the `CustomMetricLabels` feature gate
+is disabled, the controller logs a warning at startup indicating that
+the configuration will have no effect until the gate is enabled.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation (invalid keys, duplicates after sanitization produce
+  fatal startup error with descriptive message).
+- Label sanitization edge cases.
+- Metric reporting with custom labels; missing labels produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
+  only built-in labels, warning is logged.
+
+#### Integration Tests
+
+- ClusterQueue and LocalQueue with matching labels: custom labels appear
+  at `/metrics`.
+- Queue without any of the configured custom labels: empty string values
+  appear in metric series.
+- Label value change on a queue: stale series cleaned up, new series
+  appears.
+- Empty `customLabels`: only built-in labels (backward compatibility).
+- `CustomMetricLabels` enabled with `LocalQueueMetrics` disabled:
+  ClusterQueue metrics get custom labels, LocalQueue metrics are not
+  registered.
+
+### Graduation Criteria
+
+**Alpha (v0.17)**: Feature gate `CustomMetricLabels` (disabled by default),
+config field, ClusterQueue and LocalQueue metrics support, validation,
+tests, docs. LocalQueue custom labels require the `LocalQueueMetrics`
+gate to also be enabled (still alpha); otherwise only ClusterQueue
+metrics carry custom labels.
+
+**Beta**: Positive feedback, gate defaults to enabled.
+
```

  Please add a point to re-evaluate extending of the API to introduce the `SourceLabelKey` under `ControllerMetricsCustomLabel`, or `SourceAnnotationKey` to read this info from dedicated places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-24T12:33:20Z

- **keps/7066-custom-metric-labels/README.md:166** by [@mimowo](https://github.com/mimowo) — 2026-02-24T12:33:21Z
```diff
@@ -0,0 +1,336 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. At
+startup, Kueue sanitizes each entry's `name` key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label's value creates a
+new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old series
+persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is a Kubernetes label key whose value will be added as an extra
+    // Prometheus label on ClusterQueue and LocalQueue metrics.
+    // The key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    Name string `json:"name"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"
+    - name: "env"
+    - name: "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Each `name` entry is converted to a Prometheus label name as follows:
+
+1. Validate that the input is a valid [Kubernetes label key][k8s-labels].
+2. Replace every character that is **not** in `[A-Za-z0-9_]` with `_`.
+3. Prepend the prefix `custom_`.
+4. Verify the result matches the Prometheus label-name regex
+   `[a-zA-Z_][a-zA-Z0-9_]*` (guaranteed by construction since the
+   prefix starts with a letter).
+5. If two or more input keys produce the same derived name after steps
+   1–4, report a **fatal startup error** (see [Validation](#validation)).
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
```

  I'm not sure if the sanitization is the right approach here, because it effectively means we need to sanitize all CQ labels before we can find a match, this likely means a cache of all sanitized labels. I'm wondering about alternatives which are also expressive but avoid the complication.
  
  I'm thinking about more flexible patterns:
  1. support for `SourceLabelKey` / `SourceAnnotationkey` as optional params
  2. only reading from metrics with the dedicated prefix like `kueue.x-k8s.io/custom_metric_label_team`
  
  The drawback of (2.) is that it requires some users to now add a new label to CQ (potentially duplicate) per tracked "tag".
  
  So, I'm leaning towards (1.) . We could probably easily support that even for Alpha, wdyt?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-26T10:31:41Z

- **keps/7066-custom-metric-labels/README.md:166** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-26T10:31:41Z
```diff
@@ -0,0 +1,336 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. At
+startup, Kueue sanitizes each entry's `name` key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label's value creates a
+new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old series
+persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is a Kubernetes label key whose value will be added as an extra
+    // Prometheus label on ClusterQueue and LocalQueue metrics.
+    // The key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    Name string `json:"name"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"
+    - name: "env"
+    - name: "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Each `name` entry is converted to a Prometheus label name as follows:
+
+1. Validate that the input is a valid [Kubernetes label key][k8s-labels].
+2. Replace every character that is **not** in `[A-Za-z0-9_]` with `_`.
+3. Prepend the prefix `custom_`.
+4. Verify the result matches the Prometheus label-name regex
+   `[a-zA-Z_][a-zA-Z0-9_]*` (guaranteed by construction since the
+   prefix starts with a letter).
+5. If two or more input keys produce the same derived name after steps
+   1–4, report a **fatal startup error** (see [Validation](#validation)).
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
```

  Done. I've added `SourceLabelKey` and `SourceAnnotationkey` as optional fields.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-26T10:33:10Z

- **keps/7066-custom-metric-labels/README.md:343** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-26T10:33:10Z
```diff
@@ -0,0 +1,336 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Sanitization](#label-sanitization)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Validation](#validation)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels from ClusterQueue and LocalQueue objects into Prometheus metric
+labels (with a mandatory `custom_` prefix), enabling filtering and
+aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels as Prometheus metric labels would make it easier to
+build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels on ClusterQueue and LocalQueue objects
+  become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Annotation-to-metric mapping.
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (generated automatically from the
+  Kubernetes label key).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. At
+startup, Kueue sanitizes each entry's `name` key, adds a `custom_` prefix, and
+initializes ClusterQueue and LocalQueue metric vectors with the additional
+label dimensions. When reporting metrics, the corresponding Kubernetes
+label values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+produces `custom_cost_center="12345"`, enabling aggregation by cost
+center.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label's value creates a
+new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label value changes, the old series
+persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is a Kubernetes label key whose value will be added as an extra
+    // Prometheus label on ClusterQueue and LocalQueue metrics.
+    // The key is sanitized and prefixed with "custom_" to form the
+    // Prometheus label name (e.g., "team" becomes "custom_team").
+    Name string `json:"name"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of Kubernetes label keys whose values will be
+    // added as extra Prometheus labels on ClusterQueue and LocalQueue metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"
+    - name: "env"
+    - name: "cost-center"
+```
+
+Resulting metric (existing labels truncated with `...`):
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345"} 4.5
+```
+
+### Label Sanitization
+
+Each `name` entry is converted to a Prometheus label name as follows:
+
+1. Validate that the input is a valid [Kubernetes label key][k8s-labels].
+2. Replace every character that is **not** in `[A-Za-z0-9_]` with `_`.
+3. Prepend the prefix `custom_`.
+4. Verify the result matches the Prometheus label-name regex
+   `[a-zA-Z_][a-zA-Z0-9_]*` (guaranteed by construction since the
+   prefix starts with a letter).
+5. If two or more input keys produce the same derived name after steps
+   1–4, report a **fatal startup error** (see [Validation](#validation)).
+
+| Kubernetes label key           | Prometheus label name                  |
+|--------------------------------|----------------------------------------|
+| `team`                         | `custom_team`                          |
+| `cost-center`                  | `custom_cost_center`                   |
+| `app.kubernetes.io/name`       | `custom_app_kubernetes_io_name`        |
+
+### Affected Metrics
+
+Custom labels are appended to metrics that report about a specific
+queue. The selection criteria is: ClusterQueue metrics whose label set
+includes `cluster_queue`, and LocalQueue metrics whose label set
+includes `name`/`namespace`.
+
+Metrics that are not keyed by a specific queue are excluded:
+`AdmissionAttemptsTotal`, `PreemptedWorkloadsTotal`, and
+`CohortWeightedShare`.
+
+All ClusterQueue and LocalQueue metrics matching the criteria above
+are affected. The full set is determined by the metric definitions in
+`pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to pass a
+   `[]string` of custom label values as an additional parameter,
+   following the pattern used for `*roletracker.RoleTracker`. Missing
+   labels on the queue object produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted.
+5. **Value-change cleanup**: The controller keeps an in-memory map
+   from queue name to last-reported custom label values. On every
+   queue reconciliation:
+   1. Read the configured custom label keys from the queue object to
+      get the new values.
+   2. Look up the previous values for this queue in the map.
+   3. If any value differs: remove the old series from every
+      queue-scoped metric vector (ClusterQueue and LocalQueue; core,
+      cache, and resource metrics), then report with the new values.
+   4. Update the map entry with the new values.
+
+   For cumulative metrics (counters and histograms), removing and
+   re-creating a series resets it to zero. This is expected; `rate()`
+   and `increase()` handle counter resets correctly.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+### Validation
+
+At startup, each `customLabels` entry must:
+
+1. Be a valid [Kubernetes label key][k8s-labels].
+2. Produce a valid Prometheus label name after sanitization.
+3. Not produce a duplicate derived name (e.g., `cost-center` and
+   `cost.center` both map to `custom_cost_center`). If two or more
+   input keys collide after sanitization, the controller must emit a
+   **fatal startup error** that lists the derived Prometheus label name
+   and all conflicting input keys (e.g.,
+   `duplicate custom metric label "custom_cost_center" produced by keys: "cost-center", "cost.center"`).
+
+The `custom_` prefix ensures that any Kubernetes label key can be
+safely used without conflicting with built-in metric labels.
+
+Validation runs regardless of whether the `CustomMetricLabels` feature
+gate is enabled, so configuration errors are caught early. When
+`customLabels` is configured but the `CustomMetricLabels` feature gate
+is disabled, the controller logs a warning at startup indicating that
+the configuration will have no effect until the gate is enabled.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation (invalid keys, duplicates after sanitization produce
+  fatal startup error with descriptive message).
+- Label sanitization edge cases.
+- Metric reporting with custom labels; missing labels produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
+  only built-in labels, warning is logged.
+
+#### Integration Tests
+
+- ClusterQueue and LocalQueue with matching labels: custom labels appear
+  at `/metrics`.
+- Queue without any of the configured custom labels: empty string values
+  appear in metric series.
+- Label value change on a queue: stale series cleaned up, new series
+  appears.
+- Empty `customLabels`: only built-in labels (backward compatibility).
+- `CustomMetricLabels` enabled with `LocalQueueMetrics` disabled:
+  ClusterQueue metrics get custom labels, LocalQueue metrics are not
+  registered.
+
+### Graduation Criteria
+
+**Alpha (v0.17)**: Feature gate `CustomMetricLabels` (disabled by default),
+config field, ClusterQueue and LocalQueue metrics support, validation,
+tests, docs. LocalQueue custom labels require the `LocalQueueMetrics`
+gate to also be enabled (still alpha); otherwise only ClusterQueue
+metrics carry custom labels.
+
+**Beta**: Positive feedback, gate defaults to enabled.
+
```

  Since `sourceLabelKey` and `sourceAnnotationKey` are part of Alpha now, I left the graduation criteria unchanged.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T13:30:14Z

- **keps/7066-custom-metric-labels/README.md:58** by [@mimowo](https://github.com/mimowo) — 2026-02-26T13:30:14Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
```

  I'm not sure, excluding metrics for Cohorts and Worklaods from the mechanism seems ad hoc. Maybe we exclude them in alpha, but this seems like a useful generalization of the KEP in the future. It is not a non-goal for KEP. It might be non-goal for Alpha.
  
  I have two ideas here:
  1. Actually apply the extra labels reading from all types (for Alpha)
  2. Support a new param "SourceKinds: ["ClusterQueue", "LocalQueue", "Cohort", "Workload"] etc. Then, we could apply the tags only to a selected subset of Kinds, however it feels for alpha we could cover all of them.
  
  Wdyt? I think we should add a point for graduation crieria to Beta to revisit automatic selection of Kinds for which the custom tags apply.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T13:31:58Z

- **keps/7066-custom-metric-labels/README.md:60** by [@mimowo](https://github.com/mimowo) — 2026-02-26T13:31:58Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (the admin picks a `name` suffix
+  and Kueue prepends `custom_` automatically).
```

  This is not non-goal of the KEP, this is an Alternative design. Let's move it to Alternatives section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T13:32:36Z

- **keps/7066-custom-metric-labels/README.md:49** by [@mimowo](https://github.com/mimowo) — 2026-02-26T13:32:37Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
```

  This is no a goal of the KEP, this is just "Terchnical Detail" of the solution, imo

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-27T05:30:40Z

- **keps/7066-custom-metric-labels/kep.yaml:12** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T05:30:40Z
```diff
@@ -1,20 +1,15 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
 authors:
-  - "@kshalot"
+  - "@IrvingMg"
 status: provisional
-creation-date: 2026-02-04
+creation-date: 2026-02-13
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
+  - "@mimowo"
+  - "@kannon92"
+  - "@yashnib"
 approvers:
   - "@mimowo"
```

  ```suggestion
    - "@mimowo"
    - "@tenzen-y"
  ```
  
  I arrived here right now.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T09:24:51Z

- **keps/7066-custom-metric-labels/README.md:58** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T09:24:51Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
```

  Done. Updated to include all types (1.)

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T09:27:08Z

- **keps/7066-custom-metric-labels/README.md:60** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T09:27:07Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+- Custom labels on non-queue metrics (workload, cohort). Metrics that
+  are not keyed by a specific queue (e.g., `AdmissionAttemptsTotal`,
+  `PreemptedWorkloadsTotal`, `CohortWeightedShare`) are excluded.
+- User-chosen Prometheus label names (the admin picks a `name` suffix
+  and Kueue prepends `custom_` automatically).
```

  I removed it since `Configurable with Override Name` covers the same idea.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T09:27:23Z

- **keps/7066-custom-metric-labels/README.md:49** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T09:27:23Z
```diff
@@ -0,0 +1,363 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue and LocalQueue objects into
+Prometheus metric labels (with a mandatory `custom_` prefix), enabling
+filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue and
+  LocalQueue objects become Prometheus metric labels.
+- Enforce a `custom_` prefix on all user-defined Prometheus labels to
+  prevent collisions with built-in names.
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T09:27:31Z

- **keps/7066-custom-metric-labels/kep.yaml:12** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T09:27:31Z
```diff
@@ -1,20 +1,15 @@
-title: MultiKueue Orchestrated Preemption
-kep-number: 8303
+title: Custom Metadata Labels for Kueue Metrics
+kep-number: 7066
 authors:
-  - "@kshalot"
+  - "@IrvingMg"
 status: provisional
-creation-date: 2026-02-04
+creation-date: 2026-02-13
 reviewers:
-  - "@ichekrygin"
-  - "@olekzabl"
+  - "@mimowo"
+  - "@kannon92"
+  - "@yashnib"
 approvers:
   - "@mimowo"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-01T08:24:30Z

Thank you for creating this proposal!
I left some comments.

- **keps/7066-custom-metric-labels/README.md:134** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T07:59:37Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
```

  ```suggestion
  ```
  
  It would be better not to mention existing field.

- **keps/7066-custom-metric-labels/README.md:154** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T08:00:50Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of entries whose values will be added as extra
+    // Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"                                        # reads from label "team"
+    - name: "env"                                         # reads from label "env"
+    - name: "cost_center"
+      sourceLabelKey: "cost-center"                       # reads from label "cost-center"
+    - name: "budget_code"
+      sourceAnnotationKey: "billing.company.com/budget"   # reads from annotation
+```
```

  Could you move the examples to `User Stories` section?
  Combining the user story and YAML examples would be nice to understand the real use cases in KEP.

- **keps/7066-custom-metric-labels/README.md:136** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T08:06:11Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
```

  Do we have a situation where they want to filter the source resources (e.g., ClusterQueue, LocalQueue, Workload, ...)?
  
  For example, do they want to propagate only ClusterQueue labels to metrics?
  This is just a question, and does not indicate a recommendation.

- **keps/7066-custom-metric-labels/README.md:161** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-01T08:24:07Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of entries whose values will be added as extra
+    // Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
```

  Could we have validations for the number of the `customLabels` like `8`?
  Because even if we find significant performance issues due to the large number of customLabels, we can not add validations for the number of customLabels due to the leading API break.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T08:51:51Z

- **keps/7066-custom-metric-labels/README.md:134** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T08:51:51Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T08:51:58Z

- **keps/7066-custom-metric-labels/README.md:154** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T08:51:58Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of entries whose values will be added as extra
+    // Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+Example configuration:
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"                                        # reads from label "team"
+    - name: "env"                                         # reads from label "env"
+    - name: "cost_center"
+      sourceLabelKey: "cost-center"                       # reads from label "cost-center"
+    - name: "budget_code"
+      sourceAnnotationKey: "billing.company.com/budget"   # reads from annotation
+```
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T08:52:07Z

- **keps/7066-custom-metric-labels/README.md:161** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T08:52:06Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    EnableClusterQueueResources bool     `json:"enableClusterQueueResources,omitempty"`
+
+    // CustomLabels is a list of entries whose values will be added as extra
+    // Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+    // +optional
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T08:57:40Z

- **keps/7066-custom-metric-labels/README.md:136** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T08:57:40Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
```

  I don’t think there is currently a requirement to filter the source resources. That’s why, for alpha, as mentioned in https://github.com/kubernetes-sigs/kueue/pull/9225#discussion_r2859049889, it was suggested to include all of them. Then, for beta, we can revisit supporting a new parameter like `SourceKinds`, to allow selecting only a subset of kinds. 
  
  I've just added this point to the graduation criteria.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-03T09:36:08Z

/lgtm
/approve
Thank you, leaving on hold in case @tenzen-y has additional comments
/hold

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T19:09:59Z

- **keps/7066-custom-metric-labels/README.md:136** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-06T19:09:58Z
```diff
@@ -0,0 +1,374 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
```

  Thank you for sharing the current vision.
  Yes, I also don't have a strong view on such use cases. So, let's postpone the decisiont to beta or ga phase.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T19:10:55Z

- **keps/7066-custom-metric-labels/README.md:340** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-06T19:10:56Z
```diff
@@ -0,0 +1,387 @@
+# KEP-7066: Custom Metadata Labels for Kueue Metrics
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Configuration API](#configuration-api)
+  - [Label Name Validation](#label-name-validation)
+  - [Affected Metrics](#affected-metrics)
+  - [Implementation Approach](#implementation-approach)
+  - [Metrics Documentation Generator](#metrics-documentation-generator)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Single Static Label](#single-static-label)
+  - [Numbered Static Labels](#numbered-static-labels)
+  - [Configurable with Override Name](#configurable-with-override-name)
+<!-- /toc -->
+
+## Summary
+
+Allow cluster admins to configure Kueue to promote specific Kubernetes
+labels or annotations from ClusterQueue, LocalQueue, and Cohort objects
+into Prometheus metric labels (with a mandatory `custom_` prefix),
+enabling filtering and aggregation by organizational metadata.
+
+## Motivation
+
+Kueue metrics have a fixed label set. Grouping metrics by categories
+like team, environment, or cost center requires joining Kueue metrics
+with external data sources in PromQL. Allowing users to add selected
+Kubernetes labels or annotations as Prometheus metric labels would make
+it easier to build dashboards and filter or aggregate metrics.
+
+### Goals
+
+- Configure which Kubernetes labels or annotations on ClusterQueue,
+  LocalQueue, and Cohort objects become Prometheus metric labels.
+- Keep the metrics documentation auto-generation working.
+- Validate configuration at startup.
+
+### Non-Goals
+
+- Runtime-dynamic label set changes (requires restart).
+
+## Proposal
+
+Add a `customLabels` list to the `metrics` config section. Each entry
+has a `name` (used as the Prometheus label suffix after prepending
+`custom_`), and optionally a `sourceLabelKey` or `sourceAnnotationKey`
+to specify where to read the value from. If neither source field is
+set, `name` is used as the Kubernetes label key. At startup, Kueue
+validates the configuration and initializes ClusterQueue, LocalQueue,
+and Cohort metric vectors with the additional label dimensions. When
+reporting metrics, the corresponding Kubernetes label or annotation
+values are included in the label set.
+
+### User Stories
+
+**Team aggregation.** A ClusterQueue labeled `team=platform` produces
+`custom_team="platform"` in metrics, enabling PromQL like
+`sum by (custom_team) (kueue_cluster_queue_resource_usage)`.
+
+**Environment filtering.** A ClusterQueue labeled `env=prod` produces
+`custom_env="prod"`, allowing Grafana dashboards filtered by environment.
+
+**Cost center grouping.** A ClusterQueue labeled `cost-center=12345`
+is configured with `name: "cost_center"` and
+`sourceLabelKey: "cost-center"`, producing
+`custom_cost_center="12345"` in metrics.
+
+**Annotation-based billing.** A ClusterQueue annotated with
+`billing.company.com/budget=ABC-123` is configured with
+`name: "budget_code"` and
+`sourceAnnotationKey: "billing.company.com/budget"`, producing
+`custom_budget_code="ABC-123"` in metrics.
+
+**Example configuration:**
+
+```yaml
+metrics:
+  enableClusterQueueResources: true
+  customLabels:
+    - name: "team"                                        # reads from label "team"
+    - name: "env"                                         # reads from label "env"
+    - name: "cost_center"
+      sourceLabelKey: "cost-center"                       # reads from label "cost-center"
+    - name: "budget_code"
+      sourceAnnotationKey: "billing.company.com/budget"   # reads from annotation
+```
+
+Resulting metric:
+
+```
+kueue_cluster_queue_resource_usage{
+  cluster_queue="cq-1", cohort="c", ...,
+  custom_team="platform", custom_env="prod",
+  custom_cost_center="12345",
+  custom_budget_code="ABC-123"} 4.5
+```
+
+### Risks and Mitigations
+
+**Metric series growth.** Custom labels do not increase active series
+count in steady state — each queue still produces one set of label
+values per metric. However, changing a custom label or annotation value
+creates a new series; the old one must be explicitly deleted (see
+[Value-change cleanup](#implementation-approach)). Admins should prefer
+stable metadata (team, environment, cost center) and avoid values that
+change frequently.
+
+**Name collisions.** The mandatory `custom_` prefix prevents collisions
+with current or future built-in labels.
+
+**Stale metrics.** When a queue's label or annotation value changes,
+the old series persists until explicitly deleted. See
+[Value-change cleanup](#implementation-approach) for details.
+
+## Design Details
+
+### Configuration API
+
+Extend `ControllerMetrics` in
+`apis/config/v1beta2/configuration_types.go`:
+
+```go
+type ControllerMetricsCustomLabel struct {
+    // Name is the Prometheus metric label name suffix.
+    // Prepended with "custom_" to form the full Prometheus label name
+    // (e.g., "team" becomes "custom_team").
+    // Must contain only [a-zA-Z0-9_] characters and start with a letter.
+    Name string `json:"name"`
+
+    // SourceLabelKey is the Kubernetes label key to read the value from.
+    // Mutually exclusive with SourceAnnotationKey.
+    // If neither is specified, defaults to Name.
+    // +optional
+    SourceLabelKey string `json:"sourceLabelKey,omitempty"`
+
+    // SourceAnnotationKey is the Kubernetes annotation key to read the value from.
+    // Mutually exclusive with SourceLabelKey.
+    // +optional
+    SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
+}
+
+type ControllerMetrics struct {
+    ...
+    // CustomLabels is a list of entries whose values will be added as extra
+    // Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+    // +optional
+    // +kubebuilder:validation:MaxItems=8
+    CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+```
+
+### Label Name Validation
+
+Each `customLabels` entry is validated at startup:
+
+1. `Name` must match `[a-zA-Z][a-zA-Z0-9_]*` (a valid Prometheus label
+   suffix). After prepending `custom_`, the result matches the
+   Prometheus label-name regex `[a-zA-Z_][a-zA-Z0-9_]*` (guaranteed by
+   construction).
+2. `SourceLabelKey` and `SourceAnnotationKey` are mutually exclusive;
+   setting both is a **fatal startup error**.
+3. `SourceLabelKey` (if set) must be a valid [Kubernetes label
+   key][k8s-labels].
+4. `SourceAnnotationKey` (if set) must be a valid Kubernetes annotation
+   key.
+5. If neither source field is set, `Name` must also be a valid
+   Kubernetes label key (since it doubles as the source key).
+6. No two entries may share the same `Name` (**fatal startup error**
+   with a descriptive message listing the duplicates).
+
+Validation runs at startup regardless of whether the
+`CustomMetricLabels` feature gate is enabled, so configuration errors
+are caught early. When `customLabels` is configured but the feature
+gate is disabled, the controller logs a warning indicating that the
+configuration will have no effect until the gate is enabled.
+
+| Config `name`  | Source field                                            | Kubernetes source read                    | Prometheus label name  |
+|----------------|---------------------------------------------------------|-------------------------------------------|------------------------|
+| `team`         | *(omitted)*                                             | label `team`                              | `custom_team`          |
+| `cost_center`  | `sourceLabelKey: "cost-center"`                         | label `cost-center`                       | `custom_cost_center`   |
+| `app_name`     | `sourceLabelKey: "app.kubernetes.io/name"`              | label `app.kubernetes.io/name`            | `custom_app_name`      |
+| `budget_code`  | `sourceAnnotationKey: "billing.company.com/budget"`     | annotation `billing.company.com/budget`   | `custom_budget_code`   |
+
+### Affected Metrics
+
+Custom labels are appended to every metric that is keyed by a specific
+Kubernetes object — ClusterQueue, LocalQueue, or Cohort. The selection
+criteria:
+
+- **ClusterQueue metrics**: label set includes `cluster_queue`.
+  `PreemptedWorkloadsTotal` is also included — its
+  `preempting_cluster_queue` label identifies a ClusterQueue, so custom
+  label values are read from that ClusterQueue object.
+- **LocalQueue metrics**: label set includes `name`/`namespace`.
+- **Cohort metrics**: `CohortWeightedShare` (keyed by `cohort`). Custom
+  label values are read from the Cohort object's labels or annotations.
+
+Truly global metrics that have no object key are excluded:
+`AdmissionAttemptsTotal`, `admissionAttemptDuration`, and `buildInfo`.
+
+The full set of affected metrics is determined by the metric definitions
+in `pkg/metrics/metrics.go` at the time of implementation.
+
+### Implementation Approach
+
+1. **Startup**: Read `customLabels`, validate, compute Prometheus names.
+2. **Metric vector initialization**: Metric vectors are currently
+   declared as package-level variables with `prometheus.NewXxxVec(...)`.
+   Since the label set must include custom labels, the vectors need to
+   be built after the configuration is loaded. The static declarations
+   should remain in source so the metricsdoc generator can parse them,
+   but the vectors must be reassigned with the extended label set before
+   registration in `Register()`.
+3. **Reporting**: All `Report*` functions currently use positional
+   `WithLabelValues(...)` and accept primitive parameters (strings,
+   ints), not queue objects. Custom label values must be threaded
+   through these functions. The recommended approach is to add a
+   `[]string` parameter to each `Report*` function for the custom
+   label values. For each custom label entry, the source key (`Name`
+   when neither source field is set, `SourceLabelKey`, or
+   `SourceAnnotationKey`) determines where to read the value — from
+   `obj.Labels[key]` or `obj.Annotations[key]` on the relevant object
+   (ClusterQueue, LocalQueue, or Cohort). For Cohort metrics
+   (`CohortWeightedShare`), labels are read from the Cohort object.
+   For `PreemptedWorkloadsTotal`, labels are read from the preempting
+   ClusterQueue. Missing keys produce an empty string value.
+4. **Deletion cleanup**: Existing `Clear*Metrics` functions use
+   `DeletePartialMatch` and will match custom label dimensions
+   automatically when a queue is deleted. For Cohort, the
+   `CohortWeightedShare` series must also be deleted when a Cohort is
+   removed; the Cohort reconciler's delete path must call
+   `DeletePartialMatch` for it.
+5. **Value-change cleanup**: The controller keeps an in-memory map
+   from object name to last-reported custom label values. On every
+   reconciliation of a ClusterQueue, LocalQueue, or Cohort:
+   1. Read the configured source keys (labels or annotations) from
+      the object to get the new values.
+   2. Look up the previous values for this object in the map.
+   3. If any value differs: remove the old series from every metric
+      vector scoped to that object type, then report with the new
+      values.
+   4. Update the map entry with the new values.
+
+   For Cohort specifically, the Cohort reconciler must perform this
+   check before calling `ReportCohortWeightedShare`.
+
+   For cumulative metrics (counters and histograms), removing and
+   re-creating a series resets it to zero. This is expected; `rate()`
+   and `increase()` handle counter resets correctly.
+
+### Metrics Documentation Generator
+
+The metricsdoc generator (`hack/tools/metricsdoc/main.go`) parses Go
+source to find static `prometheus.NewXxxVec(...)` calls and extract their
+label lists. Since custom labels are added at runtime, the generator
+cannot see them.
+
+The static `prometheus.NewXxxVec(...)` declarations with built-in labels
+remain in source code so the generator can parse them. At startup, the
+vectors are reassigned with the extended label set before registration.
+The generator continues to work as-is for built-in labels. A
+documentation note will explain that additional labels can appear when
+`customLabels` is configured.
+
+[k8s-labels]: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#syntax-and-character-set
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require
+updates to existing tests to make this code solid enough prior to
+committing the changes necessary to implement this enhancement.
+
+#### Unit Tests
+
+- Config validation: invalid `Name` (special characters, leading digit),
+  duplicate `Name` values, both `sourceLabelKey` and
+  `sourceAnnotationKey` set, invalid `sourceLabelKey`, invalid
+  `sourceAnnotationKey`.
+- Config with `sourceLabelKey` reads from the correct Kubernetes label.
+- Config with `sourceAnnotationKey` reads from the correct annotation.
+- Omitting both source fields uses `Name` as the label key source.
+- Metric reporting with custom labels; missing labels/annotations
+  produce empty values.
+- Feature gate disabled: `customLabels` config is ignored, metrics have
+  only built-in labels, warning is logged.
+
+#### Integration Tests
+
+- ClusterQueue and LocalQueue with matching labels: custom labels appear
+  at `/metrics`.
+- Config with `sourceLabelKey`: value is read from the specified
+  Kubernetes label.
+- Config with `sourceAnnotationKey`: value is read from the specified
+  annotation.
+- Queue without any of the configured custom labels or annotations:
+  empty string values appear in metric series.
+- Label value change on a queue: stale series cleaned up, new series
+  appears.
+- Cohort with custom labels: `CohortWeightedShare` carries the custom
+  label values read from the Cohort object.
+- `PreemptedWorkloadsTotal` with custom labels: custom label values are
+  read from the preempting ClusterQueue.
+- Empty `customLabels`: only built-in labels (backward compatibility).
+- `CustomMetricLabels` enabled with `LocalQueueMetrics` disabled:
+  ClusterQueue metrics get custom labels, LocalQueue metrics are not
+  registered.
+
+### Graduation Criteria
+
+**Alpha**
+
+- Feature gate `CustomMetricLabels` (disabled by default).
+- Configuration field `customLabels` under `metrics`.
+- Custom labels applied to ClusterQueue, LocalQueue, and Cohort metrics.
+- LocalQueue custom labels require the `LocalQueueMetrics` gate to also
+  be enabled (still alpha); otherwise only ClusterQueue and Cohort
+  metrics carry custom labels.
+- Startup validation and tests.
+- Documentation.
+
+**Beta**
+
+- Positive feedback from users.
+- Gate defaults to enabled.
+- Re-evaluate whether to support a `SourceKinds` parameter (e.g.,
+  `["ClusterQueue", "LocalQueue", "Cohort", "Workload"]`) to let admins
+  select which object types carry custom labels.
```

  SGTM, thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T19:11:34Z

The latest KEP looks great to me, thank you for the update!

/lgtm
/approve
/hold cancel
