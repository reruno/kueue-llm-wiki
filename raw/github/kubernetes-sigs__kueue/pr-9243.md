# PR #9243: KEP-2724: Add multi-level topology aware scheduling design

**Summary**: KEP-2724: Add multi-level topology aware scheduling design

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9243

**Last updated**: 2026-02-21T03:04:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Huang-Wei](https://github.com/Huang-Wei)
- **Branch**: `tas-multi-layer-KEP` → `main`
- **Created**: 2026-02-15T02:11:24Z
- **Updated**: 2026-02-21T03:04:35Z
- **Merged**: 2026-02-20T11:03:39Z
- **Closed**: 2026-02-20T11:03:39Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 2   **+144 / -5**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Add the design for multi-level TAS, which extends two-level scheduling to support N slice layers across deeper topology hierarchies (e.g., datacenter → block → rack → host).

KEP for #9046.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-15T02:11:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 019f7e90e7c9dc42ee2e242b5d615e6acb6f321f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69981110b65b670008d610f5 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9243--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9243--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTkyNDMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.aFL_XUzzL5tqaRXO1RTQGDvBYn9Vo1wwpD2SAb1RVlU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9243--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@kshalot](https://github.com/kshalot) — 2026-02-16T12:36:21Z

I assume the implementation will be a follow-up, so the release note for this PR should be dropped.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-16T12:43:09Z

/release-note-edit
```release-note
NONE
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-16T17:40:54Z

LGTM label has been added.  <details>Git tree hash: 5ae36072db01f2dfaf51930b369c0d8b94a75294</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-16T18:21:13Z

> /lgtm /approve
> 
> Putting on hold in case others want to review, but I'm happy to proceed with the implementation review.
> 
> cc @tenzen-y @gabesaba /hold

Let me take a look at this proposal tomorrow.

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-20T07:47:56Z

@tenzen-y the PR is updated, and I think it covered all comments. PTAL.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-20T08:30:34Z

LGTM still. even better. Thank you folks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-20T10:21:27Z

LGTM label has been added.  <details>Git tree hash: be10fc4fc908294b9a5dea9fe6f47b4a75b26e94</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-20T10:21:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9243#" title="Author self-approved">Huang-Wei</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9243#pullrequestreview-3809817188" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9243#pullrequestreview-3831230979" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-20T10:21:30Z

/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T17:39:44Z

- **keps/2724-topology-aware-scheduling/README.md:882** by [@mimowo](https://github.com/mimowo) — 2026-02-16T17:39:44Z
```diff
@@ -811,6 +878,12 @@ the rules is deactivated):
   specified its own default. See [Slice size validation](#slice-size-validation))
 - The value of `kueue.x-k8s.io/podset-slice-size` has to be a numeric value greater or equal
   than 1. It has to evenly divide the size of a PodSet.
+- The above 2 `podset-slice-*` rules apply to additional slice layers (`kueue.x-k8s.io/podset-slice-required-topology-[X]`)
+  as well. `[X]` can be up to `2`.
```

  It this just a "sanity" limitation on the API, but the algorithm is generic? In other words, would the algo code need to be changed to support extra layers? Not a blocker, just curious on how generic the algo is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-16T17:40:47Z

/lgtm
/approve

Putting on hold in case others want to review, but I'm happy to proceed with the implementation review. 

cc @tenzen-y @gabesaba 
/hold

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-17T07:28:58Z

- **keps/2724-topology-aware-scheduling/README.md:882** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-17T07:28:58Z
```diff
@@ -811,6 +878,12 @@ the rules is deactivated):
   specified its own default. See [Slice size validation](#slice-size-validation))
 - The value of `kueue.x-k8s.io/podset-slice-size` has to be a numeric value greater or equal
   than 1. It has to evenly divide the size of a PodSet.
+- The above 2 `podset-slice-*` rules apply to additional slice layers (`kueue.x-k8s.io/podset-slice-required-topology-[X]`)
+  as well. `[X]` can be up to `2`.
```

  Yes, the algorithm is generic.
  
  > In other words, would the algo code need to be changed to support extra layers?
  
  No core changes needed if we want to support extra layers.
  
  I will submit the PR this week.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-17T21:01:23Z

Thank you for updating this enhancement.
I left a few comments.

- **keps/2724-topology-aware-scheduling/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T20:47:33Z
```diff
@@ -242,6 +253,42 @@ spec:
 In this example we indicate that all Pods created by the Job should be contained
 within the same "rack".
 
+##### Job with multi-level scheduling
+
+According to [Story 8](#story-8), some users would like to place Pods of a Job
+across multi-layer topologies.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: tas-example-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 128
+  completions: 128
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
+        kueue.x-k8s.io/podset-slice-size: "64"
+        kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
+        kueue.x-k8s.io/podset-slice-size-1: "32"
+        kueue.x-k8s.io/podset-slice-required-topology-1: cloud.provider.com/block
+        kueue.x-k8s.io/podset-slice-size-2: "16"
+        kueue.x-k8s.io/podset-slice-required-topology-2: cloud.provider.com/rack
```

  ```yaml
        annotations:
          kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
          kueue.x-k8s.io/podset-slice-size: "64"
          kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
          kueue.x-k8s.io/podset-slice-additional-required-layers: |
            [
              {"topology": "cloud.provider.com/block", "size": "32"},
              {"topology": "cloud.provider.com/rack", "size": "16"},
            ]
  ```
  
  Instead of a one-line annotation, I would propose JSON-style annotation, which is a standard approach in Kubernetes to increase representability.
  
  I think the current `kueue.x-k8s.io/podset-slice-size-X` and `kueue.x-k8s.io/podset-slice-required-topology-X` are slightly lacking in scalability for the future.
  
  Based on relaxing the number of `X`, the number of needed annotations will be increased, and that is not scalable.
  
  Or we might be able to restructure topology specifications in the following. But the current single Topology specifications could satisfy almost all users' stories. So, I'm not a big fan of this alternative (deprecating existing slice annotations).
  
  ```yaml
        annotations:
          kueue.x-k8s.io/podset-slice-required-layers: |
            [
              {"topology": "cloud.provider.com/aizone", "size": "64"},
              {"topology": "cloud.provider.com/block", "size": "32"},
              {"topology": "cloud.provider.com/rack", "size": "16"},
            ]
  ```

- **keps/2724-topology-aware-scheduling/README.md:986** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T20:51:00Z
```diff
@@ -896,6 +973,33 @@ type PodSetTopologyRequest struct {
   //
   // +optional
   PodSetSliceSize *int32 `json:"podSetSliceSize,omitempty"`
+
+  // additionalSliceLayers defines additional layers of recursive slice
+  // subdivision beyond the first slice layer (podSetSliceRequiredTopology /
+  // podSetSliceSize). Each layer further subdivides the parent layer's
+  // groups into smaller groups constrained to a finer topology domain.
+  // At most 2 additional layers are supported (for a total of 3 slice layers).
+  //
+  // +optional
+  // +listType=atomic
+  // +kubebuilder:validation:MaxItems=2
+  AdditionalSliceLayers []SliceLayer `json:"additionalSliceLayers,omitempty"`
```

  ```suggestion
    AdditionalSliceLayers []PodSetSliceAdditionalRequiredLayer `json:"additionalSliceLayers,omitempty"`
  ```
  
  Could you use the full name (`PodSetSliceAdditionalRequiredLayer`) to avoid typed name collisions across CRDs?

- **keps/2724-topology-aware-scheduling/README.md:1585** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T20:52:54Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
```

  I will revisit this NOTE when I review implementations, and whether we should decouple those in the first PR.

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T20:57:55Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  AFAIK, `LeastFreeCapacity` has already been deprecated. @mimowo Do you think that we should support multi-layer in `LeastFreeCapacity` as well?

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T20:59:18Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  @Huang-Wei This is just a question. Based on the issue and community meeting discussion, the introduced use cases are only for balanced placement. Will you plan to implement multi-layer for all supported TAS modes (BestFit / BalancedPlacement) in the first iteration?

- **keps/2724-topology-aware-scheduling/README.md:942** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T21:01:00Z
```diff
@@ -826,6 +899,10 @@ However, in case of the JobSet we expect that the most frequent use-case will be
 define PodSet Slice as a single Job, thus if `kueue.x-k8s.io/podset-slice-size`
 is not defined for JobSet it defaults to `parallelism`.
 
```

  Shouldn't we validate if the number of levels in Topology is greater than or equal to the number of topology layers defined at Job annotations?

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-18T05:36:24Z

- **keps/2724-topology-aware-scheduling/README.md:280** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-18T05:36:24Z
```diff
@@ -242,6 +253,42 @@ spec:
 In this example we indicate that all Pods created by the Job should be contained
 within the same "rack".
 
+##### Job with multi-level scheduling
+
+According to [Story 8](#story-8), some users would like to place Pods of a Job
+across multi-layer topologies.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: tas-example-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 128
+  completions: 128
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
+        kueue.x-k8s.io/podset-slice-size: "64"
+        kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
+        kueue.x-k8s.io/podset-slice-size-1: "32"
+        kueue.x-k8s.io/podset-slice-required-topology-1: cloud.provider.com/block
+        kueue.x-k8s.io/podset-slice-size-2: "16"
+        kueue.x-k8s.io/podset-slice-required-topology-2: cloud.provider.com/rack
```

  > Instead of a one-line annotation, I would propose JSON-style annotation
  
  I thought of JSON style and totally agree that is better representation. My concern was it'd end up with partial key-value style, and partial JSON style.
  
  So I'm thinking another variant:
  
  - for new multi-layer podset-slice, you use a JSON-style `kueue.x-k8s.io/podset-slice-required-topology-constraints` (renamed a bit from your suggestion) **exclusively** with existing `kueue.x-k8s.io/podset-slice-size` -  validation will error out if they're both specified
  - for existing two-layer podset-slice, it works as before (existing users that are happy with this don't need to change anything)
  
  WDYT?

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-18T05:43:06Z

- **keps/2724-topology-aware-scheduling/README.md:942** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-18T05:43:06Z
```diff
@@ -826,6 +899,10 @@ However, in case of the JobSet we expect that the most frequent use-case will be
 define PodSet Slice as a single Job, thus if `kueue.x-k8s.io/podset-slice-size`
 is not defined for JobSet it defaults to `parallelism`.
 
```

  I will add this to validation rules once we get to a consensus on the JSON-style annotation representation.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-18T05:43:33Z

- **keps/2724-topology-aware-scheduling/README.md:986** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-18T05:43:33Z
```diff
@@ -896,6 +973,33 @@ type PodSetTopologyRequest struct {
   //
   // +optional
   PodSetSliceSize *int32 `json:"podSetSliceSize,omitempty"`
+
+  // additionalSliceLayers defines additional layers of recursive slice
+  // subdivision beyond the first slice layer (podSetSliceRequiredTopology /
+  // podSetSliceSize). Each layer further subdivides the parent layer's
+  // groups into smaller groups constrained to a finer topology domain.
+  // At most 2 additional layers are supported (for a total of 3 slice layers).
+  //
+  // +optional
+  // +listType=atomic
+  // +kubebuilder:validation:MaxItems=2
+  AdditionalSliceLayers []SliceLayer `json:"additionalSliceLayers,omitempty"`
```

  Makes sense, will update once the JSON-style annotation is finalized.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-18T05:49:27Z

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-18T05:49:27Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  Oh, here I just want to highlight that the impl. didn't change/touch the sorting logic. I can update is as:
  
  ```
  sorting and selection logic (BestFit / BalancedPlacement) is applied at each
  ```

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-18T05:57:05Z

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-18T05:57:05Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  @tenzen-y yup, however it's implemented in a guaranteed way (extending what podset-slice was implemented), so IIUC it's orthogonal with TAS's mode. In other words, after the workload satisfies the mulit-layer topology constraints, it follows whatever specified TAS mode for sorting.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-19T19:01:19Z

- **keps/2724-topology-aware-scheduling/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-19T19:01:19Z
```diff
@@ -242,6 +253,42 @@ spec:
 In this example we indicate that all Pods created by the Job should be contained
 within the same "rack".
 
+##### Job with multi-level scheduling
+
+According to [Story 8](#story-8), some users would like to place Pods of a Job
+across multi-layer topologies.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: tas-example-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 128
+  completions: 128
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
+        kueue.x-k8s.io/podset-slice-size: "64"
+        kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
+        kueue.x-k8s.io/podset-slice-size-1: "32"
+        kueue.x-k8s.io/podset-slice-required-topology-1: cloud.provider.com/block
+        kueue.x-k8s.io/podset-slice-size-2: "16"
+        kueue.x-k8s.io/podset-slice-required-topology-2: cloud.provider.com/rack
```

  Thank you for improving my recommendation.
  Let me describe my understanding in the following:
  
  - two-layer podset-slice (existing slicing mechanism)
  
  ```yaml
        annotations:
          kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
          kueue.x-k8s.io/podset-slice-size: "64"
          kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
  ```
  
  - multi-layer podset-slice (new proposed mechanism)
  
  ```yaml
        annotations:
          kueue.x-k8s.io/podset-slice-required-topology-constraints: |
            [
              {"topology": "cloud.provider.com/aizone", "size": "64"},
              {"topology": "cloud.provider.com/block", "size": "32"},
              {"topology": "cloud.provider.com/rack", "size": "16"},
            ]
  ```
  
  If this is correct, I fully agree with you 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-19T19:01:40Z

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-19T19:01:40Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  The latest description sounds good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-19T19:01:56Z

- **keps/2724-topology-aware-scheduling/README.md:1605** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-19T19:01:56Z
```diff
@@ -1428,6 +1532,46 @@ Explanation:
 
 It is worth noting that the tight fit mentioned above does not guarantee that no free capacity will be left within the assigned domains.
 
+### Multi-level Topology Aware scheduling
+
+> [!NOTE]
+> For the alpha implementation, the multi-level code path and API surface are
+> kept separate from two-level scheduling. In a future iteration, we plan to
+> unify them behind a single internal data structure.
+
+In consideration of [Story 8](#story-8), multi-level scheduling extends two-level
+scheduling to support more than two levels of topology constraints (e.g.,
+datacenter → block → rack → host). Up to 2 additional slice layers
+(`AdditionalSliceLayers`) can be specified beyond the first slice layer, for a
+total of 3 slice layers. Each additional layer must reference a topology level
+strictly lower (deeper) than the previous layer, and its size must evenly divide
+the parent layer's size. This feature is gated by `TASMultiLayerTopology`
+(alpha, disabled by default since v0.17).
+
+In two-level scheduling, below the outermost slice level the algorithm
+distributes individual pods (unit size = 1). With multi-level, a
+`sliceSizeAtLevel` map records the required group size at each intermediate
+level. During the downward traversal, the algorithm looks up this map to
+distribute pods in correctly-sized groups rather than individually. Before
+assigning groups at a given inner level, the algorithm recomputes `sliceState`
+on the child domains as `state / innerSliceSize`, since the `sliceState`
+populated during phase 1 reflects only the outermost slice size. The same
+sorting and selection logic (BestFit / LeastFreeCapacity) is applied at each
+level.
```

  SGTM, thank you 👍

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-02-20T05:49:41Z

- **keps/2724-topology-aware-scheduling/README.md:280** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-20T05:49:41Z
```diff
@@ -242,6 +253,42 @@ spec:
 In this example we indicate that all Pods created by the Job should be contained
 within the same "rack".
 
+##### Job with multi-level scheduling
+
+According to [Story 8](#story-8), some users would like to place Pods of a Job
+across multi-layer topologies.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: tas-example-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 128
+  completions: 128
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
+        kueue.x-k8s.io/podset-slice-size: "64"
+        kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
+        kueue.x-k8s.io/podset-slice-size-1: "32"
+        kueue.x-k8s.io/podset-slice-required-topology-1: cloud.provider.com/block
+        kueue.x-k8s.io/podset-slice-size-2: "16"
+        kueue.x-k8s.io/podset-slice-required-topology-2: cloud.provider.com/rack
```

  Yes, that's exactly what I want to achieve. In the future, depending on how popular N-layers are needed, we can gradually deprecate the 2-layer annotation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-20T10:20:09Z

- **keps/2724-topology-aware-scheduling/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-20T10:20:09Z
```diff
@@ -242,6 +253,42 @@ spec:
 In this example we indicate that all Pods created by the Job should be contained
 within the same "rack".
 
+##### Job with multi-level scheduling
+
+According to [Story 8](#story-8), some users would like to place Pods of a Job
+across multi-layer topologies.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: tas-example-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 128
+  completions: 128
+  template:
+    metadata:
+      annotations:
+        kueue.x-k8s.io/podset-required-topology: cloud.provider.com/datacenter
+        kueue.x-k8s.io/podset-slice-size: "64"
+        kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/aizone
+        kueue.x-k8s.io/podset-slice-size-1: "32"
+        kueue.x-k8s.io/podset-slice-required-topology-1: cloud.provider.com/block
+        kueue.x-k8s.io/podset-slice-size-2: "16"
+        kueue.x-k8s.io/podset-slice-required-topology-2: cloud.provider.com/rack
```

  > Yes, that's exactly what I want to achieve. In the future, depending on how popular N-layers are needed, we can gradually deprecate the 2-layer annotation.
  
  Thank you for checking that. That sounds reasonable for me.
  Yes, let's see which infrastructure layer is standard in the future, then decide if we should deprecate 2-layer one in the future.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-20T10:21:19Z

The latest approach looks great, thank you 👍 
/lgtm
/approve
