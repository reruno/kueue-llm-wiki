# PR #9294: e2e testing: load images into kind worker nodes in parallel

**Summary**: e2e testing: load images into kind worker nodes in parallel

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9294

**Last updated**: 2026-02-20T13:12:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `load-images-into-kind-worker-nodes-in-parallel` → `main`
- **Created**: 2026-02-16T14:31:29Z
- **Updated**: 2026-02-20T13:12:13Z
- **Merged**: 2026-02-20T10:29:39Z
- **Closed**: 2026-02-20T10:29:40Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+31 / -8**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9052

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-16T14:31:32Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-16T14:31:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7413bf2eb753f7dde93166291e7162b3ab7c0f0e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6997d650495b410007d64f93 |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-02-16T14:31:46Z

/ok-to-test
/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T08:49:23Z

LGTM, thank you 👍 Leaving final tagging to @mbobrovskyi 

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T08:49:26Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9294#issuecomment-3913140137):

>LGTM, thank you 👍 Leaving final tagging to @mbobrovskyi 
>
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-20T10:01:22Z

LGTM label has been added.  <details>Git tree hash: 9fcf51c29177ae200a309e7f80911c8b2e7155f8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-20T10:01:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9294#pullrequestreview-3831124333" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9294#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/testing/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/testing/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-20T10:30:20Z

@mimowo: new pull request created: #9387

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9294#issuecomment-3913140137):

>LGTM, thank you 👍 Leaving final tagging to @mbobrovskyi 
>
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-20T10:30:57Z

@mimowo: new pull request created: #9388

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9294#issuecomment-3913140137):

>LGTM, thank you 👍 Leaving final tagging to @mbobrovskyi 
>
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:46:57Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:46:57Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  ```suggestion
      trap "rm -rf '$tmp_image'" RETURN
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T06:49:46Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T06:49:46Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  why recursive, isnt this just a tarball? (single file)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:50:10Z

- **hack/testing/e2e-common.sh:457** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:50:10Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
```

  I think these comments are not in the right place.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:50:51Z

- **hack/testing/e2e-common.sh:459** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:50:51Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
```

  Why kind-image-XXXXXX? Should we set $2 (image) instead?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:52:05Z

- **hack/testing/e2e-common.sh:459** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:52:05Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
```

  Could you please split this into two variables:
  
  1. $tmp_dir (which we need to remove on trap)
  2. $tmp_image

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:55:22Z

- **hack/testing/e2e-common.sh:460** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:55:22Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
```

  Please fix instead if ignore linter.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T06:58:24Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T06:58:24Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  I think we should remove tmp directory.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T06:58:59Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T06:58:59Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T07:00:41Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T07:00:41Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  Actually, Im not sure, maybe tmp could be reused by otger parallel processes during make?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T07:05:10Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T07:05:10Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  Oh, that’s actually a good question. If the user sets `${TMPDIR:-/tmp}`, we could accidentally remove a temporary directory used by another running operations.
  
  So we probably need to add a check: if the directory is user-specified, we keep it; if it’s a generated one, we remove it.
  
  Or alternatively just use `mktemp`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T07:07:06Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T07:07:06Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  sounds complex, i think deleting just the tarball is ok

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T07:08:40Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T07:08:40Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  > sounds complex, i think deleting just the tarball is ok
  
  Probably yes, but after hundreds of runs we’ll accumulate unnecessary directories. They look like temp directories, but still.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T07:23:39Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T07:23:39Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  Why houndreds? I expect the user will reuse the same, either /tmp or $TMPDIR. If someone generates a new tmpdir per run, they could be expected to clean also.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T07:32:38Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T07:32:38Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  OK, here’s what I see when I execute this command:
  
  ```
  ➜  kueuecopy git:(load-images-into-kind-worker-nodes-in-parallel) ✗ tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")               
  ➜  kueuecopy git:(load-images-into-kind-worker-nodes-in-parallel) ✗ echo $tmp_image
  /var/folders/82/c_p6rrjn6m7f7bb881hnb1lc0000gn/T//kind-image-XXXXXX.tar
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T07:46:06Z

- **hack/testing/e2e-common.sh:461** by [@mimowo](https://github.com/mimowo) — 2026-02-18T07:46:06Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  Do use custom TMPDIR? I'm wondering if we need to support it at all, we could just use mktemp, similarly as in other places: https://github.com/search?q=repo%3Akubernetes-sigs%2Fkueue+mktemp&type=code
  
  Then could safely delete it. Otherwise I prefer to delete just the tarball

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T07:50:25Z

- **hack/testing/e2e-common.sh:461** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T07:50:24Z
```diff
@@ -451,17 +451,40 @@ function cluster_kind_load_image {
         return 1
     fi
 
-    # Use docker save + ctr import directly to avoid the --all-platforms
+    # Save image to a temp file once, then load into all worker nodes in parallel.
+    # Using docker save + ctr import directly to avoid the --all-platforms
     # issue with multi-arch images in DinD environments.
     # See: https://github.com/kubernetes-sigs/kind/issues/3795
-    echo "Loading image '$2' to cluster '$1'"
+    local tmp_image
+    tmp_image=$(mktemp "${TMPDIR:-/tmp}/kind-image-XXXXXX.tar")
+    # shellcheck disable=SC2064 # Intentionally expand now to capture the temp file path
+    trap "rm -f '$tmp_image'" RETURN
```

  > Do use custom TMPDIR? 
  
  No, I don’t. I could use one, but I don’t want to set this variable every time I run e2e tests.
  
  > I'm wondering if we need to support it at all, we could just use mktemp, similarly as in other places: https://github.com/search?q=repo%3Akubernetes-sigs%2Fkueue+mktemp&type=code
  >
  > Then could safely delete it. Otherwise I prefer to delete just the tarball
  
  Agree! Just use `mktemp`, and that’s it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-20T10:01:15Z

/lgtm
/approve

Thank you!
