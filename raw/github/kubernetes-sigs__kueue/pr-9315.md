# PR #9315: Increase the timeout for waiting for the Deployment update to start and check if ObservedGeneration changed.

**Summary**: Increase the timeout for waiting for the Deployment update to start and check if ObservedGeneration changed.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9315

**Last updated**: 2026-02-17T18:01:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/bump-timeout` → `main`
- **Created**: 2026-02-17T11:27:39Z
- **Updated**: 2026-02-17T18:01:28Z
- **Merged**: 2026-02-17T17:11:39Z
- **Closed**: 2026-02-17T17:11:39Z
- **Labels**: `kind/bug`, `lgtm`, `kind/failing-test`, `kind/flake`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+5 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind failing-test
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Increase the timeout for waiting for the Deployment update to start and check if ObservedGeneration changed.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9313

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-17T11:27:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e90795a7e50b1e0d63b8ffc376424a929f53021c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/699493cbb352f20008525b18 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-17T11:27:55Z

/cherry-pick release-0.16
/cherry-pick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T11:27:57Z

@mbobrovskyi: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9315#issuecomment-3914152984):

>/cherry-pick release-0.16
>/cherry-pick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-17T11:47:13Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T12:49:38Z

I think this is also likely to fix https://github.com/kubernetes-sigs/kueue/issues/9301, so we may consider "Fixes #9301"

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:25:04Z

Thank you 👍 
/unhold
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T16:25:08Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9315#issuecomment-3915708289):

>Thank you 👍 
>/unhold
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T16:25:13Z

LGTM label has been added.  <details>Git tree hash: 00857c25b7804cfd5fbd8a99bda740ef8851e0e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T16:25:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9315#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9315#issuecomment-3915708289" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T17:12:19Z

@mbobrovskyi: new pull request created: #9325

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9315#issuecomment-3914152984):

>/cherry-pick release-0.16
>/cherry-pick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-17T17:12:55Z

@mbobrovskyi: new pull request created: #9326

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9315#issuecomment-3914152984):

>/cherry-pick release-0.16
>/cherry-pick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T12:03:00Z

- **test/util/e2e.go:244** by [@mimowo](https://github.com/mimowo) — 2026-02-17T12:03:00Z
```diff
@@ -240,6 +240,7 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		ginkgo.GinkgoLogr.Info("Deployment status condition before the restart", "type", deploymentCondition.Type, "status", deploymentCondition.Status, "reason", deploymentCondition.Reason)
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	beforeObservedGeneration := deployment.Status.ObservedGeneration
 	beforeUpdateTime := deploymentCondition.LastUpdateTime
```

  Should we also add waiting to cross the full second bundary here, ptal https://github.com/kubernetes-sigs/kueue/issues/9313#issuecomment-3914335676?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-17T12:42:37Z

- **test/util/e2e.go:244** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-17T12:42:37Z
```diff
@@ -240,6 +240,7 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		ginkgo.GinkgoLogr.Info("Deployment status condition before the restart", "type", deploymentCondition.Type, "status", deploymentCondition.Status, "reason", deploymentCondition.Reason)
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	beforeObservedGeneration := deployment.Status.ObservedGeneration
 	beforeUpdateTime := deploymentCondition.LastUpdateTime
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T12:47:11Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T12:47:11Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  I think we should wait before triggering the update. Otherwise it is possible the `deploymentCondition.LastUpdateTime` will still be in the previous second, we will just check it in the new second.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T13:37:29Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T13:37:29Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  if `deploymentCondition.LastUpdateTime == beforeUpdateTime, then it does not matter how long you want afterwards

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T13:37:36Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T13:37:36Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  /hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T15:31:13Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T15:31:12Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  Ok, I spent some time thinking about it, this looks correct, wdyt? 
  
  ```golang
  // Wait for the next second to ensure the Progressing process is started on the next full second
  var beforeUpdateTime time.Time
  var beforeObservedGeneration time.Time
  
  // Apply changes and update Deployment.
  gomega.Eventually(func(g gomega.Gomega) {
    g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
    deploymentCondition := FindDeploymentCondition(deployment, appsv1.DeploymentProgressing)
    g.Expect(deploymentCondition).NotTo(gomega.BeNil())
    beforeUpdateTime := deploymentCondition.LastUpdateTime
    beforeObservedGeneration = deploymentt.Status.ObservedGeneration
    
    // wait up to full second to ensure the update has a new (different) timestamp
    WaitForNextSecond(beforeUpdateTime)
    applyChanges(deployment)
     g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
  }, Timeout, Interval).Should(gomega.Succeed())
  
  // Wait for the Deployment update to be in progress.
  gomega.Eventually(func(g gomega.Gomega) {
    g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
    g.Expect(deployment.Status.ObservedGeneration).NotTo(gomega.Equal(beforeObservedGeneration))
    deploymentCondition := FindDeploymentCondition(deployment, appsv1.DeploymentProgressing)
    g.Expect(deploymentCondition).NotTo(gomega.BeNil())
    g.Expect(deploymentCondition.LastUpdateTime).NotTo(gomega.Equal(beforeUpdateTime))
  }, LongTimeout, Interval).Should(gomega.Succeed())
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T15:36:20Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T15:36:20Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  but I think we could also just replace checking `LastUpdateTime` with relying on `ObservedGeneration`. 
  
  This would simplify the check a lot, and avoid the custom WaitForNextSecond.
  
  The only drawback might be is another process / controller updates the deployment spec in the meanwhile, but that should not happen in our tests ( at least I don't know about the case). The tests which update configuration are always sequential

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T15:38:59Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T15:38:58Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  If there are concurrent modifications of Kueue deployment in e2e tests then we have a bigger problem, so honestly I would just simplify the check to something like
  ```golang
  var beforeObservedGeneration time.Time
  
  // Apply changes and update Deployment.
  gomega.Eventually(func(g gomega.Gomega) {
    g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
    deploymentCondition := FindDeploymentCondition(deployment, appsv1.DeploymentProgressing)
    g.Expect(deploymentCondition).NotTo(gomega.BeNil())
    beforeObservedGeneration = deploymentt.Status.ObservedGeneration
    
    applyChanges(deployment)
     g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
  }, Timeout, Interval).Should(gomega.Succeed())
  
  // Wait for the Deployment update to be in progress.
  gomega.Eventually(func(g gomega.Gomega) {
    g.Expect(k8sClient.Get(ctx, key, deployment)).To(gomega.Succeed())
    g.Expect(deployment.Status.ObservedGeneration).NotTo(gomega.Equal(beforeObservedGeneration))
    deploymentCondition := FindDeploymentCondition(deployment, appsv1.DeploymentProgressing)
    g.Expect(deploymentCondition).NotTo(gomega.BeNil())
  }, LongTimeout, Interval).Should(gomega.Succeed())
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:08:07Z

- **test/util/e2e.go:255** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:08:07Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  Actually, I synced with @mbobrovskyi on priv. We may have in this code an unlikely scenario when there is a pending deployment status update, which will bump the generation before our update is respected. To make sure there are no pending status we may check that `generation = status.observedGeneration` before the code above.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-17T16:14:05Z

- **test/util/e2e.go:255** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-17T16:14:04Z
```diff
@@ -249,13 +250,18 @@ func UpdateDeploymentAndWaitForProgressing(ctx context.Context, k8sClient client
 		g.Expect(k8sClient.Update(ctx, deployment)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 
+	// Wait for the next second to ensure the Progressing process has started.
+	// Otherwise, the update may happen too quickly and cause a flake.
+	WaitForNextSecond(beforeUpdateTime)
```

  Done
