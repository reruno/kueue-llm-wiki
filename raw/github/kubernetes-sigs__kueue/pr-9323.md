# PR #9323: KEP: Pod Integration Fast Quota Release

**Summary**: KEP: Pod Integration Fast Quota Release

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9323

**Last updated**: 2026-03-20T03:05:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tskillian](https://github.com/tskillian)
- **Branch**: `pod-fast-preemption-kep` → `main`
- **Created**: 2026-02-17T16:02:44Z
- **Updated**: 2026-03-20T03:05:28Z
- **Merged**: 2026-03-17T21:01:39Z
- **Closed**: 2026-03-17T21:01:39Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd)
- **Changed files**: 2   **+257 / -0**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
This KEP is in relation to https://github.com/kubernetes-sigs/kueue/issues/6143

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-17T16:02:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4b7124c718ab2115ee7ea50899c9cfdaf3d78918 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b9a335541fa20008d0ff89 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-17T16:02:57Z

Hi @tskillian. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:11:01Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:22:00Z

cc @PBundyra

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T21:03:28Z

> LGTM, mostly editorial nits. cc @tenzen-y

ACK, I landed here and will check this in the next.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-17T21:07:48Z

/release-note-none

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-02-18T09:47:45Z

LGTM overall, I will stamp it after addressing existing comments

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T07:53:02Z

@tskillian please update the TOC so that `pull-kueue-verify-main` is happy. You will find a script in ./hack dir for that

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T07:56:32Z

I consider this actually a consistency bug between the Job and Pod integrations: https://github.com/kubernetes-sigs/kueue/pull/9323/changes#diff-8f5a0126e97b6a988203d80f8e8bed06b183df1e45b40bd74af19a395f86082eR34-R37

So, I'm happy to even backport as long as we have a feature gate which will allow users to mitigate in case of issues

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:36:20Z

/kind kep

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T19:02:14Z

Lgtm overall, but multiple editorial comments to offload the sections. For the first iteration I basically propose only one feature-gate in alpha. Let's re-evaluate the future (knob, or graduation) based on feedback.

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2026-03-16T16:24:29Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: tenzen-y / name: Yuki Iwai  (612e2f5f72e19c62d2d8f4802d54027037bc14bd, f323f6302f16c21676d3643cd5c3802631f53a6a)</li><li>:white_check_mark: login: tskillian / name: Tim Killian  (05f9e1beb92c06e8051c5b4b5abba73e4a0ee85d, 36c643d259e22a90f44042da2032b837e79db0c8, 3afbb90e8ecde081b2a426b642c50ad665f24c8d, 4b7124c718ab2115ee7ea50899c9cfdaf3d78918, 612e2f5f72e19c62d2d8f4802d54027037bc14bd, 6940b11c0d0848903ad17924a82bae53540f5567, 9f2868965ac90f6708ec2e109403602026609772, b2f04a2036dc11484180651a00e01ace2e85a919, d6c7b352c86f4200b286c9a335d052d466be9e65, f03d725ed066890b14d8eb8690fbd1e4480aa5a2, f323f6302f16c21676d3643cd5c3802631f53a6a)</li></ul>

### Comment by [@tskillian](https://github.com/tskillian) — 2026-03-16T17:35:58Z

@mimowo finished addressing your last round of comments FYI

### Comment by [@tskillian](https://github.com/tskillian) — 2026-03-17T16:42:01Z

Accepted the suggested changes

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T17:47:57Z

> Accepted the suggested changes

Could you fix CI error by executing `make toc-update` in your local?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T20:45:12Z

LGTM label has been added.  <details>Git tree hash: 61a437343c9e4a3cb336846f2a4478d7cbb26915</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T20:45:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9323#pullrequestreview-3963670271" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9323#" title="Author self-approved">tskillian</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:15:02Z

- **keps/6143-pod-integration-fast-quota-release/README.md:67** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:15:02Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
```

  Actually, I wouldn't make it a non-goal. If the FG is beta, it means the behavior is changed. I'm leaning to consider it a bug. I consider the FG more as a bailout option if someone complains. Then we will need a configuraiton change in the API.

### Review by [@tkillian-sc](https://github.com/tkillian-sc) (commented) — 2026-02-17T16:17:09Z

- **keps/6143-pod-integration-fast-quota-release/README.md:67** by [@tkillian-sc](https://github.com/tkillian-sc) — 2026-02-17T16:17:09Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
```

  I think this makes sense. Would you want to initially release it with the FG defaulting to disabled, and then later changing the default to enabled? Or do you think we should start with it enabled from the start?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:17:22Z

- **keps/6143-pod-integration-fast-quota-release/README.md:100** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:17:22Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
```

  Exactly, this is my current thinking about the problem. This is how we handle Jobs, Jobsets, etc, so Pods should be handled the same way. This might have drawbacks as  you mention in the "Risks and Mitigations" section, but they are already present for Jobs. So I would approach solving the consistency first, and later the other issues (if needed, but we don't have that signal from users yet).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:18:09Z

- **keps/6143-pod-integration-fast-quota-release/README.md:110** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:18:09Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
+
+### Risks and Mitigations
+
+**Risk**: This could cause an increase in node churn if the cluster autoscaler 
+is being used. While pods are pending, the cluster autoscaler may trigger scale-up 
+even though pods could be terminating on existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
```

  I would say the mitigation is the feature gate. We will re-consider the API-level change if this proves to be an issue for some users.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:19:00Z

- **keps/6143-pod-integration-fast-quota-release/README.md:118** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:19:00Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
+
+### Risks and Mitigations
+
+**Risk**: This could cause an increase in node churn if the cluster autoscaler 
+is being used. While pods are pending, the cluster autoscaler may trigger scale-up 
+even though pods could be terminating on existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: If a pod gets "stuck" in a terminating state for whatever reason, 
+there may be a discrepancy in the amount of quota available and the amount 
+of resources available on the kubernetes cluster.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
```

  + Here I would also say we have the othoginal mitigation mechanism already, which is https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:19:27Z

- **keps/6143-pod-integration-fast-quota-release/README.md:125** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:19:27Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
+
+### Risks and Mitigations
+
+**Risk**: This could cause an increase in node churn if the cluster autoscaler 
+is being used. While pods are pending, the cluster autoscaler may trigger scale-up 
+even though pods could be terminating on existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: If a pod gets "stuck" in a terminating state for whatever reason, 
+there may be a discrepancy in the amount of quota available and the amount 
+of resources available on the kubernetes cluster.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: Even if a consistency bug, users may be relying on the current behavior (quota held until Pod
+termination).
+
+**Mitigation**: The feature is gated behind `PodIntegrationFastQuotaRelease`
+and follows the standard Alpha/Beta graduation process, giving users time to
+test and adapt.
```

  This seems like could be combined as point 1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:20:16Z

- **keps/6143-pod-integration-fast-quota-release/README.md:169** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:20:16Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
+
+### Risks and Mitigations
+
+**Risk**: This could cause an increase in node churn if the cluster autoscaler 
+is being used. While pods are pending, the cluster autoscaler may trigger scale-up 
+even though pods could be terminating on existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: If a pod gets "stuck" in a terminating state for whatever reason, 
+there may be a discrepancy in the amount of quota available and the amount 
+of resources available on the kubernetes cluster.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: Even if a consistency bug, users may be relying on the current behavior (quota held until Pod
+termination).
+
+**Mitigation**: The feature is gated behind `PodIntegrationFastQuotaRelease`
+and follows the standard Alpha/Beta graduation process, giving users time to
+test and adapt.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `PodIntegrationFastQuotaRelease` feature gate is enabled, the
+`IsActive()` method is modified to treat any Pod with a `deletionTimestamp` as
+inactive:
+
+```go
+func (p *Pod) IsActive() bool {
+    for i := range p.list.Items {
+        pod := p.list.Items[i]
+
+        // Pods that are not in the Running phase are never considered Active.
+        if pod.Status.Phase != corev1.PodRunning {
+            continue
+        }
+
+        // When PodIntegrationFastQuotaRelease is enabled, any pod with a
+        // deletionTimestamp is considered inactive, aligning with how the
+        // Job controller handles status.active.
+        if features.Enabled(features.PodIntegrationFastQuotaRelease) {
+            if pod.DeletionTimestamp != nil {
+                continue
+            }
+        } else {
+            // Legacy behavior: only skip pods stuck past their grace period.
+            if pod.DeletionTimestamp != nil && pod.DeletionGracePeriodSeconds != nil {
+                now := p.clock.Now()
+                gracePeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+                if now.After(pod.DeletionTimestamp.Add(gracePeriod)) {
+                    continue
+                }
+            }
+        }
+
+        return true
+    }
+    return false
+}
```

  Let's don't put code, it makes the KEPs obsolete very fast. Just saying wha tis the change is enough

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:21:04Z

- **keps/6143-pod-integration-fast-quota-release/README.md:204** by [@mimowo](https://github.com/mimowo) — 2026-02-17T16:21:04Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority 
+workloads to start running as quickly as possible while still honoring 
+graceful shutdown periods of preempted pods.
+
+### Notes/Constraints/Caveats
+
+- One could argue that this is a consistency bug fix. That being said, putting 
+it behind a configuration flag would still be most prudent so users don't get any 
+surprising changes in behavior.
+
+### Risks and Mitigations
+
+**Risk**: This could cause an increase in node churn if the cluster autoscaler 
+is being used. While pods are pending, the cluster autoscaler may trigger scale-up 
+even though pods could be terminating on existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: If a pod gets "stuck" in a terminating state for whatever reason, 
+there may be a discrepancy in the amount of quota available and the amount 
+of resources available on the kubernetes cluster.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood 
+behavior.
+
+**Risk**: Even if a consistency bug, users may be relying on the current behavior (quota held until Pod
+termination).
+
+**Mitigation**: The feature is gated behind `PodIntegrationFastQuotaRelease`
+and follows the standard Alpha/Beta graduation process, giving users time to
+test and adapt.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `PodIntegrationFastQuotaRelease` feature gate is enabled, the
+`IsActive()` method is modified to treat any Pod with a `deletionTimestamp` as
+inactive:
+
+```go
+func (p *Pod) IsActive() bool {
+    for i := range p.list.Items {
+        pod := p.list.Items[i]
+
+        // Pods that are not in the Running phase are never considered Active.
+        if pod.Status.Phase != corev1.PodRunning {
+            continue
+        }
+
+        // When PodIntegrationFastQuotaRelease is enabled, any pod with a
+        // deletionTimestamp is considered inactive, aligning with how the
+        // Job controller handles status.active.
+        if features.Enabled(features.PodIntegrationFastQuotaRelease) {
+            if pod.DeletionTimestamp != nil {
+                continue
+            }
+        } else {
+            // Legacy behavior: only skip pods stuck past their grace period.
+            if pod.DeletionTimestamp != nil && pod.DeletionGracePeriodSeconds != nil {
+                now := p.clock.Now()
+                gracePeriod := time.Duration(*pod.DeletionGracePeriodSeconds) * time.Second
+                if now.After(pod.DeletionTimestamp.Add(gracePeriod)) {
+                    continue
+                }
+            }
+        }
+
+        return true
+    }
+    return false
+}
+```
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Feature gate registration
+
+Add the feature gate constant and versioned spec to
+`pkg/features/kube_features.go`:
+
+```go
+const (
+    // owner: @tkillian
+    // kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/6143-pod-integration-fast-quota-release
+    //
+    // Releases quota for Pod workloads as soon as all Pods are terminating
+    // (have deletionTimestamp), aligning with Job and other integrations.
+    PodIntegrationFastQuotaRelease featuregate.Feature = "PodIntegrationFastQuotaRelease"
+)
+
+var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.VersionedSpecs{
+    // ...existing gates...
+    PodIntegrationFastQuotaRelease: {
+        {Version: version.MustParse("0.15"), Default: false, PreRelease: featuregate.Alpha},
+        {Version: version.MustParse("0.17"), Default: true, PreRelease: featuregate.Beta},
+    },
+}
```

  This is standard procedure, no need to re-iterate in README.md, info is already present in kep.yaml

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-17T16:21:46Z

LGTM, mostly editorial nits. cc @tenzen-y

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-17T16:57:59Z

- **keps/6143-pod-integration-fast-quota-release/README.md:86** by [@amy](https://github.com/amy) — 2026-02-17T16:58:00Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
+  fully complete before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+- Release quota for Pod workloads as soon as all Pods have a
+  `deletionTimestamp`, without waiting for Pods to reach a terminal phase.
+- Provide a feature gate (`PodIntegrationFastQuotaRelease`) to control this
+  behavior.
+- Target Beta in v0.17, with backports as Alpha to v0.15 and v0.16.
+
+### Non-Goals
+
+- Changing any default behavior, though it could be argued that this is a 
+consistency bug that should be fixed.
+
+## Proposal
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive when the `PodIntegrationFastQuotaRelease`
+feature gate is enabled. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+### User Stories
```

  Story 2: As a cluster administrator, I migrate ClusterQueues to a new cluster one by one. Because burst is free for the initial CQs, these CQs will burst past their nominal quota. I want subsequent CQs to be able to reclaim their nominal quota within a reasonable amount of time.

### Review by [@amy](https://github.com/amy) (commented) — 2026-02-17T17:08:53Z

- **keps/6143-pod-integration-fast-quota-release/README.md:52** by [@amy](https://github.com/amy) — 2026-02-17T17:08:53Z
```diff
@@ -0,0 +1,263 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active 
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers 
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current 
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds 
+or even minutes (and has no practical upper bound, depending on how the pod's 
+graceful shutdown period is configured). While the pod is terminating, ALL 
+pods in each cluster queue is blocked from triggering further preemptions until 
+it's finished because the terminating pod is always each potential preemptor pod's 
+ideal preemption target. This obviously creates a large bottleneck for preemption 
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where the scheduler must wait for one preemption to
```

  Nit (so feel free to ignore): I would rephrase this. 
  
  Serialized preemption where CQs are head of line blocking. Meaning the scheduler must wait for one preemption within the CQ to fully complete before starting the next.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T07:53:29Z

- **keps/6143-quota-release-strategy/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2026-02-19T07:53:29Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
```

  please add @amy  and @PBundyra

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-19T19:50:12Z

Thank you for sharing the interesting stories.

- **keps/6143-pod-integration-fast-quota-release/README.md:37** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-19T19:42:15Z
```diff
@@ -0,0 +1,200 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  I think that the Kueue Pod integration behavior is correct, and the Kueue Job integration `IsActive` is not correct. We recently fixed such Pod terminating state problems in https://github.com/kubernetes/kubernetes/issues/123775.
  
  So, my expectation for Kueue Job integration is the following:
  
  ```go
  func (j *Job) IsActive() bool {
  	for _, c := range j.Status.Conditions {
  		if (c.Type == batchv1.JobFailureTarget || c.Type == batchv1.JobSuccessCriteriaMet) && c.Status == corev1.ConditionTrue {
  			return false
  		}
  	}
  	return j.Status.Active != 0
  }
  ```
  
  @mimowo Tracking node resource capacity PoV, accurate Pod resource usage tracking is important in quota manager (e.g., Kueue).

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-19T19:49:26Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  @mimowo I'm ok with releasing this enhancement as an alpha for now, but I don't agree with this feature promotion (alpha -> beta / beta -> ga). This feature should be handled as a temporary approach until we introduce the correct approach.
  Because this potentially breaks quota management and introduce fragmentations between actual Node capacity and ClusterQueue quotas.
  
  The ideal approach is 
  
  1. Correctly handle Active state like the following:
  
  ```go
  func (j *Job) IsActive() bool {
  	for _, c := range j.Status.Conditions {
  		if (c.Type == batchv1.JobFailureTarget || c.Type == batchv1.JobSuccessCriteriaMet) && c.Status == corev1.ConditionTrue {
  			return false
  		}
  	}
  	return j.Status.Active != 0
  }
  ```
  
  2. Introduce a new feature to allow us to temporarily quota oversubscribing for `Story 2: ClusterQueue migration to new clusters`. 
  3. Introduce a new mechanism for tracking pod terminations after Fast quota release for the `Story 1: Preemption of "plain pod" workloads`. And only when the terminations succeeded, kueue-scheduler (or tas) decided to admit (or ungate).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T19:58:39Z

- **keps/6143-pod-integration-fast-quota-release/README.md:37** by [@mimowo](https://github.com/mimowo) — 2026-02-19T19:58:39Z
```diff
@@ -0,0 +1,200 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  > I think that the Kueue Pod integration behavior is correct
  
  I don't think there is a notion of "correct" here. For jobs we haven't really hard problems reported yet. Sure, it may not be ideal, but I think we need to align first.
  
  Actually, in the example you give note that:
  1. `JobFailureTarget` is set while some pods are still terminating, and might be running
  2. `return j.Status.Active != 0` answers `IsActive=false` when the pods are terminating (have deletionTimestamp), and so this is inconsistent with the Pod behavior. The KEP is introducing consistency.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T20:01:26Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-19T20:01:26Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  @tenzen-y I consider this a "consistency bug", and as a bug I think we could fix it going to Beta streight. 
  I don't like the idea of maintaining two different behaviors for long (until two features implemented, iiuc).
  
  Actually, this KEP is proposing to align pretty much with what you propose for job, because, most improtantly it corresponds to: `return j.Status.Active != 0`, see also my other comment: https://github.com/kubernetes-sigs/kueue/pull/9323#discussion_r2829878978

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T20:07:05Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-19T20:07:05Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  To make it simple, let's forget about the job termination for now. Let's think about the job suspection only. Then the code you propose is basically `return j.Status.Active != 0` which says "release quota as soon as pods are terminating". However, for Pod integration we have "release quota as soon as pods are terminating + deletionTimestamp".
  
  I'm not saying one approach is better than the other - both have drawbacks, but I haven't heard complains about Job's behavior.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T10:30:20Z

- **keps/6143-pod-integration-fast-quota-release/README.md:37** by [@mimowo](https://github.com/mimowo) — 2026-02-20T10:30:20Z
```diff
@@ -0,0 +1,200 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  > We recently fixed such Pod terminating state problems in https://github.com/kubernetes/kubernetes/issues/123775.
  
  Actually, I took part in fixing, but I think this was a slightly different issue. 
  
  Here, the issue was that the status.terminating field was stale (not being tracked down to 0) - making it unusable for Kueue. Still, the `status.active` field does not include the terminating Pods. So, if we wanted to wait for all terminating Pods (on suspend or failureTarget), then we should modify the Job's integration code to wait:
  
  `return j.Status.Active != 0 || ptr.Deref(j.Status.Terminating, 0) > 0`
  
  Then, we would get closer to what the Pod integration is doing currently. Because we have this inconsistency I would like to align one of them - either Job or Pods. Aligning Pods seem simpler, because:
  1. the same pattern as for Job is also present for JobSet, and these two are used a lot
  2. we have a user request for Pods to align with Job. We don't have user request for Jobs to align with Pods
  
  Sure, long term I consider users can choose between the two strategies depending on use case, either globally or per-Job.

### Review by [@tskillian](https://github.com/tskillian) (commented) — 2026-02-20T15:42:24Z

- **keps/6143-pod-integration-fast-quota-release/README.md:37** by [@tskillian](https://github.com/tskillian) — 2026-02-20T15:42:24Z
```diff
@@ -0,0 +1,200 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  I also think that from a principle of least surprise, aligning behavior with what jobs currently do (release quota as soon as terminating) is the superior approach for a couple of reasons:
  
  - kueue quota already does not perfectly track with usage/capacity on the cluster with how it admits workloads. Unless I'm mistaken, as soon as kueue admits a pod it reserves quota. However, that pod could then get stuck in a pending state indefinitely (maybe quota didn't align with actual cluster resources, or maybe cluster autoscaler isn't able to scale up, etc.)
  - This will cause preemption behavior that's more consistent with k8s scheduler preemption (i.e. no head-of-line blocking type behavior). I think if job termination suddenly started blocking preemption, many users would complain about it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-20T19:15:34Z

- **keps/6143-pod-integration-fast-quota-release/README.md:37** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-20T19:15:34Z
```diff
@@ -0,0 +1,200 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of "plain pod" workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces the `PodIntegrationFastQuotaRelease` feature gate to align
+the Pod integration's quota release behavior with other integrations (e.g.,
+batch/v1 Job). When enabled, quota from "plain pods" are released as soon as the
+pods are terminating (i.e. has a `deletionTimestamp`), rather than waiting for pods
+to fully terminate.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  > Still, the status.active field does not include the terminating Pods. So, if we wanted to wait for all terminating Pods (on suspend or failureTarget), then we should modify the Job's integration code to wait:
  > 
  > return j.Status.Active != 0 || ptr.Deref(j.Status.Terminating, 0) > 0
  
  This "wait" approach mostly aligns with my expectations.
  
  > we have a user request for Pods to align with Job. We don't have user request for Jobs to align with Pods
  
  I don't want to disturb your customers request. But, the strict quota management is very important, especially in a fixed-size cluster (without autoscaling). So, I'd like to see the approaches that can satisfy both users (fixed-size / autoscaling cluster).
  
  In case of a fixed-size cluster (the sum of nominal quota strictly equals to cluster capacity), enabling this feature by default indicates that we allow temporary allow capacity usage violations.
  
  Let's say we have cq-a with 10 nominal, cq-b with 5 nominal, and pod-b consumes 5 nominal of cq-b with 15 min terminationGracePeriodSeconds. 
  
  When pod-b is evicted, the cq-b can temporarily exceed the 5 nominal quota, then consume another cq-a's 5 nominal quota. If these nominal quotas exactly match the cluster capacity, this is a problem.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-20T19:19:21Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-20T19:19:21Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  As I mentioned in https://github.com/kubernetes-sigs/kueue/pull/9323#discussion_r2834726234, in the fixed-size cluster, the strict quota management is very important. 
  
  So, I'd propose introducing a knob to select fast quota release mode or strict management mode in the cluster-wise (maybe Configuration API) to Fast Quota Release Beta graduation criteria.
  
  @mimowo Please let me know if this is acceptable for you or your customers.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-20T19:20:04Z

- **keps/6143-quota-release-strategy/kep.yaml:13** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-20T19:20:04Z
```diff
@@ -0,0 +1,29 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+  - @amy
+  - @PBundyra
+approvers:
+  - @mimowo
```

  ```suggestion
    - @mimowo
    - @tenzen-y 
  ```
  
  It would be great to add me here as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T19:35:08Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-20T19:35:08Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  For sure the quota is important, Im just proposing to de-couple the two problems:
  1. simple - fixing the consistency issue (change scoped to Pod integration)
  2. difficult solving the atutoscaling issue ( change for all integrations)
  
  because 2. requires new API so certainly is harder. Also now we have ProvisioningRequests which largely eliminate the problems on autoscaled clusters.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T19:38:27Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-20T19:38:27Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  > So, I'd propose introducing a knob to select fast quota release mode or strict management mode in the cluster-wise (maybe Configuration API) to Fast Quota Release Beta graduation criteria.
  
  We can, I would just think this will make the KEP more complex. Because I consider 1 a consistency bug I would like to even consider back porting.
  
  Still I'm ok to couple the two problems, I'm just thinking it may the KEP more challenging

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-20T20:19:10Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-20T20:19:10Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  Ok, I'm syncing with @tenzen-y the current thinking is to try covering both semantics with this KEP. We may start with just feature gates for them, and then convert into knobs in a follow up iteration.
  
  The sensible semantics we are considering:
  1. release quota fast (current proposal, and implementation for most frameworks)
  2. release quota after grace period or all pods terminal (current Pods integration)
  3. release quota when all pods terminal (currently no framwork IIUC)
  
  We could probably start with 1 and 2. Will think more next week, just letting you know the direction. wdyt?

### Review by [@tskillian](https://github.com/tskillian) (commented) — 2026-02-23T15:26:09Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@tskillian](https://github.com/tskillian) — 2026-02-23T15:26:09Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  Just to make sure we're all on the same page, the desired end state is that both jobs and pod integrations release quota fast by default (current job behavior). There will be configuration for both to only release quota once their pods are fully terminated. Is this accurate?
  
  If so, is this what we're saying the rollout plan would be?
  
  1) Modify the pods integration behavior to match current job behavior (fast release), behind a feature gate
  2) Backport this change as a consistency bug
  3) Make this the default behavior, but allow users to switch modes through configuration
  4) Implement same configuration option for jobs (fast release, or release only when all terminated).
  
  My last question is whether this KEP should still focus only on the consistency bug (getting pod integration behavior to match jobs), or should it encapsulate this whole plan?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T16:55:51Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-26T16:55:51Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  > If so, is this what we're saying the rollout plan would be?
  
  I'm ok with all the points here, but let me check with @tenzen-y, are you ok with that, or have an alternative path forward?
  
  > My last question is whether this KEP should still focus only on the consistency bug (getting pod integration behavior to match jobs), or should it encapsulate this whole plan?
  
  I'm ok with either limited scope or extending, IIUC @tenzen-y is leaning towards extending the scope, is that right? In that case we should also plan for a feature gate for DelayedQuotaRelease, where the quota is released after all pods are terminal, or some graceful period elapses, say 30s. I imagine the feature gates are converted into knobs in the follow up release, 0.18.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T17:35:12Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2026-02-26T17:35:12Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  A sensible alternative for now is to introduce the PodIntegrationFastQuotaRelease feature gate as Alpha (disabled by default) for 0.17 and cherrypick. This will allow you to use the new code, and will give us more time to ponder on the path forward in Beta, wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-27T03:39:12Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T03:39:12Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  >> If so, is this what we're saying the rollout plan would be?
  >>
  > I'm ok with all the points here, but let me check with @tenzen-y, are you ok with that, or have an alternative path forward?
  
  I don't think that this is a bug. The semantics fully depend on their cluster environment. So, this will be shipped as a feature. I don't block this feature backporting, but we should disable the FG by default on the released branches (backport) because this is a pretty aggressive change, which can break the existing behavior.
  
  > > My last question is whether this KEP should still focus only on the consistency bug (getting pod integration behavior to match jobs), or should it encapsulate this whole plan?
  > 
  > I'm ok with either limited scope or extending, IIUC @tenzen-y is leaning towards extending the scope, is that right? In that case we should also plan for a feature gate for DelayedQuotaRelease, where the quota is released after all pods are terminal, or some graceful period elapses, say 30s. I imagine the feature gates are converted into knobs in the follow up release, 0.18.
  
  Yes, right. At least, I think that we should introduce both PodIntegrationFastQuotaRelease (or FastQuotaRelease) and DelayedQuotaRelease together so that we can switch the quota release mode.
  I also agree with adding knobs to replace FGs in v0.18. Additionally, we should mention those knob addition to the graduation criteria.

### Review by [@tskillian](https://github.com/tskillian) (commented) — 2026-03-04T16:12:28Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:20** by [@tskillian](https://github.com/tskillian) — 2026-03-04T16:12:28Z
```diff
@@ -0,0 +1,27 @@
+title: Pod Integration Fast Quota Release
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+approvers:
+  - @mimowo
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: beta
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.15"
+  beta: "v0.17"
```

  At this point, I'm wondering if we should go back to my original proposal of just having a reconciler-level config that will release quota upon eviction - basically causing quota to be released if a workload is inactive OR it's been marked for eviction.
  
  Otherwise, we're having to implement these feature gates for every controller (and every new on in the future).
  
  Thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:19:43Z

- **keps/6143-pod-integration-fast-quota-release/README.md:43** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:19:44Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
```

  Actually,  on the second thought I don't think we need this in the Alpha as a feature gate. This is basically a mirror of the `FastQuotaRelease`. 
  
  I think what we will need eventually, but we can re-evaluate for beta is the configuration mode (enum) like this: `scheduling.quotaReleaseStrategy: OnTermnation` (Fast), or "Delayed", where delayed is the opportunistic mode for integration which could support it. It will need to be reviewed in detail.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:21:45Z

- **keps/6143-pod-integration-fast-quota-release/README.md:34** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:21:45Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
```

  Maybe let's move the scheduling gates to the "Design details" section. I think in the summary it is suffient to say. 
  
  This KEP aims to standardize quota releasing strategy for terminating jobs, and to allow administrators to choose between different strategies.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:22:06Z

- **keps/6143-pod-integration-fast-quota-release/README.md:53** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:22:06Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
```

  I think this is too detailed for summary, let's discuss that in the "design details".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:23:51Z

- **keps/6143-pod-integration-fast-quota-release/README.md:81** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:23:51Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
```

  ```suggestion
    - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
      all Pods have reached a terminal phase, preserving strict quota tracking for
      fixed-size clusters. This mode is "opportunistic" - only for integration which support it 
      by surfacing the required information in the status.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:24:21Z

- **keps/6143-pod-integration-fast-quota-release/README.md:84** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:24:21Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
```

  This is not goal of  the KEP, this is to be discused in the graduation plan or design details.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:25:35Z

- **keps/6143-pod-integration-fast-quota-release/README.md:91** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:25:35Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
```

  ```suggestion
  Introduce two strategies for releasing quota when Jobs are terminating (either due to eviction or finish):
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:26:12Z

- **keps/6143-pod-integration-fast-quota-release/README.md:119** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:26:13Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
```

  Let's list the strategies, but avoid implementation details. This is too many details for the "Proposal" section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:32:36Z

- **keps/6143-pod-integration-fast-quota-release/README.md:187** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:32:36Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
```

  Let's combine both sections by saying "Implementation overview". Both strategies will delegate the implementation to the individual integrations, implementing the `job.IsActive` function.
  
  For the "FastQuotaRelease" the integrations will answer IsActive=false as soon as all the Pods have the `deletionTimestamp`.
  
  For the `DelayedQuotaRelease` strategy the integrations will answer`IsActive=false` once all the Pods are terminal. However, because some Jobs may not be providing this information in status, the implementation will be "opportunistic" and extended gradually as the Jobs implement such functionality.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:33:03Z

- **keps/6143-pod-integration-fast-quota-release/README.md:167** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:33:03Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
```

  Move that under the Implementation overview section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:34:00Z

- **keps/6143-pod-integration-fast-quota-release/README.md:165** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:34:00Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
```

  I think it is here the right place to say that we will introduce `FastQuotaRelease` in Alpha, and backport. We will consider graduation of the feature to Beta in 0.18.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:34:31Z

- **keps/6143-pod-integration-fast-quota-release/README.md:229** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:34:31Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
```

  Let's drop it (as in other comments)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:35:11Z

- **keps/6143-pod-integration-fast-quota-release/README.md:231** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:35:11Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
```

  No need to mention, unit tests are assumed for every feature. e2e are not typically ndeed for Alpha.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:35:31Z

- **keps/6143-pod-integration-fast-quota-release/README.md:232** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:35:31Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
+- Backport to v0.15 and v0.16 with feature gates disabled by default
```

  Let's move this note to "design details" rather than here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:36:29Z

- **keps/6143-pod-integration-fast-quota-release/README.md:238** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:36:29Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
+- Backport to v0.15 and v0.16 with feature gates disabled by default
+
+#### Beta (v0.18)
+
+- Replace feature gates with configuration knobs in the Kueue Configuration API
+  to select the quota release mode
+- `FastQuotaRelease` enabled by default
```

  - re-evaluate the enablement of `FastQuotaRelease` by default.
  
  I think this is a good idea, but @tenzen-y has some reservations. Let's leave this to be re-evaluated for the future releases

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:37:01Z

- **keps/6143-pod-integration-fast-quota-release/README.md:236** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:37:01Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
+- Backport to v0.15 and v0.16 with feature gates disabled by default
+
+#### Beta (v0.18)
+
+- Replace feature gates with configuration knobs in the Kueue Configuration API
```

  ```suggestion
  - Re-evaluate introducing a Configuration API knob (eg. `.scheduling.releaseQuotaStrategy`) to switch between the strategies
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:37:55Z

- **keps/6143-pod-integration-fast-quota-release/README.md:43** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:37:55Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
```

  Actually, let's don't mention the knob name in the main body of the text. Let's move it to the Alternatives section, and let's add a point to graduation criteria for beta to re-evaluate the knob.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:38:22Z

- **keps/6143-pod-integration-fast-quota-release/README.md:249** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:38:23Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
+- Backport to v0.15 and v0.16 with feature gates disabled by default
+
+#### Beta (v0.18)
+
+- Replace feature gates with configuration knobs in the Kueue Configuration API
+  to select the quota release mode
+- `FastQuotaRelease` enabled by default
+- Address feedback from Alpha users
+- Evaluate whether to extend the configuration to other integrations (e.g., Job)
+
+## Implementation History
+
+## Drawbacks
+
+- Introduces two feature gates for a relatively small behavioral change.
+  However, this is warranted because different cluster environments (autoscaled
+  vs. fixed-size) have different requirements for when quota should be released.
+  The two modes give administrators explicit control over this trade-off.
```

  Feature gate is not a drawback. I think we can skip this section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T18:40:02Z

- **keps/6143-pod-integration-fast-quota-release/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-03-05T18:40:03Z
```diff
@@ -0,0 +1,32 @@
+title: Pod Integration Fast Quota Release
```

  ```suggestion
  title: Quota Release Strategy
  ```
  Let's generalize according to the recent discussions, please also adjust the folder location.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-06T18:57:48Z

- **keps/6143-pod-integration-fast-quota-release/README.md:238** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-06T18:57:48Z
```diff
@@ -0,0 +1,262 @@
+# KEP-6143: Pod Integration Fast Quota Release
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [<code>IsActive()</code> modification](#isactive-modification)
+  - [Reconciliation flow](#reconciliation-flow)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces two feature gates to give users control over when the Pod
+integration releases quota:
+
+- `FastQuotaRelease`: Aligns the Pod integration's quota release behavior with
+  other integrations (e.g., batch/v1 Job). When enabled, quota from "plain pods"
+  is released as soon as the pods are terminating (i.e. have a
+  `deletionTimestamp`), rather than waiting for pods to fully terminate.
+- `DelayedQuotaRelease`: Preserves the current Pod integration behavior where
+  quota is only released once all pods have reached a terminal phase
+  (`Succeeded` or `Failed`). This is important for fixed-size clusters where
+  strict quota management is required.
+
+Both feature gates are introduced as Alpha (disabled by default) in v0.17. In
+v0.18, these feature gates will be replaced by configuration knobs in the
+Kueue Configuration API, allowing cluster administrators to select the
+appropriate quota release mode for their environment.
+
+This addresses a consistency gap where the Job integration is considered not active
+as soon as `status.active == 0` on the job, and the Kubernetes job controller considers
+a pod active only if it has no `deletionTimestamp` set. Contrast this with the current
+pod controller, which considers a plain pod as active until the pod is fully terminated.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This obviously creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Provide two quota release modes for the Pod integration:
+  - **Fast quota release** (`FastQuotaRelease`): Release quota as soon as all
+    Pods have a `deletionTimestamp`, aligning with current Job integration
+    behavior.
+  - **Delayed quota release** (`DelayedQuotaRelease`): Release quota only after
+    all Pods have reached a terminal phase, preserving strict quota tracking for
+    fixed-size clusters.
+- Introduce both modes behind feature gates in v0.17 (Alpha, disabled by default).
+- Convert feature gates into configuration knobs in the Kueue Configuration API
+  in v0.18 (Beta).
+
+### Non-Goals
+
+## Proposal
+
+Introduce two mutually exclusive quota release modes for the Pod integration,
+each controlled by a feature gate:
+
+**`FastQuotaRelease` (Alpha, disabled by default)**
+
+Modify the Pod integration's `IsActive()` method to treat Pods with a
+`deletionTimestamp` as inactive. This means:
+
+- A Pod with a `deletionTimestamp` is considered inactive, regardless of its
+  current phase (including `Running`).
+- Once all Pods in a workload are inactive (i.e., all have a
+  `deletionTimestamp`), `IsActive()` returns false.
+- The generic reconciler already uses `IsActive()` to determine when to release
+  quota for evicted workloads, so no changes to the reconciler are needed.
+
+This aligns with how the Kubernetes Job controller handles its `status.active`
+field: a Pod is no longer counted as active as soon as it has a
+`deletionTimestamp`, even if it is still running.
+
+**`DelayedQuotaRelease` (Alpha, disabled by default)**
+
+When enabled, this explicitly preserves and formalizes the current Pod
+integration behavior: quota is only released once all Pods in the workload
+have reached a terminal phase (`Succeeded` or `Failed`). This mode exists to
+provide strict quota tracking for environments (e.g., fixed-size clusters
+without autoscaling) where temporary quota oversubscription is not acceptable.
+
+When neither feature gate is enabled, the current default behavior is retained
+(delayed quota release). When both feature gates are enabled, `FastQuotaRelease`
+takes precedence.
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: `FastQuotaRelease` could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior. Users on fixed-size clusters can use `DelayedQuotaRelease` to
+maintain strict quota tracking.
+
+**Risk**: With `FastQuotaRelease`, if a pod gets "stuck" in a terminating
+state, there may be a discrepancy between the amount of quota available and the
+actual resources available on the cluster. In fixed-size clusters where the sum
+of nominal quotas strictly equals cluster capacity, this can cause temporary
+capacity oversubscription.
+
+**Mitigation**: Users who require strict quota management should use
+`DelayedQuotaRelease`. Additionally, [setup failure recovery](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_failure_recovery/)
+provides a failure recovery mechanism that automatically transitions
+pods into the Failed phase when they are assigned to unreachable nodes and stuck
+terminating.
+
+## Design Details
+
+The change is scoped to the Pod controller in
+`pkg/controller/jobs/pod/pod_controller.go`.
+
+### `IsActive()` modification
+
+When the `FastQuotaRelease` feature gate is enabled, the `IsActive()` method is
+modified to treat any Pod with a `deletionTimestamp` as inactive.
+
+When the `DelayedQuotaRelease` feature gate is enabled (or neither gate is
+enabled), the existing behavior is preserved: a Pod is only considered inactive
+once it has reached a terminal phase (`Succeeded` or `Failed`).
+
+### Reconciliation flow
+
+No changes are needed to the generic reconciler. The existing flow already
+handles this:
+
+1. When a workload is evicted (e.g., by preemption), the reconciler calls
+   `job.IsActive()` to check if the job is still active.
+2. If `IsActive()` returns false, the reconciler clears the workload's
+   admission, releasing quota.
+3. The released quota becomes available for the next scheduling cycle.
+
+### Test Plan
+
+[ ] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+None.
+
+#### Unit tests
+
+- `pkg/controller/jobs/pod`: Test `IsActive()` with each feature gate
+  combination, covering:
+  - Pod with `deletionTimestamp` and `FastQuotaRelease` enabled returns inactive.
+  - Pod with `deletionTimestamp` and `DelayedQuotaRelease` enabled returns
+    active (unless in terminal phase).
+  - Pod with `deletionTimestamp` and neither gate enabled preserves current
+    behavior.
+  - Mixed groups with some terminating and some running Pods.
+  - Single Pod (non-group) behavior.
+
+#### Integration tests
+
+- Test that when a Pod workload is evicted with the feature gate enabled, quota
+  is released as soon as all Pods have a `deletionTimestamp`.
+- Test that preempted workloads are readmitted promptly after the preempted
+  Pods begin terminating.
+
+#### e2e tests
+
+- Test preemption scenario with Pod workloads where a higher-priority workload
+  preempts a lower-priority one and is admitted while the preempted Pods are
+  still terminating.
+
+### Graduation Criteria
+
+#### Alpha (v0.17)
+
+- `FastQuotaRelease` feature gate (disabled by default)
+- `DelayedQuotaRelease` feature gate (disabled by default)
+- Unit and integration tests covering both modes
+- e2e tests for fast quota release preemption scenario
+- Backport to v0.15 and v0.16 with feature gates disabled by default
+
+#### Beta (v0.18)
+
+- Replace feature gates with configuration knobs in the Kueue Configuration API
+  to select the quota release mode
+- `FastQuotaRelease` enabled by default
```

  Thank you, @mimowo, for calling that out.
  TBH, I'm on the fence about which behavior should be defaulted. I don't say that `FastQuotaRelease` is not eligible for default behavior, but I don't block this KEP for default behavior discussion.
  
  So, as @mimowo mentioned, I'd also propose making this commitment more softly.
  In other words, I'd like to postpone the default behavior decision later.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-17T16:06:42Z

I left small nits comment.
otherwise LGTM

Thank you!

- **keps/6143-quota-release-strategy/README.md:107** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T15:59:58Z
```diff
@@ -0,0 +1,222 @@
+# KEP-6143: Quota Release Strategy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+  - [Configuration API knob instead of feature gates](#configuration-api-knob-instead-of-feature-gates)
+<!-- /toc -->
+
+## Summary
+
+This KEP aims to standardize the quota release strategy for terminating jobs.
+Currently, the Pod integration holds quota until pods reach a terminal phase,
+while most other integrations (e.g., batch/v1 Job) release quota as soon as
+pods begin terminating. This inconsistency leads to unnecessarily delayed
+admission and serialized preemption for Pod workloads. As a first step, this
+KEP introduces a `FastQuotaRelease` feature gate for the Pod integration to
+align it with the existing Job integration behavior. In future releases, this
+work may evolve into a configurable quota release strategy (e.g., a
+Configuration API knob) that allows administrators to select between different
+strategies for all integrations.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+  by introducing a `FastQuotaRelease` feature gate (Alpha, disabled by default)
+  that releases quota as soon as all Pods have a `deletionTimestamp`.
+
+### Non-Goals
+
+- Changing quota release behavior for non-Pod integrations (they already
+  release quota when their upstream controller reports no active pods).
+
+## Proposal
+
+Introduce a `FastQuotaRelease` feature gate (Alpha, disabled by default) that
+modifies the Pod integration to release quota as soon as all Pods have a
+`deletionTimestamp`, regardless of whether they are still running. This aligns
+the Pod integration with how the batch/v1 Job integration already behaves,
+since the Kubernetes Job controller does not count terminating pods in
+`status.active`.
+
+When the feature gate is disabled, the current behavior is preserved: quota is
+only released after all Pods have reached a terminal phase (`Succeeded` or
+`Failed`).
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
```

  ```suggestion
  
  #### Node churn could be increased in cluster autoscaling environment.
  ```
  
  
  Could we make the sub section per risks like this?

- **keps/6143-quota-release-strategy/README.md:117** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T16:03:39Z
```diff
@@ -0,0 +1,222 @@
+# KEP-6143: Quota Release Strategy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+  - [Configuration API knob instead of feature gates](#configuration-api-knob-instead-of-feature-gates)
+<!-- /toc -->
+
+## Summary
+
+This KEP aims to standardize the quota release strategy for terminating jobs.
+Currently, the Pod integration holds quota until pods reach a terminal phase,
+while most other integrations (e.g., batch/v1 Job) release quota as soon as
+pods begin terminating. This inconsistency leads to unnecessarily delayed
+admission and serialized preemption for Pod workloads. As a first step, this
+KEP introduces a `FastQuotaRelease` feature gate for the Pod integration to
+align it with the existing Job integration behavior. In future releases, this
+work may evolve into a configurable quota release strategy (e.g., a
+Configuration API knob) that allows administrators to select between different
+strategies for all integrations.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+  by introducing a `FastQuotaRelease` feature gate (Alpha, disabled by default)
+  that releases quota as soon as all Pods have a `deletionTimestamp`.
+
+### Non-Goals
+
+- Changing quota release behavior for non-Pod integrations (they already
+  release quota when their upstream controller reports no active pods).
+
+## Proposal
+
+Introduce a `FastQuotaRelease` feature gate (Alpha, disabled by default) that
+modifies the Pod integration to release quota as soon as all Pods have a
+`deletionTimestamp`, regardless of whether they are still running. This aligns
+the Pod integration with how the batch/v1 Job integration already behaves,
+since the Kubernetes Job controller does not count terminating pods in
+`status.active`.
+
+When the feature gate is disabled, the current behavior is preserved: quota is
+only released after all Pods have reached a terminal phase (`Succeeded` or
+`Failed`).
+
+### User Stories
+
+#### Story 1: Preemption of "plain pod" workloads
+
+As a cluster administrator, I run long-running Pod workloads managed by Kueue
+with a termination grace period of 60 seconds. When a higher-priority workload
+arrives and Kueue preempts my running workload, I want the higher-priority
+workloads to start running as quickly as possible while still honoring
+graceful shutdown periods of preempted pods.
+
+#### Story 2: ClusterQueue migration to new clusters
+
+As a cluster administrator, I migrate ClusterQueues to a new cluster one by one.
+Because burst is free for the initial CQs, these CQs will burst past their
+nominal quota. I want subsequent CQs to be able to reclaim their nominal quota
+within a reasonable amount of time.
+
+### Notes/Constraints/Caveats
+
+### Risks and Mitigations
+
+**Risk**: Fast quota release could cause an increase in node churn if the
+cluster autoscaler is being used. While pods are pending, the cluster
+autoscaler may trigger scale-up even though pods could be terminating on
+existing nodes.
+
+**Mitigation**: This is the same behavior that already exists for the Job
+integration and other integrations, so it's a well established/understood
+behavior.
+
```

  ```suggestion
  
  #### Discrepancy could occur from actual available quota
  ```
  
  Ditto

- **keps/6143-quota-release-strategy/kep.yaml:28** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T16:05:57Z
```diff
@@ -0,0 +1,31 @@
+title: Quota Release Strategy
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+  - @amy
+  - @PBundyra
+  - @tenzen-y
+approvers:
+  - @mimowo
+  - @tenzen-y
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: alpha
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.17"
+  beta: "v0.18"
+  stable:
+
+feature-gates:
+  - name: FastQuotaRelease
```

  ```suggestion
    - name: FastQuotaReleaseInPodIntegration
  ```
  
  I'm wondering if we should name this FG as Pod specific one.
  
  But, no strong view here.
  
  @mimowo Any thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T16:21:40Z

- **keps/6143-quota-release-strategy/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-03-17T16:21:40Z
```diff
@@ -0,0 +1,31 @@
+title: Quota Release Strategy
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+  - @amy
+  - @PBundyra
+  - @tenzen-y
+approvers:
+  - @mimowo
+  - @tenzen-y
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: alpha
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.17"
+  beta: "v0.18"
+  stable:
+
+feature-gates:
+  - name: FastQuotaRelease
```

  Yes, I prefer specific, because the code change is only required for PodIntegration currently

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-17T20:45:05Z

Thank you!
/lgtm
/approve

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-20T03:05:28Z

Sorry for jumping in on this KEP late. I was reviewing https://github.com/kubernetes-sigs/kueue/pull/8992 and stumbled upon this PR.

- **keps/6143-quota-release-strategy/README.md:77** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-20T03:00:16Z
```diff
@@ -0,0 +1,226 @@
+# KEP-6143: Quota Release Strategy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: Preemption of &quot;plain pod&quot; workloads](#story-1-preemption-of-plain-pod-workloads)
+    - [Story 2: ClusterQueue migration to new clusters](#story-2-clusterqueue-migration-to-new-clusters)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Node churn could be increased in cluster autoscaling environment.](#node-churn-could-be-increased-in-cluster-autoscaling-environment)
+    - [Discrepancy could occur from actual available quota](#discrepancy-could-occur-from-actual-available-quota)
+- [Design Details](#design-details)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha (v0.17)](#alpha-v017)
+    - [Beta (v0.18)](#beta-v018)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+  - [Modify the generic reconciler instead of the Pod controller](#modify-the-generic-reconciler-instead-of-the-pod-controller)
+  - [Configuration API knob instead of feature gates](#configuration-api-knob-instead-of-feature-gates)
+<!-- /toc -->
+
+## Summary
+
+This KEP aims to standardize the quota release strategy for terminating jobs.
+Currently, the Pod integration holds quota until pods reach a terminal phase,
+while most other integrations (e.g., batch/v1 Job) release quota as soon as
+pods begin terminating. This inconsistency leads to unnecessarily delayed
+admission and serialized preemption for Pod workloads. As a first step, this
+KEP introduces a `FastQuotaRelease` feature gate for the Pod integration to
+align it with the existing Job integration behavior. In future releases, this
+work may evolve into a configurable quota release strategy (e.g., a
+Configuration API knob) that allows administrators to select between different
+strategies for all integrations.
+
+## Motivation
+
+When Kueue preempts a Pod-based workload, the current Pod integration holds
+onto its quota reservation until all Pods have fully terminated (reached
+`Succeeded` or `Failed` phase). This termination often takes tens of seconds
+or even minutes (and has no practical upper bound, depending on how the pod's
+graceful shutdown period is configured). While the pod is terminating, ALL
+pods in each cluster queue is blocked from triggering further preemptions until
+it's finished because the terminating pod is always each potential preemptor pod's
+ideal preemption target. This creates a large bottleneck for preemption
+that does not occur for preemptions of other types of workloads.
+
+Ultimately, this behavioral inconsistency between integrations leads to:
+- Delayed admission of higher-priority workloads during preemption
+- Serialized preemption, where each ClusterQueue is head-of-line blocking causing
+  the scheduler to wait for one preemption within the ClusterQueue to fully complete
+  before starting the next
+
+### Goals
+
+- Align the Pod integration's quota release behavior with the Job integration
+  by introducing a `FastQuotaRelease` feature gate (Alpha, disabled by default)
+  that releases quota as soon as all Pods have a `deletionTimestamp`.
+
+### Non-Goals
+
+- Changing quota release behavior for non-Pod integrations (they already
+  release quota when their upstream controller reports no active pods).
+
+## Proposal
+
+Introduce a `FastQuotaRelease` feature gate (Alpha, disabled by default) that
+modifies the Pod integration to release quota as soon as all Pods have a
```

  The feature gate only changes behavior for pod groups. The KEP and release note don't make this clear. Users reading this would expect it to affect single pods too, but It doesn't bc single pods already release quota as fast as possible

- **keps/6143-quota-release-strategy/kep.yaml:5** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-20T03:01:00Z
```diff
@@ -0,0 +1,31 @@
+title: Quota Release Strategy
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
```

  It should be implementable

- **keps/6143-quota-release-strategy/kep.yaml:28** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-20T03:02:36Z
```diff
@@ -0,0 +1,31 @@
+title: Quota Release Strategy
+kep-number: 6143
+authors:
+  - "@tkillian"
+status: provisional
+creation-date: 2026-02-17
+reviewers:
+  - @mimowo
+  - @amy
+  - @PBundyra
+  - @tenzen-y
+approvers:
+  - @mimowo
+  - @tenzen-y
+
+see-also:
+  - "/keps/976-plain-pods"
+
+stage: alpha
+latest-milestone: "v0.17"
+
+milestone:
+  alpha: "v0.17"
+  beta: "v0.18"
+  stable:
+
+feature-gates:
+  - name: FastQuotaReleaseInPodIntegration
```

  KEP README says `FastQuotaRelease` throughout. The kep.yaml and code say `FastQuotaReleaseInPodIntegration`. Minor but should be consistent
