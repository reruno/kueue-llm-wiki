# PR #9340: Optimized migration script to allow migration of namespaced resources by namespace

**Summary**: Optimized migration script to allow migration of namespaced resources by namespace

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9340

**Last updated**: 2026-02-19T08:59:44Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/optmize-v1beta2-script` → `main`
- **Created**: 2026-02-18T11:54:13Z
- **Updated**: 2026-02-19T08:59:44Z
- **Merged**: 2026-02-19T08:53:39Z
- **Closed**: 2026-02-19T08:53:39Z
- **Labels**: `release-note`, `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+68 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Optimized migration script to allow migration of namespaced resources by namespace.
- Add --help flag.
- Add --namespace-regex flag.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

Without verbose flag:

```
➜  kueuecopy git:(cleanup/optmize-v1beta2-script) ✗ ./hack/migrate-to-v1beta2.sh
Migrating cohorts.kueue.x-k8s.io...
  → Fetching resources.
  → No cohorts found, skipping.

Migrating workloadpriorityclasses.kueue.x-k8s.io...
  → Fetching resources.
  → No workloadpriorityclasses found, skipping.

Migrating localqueues.kueue.x-k8s.io...
  → Fetching resources in namespace default.
  → Found: 1 object(s) in namespace default.
  → Progress: 100% (1/1)
  → Fetching resources in namespace kube-node-lease.
  → No localqueues found in namespace kube-node-lease, skipping.
  → Fetching resources in namespace kube-public.
  → No localqueues found in namespace kube-public, skipping.
  → Fetching resources in namespace kube-system.
  → No localqueues found in namespace kube-system, skipping.
  → Fetching resources in namespace kueue-system.
  → No localqueues found in namespace kueue-system, skipping.
  → Fetching resources in namespace local-path-storage.
  → No localqueues found in namespace local-path-storage, skipping.

Migrating clusterqueues.kueue.x-k8s.io...
  → Fetching resources.
  → Found: 1 object(s).
  → Progress: 100% (1/1)

Migrating resourceflavors.kueue.x-k8s.io...
  → Fetching resources.
  → Found: 1 object(s).
  → Progress: 100% (1/1)

Migrating admissionchecks.kueue.x-k8s.io...
  → Fetching resources.
  → No admissionchecks found, skipping.

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
  → Fetching resources.
  → No provisioningrequestconfigs found, skipping.

Migrating multikueueclusters.kueue.x-k8s.io...
  → Fetching resources.
  → No multikueueclusters found, skipping.

Migrating multikueueconfigs.kueue.x-k8s.io...
  → Fetching resources.
  → No multikueueconfigs found, skipping.

Migrating topologies.kueue.x-k8s.io...
  → Fetching resources.
  → No topologies found, skipping.

Migrating workloads.kueue.x-k8s.io...
  → Fetching resources in namespace default.
  → Found: 11 object(s) in namespace default.
  → Progress: 100% (11/11)
  → Fetching resources in namespace kube-node-lease.
  → No workloads found in namespace kube-node-lease, skipping.
  → Fetching resources in namespace kube-public.
  → No workloads found in namespace kube-public, skipping.
  → Fetching resources in namespace kube-system.
  → No workloads found in namespace kube-system, skipping.
  → Fetching resources in namespace kueue-system.
  → No workloads found in namespace kueue-system, skipping.
  → Fetching resources in namespace local-path-storage.
  → No workloads found in namespace local-path-storage, skipping.

All migrations finished successfully!
```

With verbose flags:

```
➜  kueuecopy git:(cleanup/optmize-v1beta2-script) ✗ ./hack/migrate-to-v1beta2.sh -v
Migrating cohorts.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "cohorts.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No cohorts found, skipping.

Migrating workloadpriorityclasses.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "workloadpriorityclasses.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No workloadpriorityclasses found, skipping.

Migrating localqueues.kueue.x-k8s.io...
  → Fetching resources in namespace default.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n default
  → Found: 1 object(s) in namespace default.
    └─ kubectl patch localqueues.kueue.x-k8s.io user-queue -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       localqueue.kueue.x-k8s.io/user-queue patched (no change)
  → Progress: 100% (1/1)
  → Fetching resources in namespace kube-node-lease.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-node-lease
  → No localqueues found in namespace kube-node-lease, skipping.
  → Fetching resources in namespace kube-public.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-public
  → No localqueues found in namespace kube-public, skipping.
  → Fetching resources in namespace kube-system.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-system
  → No localqueues found in namespace kube-system, skipping.
  → Fetching resources in namespace kueue-system.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kueue-system
  → No localqueues found in namespace kueue-system, skipping.
  → Fetching resources in namespace local-path-storage.
  → kubectl get "localqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n local-path-storage
  → No localqueues found in namespace local-path-storage, skipping.

Migrating clusterqueues.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "clusterqueues.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → Found: 1 object(s).
    └─ kubectl patch clusterqueues.kueue.x-k8s.io cluster-queue --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       clusterqueue.kueue.x-k8s.io/cluster-queue patched (no change)
  → Progress: 100% (1/1)

Migrating resourceflavors.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "resourceflavors.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → Found: 1 object(s).
    └─ kubectl patch resourceflavors.kueue.x-k8s.io default-flavor --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       resourceflavor.kueue.x-k8s.io/default-flavor patched (no change)
  → Progress: 100% (1/1)

Migrating admissionchecks.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "admissionchecks.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No admissionchecks found, skipping.

Migrating provisioningrequestconfigs.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "provisioningrequestconfigs.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No provisioningrequestconfigs found, skipping.

Migrating multikueueclusters.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "multikueueclusters.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No multikueueclusters found, skipping.

Migrating multikueueconfigs.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "multikueueconfigs.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No multikueueconfigs found, skipping.

Migrating topologies.kueue.x-k8s.io...
  → Fetching resources.
  → kubectl get "topologies.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'
  → No topologies found, skipping.

Migrating workloads.kueue.x-k8s.io...
  → Fetching resources in namespace default.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n default
  → Found: 11 object(s) in namespace default.
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-4rhth-a0cfc -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-4rhth-a0cfc patched (no change)
  → Progress:   9% (1/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-55t6f-48dc6 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-55t6f-48dc6 patched (no change)
  → Progress:  18% (2/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-dg9fs-b6734 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-dg9fs-b6734 patched (no change)
  → Progress:  27% (3/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-dnvhg-24aca -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-dnvhg-24aca patched (no change)
  → Progress:  36% (4/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-ffjf9-498cd -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-ffjf9-498cd patched (no change)
  → Progress:  45% (5/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-fmwb2-54e56 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-fmwb2-54e56 patched (no change)
  → Progress:  55% (6/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-m7249-260ff -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-m7249-260ff patched (no change)
  → Progress:  64% (7/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-rxv5z-6b77a -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-rxv5z-6b77a patched (no change)
  → Progress:  73% (8/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-sn829-68dc5 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-sn829-68dc5 patched (no change)
  → Progress:  82% (9/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-tb46g-b5f8d -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-tb46g-b5f8d patched (no change)
  → Progress:  91% (10/11)
    └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-tqnb9-bb773 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
       workload.kueue.x-k8s.io/job-sample-job-tqnb9-bb773 patched (no change)
  → Progress: 100% (11/11)
  → Fetching resources in namespace kube-node-lease.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-node-lease
  → No workloads found in namespace kube-node-lease, skipping.
  → Fetching resources in namespace kube-public.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-public
  → No workloads found in namespace kube-public, skipping.
  → Fetching resources in namespace kube-system.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kube-system
  → No workloads found in namespace kube-system, skipping.
  → Fetching resources in namespace kueue-system.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n kueue-system
  → No workloads found in namespace kueue-system, skipping.
  → Fetching resources in namespace local-path-storage.
  → kubectl get "workloads.kueue.x-k8s.io" -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' -n local-path-storage
  → No workloads found in namespace local-path-storage, skipping.

All migrations finished successfully!
```

Help flag:
```
➜  kueuecopy git:(cleanup/optmize-v1beta2-script) ./hack/migrate-to-v1beta2.sh -h    
Usage: migrate-to-v1beta2.sh [OPTIONS]

Migrates Kueue resources to v1beta2.

Options:
  --dry-run                Use --dry-run=client (default dry-run mode)
  --dry-run=client         Explicit client-side dry run
  --dry-run=server         Server-side dry run (if supported by the command)
  -v, --verbose            Enable verbose output
  --namespace-regex REGEX  Filter namespaces using this extended regular expression
                           Examples:
                             --namespace-regex "prod|staging"
                             --namespace-regex "^team-"
                             --namespace-regex "^(?!kube-system|default).*$"
  -h, --help               Show this help message and exit
```

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
V1beta2: Optimized migration script to allow migration of namespaced resources by namespace.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-18T11:54:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8dbf05e529c97b066d7053eeefe341c07514a515 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6996c67bf5c860000899e9b1 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T12:01:12Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-18T12:17:16Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9340/pull-kueue-test-integration-baseline-main/2024091523188002816
please open flake issue

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T07:35:52Z

/test pull-kueue-verify-main
checking if this is a flake

### Comment by [@kshalot](https://github.com/kshalot) — 2026-02-19T08:13:33Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T08:13:40Z

LGTM label has been added.  <details>Git tree hash: 57aa541ab50eb679a9d547a91759f3cd5ab906f7</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T08:31:10Z

/lgtm
/approve
Thank you folks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T08:31:17Z

LGTM label has been added.  <details>Git tree hash: f48e191fc4b93d28b964786b0bbd2008275891fc</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T08:31:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9340#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9340#issuecomment-3925586696" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T08:42:51Z

/retest

Opened issue https://github.com/kubernetes-sigs/kueue/issues/9357.

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-18T13:40:47Z

- **hack/migrate-to-v1beta2.sh:81** by [@kshalot](https://github.com/kshalot) — 2026-02-18T13:38:52Z
```diff
@@ -68,48 +70,65 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources"
+  if [[ -n "$ns" ]]; then
+    printf " in namespace %s" "${ns}"
+  fi
+  printf ".\n"
```

  Nit: I remember there is this neat shell syntax:
  
  ```suggestion
    printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
  ```
  
  It's a POSIX standard so it would work for compliant shells.
  
  I'm not saying that being terse is necessarily better, but it's something to consider. Especially since this "add more if ns set" logic is quite common in this PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T13:48:37Z

- **hack/migrate-to-v1beta2.sh:81** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T13:48:37Z
```diff
@@ -68,48 +70,65 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources"
+  if [[ -n "$ns" ]]; then
+    printf " in namespace %s" "${ns}"
+  fi
+  printf ".\n"
```

  It works! Thank you!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-18T14:28:49Z

- **hack/migrate-to-v1beta2.sh:201** by [@mimowo](https://github.com/mimowo) — 2026-02-18T14:28:49Z
```diff
@@ -149,14 +156,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
     printf "  → Progress: %3d%% (%d/%d)\n" "${percent}" "${patched}" "${total}"
   fi
 
   if [ "$patched" -ne "$total" ]; then
     failures=$(( total - patched ))
     echo "  → Error: only ${patched}/${total} object(s) patched successfully (${failures} failure(s))!"
   fi
+}
+
+for kind in "${kinds[@]}"; do
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  namespaced=$(kubectl api-resources --api-group=kueue.x-k8s.io -o json | jq -r --arg k "$kind" '.resources[] | select(.name==$k) | .namespaced')
+
+  if $namespaced; then
```

  Could you also add an option to filter namespaces by regex? It could be helpful to shard the processing into multiple runs in separate consoles. the script is taking long at scale

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T15:15:14Z

- **hack/migrate-to-v1beta2.sh:201** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T15:15:14Z
```diff
@@ -149,14 +156,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
     printf "  → Progress: %3d%% (%d/%d)\n" "${percent}" "${patched}" "${total}"
   fi
 
   if [ "$patched" -ne "$total" ]; then
     failures=$(( total - patched ))
     echo "  → Error: only ${patched}/${total} object(s) patched successfully (${failures} failure(s))!"
   fi
+}
+
+for kind in "${kinds[@]}"; do
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  namespaced=$(kubectl api-resources --api-group=kueue.x-k8s.io -o json | jq -r --arg k "$kind" '.resources[] | select(.name==$k) | .namespaced')
+
+  if $namespaced; then
```

  Done

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-18T15:20:40Z

- **hack/migrate-to-v1beta2.sh:83** by [@kshalot](https://github.com/kshalot) — 2026-02-18T15:10:29Z
```diff
@@ -68,48 +70,53 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
+
+  local resources_cmd
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}'"
+  if [ -n "$ns" ]; then
+    resources_cmd="$resources_cmd -n ${ns}"
+  fi
```

  We can use the same (now familiar) syntax here:
  ```suggestion
    local resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}' ${ns:+-n \"$ns\"}"
  ```
  
  Although it is pretty dense, I'm not going to lie. I'm fine with leaving it as is.

- **hack/migrate-to-v1beta2.sh:184** by [@kshalot](https://github.com/kshalot) — 2026-02-18T15:20:03Z
```diff
@@ -149,14 +165,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
```

  Isn't the verbose check flipped? I'd expect it to print stuff (progress in this case) if verbose is true.
  
  Similarly in the rest of the file.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T15:25:15Z

- **hack/migrate-to-v1beta2.sh:184** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T15:25:15Z
```diff
@@ -149,14 +165,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
```

  If verbose true we see progress like this:
  
  ```
  → Found: 131 object(s) in namespace default.
      └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-2pltx-a4c36 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
         workload.kueue.x-k8s.io/job-sample-job-2pltx-a4c36 patched (no change)
      └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-2z455-19df3 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
         workload.kueue.x-k8s.io/job-sample-job-2z455-19df3 patched (no change)
      └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-4bcq7-0e626 -n default --type=merge -p {"apiVersion":"kueue.x-k8s.io/v1beta2"}
  ```
  
  Progress is not useful to be honest because everytime we rewrite it with :
  ```
   workload.kueue.x-k8s.io/job-sample-job-2z455-19df3 patched (no change)
      └─ kubectl patch workloads.kueue.x-k8s.io job-sample-job-4bcq7-0e626 -n default --type=merge -p 
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T15:28:34Z

- **hack/migrate-to-v1beta2.sh:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T15:28:34Z
```diff
@@ -68,48 +70,53 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
+
+  local resources_cmd
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}'"
+  if [ -n "$ns" ]; then
+    resources_cmd="$resources_cmd -n ${ns}"
+  fi
```

  Our linter doesn't think so 😁
  
  ```
  In ./hack/migrate-to-v1beta2.sh line 122:
    local resources=$(eval "${resources_cmd}")
          ^-------^ SC2155 (warning): Declare and assign separately to avoid masking return values.
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-18T15:37:26Z

- **hack/migrate-to-v1beta2.sh:83** by [@kshalot](https://github.com/kshalot) — 2026-02-18T15:37:26Z
```diff
@@ -68,48 +70,53 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
+
+  local resources_cmd
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}'"
+  if [ -n "$ns" ]; then
+    resources_cmd="$resources_cmd -n ${ns}"
+  fi
```

  Yeah, we could keep the "declaration" on a separate line. But not worth it IMO.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T16:18:31Z

- **hack/migrate-to-v1beta2.sh:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T16:18:31Z
```diff
@@ -68,48 +70,53 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
+
+  local resources_cmd
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}'"
+  if [ -n "$ns" ]; then
+    resources_cmd="$resources_cmd -n ${ns}"
+  fi
```

  I can commit these changes for the current line, but unfortunately I can’t do the same for the other one.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-18T16:20:53Z

- **hack/migrate-to-v1beta2.sh:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-18T16:20:52Z
```diff
@@ -68,48 +70,53 @@ patch_payload='{"apiVersion":"kueue.x-k8s.io/v1beta2"}'
 
 exit_code=0
 
-for kind in "${kinds[@]}"; do
-  echo "Migrating $kind.kueue.x-k8s.io..."
+function apply_patches() {
+  local kind=$1
+  local ns=$2
+
+  printf "  → Fetching resources%s.\n" "${ns:+ in namespace $ns}"
+
+  local resources_cmd
+  resources_cmd="kubectl get \"${kind}.kueue.x-k8s.io\" -o jsonpath='{range .items[*]}{.metadata.name}{\"\n\"}{end}'"
+  if [ -n "$ns" ]; then
+    resources_cmd="$resources_cmd -n ${ns}"
+  fi
```

  > Although it is pretty dense, I'm not going to lie. I'm fine with leaving it as is.
  
  Sorry, just to clarify I'm talking about this one.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-18T20:26:36Z

- **hack/migrate-to-v1beta2.sh:36** by [@kshalot](https://github.com/kshalot) — 2026-02-18T20:03:17Z
```diff
@@ -22,6 +22,27 @@ set -o pipefail
 
 DRY_RUN=""
 VERBOSE=false
+NAMESPACE_REGEX=""
+
+show_help() {
+  cat << EOF
+Usage: $(basename "$0") [OPTIONS]
+
+Migrates Kueue resources to v1beta2.
+
+Options:
+  --dry-run                Use --dry-run=client (default dry-run mode)
+  --dry-run=client         Explicit client-side dry run
+  --dry-run=server         Server-side dry run (if supported by the command)
```

  Nit: for clarity, I'd consider consolidating those lines into one:
  ```suggestion
    --dry-run[=STRATEGY]     Perform a dry run. STRATEGY can be 'client' (default) for client-side dry run 
                             or 'server' for server-side dry run (if supported by the command).
  ```

- **hack/migrate-to-v1beta2.sh:42** by [@kshalot](https://github.com/kshalot) — 2026-02-18T20:07:17Z
```diff
@@ -22,6 +22,27 @@ set -o pipefail
 
 DRY_RUN=""
 VERBOSE=false
+NAMESPACE_REGEX=""
+
+show_help() {
+  cat << EOF
+Usage: $(basename "$0") [OPTIONS]
+
+Migrates Kueue resources to v1beta2.
+
+Options:
+  --dry-run                Use --dry-run=client (default dry-run mode)
+  --dry-run=client         Explicit client-side dry run
+  --dry-run=server         Server-side dry run (if supported by the command)
+  -v, --verbose            Enable verbose output
+  --namespace-regex REGEX  Filter namespaces using this extended regular expression
+                           Examples:
+                             --namespace-regex "prod|staging"
+                             --namespace-regex "^team-"
+                             --namespace-regex "^(?!kube-system|default).*$"
```

  Nit: Wouldn't expecting an `=` sign be better for consistency (I'm looking at `--dry-run=...`)?
  
  i.e. `--namespace-regex=REGEX`

- **hack/migrate-to-v1beta2.sh:201** by [@kshalot](https://github.com/kshalot) — 2026-02-18T20:26:28Z
```diff
@@ -149,14 +185,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
     printf "  → Progress: %3d%% (%d/%d)\n" "${percent}" "${patched}" "${total}"
   fi
 
   if [ "$patched" -ne "$total" ]; then
     failures=$(( total - patched ))
     echo "  → Error: only ${patched}/${total} object(s) patched successfully (${failures} failure(s))!"
   fi
+}
+
+for kind in "${kinds[@]}"; do
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  namespaced=$(kubectl api-resources --api-group=kueue.x-k8s.io -o json | jq -r --arg k "$kind" '.resources[] | select(.name==$k) | .namespaced')
```

  I want to point out that this will only work for `kubectl` versions `>=1.34`, because `-o json` was added in `1.34` (https://github.com/kubernetes/kubernetes/pull/132604).
  
  Technically, `1.33` is still supported. Maybe we should handle this just in case someone has an older version?
  Maybe sth like this would work?
  ```sh
  kubectl get --raw "/apis/kueue.x-k8s.io/v1beta1"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T06:06:44Z

- **hack/migrate-to-v1beta2.sh:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T06:06:44Z
```diff
@@ -22,6 +22,27 @@ set -o pipefail
 
 DRY_RUN=""
 VERBOSE=false
+NAMESPACE_REGEX=""
+
+show_help() {
+  cat << EOF
+Usage: $(basename "$0") [OPTIONS]
+
+Migrates Kueue resources to v1beta2.
+
+Options:
+  --dry-run                Use --dry-run=client (default dry-run mode)
+  --dry-run=client         Explicit client-side dry run
+  --dry-run=server         Server-side dry run (if supported by the command)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T06:06:53Z

- **hack/migrate-to-v1beta2.sh:42** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T06:06:53Z
```diff
@@ -22,6 +22,27 @@ set -o pipefail
 
 DRY_RUN=""
 VERBOSE=false
+NAMESPACE_REGEX=""
+
+show_help() {
+  cat << EOF
+Usage: $(basename "$0") [OPTIONS]
+
+Migrates Kueue resources to v1beta2.
+
+Options:
+  --dry-run                Use --dry-run=client (default dry-run mode)
+  --dry-run=client         Explicit client-side dry run
+  --dry-run=server         Server-side dry run (if supported by the command)
+  -v, --verbose            Enable verbose output
+  --namespace-regex REGEX  Filter namespaces using this extended regular expression
+                           Examples:
+                             --namespace-regex "prod|staging"
+                             --namespace-regex "^team-"
+                             --namespace-regex "^(?!kube-system|default).*$"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T06:18:09Z

- **hack/migrate-to-v1beta2.sh:201** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T06:18:09Z
```diff
@@ -149,14 +185,28 @@ for kind in "${kinds[@]}"; do
     fi
   done <<< "$filtered"
 
-  if [[ -t 1 ]]; then
+  if [[ "${VERBOSE}" != "true" && -t 1 ]]; then
     printf "  → Progress: %3d%% (%d/%d)\n" "${percent}" "${patched}" "${total}"
   fi
 
   if [ "$patched" -ne "$total" ]; then
     failures=$(( total - patched ))
     echo "  → Error: only ${patched}/${total} object(s) patched successfully (${failures} failure(s))!"
   fi
+}
+
+for kind in "${kinds[@]}"; do
+  echo "Migrating $kind.kueue.x-k8s.io..."
+
+  namespaced=$(kubectl api-resources --api-group=kueue.x-k8s.io -o json | jq -r --arg k "$kind" '.resources[] | select(.name==$k) | .namespaced')
```

  Wow! This is good point. I didn't know about it. Thank you! Done.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-02-19T08:13:24Z

- **hack/migrate-to-v1beta2.sh:37** by [@kshalot](https://github.com/kshalot) — 2026-02-19T08:08:05Z
```diff
@@ -22,6 +22,26 @@ set -o pipefail
 
 DRY_RUN=""
 VERBOSE=false
+NAMESPACE_REGEX=""
+
+show_help() {
+  cat << EOF
+Usage: $(basename "$0") [OPTIONS]
+
+Migrates Kueue resources to v1beta2.
+
+Options:
+  --dry-run[=STRATEGY]     Perform a dry run. STRATEGY can be 'client' (default) for client-side dry run
+                           or 'server' for server-side dry run.
+  -v, --verbose            Enable verbose output
+  --namespace-regex REGEX  Filter namespaces using this extended regular expression
```

  ```suggestion
    --namespace-regex=REGEX  Filter namespaces using this extended regular expression
  ```
