# PR #9356: Extend resource transformation integration tests.

**Summary**: Extend resource transformation integration tests.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9356

**Last updated**: 2026-02-19T13:18:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/add-tests-to-resource-transformation` → `main`
- **Created**: 2026-02-19T08:19:06Z
- **Updated**: 2026-02-19T13:18:40Z
- **Merged**: 2026-02-19T13:17:45Z
- **Closed**: 2026-02-19T13:17:45Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+153 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Extend resource transformation integration tests.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-19T08:19:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 99fe6fcdcfba556e5ead556a64aaa1a4356de27f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6996fda1947de30008c580c6 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T08:19:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9356#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T09:39:41Z

leaving lgtm to @mszadkow, feel free to cherrypick

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T12:17:14Z

Looks much better!
I hope all of our tests could be described so well, thanks!!

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T12:17:19Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T12:17:27Z

LGTM label has been added.  <details>Git tree hash: d11a4ee4936493407ec7d33506d67afb188317ea</details>

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-02-19T08:45:31Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:310** by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T08:45:31Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
+		})
+
+		ginkgo.By("Checking that the new workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.When("preemption enabled", func() {
+		const (
+			highPriorityClassName = "high"
+			highPriority          = 1000
+			lowPriorityClassName  = "low"
+			lowPriority           = 100
+		)
+
+		ginkgo.BeforeEach(func() {
+			tmpl.WorkloadPriorityClassRef(highPriorityClassName).Priority(highPriority)
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
```

  I don't see how this is tested here.
  What caused the new workload to become admitted, preemption or resourceTransformation ?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-02-19T08:49:20Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:290** by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T08:49:20Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
```

  Why we have to finish workload in order to allow for admission of new workload?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T08:49:33Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:310** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T08:49:33Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
+		})
+
+		ginkgo.By("Checking that the new workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.When("preemption enabled", func() {
+		const (
+			highPriorityClassName = "high"
+			highPriority          = 1000
+			lowPriorityClassName  = "low"
+			lowPriority           = 100
+		)
+
+		ginkgo.BeforeEach(func() {
+			tmpl.WorkloadPriorityClassRef(highPriorityClassName).Priority(highPriority)
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
```

  Here we are checking the possibility of preemption with resource transformation.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T08:51:45Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:290** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T08:51:45Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
```

  Due to we don't have enough resources for new workload. We should finish to release cpu_credits.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-02-19T09:13:27Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:290** by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T09:13:26Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
```

  I think it would be super cool if you left a comment pointing to which scenario is a base for those tests.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-02-19T09:19:34Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:310** by [@mszadkow](https://github.com/mszadkow) — 2026-02-19T09:19:34Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
+		})
+
+		ginkgo.By("Checking that the new workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.When("preemption enabled", func() {
+		const (
+			highPriorityClassName = "high"
+			highPriority          = 1000
+			lowPriorityClassName  = "low"
+			lowPriority           = 100
+		)
+
+		ginkgo.BeforeEach(func() {
+			tmpl.WorkloadPriorityClassRef(highPriorityClassName).Priority(highPriority)
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
```

  So we are blocked by credits, then we preempt workload and we are no longer blocked...
  What do we test here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T12:10:41Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:290** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T12:10:40Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-02-19T12:13:40Z

- **test/integration/singlecluster/scheduler/resourcetransformations/resource_transformations_test.go:310** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-19T12:13:40Z
```diff
@@ -176,3 +178,150 @@ var _ = ginkgo.Describe("Resource Transformations", ginkgo.Ordered, ginkgo.Conti
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Retain Resource Transformation (cpu -> cpu_credits)", ginkgo.Ordered, func() {
+	const (
+		cpuCredits       = "cpu_credits"
+		expectedAdmitted = 2
+	)
+
+	var (
+		ns           *corev1.Namespace
+		onDemand     *kueue.ResourceFlavor
+		spot         *kueue.ResourceFlavor
+		credits      *kueue.ResourceFlavor
+		clusterQueue *kueue.ClusterQueue
+		localQueue   *kueue.LocalQueue
+		wl           *kueue.Workload
+
+		admittedWorkloads []*kueue.Workload
+
+		tmpl *utiltestingapi.WorkloadWrapper
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup([]config.ResourceTransformation{{
+			Input:    corev1.ResourceCPU,
+			Strategy: ptr.To(config.Retain),
+			Outputs:  corev1.ResourceList{cpuCredits: resource.MustParse("1")},
+		}}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "tas-")
+
+		onDemand = utiltestingapi.MakeResourceFlavor("on-demand").
+			NodeLabel("node-type", "on-demand").
+			Obj()
+		util.MustCreate(ctx, k8sClient, onDemand)
+
+		spot = utiltestingapi.MakeResourceFlavor("spot").
+			NodeLabel("node-type", "spot").
+			Obj()
+		util.MustCreate(ctx, k8sClient, spot)
+
+		credits = utiltestingapi.MakeResourceFlavor("credits").Obj()
+		util.MustCreate(ctx, k8sClient, credits)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("team-cluster-queue").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(onDemand.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+				*utiltestingapi.MakeFlavorQuotas(spot.Name).Resource(corev1.ResourceCPU, "9").Obj(),
+			).
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas(credits.Name).Resource(cpuCredits, "8").Obj()).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+		localQueue = utiltestingapi.MakeLocalQueue("team-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+		util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+		admittedWorkloads = make([]*kueue.Workload, 0, expectedAdmitted)
+
+		tmpl = utiltestingapi.MakeWorkload("tmpl", ns.Name).
+			Queue(kueue.LocalQueueName(localQueue.Name)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "3"). // ( 8 credits / 3 ) -> 2 workloads fit
+				Obj())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, onDemand, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, spot, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, credits, true)
+	})
+
+	// We need to wait for the workload template in the preemption case.
+	// That’s why we use JustBeforeEach – it runs after all BeforeEach blocks.
+	ginkgo.JustBeforeEach(func() {
+		ginkgo.By(fmt.Sprintf("Creating %d workloads", expectedAdmitted), func() {
+			for i := range expectedAdmitted {
+				wl := tmpl.Clone().Name(fmt.Sprintf("wl-%d", i+1)).Obj()
+				util.MustCreate(ctx, k8sClient, wl)
+				admittedWorkloads = append(admittedWorkloads, wl)
+			}
+		})
+
+		ginkgo.By("Checking that all workloads are admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWorkloads...)
+		})
+
+		wl = tmpl.Clone().Name("wl").Obj()
+
+		ginkgo.By("Creating one more workload", func() {
+			util.MustCreate(ctx, k8sClient, wl)
+		})
+
+		ginkgo.By("Checking that the new workload is pending", func() {
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
+		ginkgo.By("Finishing one of the admitted workloads", func() {
+			util.FinishWorkloads(ctx, k8sClient, admittedWorkloads[0])
+		})
+
+		ginkgo.By("Checking that the new workload is admitted", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+		})
+	})
+
+	ginkgo.When("preemption enabled", func() {
+		const (
+			highPriorityClassName = "high"
+			highPriority          = 1000
+			lowPriorityClassName  = "low"
+			lowPriority           = 100
+		)
+
+		ginkgo.BeforeEach(func() {
+			tmpl.WorkloadPriorityClassRef(highPriorityClassName).Priority(highPriority)
+		})
+
+		ginkgo.It("transforms cpu requests and admits workloads up to transformed capacity", func() {
```

  1. Does the transformation work at all?
  2. Can we admit workloads after finish based on cpu_credits?
  3. Can we admit workloads on preemption based on cpu_credits?
