# PR #9371: feat: add local queue metrics config

**Summary**: feat: add local queue metrics config

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9371

**Last updated**: 2026-03-24T11:28:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/localqueuemetrics-beta` → `main`
- **Created**: 2026-02-19T13:24:22Z
- **Updated**: 2026-03-24T11:28:50Z
- **Merged**: 2026-03-24T10:36:20Z
- **Closed**: 2026-03-24T10:36:20Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 40   **+993 / -61**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add configuration for the localqueue metrics visibility

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8830

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: added LocalQueueMetrics configuration to control which metrics should be displayed.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T13:24:25Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-19T13:24:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 612721920dd3071556911d3b14f272e7d298af01 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69bda852390b2f000828b683 |

### Comment by [@kannon92](https://github.com/kannon92) — 2026-02-19T14:13:03Z

/assign

I'd like to do the first round of reviews for this.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-02-19T14:13:15Z

Please fix the merge conflicts when you get a chance also.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-02-19T14:13:38Z

And sorry I realize this is draft. let me know when its ready for review.

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-03T10:47:43Z

I think it is ready for review as the only major change I plan to introduce is custom config e2e test which will check that the label selector is working properly, but I do not expect major changes to the implementation itself

@kannon92 @mbobrovskyi PTAL

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-03T13:54:31Z

Enabling featuregate by default caused tests without explicit configuration to fail so will take a look how to pass default config to all tests

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-03T15:33:58Z

/retest-required

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-06T09:24:08Z

Added two PRs to help split this one into smaller chunks: #9700 and #9701

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T09:44:44Z

cc @olekzabl @kshalot ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-20T16:20:37Z

LGTM overall, I don't have a request to change the handling of defaulting as summarized in https://github.com/kubernetes-sigs/kueue/pull/9371#discussion_r2966700227. My question was mostly exploratory / surprise on the first reading, but the code is ok.

Thank you 👍 
/approve
Leaving final lgtm to @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T16:20:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9371#issuecomment-4099364596" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9371#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-23T07:56:12Z

/retest-required

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-23T08:14:48Z

/retest-required

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-24T10:00:17Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-24T10:00:26Z

LGTM label has been added.  <details>Git tree hash: b11979a0d137e74bea0af1d3a5c3f98e454f882b</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-19T14:10:16Z

- **apis/config/v1beta2/configuration_types.go:188** by [@kannon92](https://github.com/kannon92) — 2026-02-19T14:10:16Z
```diff
@@ -176,6 +176,22 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// LocalQueueMetrics is a configuration that provides LocalQueue metrics options.
+	// +optional
+	LocalQueueMetrics *LocalQueueMetrics `json:"localQueueMetrics,omitempty"`
+}
+
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
+type LocalQueueMetrics struct {
+	// Enable is a knob to allow metrics to be exposed for local queues. Defaults to false.
```

  I think we are trying to phase out Enable for configs. Setting this field should make it obvious that we want this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-19T14:11:25Z

- **hack/tools/go.sum:153** by [@kannon92](https://github.com/kannon92) — 2026-02-19T14:11:25Z
```diff
@@ -150,6 +150,7 @@ github.com/Masterminds/vcs v1.13.3 h1:IIA2aBdXvfbIM+yl/eTnL4hb1XwdpvuQLglAix1gwe
 github.com/Masterminds/vcs v1.13.3/go.mod h1:TiE7xuEjl1N4j016moRd6vezp6e6Lz23gypeXfzXeW8=
 github.com/MirrexOne/unqueryvet v1.5.3 h1:LpT3rsH+IY3cQddWF9bg4C7jsbASdGnrOSofY8IPEiw=
 github.com/MirrexOne/unqueryvet v1.5.3/go.mod h1:fs9Zq6eh1LRIhsDIsxf9PONVUjYdFHdtkHIgZdJnyPU=
+github.com/NYTimes/gziphandler v1.1.1/go.mod h1:n/CVRwUEOgIxrgPvAQhUUr9oeUtvrhMomdKFjzJNB0c=
```

  This also appeared in one of my PRs and I couldn't really explain how it got there. Any idea why this was modified?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-02-19T14:11:56Z

- **apis/config/v1beta2/configuration_types.go:188** by [@mykysha](https://github.com/mykysha) — 2026-02-19T14:11:56Z
```diff
@@ -176,6 +176,22 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// LocalQueueMetrics is a configuration that provides LocalQueue metrics options.
+	// +optional
+	LocalQueueMetrics *LocalQueueMetrics `json:"localQueueMetrics,omitempty"`
+}
+
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
+type LocalQueueMetrics struct {
+	// Enable is a knob to allow metrics to be exposed for local queues. Defaults to false.
```

  Makes sense, will adjust accordingly

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-02-19T14:13:35Z

- **hack/tools/go.sum:153** by [@mykysha](https://github.com/mykysha) — 2026-02-19T14:13:34Z
```diff
@@ -150,6 +150,7 @@ github.com/Masterminds/vcs v1.13.3 h1:IIA2aBdXvfbIM+yl/eTnL4hb1XwdpvuQLglAix1gwe
 github.com/Masterminds/vcs v1.13.3/go.mod h1:TiE7xuEjl1N4j016moRd6vezp6e6Lz23gypeXfzXeW8=
 github.com/MirrexOne/unqueryvet v1.5.3 h1:LpT3rsH+IY3cQddWF9bg4C7jsbASdGnrOSofY8IPEiw=
 github.com/MirrexOne/unqueryvet v1.5.3/go.mod h1:fs9Zq6eh1LRIhsDIsxf9PONVUjYdFHdtkHIgZdJnyPU=
+github.com/NYTimes/gziphandler v1.1.1/go.mod h1:n/CVRwUEOgIxrgPvAQhUUr9oeUtvrhMomdKFjzJNB0c=
```

  I think it appeared here after running `make generate`, will look deeper to find what exactly caused it

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:23:04Z

- **apis/config/v1beta2/configuration_types.go:216** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:23:04Z
```diff
@@ -176,6 +176,21 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// LocalQueueMetrics is a configuration that provides LocalQueue metrics options.
+	// +optional
+	LocalQueueMetrics *LocalQueueMetrics `json:"localQueueMetrics,omitempty"`
+}
+
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
+type LocalQueueMetrics struct {
```

  Can we add it to v1beta1 as well?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:30:28Z

- **pkg/workload/workload.go:1478** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:30:28Z
```diff
@@ -1457,18 +1459,25 @@ func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder,
 		// This is an extra safeguard for access to `wl.Status.Admission`.
 		// This function is expected to be called only for workload which have
 		// Admission.
-		log := log.FromContext(ctx)
 		log.V(3).Info("WARNING: unexpected eviction of workload without status.Admission", "workload", klog.KObj(wl))
 		return nil
 	}
-	reportEvictedWorkload(recorder, wl, wl.Status.Admission.ClusterQueue, reason, msg, underlyingCause, tracker)
+	lqRef := metrics.LQRefFromWorkload(wl)
+	lq, err := metrics.LQFromRef(c, lqRef)
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+	}
+	reportEvictedWorkload(recorder, wl, lq, wl.Status.Admission.ClusterQueue, reason, msg, underlyingCause, lqMetrics, tracker)
 	if reportWorkloadEvictedOnce {
 		metrics.ReportEvictedWorkloadsOnce(wl.Status.Admission.ClusterQueue, reason, string(underlyingCause), PriorityClassName(wl), tracker)
 	}
 	return nil
 }
 
-func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, msg string, clock clock.Clock, tracker *roletracker.RoleTracker) error {
+func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, msg string,
+	clock clock.Clock, lqMetrics *metrics.LocalQueueMetricsConfig, tracker *roletracker.RoleTracker) error {
```

  I think it's time to add options for this function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:34:14Z

I'm wondering if we can split to graduation and configuration PRs. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:37:05Z

- **pkg/controller/core/localqueue_controller.go:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:37:05Z
```diff
@@ -226,7 +236,10 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker)
+		lq := e.Object
```

  Can we avoid this variable?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:40:50Z

- **pkg/metrics/localqueue.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:40:50Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
+	lq := &kueue.LocalQueue{}
+	if err := k8sClient.Get(context.Background(), client.ObjectKey{Name: string(lqRef.Name), Namespace: lqRef.Namespace}, lq); err != nil {
```

  Can we propagate ctx?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:41:06Z

- **pkg/metrics/localqueue.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:41:06Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
```

  Should we move this to utils?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-04T12:41:55Z

- **pkg/controller/core/localqueue_controller.go:239** by [@mykysha](https://github.com/mykysha) — 2026-03-04T12:41:55Z
```diff
@@ -226,7 +236,10 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker)
+		lq := e.Object
```

  We can, but we still need to access the labels so it would like
  ```go
  if r.lqMetrics.ShouldExposeLocalQueueMetrics(e.Object.GetLabels()) {
      recordLocalQueueUsageMetrics(e.Object, r.roleTracker)
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:42:07Z

- **pkg/metrics/localqueue.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:42:06Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
```

  Actually do we need this? Can we catch it from cache?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-04T12:42:59Z

- **pkg/metrics/localqueue.go:77** by [@mykysha](https://github.com/mykysha) — 2026-03-04T12:42:59Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
+	lq := &kueue.LocalQueue{}
+	if err := k8sClient.Get(context.Background(), client.ObjectKey{Name: string(lqRef.Name), Namespace: lqRef.Namespace}, lq); err != nil {
```

  We can, but in many cases we will be creating it the same way just a layer higher

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:43:09Z

- **pkg/controller/core/localqueue_controller.go:239** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:43:09Z
```diff
@@ -226,7 +236,10 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker)
+		lq := e.Object
```

  sgtm

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T12:44:26Z

- **pkg/metrics/localqueue.go:77** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T12:44:26Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
+	lq := &kueue.LocalQueue{}
+	if err := k8sClient.Get(context.Background(), client.ObjectKey{Name: string(lqRef.Name), Namespace: lqRef.Namespace}, lq); err != nil {
```

  But not everywhere, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-06T16:18:43Z

- **pkg/scheduler/preemption/preemption.go:204** by [@mimowo](https://github.com/mimowo) — 2026-03-06T16:18:43Z
```diff
@@ -197,7 +201,7 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 		message := preemptionMessage(preemptor.Obj, target.Reason, preemptorPath, preempteePath)
 		wlCopy := target.WorkloadInfo.Obj.DeepCopy()
 		err := workload.Evict(
-			ctx, p.client, p.recorder, wlCopy, kueue.WorkloadEvictedByPreemption, message, "", p.clock, p.roleTracker,
+			ctx, p.client, p.recorder, wlCopy, kueue.WorkloadEvictedByPreemption, message, "", p.clock, metrics.NewDefaultLocalQueueMetricsConfig(), p.roleTracker,
```

  Hold on, why are we using `metrics.NewDefaultLocalQueueMetricsConfig()`. I think this helper should be only for tests. For production use we should rely on the config.
  
  Same for multiple other places, this is very confusing.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:18:42Z

- **pkg/metrics/localqueue.go:71** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:18:42Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
```

  ```suggestion
      if cfg.QueueSelector == nil {
  		return cfg.Enabled
  	}
  	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:20:29Z

- **pkg/cache/scheduler/clusterqueue.go:523** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:20:29Z
```diff
@@ -519,7 +520,9 @@ func (c *clusterQueue) updateWorkloadUsage(log logr.Logger, wi *workload.Info, o
 			lq.admittedWorkloads += op.asSignedOne()
 		}
 		if features.Enabled(features.LocalQueueMetrics) {
-			lq.reportActiveWorkloads(c.roleTracker)
+			if c.lqMetrics.ShouldExposeLocalQueueMetrics(lq.labels) {
```

  ```suggestion
  			if c.lqMetrics != nil && c.lqMetrics.ShouldExposeLocalQueueMetrics(lq.labels) {
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:21:32Z

- **pkg/controller/core/workload_controller.go:492** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:21:32Z
```diff
@@ -475,18 +484,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			metrics.AdmittedWorkload(cqName, priorityClassName, queuedWaitTime, r.roleTracker)
 			metrics.ReportAdmissionChecksWaitTime(cqName, priorityClassName, quotaReservedWaitTime, r.roleTracker)
 			if features.Enabled(features.LocalQueueMetrics) {
-				metrics.LocalQueueAdmittedWorkload(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					queuedWaitTime,
-					r.roleTracker,
-				)
-				metrics.ReportLocalQueueAdmissionChecksWaitTime(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					quotaReservedWaitTime,
-					r.roleTracker,
-				)
+				lqRef := metrics.LQRefFromWorkload(&wl)
+				lq, err := metrics.LQFromRef(r.client, lqRef)
+				if err != nil {
+					log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+				}
+				if lq != nil && r.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
```

  ```suggestion
  				if lq != nil && lqMetrics != nil && lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:25:44Z

- **pkg/controller/core/localqueue_controller.go:273** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:25:44Z
```diff
@@ -254,7 +267,10 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	log.V(2).Info("Queue update event")
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		updateLocalQueueResourceMetrics(e.ObjectNew, r.roleTracker)
+		lq := e.ObjectNew
+		if r.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
+			updateLocalQueueResourceMetrics(lq, r.roleTracker)
+		}
```

  ```suggestion
  		oldShouldExpose := r.lqMetrics.ShouldExposeLocalQueueMetrics(e.ObjectOld.GetLabels())
  	newShouldExpose := r.lqMetrics.ShouldExposeLocalQueueMetrics(e.ObjectNew.GetLabels())
  	if oldShouldExpose && !newShouldExpose {
  		metrics.ClearLocalQueueResourceMetrics(localQueueReferenceFromLocalQueue(e.ObjectNew))
  	} else if newShouldExpose {
  		updateLocalQueueResourceMetrics(e.ObjectNew, r.roleTracker)
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:26:55Z

- **pkg/scheduler/preemption/preemption.go:204** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:26:55Z
```diff
@@ -197,7 +201,7 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 		message := preemptionMessage(preemptor.Obj, target.Reason, preemptorPath, preempteePath)
 		wlCopy := target.WorkloadInfo.Obj.DeepCopy()
 		err := workload.Evict(
-			ctx, p.client, p.recorder, wlCopy, kueue.WorkloadEvictedByPreemption, message, "", p.clock, p.roleTracker,
+			ctx, p.client, p.recorder, wlCopy, kueue.WorkloadEvictedByPreemption, message, "", p.clock, metrics.NewDefaultLocalQueueMetricsConfig(), p.roleTracker,
```

  ```suggestion
  			ctx, p.client, p.recorder, wlCopy, kueue.WorkloadEvictedByPreemption, message, "", p.clock, p.lqMetrics, p.roleTracker,
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T12:57:36Z

- **pkg/controller/core/workload_controller.go:492** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T12:49:11Z
```diff
@@ -475,18 +484,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			metrics.AdmittedWorkload(cqName, priorityClassName, queuedWaitTime, r.roleTracker)
 			metrics.ReportAdmissionChecksWaitTime(cqName, priorityClassName, quotaReservedWaitTime, r.roleTracker)
 			if features.Enabled(features.LocalQueueMetrics) {
-				metrics.LocalQueueAdmittedWorkload(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					queuedWaitTime,
-					r.roleTracker,
-				)
-				metrics.ReportLocalQueueAdmissionChecksWaitTime(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					quotaReservedWaitTime,
-					r.roleTracker,
-				)
+				lqRef := metrics.LQRefFromWorkload(&wl)
+				lq, err := metrics.LQFromRef(r.client, lqRef)
+				if err != nil {
+					log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+				}
+				if lq != nil && r.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
```

  This sequence of checks is repeated several times.
  How about introducing another helper, which would take a workload and do the full check (including checking the FG)? Just workload -> bool.

- **pkg/scheduler/scheduler.go:897** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T12:54:24Z
```diff
@@ -858,28 +869,46 @@ func (s *Scheduler) recordQuotaReservationMetrics(newWorkload, originalWorkload
 	priorityClassName := workload.PriorityClassName(newWorkload)
 	metrics.QuotaReservedWorkload(admission.ClusterQueue, priorityClassName, waitTime, s.roleTracker)
 	if features.Enabled(features.LocalQueueMetrics) {
-		metrics.LocalQueueQuotaReservedWorkload(metrics.LQRefFromWorkload(newWorkload), priorityClassName, waitTime, s.roleTracker)
+		lqRef := metrics.LQRefFromWorkload(newWorkload)
+		lq, err := metrics.LQFromRef(s.client, lqRef)
+		if err != nil {
+			log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(newWorkload.Namespace, string(newWorkload.Spec.QueueName)))
+		}
+		if lq != nil && s.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
+			metrics.LocalQueueQuotaReservedWorkload(lqRef, priorityClassName, waitTime, s.roleTracker)
+		}
 	}
 }
 
 // recordWorkloadAdmissionEvents records metrics and events for workload admission
-func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration) {
+func (s *Scheduler) recordWorkloadAdmissionEvents(log logr.Logger, newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration) {
 	if !workload.IsAdmitted(newWorkload) || workload.HasUnhealthyNodes(originalWorkload) {
 		return
 	}
 
 	s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s", admission.ClusterQueue)
 
 	priorityClassName := workload.PriorityClassName(newWorkload)
+
+	lqRef := metrics.LQRefFromWorkload(newWorkload)
+	lq, err := metrics.LQFromRef(s.client, lqRef)
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(newWorkload.Namespace, string(newWorkload.Spec.QueueName)))
+	}
```

  (Here the sequence of checks is split into 2 parts - but I think it's another place where a common "workload -> bool" helper would fit)

- **test/e2e/customconfigs/metrics_test.go:233** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T12:57:03Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package customconfigse2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("LocalQueue metrics", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+
+		metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+
+		curlContainerName string
+		curlPod           *corev1.Pod
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			cfg.Metrics.LocalQueueMetrics = &configapi.LocalQueueMetrics{
+				Enable: true,
+				LocalQueueSelector: &metav1.LabelSelector{
+					MatchLabels: map[string]string{
+						"metrics-test": "true",
+					},
+				},
+			}
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-customconfig-lq-metrics-")
+
+		resourceFlavor = utiltestingapi.MakeResourceFlavor("test-flavor-" + ns.Name).Obj()
+		util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+		metricsReaderClusterRoleBinding = &rbacv1.ClusterRoleBinding{
+			ObjectMeta: metav1.ObjectMeta{Name: "lq-metrics-reader-rolebinding-" + ns.Name},
+			Subjects: []rbacv1.Subject{
+				{
+					Kind:      "ServiceAccount",
+					Name:      serviceAccountName,
+					Namespace: kueueNS,
+				},
+			},
+			RoleRef: rbacv1.RoleRef{
+				APIGroup: rbacv1.GroupName,
+				Kind:     "ClusterRole",
+				Name:     metricsReaderClusterRoleName,
+			},
+		}
+		util.MustCreate(ctx, k8sClient, metricsReaderClusterRoleBinding)
+
+		curlPod = testingjobspod.MakePod("curl-metrics-"+ns.Name, kueueNS).
+			ServiceAccountName(serviceAccountName).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			TerminationGracePeriod(1).
+			Obj()
+		util.MustCreate(ctx, k8sClient, curlPod)
+
+		ginkgo.By("Waiting for the curl-metrics pod to run.", func() {
+			util.WaitForPodRunning(ctx, k8sClient, curlPod)
+		})
+
+		curlContainerName = curlPod.Spec.Containers[0].Name
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, metricsReaderClusterRoleBinding, true)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, curlPod, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("workload is admitted to a lq with matching labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Label("metrics-test", "true").
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, workload)
+
+			metrics := [][]string{
+				{"kueue_local_queue_pending_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that default metrics are available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, metrics)
+			})
+
+			ginkgo.By("deleting the cluster queue", func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			})
+
+			deletedMetrics := [][]string{
+				// LocalQueueMetrics
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should have been deleted are no longer available", func() {
+				util.ExpectMetricsNotToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, deletedMetrics)
+			})
+
+			notDeletedMetrics := [][]string{
+				// Cleared metrics with 0 value
+				{"kueue_local_queue_pending_workloads", "active", "0", ns.Name, localQueue.Name},
+				{"kueue_local_queue_pending_workloads", "inadmissible", "0", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should not have been deleted are still available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, notDeletedMetrics)
+			})
+		})
+	})
+
+	ginkgo.When("workload is admitted to a lq without labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
```

  Did you mean "are _not_ available"?

- **test/e2e/customconfigs/metrics_test.go:247** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T12:57:12Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package customconfigse2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("LocalQueue metrics", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+
+		metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+
+		curlContainerName string
+		curlPod           *corev1.Pod
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			cfg.Metrics.LocalQueueMetrics = &configapi.LocalQueueMetrics{
+				Enable: true,
+				LocalQueueSelector: &metav1.LabelSelector{
+					MatchLabels: map[string]string{
+						"metrics-test": "true",
+					},
+				},
+			}
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-customconfig-lq-metrics-")
+
+		resourceFlavor = utiltestingapi.MakeResourceFlavor("test-flavor-" + ns.Name).Obj()
+		util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+		metricsReaderClusterRoleBinding = &rbacv1.ClusterRoleBinding{
+			ObjectMeta: metav1.ObjectMeta{Name: "lq-metrics-reader-rolebinding-" + ns.Name},
+			Subjects: []rbacv1.Subject{
+				{
+					Kind:      "ServiceAccount",
+					Name:      serviceAccountName,
+					Namespace: kueueNS,
+				},
+			},
+			RoleRef: rbacv1.RoleRef{
+				APIGroup: rbacv1.GroupName,
+				Kind:     "ClusterRole",
+				Name:     metricsReaderClusterRoleName,
+			},
+		}
+		util.MustCreate(ctx, k8sClient, metricsReaderClusterRoleBinding)
+
+		curlPod = testingjobspod.MakePod("curl-metrics-"+ns.Name, kueueNS).
+			ServiceAccountName(serviceAccountName).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			TerminationGracePeriod(1).
+			Obj()
+		util.MustCreate(ctx, k8sClient, curlPod)
+
+		ginkgo.By("Waiting for the curl-metrics pod to run.", func() {
+			util.WaitForPodRunning(ctx, k8sClient, curlPod)
+		})
+
+		curlContainerName = curlPod.Spec.Containers[0].Name
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, metricsReaderClusterRoleBinding, true)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, curlPod, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("workload is admitted to a lq with matching labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Label("metrics-test", "true").
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, workload)
+
+			metrics := [][]string{
+				{"kueue_local_queue_pending_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that default metrics are available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, metrics)
+			})
+
+			ginkgo.By("deleting the cluster queue", func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			})
+
+			deletedMetrics := [][]string{
+				// LocalQueueMetrics
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should have been deleted are no longer available", func() {
+				util.ExpectMetricsNotToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, deletedMetrics)
+			})
+
+			notDeletedMetrics := [][]string{
+				// Cleared metrics with 0 value
+				{"kueue_local_queue_pending_workloads", "active", "0", ns.Name, localQueue.Name},
+				{"kueue_local_queue_pending_workloads", "inadmissible", "0", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should not have been deleted are still available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, notDeletedMetrics)
+			})
+		})
+	})
+
+	ginkgo.When("workload is admitted to a lq without labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, workload)
+
+			metrics := [][]string{
+				{"kueue_local_queue_pending_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that default metrics are available", func() {
```

  Did you mean "are _not_ available"?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:53:53Z

- **apis/config/v1beta2/configuration_types.go:216** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:53:53Z
```diff
@@ -176,6 +176,21 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// LocalQueueMetrics is a configuration that provides LocalQueue metrics options.
+	// +optional
+	LocalQueueMetrics *LocalQueueMetrics `json:"localQueueMetrics,omitempty"`
+}
+
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
+type LocalQueueMetrics struct {
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:55:23Z

- **pkg/workload/workload.go:1478** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:55:23Z
```diff
@@ -1457,18 +1459,25 @@ func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder,
 		// This is an extra safeguard for access to `wl.Status.Admission`.
 		// This function is expected to be called only for workload which have
 		// Admission.
-		log := log.FromContext(ctx)
 		log.V(3).Info("WARNING: unexpected eviction of workload without status.Admission", "workload", klog.KObj(wl))
 		return nil
 	}
-	reportEvictedWorkload(recorder, wl, wl.Status.Admission.ClusterQueue, reason, msg, underlyingCause, tracker)
+	lqRef := metrics.LQRefFromWorkload(wl)
+	lq, err := metrics.LQFromRef(c, lqRef)
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+	}
+	reportEvictedWorkload(recorder, wl, lq, wl.Status.Admission.ClusterQueue, reason, msg, underlyingCause, lqMetrics, tracker)
 	if reportWorkloadEvictedOnce {
 		metrics.ReportEvictedWorkloadsOnce(wl.Status.Admission.ClusterQueue, reason, string(underlyingCause), PriorityClassName(wl), tracker)
 	}
 	return nil
 }
 
-func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, msg string, clock clock.Clock, tracker *roletracker.RoleTracker) error {
+func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, msg string,
+	clock clock.Clock, lqMetrics *metrics.LocalQueueMetricsConfig, tracker *roletracker.RoleTracker) error {
```

  Will be done in #9701

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:55:34Z

- **pkg/controller/core/localqueue_controller.go:239** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:55:34Z
```diff
@@ -226,7 +236,10 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker)
+		lq := e.Object
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:56:29Z

- **pkg/metrics/localqueue.go:75** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:56:29Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
```

  We can use cache, and I have updated the cases where we can directly access cache now. After the work on #9943 is finished, we can remove this helper altogether

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:59:02Z

- **pkg/metrics/localqueue.go:77** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:59:02Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
+	lq := &kueue.LocalQueue{}
+	if err := k8sClient.Get(context.Background(), client.ObjectKey{Name: string(lqRef.Name), Namespace: lqRef.Namespace}, lq); err != nil {
```

  After updating workload Finish function it was possible to propagate ctx everywhere

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T13:59:59Z

- **test/e2e/customconfigs/metrics_test.go:233** by [@mykysha](https://github.com/mykysha) — 2026-03-19T13:59:58Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package customconfigse2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("LocalQueue metrics", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+
+		metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+
+		curlContainerName string
+		curlPod           *corev1.Pod
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			cfg.Metrics.LocalQueueMetrics = &configapi.LocalQueueMetrics{
+				Enable: true,
+				LocalQueueSelector: &metav1.LabelSelector{
+					MatchLabels: map[string]string{
+						"metrics-test": "true",
+					},
+				},
+			}
+		})
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-customconfig-lq-metrics-")
+
+		resourceFlavor = utiltestingapi.MakeResourceFlavor("test-flavor-" + ns.Name).Obj()
+		util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+		metricsReaderClusterRoleBinding = &rbacv1.ClusterRoleBinding{
+			ObjectMeta: metav1.ObjectMeta{Name: "lq-metrics-reader-rolebinding-" + ns.Name},
+			Subjects: []rbacv1.Subject{
+				{
+					Kind:      "ServiceAccount",
+					Name:      serviceAccountName,
+					Namespace: kueueNS,
+				},
+			},
+			RoleRef: rbacv1.RoleRef{
+				APIGroup: rbacv1.GroupName,
+				Kind:     "ClusterRole",
+				Name:     metricsReaderClusterRoleName,
+			},
+		}
+		util.MustCreate(ctx, k8sClient, metricsReaderClusterRoleBinding)
+
+		curlPod = testingjobspod.MakePod("curl-metrics-"+ns.Name, kueueNS).
+			ServiceAccountName(serviceAccountName).
+			Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+			TerminationGracePeriod(1).
+			Obj()
+		util.MustCreate(ctx, k8sClient, curlPod)
+
+		ginkgo.By("Waiting for the curl-metrics pod to run.", func() {
+			util.WaitForPodRunning(ctx, k8sClient, curlPod)
+		})
+
+		curlContainerName = curlPod.Spec.Containers[0].Name
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, metricsReaderClusterRoleBinding, true)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, curlPod, true, util.LongTimeout)
+		util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
+	})
+
+	ginkgo.When("workload is admitted to a lq with matching labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Label("metrics-test", "true").
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, workload)
+
+			metrics := [][]string{
+				{"kueue_local_queue_pending_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that default metrics are available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, metrics)
+			})
+
+			ginkgo.By("deleting the cluster queue", func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			})
+
+			deletedMetrics := [][]string{
+				// LocalQueueMetrics
+				{"kueue_local_queue_reserving_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_active_workloads", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_workloads_total", ns.Name, localQueue.Name},
+				{"kueue_local_queue_quota_reserved_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_admitted_workloads_total", ns.Name, localQueue.Name, ""},
+				{"kueue_local_queue_admission_wait_time_seconds", ns.Name, localQueue.Name},
+				{"kueue_local_queue_status", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should have been deleted are no longer available", func() {
+				util.ExpectMetricsNotToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, deletedMetrics)
+			})
+
+			notDeletedMetrics := [][]string{
+				// Cleared metrics with 0 value
+				{"kueue_local_queue_pending_workloads", "active", "0", ns.Name, localQueue.Name},
+				{"kueue_local_queue_pending_workloads", "inadmissible", "0", ns.Name, localQueue.Name},
+			}
+
+			ginkgo.By("checking that metrics that should not have been deleted are still available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, notDeletedMetrics)
+			})
+		})
+	})
+
+	ginkgo.When("workload is admitted to a lq without labels", func() {
+		var (
+			clusterQueue *kueue.ClusterQueue
+			localQueue   *kueue.LocalQueue
+			workload     *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueue = utiltestingapi.MakeClusterQueue("").
+				GeneratedName("test-cq-").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+
+			workload = utiltestingapi.MakeWorkload("test-workload", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				PodSets(
+					*utiltestingapi.MakePodSet("ps1", 1).Obj(),
+				).
+				RequestAndLimit(corev1.ResourceCPU, "1").
+				Obj()
+			util.MustCreate(ctx, k8sClient, workload)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, workload, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("should ensure the localqueue metrics are available", func() {
```

  Thanks for catching, fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:05:52Z

- **pkg/controller/admissionchecks/multikueue/workload.go:819** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:05:53Z
```diff
@@ -816,8 +816,7 @@ func (w *wlReconciler) Generic(_ event.GenericEvent) bool {
 
 func newWlReconciler(c client.Client, helper *admissioncheck.MultiKueueStoreHelper, cRec *clustersReconciler, origin string,
 	recorder record.EventRecorder, workerLostTimeout, eventsBatchPeriod time.Duration,
-	adapters map[string]jobframework.MultiKueueAdapter, dispatcherName string, roleTracker *roletracker.RoleTracker,
-	options ...Option,
+	adapters map[string]jobframework.MultiKueueAdapter, dispatcherName string, roleTracker *roletracker.RoleTracker, options ...Option,
```

  Please revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:07:16Z

- **pkg/controller/core/localqueue_controller.go:262** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:07:16Z
```diff
@@ -250,7 +259,9 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker, r.customLabels)
+		if r.lqMetrics.ShouldExposeLocalQueueMetrics(e.Object.GetLabels()) {
```

  Please merge these two if statements into one.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:07:38Z

- **pkg/controller/core/localqueue_controller.go:307** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:07:38Z
```diff
@@ -293,7 +304,9 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		updateLocalQueueResourceMetrics(e.ObjectNew, r.roleTracker, r.customLabels)
+		if r.lqMetrics.ShouldExposeLocalQueueMetrics(e.ObjectNew.GetLabels()) {
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:08:05Z

- **pkg/controller/core/localqueue_controller.go:597** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:08:05Z
```diff
@@ -581,10 +594,12 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 			ObservedGeneration: queue.Generation,
 		})
 		if features.Enabled(features.LocalQueueMetrics) {
-			metrics.ReportLocalQueueStatus(metrics.LocalQueueReference{
-				Name:      kueue.LocalQueueName(queue.Name),
-				Namespace: queue.Namespace,
-			}, conditionStatus, r.customLabels.LQGet(utilqueue.Key(queue)), r.roleTracker)
+			if r.lqMetrics.ShouldExposeLocalQueueMetrics(queue.GetLabels()) {
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:09:37Z

- **pkg/controller/core/localqueue_controller.go:597** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:09:37Z
```diff
@@ -581,10 +594,12 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 			ObservedGeneration: queue.Generation,
 		})
 		if features.Enabled(features.LocalQueueMetrics) {
-			metrics.ReportLocalQueueStatus(metrics.LocalQueueReference{
-				Name:      kueue.LocalQueueName(queue.Name),
-				Namespace: queue.Namespace,
-			}, conditionStatus, r.customLabels.LQGet(utilqueue.Key(queue)), r.roleTracker)
+			if r.lqMetrics.ShouldExposeLocalQueueMetrics(queue.GetLabels()) {
```

  Or, actually, I think we can move this check if feature gate enabled inside ShouldExposeLocalQueueMetrics function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:18:21Z

- **pkg/controller/tas/node_controller_test.go:860** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:18:22Z
```diff
@@ -856,7 +857,7 @@ func TestNodeFailureReconciler(t *testing.T) {
 			}
 			cl := clientBuilder.Build()
 			recorder := &utiltesting.EventRecorder{}
-			r := newNodeReconciler(cl, recorder, nil, nil)
+			r := newNodeReconciler(cl, recorder, nil, metrics.NewDefaultLocalQueueMetricsConfig(), nil)
```

  It would be great to have options for this. But I think we can do it in follow-up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:18:44Z

- **pkg/features/kube_features.go:384** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:18:44Z
```diff
@@ -381,6 +381,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	LocalQueueMetrics: {
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Beta},
```

  ```suggestion
  		{Version: version.MustParse("0.17"), Default: true, PreRelease: featuregate.Beta},
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:19:33Z

- **pkg/metrics/localqueue.go:36** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:19:33Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
```

  Please add a method IsEnabled() and check inside it whether the feature gate is enabled as well. In the future, after graduation, we will remove it only inside this function.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-19T14:19:49Z

- **pkg/features/kube_features.go:384** by [@mykysha](https://github.com/mykysha) — 2026-03-19T14:19:49Z
```diff
@@ -381,6 +381,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	LocalQueueMetrics: {
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Beta},
```

  I think this one needs to be removed from the PR altogether, as it is addressed in #9700

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:24:16Z

- **pkg/metrics/localqueue.go:71** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:24:16Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	if cfg == nil {
+		return NewDefaultLocalQueueMetricsConfig()
+	}
+
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled: cfg.Enable,
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+	if err != nil {
+		return nil
+	}
+	if cfg.LocalQueueSelector == nil {
+		q = labels.Everything()
+	}
+
+	lqMetricsConfig.QueueSelector = q
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
```

  Is it possible nil?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:29:28Z

- **pkg/metrics/localqueue.go:78** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:29:28Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"context"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
+	return &LocalQueueMetricsConfig{
+		Enabled:       true,
+		QueueSelector: labels.Everything(),
+	}
+}
+
+func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMetricsConfig {
+	lqMetricsConfig := &LocalQueueMetricsConfig{
+		Enabled:       cfg.Enable,
+		QueueSelector: labels.Everything(),
+	}
+
+	if !cfg.Enable {
+		return lqMetricsConfig
+	}
+
+	if cfg.LocalQueueSelector != nil {
+		q, err := metav1.LabelSelectorAsSelector(cfg.LocalQueueSelector)
+		if err != nil {
+			return nil
+		}
+
+		lqMetricsConfig.QueueSelector = q
+	}
+
+	return lqMetricsConfig
+}
+
+// ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
+// based on the global configuration.
+func (cfg LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+}
+
+// LQFromRef retrieves a LocalQueue object from the Kubernetes API based on the provided reference.
+func LQFromRef(ctx context.Context, k8sClient client.Client, lqRef LocalQueueReference) (*kueue.LocalQueue, error) {
+	lq := &kueue.LocalQueue{}
+	if err := k8sClient.Get(ctx, client.ObjectKey{Name: string(lqRef.Name), Namespace: lqRef.Namespace}, lq); err != nil {
+		return nil, err
+	}
+
+	return lq, nil
+}
```

  I don’t think this is good. Can we get it from the cache?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:32:58Z

- **pkg/scheduler/scheduler.go:889** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:32:58Z
```diff
@@ -868,15 +878,15 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 }
 
 // recordWorkloadAdmissionMetrics records metrics and events for workload admission process
-func (s *Scheduler) recordWorkloadAdmissionMetrics(newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, consideredFlavors string) {
+func (s *Scheduler) recordWorkloadAdmissionMetrics(ctx context.Context, log logr.Logger, newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, consideredFlavors string) {
 	waitTime := workload.QueuedWaitTime(newWorkload, s.clock)
 
-	s.recordQuotaReservationMetrics(newWorkload, originalWorkload, admission, waitTime, consideredFlavors)
-	s.recordWorkloadAdmissionEvents(newWorkload, originalWorkload, admission, waitTime)
+	s.recordQuotaReservationMetrics(ctx, log, newWorkload, originalWorkload, admission, waitTime, consideredFlavors)
+	s.recordWorkloadAdmissionEvents(ctx, log, newWorkload, originalWorkload, admission, waitTime)
 }
 
 // recordQuotaReservationMetrics records metrics and events for quota reservation
-func (s *Scheduler) recordQuotaReservationMetrics(newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration, consideredFlavors string) {
+func (s *Scheduler) recordQuotaReservationMetrics(ctx context.Context, log logr.Logger, newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration, consideredFlavors string) {
```

  ```suggestion
  func (s *Scheduler) recordQuotaReservationMetrics(log logr.Logger, newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration, consideredFlavors string) {
  ```
  Do we need ctx?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:33:18Z

- **pkg/scheduler/scheduler.go:916** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:33:19Z
```diff
@@ -892,12 +902,18 @@ func (s *Scheduler) recordQuotaReservationMetrics(newWorkload, originalWorkload
 	metrics.QuotaReservedWorkload(admission.ClusterQueue, priorityClassName, waitTime, s.customLabels.CQGet(admission.ClusterQueue), s.roleTracker)
 	if features.Enabled(features.LocalQueueMetrics) {
 		lqRef := metrics.LQRefFromWorkload(newWorkload)
-		metrics.LocalQueueQuotaReservedWorkload(lqRef, priorityClassName, waitTime, s.customLabels.LQGet(utilqueue.KeyFromWorkload(newWorkload)), s.roleTracker)
+		lq, err := s.cache.GetCacheLocalQueue(admission.ClusterQueue, utilqueue.NewLocalQueueReference(lqRef.Namespace, lqRef.Name))
+		if err != nil {
+			log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(newWorkload.Namespace, string(newWorkload.Spec.QueueName)))
+		}
+		if lq != nil && s.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
+			metrics.LocalQueueQuotaReservedWorkload(lqRef, priorityClassName, waitTime, s.customLabels.LQGet(utilqueue.KeyFromWorkload(newWorkload)), s.roleTracker)
+		}
 	}
 }
 
 // recordWorkloadAdmissionEvents records metrics and events for workload admission
-func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration) {
+func (s *Scheduler) recordWorkloadAdmissionEvents(ctx context.Context, log logr.Logger, newWorkload, originalWorkload *kueue.Workload, admission *kueue.Admission, waitTime time.Duration) {
```

  Also here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T14:43:09Z

- **pkg/features/kube_features.go:384** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T14:43:09Z
```diff
@@ -381,6 +381,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	LocalQueueMetrics: {
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Beta},
```

  Yes, please, revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T15:28:51Z

- **apis/config/v1beta1/configuration_types.go:222** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T15:28:52Z
```diff
@@ -214,6 +218,17 @@ type ControllerMetricsCustomLabel struct {
 	SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
 }
 
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
```

  ```suggestion
  // If left empty, then metrics will expose it for all local queues across namespaces.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T15:29:53Z

- **apis/config/v1beta1/configuration_types.go:228** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T15:29:53Z
```diff
@@ -214,6 +218,17 @@ type ControllerMetricsCustomLabel struct {
 	SourceAnnotationKey string `json:"sourceAnnotationKey,omitempty"`
 }
 
+// LocalQueueMetrics defines the configuration options for local queue metrics.
+// If left empty, then metrics will expose for all local queues across namespaces.
+type LocalQueueMetrics struct {
+	// Enable is a knob to allow metrics to be exposed for local queues. Defaults to true.
+	// +optional
+	Enable bool `json:"enable,omitempty"`
+	// LocalQueueSelector can be used to choose the local queues that need metrics to be collected.
```

  ```suggestion
  	
  	// LocalQueueSelector can be used to choose the local queues that need metrics to be collected.
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T15:33:25Z

- **cmd/kueue/main.go:558** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T15:33:25Z
```diff
@@ -544,7 +554,8 @@ func setupProbeEndpoints(mgr ctrl.Manager, certsReady <-chan struct{}) error {
 	return nil
 }
 
-func setupScheduler(mgr ctrl.Manager, cCache *schdcache.Cache, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker, preemptionExpectations *expectations.Store, customLabels *metrics.CustomLabels) error {
+func setupScheduler(mgr ctrl.Manager, cCache *schdcache.Cache, queues *qcache.Manager, cfg *configapi.Configuration,
+	roleTracker *roletracker.RoleTracker, preemptionExpectations *expectations.Store, customLabels *metrics.CustomLabels, lqMetrics *metrics.LocalQueueMetricsConfig) error {
```

  ```suggestion
  	roleTracker *roletracker.RoleTracker, preemptionExpectations *expectations.Store, customLabels *metrics.CustomLabels, 
  	lqMetrics *metrics.LocalQueueMetricsConfig) error {
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T15:47:33Z

- **test/e2e/customconfigs/metrics_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T15:47:33Z
  I'm wondering if we can test it in integration tests.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-20T10:04:17Z

- **pkg/controller/core/workload_controller.go:492** by [@mykysha](https://github.com/mykysha) — 2026-03-20T10:04:17Z
```diff
@@ -475,18 +484,25 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			metrics.AdmittedWorkload(cqName, priorityClassName, queuedWaitTime, r.roleTracker)
 			metrics.ReportAdmissionChecksWaitTime(cqName, priorityClassName, quotaReservedWaitTime, r.roleTracker)
 			if features.Enabled(features.LocalQueueMetrics) {
-				metrics.LocalQueueAdmittedWorkload(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					queuedWaitTime,
-					r.roleTracker,
-				)
-				metrics.ReportLocalQueueAdmissionChecksWaitTime(
-					metrics.LQRefFromWorkload(&wl),
-					priorityClassName,
-					quotaReservedWaitTime,
-					r.roleTracker,
-				)
+				lqRef := metrics.LQRefFromWorkload(&wl)
+				lq, err := metrics.LQFromRef(r.client, lqRef)
+				if err != nil {
+					log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+				}
+				if lq != nil && r.lqMetrics.ShouldExposeLocalQueueMetrics(lq.GetLabels()) {
```

  Introduced the helper, and it now fetches the lq from cache

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-20T10:04:48Z

- **pkg/controller/core/localqueue_controller.go:597** by [@mykysha](https://github.com/mykysha) — 2026-03-20T10:04:47Z
```diff
@@ -581,10 +594,12 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 			ObservedGeneration: queue.Generation,
 		})
 		if features.Enabled(features.LocalQueueMetrics) {
-			metrics.ReportLocalQueueStatus(metrics.LocalQueueReference{
-				Name:      kueue.LocalQueueName(queue.Name),
-				Namespace: queue.Namespace,
-			}, conditionStatus, r.customLabels.LQGet(utilqueue.Key(queue)), r.roleTracker)
+			if r.lqMetrics.ShouldExposeLocalQueueMetrics(queue.GetLabels()) {
```

  Moved inside the check

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T12:16:49Z

- **pkg/cache/queue/manager.go:183** by [@mimowo](https://github.com/mimowo) — 2026-03-20T12:16:49Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  Why using default, this feels unsafe. For example admin may disable the metrics in config map, but this controller will use default values (enabled).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T12:18:26Z

- **pkg/metrics/localqueue.go:32** by [@mimowo](https://github.com/mimowo) — 2026-03-20T12:18:26Z
```diff
@@ -0,0 +1,69 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/labels"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+type LocalQueueMetricsConfig struct {
+	Enabled       bool
+	QueueSelector labels.Selector
+}
+
+func NewDefaultLocalQueueMetricsConfig() *LocalQueueMetricsConfig {
```

  This function should be only used for testing, let's rename it as NewDefaultLocalQueueMetricsForTest() and move to the testing package. Similarly as we have NewManagerForIntegrationTest, etc

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-20T12:19:00Z

- **pkg/cache/queue/manager.go:183** by [@mykysha](https://github.com/mykysha) — 2026-03-20T12:19:00Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  It is just the default value if no WIthLocalQueueMetrics option was provided. The option should overwrite this default value

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-20T12:20:55Z

- **pkg/cache/queue/manager.go:183** by [@mykysha](https://github.com/mykysha) — 2026-03-20T12:20:54Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  If we remove the default value in config, in every integration test we would need to provide the default config which is introducing lots of repeated code in setup_tests, so feels like both approaches have drawbacks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T12:23:36Z

- **pkg/cache/scheduler/localqueue.go:39** by [@mimowo](https://github.com/mimowo) — 2026-03-20T12:23:36Z
```diff
@@ -36,6 +36,7 @@ type LocalQueue struct {
 	admittedUsage      resources.FlavorResourceQuantities
 	// values extracted from K8s labels/annotations, used as custom Prometheus metric labels
 	customMetricLabelValues []string
+	labels                  map[string]string
```

  I'm wondering - do we ever mutate them? I don't see any update operation, maybe `addLocalQueue` does that but it is not obvious. 
  
  Do we have an integration test for a scenario when a user updates the set of Labels for LQ and it starts to match selector?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-20T14:57:34Z

- **pkg/cache/scheduler/localqueue.go:39** by [@mykysha](https://github.com/mykysha) — 2026-03-20T14:57:34Z
```diff
@@ -36,6 +36,7 @@ type LocalQueue struct {
 	admittedUsage      resources.FlavorResourceQuantities
 	// values extracted from K8s labels/annotations, used as custom Prometheus metric labels
 	customMetricLabelValues []string
+	labels                  map[string]string
```

  Added integration test, as well as this case there

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T15:09:31Z

- **pkg/cache/queue/manager.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T15:09:31Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  I propose to keep as nil by default. In that case NewDefaultLocalQueueMetricsConfig() no need at all.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T15:12:29Z

- **pkg/workload/workload.go:1578** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T15:12:29Z
```diff
@@ -1575,7 +1575,7 @@ func EvictWithRetryOnConflictForPatch() EvictOption {
 	}
 }
 
-func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, msg string, underlyingCause kueue.EvictionUnderlyingCause, clock clock.Clock, tracker *roletracker.RoleTracker, cl *metrics.CustomLabels, options ...EvictOption) error {
+func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, msg string, underlyingCause kueue.EvictionUnderlyingCause, clock clock.Clock, exposeLqMetrics bool, tracker *roletracker.RoleTracker, cl *metrics.CustomLabels, options ...EvictOption) error {
```

  I think it’s much better to propagate lqMetrics as an option here and check exposeLqMetrics inside. In that case, we don’t need to change the tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T15:23:56Z

- **pkg/util/metrics/metrics.go:38** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T15:23:56Z
```diff
@@ -0,0 +1,40 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/klog/v2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, cache *schdcache.Cache, lqMetrics *metrics.LocalQueueMetricsConfig, wl *kueue.Workload) bool {
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
+		return false
+	}
+	lq, err := cache.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, utilqueue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName))
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
+		return false
+	}
```

  I propose moving this to a separate method in the cache  (e.g. GetLocalQueueLabels) and creating an interface with this method to avoid a circular dependency. In that case, we will have only one ShouldExposeLocalQueueMetrics.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T15:48:25Z

- **pkg/cache/queue/manager.go:183** by [@mimowo](https://github.com/mimowo) — 2026-03-20T15:48:25Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  I actually prefer to adjust tests than using `NewDefaultLocalQueueMetricsConfig`, because then it risks that we forget to pass the value. If we panic, then we know exactly where we forgot to pass the value.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T16:00:34Z

- **pkg/cache/queue/manager.go:183** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T16:00:34Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  If we want to panic, it shouldn’t be optional but a required field, right? I see that roleTracker and custom labels are optional as well, so I’m not sure about this. I think having a default value is enough, and we can use the config if provided. We also have tests to cover this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T16:19:02Z

- **pkg/cache/queue/manager.go:183** by [@mimowo](https://github.com/mimowo) — 2026-03-20T16:19:02Z
```diff
@@ -172,6 +180,7 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		AfsEntryPenalties:      queueafs.NewPenaltyMap(),
 		AfsConsumedResources:   queueafs.NewAfsConsumedResources(),
 		requeuer:               requeuer,
+		lqMetrics:              metrics.NewDefaultLocalQueueMetricsConfig(),
```

  Yeah, I guess we are not very consistent here. Let's go with any option: 
  1. ptr which panics if nil
  2. ptr which defaults if nil
  3. default value which is never nil
  
  IIuc the current approach is (3.). I normally prefer (1.), but I agree (2.) means less changes. 
  
  I'm ok with any actually as long as our tests are providing good coverage so that we can confidently change the approach later.
