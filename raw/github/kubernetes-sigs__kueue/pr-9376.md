# PR #9376: Add common code into raycluster/common.go

**Summary**: Add common code into raycluster/common.go

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9376

**Last updated**: 2026-02-19T21:28:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@hiboyang](https://github.com/hiboyang)
- **Branch**: `main-bo-rayservice-controller-01_prep-01` → `main`
- **Created**: 2026-02-19T17:54:32Z
- **Updated**: 2026-02-19T21:28:27Z
- **Merged**: 2026-02-19T21:03:38Z
- **Closed**: 2026-02-19T21:03:38Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 6   **+1206 / -238**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

This is prep work for https://github.com/kubernetes-sigs/kueue/pull/9102 to refactor common code into raycluster/common.go

#### Which issue(s) this PR fixes:

Prepare for https://github.com/kubernetes-sigs/kueue/pull/9102

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T17:54:41Z

Hi @hiboyang. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-19T17:55:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0b435ca404f97425e01de3ece009a27a107ec662 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6997736c82426d0008b88ca9 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T18:36:42Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T19:13:28Z

Ok, I have diffed the functions so far, and it looks reasonable:
- [BuildPodSets](https://www.diffchecker.com/d5VfcDAV/)
- [UpdatePodSets](https://www.diffchecker.com/gi8lQbzw/)
- [ValidateTopologyRequest](https://www.diffchecker.com/ptbS28NR/)
- [ValidateCreate](https://www.diffchecker.com/tDd2O1hY/)
- [BuildPodSetAnnotationsPathByNameMap](https://www.diffchecker.com/eE38Tq3G/)

I posted comments for the places which are found questionable, the rest looks ok.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T20:40:07Z

/lgtm
/approve
Thanks, I checked the changes and look safe, let me cherrypick to make future cherrypicks feasible
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-19T20:40:11Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9376#issuecomment-3929848449):

>/lgtm
>/approve
>Thanks, I checked the changes and look safe, let me cherrypick to make future cherrypicks feasible
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T20:40:15Z

LGTM label has been added.  <details>Git tree hash: f2497fc9e69aaeae4964097e0e81393ddfc12fb8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-19T20:40:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9376#" title="Author self-approved">hiboyang</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9376#issuecomment-3929848449" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-19T20:40:37Z

@hiboyang please prepare the cherrypicks manually if the robot fails on conflicts

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-19T21:04:17Z

@mimowo: #9376 failed to apply on top of branch "release-0.16":
```
Applying: Add common code into raycluster/common.go
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/raycluster/raycluster_webhook.go
M	pkg/controller/jobs/rayjob/rayjob_webhook.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/rayjob/rayjob_webhook.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/rayjob/rayjob_webhook.go
Auto-merging pkg/controller/jobs/raycluster/raycluster_webhook.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Add common code into raycluster/common.go

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9376#issuecomment-3929848449):

>/lgtm
>/approve
>Thanks, I checked the changes and look safe, let me cherrypick to make future cherrypicks feasible
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-02-19T21:04:51Z

@mimowo: #9376 failed to apply on top of branch "release-0.15":
```
Applying: Add common code into raycluster/common.go
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/raycluster/raycluster_webhook.go
M	pkg/controller/jobs/rayjob/rayjob_controller.go
M	pkg/controller/jobs/rayjob/rayjob_webhook.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/rayjob/rayjob_webhook.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/rayjob/rayjob_webhook.go
Auto-merging pkg/controller/jobs/rayjob/rayjob_controller.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/rayjob/rayjob_controller.go
Auto-merging pkg/controller/jobs/raycluster/raycluster_webhook.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Add common code into raycluster/common.go

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9376#issuecomment-3929848449):

>/lgtm
>/approve
>Thanks, I checked the changes and look safe, let me cherrypick to make future cherrypicks feasible
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T21:23:19Z

Here is cherrypick to 0.16: https://github.com/kubernetes-sigs/kueue/pull/9381

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T21:28:27Z

Here is cherrypick to 015: https://github.com/kubernetes-sigs/kueue/pull/9382

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T18:10:12Z

- **pkg/controller/jobs/raycluster/common.go:40** by [@mimowo](https://github.com/mimowo) — 2026-02-19T18:10:08Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSetsByRayClusterSpec builds PodSets from RayClusterSpec
+func BuildPodSetsByRayClusterSpec(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
```

  nit, but no need to add to function names ByRayClusterSpec suffixes. This is implied by the package name

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-02-19T18:48:43Z

- **pkg/controller/jobs/raycluster/common.go:40** by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T18:48:43Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSetsByRayClusterSpec builds PodSets from RayClusterSpec
+func BuildPodSetsByRayClusterSpec(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
```

  updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T19:04:29Z

- **pkg/controller/jobs/raycluster/common.go:154** by [@mimowo](https://github.com/mimowo) — 2026-02-19T19:04:29Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSets builds PodSets from RayClusterSpec
+func BuildPodSets(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
+	podSets := make([]kueue.PodSet, 0)
+
+	// head
+	headPodSet := kueue.PodSet{
+		Name:     headGroupPodSetName,
+		Template: *rayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+	if features.Enabled(features.TopologyAwareScheduling) {
+		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
+			&rayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
+		if err != nil {
+			return nil, err
+		}
+		headPodSet.TopologyRequest = topologyRequest
+	}
+	podSets = append(podSets, headPodSet)
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		wgs := &rayClusterSpec.WorkerGroupSpecs[index]
+		count := int32(1)
+		if wgs.Replicas != nil {
+			count = *wgs.Replicas
+		}
+		if wgs.NumOfHosts > 1 {
+			count *= wgs.NumOfHosts
+		}
+		workerPodSet := kueue.PodSet{
+			Name:     kueue.NewPodSetReference(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    count,
+		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
+			if err != nil {
+				return nil, err
+			}
+			workerPodSet.TopologyRequest = topologyRequest
+		}
+		podSets = append(podSets, workerPodSet)
+	}
+
+	return podSets, nil
+}
+
+func UpdatePodSets(ctx context.Context, podSets []kueue.PodSet, c client.Client, object client.Object, enableInTreeAutoscaling *bool, rayClusterName string) ([]kueue.PodSet, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Only update podSets from RayCluster if:
+	// 1. The service is workload slicing enabled
+	// 2. AND the service has enableInTreeAutoscaling
+	if workloadslicing.Enabled(object) && ptr.Deref(enableInTreeAutoscaling, false) {
+		// If rayClusterName is set, try to fetch the RayCluster and update PodSets from it
+		if rayClusterName != "" {
+			var rayClusterObj rayv1.RayCluster
+			err := c.Get(ctx, types.NamespacedName{
+				Namespace: object.GetNamespace(),
+				Name:      rayClusterName,
+			}, &rayClusterObj)
+			if err != nil {
+				// Check if the error is a NotFound error
+				if apierrors.IsNotFound(err) {
+					log.V(2).Info("RayCluster does not exist, do not update podsets",
+						"rayCluster", rayClusterName)
+				} else {
+					return nil, fmt.Errorf("failed to get RayCluster %s: %w", rayClusterName, err)
+				}
+			} else {
+				// Create a map of podSets from Ray object spec for quick lookup by name
+				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
+				for i := range podSets {
+					podSetMap[podSets[i].Name] = &podSets[i]
+				}
+
+				// Iterate through RayCluster's worker groups and update the count in matching podSets
+				for i := range rayClusterObj.Spec.WorkerGroupSpecs {
+					wgs := &rayClusterObj.Spec.WorkerGroupSpecs[i]
+					podSetName := kueue.NewPodSetReference(wgs.GroupName)
+
+					podSet, exists := podSetMap[podSetName]
+					if !exists {
+						return nil, fmt.Errorf("PodSet name mismatch: RayCluster %s has worker group %s which is not found in Ray object %s spec", rayClusterName, wgs.GroupName, object.GetName())
+					}
+
+					if wgs.Replicas == nil {
+						continue
+					}
+
+					// Calculate the count based on RayCluster's worker group replicas
+					count := *wgs.Replicas
+					if wgs.NumOfHosts > 1 {
+						count *= wgs.NumOfHosts
+					}
+
+					// Update the count in the PodSet only if it's different
+					if podSet.Count != count {
+						log.V(2).Info("Updated PodSet worker count from RayCluster",
+							"rayObject", object.GetName(),
+							"rayCluster", rayClusterName,
+							"workerGroup", wgs.GroupName,
+							"oldCount", podSet.Count,
+							"newCount", count)
+						podSet.Count = count
+					}
+				}
+			}
+		}
+	}
+
+	return podSets, nil
+}
+
+func UpdateRayClusterSpecToRunWithPodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) error {
```

  Is this used at all? I cannot find usage with simple ctrl+f

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T19:05:44Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:247** by [@mimowo](https://github.com/mimowo) — 2026-02-19T19:05:44Z
```diff
@@ -162,126 +160,22 @@ func (j *RayJob) PodLabelSelector() string {
 	return ""
 }
 
-// buildPodSetsFromRayJobSpec builds PodSets from RayJob's RayClusterSpec
-func (j *RayJob) buildPodSetsFromRayJobSpec() ([]kueue.PodSet, error) {
-	podSets := make([]kueue.PodSet, 0)
-
-	// head
-	headPodSet := kueue.PodSet{
-		Name:     headGroupPodSetName,
-		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
-		Count:    1,
-	}
-	if features.Enabled(features.TopologyAwareScheduling) {
-		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
-			&j.Spec.RayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
-		if err != nil {
-			return nil, err
-		}
-		headPodSet.TopologyRequest = topologyRequest
-	}
-	podSets = append(podSets, headPodSet)
-
-	// workers
-	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
-		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
-		count := int32(1)
-		if wgs.Replicas != nil {
-			count = *wgs.Replicas
-		}
-		if wgs.NumOfHosts > 1 {
-			count *= wgs.NumOfHosts
-		}
-		workerPodSet := kueue.PodSet{
-			Name:     kueue.NewPodSetReference(wgs.GroupName),
-			Template: *wgs.Template.DeepCopy(),
-			Count:    count,
-		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
-			if err != nil {
-				return nil, err
-			}
-			workerPodSet.TopologyRequest = topologyRequest
-		}
-		podSets = append(podSets, workerPodSet)
-	}
-
-	// submitter Job
-	return j.addSubmitterPodSet(podSets)
-}
-
 func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
-	log := ctrl.LoggerFrom(ctx)
-
 	// Always build PodSets from RayJob spec first
-	podSets, err := j.buildPodSetsFromRayJobSpec()
+	podSets, err := raycluster.BuildPodSets(j.Spec.RayClusterSpec)
 	if err != nil {
 		return nil, err
 	}
 
-	// Only update podSets from RayCluster if:
-	// 1. The job is workload slicing enabled
-	// 2. AND the job has enableInTreeAutoscaling
-	if workloadslicing.Enabled(j.Object()) && ptr.Deref(j.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) {
-		// If RayClusterName is set in status, try to fetch the RayCluster and update PodSets from it
-		if j.Status.RayClusterName != "" {
-			var rayClusterObj rayv1.RayCluster
-			err := reconciler.client.Get(ctx, types.NamespacedName{
-				Namespace: j.Namespace,
-				Name:      j.Status.RayClusterName,
-			}, &rayClusterObj)
-			if err != nil {
-				// Check if the error is a NotFound error
-				if apierrors.IsNotFound(err) {
-					log.V(2).Info("RayCluster does not exist, falling back to RayJob spec",
-						"rayCluster", j.Status.RayClusterName)
-				} else {
-					return nil, fmt.Errorf("failed to get RayCluster %s: %w", j.Status.RayClusterName, err)
-				}
-			} else {
-				// Create a map of podSets from RayJob spec for quick lookup by name
-				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
-				for i := range podSets {
-					// Skip submitter PodSet as it's not in RayCluster
-					if podSets[i].Name != submitterJobPodSetName {
```

  I cannot find this `if podSets[i].Name != submitterJobPodSetName {` in the green code: https://github.com/kubernetes-sigs/kueue/pull/9376/changes#diff-10fa4604c18dfab542b85071a5241b0ff0f9500ae99c36d17bb97e402f0c7718R111-R114
  
  is this expected?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T19:11:58Z

- **pkg/controller/jobs/raycluster/common.go:174** by [@mimowo](https://github.com/mimowo) — 2026-02-19T19:11:58Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSets builds PodSets from RayClusterSpec
+func BuildPodSets(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
+	podSets := make([]kueue.PodSet, 0)
+
+	// head
+	headPodSet := kueue.PodSet{
+		Name:     headGroupPodSetName,
+		Template: *rayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+	if features.Enabled(features.TopologyAwareScheduling) {
+		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
+			&rayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
+		if err != nil {
+			return nil, err
+		}
+		headPodSet.TopologyRequest = topologyRequest
+	}
+	podSets = append(podSets, headPodSet)
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		wgs := &rayClusterSpec.WorkerGroupSpecs[index]
+		count := int32(1)
+		if wgs.Replicas != nil {
+			count = *wgs.Replicas
+		}
+		if wgs.NumOfHosts > 1 {
+			count *= wgs.NumOfHosts
+		}
+		workerPodSet := kueue.PodSet{
+			Name:     kueue.NewPodSetReference(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    count,
+		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
+			if err != nil {
+				return nil, err
+			}
+			workerPodSet.TopologyRequest = topologyRequest
+		}
+		podSets = append(podSets, workerPodSet)
+	}
+
+	return podSets, nil
+}
+
+func UpdatePodSets(ctx context.Context, podSets []kueue.PodSet, c client.Client, object client.Object, enableInTreeAutoscaling *bool, rayClusterName string) ([]kueue.PodSet, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Only update podSets from RayCluster if:
+	// 1. The service is workload slicing enabled
+	// 2. AND the service has enableInTreeAutoscaling
+	if workloadslicing.Enabled(object) && ptr.Deref(enableInTreeAutoscaling, false) {
+		// If rayClusterName is set, try to fetch the RayCluster and update PodSets from it
+		if rayClusterName != "" {
+			var rayClusterObj rayv1.RayCluster
+			err := c.Get(ctx, types.NamespacedName{
+				Namespace: object.GetNamespace(),
+				Name:      rayClusterName,
+			}, &rayClusterObj)
+			if err != nil {
+				// Check if the error is a NotFound error
+				if apierrors.IsNotFound(err) {
+					log.V(2).Info("RayCluster does not exist, do not update podsets",
+						"rayCluster", rayClusterName)
+				} else {
+					return nil, fmt.Errorf("failed to get RayCluster %s: %w", rayClusterName, err)
+				}
+			} else {
+				// Create a map of podSets from Ray object spec for quick lookup by name
+				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
+				for i := range podSets {
+					podSetMap[podSets[i].Name] = &podSets[i]
+				}
+
+				// Iterate through RayCluster's worker groups and update the count in matching podSets
+				for i := range rayClusterObj.Spec.WorkerGroupSpecs {
+					wgs := &rayClusterObj.Spec.WorkerGroupSpecs[i]
+					podSetName := kueue.NewPodSetReference(wgs.GroupName)
+
+					podSet, exists := podSetMap[podSetName]
+					if !exists {
+						return nil, fmt.Errorf("PodSet name mismatch: RayCluster %s has worker group %s which is not found in Ray object %s spec", rayClusterName, wgs.GroupName, object.GetName())
+					}
+
+					if wgs.Replicas == nil {
+						continue
+					}
+
+					// Calculate the count based on RayCluster's worker group replicas
+					count := *wgs.Replicas
+					if wgs.NumOfHosts > 1 {
+						count *= wgs.NumOfHosts
+					}
+
+					// Update the count in the PodSet only if it's different
+					if podSet.Count != count {
+						log.V(2).Info("Updated PodSet worker count from RayCluster",
+							"rayObject", object.GetName(),
+							"rayCluster", rayClusterName,
+							"workerGroup", wgs.GroupName,
+							"oldCount", podSet.Count,
+							"newCount", count)
+						podSet.Count = count
+					}
+				}
+			}
+		}
+	}
+
+	return podSets, nil
+}
+
+func UpdateRayClusterSpecToRunWithPodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) error {
+	// head
+	headPod := &rayClusterSpec.HeadGroupSpec.Template
+	info := podSetsInfo[0]
+	if err := podset.Merge(&headPod.ObjectMeta, &headPod.Spec, info); err != nil {
+		return err
+	}
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		workerPod := &rayClusterSpec.WorkerGroupSpecs[index].Template
+		info := podSetsInfo[index+1]
+		if err := podset.Merge(&workerPod.ObjectMeta, &workerPod.Spec, info); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func RestorePodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) bool {
```

  This also I could not find with crtl+f

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-02-19T20:32:18Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:247** by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T20:32:18Z
```diff
@@ -162,126 +160,22 @@ func (j *RayJob) PodLabelSelector() string {
 	return ""
 }
 
-// buildPodSetsFromRayJobSpec builds PodSets from RayJob's RayClusterSpec
-func (j *RayJob) buildPodSetsFromRayJobSpec() ([]kueue.PodSet, error) {
-	podSets := make([]kueue.PodSet, 0)
-
-	// head
-	headPodSet := kueue.PodSet{
-		Name:     headGroupPodSetName,
-		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
-		Count:    1,
-	}
-	if features.Enabled(features.TopologyAwareScheduling) {
-		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
-			&j.Spec.RayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
-		if err != nil {
-			return nil, err
-		}
-		headPodSet.TopologyRequest = topologyRequest
-	}
-	podSets = append(podSets, headPodSet)
-
-	// workers
-	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
-		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
-		count := int32(1)
-		if wgs.Replicas != nil {
-			count = *wgs.Replicas
-		}
-		if wgs.NumOfHosts > 1 {
-			count *= wgs.NumOfHosts
-		}
-		workerPodSet := kueue.PodSet{
-			Name:     kueue.NewPodSetReference(wgs.GroupName),
-			Template: *wgs.Template.DeepCopy(),
-			Count:    count,
-		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
-			if err != nil {
-				return nil, err
-			}
-			workerPodSet.TopologyRequest = topologyRequest
-		}
-		podSets = append(podSets, workerPodSet)
-	}
-
-	// submitter Job
-	return j.addSubmitterPodSet(podSets)
-}
-
 func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
-	log := ctrl.LoggerFrom(ctx)
-
 	// Always build PodSets from RayJob spec first
-	podSets, err := j.buildPodSetsFromRayJobSpec()
+	podSets, err := raycluster.BuildPodSets(j.Spec.RayClusterSpec)
 	if err != nil {
 		return nil, err
 	}
 
-	// Only update podSets from RayCluster if:
-	// 1. The job is workload slicing enabled
-	// 2. AND the job has enableInTreeAutoscaling
-	if workloadslicing.Enabled(j.Object()) && ptr.Deref(j.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) {
-		// If RayClusterName is set in status, try to fetch the RayCluster and update PodSets from it
-		if j.Status.RayClusterName != "" {
-			var rayClusterObj rayv1.RayCluster
-			err := reconciler.client.Get(ctx, types.NamespacedName{
-				Namespace: j.Namespace,
-				Name:      j.Status.RayClusterName,
-			}, &rayClusterObj)
-			if err != nil {
-				// Check if the error is a NotFound error
-				if apierrors.IsNotFound(err) {
-					log.V(2).Info("RayCluster does not exist, falling back to RayJob spec",
-						"rayCluster", j.Status.RayClusterName)
-				} else {
-					return nil, fmt.Errorf("failed to get RayCluster %s: %w", j.Status.RayClusterName, err)
-				}
-			} else {
-				// Create a map of podSets from RayJob spec for quick lookup by name
-				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
-				for i := range podSets {
-					// Skip submitter PodSet as it's not in RayCluster
-					if podSets[i].Name != submitterJobPodSetName {
```

  Actually we do not need `podSets[i].Name != submitterJobPodSetName` checking here, because there will be no submitter job podSet from RayCluster. Even when podSetMap contains submitter job podSet, the previous built podSets will not get updated due to RayCluster not having the matching podSet.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-02-19T20:33:19Z

- **pkg/controller/jobs/raycluster/common.go:154** by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T20:33:19Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSets builds PodSets from RayClusterSpec
+func BuildPodSets(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
+	podSets := make([]kueue.PodSet, 0)
+
+	// head
+	headPodSet := kueue.PodSet{
+		Name:     headGroupPodSetName,
+		Template: *rayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+	if features.Enabled(features.TopologyAwareScheduling) {
+		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
+			&rayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
+		if err != nil {
+			return nil, err
+		}
+		headPodSet.TopologyRequest = topologyRequest
+	}
+	podSets = append(podSets, headPodSet)
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		wgs := &rayClusterSpec.WorkerGroupSpecs[index]
+		count := int32(1)
+		if wgs.Replicas != nil {
+			count = *wgs.Replicas
+		}
+		if wgs.NumOfHosts > 1 {
+			count *= wgs.NumOfHosts
+		}
+		workerPodSet := kueue.PodSet{
+			Name:     kueue.NewPodSetReference(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    count,
+		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
+			if err != nil {
+				return nil, err
+			}
+			workerPodSet.TopologyRequest = topologyRequest
+		}
+		podSets = append(podSets, workerPodSet)
+	}
+
+	return podSets, nil
+}
+
+func UpdatePodSets(ctx context.Context, podSets []kueue.PodSet, c client.Client, object client.Object, enableInTreeAutoscaling *bool, rayClusterName string) ([]kueue.PodSet, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Only update podSets from RayCluster if:
+	// 1. The service is workload slicing enabled
+	// 2. AND the service has enableInTreeAutoscaling
+	if workloadslicing.Enabled(object) && ptr.Deref(enableInTreeAutoscaling, false) {
+		// If rayClusterName is set, try to fetch the RayCluster and update PodSets from it
+		if rayClusterName != "" {
+			var rayClusterObj rayv1.RayCluster
+			err := c.Get(ctx, types.NamespacedName{
+				Namespace: object.GetNamespace(),
+				Name:      rayClusterName,
+			}, &rayClusterObj)
+			if err != nil {
+				// Check if the error is a NotFound error
+				if apierrors.IsNotFound(err) {
+					log.V(2).Info("RayCluster does not exist, do not update podsets",
+						"rayCluster", rayClusterName)
+				} else {
+					return nil, fmt.Errorf("failed to get RayCluster %s: %w", rayClusterName, err)
+				}
+			} else {
+				// Create a map of podSets from Ray object spec for quick lookup by name
+				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
+				for i := range podSets {
+					podSetMap[podSets[i].Name] = &podSets[i]
+				}
+
+				// Iterate through RayCluster's worker groups and update the count in matching podSets
+				for i := range rayClusterObj.Spec.WorkerGroupSpecs {
+					wgs := &rayClusterObj.Spec.WorkerGroupSpecs[i]
+					podSetName := kueue.NewPodSetReference(wgs.GroupName)
+
+					podSet, exists := podSetMap[podSetName]
+					if !exists {
+						return nil, fmt.Errorf("PodSet name mismatch: RayCluster %s has worker group %s which is not found in Ray object %s spec", rayClusterName, wgs.GroupName, object.GetName())
+					}
+
+					if wgs.Replicas == nil {
+						continue
+					}
+
+					// Calculate the count based on RayCluster's worker group replicas
+					count := *wgs.Replicas
+					if wgs.NumOfHosts > 1 {
+						count *= wgs.NumOfHosts
+					}
+
+					// Update the count in the PodSet only if it's different
+					if podSet.Count != count {
+						log.V(2).Info("Updated PodSet worker count from RayCluster",
+							"rayObject", object.GetName(),
+							"rayCluster", rayClusterName,
+							"workerGroup", wgs.GroupName,
+							"oldCount", podSet.Count,
+							"newCount", count)
+						podSet.Count = count
+					}
+				}
+			}
+		}
+	}
+
+	return podSets, nil
+}
+
+func UpdateRayClusterSpecToRunWithPodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) error {
```

  Right, updated code to use this function.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-02-19T20:34:01Z

- **pkg/controller/jobs/raycluster/common.go:174** by [@hiboyang](https://github.com/hiboyang) — 2026-02-19T20:34:01Z
```diff
@@ -0,0 +1,254 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package raycluster
+
+import (
+	"context"
+	"fmt"
+
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/podset"
+	utilpodset "sigs.k8s.io/kueue/pkg/util/podset"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+// BuildPodSets builds PodSets from RayClusterSpec
+func BuildPodSets(rayClusterSpec *rayv1.RayClusterSpec) ([]kueue.PodSet, error) {
+	podSets := make([]kueue.PodSet, 0)
+
+	// head
+	headPodSet := kueue.PodSet{
+		Name:     headGroupPodSetName,
+		Template: *rayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
+		Count:    1,
+	}
+	if features.Enabled(features.TopologyAwareScheduling) {
+		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
+			&rayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
+		if err != nil {
+			return nil, err
+		}
+		headPodSet.TopologyRequest = topologyRequest
+	}
+	podSets = append(podSets, headPodSet)
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		wgs := &rayClusterSpec.WorkerGroupSpecs[index]
+		count := int32(1)
+		if wgs.Replicas != nil {
+			count = *wgs.Replicas
+		}
+		if wgs.NumOfHosts > 1 {
+			count *= wgs.NumOfHosts
+		}
+		workerPodSet := kueue.PodSet{
+			Name:     kueue.NewPodSetReference(wgs.GroupName),
+			Template: *wgs.Template.DeepCopy(),
+			Count:    count,
+		}
+		if features.Enabled(features.TopologyAwareScheduling) {
+			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
+			if err != nil {
+				return nil, err
+			}
+			workerPodSet.TopologyRequest = topologyRequest
+		}
+		podSets = append(podSets, workerPodSet)
+	}
+
+	return podSets, nil
+}
+
+func UpdatePodSets(ctx context.Context, podSets []kueue.PodSet, c client.Client, object client.Object, enableInTreeAutoscaling *bool, rayClusterName string) ([]kueue.PodSet, error) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Only update podSets from RayCluster if:
+	// 1. The service is workload slicing enabled
+	// 2. AND the service has enableInTreeAutoscaling
+	if workloadslicing.Enabled(object) && ptr.Deref(enableInTreeAutoscaling, false) {
+		// If rayClusterName is set, try to fetch the RayCluster and update PodSets from it
+		if rayClusterName != "" {
+			var rayClusterObj rayv1.RayCluster
+			err := c.Get(ctx, types.NamespacedName{
+				Namespace: object.GetNamespace(),
+				Name:      rayClusterName,
+			}, &rayClusterObj)
+			if err != nil {
+				// Check if the error is a NotFound error
+				if apierrors.IsNotFound(err) {
+					log.V(2).Info("RayCluster does not exist, do not update podsets",
+						"rayCluster", rayClusterName)
+				} else {
+					return nil, fmt.Errorf("failed to get RayCluster %s: %w", rayClusterName, err)
+				}
+			} else {
+				// Create a map of podSets from Ray object spec for quick lookup by name
+				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
+				for i := range podSets {
+					podSetMap[podSets[i].Name] = &podSets[i]
+				}
+
+				// Iterate through RayCluster's worker groups and update the count in matching podSets
+				for i := range rayClusterObj.Spec.WorkerGroupSpecs {
+					wgs := &rayClusterObj.Spec.WorkerGroupSpecs[i]
+					podSetName := kueue.NewPodSetReference(wgs.GroupName)
+
+					podSet, exists := podSetMap[podSetName]
+					if !exists {
+						return nil, fmt.Errorf("PodSet name mismatch: RayCluster %s has worker group %s which is not found in Ray object %s spec", rayClusterName, wgs.GroupName, object.GetName())
+					}
+
+					if wgs.Replicas == nil {
+						continue
+					}
+
+					// Calculate the count based on RayCluster's worker group replicas
+					count := *wgs.Replicas
+					if wgs.NumOfHosts > 1 {
+						count *= wgs.NumOfHosts
+					}
+
+					// Update the count in the PodSet only if it's different
+					if podSet.Count != count {
+						log.V(2).Info("Updated PodSet worker count from RayCluster",
+							"rayObject", object.GetName(),
+							"rayCluster", rayClusterName,
+							"workerGroup", wgs.GroupName,
+							"oldCount", podSet.Count,
+							"newCount", count)
+						podSet.Count = count
+					}
+				}
+			}
+		}
+	}
+
+	return podSets, nil
+}
+
+func UpdateRayClusterSpecToRunWithPodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) error {
+	// head
+	headPod := &rayClusterSpec.HeadGroupSpec.Template
+	info := podSetsInfo[0]
+	if err := podset.Merge(&headPod.ObjectMeta, &headPod.Spec, info); err != nil {
+		return err
+	}
+
+	// workers
+	for index := range rayClusterSpec.WorkerGroupSpecs {
+		workerPod := &rayClusterSpec.WorkerGroupSpecs[index].Template
+		info := podSetsInfo[index+1]
+		if err := podset.Merge(&workerPod.ObjectMeta, &workerPod.Spec, info); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func RestorePodSetsInfo(rayClusterSpec *rayv1.RayClusterSpec, podSetsInfo []podset.PodSetInfo) bool {
```

  Updated code to use this function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-19T20:38:52Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:247** by [@mimowo](https://github.com/mimowo) — 2026-02-19T20:38:52Z
```diff
@@ -162,126 +160,22 @@ func (j *RayJob) PodLabelSelector() string {
 	return ""
 }
 
-// buildPodSetsFromRayJobSpec builds PodSets from RayJob's RayClusterSpec
-func (j *RayJob) buildPodSetsFromRayJobSpec() ([]kueue.PodSet, error) {
-	podSets := make([]kueue.PodSet, 0)
-
-	// head
-	headPodSet := kueue.PodSet{
-		Name:     headGroupPodSetName,
-		Template: *j.Spec.RayClusterSpec.HeadGroupSpec.Template.DeepCopy(),
-		Count:    1,
-	}
-	if features.Enabled(features.TopologyAwareScheduling) {
-		topologyRequest, err := jobframework.NewPodSetTopologyRequest(
-			&j.Spec.RayClusterSpec.HeadGroupSpec.Template.ObjectMeta).Build()
-		if err != nil {
-			return nil, err
-		}
-		headPodSet.TopologyRequest = topologyRequest
-	}
-	podSets = append(podSets, headPodSet)
-
-	// workers
-	for index := range j.Spec.RayClusterSpec.WorkerGroupSpecs {
-		wgs := &j.Spec.RayClusterSpec.WorkerGroupSpecs[index]
-		count := int32(1)
-		if wgs.Replicas != nil {
-			count = *wgs.Replicas
-		}
-		if wgs.NumOfHosts > 1 {
-			count *= wgs.NumOfHosts
-		}
-		workerPodSet := kueue.PodSet{
-			Name:     kueue.NewPodSetReference(wgs.GroupName),
-			Template: *wgs.Template.DeepCopy(),
-			Count:    count,
-		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			topologyRequest, err := jobframework.NewPodSetTopologyRequest(&wgs.Template.ObjectMeta).Build()
-			if err != nil {
-				return nil, err
-			}
-			workerPodSet.TopologyRequest = topologyRequest
-		}
-		podSets = append(podSets, workerPodSet)
-	}
-
-	// submitter Job
-	return j.addSubmitterPodSet(podSets)
-}
-
 func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
-	log := ctrl.LoggerFrom(ctx)
-
 	// Always build PodSets from RayJob spec first
-	podSets, err := j.buildPodSetsFromRayJobSpec()
+	podSets, err := raycluster.BuildPodSets(j.Spec.RayClusterSpec)
 	if err != nil {
 		return nil, err
 	}
 
-	// Only update podSets from RayCluster if:
-	// 1. The job is workload slicing enabled
-	// 2. AND the job has enableInTreeAutoscaling
-	if workloadslicing.Enabled(j.Object()) && ptr.Deref(j.Spec.RayClusterSpec.EnableInTreeAutoscaling, false) {
-		// If RayClusterName is set in status, try to fetch the RayCluster and update PodSets from it
-		if j.Status.RayClusterName != "" {
-			var rayClusterObj rayv1.RayCluster
-			err := reconciler.client.Get(ctx, types.NamespacedName{
-				Namespace: j.Namespace,
-				Name:      j.Status.RayClusterName,
-			}, &rayClusterObj)
-			if err != nil {
-				// Check if the error is a NotFound error
-				if apierrors.IsNotFound(err) {
-					log.V(2).Info("RayCluster does not exist, falling back to RayJob spec",
-						"rayCluster", j.Status.RayClusterName)
-				} else {
-					return nil, fmt.Errorf("failed to get RayCluster %s: %w", j.Status.RayClusterName, err)
-				}
-			} else {
-				// Create a map of podSets from RayJob spec for quick lookup by name
-				podSetMap := make(map[kueue.PodSetReference]*kueue.PodSet)
-				for i := range podSets {
-					// Skip submitter PodSet as it's not in RayCluster
-					if podSets[i].Name != submitterJobPodSetName {
```

  I see, because there could never be submitter when iterating over workers on this line later: https://github.com/kubernetes-sigs/kueue/pull/9376/changes#diff-10fa4604c18dfab542b85071a5241b0ff0f9500ae99c36d17bb97e402f0c7718R117
