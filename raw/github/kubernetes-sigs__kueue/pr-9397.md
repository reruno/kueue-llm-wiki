# PR #9397: Feat move env js to cm

**Summary**: Feat move env js to cm

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9397

**Last updated**: 2026-03-19T18:06:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ziadmoubayed](https://github.com/ziadmoubayed)
- **Branch**: `feat-move-env-js-to-cm` → `main`
- **Created**: 2026-02-20T13:54:22Z
- **Updated**: 2026-03-19T18:06:36Z
- **Merged**: 2026-03-19T18:06:33Z
- **Closed**: 2026-03-19T18:06:33Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`, `area/dashboard`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@akram](https://github.com/akram), [@kannon92](https://github.com/kannon92)
- **Changed files**: 19   **+240 / -121**

## Description

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

Mount env.js via a ConfigMap instead of generating it at runtime.

#### Which issue(s) this PR fixes:

Fixes #9334

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
KueueViz: The backend WebSocket URL is no longer configured via a container environment variable. It is now automatically derived from the backend ingress configuration and mounted as a ConfigMap.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-20T13:54:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 86cbfb4a01cbfbcc9a3106f52003013d8320a0df |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b3c7fb154faa0008c5fb07 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9397--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9397--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTkzOTctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.iWv5iD4sgTeRanjEWD4eHJ7X_2Dk3VMNdJL-ONmYP_Y)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9397--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-20T13:54:32Z

Hi @ziadmoubayed. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-02-20T15:39:37Z

/ok-to-test

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-02-24T13:03:22Z

/test pull-kueue-test-unit-main

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-26T15:59:11Z

@ziadmoubayed  the release note is great `The backend WebSocket URL is no longer configured via a container environment variable. It is now automatically derived from the backend ingress configuration and mounted as a ConfigMap.` but it focuses on the technical aspects. Is there any difference in terms of defaults, or the way of deploying that is user-observable?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T15:12:40Z

@ziadmoubayed do you maybe have cycles to address the remaining comments?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T15:13:33Z

/area dashboard

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-10T15:15:41Z

> @ziadmoubayed do you maybe have cycles to address the remaining comments?

I came back from vacation today. Will address the comments this week :)

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T15:26:03Z

welcome back, hope you had a good one

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-16T12:49:26Z

@samzong can you please check again when you can

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-16T13:01:00Z

> @ziadmoubayed the release note is great `The backend WebSocket URL is no longer configured via a container environment variable. It is now automatically derived from the backend ingress configuration and mounted as a ConfigMap.` but it focuses on the technical aspects. Is there any difference in terms of defaults, or the way of deploying that is user-observable?

@mimowo I updated the install instructions to reflect this change. 
For existing deployments/upgrades, since `frontend.env.REACT_APP_WEBSOCKET_URL` is no longer configuring backend url, we can mention that users can drop this value/config. However the change is backwards compatible, if the env stays, the user should not have issues.

Please let me know how to proceed.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T17:23:45Z

/lgtm
/approve
@ziadmoubayed thank you for the contibutions and addressing all comments from @samzong and me.

Are we now able to use `readOnlyRootFilesystem:true` by default in the deployment? Seems like a good practice? This would be a follow up in any case.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T17:23:54Z

LGTM label has been added.  <details>Git tree hash: d9793d231307ccd3b0e71981b7f30a2e77a80222</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T17:23:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9397#issuecomment-4091942032" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9397#" title="Author self-approved">ziadmoubayed</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T17:26:11Z

/release-note-edit
```release-note
KueueViz: The backend WebSocket URL is no longer configured via a container environment variable. It is now automatically derived from the backend ingress configuration and mounted as a ConfigMap.
```

## Reviews

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-07T16:23:19Z

overall this is a good improvement. but a few things need to be cleaned up before merging.

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:71** by [@samzong](https://github.com/samzong) — 2026-03-07T16:09:57Z
```diff
@@ -65,6 +65,15 @@ spec:
           securityContext:
             {{- toYaml . | nindent 12 }}
           {{- end }}
+          {{- with .Values.kueueViz.frontend.env }}
+          env:
+            {{- toYaml . | nindent 12 }}
+          {{- end }}
```

  This `frontend.env` is dead config, it's still renders into the container, but since `start.sh` is gone, no code reads container env vars anymore

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:79** by [@samzong](https://github.com/samzong) — 2026-03-07T16:13:13Z
```diff
@@ -65,6 +65,15 @@ spec:
           securityContext:
             {{- toYaml . | nindent 12 }}
           {{- end }}
+          {{- with .Values.kueueViz.frontend.env }}
+          env:
+            {{- toYaml . | nindent 12 }}
+          {{- end }}
           ports:
             - containerPort: 8080
+          volumeMounts:
+            - name: env-config
+              mountPath: /app/build/env.js
+              subPath: env.js
```

  subPath mounts don't auto-update on ConfigMap changes, need add checksum annotation

- **charts/kueue/tests/kueue_test.yaml:287** by [@samzong](https://github.com/samzong) — 2026-03-07T16:16:11Z
```diff
@@ -226,32 +229,47 @@ tests:
     asserts:
       - notExists:
           path: spec.template.spec.containers[0].env[0]
-  - it: should set the environment variables for frontend deployment
-    template: kueueviz/frontend-deployment.yaml
+  - it: should render frontend configmap with wss when backend tls is set
+    template: kueueviz/frontend-configmap.yaml
     set:
       enableKueueViz: true
       kueueViz:
-        frontend:
-          env:
-            - name: REACT_APP_WEBSOCKET_URL
-              value: "wss://backend.kueueviz.local"
+        backend:
+          ingress:
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-backend-tls"
     asserts:
-      - equal:
-          path: spec.template.spec.containers[0].env[0].name
-          value: REACT_APP_WEBSOCKET_URL
-      - equal:
-          path: spec.template.spec.containers[0].env[0].value
-          value: "wss://backend.kueueviz.local"
-  - it: should not set the environment variables if not set for frontend deployment
-    template: kueueviz/frontend-deployment.yaml
+      - isKind:
+          of: ConfigMap
+      - matchRegex:
+          path: data["env.js"]
+          pattern: "wss://backend.kueueviz.local"
+  - it: should render frontend configmap with ws when backend tls is empty
+    template: kueueviz/frontend-configmap.yaml
     set:
       enableKueueViz: true
       kueueViz:
-        frontend:
-          env: []
+        backend:
+          ingress:
+            host: "backend.kueueviz.local"
+            tlsSecretName: ""
     asserts:
-      - notExists:
-          path: spec.template.spec.containers[0].env[0]
+      - matchRegex:
+          path: data["env.js"]
+          pattern: "ws://backend.kueueviz.local"
+  - it: should render frontend configmap with custom backend host
+    template: kueueviz/frontend-configmap.yaml
```

  this tests cover the new ConfigMap path, but the old frontend.env override tests were removed without replacement.

- **site/content/en/docs/tasks/manage/enable_kueueviz.md:82** by [@samzong](https://github.com/samzong) — 2026-03-07T16:18:51Z
```diff
@@ -79,7 +79,28 @@ kubectl port-forward svc/kueue-kueueviz-frontend -n kueue-system 8080
 kubectl port-forward svc/kueue-kueueviz-backend  -n kueue-system 8081:8080
 ```
 
-Edit the kueue-viz-frontend Deployment to set env `REACT_APP_WEBSOCKET_URL=ws://localhost:8081`.
+Edit the `kueueviz-frontend-env` ConfigMap to set the backend WebSocket URL:
```

  lost cmd/kueueviz/INSTALL.md, need update also.

- **cmd/kueueviz/frontend/Makefile:20** by [@samzong](https://github.com/samzong) — 2026-03-07T16:21:54Z
```diff
@@ -17,7 +17,7 @@ install:
 # Run target: serves the built app
 .PHONY: run
 run:
-	npx react-inject-env set && npx http-server build
+	npx serve -s build -l tcp://0.0.0.0:8080
```

  npx serve will check package. suggestion use `npm install -g serve` the just run `serve`，reduce boot time and network dependency

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-13T07:01:33Z

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:79** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-13T07:01:33Z
```diff
@@ -65,6 +65,15 @@ spec:
           securityContext:
             {{- toYaml . | nindent 12 }}
           {{- end }}
+          {{- with .Values.kueueViz.frontend.env }}
+          env:
+            {{- toYaml . | nindent 12 }}
+          {{- end }}
           ports:
             - containerPort: 8080
+          volumeMounts:
+            - name: env-config
+              mountPath: /app/build/env.js
+              subPath: env.js
```

  added

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-13T07:01:51Z

- **charts/kueue/tests/kueue_test.yaml:287** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-13T07:01:51Z
```diff
@@ -226,32 +229,47 @@ tests:
     asserts:
       - notExists:
           path: spec.template.spec.containers[0].env[0]
-  - it: should set the environment variables for frontend deployment
-    template: kueueviz/frontend-deployment.yaml
+  - it: should render frontend configmap with wss when backend tls is set
+    template: kueueviz/frontend-configmap.yaml
     set:
       enableKueueViz: true
       kueueViz:
-        frontend:
-          env:
-            - name: REACT_APP_WEBSOCKET_URL
-              value: "wss://backend.kueueviz.local"
+        backend:
+          ingress:
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-backend-tls"
     asserts:
-      - equal:
-          path: spec.template.spec.containers[0].env[0].name
-          value: REACT_APP_WEBSOCKET_URL
-      - equal:
-          path: spec.template.spec.containers[0].env[0].value
-          value: "wss://backend.kueueviz.local"
-  - it: should not set the environment variables if not set for frontend deployment
-    template: kueueviz/frontend-deployment.yaml
+      - isKind:
+          of: ConfigMap
+      - matchRegex:
+          path: data["env.js"]
+          pattern: "wss://backend.kueueviz.local"
+  - it: should render frontend configmap with ws when backend tls is empty
+    template: kueueviz/frontend-configmap.yaml
     set:
       enableKueueViz: true
       kueueViz:
-        frontend:
-          env: []
+        backend:
+          ingress:
+            host: "backend.kueueviz.local"
+            tlsSecretName: ""
     asserts:
-      - notExists:
-          path: spec.template.spec.containers[0].env[0]
+      - matchRegex:
+          path: data["env.js"]
+          pattern: "ws://backend.kueueviz.local"
+  - it: should render frontend configmap with custom backend host
+    template: kueueviz/frontend-configmap.yaml
```

  brought back old test. thanks

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-13T07:06:44Z

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:71** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-13T07:06:44Z
```diff
@@ -65,6 +65,15 @@ spec:
           securityContext:
             {{- toYaml . | nindent 12 }}
           {{- end }}
+          {{- with .Values.kueueViz.frontend.env }}
+          env:
+            {{- toYaml . | nindent 12 }}
+          {{- end }}
```

  we set default `.Values.kueueViz.frontend.env` to `env: []`. This means env does not render into the container if not explicitly set by the user.

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-13T07:06:55Z

- **cmd/kueueviz/frontend/Makefile:20** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-13T07:06:55Z
```diff
@@ -17,7 +17,7 @@ install:
 # Run target: serves the built app
 .PHONY: run
 run:
-	npx react-inject-env set && npx http-server build
+	npx serve -s build -l tcp://0.0.0.0:8080
```

  fixed thanks

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-13T09:06:37Z

- **site/content/en/docs/tasks/manage/enable_kueueviz.md:82** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-13T09:06:36Z
```diff
@@ -79,7 +79,28 @@ kubectl port-forward svc/kueue-kueueviz-frontend -n kueue-system 8080
 kubectl port-forward svc/kueue-kueueviz-backend  -n kueue-system 8081:8080
 ```
 
-Edit the kueue-viz-frontend Deployment to set env `REACT_APP_WEBSOCKET_URL=ws://localhost:8081`.
+Edit the `kueueviz-frontend-env` ConfigMap to set the backend WebSocket URL:
```

  aligned the docs
