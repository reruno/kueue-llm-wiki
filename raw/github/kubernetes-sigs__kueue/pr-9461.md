# PR #9461: Add subpage in observability about resource usage monitoring

**Summary**: Add subpage in observability about resource usage monitoring

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9461

**Last updated**: 2026-03-11T09:57:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dkaluza](https://github.com/dkaluza)
- **Branch**: `observability-webpage-for-resource-monitoring` → `main`
- **Created**: 2026-02-24T17:53:23Z
- **Updated**: 2026-03-11T09:57:37Z
- **Merged**: 2026-02-27T10:11:57Z
- **Closed**: 2026-02-27T10:11:57Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+136 / -0**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
Adds description to the website how to use the new pod labels to monitor real resource usage of the queues.

#### Which issue(s) this PR fixes:
Fixes #9435

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

Result of the commands execution in my local cluster:

<img width="2541" height="1048" alt="image" src="https://github.com/user-attachments/assets/a6816ec8-a654-4594-833e-73a2804c498a" />
<img width="896" height="206" alt="image" src="https://github.com/user-attachments/assets/0938785b-4284-4689-8fc2-88fdf79da578" />

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-24T17:53:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 14ee02b29d39ab650a8c2daafc79d684ea43027a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69a081e0eb7fe20008bf7f08 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9461--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9461--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk0NjEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.WvJRHHMzxll63BgIQ4KtuCteTkvIrykNUJNBQ_sIFus)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9461--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-24T17:53:33Z

Hi @dkaluza. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-24T17:56:05Z

/ok-to-test
cc @kannon92 ptal

### Comment by [@dkaluza](https://github.com/dkaluza) — 2026-02-25T09:46:27Z

> 8:55:49 AM: Failed during stage 'preparing repo': For more information, visit https://docs.netlify.com/configure-builds/troubleshooting-tips/#build-fails-with-error-128: fatal: unable to access 'https://github.com/kubernetes-sigs/kueue/': Could not resolve host: github.com
: exit status 128

Looks like just random dns issue not related to the change. Will `retest` try to build again? Or should I make a new commit? (previous built without issues)

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-25T11:38:10Z

> Looks like just random dns issue not related to the change. Will retest try to build again? Or should I make a new commit? (previous built without issues)

You can push a no-op change just to trigger the CI.

Something like 
```sh
git commit --amend --no-edit
git push --force-with-lease
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-26T07:55:51Z

cc @MichalZylinski, would you like to review?

### Comment by [@dkaluza](https://github.com/dkaluza) — 2026-02-26T17:07:01Z

Thanks!
I have introduced the wording improvements, and changed the  job yaml to a simple script applying the job 3 times in a row.

Let me know if I should embed the images in the docs, generally it is not popular in Kueue docs, but it might be valuable for the user.s

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-27T09:35:26Z

LGTM label has been added.  <details>Git tree hash: 506613fbd023ef62ad91b5c61dd755838e0e434b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-02-27T09:35:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9461#" title="Author self-approved">dkaluza</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9461#pullrequestreview-3865939344" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@MichalZylinski](https://github.com/MichalZylinski) (changes_requested) — 2026-02-26T10:24:47Z

I left a few suggestions streamlining the wording and the examples provided

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:2** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:41:43Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
```

  ```suggestion
  title: "Monitor actual resource usage"
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:6** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:42:04Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
```

  ```suggestion
    Monitor actual resource usage with aggregation or filtering by specific queue.
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:41** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:44:13Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
```

  The YAML example creates just one job with one pod - maybe it would be worth adding several more or at least increase the parallelism value?

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:73** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:45:49Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
```

  Can we see add the example result of such command as well?

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:91** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:46:34Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
```

  ```suggestion
  The commands below assume you are using the `kube-prometheus-stack` Helm chart (which includes both tools). If you use a different setup, adjust your configuration accordingly. To proceed with the Helm chart, add the following fragment to your `values.yaml` file:
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:88** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:50:14Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
```

  ```suggestion
  1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes/kube-state-metrics) according to your cluster's setup. Next, configure your `kube-state-metrics` to allowlist Kueue pod labels.
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:89** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T09:55:37Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
```

  ```suggestion
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:104** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:03:24Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
```

  ```suggestion
  2. If you do not have the Prometheus UI available, forward a port for the Prometheus service:
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:108** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:04:47Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
+```sh
+kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090
+```
+3. Deploy some jobs, for example one defined above.
```

  ```suggestion
  3. Deploy some jobs, such as the sample job defined earlier.
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:110** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:06:48Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
+```sh
+kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090
+```
+3. Deploy some jobs, for example one defined above.
+
+4. Verify that new labels are available in the PromQL queries in [UI](http://localhost:9090/) (use your own address if you did not forward the port in one of the previous steps):
```

  ```suggestion
  4. Verify that the new labels are available by running a PromQL query in the [Prometheus UI](http://localhost:9090/). (If you are not using port-forwarding, use your specific cluster address instead.)
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:128** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:09:54Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
+```sh
+kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090
+```
+3. Deploy some jobs, for example one defined above.
+
+4. Verify that new labels are available in the PromQL queries in [UI](http://localhost:9090/) (use your own address if you did not forward the port in one of the previous steps):
+```promql
+kube_pod_labels{label_kueue_x_k8s_io_local_queue_name!=""}
+```
+
+5. Now you can attach queue labels to the existing pod resource usage metrics with [group_left](https://prometheus.io/docs/prometheus/latest/querying/operators/#many-to-one-and-one-to-many-vector-matches).
+
+Use them to aggregate the metrics, for example:
+```
+sum by (label_kueue_x_k8s_io_local_queue_name) (
+  sum by (namespace, pod) (
+    rate(container_cpu_usage_seconds_total{container!="", pod!=""}[5m])
+  )
+  * on(namespace, pod) group_left(label_kueue_x_k8s_io_local_queue_name)
+  kube_pod_labels{label_kueue_x_k8s_io_local_queue_name!=""}
+)
+```
+
+Use `label_kueue_x_k8s_io_cluster_queue_name` to filter and aggregate by cluster queue.
```

  ```suggestion
  To filter or aggregate by cluster queue instead, replace the local queue label with  `label_kueue_x_k8s_io_cluster_queue_name`.
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:115** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:12:16Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
+```sh
+kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090
+```
+3. Deploy some jobs, for example one defined above.
+
+4. Verify that new labels are available in the PromQL queries in [UI](http://localhost:9090/) (use your own address if you did not forward the port in one of the previous steps):
+```promql
+kube_pod_labels{label_kueue_x_k8s_io_local_queue_name!=""}
+```
+
+5. Now you can attach queue labels to the existing pod resource usage metrics with [group_left](https://prometheus.io/docs/prometheus/latest/querying/operators/#many-to-one-and-one-to-many-vector-matches).
```

  ```suggestion
  5. You can now join the queue labels with your existing pod resource metrics using [group_left](https://prometheus.io/docs/prometheus/latest/querying/operators/#many-to-one-and-one-to-many-vector-matches). For example, use this query to aggregate CPU usage by local queue:
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:117** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:12:37Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
+Adjust the kube-state-metrics configuration to allowlist Kueue pod labels.
+
+Below commands assume that you are using the kube-prometheus-stack helm chart, which contains both prometheus and kube-state-metrics.
+If you are using other setup adjust them appropriately.
+Add the following fragment to your `values.yaml`:
+```yaml
+kube-state-metrics:
+  metricLabelsAllowlist:
+    - pods=[kueue.x-k8s.io/cluster-queue-name, kueue.x-k8s.io/local-queue-name]
+```
+
+Deploy prometheus stack helm chart with `values.yaml`:
+```sh
+helm install kube-prometheus-stack oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack -f values.yaml
+```
+2. If you do not have the Prometheus UI available online - forward port for prometheus service:
+```sh
+kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090
+```
+3. Deploy some jobs, for example one defined above.
+
+4. Verify that new labels are available in the PromQL queries in [UI](http://localhost:9090/) (use your own address if you did not forward the port in one of the previous steps):
+```promql
+kube_pod_labels{label_kueue_x_k8s_io_local_queue_name!=""}
+```
+
+5. Now you can attach queue labels to the existing pod resource usage metrics with [group_left](https://prometheus.io/docs/prometheus/latest/querying/operators/#many-to-one-and-one-to-many-vector-matches).
+
+Use them to aggregate the metrics, for example:
```

  ```suggestion
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:34** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:22:17Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
```

  ```suggestion
  for quick troubleshooting but is not a substitute for full-scale monitoring. See the Production resource monitoring](#production-resource-monitoring) section for a more robust setup.
  ```

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:35** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T10:22:31Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
```

  ```suggestion
  ```

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-02-26T16:14:19Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:73** by [@dkaluza](https://github.com/dkaluza) — 2026-02-26T16:14:19Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
```

  Oh, please note that the screenshot is just available in the PR it is currently not a part of the documentation webpage. If we wish to make them a part we can, should I introduce needed changes?
  
  Sure I can also add a result of kubectl top it looks like this:
  <img width="896" height="206" alt="image" src="https://github.com/user-attachments/assets/0938785b-4284-4689-8fc2-88fdf79da578" />

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-02-26T16:18:09Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:41** by [@dkaluza](https://github.com/dkaluza) — 2026-02-26T16:18:09Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
```

  I was personally just repeatedly creating new jobs using the provided snippet.
  It aligns with the real usage quite well as there are multiple jobs instead of one.
  
  I will make the command apply 3 distinct jobs, wdyt?

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-02-26T17:00:46Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:88** by [@dkaluza](https://github.com/dkaluza) — 2026-02-26T17:00:45Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
+
+4. Similarly, you can monitor the usage of the resources by jobs admitted to a cluster queue:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/cluster-queue-name=cluster-queue'
+```
+
+{{% alert title="Note" color="primary" %}}
+If you encounter an error like "error: Metrics API not available", it may be caused by certificate
+verification problems in your cluster. You can skip the verification in metrics-server by
+editing the deployment `kubectl edit deployment metrics-server -n kube-system`
+and adding `--kubelet-insecure-tls` to the container arguments, however this
+is **highly discouraged in production environments**.
+{{% /alert %}}
+
+
+## Production resource monitoring
+
+1. Install [prometheus](/docs/tasks/manage/observability/setup_prometheus) and [kube-state-metrics](https://github.com/kubernetes-sigs/kube-state-metrics) in a way that matches your setup.
```

  Thanks! I don't know how I have missed the fact that link is wrong, I though I tested all of them...

### Review by [@MichalZylinski](https://github.com/MichalZylinski) (commented) — 2026-02-26T17:06:04Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:41** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T17:06:04Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
```

  makes sense to me

### Review by [@MichalZylinski](https://github.com/MichalZylinski) (commented) — 2026-02-26T17:07:27Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:73** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T17:07:27Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
```

  Can we just turn it into markdown instead for consistency?

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-02-26T17:15:33Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:73** by [@dkaluza](https://github.com/dkaluza) — 2026-02-26T17:15:33Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
```

  This one - sure,  but not the UI one. 
  If we want it in the document it should be a screenshot.

### Review by [@MichalZylinski](https://github.com/MichalZylinski) (commented) — 2026-02-26T17:37:57Z

- **site/content/en/docs/tasks/manage/observability/monitor_resource_usage.md:73** by [@MichalZylinski](https://github.com/MichalZylinski) — 2026-02-26T17:37:57Z
```diff
@@ -0,0 +1,128 @@
+---
+title: "Monitor real resource usage"
+date: 2026-02-24
+weight: 3
+description: >
+  Monitor real resource usage with aggregation or filtering by specific queue.
+---
+
+This page shows you how to use Kueue labels assigned to pods to monitor resource
+usage.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+- The cluster has at least one [local queue and cluster queue](/docs/tasks/manage/administer_cluster_quotas). Presented commands assume your local queue is named "user-queue" and cluster queue is named "cluster-queue", if this is not the case adjust them appropriately.
+
+
+{{% alert title="Note" color="primary" %}}
+The queries in this page require `AssignQueueLabelsForPods` Feature Gate, which is enabled by default.
+If it is not enabled, see [Installation](/docs/installation/#change-the-feature-gates-configuration) for details how to enable it.
+{{% /alert %}}
+
+## `kubectl top` for command line resource usage debugging
+
+{{% alert title="Warning" color="warning" %}}
+As mentioned on  [Metrics Server repository site](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#kubernetes-metrics-server),
+Metrics Server and provided command line tool - `kubectl top` - is not meant as a monitoring solution. The tool is convenient
+for local debugging but it is not a replacement for real monitoring. For the setup more prepared for production see the
+[Production resource monitoring](#production-resource-monitoring) section.
+
+{{% /alert %}}
+
+1. To use `kubectl top` you need to install metrics-server for you cluster.
+Follow the [Metrics Server Installation](https://github.com/kubernetes-sigs/metrics-server?tab=readme-ov-file#installation)
+2. Schedule a couple of jobs that have some real cpu usage to your local queue:
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: "user-queue"
+spec:
+  parallelism: 1
+  completions: 1
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: registry.k8s.io/e2e-test-images/agnhost:2.53
+        command: [ "/bin/sh" ]
+        args: [ "-c", "end_time=$(($(date +%s) + 60)); while [ $(date +%s) -lt $end_time ]; do :; done"]
+        resources:
+          requests:
+            cpu: "1"
+
+      restartPolicy: Never
+```
+3. Monitor the usage of cpu and memory in nearly real time for this local queue with `kubectl top`:
+```sh
+watch -n 15  'kubectl top pod --sum -l kueue.x-k8s.io/local-queue-name=user-queue'
+```
+You will see a list of pods currently running on the local queue named "user-queue" with their current cpu and memory measurements and the sum of the usage at the bottom. The list will be automatically refreshed every 15 seconds.
```

  Yep, I would add the screenshot as well. Otherwise lgtm.

### Review by [@MichalZylinski](https://github.com/MichalZylinski) (changes_requested) — 2026-02-26T17:38:43Z

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T09:35:18Z

Very useful write up, thank you 👍 
/lgtm
/approve
