# PR #9487: Fix missing replica_role=leader metrics after HA role transition

**Summary**: Fix missing replica_role=leader metrics after HA role transition

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9487

**Last updated**: 2026-03-10T19:09:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `fix/ha-role-transition-metrics-resync` → `main`
- **Created**: 2026-02-25T18:02:45Z
- **Updated**: 2026-03-10T19:09:52Z
- **Merged**: 2026-03-10T18:59:11Z
- **Closed**: 2026-03-10T18:59:11Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 12   **+358 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug

#### What this PR does / why we need it:

After an HA role transition from `follower` to `leader`, gauge metrics were still tagged with `replica_role` set to `follower` and were never re-reported with `replica_role` set to `leader`.

This change adds an `OnTransition` callback to `RoleTracker` that clears stale follower metrics and resynchronizes all metric-emitting components.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9288 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: Fix missing replica_role=leader gauge metrics after HA role transition.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-25T18:02:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | df165325f4792279b6ac77074c9df85605cdf5dc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b061b9cfa077000897f0af |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9487--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9487--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk0ODctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.OyfYqeWtRQZO6NPd5cuimlkA8drtT-iF8iEEgWVSXw4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9487--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mveroone](https://github.com/mveroone) — 2026-02-27T09:33:13Z

@IrvingMg From your experience, are the logger reporting the correct role, while metrics not ?

In my case, I can always see the scheduler thread role to be follower, even in single-replica setups.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T09:55:36Z

> @IrvingMg From your experience, are the logger reporting the correct role, while metrics not ?
> 
> In my case, I can always see the scheduler thread role to be follower, even in single-replica setups.

Over the past days, we’ve been working on a few fixes for the roleTracker. One of them was related to the scheduler: https://github.com/kubernetes-sigs/kueue/pull/9429, so this might be the one you’re experiencing.

We also fixed a couple of issue related to logs missing the roleTracker: https://github.com/kubernetes-sigs/kueue/pull/9445 and https://github.com/kubernetes-sigs/kueue/pull/9433.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T12:15:46Z

The so-far fixes (except this pending PR) will all be included in the coming 0.16.2 patch release (being done today as we speak): https://github.com/kubernetes-sigs/kueue/issues/9378.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-03T13:51:17Z

@IrvingMg 
 Have you tried the re-sync in a cluster scenario?

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T18:21:12Z

> @IrvingMg Have you tried the re-sync in a cluster scenario?

No, but I added an integration test for it. I could also add an e2e test if needed. Or do you mean doing a manual check with a kind cluster, for example?

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-04T09:14:44Z

> > @IrvingMg Have you tried the re-sync in a cluster scenario?
> 
> No, but I added an integration test for it. I could also add an e2e test if needed. Or do you mean doing a manual check with a kind cluster, for example?

Yes, I mean to test with kind cluster and Grafana.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-04T10:09:45Z

overall lgtm
please check the PR with manual tests -  it can reveal some issues that can't be catch in integration

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T14:55:12Z

I also ran a manual verification and it seems to be working correctly. These are the steps I followed:

1. Created a ClusterQueue.
2. Verified the baseline metrics in Prometheus. Both pods were reporting metrics correctly:
   - `kueue_cluster_queue_status`:  `cnf4f` with `replica_role="leader"`, `668gt` with `replica_role="follower"`.
   - `kueue_cluster_queue_nominal_quota`: same pattern.
   
<img width="1902" height="640" alt="Screenshot 2026-03-05 at 15 36 59" src="https://github.com/user-attachments/assets/c8c1de72-0c4c-432d-a268-e941f8d2a4dc" />
<img width="1902" height="640" alt="Screenshot 2026-03-05 at 15 37 34" src="https://github.com/user-attachments/assets/3e3de617-2130-463d-9abd-24a393049d5f" />

3. Killed the leader pod (`cnf4f`), so the existing follower (`668gt`) became the new leader. 
4. After the transition, the metrics of `668gt` changed from `replica_role="follower"` to `replica_role="leader"` with the correct values, and no stale follower metrics remained. 

<img width="1902" height="640" alt="Screenshot 2026-03-05 at 15 41 37" src="https://github.com/user-attachments/assets/a0248ecf-0684-47b3-ac8a-6992e107a7a4" />
<img width="1902" height="640" alt="Screenshot 2026-03-05 at 15 38 58" src="https://github.com/user-attachments/assets/e7a37168-b8bc-4200-af54-e35fe75e57d2" />

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T15:02:21Z

/test pull-kueue-test-e2e-main-1-32

Looks like infra issue:

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9487/pull-kueue-test-e2e-main-1-32/2029569601565626368

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T08:39:16Z

cc @mbobrovskyi @kshalot would you like to give it a pass?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:44:37Z

LGTM, I'm happy to merge it, but I would like to align on some path forward around the code duplication: https://github.com/kubernetes-sigs/kueue/pull/9487#discussion_r2913427144.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:27:25Z

Thank you 👍 
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T18:27:34Z

LGTM label has been added.  <details>Git tree hash: cc065d6bd7f97df29c5dde85838f840183a54cfa</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T18:27:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9487#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9487#issuecomment-4033548255" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:28:28Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T18:28:32Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9487#issuecomment-4033555250):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:29:05Z

@IrvingMg please prepare cherrypicks manually in case of conflicts

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T18:59:52Z

@mimowo: new pull request created: #9794

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9487#issuecomment-4033555250):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T19:00:28Z

@mimowo: #9487 failed to apply on top of branch "release-0.15":
```
Applying: Fix missing replica_role=leader metrics after HA role transition
Using index info to reconstruct a base tree...
M	cmd/kueue/main.go
M	pkg/cache/queue/manager.go
M	pkg/cache/scheduler/cache.go
M	pkg/cache/scheduler/clusterqueue.go
M	pkg/cache/scheduler/localqueue.go
M	pkg/controller/core/core.go
M	pkg/metrics/metrics.go
M	pkg/metrics/metrics_test.go
A	pkg/util/roletracker/tracker.go
A	pkg/util/roletracker/tracker_test.go
M	test/integration/singlecluster/controller/core/clusterqueue_controller_test.go
M	test/integration/singlecluster/controller/core/suite_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/controller/core/suite_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/controller/core/suite_test.go
Auto-merging test/integration/singlecluster/controller/core/clusterqueue_controller_test.go
CONFLICT (modify/delete): pkg/util/roletracker/tracker_test.go deleted in HEAD and modified in Fix missing replica_role=leader metrics after HA role transition. Version Fix missing replica_role=leader metrics after HA role transition of pkg/util/roletracker/tracker_test.go left in tree.
CONFLICT (modify/delete): pkg/util/roletracker/tracker.go deleted in HEAD and modified in Fix missing replica_role=leader metrics after HA role transition. Version Fix missing replica_role=leader metrics after HA role transition of pkg/util/roletracker/tracker.go left in tree.
Auto-merging pkg/metrics/metrics_test.go
CONFLICT (content): Merge conflict in pkg/metrics/metrics_test.go
Auto-merging pkg/metrics/metrics.go
CONFLICT (content): Merge conflict in pkg/metrics/metrics.go
Auto-merging pkg/controller/core/core.go
Auto-merging pkg/cache/scheduler/localqueue.go
Auto-merging pkg/cache/scheduler/clusterqueue.go
Auto-merging pkg/cache/scheduler/cache.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/cache.go
Auto-merging pkg/cache/queue/manager.go
Auto-merging cmd/kueue/main.go
CONFLICT (content): Merge conflict in cmd/kueue/main.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix missing replica_role=leader metrics after HA role transition

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9487#issuecomment-4033555250):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T19:09:50Z

RoleTracker was released in v0.16 (https://github.com/kubernetes-sigs/kueue/pull/8107) so there is no need to cherry-pick to release-0.15.

cc @mimowo

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-25T18:14:05Z

- **pkg/controller/core/clusterqueue_controller.go:377** by [@mimowo](https://github.com/mimowo) — 2026-02-25T18:14:01Z
```diff
@@ -369,6 +369,31 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
+// ResyncMetrics re-reports CQ resource and weighted share gauge metrics.
+func (r *ClusterQueueReconciler) ResyncMetrics(ctx context.Context) {
+	if !r.reportResourceMetrics {
+		return
+	}
+	var cqList kueue.ClusterQueueList
```

  don't we have access to the list of CQs in the cache? listing is ok-ish but event handlers don't have error handling and the list may fail with error

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-26T15:45:58Z

- **pkg/metrics/metrics.go:850** by [@mimowo](https://github.com/mimowo) — 2026-02-26T15:45:59Z
```diff
@@ -847,6 +847,39 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var roleTaggedGauges = []*prometheus.GaugeVec{
```

  Could we automate listing those by some fliters?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T11:58:59Z

- **pkg/controller/core/clusterqueue_controller.go:377** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T11:58:59Z
```diff
@@ -369,6 +369,31 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
+// ResyncMetrics re-reports CQ resource and weighted share gauge metrics.
+func (r *ClusterQueueReconciler) ResyncMetrics(ctx context.Context) {
+	if !r.reportResourceMetrics {
+		return
+	}
+	var cqList kueue.ClusterQueueList
```

  I removed the API calls and moved resource metrics reporting into Cache.ResyncMetrics(). Thanks.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-02-27T12:02:14Z

- **pkg/metrics/metrics.go:850** by [@IrvingMg](https://github.com/IrvingMg) — 2026-02-27T12:02:13Z
```diff
@@ -847,6 +847,39 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var roleTaggedGauges = []*prometheus.GaugeVec{
```

  I attempted to introduce a helper, `newRoleTaggedGaugeVec`, to add the role label and append it to the list automatically. 
  
  However, this breaks the metrics doc generator because the helper hides `prometheus.NewGaugeVec`. 
  
  I'd suggest to handle this in a follow-up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T16:02:49Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1191** by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:02:49Z
```diff
@@ -1133,3 +1139,85 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+	})
+
+	ginkgo.AfterEach(func() {
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+	})
+
+	ginkgo.It("Should resync metrics after role transition from follower to leader", func() {
+		followerLabels := map[string]string{
+			"cluster_queue": cq.Name,
+			"replica_role":  roletracker.RoleFollower,
+		}
+
+		ginkgo.By("Waiting for CQ to become active")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updatedCQ kueue.ClusterQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updatedCQ)).To(gomega.Succeed())
+			g.Expect(apimeta.IsStatusConditionTrue(updatedCQ.Status.Conditions, kueue.ClusterQueueActive)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  We have a helper for that. ptal, I think we have a function even which already waits for the CQ to be active when creating

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T16:07:12Z

- **pkg/metrics/metrics.go:850** by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:07:11Z
```diff
@@ -847,6 +847,39 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var roleTaggedGauges = []*prometheus.GaugeVec{
```

  This is only used in roleTaggedGauges. So what I was thinking about is to list the metrics from `metrics.Registry`, and iterate over them. Then only clear those which belong to Kueue, and are guage. If this wouldn't work, then ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T16:08:58Z

- **pkg/cache/scheduler/cache.go:972** by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:08:58Z
```diff
@@ -905,6 +915,78 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.hm.ClusterQueues() {
+		metrics.ReportClusterQueueStatus(cq.Name, cq.Status, c.roleTracker)
+		cq.reportActiveWorkloads()
+		if c.resourceMetricsEnabled {
+			c.reportCQResourceMetrics(cq)
+		}
+		if features.Enabled(features.LocalQueueMetrics) {
+			for _, lq := range cq.localQueues {
+				lq.reportActiveWorkloads(c.roleTracker)
+				if c.resourceMetricsEnabled {
+					c.reportLQResourceMetrics(cq, lq)
+				}
+			}
+		}
+	}
+	if c.fairSharingEnabled {
+		for _, cohort := range c.hm.Cohorts() {
+			drs := dominantResourceShare(cohort, nil)
+			metrics.ReportCohortWeightedShare(string(cohort.Name), drs.PreciseWeightedShare(), c.roleTracker)
+		}
+	}
+}
+
+func (c *Cache) reportCQResourceMetrics(cq *clusterQueue) {
+	var cohort kueue.CohortReference
+	if cq.HasParent() {
+		cohort = cq.Parent().GetName()
+	}
+	cqName := string(cq.Name)
+	for fr, quota := range cq.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, cq.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, cq.AdmittedUsage[fr]), c.roleTracker)
+	}
+	if c.fairSharingEnabled {
+		drs := dominantResourceShare(cq, nil)
+		weightedShare := drs.PreciseWeightedShare()
+		if weightedShare == math.Inf(1) {
+			weightedShare = math.NaN()
+		}
+		metrics.ReportClusterQueueWeightedShare(cqName, string(cohort), weightedShare, c.roleTracker)
+	}
```

  This is a lot of code, don't we have already the code to report guage metrics we could call? I think we should try to commonize the code

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T18:00:35Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1191** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T18:00:35Z
```diff
@@ -1133,3 +1139,85 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+	})
+
+	ginkgo.AfterEach(func() {
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+	})
+
+	ginkgo.It("Should resync metrics after role transition from follower to leader", func() {
+		followerLabels := map[string]string{
+			"cluster_queue": cq.Name,
+			"replica_role":  roletracker.RoleFollower,
+		}
+
+		ginkgo.By("Waiting for CQ to become active")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updatedCQ kueue.ClusterQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updatedCQ)).To(gomega.Succeed())
+			g.Expect(apimeta.IsStatusConditionTrue(updatedCQ.Status.Conditions, kueue.ClusterQueueActive)).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T18:01:17Z

- **pkg/cache/scheduler/cache.go:972** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T18:01:17Z
```diff
@@ -905,6 +915,78 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.hm.ClusterQueues() {
+		metrics.ReportClusterQueueStatus(cq.Name, cq.Status, c.roleTracker)
+		cq.reportActiveWorkloads()
+		if c.resourceMetricsEnabled {
+			c.reportCQResourceMetrics(cq)
+		}
+		if features.Enabled(features.LocalQueueMetrics) {
+			for _, lq := range cq.localQueues {
+				lq.reportActiveWorkloads(c.roleTracker)
+				if c.resourceMetricsEnabled {
+					c.reportLQResourceMetrics(cq, lq)
+				}
+			}
+		}
+	}
+	if c.fairSharingEnabled {
+		for _, cohort := range c.hm.Cohorts() {
+			drs := dominantResourceShare(cohort, nil)
+			metrics.ReportCohortWeightedShare(string(cohort.Name), drs.PreciseWeightedShare(), c.roleTracker)
+		}
+	}
+}
+
+func (c *Cache) reportCQResourceMetrics(cq *clusterQueue) {
+	var cohort kueue.CohortReference
+	if cq.HasParent() {
+		cohort = cq.Parent().GetName()
+	}
+	cqName := string(cq.Name)
+	for fr, quota := range cq.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, cq.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, cq.AdmittedUsage[fr]), c.roleTracker)
+	}
+	if c.fairSharingEnabled {
+		drs := dominantResourceShare(cq, nil)
+		weightedShare := drs.PreciseWeightedShare()
+		if weightedShare == math.Inf(1) {
+			weightedShare = math.NaN()
+		}
+		metrics.ReportClusterQueueWeightedShare(cqName, string(cohort), weightedShare, c.roleTracker)
+	}
```

  Moved reporting to `clusterQueue.reportResourceMetrics()` and `LocalQueue.reportResourceMetrics()` struct methods, following the `reportActiveWorkloads` pattern.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-03T18:15:41Z

- **pkg/metrics/metrics.go:850** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-03T18:15:41Z
```diff
@@ -847,6 +847,39 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var roleTaggedGauges = []*prometheus.GaugeVec{
```

  The Prometheus client only allows reading metrics from the registry, so looks like it's not possible to clean up specific time series from it.
  
  Instead, I added a `registerGaugeVecs()` function that registers each gauge and appends it to `allGaugeVecs`, ensuring it’s included in the role transition cleanup.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-04T09:53:03Z

- **pkg/util/roletracker/tracker_test.go:110** by [@mszadkow](https://github.com/mszadkow) — 2026-03-04T09:53:03Z
```diff
@@ -89,6 +89,65 @@ func TestRoleTracker_StartLeaderElection(t *testing.T) {
 	}
 }
 
+func TestOnTransition(t *testing.T) {
+	tests := []struct {
+		name            string
+		registerCb      bool
+		closeChannel    bool
+		cancelContext   bool
+		expectCalled    bool
+		expectPrevRole  string
+		expectFinalRole string
+	}{
+		{
+			name:            "called on election with previous role",
+			registerCb:      true,
+			closeChannel:    true,
+			expectCalled:    true,
+			expectPrevRole:  RoleFollower,
+			expectFinalRole: RoleLeader,
+		},
+	}
```

  This test seems to be missing some scenarios, otherwise I don't know why we need so many options

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-05T14:48:48Z

- **pkg/util/roletracker/tracker_test.go:110** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-05T14:48:48Z
```diff
@@ -89,6 +89,65 @@ func TestRoleTracker_StartLeaderElection(t *testing.T) {
 	}
 }
 
+func TestOnTransition(t *testing.T) {
+	tests := []struct {
+		name            string
+		registerCb      bool
+		closeChannel    bool
+		cancelContext   bool
+		expectCalled    bool
+		expectPrevRole  string
+		expectFinalRole string
+	}{
+		{
+			name:            "called on election with previous role",
+			registerCb:      true,
+			closeChannel:    true,
+			expectCalled:    true,
+			expectPrevRole:  RoleFollower,
+			expectFinalRole: RoleLeader,
+		},
+	}
```

  Done. Added a couple more scenarios.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-06T08:57:26Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1172** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T08:57:26Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
```

  ```suggestion
  		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-06T08:57:38Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1188** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T08:57:38Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+	})
+
+	ginkgo.AfterEach(func() {
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+	})
+
+	ginkgo.It("Should resync metrics after role transition from follower to leader", func() {
+		followerLabels := map[string]string{
+			"cluster_queue": cq.Name,
+			"replica_role":  roletracker.RoleFollower,
+		}
+
+		ginkgo.By("Waiting for CQ to become active")
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-06T08:59:27Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T08:59:27Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
```

  ```suggestion
  	ginkgo.BeforeEach(func() {
  ```
  nit: because if we add one more test, electedChan will never become active.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-06T09:05:59Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T09:05:59Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
```

  ```suggestion
  		if _, ok := <-electedChan; ok {
  			close(electedChan)
  		}
  		trackerCancel()
  ```
  Just to make sure that we close channel on failure.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-06T09:11:04Z

- **test/integration/singlecluster/controller/core/suite_test.go:122** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T09:11:04Z
```diff
@@ -104,11 +115,20 @@ func managerAndControllerSetup(controllersCfg *config.Configuration, options ...
 
 		controllersCfg.Metrics.EnableClusterQueueResources = true
 
-		cCache := schdcache.New(mgr.GetClient())
-		queues := util.NewManagerForIntegrationTests(ctx, mgr.GetClient(), cCache)
+		cacheOpts := []schdcache.Option{
+			schdcache.WithResourceMetrics(controllersCfg.Metrics.EnableClusterQueueResources),
+		}
+		var queueOpts []qcache.Option
+		if opts.roleTracker != nil {
```

  I think we can avoid this if and propagate this options directly.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-06T09:49:59Z

- **pkg/controller/core/core.go:113** by [@kshalot](https://github.com/kshalot) — 2026-03-06T09:49:59Z
```diff
@@ -102,6 +103,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *qcache.Manager, cc *schdcache.
 	}
 	qManager.AddTopologyUpdateWatcher(cqRec)
 	qManager.AddWorkloadUpdateWatcher(qRec)
+
+	if roleTracker != nil {
+		roleTracker.OnTransition(func(previousRole string) {
+			metrics.ClearGaugeMetricsForRole(previousRole)
+			cc.ResyncMetrics()
+			qManager.ResyncMetrics()
+		})
+	}
```

  Nit: Should this be inside `SetupControllers` or higher, a little bit closer to where the role tracker is initialized (I guess this would be the `main` func)? `cc`, `qManager` and `roleTracker`  are all passed from main and I don't see any local var being referenced here so this is not a closure either.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-06T10:21:50Z

- **pkg/util/roletracker/tracker.go:59** by [@kshalot](https://github.com/kshalot) — 2026-03-06T10:12:07Z
```diff
@@ -51,12 +52,22 @@ func NewFakeRoleTracker(role string) *RoleTracker {
 	return rt
 }
 
+// OnTransition registers a callback invoked after the role changes,
+// receiving the previous role. Must be called before Start.
+func (rt *RoleTracker) OnTransition(fn func(previousRole string)) {
+	rt.onTransition = fn
+}
```

  Nit: I'm wondering about the naming, because `OnTransition` is essentially `OnElected` in our case, right? From what I understand, there is no way for this to trigger with `previousRole` being anything other than "follower". And IIUC there is no way for this to be triggered more than once per replica.
  
  I'd consider making the code a little less flexible but more explicit, unless there is some case here that I'm missing. WDYT?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:51:56Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1172** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:51:56Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:52:06Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1188** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:52:06Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.BeforeEach(func() {
+		rf = utiltestingapi.MakeResourceFlavor("ha-transition-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, rf)
+
+		cq = utiltestingapi.MakeClusterQueue("ha-transition-cq").
+			ResourceGroup(*utiltestingapi.MakeFlavorQuotas("ha-transition-flavor").Resource(corev1.ResourceCPU, "10").Obj()).
+			Obj()
+		util.MustCreate(ctx, k8sClient, cq)
+	})
+
+	ginkgo.AfterEach(func() {
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+	})
+
+	ginkgo.It("Should resync metrics after role transition from follower to leader", func() {
+		followerLabels := map[string]string{
+			"cluster_queue": cq.Name,
+			"replica_role":  roletracker.RoleFollower,
+		}
+
+		ginkgo.By("Waiting for CQ to become active")
+		util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:54:34Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1151** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:54:34Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
```

  Done. Also updated to `AfterEach`.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:56:29Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:1161** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:56:28Z
```diff
@@ -1133,3 +1139,81 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 		})
 	})
 })
+
+var _ = ginkgo.Describe("ClusterQueue controller with RoleTracker", ginkgo.Label("controller:clusterqueue", "area:core"), ginkgo.Ordered, ginkgo.Serial, func() {
+	var (
+		electedChan   chan struct{}
+		trackerCancel context.CancelFunc
+		rf            *kueue.ResourceFlavor
+		cq            *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		electedChan = make(chan struct{})
+		tracker := roletracker.NewRoleTracker(electedChan)
+		var trackerCtx context.Context
+		trackerCtx, trackerCancel = context.WithCancel(ctx)
+		go tracker.Start(trackerCtx, ctrl.Log)
+		fwk.StartManager(ctx, cfg, managerAndControllerSetup(nil, withRoleTracker(tracker)))
+	})
+
+	ginkgo.AfterAll(func() {
+		trackerCancel()
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:56:52Z

- **test/integration/singlecluster/controller/core/suite_test.go:122** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:56:52Z
```diff
@@ -104,11 +115,20 @@ func managerAndControllerSetup(controllersCfg *config.Configuration, options ...
 
 		controllersCfg.Metrics.EnableClusterQueueResources = true
 
-		cCache := schdcache.New(mgr.GetClient())
-		queues := util.NewManagerForIntegrationTests(ctx, mgr.GetClient(), cCache)
+		cacheOpts := []schdcache.Option{
+			schdcache.WithResourceMetrics(controllersCfg.Metrics.EnableClusterQueueResources),
+		}
+		var queueOpts []qcache.Option
+		if opts.roleTracker != nil {
```

  Done. Now always passes both options unconditionally.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:57:17Z

- **pkg/controller/core/core.go:113** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:57:17Z
```diff
@@ -102,6 +103,15 @@ func SetupControllers(mgr ctrl.Manager, qManager *qcache.Manager, cc *schdcache.
 	}
 	qManager.AddTopologyUpdateWatcher(cqRec)
 	qManager.AddWorkloadUpdateWatcher(qRec)
+
+	if roleTracker != nil {
+		roleTracker.OnTransition(func(previousRole string) {
+			metrics.ClearGaugeMetricsForRole(previousRole)
+			cc.ResyncMetrics()
+			qManager.ResyncMetrics()
+		})
+	}
```

  Done. Moved it to main() right after queues is created.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T12:58:37Z

- **pkg/util/roletracker/tracker.go:59** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T12:58:37Z
```diff
@@ -51,12 +52,22 @@ func NewFakeRoleTracker(role string) *RoleTracker {
 	return rt
 }
 
+// OnTransition registers a callback invoked after the role changes,
+// receiving the previous role. Must be called before Start.
+func (rt *RoleTracker) OnTransition(fn func(previousRole string)) {
+	rt.onTransition = fn
+}
```

  Good idea. I renamed it to `onElected` and remove the `previousRole` param.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-06T13:29:13Z

- **pkg/metrics/metrics.go:892** by [@kshalot](https://github.com/kshalot) — 2026-03-06T13:29:13Z
```diff
@@ -847,6 +847,18 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var allGaugeVecs []*prometheus.GaugeVec
+
+// ClearGaugeMetricsForRole deletes all gauge metric time series matching
+// replica_role=role. Called during HA role transitions to remove stale
+// time series reported under the old role.
+func ClearGaugeMetricsForRole(role string) {
+	lbls := prometheus.Labels{"replica_role": role}
+	for _, g := range allGaugeVecs {
+		g.DeletePartialMatch(lbls)
+	}
+}
```

  If there are >1 followers, won't this remove the gauges for all of them? Or are they implicitly scoped to a replica?
  
  I'm asking as a precaution, because from what I can see it was tested with 1 follower and 1 leader.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-06T14:06:55Z

- **pkg/metrics/metrics.go:892** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-06T14:06:55Z
```diff
@@ -847,6 +847,18 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var allGaugeVecs []*prometheus.GaugeVec
+
+// ClearGaugeMetricsForRole deletes all gauge metric time series matching
+// replica_role=role. Called during HA role transitions to remove stale
+// time series reported under the old role.
+func ClearGaugeMetricsForRole(role string) {
+	lbls := prometheus.Labels{"replica_role": role}
+	for _, g := range allGaugeVecs {
+		g.DeletePartialMatch(lbls)
+	}
+}
```

  This should run only on the replica that transitions. As I understand it, each replica has its own registry, so clearing them should be scoped to that replica.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T11:00:52Z

- **pkg/metrics/metrics.go:892** by [@kshalot](https://github.com/kshalot) — 2026-03-09T11:00:52Z
```diff
@@ -847,6 +847,18 @@ func ReportCohortWeightedShare(cohort string, weightedShare float64, tracker *ro
 	CohortWeightedShare.WithLabelValues(cohort, roletracker.GetRole(tracker)).Set(weightedShare)
 }
 
+var allGaugeVecs []*prometheus.GaugeVec
+
+// ClearGaugeMetricsForRole deletes all gauge metric time series matching
+// replica_role=role. Called during HA role transitions to remove stale
+// time series reported under the old role.
+func ClearGaugeMetricsForRole(role string) {
+	lbls := prometheus.Labels{"replica_role": role}
+	for _, g := range allGaugeVecs {
+		g.DeletePartialMatch(lbls)
+	}
+}
```

  Yeah, that was my understanding, but wanted to double check

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T11:27:12Z

- **pkg/cache/scheduler/clusterqueue.go:516** by [@kshalot](https://github.com/kshalot) — 2026-03-09T11:27:11Z
```diff
@@ -492,6 +493,40 @@ func (c *clusterQueue) reportActiveWorkloads() {
 	metrics.ReservingActiveWorkloads.WithLabelValues(string(c.Name), role).Set(float64(len(c.Workloads)))
 }
 
+func (c *clusterQueue) reportResourceMetrics(fairSharingEnabled bool) {
+	var cohort kueue.CohortReference
+	if c.HasParent() {
+		cohort = c.Parent().GetName()
+	}
+	cqName := string(c.Name)
+	for fr, quota := range c.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.AdmittedUsage[fr]), c.roleTracker)
+	}
```

  Unless I'm missing something, I see some overlap between this and existing code, like this:
  https://github.com/kubernetes-sigs/kueue/blob/307e822b65f945b7f0a963d25ccf3d41c9571482/pkg/controller/core/clusterqueue_controller.go#L381-L411
  
  Or this for LQ:
  https://github.com/kubernetes-sigs/kueue/blob/307e822b65f945b7f0a963d25ccf3d41c9571482/pkg/controller/core/localqueue_controller.go#L374-L385
  
  I suspect there are some differences in how to handle a total re-sync and updates like in those snippets, but maybe there is some way to re-use the logic. Or at least maybe gather it in some place? WDYT?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T12:05:25Z

- **pkg/cache/scheduler/cache.go:932** by [@kshalot](https://github.com/kshalot) — 2026-03-09T12:05:25Z
```diff
@@ -905,6 +914,38 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.hm.ClusterQueues() {
+		metrics.ReportClusterQueueStatus(cq.Name, cq.Status, c.roleTracker)
+		cq.reportActiveWorkloads()
+		if c.resourceMetricsEnabled {
+			cq.reportResourceMetrics(c.fairSharingEnabled)
+		}
+		if features.Enabled(features.LocalQueueMetrics) {
+			for _, lq := range cq.localQueues {
+				lq.reportActiveWorkloads(c.roleTracker)
+				if c.resourceMetricsEnabled {
+					lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+				}
```

  To illustrate my point from [this comment](https://github.com/kubernetes-sigs/kueue/pull/9487/files#r2904826910) - here we will only report LQ resource metrics if `c.resourceMetricsEnabled` (which is `controllersCfg.Metrics.EnableClusterQueueResources`), but I don't think this check is performed in the other LQ metrics logic, which seems to only check `features.Enabled(features.LocalQueueMetrics)`:
  https://github.com/kubernetes-sigs/kueue/blob/307e822b65f945b7f0a963d25ccf3d41c9571482/pkg/controller/core/localqueue_controller.go#L228-L230
  
  So, unless I'm missing something, there logic diverges here.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T11:37:20Z

- **pkg/cache/scheduler/clusterqueue.go:516** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T11:37:20Z
```diff
@@ -492,6 +493,40 @@ func (c *clusterQueue) reportActiveWorkloads() {
 	metrics.ReservingActiveWorkloads.WithLabelValues(string(c.Name), role).Set(float64(len(c.Workloads)))
 }
 
+func (c *clusterQueue) reportResourceMetrics(fairSharingEnabled bool) {
+	var cohort kueue.CohortReference
+	if c.HasParent() {
+		cohort = c.Parent().GetName()
+	}
+	cqName := string(c.Name)
+	for fr, quota := range c.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.AdmittedUsage[fr]), c.roleTracker)
+	}
```

  Yeah, the common part is that they call the same `metrics.Report*` functions. However, the rest differs because they read from different data sources: the cache uses maps, while the controller uses API objects. So I don’t think they can be commonized much, or that there would be much benefit in doing so.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T11:37:32Z

- **pkg/cache/scheduler/cache.go:932** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T11:37:32Z
```diff
@@ -905,6 +914,38 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.hm.ClusterQueues() {
+		metrics.ReportClusterQueueStatus(cq.Name, cq.Status, c.roleTracker)
+		cq.reportActiveWorkloads()
+		if c.resourceMetricsEnabled {
+			cq.reportResourceMetrics(c.fairSharingEnabled)
+		}
+		if features.Enabled(features.LocalQueueMetrics) {
+			for _, lq := range cq.localQueues {
+				lq.reportActiveWorkloads(c.roleTracker)
+				if c.resourceMetricsEnabled {
+					lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+				}
```

  Good catch. Removed the `c.resourceMetricsEnabled` check, LQ resource metrics should be gated only by the `LocalQueueMetrics` feature gate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:43:30Z

- **pkg/cache/scheduler/clusterqueue.go:516** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:43:30Z
```diff
@@ -492,6 +493,40 @@ func (c *clusterQueue) reportActiveWorkloads() {
 	metrics.ReservingActiveWorkloads.WithLabelValues(string(c.Name), role).Set(float64(len(c.Workloads)))
 }
 
+func (c *clusterQueue) reportResourceMetrics(fairSharingEnabled bool) {
+	var cohort kueue.CohortReference
+	if c.HasParent() {
+		cohort = c.Parent().GetName()
+	}
+	cqName := string(c.Name)
+	for fr, quota := range c.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.AdmittedUsage[fr]), c.roleTracker)
+	}
```

  I agree with @kshalot . I believe this is the same motivation and sentiment as in my comment here: https://github.com/kubernetes-sigs/kueue/pull/9487#discussion_r2865072597
  
  Sure, the code has similar structure in both places, but I cannot imagine a reviewer would notice if someone forgot to adjust the MetricsResync when introducing a new metric. A contributor may notice by following the links (function references). The reviewer is almost certainly lost, unless she/he or checkout the code.
  
  I agree that currently both functions use different data sources, but could we replace the existing occurrences with the new ones: https://github.com/kubernetes-sigs/kueue/blob/307e822b65f945b7f0a963d25ccf3d41c9571482/pkg/controller/core/clusterqueue_controller.go#L381-L411
  
  I believe the correct approach is for both places to use the cache as the data source. This is something I discussed in the past with @mbobrovskyi on the occassion of a metric for finished workloads, was that our current model of building metrics based on statuses is too constraining. I prefer the status and the metrics to both have the source in cache.
  
  wdyt @IrvingMg . If we agree it does not necessarily mean it needs to be this PR, but we could open a follow up issue to commonize the approach.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:47:17Z

- **pkg/cache/queue/manager.go:845** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:47:17Z
```diff
@@ -855,14 +834,29 @@ func (m *Manager) queueSecondPass(ctx context.Context, w *kueue.Workload, iterat
 
 	log := ctrl.LoggerFrom(ctx)
 	wInfo := workload.NewInfo(w, m.workloadInfoOptions...)
-	wInfo.UpdateSchedulingHash(log)
 	wInfo.SecondPassIteration = iteration
 	if m.secondPassQueue.queue(wInfo) {
 		log.V(3).Info("Workload queued for second pass of scheduling", "workload", workload.Key(w))
 		m.Broadcast()
 	}
 }
 
+// ResyncMetrics re-reports pending and finished workload gauge metrics.
+func (m *Manager) ResyncMetrics() {
```

  ```suggestion
  func (m *Manager) ResyncGuageMetrics() {
  ```
  nit, I think this is by design only related to guage metrics, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:47:28Z

- **pkg/cache/scheduler/cache.go:918** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:47:29Z
```diff
@@ -905,6 +914,36 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
```

  ```suggestion
  func (c *Cache) ResyncGuageMetrics() {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:47:49Z

- **cmd/kueue/main.go:342** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:47:49Z
```diff
@@ -334,6 +335,14 @@ func main() {
 		os.Exit(1)
 	}
 
+	if roleTracker != nil {
+		roleTracker.OnElected(func() {
+			metrics.ClearGaugeMetricsForRole(roletracker.RoleFollower)
+			cCache.ResyncMetrics()
+			queues.ResyncMetrics()
```

  ```suggestion
  			cCache.ResyncGuageMetrics()
  			queues.ResyncGuageMetrics()
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T18:17:26Z

- **pkg/cache/scheduler/clusterqueue.go:516** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T18:17:26Z
```diff
@@ -492,6 +493,40 @@ func (c *clusterQueue) reportActiveWorkloads() {
 	metrics.ReservingActiveWorkloads.WithLabelValues(string(c.Name), role).Set(float64(len(c.Workloads)))
 }
 
+func (c *clusterQueue) reportResourceMetrics(fairSharingEnabled bool) {
+	var cohort kueue.CohortReference
+	if c.HasParent() {
+		cohort = c.Parent().GetName()
+	}
+	cqName := string(c.Name)
+	for fr, quota := range c.resourceNode.Quotas {
+		fName, rName := string(fr.Flavor), string(fr.Resource)
+		nominal := resourceFloat(fr.Resource, quota.Nominal)
+		var borrowing, lending float64
+		if quota.BorrowingLimit != nil {
+			borrowing = resourceFloat(fr.Resource, *quota.BorrowingLimit)
+		}
+		if quota.LendingLimit != nil {
+			lending = resourceFloat(fr.Resource, *quota.LendingLimit)
+		}
+		metrics.ReportClusterQueueQuotas(cohort, cqName, fName, rName, nominal, borrowing, lending, c.roleTracker)
+		metrics.ReportClusterQueueResourceReservations(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.resourceNode.Usage[fr]), c.roleTracker)
+		metrics.ReportClusterQueueResourceUsage(cohort, cqName, fName, rName, resourceFloat(fr.Resource, c.AdmittedUsage[fr]), c.roleTracker)
+	}
```

  Okay, I've created an issue for the follow-up: https://github.com/kubernetes-sigs/kueue/issues/9793

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T18:18:17Z

- **pkg/cache/queue/manager.go:845** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T18:18:16Z
```diff
@@ -855,14 +834,29 @@ func (m *Manager) queueSecondPass(ctx context.Context, w *kueue.Workload, iterat
 
 	log := ctrl.LoggerFrom(ctx)
 	wInfo := workload.NewInfo(w, m.workloadInfoOptions...)
-	wInfo.UpdateSchedulingHash(log)
 	wInfo.SecondPassIteration = iteration
 	if m.secondPassQueue.queue(wInfo) {
 		log.V(3).Info("Workload queued for second pass of scheduling", "workload", workload.Key(w))
 		m.Broadcast()
 	}
 }
 
+// ResyncMetrics re-reports pending and finished workload gauge metrics.
+func (m *Manager) ResyncMetrics() {
```

  Right. Renamed.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T18:18:24Z

- **pkg/cache/scheduler/cache.go:918** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T18:18:24Z
```diff
@@ -905,6 +914,36 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[kueue
 	return cqs
 }
 
+// ResyncMetrics re-reports CQ/LQ status, active workload, resource, and weighted share gauge metrics.
+func (c *Cache) ResyncMetrics() {
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T18:18:31Z

- **cmd/kueue/main.go:342** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T18:18:31Z
```diff
@@ -334,6 +335,14 @@ func main() {
 		os.Exit(1)
 	}
 
+	if roleTracker != nil {
+		roleTracker.OnElected(func() {
+			metrics.ClearGaugeMetricsForRole(roletracker.RoleFollower)
+			cCache.ResyncMetrics()
+			queues.ResyncMetrics()
```

  Done.
