# PR #950: add python examples folder

**Summary**: add python examples folder

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/950

**Last updated**: 2023-07-06T22:32:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vsoch](https://github.com/vsoch)
- **Branch**: `docs/add-python-examples` → `main`
- **Created**: 2023-07-05T20:45:33Z
- **Updated**: 2023-07-06T22:32:13Z
- **Merged**: 2023-07-06T22:25:04Z
- **Closed**: 2023-07-06T22:25:04Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@moficodes](https://github.com/moficodes)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 6   **+413 / -0**

## Description

Problem: There are not examples for interacting with Kueue from Python Solution: Start a Python code samples directory. I am planning to add more to this but wanted to start with basic examples for installing Kueue and submitting a job.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

There aren't good (or any) examples of interacting with Kueue via Python.

#### Which issue(s) this PR fixes:

This will start to fix #947, however I'd like to add more complex examples to create an MPI job, a Flux Mini Cluster, and interact with queues / resource flavors from Python. I'm developing these into a workflow tool so likely will do them in subsequent PRs.

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

Sort of? It adds a README.md intended to be read by a user to the examples directory. However, there aren't changes to APIs, clients, etc, just scoped under documentation.

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-05T20:45:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5dbe3d9d307b669862084849ae6345c1ff800d29 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64a7345e46f284000798cf40 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-950--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-950--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk1MC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.VttQBvxKQmuH0zX9m_yIBG4y5I1mf2aOiD-wmbB2ItU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-05T20:45:41Z

Welcome @vsoch! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-05T20:45:42Z

Hi @vsoch. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T14:16:40Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T21:21:39Z

/release-note-edit
```release-note
NONE
```

Documentation is not part of the release, so it doesn't need a release note :)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T22:23:22Z

You rock!

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-06T22:23:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/950#issuecomment-1624384746" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/950#pullrequestreview-1516840596" title="LGTM">moficodes</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/950#" title="Author self-approved">vsoch</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-05T20:59:44Z

- **examples/python/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-05T20:59:44Z
  Can you put it in `site/content/en/docs/tasks` instead?
  Then it would make it into the website.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-05T21:03:10Z

/assign @moficodes

- **examples/python/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-05T21:01:06Z
  I would also use "Run Jobs Using Python"

- **examples/python/README.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-05T21:01:32Z
```diff
@@ -0,0 +1,88 @@
+# Kueue in Python
+
+Kueue at the core is a controller for a [Custom Resource](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/),
+and so to interact with it from Python we don't need a custom SDK, but rather we can use the generic functions provided by the
+[Kubernetes Python](https://github.com/kubernetes-client/python) library. In this directory, we provide several examples
+for interacting with Kueue in this fashion. If you would like to request a new example or would like help for a specific use
+case, please [open an issue](https://github.com/kubernetes-sigs/kueue/issues).
+
+![img/q-kueue-queue.png](img/q-kueue-queue.png)
+
+## Setup
```

  We have a generic "before you start" section in other documents

- **examples/python/README.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-05T21:02:38Z
```diff
@@ -0,0 +1,88 @@
+# Kueue in Python
+
+Kueue at the core is a controller for a [Custom Resource](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/),
+and so to interact with it from Python we don't need a custom SDK, but rather we can use the generic functions provided by the
+[Kubernetes Python](https://github.com/kubernetes-client/python) library. In this directory, we provide several examples
+for interacting with Kueue in this fashion. If you would like to request a new example or would like help for a specific use
+case, please [open an issue](https://github.com/kubernetes-sigs/kueue/issues).
+
+![img/q-kueue-queue.png](img/q-kueue-queue.png)
+
+## Setup
+
+For any of the examples above, we need a Kubernetes cluster. If you don't have a production cluster,
+you can create one with [kind](https://kind.sigs.k8s.io/docs/user/quick-start/).
+
+```bash
+$ kind create cluster
+```
+
+You'll also need kubernetes python installed. We recommend a virtual environment.
+
+```bash
+$ python -m venv env
+$ source env/bin/activate
+$ pip install -r requirements.txt
+```
+
+You can either follow the [install instructions](https://github.com/kubernetes-sigs/kueue#installation) or use the install example, below.
+
+## Examples
+
+The following examples demonstrate different use cases for using Kueue in Python.
+
+### Install
+
+This example demonstrates installing Kueue to an existing cluster.
+
+```bash
+$ python install-kueue-queues.py 
+```
+```console
+⭐️ Installing Kueue...
+⭐️ Applying queues from single-clusterqueue-setup.yaml...
+```
+
+You can also target a specific version:
+
+```bash
+$ python install-kueue-queues.py --version 0.3.2
+```
+
+### Sample Job
+
+For the next example, let's start with a cluster with Kueue installed, and first create our Kueues:
+
+```bash
+$ kubectl apply -f ../single-clusterqueue-setup.yaml 
```

  you can reference to `docs/tasks/administer_cluster_quotas`

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-05T21:17:50Z

- **examples/python/README.md:1** by [@vsoch](https://github.com/vsoch) — 2023-07-05T21:17:50Z
  Should there not be examples directly in the examples folder? What do you think about having the script files there, and then in the site retrieving them from there and dynamically adding them to the page? I definitely would like to see them in both places (but without the redundancy).

### Review by [@moficodes](https://github.com/moficodes) (changes_requested) — 2023-07-06T14:28:59Z

- **site/content/en/docs/tasks/run_python_jobs.md:17** by [@moficodes](https://github.com/moficodes) — 2023-07-06T14:23:43Z
```diff
@@ -0,0 +1,237 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+You'll also need kubernetes python installed. We recommend a virtual environment.
+
+```bash
+$ python -m venv env
```

  Remove `$` signs as they prevent from being able to copy paste.

- **site/content/en/docs/tasks/run_python_jobs.md:14** by [@moficodes](https://github.com/moficodes) — 2023-07-06T14:26:51Z
```diff
@@ -0,0 +1,237 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+You'll also need kubernetes python installed. We recommend a virtual environment.
```

  We should mention what version of Python we have used in this example.

- **site/content/en/docs/tasks/run_python_jobs.md:31** by [@moficodes](https://github.com/moficodes) — 2023-07-06T14:28:37Z
```diff
@@ -0,0 +1,237 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+You'll also need kubernetes python installed. We recommend a virtual environment.
+
+```bash
+$ python -m venv env
+$ source env/bin/activate
+$ pip install kubernetes requests
+```
+
+You can either follow the [install instructions](https://github.com/kubernetes-sigs/kueue#installation) for Kueue, or use the install example, below.
+
+## Kueue in Python
+
+Kueue at the core is a controller for a [Custom Resource](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/), and so to interact with it from Python we don't need a custom SDK, but rather we can use the generic functions provided by the
+[Kubernetes Python](https://github.com/kubernetes-client/python) library. In this guide, we provide several examples
+for interacting with Kueue in this fashion. If you would like to request a new example or would like help for a specific use
+case, please [open an issue](https://github.com/kubernetes-sigs/kueue/issues).
+
+![/images/q-kueue-queue.png](/images/q-kueue-queue.png)
```

  @alculquicondor where do we stand on having images like this in the docsite?

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T14:48:02Z

- **site/content/en/docs/tasks/run_python_jobs.md:14** by [@vsoch](https://github.com/vsoch) — 2023-07-06T14:48:02Z
```diff
@@ -0,0 +1,237 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+You'll also need kubernetes python installed. We recommend a virtual environment.
```

  Added! I also added the other two dependency libraries, kubernetes + requests and Python.

### Review by [@moficodes](https://github.com/moficodes) (changes_requested) — 2023-07-06T14:57:53Z

- **site/content/en/docs/tasks/run_python_jobs.md:31** by [@moficodes](https://github.com/moficodes) — 2023-07-06T14:56:49Z
```diff
@@ -0,0 +1,237 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Check [administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial cluster setup.
+You'll also need kubernetes python installed. We recommend a virtual environment.
+
+```bash
+$ python -m venv env
+$ source env/bin/activate
+$ pip install kubernetes requests
+```
+
+You can either follow the [install instructions](https://github.com/kubernetes-sigs/kueue#installation) for Kueue, or use the install example, below.
+
+## Kueue in Python
+
+Kueue at the core is a controller for a [Custom Resource](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/), and so to interact with it from Python we don't need a custom SDK, but rather we can use the generic functions provided by the
+[Kubernetes Python](https://github.com/kubernetes-client/python) library. In this guide, we provide several examples
+for interacting with Kueue in this fashion. If you would like to request a new example or would like help for a specific use
+case, please [open an issue](https://github.com/kubernetes-sigs/kueue/issues).
+
+![/images/q-kueue-queue.png](/images/q-kueue-queue.png)
```

  @vsoch 
  I don't think we need this here. The text covers it all.

- **site/static/images/q-kueue-queue.png:1** by [@moficodes](https://github.com/moficodes) — 2023-07-06T14:57:49Z
  Remove this as well. 🙏🏼

### Review by [@moficodes](https://github.com/moficodes) (approved) — 2023-07-06T15:45:12Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-06T21:06:24Z

- **examples/python/sample-job.py:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T21:03:37Z
```diff
@@ -0,0 +1,79 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    return parser
+
+
+def generate_job_crd(job_name):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
```

  it would be cool to get this and the queue name from params :) And maybe the requests?
  But it could be a follow up.

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T21:05:22Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  ```suggestion
  This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kubernetes from Python. For more information, see [Kueue's overview](/docs/overview).
  ```
  ?
  
  Interacting with Kueue from Python is rather what this guide is about and shouldn't be a pre-requisite.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:14:53Z

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:14:53Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  I agree - that was probably muscle memory starting with Ku and choosing poorly. :)

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:15:16Z

- **examples/python/sample-job.py:38** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:15:16Z
```diff
@@ -0,0 +1,79 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    return parser
+
+
+def generate_job_crd(job_name):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
```

  Added - also added a parameter for `--args`. What do you  mean by requests?

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:16:14Z

- **examples/python/sample-job.py:38** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:16:14Z
```diff
@@ -0,0 +1,79 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    return parser
+
+
+def generate_job_crd(job_name):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
```

  Oh resources:
  
  ```
  resources={
              "requests": {
                  "cpu": 1,
                  "memory": "200Mi",
              }
          }
  ```
  I think for an example, this hard coded low value is reasonable. This isn't intended to be a submission script to actually use, but rather just provide an example to customize.

### Review by [@moficodes](https://github.com/moficodes) (commented) — 2023-07-06T21:17:18Z

- **examples/python/sample-job.py:53** by [@moficodes](https://github.com/moficodes) — 2023-07-06T21:17:17Z
```diff
@@ -0,0 +1,91 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    parser.add_argument(
+        "--image",
+        help="container image to use",
+        default="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
+    )
+    parser.add_argument(
+        "--args",
+        nargs="+",
+        help="args for container",
+        default=["30s"],
+    )
+    return parser
+
+
+def generate_job_crd(job_name, image, args):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image=image,
+        name="dummy-job",
+        args=args,
+        resources={
+            "requests": {
```

  @vsoch I think Aldo meant this.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:18:23Z

- **examples/python/sample-job.py:53** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:18:23Z
```diff
@@ -0,0 +1,91 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    parser.add_argument(
+        "--image",
+        help="container image to use",
+        default="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
+    )
+    parser.add_argument(
+        "--args",
+        nargs="+",
+        help="args for container",
+        default=["30s"],
+    )
+    return parser
+
+
+def generate_job_crd(job_name, image, args):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image=image,
+        name="dummy-job",
+        args=args,
+        resources={
+            "requests": {
```

  This is what I was thinking - https://github.com/kubernetes-sigs/kueue/pull/950#discussion_r1254927598 - do you think that’s ok?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-06T21:19:50Z

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T21:19:50Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  did you forget to commit? 😅

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-06T21:20:31Z

- **examples/python/sample-job.py:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T21:20:30Z
```diff
@@ -0,0 +1,79 @@
+#!/usr/bin/env python3
+
+import argparse
+from kubernetes import config, client
+
+# create_job.py
+# This example will demonstrate full steps to submit a Job.
+
+# Make sure your cluster is running!
+config.load_kube_config()
+crd_api = client.CustomObjectsApi()
+api_client = crd_api.api_client
+
+
+def get_parser():
+    parser = argparse.ArgumentParser(
+        description="Submit Kueue Job Example",
+        formatter_class=argparse.RawTextHelpFormatter,
+    )
+    parser.add_argument(
+        "--job-name",
+        help="generateName field to set for job",
+        default="sample-job-",
+    )
+    return parser
+
+
+def generate_job_crd(job_name):
+    """
+    Generate an equivalent job CRD to sample-job.yaml
+    """
+    metadata = client.V1ObjectMeta(
+        generate_name=job_name, labels={"kueue.x-k8s.io/queue-name": "user-queue"}
+    )
+
+    # Job container
+    container = client.V1Container(
+        image="gcr.io/k8s-staging-perf-tests/sleep:v0.0.3",
```

  right, we don't need to bloat the script :)

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:36:46Z

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:36:46Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  lol I think I changed the wrong one. I guess that's worse :laughing:

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:39:27Z

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:39:27Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  ![image](https://github.com/kubernetes-sigs/kueue/assets/814322/b1338bba-bfe8-4b0d-aecc-5df65c28ddb8)

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-06T21:39:42Z

- **site/content/en/docs/tasks/run_python_jobs.md:9** by [@vsoch](https://github.com/vsoch) — 2023-07-06T21:39:42Z
```diff
@@ -0,0 +1,241 @@
+---
+title: "Run Jobs Using Python"
+date: 2023-07-05
+weight: 7
+description: >
+  Run Kueue jobs programmatically with Python
+---
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of interacting with Kueue from Python. For more information, see [Kueue's overview](/docs/overview).
```

  _I had one job..._
