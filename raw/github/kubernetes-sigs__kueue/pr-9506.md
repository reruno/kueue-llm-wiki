# PR #9506: feat: add multi-layer topology constraints for TAS

**Summary**: feat: add multi-layer topology constraints for TAS

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9506

**Last updated**: 2026-03-21T22:12:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Huang-Wei](https://github.com/Huang-Wei)
- **Branch**: `ft-multilayer-tas` → `main`
- **Created**: 2026-02-26T07:12:06Z
- **Updated**: 2026-03-21T22:12:54Z
- **Merged**: 2026-03-06T09:44:20Z
- **Closed**: 2026-03-06T09:44:20Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd)
- **Changed files**: 25   **+1869 / -56**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Introduce the TASMultiLayerTopology feature gate (alpha) to support
up to 3 hierarchical slice layers in topology-aware scheduling.

- Add `PodsetSliceRequiredTopologyConstraints` field to
  `PodSetTopologyRequest` and the corresponding pod annotation
- Extend TAS flavor snapshot to handle multi-layer placement
- Add validation for the new field (mutual exclusivity with
  existing single-layer fields, layer ordering, size divisibility)
- Add v1beta1 <-> v1beta2 conversion support
- Add integration tests for multi-layer scheduling

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9046

#### Special notes for your reviewer:

> [!NOTE]
> This PR aims to impose minimum changes to the existing 2-layer annotation and impl.,
> so enabling or disabling this feature gate will have a minimal impact.

#### Does this PR introduce a user-facing change?

```release-note
TAS: Introduce the `TASMultiLayerTopology` feature gate (alpha) to support up to 3 hierarchical slice layers
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-26T07:12:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 22defd7f88a28889de8f4ceb548beb45d7a471d1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69aa9165f5d59b00080120ab |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9506--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9506--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk1MDYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.9wpF9VoX0etY7vnRlQaE_X8qNt-3z3BEegjQfs0nbsU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9506--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-02-26T07:57:56Z

cc @mimowo @tenzen-y @kannon92 PTAL when you get a chance, thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-26T08:10:06Z

> cc @mimowo @tenzen-y @kannon92 PTAL when you get a chance, thanks!

ack, I will try to get here tomorrow

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-03T07:23:29Z

Kindly ping @mimowo @tenzen-y :)

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-04T11:55:15Z

Im yet to review the algo itself

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T08:09:21Z

Thanks @mimowo ! I resolved most of the comments I think.

Regarding:

> 1 NodeHotSwap support , I suppose requiredReplacementDomain may require an adjustment to only look for replacements within the innermost .
> 3 I suppose the messages in case of the inner slices might be suboptimal because the PR does not currently adjust notFitMessage

I updated some logic in `findReplacementAssignment()` - where I got NPE initially, and used UT `TestFindTopologyAssignmentsMultiLayerReplacement` to guard them. But they're just preventive code. I will check  requiredReplacementDomain() to raise follow-ups if needed.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T11:42:14Z

/approve
Thank you @Huang-Wei, lgtm, please rebase. And let's follow up with the NodeHotSwap improvements to make sure the newly constructed assignment conforms to the restrictions.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T08:42:09Z

Thank you, this is super useful 👍 Let's revisit the NodeHotSwap support, and better NoFit messages in follow ups.
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T08:42:17Z

LGTM label has been added.  <details>Git tree hash: 452d186b929ba4a6ddbc5bc75658fce5c2f8da1e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T08:42:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9506#" title="Author self-approved">Huang-Wei</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9506#issuecomment-4010369809" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-06T08:53:36Z

@mimowo really appreciate your thorough review!

> Let's revisit the NodeHotSwap support, and better NoFit messages in follow ups.

I will raise follow up prs as well as doc pr.

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-06T09:17:38Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-21T22:12:54Z

/release-note-edit
```release-note
TAS: Introduce the `TASMultiLayerTopology` feature gate (alpha) to support up to 3 hierarchical slice layers
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T11:54:55Z

- **pkg/controller/jobframework/tas_test.go:36** by [@mimowo](https://github.com/mimowo) — 2026-03-04T10:34:31Z
```diff
@@ -28,16 +28,18 @@ import (
 	jobsetapi "sigs.k8s.io/jobset/api/jobset/v1alpha2"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func TestPodSetTopologyRequestBuilder(t *testing.T) {
 	testCases := map[string]struct {
-		meta               *metav1.ObjectMeta
-		podIndexLabel      *string
-		subGroupIndexLabel *string
-		subGroupCount      *int32
-		wantReq            *kueue.PodSetTopologyRequest
-		wantErr            error
+		enableTASMultiLayer bool
```

  nit, maybe we could introduce generic `featureGates: map[featuretGate]: bool`, we do it already here: https://github.com/kubernetes-sigs/kueue/blob/5d9e98e23f51d283da2a8f30571ff48bc391b1f1/pkg/controller/jobframework/validation_test.go#L261 
  and it is very convenient

- **pkg/controller/jobframework/tas_test.go:222** by [@mimowo](https://github.com/mimowo) — 2026-03-04T10:38:02Z
```diff
@@ -192,9 +194,77 @@ func TestPodSetTopologyRequestBuilder(t *testing.T) {
 			},
 			wantErr: strconv.ErrSyntax,
 		},
+		"multi-layer: three constraint layers with feature gate": {
```

  Let's also have a test case with 0 levels and 4 levels. I think having 0 levels doesn't make sense for the annotation too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:11:33Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:788** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:11:34Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
```

  Extract building the map to a helper function, to offload the already big main function. The comment could follow the function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:12:23Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:792** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:12:24Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
+		prevSize := sliceSize
+		prevLevelIdx := sliceLevelIdx
+		// TODO: once TASMultiLayerTopology graduates to beta, use "PodsetSliceRequiredTopologyConstraints" as
+		// first-class citizen to generalize the logic of handling N-layer topology constraint.
```

  What are actually code changes this entails? It would be good to clarify.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:14:34Z

- **pkg/controller/jobframework/tas_validation.go:344** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:14:34Z
```diff
@@ -271,3 +291,88 @@ func topologyRequestsValid(r1, r2 *kueue.PodSetTopologyRequest) bool {
 	// Check that the non-nil pair has the same value.
 	return ptr.Equal(r1.Required, r2.Required) && ptr.Equal(r1.Preferred, r2.Preferred)
 }
+
+// validateSliceRequiredTopologyConstraintsAnnotation validates the
+// multi-layer topology constraints annotation syntactically.
+// NOTE: Coarsest-to-finest ordering is NOT validated here because the
+// Topology CR's level hierarchy is unavailable at admission time (the
+// ResourceFlavor is assigned by the scheduler). Ordering validation
+// happens at scheduling time in TASFlavorSnapshot.findTopologyAssignment.
+func validateSliceRequiredTopologyConstraintsAnnotation(annotationsPath *field.Path, replicaMetadata *metav1.ObjectMeta, sliceRequiredFound bool, sliceSizeFound bool, podSetGroupNameFound bool) field.ErrorList {
+	var allErrs field.ErrorList
+
+	constraintsJSON, constraintsFound := replicaMetadata.Annotations[kueue.PodSetSliceRequiredTopologyConstraintsAnnotation]
+	if !constraintsFound {
+		return nil
+	}
+
+	fldPath := annotationsPath.Key(kueue.PodSetSliceRequiredTopologyConstraintsAnnotation)
+
+	if !features.Enabled(features.TASMultiLayerTopology) {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("the %s feature gate must be enabled to use this annotation", features.TASMultiLayerTopology)))
+		return allErrs
+	}
+
+	// Mutual exclusivity with two-level fields.
+	if sliceRequiredFound {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceRequiredTopologyAnnotation)))
+	}
+	if sliceSizeFound {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceSizeAnnotation)))
+	}
+
+	// Incompatible with podset-group-name.
+	if podSetGroupNameFound {
+		allErrs = append(allErrs, field.Forbidden(annotationsPath.Key(kueue.PodSetGroupName),
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceRequiredTopologyConstraintsAnnotation)))
+	}
+
+	// Parse JSON.
+	var constraints []kueue.PodsetSliceRequiredTopologyConstraint
+	if err := json.Unmarshal([]byte(constraintsJSON), &constraints); err != nil {
+		allErrs = append(allErrs, field.Invalid(fldPath, constraintsJSON, fmt.Sprintf("must be a valid JSON array: %v", err)))
+		return allErrs
+	}
+
+	if len(constraints) == 0 {
+		allErrs = append(allErrs, field.Invalid(fldPath, constraintsJSON, "must contain at least 1 entry"))
+		return allErrs
+	}
+	if len(constraints) > 3 {
```

  nit, but let's define a named const for that. If only used in this function, then we could scope it to the function. Still, the named const will be more readiable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:21:01Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:787** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:21:01Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
```

  Please check with some unit test coverage tool for golang if the new unit test cover 100% of the new code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:24:13Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:797** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:24:14Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
+		prevSize := sliceSize
+		prevLevelIdx := sliceLevelIdx
+		// TODO: once TASMultiLayerTopology graduates to beta, use "PodsetSliceRequiredTopologyConstraints" as
+		// first-class citizen to generalize the logic of handling N-layer topology constraint.
+		minusOneLayers := workersTasPodSetRequests.PodSet.TopologyRequest.PodsetSliceRequiredTopologyConstraints
+		if len(minusOneLayers) > 1 {
+			minusOneLayers = minusOneLayers[1:] // skip the first (outermost) layer, already handled above
+		} else {
+			minusOneLayers = nil
```

  nit, is this needed? If the slice is empty than already the iteration below will skip

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:29:45Z

- **pkg/cache/scheduler/tas_cache_test.go:5405** by [@mimowo](https://github.com/mimowo) — 2026-03-04T16:29:46Z
```diff
@@ -5398,6 +5402,234 @@ func TestFindTopologyAssignments(t *testing.T) {
 			}},
 			enableFeatureGates: []featuregate.Feature{features.ElasticJobsViaWorkloadSlices, features.ElasticJobsViaWorkloadSlicesWithTAS},
 		},
+		"multi-layer topology: block required; rack slices of 4; host slices of 2; TASMultiLayerTopology": {
```

  Please also add some negative test cases, where the Workload could fit if not for the multi-level requirements

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T16:38:17Z

LGTM overall. Excellent work  👍

 Aside from the comments on the diff I suspect improvements might be needed:
1. NodeHotSwap support , I suppose `requiredReplacementDomain` may require an adjustment to only look for replacements within the innermost . 
2. I would like to have unit tests in scheduler_tas_test.go, one positive and one negative to demonstrate the messages when NoFit due to the addional constraints
3. I suppose the messages in case of the inner slices might be suboptimal because the PR does not currently adjust `notFitMessage`

Even if (1.) and (3.) require extra work I'm ok to leave it for follow PRs, because this is still alpha, and to make reviewing easier - focused on specific problem like NodeHotSwap or failure messages.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:53:19Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:797** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:53:19Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
+		prevSize := sliceSize
+		prevLevelIdx := sliceLevelIdx
+		// TODO: once TASMultiLayerTopology graduates to beta, use "PodsetSliceRequiredTopologyConstraints" as
+		// first-class citizen to generalize the logic of handling N-layer topology constraint.
+		minusOneLayers := workersTasPodSetRequests.PodSet.TopologyRequest.PodsetSliceRequiredTopologyConstraints
+		if len(minusOneLayers) > 1 {
+			minusOneLayers = minusOneLayers[1:] // skip the first (outermost) layer, already handled above
+		} else {
+			minusOneLayers = nil
```

  When len(minusOneLayers) == 1, previously the 1-layer topology constraint has been processed (re-use old logic to strip layers[0]), so here we don't need to process it again and hence I drop it.
  
  It's a bit nuanced I know, that's why I mentioned the alpha impl. is intentional to try not introducing massive change to existing logic (see `Special notes for your reviewer` section).

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:54:12Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:787** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:54:12Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
```

  It's around ~86%:
  
  ```
  ⇒  go tool cover -func=/tmp/tas_cover.out | grep buildSliceSizeAtLevel
  sigs.k8s.io/kueue/pkg/cache/scheduler/tas_flavor_snapshot.go:942:	buildSliceSizeAtLevel			86.4%
  ```
  
  and missing hits are mostly about validation error paths for malformed constraint inputs (like `prevSize%layer.Size != 0`)

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:56:08Z

- **pkg/controller/jobframework/tas_validation.go:344** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:56:08Z
```diff
@@ -271,3 +291,88 @@ func topologyRequestsValid(r1, r2 *kueue.PodSetTopologyRequest) bool {
 	// Check that the non-nil pair has the same value.
 	return ptr.Equal(r1.Required, r2.Required) && ptr.Equal(r1.Preferred, r2.Preferred)
 }
+
+// validateSliceRequiredTopologyConstraintsAnnotation validates the
+// multi-layer topology constraints annotation syntactically.
+// NOTE: Coarsest-to-finest ordering is NOT validated here because the
+// Topology CR's level hierarchy is unavailable at admission time (the
+// ResourceFlavor is assigned by the scheduler). Ordering validation
+// happens at scheduling time in TASFlavorSnapshot.findTopologyAssignment.
+func validateSliceRequiredTopologyConstraintsAnnotation(annotationsPath *field.Path, replicaMetadata *metav1.ObjectMeta, sliceRequiredFound bool, sliceSizeFound bool, podSetGroupNameFound bool) field.ErrorList {
+	var allErrs field.ErrorList
+
+	constraintsJSON, constraintsFound := replicaMetadata.Annotations[kueue.PodSetSliceRequiredTopologyConstraintsAnnotation]
+	if !constraintsFound {
+		return nil
+	}
+
+	fldPath := annotationsPath.Key(kueue.PodSetSliceRequiredTopologyConstraintsAnnotation)
+
+	if !features.Enabled(features.TASMultiLayerTopology) {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("the %s feature gate must be enabled to use this annotation", features.TASMultiLayerTopology)))
+		return allErrs
+	}
+
+	// Mutual exclusivity with two-level fields.
+	if sliceRequiredFound {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceRequiredTopologyAnnotation)))
+	}
+	if sliceSizeFound {
+		allErrs = append(allErrs, field.Forbidden(fldPath,
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceSizeAnnotation)))
+	}
+
+	// Incompatible with podset-group-name.
+	if podSetGroupNameFound {
+		allErrs = append(allErrs, field.Forbidden(annotationsPath.Key(kueue.PodSetGroupName),
+			fmt.Sprintf("may not be set when '%s' is specified", kueue.PodSetSliceRequiredTopologyConstraintsAnnotation)))
+	}
+
+	// Parse JSON.
+	var constraints []kueue.PodsetSliceRequiredTopologyConstraint
+	if err := json.Unmarshal([]byte(constraintsJSON), &constraints); err != nil {
+		allErrs = append(allErrs, field.Invalid(fldPath, constraintsJSON, fmt.Sprintf("must be a valid JSON array: %v", err)))
+		return allErrs
+	}
+
+	if len(constraints) == 0 {
+		allErrs = append(allErrs, field.Invalid(fldPath, constraintsJSON, "must contain at least 1 entry"))
+		return allErrs
+	}
+	if len(constraints) > 3 {
```

  done via `errTopologyConstraintsLayerCount`

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:56:28Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:792** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:56:28Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
+		prevSize := sliceSize
+		prevLevelIdx := sliceLevelIdx
+		// TODO: once TASMultiLayerTopology graduates to beta, use "PodsetSliceRequiredTopologyConstraints" as
+		// first-class citizen to generalize the logic of handling N-layer topology constraint.
```

  Update the comments.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:56:36Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:788** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:56:35Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
```

  Done.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:56:59Z

- **pkg/cache/scheduler/tas_cache_test.go:5405** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:56:59Z
```diff
@@ -5398,6 +5402,234 @@ func TestFindTopologyAssignments(t *testing.T) {
 			}},
 			enableFeatureGates: []featuregate.Feature{features.ElasticJobsViaWorkloadSlices, features.ElasticJobsViaWorkloadSlicesWithTAS},
 		},
+		"multi-layer topology: block required; rack slices of 4; host slices of 2; TASMultiLayerTopology": {
```

  Done.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:57:26Z

- **pkg/controller/jobframework/tas_test.go:36** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:57:26Z
```diff
@@ -28,16 +28,18 @@ import (
 	jobsetapi "sigs.k8s.io/jobset/api/jobset/v1alpha2"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
 )
 
 func TestPodSetTopologyRequestBuilder(t *testing.T) {
 	testCases := map[string]struct {
-		meta               *metav1.ObjectMeta
-		podIndexLabel      *string
-		subGroupIndexLabel *string
-		subGroupCount      *int32
-		wantReq            *kueue.PodSetTopologyRequest
-		wantErr            error
+		enableTASMultiLayer bool
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T07:57:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:787** by [@mimowo](https://github.com/mimowo) — 2026-03-05T07:57:42Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
```

  I see, yeah lets make sure we cover 100 percent for the positive scenarios

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-05T07:57:55Z

- **pkg/controller/jobframework/tas_test.go:222** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-05T07:57:55Z
```diff
@@ -192,9 +194,77 @@ func TestPodSetTopologyRequestBuilder(t *testing.T) {
 			},
 			wantErr: strconv.ErrSyntax,
 		},
+		"multi-layer: three constraint layers with feature gate": {
```

  Actually I missed the logic to guard 0 and >=3, added the tests and logic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T11:32:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:797** by [@mimowo](https://github.com/mimowo) — 2026-03-05T11:32:21Z
```diff
@@ -777,6 +778,46 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 		return nil, fmt.Sprintf("podset slice topology %s is above the podset topology %s", sliceTopologyKey, *topologyKey)
 	}
 
+	// Build the map of slice sizes per level for multi-layer support.
+	// sliceSizeAtLevel map is keyed with topology level index and valued with the slice size that
+	// applies when distributing pods into domains at that level.
+	// All levels between the outermost slice level and the innermost additional layer
+	// must distribute in multiples of the inner layer's size to ensure
+	// clean grouping at the leaf level.
+	sliceSizeAtLevel := make(map[int]int32)
+	if features.Enabled(features.TASMultiLayerTopology) && workersTasPodSetRequests.PodSet.TopologyRequest != nil {
+		prevSize := sliceSize
+		prevLevelIdx := sliceLevelIdx
+		// TODO: once TASMultiLayerTopology graduates to beta, use "PodsetSliceRequiredTopologyConstraints" as
+		// first-class citizen to generalize the logic of handling N-layer topology constraint.
+		minusOneLayers := workersTasPodSetRequests.PodSet.TopologyRequest.PodsetSliceRequiredTopologyConstraints
+		if len(minusOneLayers) > 1 {
+			minusOneLayers = minusOneLayers[1:] // skip the first (outermost) layer, already handled above
+		} else {
+			minusOneLayers = nil
```

  I see, sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T11:41:05Z

- **pkg/cache/scheduler/tas_cache_test.go:5742** by [@mimowo](https://github.com/mimowo) — 2026-03-05T11:41:05Z
```diff
@@ -5630,6 +5630,118 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"multi-layer topology: host slice rounding makes rack slice impossible": {
+			// block → rack → hostname
+			//        b1
+			//        |
+			//       r1
+			//      /    \
+			//   x1(3)  x2(3)
+			enableFeatureGates: []featuregate.Feature{features.TASMultiLayerTopology},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Required: ptr.To(tasBlockLabel),
+					PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+						{Topology: tasRackLabel, Size: 6},
+						{Topology: corev1.LabelHostname, Size: 2},
+					},
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count:      6,
+				wantReason: `topology "default" doesn't allow to fit any of 1 slice(s)`,
+			}},
+		},
+		"multi-layer topology: small host kills rack slices despite enough total capacity": {
+			// block → rack → hostname
+			//              b1
+			//       /             \
+			//      r1             r2
+			//   /      \        /    \
+			//  x1(7)  x2(3)  x3(7)  x4(3)
+			enableFeatureGates: []featuregate.Feature{features.TASMultiLayerTopology},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("7"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2-x3").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r2").
+					Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("7"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2-x4").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r2").
+					Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Required: ptr.To(tasBlockLabel),
+					PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+						{Topology: tasRackLabel, Size: 8},
+						{Topology: corev1.LabelHostname, Size: 4},
+					},
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count:      16,
+				wantReason: `topology "default" doesn't allow to fit any of 2 slice(s)`,
```

  I'm wondering how we could improve the message here to be useful.
  
  Maybe we could describe the next domain which could fit some of the Pods, but not everything (next next best domain). 
  
  So, maybe say: `topology "default" doesn't allow to fit any of 2 slice(s); the next domain "xyz" could only fit 1/2 sub-slices at the kubernetes.io/hostname level`. 
  
  However, I'm not totally convinced this is the best approach, we may wait for the user feedback here / see what is useful for debugging.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-06T08:39:05Z

- **pkg/cache/scheduler/tas_cache_test.go:5742** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-06T08:39:05Z
```diff
@@ -5630,6 +5630,118 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"multi-layer topology: host slice rounding makes rack slice impossible": {
+			// block → rack → hostname
+			//        b1
+			//        |
+			//       r1
+			//      /    \
+			//   x1(3)  x2(3)
+			enableFeatureGates: []featuregate.Feature{features.TASMultiLayerTopology},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Required: ptr.To(tasBlockLabel),
+					PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+						{Topology: tasRackLabel, Size: 6},
+						{Topology: corev1.LabelHostname, Size: 2},
+					},
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count:      6,
+				wantReason: `topology "default" doesn't allow to fit any of 1 slice(s)`,
+			}},
+		},
+		"multi-layer topology: small host kills rack slices despite enough total capacity": {
+			// block → rack → hostname
+			//              b1
+			//       /             \
+			//      r1             r2
+			//   /      \        /    \
+			//  x1(7)  x2(3)  x3(7)  x4(3)
+			enableFeatureGates: []featuregate.Feature{features.TASMultiLayerTopology},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-x1").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("7"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r1-x2").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r1").
+					Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2-x3").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r2").
+					Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("7"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+				*testingnode.MakeNode("b1-r2-x4").
+					Label(tasBlockLabel, "b1").
+					Label(tasRackLabel, "r2").
+					Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("3"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).
+					Ready().
+					Obj(),
+			},
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Required: ptr.To(tasBlockLabel),
+					PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+						{Topology: tasRackLabel, Size: 8},
+						{Topology: corev1.LabelHostname, Size: 4},
+					},
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count:      16,
+				wantReason: `topology "default" doesn't allow to fit any of 2 slice(s)`,
```

  Given we have a restriction on # of layers, I may lean towards dump the stats of error reasons on all layers, like:
  
  ```
  topology "default" doesn't allow to fit; 1/2 slice(s) fit on level cloud.com/topology-rack; 3/4 slice(s) fit on level kubernetes.io/hostname
  ```
  
  If we go w/ this, the current err msg pattern doesn't quite match b/c it's based on single-layer:
  
  https://github.com/kubernetes-sigs/kueue/blob/2b0916e12fdcb73a63ceffaa6ec6313cd30099a8/pkg/cache/scheduler/tas_flavor_snapshot.go#L1497-L1501
  
  so I may come up w/ a standalone err msg builder for it, and unify it when this feature reaches to beta.
  
  I will need some time to think about it. (code is rebased btw)
