# PR #954: [site] Document partial admission and feature gate usage.

**Summary**: [site] Document partial admission and feature gate usage.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/954

**Last updated**: 2023-07-07T13:09:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `partial-admission-doc` → `website-release-0.4`
- **Created**: 2023-07-06T14:15:39Z
- **Updated**: 2023-07-07T13:09:10Z
- **Merged**: 2023-07-07T13:09:05Z
- **Closed**: 2023-07-07T13:09:05Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 3   **+98 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Document partial admission and feature gate usage.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #935

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@trasc](https://github.com/trasc) — 2023-07-06T14:17:38Z

/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-07T13:08:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/954#pullrequestreview-1518836112" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/954#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/website-release-0.4/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-06T20:58:06Z

User documentation is generally written using the second person.

- **site/content/en/docs/installation/_index.md:142** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T19:29:33Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+
+In order to change the default of a feature, a batch administrator needs to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include 
+
+```
+--feature-gates=...,<FeatureName>=<true|false>
+```
+
+For example, to enable `PartialAdmission`, one should change the manager deployment as follows:
```

  ```suggestion
  For example, to enable `PartialAdmission`, you should change the manager deployment as follows:
  ```

- **site/content/en/docs/installation/_index.md:136** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T19:29:56Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+
+In order to change the default of a feature, a batch administrator needs to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include 
```

  ```suggestion
  In order to change the default of a feature, you need to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include 
  ```

- **site/content/en/docs/installation/_index.md:134** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T19:31:07Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
```

  ```suggestion
  Kueue uses a similar mechanism to enable features in alpha or beta stages as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
  ```

- **site/content/en/docs/tasks/run_jobs.md:228** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T19:34:50Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
```

  ```suggestion
  From version v0.4.0, Kueue provides the ability for a batch user to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit within the available quota.
  ```

- **site/content/en/docs/tasks/run_jobs.md:230** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T20:54:37Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
```

  ```suggestion
  Kueue will only attempt to decrease the parallelism after both _borrowing_ and _preemption_ was taken into account in the admission process, and none of them are feasible.
  ```

- **site/content/en/docs/tasks/run_jobs.md:232** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T20:55:58Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
```

  ```suggestion
  To allow partial admission you can provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted, its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
  ```

- **site/content/en/docs/tasks/run_jobs.md:234** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T20:56:24Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
+
+For example a Job defined as:
```

  ```suggestion
  For example, for a Job defined by the following manifest:
  ```

- **site/content/en/docs/tasks/run_jobs.md:262** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-06T20:57:24Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
+
+For example a Job defined as:
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-partial-admission
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+  annotations:
+    kueue.x-k8s.io/job-min-parallelism: "5"
+spec:
+  parallelism: 20
+  completions: 20
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+running in a queue with only 9 CPU available, it will be admitted with `Pn` of 9 and executed in 3 steps,9 + 9 + 2 completions.
```

  ```suggestion
  When queued in a ClusterQueue with only 9 CPUs available, it will be admitted with `parallelism=9`. Note that the number of completions doesn't change.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-07T05:14:15Z

- **site/content/en/docs/installation/_index.md:134** by [@trasc](https://github.com/trasc) — 2023-07-07T05:01:10Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
```

  partially done, however there is no restriction that says that you can only enable or can only be used for alpha or beta features.

- **site/content/en/docs/installation/_index.md:136** by [@trasc](https://github.com/trasc) — 2023-07-07T05:01:59Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+
+In order to change the default of a feature, a batch administrator needs to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include 
```

  done

- **site/content/en/docs/installation/_index.md:142** by [@trasc](https://github.com/trasc) — 2023-07-07T05:02:33Z
```diff
@@ -129,6 +129,40 @@ more about using `waitForPodsReady` for Kueue.
 kubectl apply -f manifests.yaml
 ```
 
+## Change the feature gates configuration
+
+Kueue uses a similar mechanism to configure optional feature as described in [Kubernetes Feature Gates](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates).
+
+In order to change the default of a feature, a batch administrator needs to edit the `kueue-controller-manager` deployment within the kueue installation namespace and change the `manager` container arguments to include 
+
+```
+--feature-gates=...,<FeatureName>=<true|false>
+```
+
+For example, to enable `PartialAdmission`, one should change the manager deployment as follows:
```

  done

- **site/content/en/docs/tasks/run_jobs.md:228** by [@trasc](https://github.com/trasc) — 2023-07-07T05:05:16Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
```

  done

- **site/content/en/docs/tasks/run_jobs.md:230** by [@trasc](https://github.com/trasc) — 2023-07-07T05:07:58Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
```

  done

- **site/content/en/docs/tasks/run_jobs.md:232** by [@trasc](https://github.com/trasc) — 2023-07-07T05:10:02Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
```

  done

- **site/content/en/docs/tasks/run_jobs.md:234** by [@trasc](https://github.com/trasc) — 2023-07-07T05:11:10Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
+
+For example a Job defined as:
```

  done

- **site/content/en/docs/tasks/run_jobs.md:262** by [@trasc](https://github.com/trasc) — 2023-07-07T05:13:04Z
```diff
@@ -222,3 +222,43 @@ Events:
 
 Since events have a timestamp with a resolution of seconds, the events might
 be listed in a slightly different order from which they actually occurred.
+
+## Partial admission
+
+From version v0.4.0, `kueue` provides the ability to a batch user, to create Jobs that ideally will run with a parallelism `P0` but can accept accept a smaller parallelism, `Pn`, if the Job dose not fit in the queue.
+
+Kueue will only attempt to decrease the parallelism after both `borrowing` and `premption` was taken into account, in the scheduling process, and none of them are available.
+
+To achieve this, the batch user should provide the minimum acceptable parallelism `Pmin` in `kueue.x-k8s.io/job-min-parallelism` annotation of the Job, `Pn` should be grater that 0 and less that `P0`. When a Job is partially admitted its parallelism will be set to `Pn`, `Pn` will be set to the maximum acceptable value between `Pmin` and `P0`. The Job's completions count will not be changed.
+
+For example a Job defined as:
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job-partial-admission
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+  annotations:
+    kueue.x-k8s.io/job-min-parallelism: "5"
+spec:
+  parallelism: 20
+  completions: 20
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+running in a queue with only 9 CPU available, it will be admitted with `Pn` of 9 and executed in 3 steps,9 + 9 + 2 completions.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-07T13:08:04Z

/lgtm
/approve
