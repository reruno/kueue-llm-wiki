# PR #9541: Add information about scheduling gates to pod integration and add troubleshooting

**Summary**: Add information about scheduling gates to pod integration and add troubleshooting

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9541

**Last updated**: 2026-04-21T19:28:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `scheduling-gate-pod-integration` → `main`
- **Created**: 2026-02-26T21:49:19Z
- **Updated**: 2026-04-21T19:28:33Z
- **Merged**: 2026-04-21T19:27:53Z
- **Closed**: 2026-04-21T19:27:53Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+12 / -1**

## Description



<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a section on scheduling gates for pod integration.
Mention what happens if you use an external schedulingGate and add a section to troubleshooting.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-02-26T21:49:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5e6e5c0751bfe1a40625bec73ef3db79687f531f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e65364367d790007bd5577 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9541--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9541--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk1NDEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.V8C53DeS9aMdPlXVNB7DWSCslKjfIRjPVu0HYFpLyt0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9541--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T17:53:34Z

cc @MichalZylinski

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-17T18:22:15Z

/assign @mimowo @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T16:26:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9541#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9541#pullrequestreview-4141735938" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:26:45Z

/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-20T16:26:48Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9541#issuecomment-4282541016):

>/cherrypick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-21T18:58:22Z

/assign @mimowo 

Needing a lgtm.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T19:11:09Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-21T19:11:16Z

LGTM label has been added.  <details>Git tree hash: 6e6c23b606dbbde95bb69fea14bb9e2eba83f47a</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T19:28:33Z

@tenzen-y: new pull request created: #10661

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9541#issuecomment-4282541016):

>/cherrypick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-02-27T03:18:39Z

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T03:13:25Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
```

  ```suggestion
  Once the pods are able to be admitted, kueue will patch the pods and remove this scheduling gate.
  ```
  
  nits

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@tenzen-y](https://github.com/tenzen-y) — 2026-02-27T03:18:32Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  Is this really a Pod integration limitation? I think that this is a generic Kueue behavior regardless of scheduling gates owner.
  
  For example, TAS workloads get Kueue scheduling Gates regardless of Job type (not only for Pod) when queueing. Even after admission, the Pods still have Kueue scheduling gates. After that TAS ungater will ungate Kueue schedulingGates.
  
  In other words, regardless of scheduling gates owner (Kueue or others), the Admitted Pods could have scheduling gates.
  
  cc @mimowo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-27T03:21:37Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@kannon92](https://github.com/kannon92) — 2026-02-27T03:21:37Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  I can remove this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-02-27T06:27:57Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@mimowo](https://github.com/mimowo) — 2026-02-27T06:27:56Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  Indeed, I would rather see a subpage in https://kueue.sigs.k8s.io/docs/tasks/manage/, maybe something like "Using custom scheduling-gates", where you could detail the user story for OpenShift and also mention the nuance. Then we could link the page from the Pod integration. Wdyt?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-27T16:42:03Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@kannon92](https://github.com/kannon92) — 2026-02-27T16:42:03Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  Okay. So I'll remove the limitations.
  
  I like my addition of a section about scheduling gates here as I think its useful to mention.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-02-27T18:11:10Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@kannon92](https://github.com/kannon92) — 2026-02-27T18:11:10Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  > Indeed, I would rather see a subpage in https://kueue.sigs.k8s.io/docs/tasks/manage/, maybe something like "Using custom scheduling-gates", where you could detail the user story for OpenShift and also mention the nuance. Then we could link the page from the Pod integration. Wdyt?
  
  Are you open to some openshift specifics in upstream? 
  
  I'm happy with this but not sure if we should have a separation.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-02T04:33:57Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@kannon92](https://github.com/kannon92) — 2026-03-02T04:33:57Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  I'm not sure custom schedlin-gates needs a full page.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-03-03T12:52:30Z

- **site/content/en/docs/tasks/run/plain_pods.md:107** by [@mwielgus](https://github.com/mwielgus) — 2026-03-03T12:51:25Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
```

  Please use consistent capitalization. In this PR there is Scheduling Gate, Scheduling gate and scheduling gate.

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@mwielgus](https://github.com/mwielgus) — 2026-03-03T12:51:49Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods are able to be admitted, kueue will patch the pods and remove this scheduling gate.
```

  Kueue, here and in the other file.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-17T16:48:19Z

- **site/content/en/docs/tasks/run/plain_pods.md:107** by [@kannon92](https://github.com/kannon92) — 2026-03-17T16:48:19Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-17T16:48:27Z

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@kannon92](https://github.com/kannon92) — 2026-03-17T16:48:27Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods are able to be admitted, kueue will patch the pods and remove this scheduling gate.
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-17T16:48:39Z

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@kannon92](https://github.com/kannon92) — 2026-03-17T16:48:39Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-17T18:21:56Z

- **site/content/en/docs/tasks/run/plain_pods.md:119** by [@kannon92](https://github.com/kannon92) — 2026-04-17T18:21:55Z
```diff
@@ -104,11 +104,19 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that kueue is managing.
+Once the pods able to be admitted, kueue will patch the pods and remove this scheduling gate.
+
+### e. Limitations
 
 - A Kueue managed Pod cannot be created in `kube-system` or `kueue-system` namespaces.
 - In case of [preemption](/docs/concepts/cluster_queue/#preemption), the Pod will
   be terminated and deleted.
+- In case you use additional scheduling gates, Kueue will only remove the scheduling gates it adds (ie `kueue.x-k8s.io/admission`)
+  The pod will be marked as admitted in this case but the pod will still be blocked from scheduling
+  until something removes the scheduling gate.
```

  I think we can go ahead with this PR as is.
  
  I changed this to not be a limitation and I think this can merge.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T08:02:56Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:117** by [@mimowo](https://github.com/mimowo) — 2026-04-20T08:02:56Z
```diff
@@ -108,3 +108,10 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+Kueue adds a [scheduling gate](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) to Pods to
+gate admission. Kueue will only remove scheduling gates that are added by Kueue.
+If you use scheduling gates, Kueue will mark the workload as admitted but the Pod will not be scheduled due to the existing
+scheduling gates.
```

  ```suggestion
  If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) , then your Pods will stay gated even if Kueue marks the 
  workload as admitted and removes the scheduling gates it is managing (such as `keueu.x-k8s.io/admission` or `keueu.x-k8s.io/topology`), because Kueue only removes scheduling gates
  managed by Kueue.
  ```
  Mostly rephrasing, but also  to clarify it is not just "a scheduling gate", but actually at least two gates we can list.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T08:04:29Z

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@mimowo](https://github.com/mimowo) — 2026-04-20T08:04:29Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling Gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that Kueue is managing.
+Once the pods are able to be admitted, Kueue will patch the pods and remove this scheduling gate.
```

  ```suggestion
  Kueue injects the `kueue.x-k8s.io/admission` scheduling gate to the Pods that Kueue is managing.
  Once the corresponding Workload is admitted, Kueue will patch the pods and remove this scheduling gate.
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T15:12:10Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:117** by [@kannon92](https://github.com/kannon92) — 2026-04-20T15:12:10Z
```diff
@@ -108,3 +108,10 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+Kueue adds a [scheduling gate](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) to Pods to
+gate admission. Kueue will only remove scheduling gates that are added by Kueue.
+If you use scheduling gates, Kueue will mark the workload as admitted but the Pod will not be scheduled due to the existing
+scheduling gates.
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T15:12:22Z

- **site/content/en/docs/tasks/run/plain_pods.md:110** by [@kannon92](https://github.com/kannon92) — 2026-04-20T15:12:21Z
```diff
@@ -104,7 +104,12 @@ The resource needs of the workload can be configured in the `spec.containers`.
 
 Kueue will inject the `kueue.x-k8s.io/managed=true` label to indicate which pods are managed by it.
 
-### d. Limitations
+### d. Scheduling Gates
+
+Kueue will inject the `kueue.x-k8s.io/admission` scheduling gate to the pods that Kueue is managing.
+Once the pods are able to be admitted, Kueue will patch the pods and remove this scheduling gate.
```

  updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T16:07:51Z

Just a small nit.
Otherwise, LGTM. Thank you!

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:114** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:07:26Z
```diff
@@ -108,3 +108,9 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) , then your Pods will stay gated even if Kueue marks the 
```

  ```suggestion
  If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) ,then your Pods will stay gated even if Kueue marks the 
  ```
  
  nit

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T16:24:29Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:114** by [@kannon92](https://github.com/kannon92) — 2026-04-20T16:24:29Z
```diff
@@ -108,3 +108,9 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) , then your Pods will stay gated even if Kueue marks the 
```

  Thanks your suggestion has one issue so I'll apply this separately.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T16:24:56Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:114** by [@kannon92](https://github.com/kannon92) — 2026-04-20T16:24:56Z
```diff
@@ -108,3 +108,9 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) , then your Pods will stay gated even if Kueue marks the 
```

  ```suggestion
  If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/), then your Pods will stay gated even if Kueue marks the 
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T16:25:57Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_pods.md:114** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:25:57Z
```diff
@@ -108,3 +108,9 @@ Kueue will remove the finalizer from Pods when:
 Once a Pod doesn't have any finalizers, Kubernetes will delete the Pods based on:
 - Whether a user or a controller has issued a Pod deletion.
 - The [Pod garbage collector](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-garbage-collection).
+
+## Why is my Pod not being scheduled if I am using external scheduling gates
+
+If you use custom [scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/) , then your Pods will stay gated even if Kueue marks the 
```

  Oh, yes, right, thanks 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T16:26:20Z

Thank you!
/approve

Leaving LGTM on @mimowo
