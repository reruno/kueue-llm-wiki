# PR #956: [artifacts] Add helm package

**Summary**: [artifacts] Add helm package

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/956

**Last updated**: 2023-07-11T08:42:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `helm-artifacts` → `main`
- **Created**: 2023-07-07T09:11:50Z
- **Updated**: 2023-07-11T08:42:59Z
- **Merged**: 2023-07-07T13:27:05Z
- **Closed**: 2023-07-07T13:27:05Z
- **Labels**: `kind/feature`, `lgtm`, `tide/merge-method-squash`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+18 / -18**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Generate the helm package along with the manifests.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #745 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-07T09:11:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cd366878e960bf64bae363639c2c5e59c873e305 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64a7f6fdec4dcb000856ce2b |

### Comment by [@trasc](https://github.com/trasc) — 2023-07-07T09:27:35Z

/cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-07T13:13:59Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-07T13:14:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/956#issuecomment-1625399028" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/956#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-07T13:17:52Z

/label tide/merge-method-squash

## Reviews

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-07T11:12:18Z

- **config/components/manager/kustomization.yaml:3** by [@stuton](https://github.com/stuton) — 2023-07-07T11:12:18Z
```diff
@@ -1,7 +1,7 @@
-resources:
-- manager.yaml
 # Comment this line if you want to disable the service for auth proxy (https://github.com/brancz/kube-rbac-proxy)
 # which protects your /metrics endpoint.
+resources:
```

  It's better to revert this back, because comment was addressed exact `auth_proxy_service.yaml`, not all resources

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-07T11:20:07Z

- **config/components/manager/kustomization.yaml:3** by [@trasc](https://github.com/trasc) — 2023-07-07T11:20:07Z
```diff
@@ -1,7 +1,7 @@
-resources:
-- manager.yaml
 # Comment this line if you want to disable the service for auth proxy (https://github.com/brancz/kube-rbac-proxy)
 # which protects your /metrics endpoint.
+resources:
```

  In more that one target in the makefile  we use `kustomize edit` to set and reset some values (image),  if we don't change this, every time you run make you might end up "modifying" the files. 
  
  This is especially problematic when extracting git tags, when the tag will get an extra "-dirty" mid run.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-07T11:29:20Z

- **config/components/manager/kustomization.yaml:3** by [@trasc](https://github.com/trasc) — 2023-07-07T11:29:15Z
```diff
@@ -1,7 +1,7 @@
-resources:
-- manager.yaml
 # Comment this line if you want to disable the service for auth proxy (https://github.com/brancz/kube-rbac-proxy)
 # which protects your /metrics endpoint.
+resources:
```

  comment updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-07T12:59:19Z

- **Makefile:267** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-07T12:58:14Z
```diff
@@ -258,7 +259,12 @@ artifacts: kustomize
 	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
 	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
 	@$(call clean-manifests)
-	cp -r charts artifacts/charts
+	# Update the image tag and policy
+	yq  e  '.controllerManager.manager.image.repository = "$(IMAGE_REPO)" | .controllerManager.manager.image.tag = "$(GIT_TAG)" | .controllerManager.manager.image.pullPolicy = "IfNotPresent"' -i charts/kueue/values.yaml
+	# create the package. TODO: consider signing it
+	helm package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
+	# Revert the image changes
+	yq  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/$(IMAGE_NAME)" | .controllerManager.manager.image.tag = "main" | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  make this part of clean-manifiests

- **Makefile:263** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-07T12:58:37Z
```diff
@@ -258,7 +259,12 @@ artifacts: kustomize
 	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
 	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
 	@$(call clean-manifests)
-	cp -r charts artifacts/charts
+	# Update the image tag and policy
+	yq  e  '.controllerManager.manager.image.repository = "$(IMAGE_REPO)" | .controllerManager.manager.image.tag = "$(GIT_TAG)" | .controllerManager.manager.image.pullPolicy = "IfNotPresent"' -i charts/kueue/values.yaml
```

  can you add a make rule to install `yq`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-07T13:03:58Z

- **Makefile:267** by [@trasc](https://github.com/trasc) — 2023-07-07T13:03:57Z
```diff
@@ -258,7 +259,12 @@ artifacts: kustomize
 	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
 	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
 	@$(call clean-manifests)
-	cp -r charts artifacts/charts
+	# Update the image tag and policy
+	yq  e  '.controllerManager.manager.image.repository = "$(IMAGE_REPO)" | .controllerManager.manager.image.tag = "$(GIT_TAG)" | .controllerManager.manager.image.pullPolicy = "IfNotPresent"' -i charts/kueue/values.yaml
+	# create the package. TODO: consider signing it
+	helm package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
+	# Revert the image changes
+	yq  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/$(IMAGE_NAME)" | .controllerManager.manager.image.tag = "main" | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  let's keep it separate, this is only useful here,  clean manifests is used on a lot more targets

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-07T13:06:15Z

- **Makefile:263** by [@trasc](https://github.com/trasc) — 2023-07-07T13:06:15Z
```diff
@@ -258,7 +259,12 @@ artifacts: kustomize
 	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
 	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
 	@$(call clean-manifests)
-	cp -r charts artifacts/charts
+	# Update the image tag and policy
+	yq  e  '.controllerManager.manager.image.repository = "$(IMAGE_REPO)" | .controllerManager.manager.image.tag = "$(GIT_TAG)" | .controllerManager.manager.image.pullPolicy = "IfNotPresent"' -i charts/kueue/values.yaml
```

  I see it as a standard utility ... I don't think we should do it

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-07T13:13:27Z

- **Makefile:263** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-07T13:13:26Z
```diff
@@ -258,7 +259,12 @@ artifacts: kustomize
 	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
 	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
 	@$(call clean-manifests)
-	cp -r charts artifacts/charts
+	# Update the image tag and policy
+	yq  e  '.controllerManager.manager.image.repository = "$(IMAGE_REPO)" | .controllerManager.manager.image.tag = "$(GIT_TAG)" | .controllerManager.manager.image.pullPolicy = "IfNotPresent"' -i charts/kueue/values.yaml
```

  I don't know how much `yq` could change behavior from one version to the next.
  But we can leave it as a follow up.
