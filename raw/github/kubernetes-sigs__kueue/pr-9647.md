# PR #9647: Add an option to E2E_MODE=dev to install a released version of Kueue

**Summary**: Add an option to E2E_MODE=dev to install a released version of Kueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9647

**Last updated**: 2026-04-17T17:05:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `dev-mode-released-version9415` → `main`
- **Created**: 2026-03-03T05:02:50Z
- **Updated**: 2026-04-17T17:05:04Z
- **Merged**: 2026-04-17T15:29:05Z
- **Closed**: 2026-04-17T15:29:05Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@samzong](https://github.com/samzong), [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl), [@my-git9](https://github.com/my-git9), [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 4   **+64 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9415

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-03T05:02:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 33a95326f2ffc90372384c5add5199b6f96f70cf |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69cd7fcec62e8500083ae7b3 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9647--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9647--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk2NDctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.g3ViaDmz3Th_6t2iRJbs6H-2UYEagbWDQZth3zzPVsg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9647--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-03T05:03:17Z

/kind cleanup
/assign
/cc @mimowo @olekzabl

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:58:01Z

Please make sure we also update the guide for using released versions with the correct manifests, as mentioned in https://github.com/kubernetes-sigs/kueue/pull/9647#discussion_r2888609060

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T14:35:21Z

@vladikkuzn could you please address https://github.com/kubernetes-sigs/kueue/pull/9647#discussion_r2888609060?

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-08T15:30:42Z

/retest-required

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-10T12:30:18Z

I've added "Using a released version with matching manifests" to the docs, I think that should be enough for manifests

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-14T11:55:31Z

/cc @windsonsea @samzong @my-git9
Please review translation since it's generated by llm

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-18T10:50:09Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-18T10:50:18Z

LGTM label has been added.  <details>Git tree hash: aae0bf2bb00df1351665c6ecc7b1f1db9cb7d395</details>

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-23T21:27:37Z

/сс @gabesaba @tenzen-y

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-02T08:37:23Z

/retest-required

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-14T20:37:10Z

/test pull-kueue-test-e2e-multikueue-sequential-main

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-17T14:25:37Z

/approve
/cherrypick website
Thank you 👍

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-17T14:25:40Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9647#issuecomment-4268889633):

>/approve
>/cherrypick website
>Thank you 👍 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-17T14:25:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9647#issuecomment-4268889633" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9647#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/testing/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/testing/OWNERS)~~ [mimowo]
- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T14:51:39Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-17T14:51:50Z

LGTM label has been added.  <details>Git tree hash: 36b5db8d2593284a4ea5d23490d47386871c5bd2</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-17T15:29:46Z

@mimowo: new pull request created: #10583

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9647#issuecomment-4268889633):

>/approve
>/cherrypick website
>Thank you 👍 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-03T16:02:58Z

- **Makefile-test.mk:57** by [@mimowo](https://github.com/mimowo) — 2026-03-03T16:02:59Z
```diff
@@ -51,6 +51,11 @@ PROMETHEUS_OPERATOR_VERSION ?= v0.89.0
 # When truthy, force re-installing external operators (MPI, Ray, etc.) on each run, even in E2E_MODE=dev.
 E2E_ENFORCE_OPERATOR_UPDATE ?= false
 
+# When set (e.g. v0.16.0), use a released Kueue image instead of building. Skip kind-image-build when using this.
+ifdef E2E_KUEUE_VERSION
+IMAGE_TAG := registry.k8s.io/kueue/kueue:$(E2E_KUEUE_VERSION)
+endif
```

  I'm wondering wouldn't this already work if someone passed the `IMAGE_TAG` parameter as `registry.k8s.io/kueue/kueue:v0.16.0`? If this already would work, maybe we don't need a new option, just a bit more of "education" how to pass existing params, wdyt?
  
  cc @mbobrovskyi

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-03T16:26:53Z

- **Makefile-test.mk:57** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-03T16:26:52Z
```diff
@@ -51,6 +51,11 @@ PROMETHEUS_OPERATOR_VERSION ?= v0.89.0
 # When truthy, force re-installing external operators (MPI, Ray, etc.) on each run, even in E2E_MODE=dev.
 E2E_ENFORCE_OPERATOR_UPDATE ?= false
 
+# When set (e.g. v0.16.0), use a released Kueue image instead of building. Skip kind-image-build when using this.
+ifdef E2E_KUEUE_VERSION
+IMAGE_TAG := registry.k8s.io/kueue/kueue:$(E2E_KUEUE_VERSION)
+endif
```

  Yeah, I think this is a good idea. But what if we want to pull staging image – for example, to test a specific commit?
  I’m wondering if we could check whether the image already exists localy and only try to pull it if it doesn’t. Something like:
  
  ```
  if ! docker image inspect "$IMAGE_TAG" > /dev/null 2>&1; then
      docker pull "$IMAGE_TAG"
  fi
  ```
  
  @mimowo @vladikkuzn WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-03T16:43:57Z

- **Makefile-test.mk:57** by [@mimowo](https://github.com/mimowo) — 2026-03-03T16:43:57Z
```diff
@@ -51,6 +51,11 @@ PROMETHEUS_OPERATOR_VERSION ?= v0.89.0
 # When truthy, force re-installing external operators (MPI, Ray, etc.) on each run, even in E2E_MODE=dev.
 E2E_ENFORCE_OPERATOR_UPDATE ?= false
 
+# When set (e.g. v0.16.0), use a released Kueue image instead of building. Skip kind-image-build when using this.
+ifdef E2E_KUEUE_VERSION
+IMAGE_TAG := registry.k8s.io/kueue/kueue:$(E2E_KUEUE_VERSION)
+endif
```

  > Yeah, I think this is a good idea. 
  
  Cool, so if that works, let's try to focus on updating the Contribution guide with the usage example.
  
  > I’m wondering if we could check whether the image already exists localy and only try to pull it if it doesn’t. Something like:
  
  Maybe, but wouldn't the docker pull command already check that internally? This seems like unrelated improvement.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T08:30:47Z

- **hack/testing/e2e-common.sh:368** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T08:30:47Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  Can we avoid this if? I’m wondering if we can use `us-central1-docker.pkg.dev/k8s-staging-images/kueue` as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T08:34:33Z

- **hack/testing/e2e-common.sh:368** by [@mimowo](https://github.com/mimowo) — 2026-03-04T08:34:33Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  The ability of using both released and staging would be great

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-04T08:35:15Z

- **hack/testing/e2e-common.sh:368** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-04T08:35:15Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  For example, 
  
  IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-04T17:32:11Z

- **hack/testing/e2e-common.sh:368** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-04T17:32:11Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  Are staging images "us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue" changing? I mean can someone retag them? Is `docker image inspect` needed?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-04T17:35:55Z

- **hack/testing/e2e-common.sh:368** by [@mimowo](https://github.com/mimowo) — 2026-03-04T17:35:55Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  > Is docker image inspect needed?
  
  Yeah, I see no need, the docker pull should already check if present or not.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T06:02:38Z

- **hack/testing/e2e-common.sh:368** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T06:02:37Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  > Are staging images "us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue" changing? I mean can someone retag them?
  
  Yes, the main tag is retagged every time, so we do need docker pull.
  
  The problem is that IMAGE_TAG is used locally after `kueue-image-build` (e.g., `us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-310-g5fa614825-dirty`) and this tag no need to pull.
  
  Maybe we can just run docker pull and ignore the error if the image doesn’t exist.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T07:00:20Z

- **hack/testing/e2e-common.sh:368** by [@mimowo](https://github.com/mimowo) — 2026-03-05T07:00:20Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  is it the logic we want to have? We can probably improve it by skipping the remote check for staging tags. 
  
  ```sh
  echo "Checking remote image: $IMAGE_TAG"
  
  # Check if the image exists remotely
  if docker manifest inspect "$IMAGE_TAG" >/dev/null 2>&1; then
    echo "Remote image found. Pulling..."
    docker pull "$IMAGE_TAG"
    exit 0
  fi
  
  echo "Remote image not found. Checking local image..."
  
  # Check if the image exists locally
  if docker image inspect "$IMAGE_TAG" >/dev/null 2>&1; then
    echo "Using local image: $IMAGE_TAG"
    exit 0
  fi
  
  echo "ERROR: Image '$IMAGE_TAG' not found remotely or locally."
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-05T07:47:33Z

- **hack/testing/e2e-common.sh:368** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-05T07:47:33Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  Does this look good? Or is if (`if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]`) unnecessary?
  ```sh
  if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
          if docker manifest inspect "$IMAGE_TAG" >/dev/null 2>&1; then
              docker pull "$IMAGE_TAG"
          elif ! docker image inspect "$IMAGE_TAG" >/dev/null 2>&1; then
              echo "ERROR: Image '$IMAGE_TAG' not found remotely or locally." >&2
              return 1
          fi
      fi
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T07:49:54Z

- **hack/testing/e2e-common.sh:368** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T07:49:53Z
```diff
@@ -364,6 +364,13 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_OLD_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE_OLD"
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
+    # When using a released Kueue image, pull it so it's available for kind load (only if not already present locally).
+    if [[ "${IMAGE_TAG}" == registry.k8s.io/kueue/kueue:* ]]; then
```

  Yes, it’s unnecessary because we also need to cover `us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T08:40:40Z

- **site/content/en/docs/contribution_guidelines/testing.md:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T08:40:40Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** Kueue version instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image (e.g. `registry.k8s.io/kueue/kueue:v0.16.0`):
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  ```suggestion
  E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-332-g701268f80 make test-multikueue-e2e
  ```
  Let's add an example with staging image.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T08:41:09Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:142** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T08:41:09Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
 ```
 
+若希望使用**已发布**的 Kueue 版本而非从源码构建（无需执行 `kind-image-build`），可传入 `IMAGE_TAG` 并指定所需镜像（例如 `registry.k8s.io/kueue/kueue:v0.16.0`）：
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  ```suggestion
  E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-332-g701268f80 make test-multikueue-e2e
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T08:41:52Z

Overall LGTM.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:47:23Z

- **site/content/en/docs/contribution_guidelines/testing.md:146** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:47:23Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** Kueue version instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image (e.g. `registry.k8s.io/kueue/kueue:v0.16.0`):
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  Hm, why staging example? I think this option will mostly be used with released images actually. Also, for released images we can easier bump the version in the `prepare-release-branch` script by simple string replacements. If we use staging we will need to maintain manually. Unless using staging which points to "main" tag.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:47:57Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:142** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:47:57Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
 ```
 
+若希望使用**已发布**的 Kueue 版本而非从源码构建（无需执行 `kind-image-build`），可传入 `IMAGE_TAG` 并指定所需镜像（例如 `registry.k8s.io/kueue/kueue:v0.16.0`）：
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  Same comment as in https://github.com/kubernetes-sigs/kueue/pull/9647#discussion_r2888560095, I would rather use released image but fix the `prepare-release-branch` script.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T08:51:22Z

- **site/content/en/docs/contribution_guidelines/testing.md:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T08:51:22Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** Kueue version instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image (e.g. `registry.k8s.io/kueue/kueue:v0.16.0`):
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  > Hm, why staging example? I think this option will mostly be used with released images actually. 
  
  Hmm. I think it's good just for example. What if we set `MAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main make test-multikueue-e2e`? WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T08:54:02Z

- **site/content/en/docs/contribution_guidelines/testing.md:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T08:54:02Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** Kueue version instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image (e.g. `registry.k8s.io/kueue/kueue:v0.16.0`):
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  > bump the version in the prepare-release-branch script by simple string replacements
  
  This is good point! I think no need to bump in prepare-release-branch. We can use this syntax:
  
  ```
  E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:{{< param "version" >}} make test-multikueue-e2e
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-05T08:57:12Z

- **site/content/en/docs/contribution_guidelines/testing.md:146** by [@mimowo](https://github.com/mimowo) — 2026-03-05T08:57:12Z
```diff
@@ -134,6 +134,13 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** Kueue version instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image (e.g. `registry.k8s.io/kueue/kueue:v0.16.0`):
+
+```shell
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
```

  I'm good with the example using main. 
  
  Ah, I just realized the PR will not actually support well older released versions - because we would still use the lastest artifacts, right? 
  
  The idea for supporting released images is that some people are involved in oncall, and need to reproduce problems reported by users. Users report problems for the released version, not main, and these sometimes are very different. So being able to spin up quickly a cluster which matches the version used by a user is nice,  but for that we need more - also the correct manifests. 
  
  For that I guess we need to still checkout the tagged version, build manifests, and only then follow the E2E_MODE command, right? It would be nice to also describe this in the guide.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-05T14:34:49Z

- **site/content/en/docs/contribution_guidelines/testing.md:145** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-05T14:34:49Z
```diff
@@ -134,6 +134,21 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 ```
 
+To use a **released** or **staging** Kueue image instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image:
+
+```shell
+# Released version
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
+
+# Staging image (e.g. from a PR or nightly)
+E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-332-g701268f80 make test-e2e
```

  ```suggestion
  E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main make test-e2e
  ```
  
  As we discussed here: https://github.com/kubernetes-sigs/kueue/pull/9647#discussion_r2888579353, we decided to use the main tag. Could you please address it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:42:19Z

- **site/content/en/docs/tasks/dev/setup_multikueue_development_environment.md:28** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:42:19Z
```diff
@@ -20,7 +20,15 @@ For example:
 E2E_MODE=dev make kind-image-build test-multikueue-e2e
 ```
 
-For more information about the DEV mode, refer to the testing documentation.
+To use a released or staging Kueue image without building (no `kind-image-build` needed), pass `IMAGE_TAG`:
+```sh
+# Released
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
+# Staging (e.g. from a PR or nightly)
+E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-332-g701268f80 make test-multikueue-e2e
```

  ```suggestion
  E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main make test-multikueue-e2e
  ```
  Let's build examples using main, using some custom builds will not age well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:45:14Z

- **site/content/zh-CN/docs/contribution_guidelines/testing.md:137** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:45:15Z
```diff
@@ -134,6 +134,21 @@ E2E_MODE=dev make test-multikueue-e2e
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make test-e2e
 ```
 
+若希望使用**已发布**或**预发布（staging）**的 Kueue 镜像而非从源码构建（无需执行 `kind-image-build`），可传入 `IMAGE_TAG` 并指定所需镜像：
```

  It looks out of sync. Please drop, we have @windsonsea @samzong and @my-git9 who will be happy to translate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:46:34Z

- **site/content/en/docs/tasks/dev/setup_multikueue_development_environment.md:28** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:46:34Z
```diff
@@ -20,7 +20,15 @@ For example:
 E2E_MODE=dev make kind-image-build test-multikueue-e2e
 ```
 
-For more information about the DEV mode, refer to the testing documentation.
+To use a released or staging Kueue image without building (no `kind-image-build` needed), pass `IMAGE_TAG`:
+```sh
+# Released
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
+# Staging (e.g. from a PR or nightly)
+E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.17.0-devel-332-g701268f80 make test-multikueue-e2e
```

  Maybe let's only mention staging "main" here, using released version is more involving becuase of the manifests. let's link to the contribution guide

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:50:49Z

- **site/content/en/docs/contribution_guidelines/testing.md:151** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:50:49Z
```diff
@@ -132,12 +132,26 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 
 # Loop a suite (until it fails) while keeping the cluster
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
+```
+
+To use a **released** or **staging** Kueue image instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image:
+
+```shell
+# Released version
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
 
-# Skip reinstallation of kueue (works only in dev mode)
-E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-e2e
-E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
+# Staging image (e.g. from a PR or nightly)
+E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main make test-e2e
 ```
 
+**Using a released version with matching manifests:** The e2e framework deploys CRDs and other resources from the repo's config and overrides only the controller image via `IMAGE_TAG`. To run e2e against a specific release with manifests that match that image:
+
+1. Check out that version's tag (e.g. `git checkout v0.16.0`).
+2. Run the command above with the same image tag, e.g. `E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e`.
```

  is it enough to just checkout? I think we should also call `make manifests`? please check, but it might be needed, because iirc the final manifests might not be git versioned, but maybe kustomize is used?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-14T11:54:40Z

- **site/content/en/docs/contribution_guidelines/testing.md:151** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-14T11:54:40Z
```diff
@@ -132,12 +132,26 @@ E2E_MODE=dev make kind-image-build test-multikueue-e2e
 
 # Loop a suite (until it fails) while keeping the cluster
 E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
+```
+
+To use a **released** or **staging** Kueue image instead of building from source (no `kind-image-build` needed), pass `IMAGE_TAG` with the desired image:
+
+```shell
+# Released version
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e
+E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-multikueue-e2e
 
-# Skip reinstallation of kueue (works only in dev mode)
-E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-e2e
-E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
+# Staging image (e.g. from a PR or nightly)
+E2E_MODE=dev IMAGE_TAG=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:main make test-e2e
 ```
 
+**Using a released version with matching manifests:** The e2e framework deploys CRDs and other resources from the repo's config and overrides only the controller image via `IMAGE_TAG`. To run e2e against a specific release with manifests that match that image:
+
+1. Check out that version's tag (e.g. `git checkout v0.16.0`).
+2. Run the command above with the same image tag, e.g. `E2E_MODE=dev IMAGE_TAG=registry.k8s.io/kueue/kueue:v0.16.0 make test-e2e`.
```

  The CRD and deployment config are committed at each release, so no make manifests step is needed
