# PR #97: Have a single heap per ClusterQueue

**Summary**: Have a single heap per ClusterQueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/97

**Last updated**: 2022-03-10T18:11:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `cluster-queue` → `main`
- **Created**: 2022-03-07T22:04:15Z
- **Updated**: 2022-03-10T18:11:31Z
- **Merged**: 2022-03-10T17:39:38Z
- **Closed**: 2022-03-10T17:39:38Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Requested reviewers**: _none_
- **Changed files**: 13   **+629 / -266**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Introduces a second implementation of ClusterQueue in `pkg/queue` which specializes on queueing.

Workloads associated to a Queue move as a block if the clusterQueue changes.

#### Which issue(s) this PR fixes:

Fixes #87

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-07T22:04:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/97#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T15:37:30Z

/retest
It looks like #101 again

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-10T15:55:47Z

```bash
Test started today at 10:32 AM failed after 15m0s. ([more info](https://prow.k8s.io/spyglass/lens/metadata/iframe?req=%7B%22artifacts%22%3A%5B%22finished.json%22%2C%22podinfo.json%22%2C%22prowjob.json%22%2C%22started.json%22%5D%2C%22index%22%3A0%2C%22src%22%3A%22gs%2Fkubernetes-jenkins%2Fpr-logs%2Fpull%2Fkubernetes-sigs_kueue%2F97%2Fpull-kueue-test-unit-main%2F1501944129200852992%22%7D&topURL=https%3A//prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/97/pull-kueue-test-unit-main/1501944129200852992&lensIndex=0#))

Init container clonerefs not ready: (state: waiting, reason: "PodInitializing", message: "") Init container initupload not ready: (state: waiting, reason: "PodInitializing", message: "") Init container place-entrypoint not ready: (state: waiting, reason: "PodInitializing", message: "")
```
/retest-required

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T15:59:37Z

The integration test had failed earlier :)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T16:06:41Z

Cool, this is ready for review now :D

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T17:43:03Z

Oops, should have put a hold. I wanted at least a couple of eyes on the code.

I can still do follow ups if necessary.

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-10T18:11:31Z

🤦🏽‍♂️  sorry, since I don't have approval lvl, I felt ok just giving a lgtm

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-08T04:51:08Z

- **pkg/queue/manager.go:143** by [@denkensk](https://github.com/denkensk) — 2022-03-08T04:47:01Z
```diff
@@ -59,25 +123,24 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if _, ok := m.queues[key]; ok {
 		return fmt.Errorf("queue %q already exists", q.Name)
 	}
-	qImpl := newQueue()
-	qImpl.setProperties(q)
+	qImpl := newQueue(q)
 	m.queues[key] = qImpl
 	// Iterate through existing workloads, as workloads corresponding to this
 	// queue might have been added earlier.
 	var workloads kueue.QueuedWorkloadList
 	if err := m.client.List(ctx, &workloads, client.MatchingFields{workloadQueueKey: q.Name}, client.InNamespace(q.Namespace)); err != nil {
 		return fmt.Errorf("listing workloads that match the queue: %w", err)
 	}
-	addedWorkloads := false
-	for i, w := range workloads.Items {
+	for _, w := range workloads.Items {
+		w := w
 		// Checking queue name again because the field index is not available in tests.
 		if w.Spec.QueueName != q.Name || w.Spec.Admission != nil {
 			continue
 		}
-		qImpl.PushOrUpdate(&workloads.Items[i])
-		addedWorkloads = true
+		qImpl.items[workload.Key(&w)] = &w
 	}
-	if addedWorkloads {
+	cq := m.clusterQueues[qImpl.ClusterQueue]
```

  How about if the ClusterQueue doesn't exist?

- **pkg/queue/manager.go:156** by [@denkensk](https://github.com/denkensk) — 2022-03-08T04:49:03Z
```diff
@@ -88,9 +151,19 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 	defer m.Unlock()
 	qImpl, ok := m.queues[Key(q)]
 	if !ok {
-		return errQueueDoesntExist
+		return queueDoesNotExistErr
+	}
```

  Shall we allow user to change the ClusterQueue of the Queue？
  I think it is better to prohibit this operation？

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T14:40:37Z

- **pkg/queue/manager.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T14:40:37Z
```diff
@@ -88,9 +151,19 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 	defer m.Unlock()
 	qImpl, ok := m.queues[Key(q)]
 	if !ok {
-		return errQueueDoesntExist
+		return queueDoesNotExistErr
+	}
```

  So the alternative would be to delete the existing Q and create a new one with the same name. Depending on how fast this happens and how busy the apiserver and kueue are, this set of operations could be collapsed into a single update. So we need to handle it.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-08T15:12:34Z

- **pkg/queue/manager.go:156** by [@denkensk](https://github.com/denkensk) — 2022-03-08T15:12:34Z
```diff
@@ -88,9 +151,19 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 	defer m.Unlock()
 	qImpl, ok := m.queues[Key(q)]
 	if !ok {
-		return errQueueDoesntExist
+		return queueDoesNotExistErr
+	}
```

  I'm not sure if it still collapses the delete and create into a single update event even when the object's UID is different. 
  
  If it will, would it be better to use UID as a key to avoid this this? The reason I have this question is because I'm worried about the workloads which are already scheduled and how you're going to handle them if the queue switches to another capacity?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T15:25:48Z

- **pkg/queue/manager.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T15:25:48Z
```diff
@@ -88,9 +151,19 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 	defer m.Unlock()
 	qImpl, ok := m.queues[Key(q)]
 	if !ok {
-		return errQueueDoesntExist
+		return queueDoesNotExistErr
+	}
```

  I know that the raw go-client does collapse the events even if the UIDs are different. I don't know about the controller-runtime's client. Sure, we can deal with this in the event handler instead, but I'm not too concerned.
  
  >  The reason I have this question is because I'm worried about the workloads which are already scheduled and how you're going to handle them if the queue switches to another capacity?
  
  I think we shouldn't disturb the running workloads, just the pending ones.
  
  That's why there's a `.spec.admission.ClusterQueue` field that already pins it to the `ClusterQueue`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T18:57:24Z

- **pkg/queue/manager.go:143** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T18:57:20Z
```diff
@@ -59,25 +123,24 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if _, ok := m.queues[key]; ok {
 		return fmt.Errorf("queue %q already exists", q.Name)
 	}
-	qImpl := newQueue()
-	qImpl.setProperties(q)
+	qImpl := newQueue(q)
 	m.queues[key] = qImpl
 	// Iterate through existing workloads, as workloads corresponding to this
 	// queue might have been added earlier.
 	var workloads kueue.QueuedWorkloadList
 	if err := m.client.List(ctx, &workloads, client.MatchingFields{workloadQueueKey: q.Name}, client.InNamespace(q.Namespace)); err != nil {
 		return fmt.Errorf("listing workloads that match the queue: %w", err)
 	}
-	addedWorkloads := false
-	for i, w := range workloads.Items {
+	for _, w := range workloads.Items {
+		w := w
 		// Checking queue name again because the field index is not available in tests.
 		if w.Spec.QueueName != q.Name || w.Spec.Admission != nil {
 			continue
 		}
-		qImpl.PushOrUpdate(&workloads.Items[i])
-		addedWorkloads = true
+		qImpl.items[workload.Key(&w)] = &w
 	}
-	if addedWorkloads {
+	cq := m.clusterQueues[qImpl.ClusterQueue]
```

  Nothing happens, those workloads are ignored. When the ClusterQueue is created, we insert all the workloads from queues that match.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T19:17:05Z

- **pkg/queue/manager.go:143** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T19:17:05Z
```diff
@@ -59,25 +123,24 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if _, ok := m.queues[key]; ok {
 		return fmt.Errorf("queue %q already exists", q.Name)
 	}
-	qImpl := newQueue()
-	qImpl.setProperties(q)
+	qImpl := newQueue(q)
 	m.queues[key] = qImpl
 	// Iterate through existing workloads, as workloads corresponding to this
 	// queue might have been added earlier.
 	var workloads kueue.QueuedWorkloadList
 	if err := m.client.List(ctx, &workloads, client.MatchingFields{workloadQueueKey: q.Name}, client.InNamespace(q.Namespace)); err != nil {
 		return fmt.Errorf("listing workloads that match the queue: %w", err)
 	}
-	addedWorkloads := false
-	for i, w := range workloads.Items {
+	for _, w := range workloads.Items {
+		w := w
 		// Checking queue name again because the field index is not available in tests.
 		if w.Spec.QueueName != q.Name || w.Spec.Admission != nil {
 			continue
 		}
-		qImpl.PushOrUpdate(&workloads.Items[i])
-		addedWorkloads = true
+		qImpl.items[workload.Key(&w)] = &w
 	}
-	if addedWorkloads {
+	cq := m.clusterQueues[qImpl.ClusterQueue]
```

  we need to find a way to update the status condition of those workloads to explain why they are pending.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-08T19:22:01Z

sgtm

- **pkg/queue/queue.go:41** by [@ahg-g](https://github.com/ahg-g) — 2022-03-08T19:21:27Z
```diff
@@ -36,59 +34,95 @@ func keyForWorkload(w *kueue.QueuedWorkload) string {
 	return fmt.Sprintf("%s/%s", w.Namespace, w.Spec.QueueName)
 }
 
+// ClusterQueue is the internal implementation of kueue.ClusterQueue that
+// holds pending workloads.
+type ClusterQueue struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this Capacity.
```

  ```suggestion
  	// across the queues in this CQ.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-08T19:33:32Z

- **pkg/queue/manager.go:143** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T19:33:31Z
```diff
@@ -59,25 +123,24 @@ func (m *Manager) AddQueue(ctx context.Context, q *kueue.Queue) error {
 	if _, ok := m.queues[key]; ok {
 		return fmt.Errorf("queue %q already exists", q.Name)
 	}
-	qImpl := newQueue()
-	qImpl.setProperties(q)
+	qImpl := newQueue(q)
 	m.queues[key] = qImpl
 	// Iterate through existing workloads, as workloads corresponding to this
 	// queue might have been added earlier.
 	var workloads kueue.QueuedWorkloadList
 	if err := m.client.List(ctx, &workloads, client.MatchingFields{workloadQueueKey: q.Name}, client.InNamespace(q.Namespace)); err != nil {
 		return fmt.Errorf("listing workloads that match the queue: %w", err)
 	}
-	addedWorkloads := false
-	for i, w := range workloads.Items {
+	for _, w := range workloads.Items {
+		w := w
 		// Checking queue name again because the field index is not available in tests.
 		if w.Spec.QueueName != q.Name || w.Spec.Admission != nil {
 			continue
 		}
-		qImpl.PushOrUpdate(&workloads.Items[i])
-		addedWorkloads = true
+		qImpl.items[workload.Key(&w)] = &w
 	}
-	if addedWorkloads {
+	cq := m.clusterQueues[qImpl.ClusterQueue]
```

  created #102

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-09T02:35:35Z

- **pkg/queue/manager.go:156** by [@denkensk](https://github.com/denkensk) — 2022-03-09T02:35:35Z
```diff
@@ -88,9 +151,19 @@ func (m *Manager) UpdateQueue(q *kueue.Queue) error {
 	defer m.Unlock()
 	qImpl, ok := m.queues[Key(q)]
 	if !ok {
-		return errQueueDoesntExist
+		return queueDoesNotExistErr
+	}
```

  > I think we shouldn't disturb the running workloads, just the pending ones.
  
  got it.

### Review by [@xieydd](https://github.com/xieydd) (commented) — 2022-03-09T10:24:46Z

- **pkg/queue/manager.go:35** by [@xieydd](https://github.com/xieydd) — 2022-03-09T09:52:46Z
```diff
@@ -30,16 +30,24 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadQueueKey = "spec.queueName"
+const (
+	workloadQueueKey     = "spec.queueName"
+	queueClusterQueueKey = "spec.clusterQueue"
```

  ```suggestion
  	clusterQueueKey = "spec.clusterQueue"
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-09T22:35:25Z

Still working on this.
I got the queue manager sorted out. I still need to update the scheduler and the controllers.

- **pkg/queue/queue.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-08T19:34:47Z
```diff
@@ -36,59 +34,95 @@ func keyForWorkload(w *kueue.QueuedWorkload) string {
 	return fmt.Sprintf("%s/%s", w.Namespace, w.Spec.QueueName)
 }
 
+// ClusterQueue is the internal implementation of kueue.ClusterQueue that
+// holds pending workloads.
+type ClusterQueue struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this Capacity.
```

  Done

- **pkg/queue/manager.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-09T22:34:32Z
```diff
@@ -30,16 +30,24 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadQueueKey = "spec.queueName"
+const (
+	workloadQueueKey     = "spec.queueName"
+	queueClusterQueueKey = "spec.clusterQueue"
```

  this was intentional, as it refers to a field in Queue.

### Review by [@xieydd](https://github.com/xieydd) (commented) — 2022-03-10T03:02:32Z

- **pkg/queue/manager.go:35** by [@xieydd](https://github.com/xieydd) — 2022-03-10T03:02:32Z
```diff
@@ -30,16 +30,24 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-const workloadQueueKey = "spec.queueName"
+const (
+	workloadQueueKey     = "spec.queueName"
+	queueClusterQueueKey = "spec.clusterQueue"
```

  got it.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-10T17:37:27Z

/lgtm
