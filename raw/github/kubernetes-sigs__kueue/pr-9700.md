# PR #9700: feat: graduate localqueuemetrics to beta

**Summary**: feat: graduate localqueuemetrics to beta

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9700

**Last updated**: 2026-03-26T12:03:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/lqmetrics-beta` → `main`
- **Created**: 2026-03-06T09:20:21Z
- **Updated**: 2026-03-26T12:03:55Z
- **Merged**: 2026-03-26T10:38:17Z
- **Closed**: 2026-03-26T10:38:17Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@vladikkuzn](https://github.com/vladikkuzn), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 9   **+28 / -15**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Graduate LocalQueueMetrics featuregate to beta.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8830 

#### Special notes for your reviewer:
Related PRs:
LocalQueueMetrics configuration: #9371

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: graduated LocalQueueMetrics to Beta.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T09:20:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5770b83c533f27a3d25da09658c8872913faac3b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c4075b423db00008588c6d |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-06T09:22:35Z

/hold for https://github.com/kubernetes-sigs/kueue/pull/9371.

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T07:45:49Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T07:45:56Z

LGTM label has been added.  <details>Git tree hash: fb3480d7809ea5c62f0f5871c9749d1e4080c7ad</details>

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:31:44Z

/unhold
as the configuration is merged already

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:35:04Z

> /hold for https://github.com/kubernetes-sigs/kueue/pull/9371.

unhold as the dependency PR is already merged

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-25T14:46:08Z

On one hand not sure that we should depend on the config for the cleanup of the metrics where needed, as we might risk the leftover lingering metrics for some long deleted local queues
On the other hand, if we disable the feature gate currently that is exactly the behavior we are going to get, so it makes some sense to align here as well

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-25T16:20:10Z

> Just one question: do we need to check IsActive() in the places where we clean the metrics?

I assume you ask  about IsEnabled. I think no need. By enabling the LocalQueue feature the user agrees to run the code. Switch "enable" in the config makes recording of the metrics, so if they disable the config, then nothing is recorded. If nothing is recorded, then the cleanup is no-op, much just a couple of unnecessary instructions, but I wouldn't worry. If there is a serious bug in the clueanup logic the  user can always just disable the FG.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-25T16:20:44Z

LGTM label has been added.  <details>Git tree hash: b78b1675b080abad3d5bfa390788381336854310</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2026-03-26T10:01:43Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-26T10:01:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9700#issuecomment-4133243097" title="Approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9700#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [gabesaba]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T12:36:48Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:100** by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:36:48Z
```diff
@@ -97,7 +97,6 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 	})
 
 	ginkgo.BeforeEach(func() {
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.LocalQueueMetrics, true)
```

  nit, let's keep these checks in. The expand the diff for now reason.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T12:51:02Z

- **test/integration/singlecluster/controller/core/clusterqueue_controller_test.go:100** by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:51:01Z
```diff
@@ -97,7 +97,6 @@ var _ = ginkgo.Describe("ClusterQueue controller", ginkgo.Label("controller:clus
 	})
 
 	ginkgo.BeforeEach(func() {
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.LocalQueueMetrics, true)
```

  Brought these functions back

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T12:53:49Z

- **pkg/metrics/localqueue.go:86** by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:53:49Z
```diff
@@ -63,16 +63,30 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if cfg == nil {
+		return true
+	}
+	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled
+}
+
 // ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
 // based on the global configuration.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
 	if cfg == nil {
 		return true
 	}
-	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+	return cfg.IsEnabled() && cfg.QueueSelector.Matches(labels.Set(lqLabels))
 }
 
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, lqLabelStorage LocalQueueLabelStorage, wl *kueue.Workload) bool {
+	if cfg == nil {
```

  Check the LocalQueueMetrics FG here to avoid any downstream processing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T12:54:19Z

- **pkg/metrics/localqueue.go:69** by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:54:19Z
```diff
@@ -63,16 +63,30 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if cfg == nil {
```

  check the FG enablement first

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T13:20:28Z

- **pkg/metrics/localqueue.go:86** by [@mykysha](https://github.com/mykysha) — 2026-03-25T13:20:28Z
```diff
@@ -63,16 +63,30 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if cfg == nil {
+		return true
+	}
+	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled
+}
+
 // ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
 // based on the global configuration.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
 	if cfg == nil {
 		return true
 	}
-	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+	return cfg.IsEnabled() && cfg.QueueSelector.Matches(labels.Set(lqLabels))
 }
 
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, lqLabelStorage LocalQueueLabelStorage, wl *kueue.Workload) bool {
+	if cfg == nil {
```

  Done here and in other places as well

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T13:58:06Z

- **pkg/metrics/localqueue.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T13:58:06Z
```diff
@@ -63,16 +63,39 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
+	if cfg == nil {
+		return true
+	}
+	return cfg.Enabled
```

  ```suggestion
  	return features.Enabled(features.LocalQueueMetrics) && (cfg == nil || cfg.Enabled)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:01:44Z

- **pkg/metrics/localqueue.go:87** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T14:01:45Z
```diff
@@ -63,16 +63,39 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
+	if cfg == nil {
+		return true
+	}
+	return cfg.Enabled
+}
+
 // ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
 // based on the global configuration.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
 	if cfg == nil {
 		return true
 	}
-	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
```

  ```suggestion
  	return cfg.IsEnabled() && (cfg == nil || cfg.QueueSelector.Matches(labels.Set(lqLabels)))
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:05:55Z

- **pkg/metrics/localqueue.go:94** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T14:05:55Z
```diff
@@ -63,16 +63,39 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
+	if cfg == nil {
+		return true
+	}
+	return cfg.Enabled
+}
+
 // ShouldExposeLocalQueueMetrics determines if a specific LocalQueue should report metrics
 // based on the global configuration.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetrics(lqLabels map[string]string) bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
 	if cfg == nil {
 		return true
 	}
-	return features.Enabled(features.LocalQueueMetrics) && cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
+	return cfg.Enabled && cfg.QueueSelector.Matches(labels.Set(lqLabels))
 }
 
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
 func (cfg *LocalQueueMetricsConfig) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, lqLabelStorage LocalQueueLabelStorage, wl *kueue.Workload) bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
```

  ```suggestion
  	if !cfg.IsEnabled() {
  		return false
  	}
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:38:36Z

Overall LGTM.

Just one question: do we need to check IsActive() in the places where we clean the metrics?

For example:
https://github.com/epam/kubernetes-kueue/blob/f5253d57ecc207b24fb77d33000dc305543957c9/pkg/cache/queue/metrics.go#L93-L96
https://github.com/epam/kubernetes-kueue/blob/f5253d57ecc207b24fb77d33000dc305543957c9/pkg/cache/scheduler/cache.go#L504-L512

@mykysha @mimowo WDYT?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T15:04:58Z

- **pkg/metrics/localqueue.go:75** by [@mykysha](https://github.com/mykysha) — 2026-03-25T15:04:33Z
```diff
@@ -63,16 +63,39 @@ func NewLocalQueueMetricsConfig(cfg *configapi.LocalQueueMetrics) *LocalQueueMet
 	return lqMetricsConfig
 }
 
+// IsEnabled reports whether LocalQueue metric reporting is enabled or not,
+// regardless of label configuration.
+func (cfg *LocalQueueMetricsConfig) IsEnabled() bool {
+	if !features.Enabled(features.LocalQueueMetrics) {
+		return false
+	}
+	if cfg == nil {
+		return true
+	}
+	return cfg.Enabled
```

  Updated here and in the other places

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T16:20:36Z

/lgtm
/apporve
Thank you folks 👍
