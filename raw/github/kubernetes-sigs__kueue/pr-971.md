# PR #971: [batch/job] Partial admission - sync completions

**Summary**: [batch/job] Partial admission - sync completions

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/971

**Last updated**: 2023-10-25T17:48:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `partial-admission-with-sync` → `main`
- **Created**: 2023-07-11T10:17:41Z
- **Updated**: 2023-10-25T17:48:20Z
- **Merged**: 2023-07-21T15:16:08Z
- **Closed**: 2023-07-21T15:16:08Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 4   **+143 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Synchronize `batch/job.completions` with `parallelism` in case of partial admission

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to: #970

#### Special notes for your reviewer:

This is the first step fixing #970.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
An option to synchronize batch/job.completions with parallelism in case of partial admission
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-11T10:17:43Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-11T10:17:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8f0cd5ce378da9bd49aa60b66ae17cb7d87c9e8a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64b8e81e23f1ad0008f6c40e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-971--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-971--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk3MS0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.lbxf8z7O2gCPD8atn5kPjGB1lZH_RDUsr-_O2Ma1j58)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-21T15:14:10Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-21T15:14:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/971#issuecomment-1645745333" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/971#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:48:18Z

/release-note-edit
```release-note
An option to synchronize batch/job.completions with parallelism in case of partial admission
```

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-16T18:09:37Z

Generally, LGTM.

- **pkg/controller/jobs/job/job_webhook.go:136** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-16T18:07:05Z
```diff
@@ -117,6 +124,15 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled && pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+			allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
+		}
+	}
```

  Should we add a validation to reject jobs with `NonIndexed` mode and `kueue.x-k8s.io/job-completions-equal-parallelism=true` annotation?
  IIUC, the `completions` field is immutable when the job is created with `NonIndexed` mode.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-17T12:14:37Z

- **pkg/controller/jobs/job/job_webhook.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-17T12:14:36Z
```diff
@@ -117,6 +124,15 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled && pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+			allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
+		}
+	}
```

  +1

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-18T07:27:08Z

- **pkg/controller/jobs/job/job_webhook.go:136** by [@trasc](https://github.com/trasc) — 2023-07-18T07:27:03Z
```diff
@@ -117,6 +124,15 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled && pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+			allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
+		}
+	}
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-18T08:11:58Z

- **pkg/controller/jobs/job/job_webhook.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-18T08:10:34Z
```diff
@@ -117,6 +124,20 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled {
+			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
```

  ```suggestion
  			if job.Spec.CompletionMode == nil || (job.Spec.CompletionMode != nil && *job.Spec.CompletionMode == batchv1.NonIndexedCompletion) {
  ```
  
  Here is a potential nil pointer exception.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-18T10:40:24Z

- **pkg/controller/jobs/job/job_webhook.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-18T10:40:24Z
```diff
@@ -117,6 +124,20 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled {
+			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
```

  This is my misunderstanding.
  If the `job.Spec.CompletionMode` is nil, `*job.Spec.CompletionMode == batchv1.NonIndexedCompletion` will not be performed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-18T10:41:08Z

/lgtm
/assign @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-19T15:22:46Z

- **pkg/controller/jobs/job/job_controller.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-19T15:15:20Z
```diff
@@ -51,7 +51,8 @@ var (
 )
 
 const (
-	JobMinParallelismAnnotation = "kueue.x-k8s.io/job-min-parallelism"
+	JobMinParallelismAnnotation      = "kueue.x-k8s.io/job-min-parallelism"
+	JobSyncCompletionWithParallelism = "kueue.x-k8s.io/job-completions-equal-parallelism"
```

  ```suggestion
  	JobCompletionsEqualParallelismAnnotation = "kueue.x-k8s.io/job-completions-equal-parallelism"
  ```

- **pkg/controller/jobs/job/job_webhook.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-19T15:20:25Z
```diff
@@ -81,6 +83,11 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	}
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if job.Spec.Completions == nil && job.syncCompletionWithParallelism() {
```

  This should be the users' responsibility. I prefer we don't change the experience from using plain k8s.

- **pkg/controller/jobs/job/job_webhook.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-19T15:22:40Z
```diff
@@ -117,6 +124,20 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled {
+			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
+				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], "should not be enabled for NonIndexed jobs"))
+			}
+			if pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+				allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
```

  if the name of the annotation is "completions equal parallelism", the errors should be consistent with that.
  
  But I'm open to suggestions for better names for the annotation.
  
  @tenzen-y, any proposals?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T07:54:20Z

- **pkg/controller/jobs/job/job_controller.go:55** by [@trasc](https://github.com/trasc) — 2023-07-20T07:01:01Z
```diff
@@ -51,7 +51,8 @@ var (
 )
 
 const (
-	JobMinParallelismAnnotation = "kueue.x-k8s.io/job-min-parallelism"
+	JobMinParallelismAnnotation      = "kueue.x-k8s.io/job-min-parallelism"
+	JobSyncCompletionWithParallelism = "kueue.x-k8s.io/job-completions-equal-parallelism"
```

  done

- **pkg/controller/jobs/job/job_webhook.go:87** by [@trasc](https://github.com/trasc) — 2023-07-20T07:02:31Z
```diff
@@ -81,6 +83,11 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	}
 
 	jobframework.ApplyDefaultForSuspend(job, w.manageJobsWithoutQueueName)
+
+	if job.Spec.Completions == nil && job.syncCompletionWithParallelism() {
```

  done

- **pkg/controller/jobs/job/job_webhook.go:137** by [@trasc](https://github.com/trasc) — 2023-07-20T07:08:00Z
```diff
@@ -117,6 +124,20 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled {
+			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
+				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], "should not be enabled for NonIndexed jobs"))
+			}
+			if pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+				allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-20T08:40:48Z

- **pkg/controller/jobs/job/job_webhook.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-20T08:40:48Z
```diff
@@ -117,6 +124,20 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			}
 		}
 	}
+	if strVal, found := job.Annotations[JobSyncCompletionWithParallelism]; found {
+		enabled, err := strconv.ParseBool(strVal)
+		if err != nil {
+			allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], err.Error()))
+		}
+		if enabled {
+			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
+				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobSyncCompletionWithParallelism], "should not be enabled for NonIndexed jobs"))
+			}
+			if pointer.Int32Deref(job.Spec.Parallelism, 1) != pointer.Int32Deref(job.Spec.Completions, 1) {
+				allErrs = append(allErrs, field.Invalid(field.NewPath("spec", "completions"), job.Spec.Completions, "should be equal to parallelism when sync completions is enabled"))
```

  > But I'm open to suggestions for better names for the annotation.
  
  I'm ok with `kueue.x-k8s.io/job-completions-equal-parallelism`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-21T14:45:16Z

/lgtm
/approval
