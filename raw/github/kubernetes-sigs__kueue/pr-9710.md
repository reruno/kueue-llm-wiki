# PR #9710: KEP-2724 - Update handling of pending pods for node replacement feature

**Summary**: KEP-2724 - Update handling of pending pods for node replacement feature

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9710

**Last updated**: 2026-03-13T11:28:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `update-taint-node-replacement-kep` → `main`
- **Created**: 2026-03-06T10:36:48Z
- **Updated**: 2026-03-13T11:28:22Z
- **Merged**: 2026-03-11T17:37:36Z
- **Closed**: 2026-03-11T17:37:36Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@olekzabl](https://github.com/olekzabl)
- **Requested reviewers**: _none_
- **Changed files**: 1   **+21 / -19**

## Description

#### What this PR does / why we need it:
The current implementation of the node replacement feature in TAS contains several bugs and race conditions that might cause problems with node replacement:

- Stuck Pending Pods: When a workload is assigned a specific topology, and a node within that topology becomes tainted or enters a NotReady state, the resulting pods are still created with node selectors pointing to that node. Consequently, the Kubernetes scheduler rightfully refuses to schedule them. This leaves the pods stuck in a `Pending` state indefinitely unless manually intervened.

- Race Conditions in Node Failure Detection: The node failure controller currently reacts to node taints or `NotReady` condition without ensuring all of a workload's pods have actually been created. Evaluating early against an empty pod list leads to falsely concluding that all pods are terminated, prematurely marking the node as unhealthy. To act predictably, the controller must first wait for a stable state where all pods have been created and settled (accounting for aspects like `tolerationSeconds`).

- Event-Driven Correctness over Polling: While establishing an initial stable state is necessary, relying on polling to manage that state is fundamentally flawed because underlying controllers act instantaneously. For example: suppose a stable state is reached where one pod is stuck in `Pending` due to a taint. The controller transitions that pod to `Failed` and proceeds to mark the node as unhealthy. Immediately, the Job controller creates a new replacement pod. Before the node replacement is fully completed, this new pod can also get stuck in `Pending` on the same tainted node. A polling-based approach will likely miss this newly created pod, leaving it orphaned and permanently "out of watch". By transitioning to an event-driven model, the controller reacts continuously to Pod events, instantly catching and handling these fast-arriving replacement pods.

- Expanded Condition Support: The original design heavily assumed these edge-cases only occurred with `NoSchedule` taints. In reality, these issues persist equally with NoExecute taints and `NotReady` states. The event-driven implementation unifies the handling of all three node conditions.
    
#### Which issue(s) this PR fixes:
Fixes #9616
    
#### Special notes for your reviewer:
    
#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T10:37:00Z

Hi @j-skiba. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T10:37:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | efda924ea97951fb1a8bf268ce2064f7b64eff3e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b12d961dfa4800082c6327 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T10:51:54Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T12:10:41Z

LGTM, editorial nits to make the text a bit smaller, and flowing more naturally. Please interpret my comments are just suggestions and feel free to still clean them up.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-11T09:11:53Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T09:12:02Z

LGTM label has been added.  <details>Git tree hash: dd516d5a5032946a466ce7d6fd523e68ca26e814</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T17:19:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9710#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9710#pullrequestreview-3931223857" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-13T11:12:59Z

Is this related to #9616? Can you reference it explicitly in the PR description?

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-06T18:41:58Z

- **keps/2724-topology-aware-scheduling/README.md:1426** by [@mimowo](https://github.com/mimowo) — 2026-03-06T18:41:58Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
```

  I think we don't need to describe here the details "how" the controller does things. 
  
  In paricular, I'm not sure we need to describe reacting to Pod / Node events with such detail. Such things are likely to change / evolve. In paricular, I don't see reacting to Pod events in the implementation: https://github.com/kubernetes-sigs/kueue/pull/9615.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T09:11:38Z

- **keps/2724-topology-aware-scheduling/README.md:1435** by [@olekzabl](https://github.com/olekzabl) — 2026-03-08T14:56:24Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+When transitioning these Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
```

  I think I'm not following. Wouldn't just marking the old Pods `Failed` trigger some from of "recovery", in particular creating new Pods which will then get assigned to some other nodes?
  
  Why is this the `TerminatedByKueue` condition needed, actually?

- **keps/2724-topology-aware-scheduling/README.md:1426** by [@olekzabl](https://github.com/olekzabl) — 2026-03-08T15:03:23Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
```

  > to easily filter out
  
  Maybe this doesn't need expanding in this PR, but at least for my better understanding: what's this "easy" way of filtering out? Checking the timestamp when the node was marked unhealthy, and filtering out Pods whose Conditions changed after that?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T11:22:47Z

- **keps/2724-topology-aware-scheduling/README.md:1426** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T11:22:47Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
```

  I removed the implementation details

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T11:26:18Z

- **keps/2724-topology-aware-scheduling/README.md:1426** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T11:26:18Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
```

  Currently the node failure controller is node event driven and it makes scenarios when pods are created too slowly hard to handle - we have to requeue after some period and it's hard to tell when to stop doing requeues. 
  
  Nevertheless I removed that part because these are basically implementation details

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T11:28:07Z

- **keps/2724-topology-aware-scheduling/README.md:1435** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T11:28:07Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+When transitioning these Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
```

  Yes, marking them as failed triggers creation of a replacement pod. The condition is added to make the termination of the pods traceable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T12:04:54Z

- **keps/2724-topology-aware-scheduling/README.md:1419** by [@mimowo](https://github.com/mimowo) — 2026-03-10T12:04:54Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
```

  ```suggestion
  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
  ```
  to avoid saying "unavoidable" node which may be misiterpreted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T12:08:05Z

- **keps/2724-topology-aware-scheduling/README.md:1421** by [@mimowo](https://github.com/mimowo) — 2026-03-10T12:08:05Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
```

  ```suggestion
  Notably the analogous problem exists not just Pods assigned to nodes tainted with  `NoSchedule` , but also nodes with the untolerated `NoExecute` taint, or with `NotReady` state. 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T12:09:24Z

- **keps/2724-topology-aware-scheduling/README.md:1423** by [@mimowo](https://github.com/mimowo) — 2026-03-10T12:09:24Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the node failure controller remediates this scenario by waiting for all pods that are expected to be on the node to be created, and then running the unhealthy handling procedures. The controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  ```suggestion
  In order to remediate the problem, for workloads where single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T12:12:08Z

- **keps/2724-topology-aware-scheduling/README.md:1423** by [@mimowo](https://github.com/mimowo) — 2026-03-10T12:12:07Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the node failure controller remediates this scenario by waiting for all pods that are expected to be on the node to be created, and then running the unhealthy handling procedures. The controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  I think here (or below) is also the right place to mention, that Kueue will not terminate the Pods if there is more than one node requiring replacement, because this scenario is handled by full workload eviction.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-10T12:37:08Z

- **keps/2724-topology-aware-scheduling/README.md:1435** by [@olekzabl](https://github.com/olekzabl) — 2026-03-10T12:37:08Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+When transitioning these Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
```

  Ok, this makes sense. Then I think I was misled by the last sentence here:
  
  > _Without this step, the pending pods would block the creation of replacement pods._
  
  I interpreted "this step" as "adding the `TerminatedByKueue` condition". As I understand now, this is not the intended meaning.
  
  Could it be clearer if you moved that sentence higher up, placing it right after this one:
  
  > _The controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created. Without this step, the pending pods would block the creation of replacement pods._
  
  ?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T13:43:55Z

- **keps/2724-topology-aware-scheduling/README.md:1435** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T13:43:55Z
```diff
@@ -1415,25 +1414,28 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the controller remediates this scenario by reacting to Node and Pod events:
+
+- **On Node events:** When a node becomes unhealthy (due to a taint or the `NotReady` state), the controller evaluates the Pods on the node. If their status indicates an issue, the node is marked as unhealthy.
+- **On Pod events:** When a node is already tainted or `NotReady`, a change in a Pod's status can cause the node to fulfill the unhealthy requirements, allowing the controller to mark the node as unhealthy. Crucially, reacting to Pod events enables the controller to easily filter out Pods that were created too slowly or became stuck in the `Pending` phase. The controller transitions these Pods to the `Failed` phase so that replacement Pods are created—an event-driven approach that completely avoids polling.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+When transitioning these Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
```

  Yes, that sentence was misleading. I moved it as you said

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T13:44:09Z

- **keps/2724-topology-aware-scheduling/README.md:1419** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T13:44:09Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T14:21:39Z

- **keps/2724-topology-aware-scheduling/README.md:1421** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T14:21:39Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
```

  updated

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-10T14:25:20Z

- **keps/2724-topology-aware-scheduling/README.md:1423** by [@j-skiba](https://github.com/j-skiba) — 2026-03-10T14:25:20Z
```diff
@@ -1415,25 +1414,25 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
+an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Pods belonging to a workload might remain in the `Pending` phase if Kueue calculates a topology assignment that includes a specific node, but that node subsequently becomes tainted with `NoSchedule` or `NoExecute`, or enters the `NotReady` state. If a Pod is created or updated with a node selector pointing to this node, the Kubernetes scheduler is unable to schedule it due to the node's condition. Consequently, the Pod remains bound to an unavailable node by its topology assignment.
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+For workloads where single Node replacement is possible, the node failure controller remediates this scenario by waiting for all pods that are expected to be on the node to be created, and then running the unhealthy handling procedures. The controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  updated

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-10T15:38:46Z

- **keps/2724-topology-aware-scheduling/README.md:1424** by [@olekzabl](https://github.com/olekzabl) — 2026-03-10T15:38:00Z
```diff
@@ -1415,25 +1414,27 @@ If `tolerationSeconds` is specified, Kueue waits for the duration before treatin
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with  `NoSchedule` , but also nodes with the untolerated `NoExecute` taint, or with `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
+
+Kueue will not terminate the Pods if there is more than one node requiring replacement, because this scenario is handled by full workload eviction.
```

  Now I think this should be moved - but it's tricky where to :)
  Perhaps to the very bottom of your change?
  Right now, it seems to poorly fit in various other places because:
  
  - the next paragraph starts with "this", which does not relate to _this_ paragraph, but to previous one
  - the following paragraph says "these Pods", and again it's not about _these pods_ from this pragraph
  - the subsequent paragraph speaks about "the Pod", and again it's not about Pods from this paragraph
  
  Or maybe you can make some other rewording; just please consider how it reads "end to end".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T17:19:25Z

/approve
Thanks 👍

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-11T17:59:52Z

Overall LGTM

- **keps/2724-topology-aware-scheduling/README.md:1422** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-11T17:59:47Z
```diff
@@ -1407,35 +1406,37 @@ We introduce the `TASReplaceNodeOnNodeTaints` feature gate from v0.17 as Beta, a
 When enabled, Kueue treats tainted nodes as unhealthy. This applies to nodes with `NoExecute` taint,
 or nodes with `NoSchedule` taint where all pods of the workload running on that node are failing, terminating, or in unscheduled state.
 
-- **NoExecute**: Nodes with the `NoExecute` effect taint, that is not tolerated by the workload, are considered unhealthy.
+- **NoExecute**: Nodes with the `NoExecute` taint, that is not tolerated by the workload, are considered unhealthy.
 The pods on such nodes are expected to be terminated by the node controller. Once terminated, Kueue will attempt
 to replace the node if `TASFailedNodeReplacement` is enabled, and evict the workload if no replacement is possible.
 If `tolerationSeconds` is specified, Kueue waits for the duration before treating the node as unhealthy.
-- **NoSchedule**: Nodes with the `NoSchedule` effect taint, that is not tolerated by the workload, are considered unhealthy
+- **NoSchedule**: Nodes with the `NoSchedule` taint, that is not tolerated by the workload, are considered unhealthy
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While the `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after the `NoSchedule` taint is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with the `NoSchedule` taint, but also nodes with the untolerated `NoExecute` taint, or in the `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where a single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  @j-skiba @mimowo Can we ignore these Kueue triggering Failures by the Pod Failure Policy?
  In general, the Job can ignore such Node Disruption, then the Job controller (KCM) doesn't count the failure and doesn't affect the backoffLimit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T18:07:41Z

- **keps/2724-topology-aware-scheduling/README.md:1422** by [@mimowo](https://github.com/mimowo) — 2026-03-11T18:07:41Z
```diff
@@ -1407,35 +1406,37 @@ We introduce the `TASReplaceNodeOnNodeTaints` feature gate from v0.17 as Beta, a
 When enabled, Kueue treats tainted nodes as unhealthy. This applies to nodes with `NoExecute` taint,
 or nodes with `NoSchedule` taint where all pods of the workload running on that node are failing, terminating, or in unscheduled state.
 
-- **NoExecute**: Nodes with the `NoExecute` effect taint, that is not tolerated by the workload, are considered unhealthy.
+- **NoExecute**: Nodes with the `NoExecute` taint, that is not tolerated by the workload, are considered unhealthy.
 The pods on such nodes are expected to be terminated by the node controller. Once terminated, Kueue will attempt
 to replace the node if `TASFailedNodeReplacement` is enabled, and evict the workload if no replacement is possible.
 If `tolerationSeconds` is specified, Kueue waits for the duration before treating the node as unhealthy.
-- **NoSchedule**: Nodes with the `NoSchedule` effect taint, that is not tolerated by the workload, are considered unhealthy
+- **NoSchedule**: Nodes with the `NoSchedule` taint, that is not tolerated by the workload, are considered unhealthy
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While the `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after the `NoSchedule` taint is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with the `NoSchedule` taint, but also nodes with the untolerated `NoExecute` taint, or in the `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where a single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  Yes, but we would leave it to the user. A user can specify the following rule:
  
  ```yaml
  podFailurePolicy:
    rules:
    - action: Ignore
      onPodConditions:
      - type: TerminatedByKueue
        status: true
  ```
  Still, some users may prefer to increase backoffLimit actually. I don't think this is something we should be adjusting in Kueue. Maybe we can document.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-11T18:14:57Z

- **keps/2724-topology-aware-scheduling/README.md:1422** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-11T18:14:56Z
```diff
@@ -1407,35 +1406,37 @@ We introduce the `TASReplaceNodeOnNodeTaints` feature gate from v0.17 as Beta, a
 When enabled, Kueue treats tainted nodes as unhealthy. This applies to nodes with `NoExecute` taint,
 or nodes with `NoSchedule` taint where all pods of the workload running on that node are failing, terminating, or in unscheduled state.
 
-- **NoExecute**: Nodes with the `NoExecute` effect taint, that is not tolerated by the workload, are considered unhealthy.
+- **NoExecute**: Nodes with the `NoExecute` taint, that is not tolerated by the workload, are considered unhealthy.
 The pods on such nodes are expected to be terminated by the node controller. Once terminated, Kueue will attempt
 to replace the node if `TASFailedNodeReplacement` is enabled, and evict the workload if no replacement is possible.
 If `tolerationSeconds` is specified, Kueue waits for the duration before treating the node as unhealthy.
-- **NoSchedule**: Nodes with the `NoSchedule` effect taint, that is not tolerated by the workload, are considered unhealthy
+- **NoSchedule**: Nodes with the `NoSchedule` taint, that is not tolerated by the workload, are considered unhealthy
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While the `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after the `NoSchedule` taint is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with the `NoSchedule` taint, but also nodes with the untolerated `NoExecute` taint, or in the `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where a single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
```

  > Still, some users may prefer to increase backoffLimit actually. I don't think this is something we should be adjusting in Kueue. Maybe we can document.
  
  yes, I agree with you. I think we should documentize how to ignore node failure in Job FailurePoliicy when they use TAS Node Failure controller both in this KEP and user documentations.
  
  Because users expect to ignore node disruptions by the following podFailurePolicy and no way to find that they should specify `TerminatedByKueue` unless they read Kueue implementations.
  
  ```yaml
  podFailurePolicy:
    rules:
    - action: Ignore
      onPodConditions:
      - type: DisruptionTarget
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-11T18:42:40Z

- **keps/2724-topology-aware-scheduling/README.md:1440** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-11T18:42:40Z
```diff
@@ -1407,35 +1406,37 @@ We introduce the `TASReplaceNodeOnNodeTaints` feature gate from v0.17 as Beta, a
 When enabled, Kueue treats tainted nodes as unhealthy. This applies to nodes with `NoExecute` taint,
 or nodes with `NoSchedule` taint where all pods of the workload running on that node are failing, terminating, or in unscheduled state.
 
-- **NoExecute**: Nodes with the `NoExecute` effect taint, that is not tolerated by the workload, are considered unhealthy.
+- **NoExecute**: Nodes with the `NoExecute` taint, that is not tolerated by the workload, are considered unhealthy.
 The pods on such nodes are expected to be terminated by the node controller. Once terminated, Kueue will attempt
 to replace the node if `TASFailedNodeReplacement` is enabled, and evict the workload if no replacement is possible.
 If `tolerationSeconds` is specified, Kueue waits for the duration before treating the node as unhealthy.
-- **NoSchedule**: Nodes with the `NoSchedule` effect taint, that is not tolerated by the workload, are considered unhealthy
+- **NoSchedule**: Nodes with the `NoSchedule` taint, that is not tolerated by the workload, are considered unhealthy
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While the `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after the `NoSchedule` taint is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with the `NoSchedule` taint, but also nodes with the untolerated `NoExecute` taint, or in the `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where a single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
+
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
+pods in the `Failed` state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+
+When transitioning the `Pending` Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+
+In addition, Kueue emits a Normal event with the reason `PodTerminated` on Pod termination.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+Kueue will not terminate a `Pending` Pod if there is more than one node requiring replacement, because this scenario is handled by a full workload eviction.
 
-Nodes with `.spec.unschedulable` set to true are treated as having `NoSchedule` taint.
+Nodes with `.spec.unschedulable` set to true are treated as having the `NoSchedule` taint.
 
```

  I added my suggestions in the following:
  
  ```suggestion
  
  The forced transition from `Pending` to `Failed` blocks to ignore PodFailurePolicy  (`type: DisruptionTarget`)in the Job, even though the Pod disruption might be node failures. So, batch users should include (`type: TerminatedByKueue `) in their Job PodFailurePolicy if they want to ignore those failures.
  ```
  
  Additionally, could you add user documentation to https://kueue.sigs.k8s.io/docs/concepts/topology_aware_scheduling/?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-11T18:43:06Z

- **keps/2724-topology-aware-scheduling/README.md:1440** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-11T18:43:06Z
```diff
@@ -1407,35 +1406,37 @@ We introduce the `TASReplaceNodeOnNodeTaints` feature gate from v0.17 as Beta, a
 When enabled, Kueue treats tainted nodes as unhealthy. This applies to nodes with `NoExecute` taint,
 or nodes with `NoSchedule` taint where all pods of the workload running on that node are failing, terminating, or in unscheduled state.
 
-- **NoExecute**: Nodes with the `NoExecute` effect taint, that is not tolerated by the workload, are considered unhealthy.
+- **NoExecute**: Nodes with the `NoExecute` taint, that is not tolerated by the workload, are considered unhealthy.
 The pods on such nodes are expected to be terminated by the node controller. Once terminated, Kueue will attempt
 to replace the node if `TASFailedNodeReplacement` is enabled, and evict the workload if no replacement is possible.
 If `tolerationSeconds` is specified, Kueue waits for the duration before treating the node as unhealthy.
-- **NoSchedule**: Nodes with the `NoSchedule` effect taint, that is not tolerated by the workload, are considered unhealthy
+- **NoSchedule**: Nodes with the `NoSchedule` taint, that is not tolerated by the workload, are considered unhealthy
 only if all pods of the workload that have topology assignment to that node are terminating, in the failed state,
 or if they are unscheduled. In this case, Kueue can trigger node replacement.
 
-  While `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
-  If a workload is assigned to a node that is tainted with `NoSchedule`, the pods will remain in a pending state because
-  the Kubernetes scheduler will not schedule them on that node.
+While the `NoSchedule` taint does not evict running pods (unlike `NoExecute`), Kueue triggers recovery for unscheduled or failed pods.
+Kueue also performs node replacement if pods fail or terminate after the `NoSchedule` taint is assigned, ensuring the workload is not blocked by unschedulable Pods which are assigned to a tainted node.
 
-  Kueue also performs node replacement if pods fail or terminate after `NoSchedule` is assigned. Since the taint may indicate
-  an underlying node problem, replacement ensures the workload is not blocked by the unavailable node.
+Notably the analogous problem exists not just for Pods assigned to nodes tainted with the `NoSchedule` taint, but also nodes with the untolerated `NoExecute` taint, or in the `NotReady` state. 
 
-For workloads for which a single Node replacement is possible, and the pods bound to the node are unscheduled (no `spec.nodeName` set),
-because they cannot run due to a taint, Kueue marks the pods as `Failed` and adds the following condition to the pods:
-  ```yaml
-  type: TerminatedByKueue
-  status: True
-  reason: UnschedulableOnAssignedNode
-  message: "..."
-  ```
-  This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
-  pods in Failed state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+In order to remediate the problem, for workloads where a single Node replacement is possible, the node failure controller transitions these `Pending` Pods to the `Failed` phase so that replacement Pods are created.
+
+This ensures that the pods are re-created by the Job controller for placement on other nodes, while keeping the original
+pods in the `Failed` state for debuggability. Without this step, the pending pods would block the creation of replacement pods.
+
+When transitioning the `Pending` Pods, the controller appends the following condition:
+```yaml
+type: TerminatedByKueue
+status: True
+reason: UnschedulableOnAssignedNode
+message: "..."
+```
+
+In addition, Kueue emits a Normal event with the reason `PodTerminated` on Pod termination.
 
-  In addition, Kueue emits a Normal event with reason `PodTerminated` on the Pod to inform about the termination.
+Kueue will not terminate a `Pending` Pod if there is more than one node requiring replacement, because this scenario is handled by a full workload eviction.
 
-Nodes with `.spec.unschedulable` set to true are treated as having `NoSchedule` taint.
+Nodes with `.spec.unschedulable` set to true are treated as having the `NoSchedule` taint.
 
```

  Feel free to refine my suggestion.
