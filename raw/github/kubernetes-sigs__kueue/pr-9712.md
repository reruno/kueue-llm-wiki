# PR #9712: TAS: Improve scheduling performance by caching nodes

**Summary**: TAS: Improve scheduling performance by caching nodes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9712

**Last updated**: 2026-03-10T12:04:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/improve-scheduling-performance-by-caching-nodes` → `main`
- **Created**: 2026-03-06T12:36:01Z
- **Updated**: 2026-03-10T12:04:42Z
- **Merged**: 2026-03-10T11:47:11Z
- **Closed**: 2026-03-10T11:47:11Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@olekzabl](https://github.com/olekzabl)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 17   **+453 / -242**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Improve scheduling performance by caching nodes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9337

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Fix performance bug where snapshotting would take very long due to List and DeepCopy
of all Nodes. Now the cached set of nodes is maintained in event-driven fashion.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T12:36:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ead36f555bcc48faef1a3a414133088b7c6299ef |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69afe51ace65630008a5cd95 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T12:55:19Z

@mbobrovskyi when you have some stable prototype please report the CPU time before and after at the scale of 5k nodes using the tas-performance tests, and report the pprof stats as in the original issue. I think no need for preliminary results to test on 20k, but getting some numbers at 5k will build confidence the fix is improving performance.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T10:42:54Z

/retest

Know issue https://github.com/kubernetes-sigs/kueue/issues/9752.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T13:44:03Z

> @mbobrovskyi when you have some stable prototype please report the CPU time before and after at the scale of 5k nodes using the tas-performance tests, and report the pprof stats as in the original issue. I think no need for preliminary results to test on 20k, but getting some numbers at 5k will build confidence the fix is improving performance.

[20k nodes configuration](https://gist.github.com/j-skiba/f53a74e94aafd67421134721f7e62ff4):

CPU before:

<img width="1496" height="656" alt="cpu_before" src="https://github.com/user-attachments/assets/6964b814-aae4-4c5e-9367-07621db738cd" />

CPU after:

<img width="1496" height="564" alt="cpu_after" src="https://github.com/user-attachments/assets/df416b50-c795-4400-97c7-022a580e7e41" />

Heap before: 

<img width="1496" height="819" alt="mem_before" src="https://github.com/user-attachments/assets/0010ef2e-9e0c-45b1-b5b6-215b4c476834" />

Heap after:

<img width="1496" height="821" alt="mem_after" src="https://github.com/user-attachments/assets/701839fe-b4fd-4797-afc1-85dba89b1b25" />

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T14:05:45Z

/area tas

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T14:10:21Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T14:10:29Z

LGTM label has been added.  <details>Git tree hash: a4abb67940cbf970bb82ce02c89895cc95825d42</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T14:13:45Z

@mbobrovskyi the direction is good, I have a doubt about locks - posted comment.

Could you also measure and report:
1. the wall time before vs after, for the entire test duration
2. the wall time before vs after for the "snapshoting" - might require injecting some logging

Also, for the follow up, please think how to improve the time of `addNode` (follow up PR please). Overall we need to improve performance of taking snapshot as this work is a part of bigger problem https://github.com/kubernetes-sigs/kueue/issues/9715

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T09:01:55Z

/remove-kind feature
/kind bug
/release-note-edit
```release-note
TAS: Fix performance bug where snapshotting would take very long due to List and DeepCopy
of all Nodes. Now the cached set of nodes is maintained in event-driven fashion.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T11:07:16Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T11:07:21Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4030584914):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T11:07:25Z

LGTM label has been added.  <details>Git tree hash: ad521047739be1309749484a18a379f999992b2d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T11:07:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9712#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4030584914" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T11:07:46Z

@mbobrovskyi Please prepare the cherrypicks manually in case of conflicts

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T11:10:29Z

The pprof  shows there is less CPU used at the 20k scale the `snapshotForNodes < (2x) DeepCopy`, and after the change `snapshotForNodes > (2x) filterNodes`. Still it would be nice to collect wall time data.

Reference: https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4023895532

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T11:47:50Z

@mimowo: #9712 failed to apply on top of branch "release-0.16":
```
Applying: TAS: Improve scheduling performance by caching nodes
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/cache_test.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
M	pkg/cache/scheduler/tas_flavor_snapshot_test.go
M	pkg/controller/tas/node_failure_controller.go
M	pkg/controller/tas/node_failure_controller_test.go
M	pkg/scheduler/scheduler_tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler_tas_test.go
Auto-merging pkg/controller/tas/node_controller_test.go
Auto-merging pkg/controller/tas/node_controller.go
CONFLICT (content): Merge conflict in pkg/controller/tas/node_controller.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot_test.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/cache_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 TAS: Improve scheduling performance by caching nodes

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4030584914):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-10T11:48:24Z

@mimowo: #9712 failed to apply on top of branch "release-0.15":
```
Applying: TAS: Improve scheduling performance by caching nodes
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/cache_test.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
M	pkg/cache/scheduler/tas_flavor_snapshot_test.go
M	pkg/controller/tas/controllers.go
M	pkg/controller/tas/node_failure_controller.go
M	pkg/controller/tas/node_failure_controller_test.go
M	pkg/controller/tas/resource_flavor.go
M	pkg/scheduler/scheduler_tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/scheduler/scheduler_tas_test.go
Auto-merging pkg/controller/tas/resource_flavor.go
CONFLICT (content): Merge conflict in pkg/controller/tas/resource_flavor.go
Auto-merging pkg/controller/tas/node_controller_test.go
CONFLICT (content): Merge conflict in pkg/controller/tas/node_controller_test.go
Auto-merging pkg/controller/tas/node_controller.go
CONFLICT (content): Merge conflict in pkg/controller/tas/node_controller.go
Auto-merging pkg/controller/tas/controllers.go
CONFLICT (content): Merge conflict in pkg/controller/tas/controllers.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot_test.go
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/cache_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 TAS: Improve scheduling performance by caching nodes

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4030584914):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-09T08:13:54Z

- **pkg/controller/tas/resource_flavor.go:112** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T08:13:54Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  ```go
  	select {
          case r.nodeUpdateCh <- event.GenericEvent{Object: newNode}:
      default:
          r.logger().V(3).Info("Node update channel full, dropping new node event", "node", newNode.Name)
      }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T10:00:29Z

- **pkg/controller/tas/resource_flavor.go:112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T10:00:29Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  What does it mean? I didn't got.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T11:00:52Z

- **pkg/controller/tas/resource_flavor.go:112** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T10:45:25Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  I'm assuming that, if `newNode == oldNode`, we rely on something inside kube-controller library to deduplicate the reconciliations (at least usually), is that right?

- **pkg/cache/scheduler/tas_cache.go:139** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T10:53:46Z
```diff
@@ -127,3 +129,23 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) AddOrUpdateNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
+		t.nodes[node.Name] = newNodeInfo(node)
+	} else {
+		t.deleteNodeWithoutLock(node.Name)
```

  Nit: it's a bit counter-intuitive that `AddOrUpdateX` may also delete X.
  Though I don't have ideas how to rename this method... so feel free to ignore.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T11:10:37Z

- **pkg/controller/tas/resource_flavor.go:112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T11:10:37Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  This is just an event. We handle it inside [nodeHandler](https://github.com/epam/kubernetes-kueue/blob/4718ce7071570b6180fc601638fc8bba853be4b2/pkg/controller/tas/resource_flavor.go#L130-L143). [Here](https://github.com/epam/kubernetes-kueue/blob/4718ce7071570b6180fc601638fc8bba853be4b2/pkg/controller/tas/resource_flavor.go#L138-L140), we are adding it to the workqueue.
  And yes, controller-runtime should execute it only once, even if we have multiple events.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T12:05:06Z

- **pkg/cache/scheduler/tas_cache.go:139** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T12:05:06Z
```diff
@@ -127,3 +129,23 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) AddOrUpdateNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
+		t.nodes[node.Name] = newNodeInfo(node)
+	} else {
+		t.deleteNodeWithoutLock(node.Name)
```

  Maybe SyncNode()? WDYT?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T13:05:22Z

- **pkg/cache/scheduler/tas_cache.go:139** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T13:05:22Z
```diff
@@ -127,3 +129,23 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) AddOrUpdateNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
+		t.nodes[node.Name] = newNodeInfo(node)
+	} else {
+		t.deleteNodeWithoutLock(node.Name)
```

  Sounds good. In particular, `SyncAdmittedCondition` in `scheduler.go` sounds like a "precedent for this convention".
  
  Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-09T13:47:35Z

- **pkg/controller/tas/resource_flavor.go:112** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-09T13:47:34Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  Done

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T14:10:15Z

- **pkg/controller/tas/resource_flavor.go:112** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T14:10:14Z
```diff
@@ -97,49 +96,42 @@ func (r *rfReconciler) setupWithManager(mgr ctrl.Manager, cache *schdcache.Cache
 		Complete(core.WithLeadingManager(mgr, r, &kueue.ResourceFlavor{}, cfg))
 }
 
+var _ NodeUpdateWatcher = (*rfReconciler)(nil)
+
+func (r *rfReconciler) NotifyNodeUpdate(oldNode *corev1.Node, newNode *corev1.Node) {
+	if oldNode != nil && newNode != nil && checkNodeSchedulingPropertiesChanged(newNode, oldNode) == nodeUnchanged {
+		r.logger().V(5).Info("Skipping node update as scheduling properties are unchanged", "node", newNode.Name)
+		return
+	}
+	if oldNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: oldNode}
+	}
+	if newNode != nil {
+		r.nodeUpdateCh <- event.GenericEvent{Object: newNode}
+	}
```

  I _guess_ what @vladikkuzn meant was to explicitly handle the case when the channel buffer is full (i.e. there are 10 pending events). Then, your current code would wait until the buffer becomes not full. The proposed code would instead explicitly discard the change - in order not to block.
  
  (In particular, I _don't think_ it's a proposal to no longer consider `oldNode`).
  
  ---
  
  However, I don't see this practice applied anywhere else, so I personally _don't support_ this comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T14:12:05Z

- **pkg/cache/scheduler/tas_flavor.go:148** by [@mimowo](https://github.com/mimowo) — 2026-03-09T14:12:05Z
```diff
@@ -136,10 +117,44 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(log logr.Logger, nodes map[string]*nodeInfo) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
+	return c.snapshotForNodes(log, c.filterNodes(nodes))
+}
+
+func (c *TASFlavorCache) filterNodes(nodes map[string]*nodeInfo) []*nodeInfo {
+	var reqs []labels.Requirement
+
+	// 1. Add Exact Match requirements (MatchingLabels).
+	for k, v := range c.flavor.NodeLabels {
+		req, _ := labels.NewRequirement(k, selection.Equals, []string{v})
+		reqs = append(reqs, *req)
+	}
+
+	// 2. Add Exists requirements (HasLabels).
+	for _, key := range c.topology.Levels {
+		if _, exists := c.flavor.NodeLabels[key]; !exists {
+			req, _ := labels.NewRequirement(key, selection.Exists, []string{})
+			reqs = append(reqs, *req)
+		}
+	}
+
+	// 3. Create a single selector from all combined requirements.
+	selector := labels.NewSelector().Add(reqs...)
+
+	// 4. Filter the nodes.
+	filteredNodes := make([]*nodeInfo, 0, len(nodes))
+	for _, node := range nodes {
```

  This access to nodes is not protected by the read lock, right? 
  
  I would rather introduce a dedicacted struct protected by R/W lock for nodes. Rather than relying on higher level locks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T07:40:14Z

- **pkg/cache/scheduler/tas_cache.go:163** by [@mimowo](https://github.com/mimowo) — 2026-03-10T07:40:14Z
```diff
@@ -127,3 +130,35 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) SyncNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
+		t.nodes[node.Name] = newNodeInfo(node)
+	} else {
+		t.deleteNodeWithoutLock(node.Name)
+	}
+}
+
+func (t *tasCache) DeleteNode(nodeName string) {
+	t.Lock()
+	defer t.Unlock()
+	t.deleteNodeWithoutLock(nodeName)
+}
+
+func (t *tasCache) deleteNodeWithoutLock(nodeName string) {
+	delete(t.nodes, nodeName)
+}
+
+func (t *tasCache) findNodes(nodeLabels map[string]string, levels []string) []*nodeInfo {
+	t.RLock()
+	defer t.RUnlock()
+	filteredNodes := make([]*nodeInfo, 0, len(t.nodes))
+	for _, node := range t.nodes {
+		if utilnode.NodeMatchesFlavor(node.Labels, nodeLabels, levels) {
+			filteredNodes = append(filteredNodes, node)
+		}
+	}
+	return filteredNodes
```

  nit, but I'm wondering if we could first clone the nodes inside the lock, then use slices.DeleteFunc outside the lock. It would move the match checking outside the lock.
  
  We could also just move the nodes under a dedicated thread safe struct which has Clone

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T07:40:56Z

- **pkg/cache/scheduler/tas_cache.go:137** by [@mimowo](https://github.com/mimowo) — 2026-03-10T07:40:56Z
```diff
@@ -127,3 +130,35 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) SyncNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
```

  nit, this line does not need to be under lock. However, we may keep it if it is tricky to refactor, ptal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T07:47:17Z

- **pkg/util/tas/node.go:21** by [@mimowo](https://github.com/mimowo) — 2026-03-10T07:47:17Z
```diff
@@ -0,0 +1,33 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package node
+
+// NodeMatchesFlavor checks if a node's labels match the required labels
+// and contains all required topology levels. Returns true if matches.
+func NodeMatchesFlavor(nodeLabels map[string]string, requiredLabels map[string]string, requiredLevels []string) bool {
```

  We already have this capability in the `nodeBelongsToFlavor` in the tas controllers package. 
  
  I would propose de-duplicate in this PR  by using the new function from the old place. 
  
  
  I'm just thinking `pkg/util/tas/` might be a better location for the file, because it belongs there functionally, and so people will search it there. "Node" itself does not know about "Flavor" concept, only TAS.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T08:48:19Z

- **pkg/util/tas/node.go:21** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T08:48:19Z
```diff
@@ -0,0 +1,33 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package node
+
+// NodeMatchesFlavor checks if a node's labels match the required labels
+// and contains all required topology levels. Returns true if matches.
+func NodeMatchesFlavor(nodeLabels map[string]string, requiredLabels map[string]string, requiredLevels []string) bool {
```

  > We already have this capability in the nodeBelongsToFlavor in the tas controllers package.
  
  Yeah, I moved this function (https://github.com/kubernetes-sigs/kueue/pull/9712/changes#diff-7f688e0706bb6b6775d35b320ee0d496f92ed367d87cb845979731898ea9d1eaL208-L221).
  
  > I'm just thinking pkg/util/tas/ might be a better location for the file, because it belongs there functionally, and so people will search it there. "Node" itself does not know about "Flavor" concept, only TAS.
  
  Good point. Moved.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T08:48:33Z

- **pkg/cache/scheduler/tas_cache.go:137** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T08:48:33Z
```diff
@@ -127,3 +130,35 @@ func (t *tasCache) Update(pod *corev1.Pod, log logr.Logger) {
 func (t *tasCache) DeletePodByKey(key client.ObjectKey) {
 	t.nonTasUsageCache.delete(key)
 }
+
+func (t *tasCache) SyncNode(node *corev1.Node) {
+	t.Lock()
+	defer t.Unlock()
+	if !node.Spec.Unschedulable && utiltas.IsNodeStatusConditionTrue(node.Status.Conditions, corev1.NodeReady) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T08:49:41Z

- **pkg/cache/scheduler/tas_flavor.go:148** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T08:49:41Z
```diff
@@ -136,10 +117,44 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshotForNodes(log logr.Logger, nodes []corev1.Node) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(log logr.Logger, nodes map[string]*nodeInfo) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
+	return c.snapshotForNodes(log, c.filterNodes(nodes))
+}
+
+func (c *TASFlavorCache) filterNodes(nodes map[string]*nodeInfo) []*nodeInfo {
+	var reqs []labels.Requirement
+
+	// 1. Add Exact Match requirements (MatchingLabels).
+	for k, v := range c.flavor.NodeLabels {
+		req, _ := labels.NewRequirement(k, selection.Equals, []string{v})
+		reqs = append(reqs, *req)
+	}
+
+	// 2. Add Exists requirements (HasLabels).
+	for _, key := range c.topology.Levels {
+		if _, exists := c.flavor.NodeLabels[key]; !exists {
+			req, _ := labels.NewRequirement(key, selection.Exists, []string{})
+			reqs = append(reqs, *req)
+		}
+	}
+
+	// 3. Create a single selector from all combined requirements.
+	selector := labels.NewSelector().Add(reqs...)
+
+	// 4. Filter the nodes.
+	filteredNodes := make([]*nodeInfo, 0, len(nodes))
+	for _, node := range nodes {
```

  Done
