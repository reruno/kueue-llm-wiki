# PR #9721: Implement MultiKueue single-cluster preemption guarantee

**Summary**: Implement MultiKueue single-cluster preemption guarantee

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9721

**Last updated**: 2026-03-31T08:29:27Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `feat/multikueue-preemption-orchestration` → `main`
- **Created**: 2026-03-06T17:23:43Z
- **Updated**: 2026-03-31T08:29:27Z
- **Merged**: 2026-03-17T15:31:43Z
- **Closed**: 2026-03-17T15:31:44Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`, `area/multikueue`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@olekzabl](https://github.com/olekzabl), [@Singularity23x0](https://github.com/Singularity23x0)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl), [@Singularity23x0](https://github.com/Singularity23x0), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 27   **+1855 / -27**

## Description

#### What type of PR is this?
/kind feature
/area multikueue

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
Fixes #8303

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
MultiKueue: Add the  `MultiKueueOrchestratedPreemption` alpha feature gate proposed in KEP-8303, which enables the orchestration of preemptions within MultiKueue worker clusters, guaranteeing that the preemptions are executed sequentially.
Introduce a new `PreemptionGate` API to `Workload`, which governs whether a workload can execute preemptions.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T17:23:46Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T17:23:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | af344ea93cbd2d021eea86cfa804367931dfd2a4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b948eb915381000757ddef |

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-11T10:39:36Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T10:39:44Z

LGTM label has been added.  <details>Git tree hash: 9373d3abc0609bb094389b20456196a9f3ce8c0d</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-11T10:57:42Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T10:57:49Z

LGTM label has been added.  <details>Git tree hash: ecb310d4dbe28c1459f4ba22fc3f3b891c9e06c6</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-12T10:48:31Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-12T10:48:37Z

LGTM label has been added.  <details>Git tree hash: c3a3eb122d6c2bc65fe34b6b7d91aeaae5520fff</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-12T21:11:33Z

/lgtm

Because both my recent comments may be non-blocking

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-12T21:11:41Z

LGTM label has been added.  <details>Git tree hash: 73aaac108663e76b39bdcc69e4f54d9911087af1</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T10:36:44Z

@kshalot sanity-check question (a bit late post KEP merging, but it bothers me): once MultiKueueOrchestratedPreemption graduates to Beta, this would effectively make single preemption the default behavior on all clusters? 

That sounds reasonable to me, but it means semantic change, so I want to make sure we have thought through the consequences and we are comfortable assuming there are no legitimate use cases for concurrent preemptions.

Asking, because I’m wondering whether, this feature gate should evolve into an explicit opt-in knob such as  `PreemptionsMode`: `Concurrent/AtMostOne`, and config like `MaxConcurrentPreemptionsCount: 1/2`. 

I'm also ok to defer the decision to when we graduate to Beta, but I would like to know what is our current stance on that, wdyt?

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-13T10:43:24Z

> @kshalot sanity-check question (a bit late post KEP merging, but it bothers me): once MultiKueueOrchestratedPreemption graduates to Beta, this would effectively make single preemption the default behavior on all clusters?
> 
> That sounds reasonable to me, but it means semantic change, so I want to make sure we have thought through the consequences and we are comfortable assuming there are no legitimate use cases for concurrent preemptions.
> 
> Asking, because I’m wondering whether, this feature gate should evolve into an explicit opt-in knob such as `PreemptionsMode`: `Concurrent/AtMostOne`, and config like `MaxConcurrentPreemptionsCount: 1/2`.
> 
> I'm also ok to defer the decision to when we graduate to Beta, but I would like to know what is our current stance on that, wdyt?

Thanks for raising that, this should be mentioned in the KEP explicitly but it wasn't. I agree that the feature gate should be replaced with an API in the beta, so there is an explicit opt-in. I'm not sure which mode would be the default yet. Right now, obviously,  the feature gate serves as the binary opt-in mechanism.

I'll update the graduation criteria in the KEP and create an issue.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T10:53:40Z

Cool, let's make a follow up PR to update the KEP with re-evaluating the configuration for enabling the feature, and if it should be default or not.

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-13T17:08:12Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T09:04:31Z

LGTM label has been added.  <details>Git tree hash: 5f44c07fdc287589afffb73e71fff6e862bd5538</details>

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-03-16T11:24:31Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T11:24:38Z

LGTM label has been added.  <details>Git tree hash: 2806ba27b38af34beda173a020e588c5aa253e0b</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-17T14:58:06Z

Thank you 👍 This solves the epic problem for MultiKueue, at least it is the first milestone 🥇 
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T14:58:15Z

LGTM label has been added.  <details>Git tree hash: cde41fde3df4f01c45e84a8c480ac0046e05bdb0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T14:58:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9721#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9721#issuecomment-4075623030" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-31T08:29:24Z

/release-note-edit

```release-note
MultiKueue: Add the  `MultiKueueOrchestratedPreemption` alpha feature gate proposed in KEP-8303, which enables the orchestration of preemptions within MultiKueue worker clusters, guaranteeing that the preemptions are executed sequentially.
Introduce a new `PreemptionGate` API to `Workload`, which governs whether a workload can execute preemptions.
```

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T14:43:15Z

- **pkg/controller/admissionchecks/multikueue/workload.go:542** by [@kshalot](https://github.com/kshalot) — 2026-03-09T14:43:15Z
```diff
@@ -501,11 +511,24 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+		if remClient != nil && remWl != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
+	return !equality.Semantic.DeepEqual(local, remote)
```

  I also considered doing `DeepDerivative` here which would not report cases like:
  ```
  local.field = nil
  remote.field = "value"
  ```
  as diffs. But that could have an impact on the rest of the app.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T17:40:05Z

Nice!

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T14:56:35Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  I don't like the side-effect on `remote`.
  I'd consider restoring the previous state before returning - even if it doesn't directly simplify your current caller code.

- **pkg/controller/admissionchecks/multikueue/workload.go:426** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T15:03:07Z
  If you follow my other comment (on restoring `remote` inside `isRemoteSpecOutOfSync`), you'll be able to do the whole "saving of gates" just around this line. Making the code "more locally self-contained".

- **pkg/controller/admissionchecks/multikueue/workload.go:1051** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T16:01:11Z
```diff
@@ -990,13 +1016,59 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient) {
+	var previousUngateTime *metav1.Time
+	var oldestPreemptionSignalTime *metav1.Time
+	var workloadToUngate *kueue.Workload
+	var remote *remoteClient
+
+	for name, wl := range group.remotes {
+		if wl == nil {
+			continue
+		}
+
+		openMkGateStateIdx := slices.IndexFunc(wl.Status.PreemptionGates, func(gate kueue.PreemptionGateState) bool {
+			return gate.Name == constants.MultiKueuePreemptionGate && gate.State == kueue.GateStateOpen
+		})
+		if openMkGateStateIdx == -1 {
+			preemptionSignalCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreemptionBlocked)
+			wlRequiresPreemption := preemptionSignalCond != nil && preemptionSignalCond.Status == metav1.ConditionTrue
+			wlHasOlderPreemptionSignal := oldestPreemptionSignalTime == nil || preemptionSignalCond.LastTransitionTime.Before(oldestPreemptionSignalTime)
+			if wlRequiresPreemption && wlHasOlderPreemptionSignal {
+				workloadToUngate = wl
+				remote = group.remoteClients[name]
+				oldestPreemptionSignalTime = &preemptionSignalCond.LastTransitionTime
+			}
+		} else {
+			openMkGateState := wl.Status.PreemptionGates[openMkGateStateIdx]
+			gateOpenedTime := openMkGateState.LastTransitionTime
+			if previousUngateTime == nil || gateOpenedTime.After(previousUngateTime.Time) {
+				previousUngateTime = &gateOpenedTime
+			}
+		}
+	}
+
+	// If the timeout elapsed or no workload was ungated yet, return the workload with he oldest signal.
```

  ```suggestion
  	// If the timeout elapsed or no workload was ungated yet, return the workload with the oldest signal.
  ```

- **pkg/controller/admissionchecks/multikueue/workload.go:518** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T16:13:35Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
```

  This implements step 5 from the KEP procedure:
  
  > _5. Mark the `kueue.x-k8s.io/multikueue` gate of the found workload as `Open`._
  
  Then, how about the last step:
  
  > _6. Schedule a reconciliation in `SingleClusterPreemptionTimeout`._
  
  ? Are we guaranteed to have this reconciler invoked again after that timeout?

- **pkg/controller/core/workload_controller.go:704** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T17:09:26Z
```diff
@@ -678,6 +688,40 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 	return false, nil
 }
 
+func (r *WorkloadReconciler) syncPreemptionGateStates(wl *kueue.Workload) bool {
+	changed := false
```

  Thinking loud: what if we extracted this logic to a helper, called from here but also from `SetPreemptionGateState` and maybe also from `HasClosedPreemptionGate`?
  
  (Then you could remove "edge case handling" from those 2 methods)
  
  The not-so-nice part would be that `HasClosedPreemptionGate` would do the "syncing" but then drop it.

- **pkg/controller/admissionchecks/multikueue/workload.go:808** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T17:17:04Z
```diff
@@ -781,21 +805,23 @@ func (w *wlReconciler) Generic(_ event.GenericEvent) bool {
 func newWlReconciler(c client.Client, helper *admissioncheck.MultiKueueStoreHelper, cRec *clustersReconciler, origin string,
 	recorder record.EventRecorder, workerLostTimeout, eventsBatchPeriod time.Duration,
 	adapters map[string]jobframework.MultiKueueAdapter, dispatcherName string, roleTracker *roletracker.RoleTracker,
+	singleClusterPreemptionTimeout time.Duration,
```

  This arg list is long, and you've just added a 2nd `time.Duration` to it :(
  I'd consider refactoring, e.g. into a named-field struct.
  (Pretty much copying the signature of `wlReconciler`, just with public fields through which we can pass the initial values to the current private fields).
  Unless you have another idea.

- **pkg/controller/admissionchecks/multikueue/workload_test.go:1674** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T17:24:34Z
```diff
@@ -1521,6 +1523,243 @@ func TestWlReconcile(t *testing.T) {
 				},
 			},
 		},
+		"creating remote workloads with preemption gates": {
+			features: map[featuregate.Feature]bool{
+				features.MultiKueueOrchestratedPreemption: true,
+			},
+			reconcileFor: "wl1",
+
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+
+			useSecondWorker: true,
+
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					NominatedClusterNames("worker1", "worker2").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					Obj(),
+			},
+			wantWorker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					Obj(),
+			},
+		},
+		"opening preemption gate for first workload requiring preemption": {
+			features: map[featuregate.Feature]bool{
+				features.MultiKueueOrchestratedPreemption: true,
+			},
+			reconcileFor: "wl1",
+
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+
+			worker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Obj(),
+			},
+			useSecondWorker: true,
+			worker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(earlier),
+					}).
+					Obj(),
+			},
+
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					NominatedClusterNames("worker1", "worker2").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Obj(),
+			},
+			wantWorker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(
+						kueue.PreemptionGateState{
+							Name:               constants.MultiKueuePreemptionGate,
+							State:              kueue.GateStateOpen,
+							LastTransitionTime: metav1.NewTime(now),
+						},
+					).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(earlier),
+					}).
+					Obj(),
+			},
+		},
+		"not opening preemption gate if timeout did not elapse": {
```

  How about a test case for timeout-driven opening (for the 2nd worker)?

- **test/integration/multikueue/scheduler/scheduler_test.go:625** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T17:33:44Z
```diff
@@ -523,4 +534,122 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 			util.ExpectWorkloadsWithWorkloadPriority(worker1TestCluster.ctx, worker1TestCluster.client, managerLowWPC.Name, managerLowWPC.Value, wlKey)
 		})
 	})
+
+	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
+
+		// Fits only in worker1
+		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
+
+		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+		managerLowWl1 := &kueue.Workload{}
+		workerLowW1 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits only in worker2
+		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
+
+		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+		managerLowWl2 := &kueue.Workload{}
+		workerLowW2 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits in both workers
+		highJob := testingjob.MakeJob("high-job", managerNs.Name).
+			WorkloadPriorityClass(managerHighWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
+
+		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+		managerHighWl := &kueue.Workload{}
+		workerHighWorkload := &kueue.Workload{}
+
+		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the high-priority workload is admitted in exactly one of the workers", func() {
```

  IIUC this would be true even without your feature. Even with multiple preemptions, as soon as one of the remotes is admitted, the other remotes are removed.
  So I'm not sure if this is worth checking here.

- **test/integration/multikueue/scheduler/scheduler_test.go:651** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T17:38:00Z
```diff
@@ -523,4 +534,122 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 			util.ExpectWorkloadsWithWorkloadPriority(worker1TestCluster.ctx, worker1TestCluster.client, managerLowWPC.Name, managerLowWPC.Value, wlKey)
 		})
 	})
+
+	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
+
+		// Fits only in worker1
+		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
+
+		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+		managerLowWl1 := &kueue.Workload{}
+		workerLowW1 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits only in worker2
+		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
+
+		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+		managerLowWl2 := &kueue.Workload{}
+		workerLowW2 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits in both workers
+		highJob := testingjob.MakeJob("high-job", managerNs.Name).
+			WorkloadPriorityClass(managerHighWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
+
+		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+		managerHighWl := &kueue.Workload{}
+		workerHighWorkload := &kueue.Workload{}
+
+		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the high-priority workload is admitted in exactly one of the workers", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
+				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
+
+				g.Expect((worker1Error == nil) != (worker2Error == nil)).To(gomega.BeTrue())
+				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+				w1Requeued := apimeta.IsStatusConditionTrue(workerLowW1.Status.Conditions, kueue.WorkloadRequeued)
+				w2Requeued := apimeta.IsStatusConditionTrue(workerLowW2.Status.Conditions, kueue.WorkloadRequeued)
+				g.Expect(w1Requeued != w2Requeued).To(gomega.BeTrue())
+
+				g.Expect(workload.IsEvicted(workerLowW1) != workload.IsEvicted(workerLowW2)).To(gomega.BeTrue())
+				var evictedWorkload *kueue.Workload
+				if workload.IsEvicted(workerLowW1) {
+					evictedWorkload = workerLowW1
+				} else {
+					evictedWorkload = workerLowW2
+				}
+				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
```

  Nit: the structure of this is a bit surprising. You check for
  - exactly one "requeued"
  - (independently) exactly one "evicted"
  - whether the "evicted" one is also "preempted".
  
  Not sure why some things are independent, others are not.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T17:49:55Z

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@kshalot](https://github.com/kshalot) — 2026-03-09T17:49:55Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  It's pass-by-value though, no? I could pass the reference here, but I figured it's nicer to have this guarantee that it won't change.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T17:55:15Z

- **test/integration/multikueue/scheduler/scheduler_test.go:625** by [@kshalot](https://github.com/kshalot) — 2026-03-09T17:55:15Z
```diff
@@ -523,4 +534,122 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 			util.ExpectWorkloadsWithWorkloadPriority(worker1TestCluster.ctx, worker1TestCluster.client, managerLowWPC.Name, managerLowWPC.Value, wlKey)
 		})
 	})
+
+	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
+
+		// Fits only in worker1
+		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
+
+		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+		managerLowWl1 := &kueue.Workload{}
+		workerLowW1 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits only in worker2
+		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
+
+		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+		managerLowWl2 := &kueue.Workload{}
+		workerLowW2 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits in both workers
+		highJob := testingjob.MakeJob("high-job", managerNs.Name).
+			WorkloadPriorityClass(managerHighWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
+
+		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+		managerHighWl := &kueue.Workload{}
+		workerHighWorkload := &kueue.Workload{}
+
+		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the high-priority workload is admitted in exactly one of the workers", func() {
```

  This is equivalent to saying "workload is admitted". I just wanted to have a step for this to make sure this happens before the test continues. In your opinion, would just saying "Checking that the high-priority workload is admitted" be better? I guess it would. It doesn't promise some "exclusive" admission which is just the way Kueue always works.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T22:22:36Z

- **pkg/controller/admissionchecks/multikueue/workload.go:426** by [@kshalot](https://github.com/kshalot) — 2026-03-09T22:22:36Z
  Like I said in that comment - there is no side effect. I restore the `remote` as low as possible intentionally - I wanted to avoid a situation where someone adds a new case without adjusting the preemption gates.
  
  This way it's clear that **whatever you update**, the preemption gates shouldn't be overriden, rather than suggesting that this is some case specific to the elastic workload.
  
  WDYT?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T22:51:35Z

- **test/integration/multikueue/scheduler/scheduler_test.go:651** by [@kshalot](https://github.com/kshalot) — 2026-03-09T22:51:35Z
```diff
@@ -523,4 +534,122 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 			util.ExpectWorkloadsWithWorkloadPriority(worker1TestCluster.ctx, worker1TestCluster.client, managerLowWPC.Name, managerLowWPC.Value, wlKey)
 		})
 	})
+
+	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
+
+		// Fits only in worker1
+		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
+
+		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+		managerLowWl1 := &kueue.Workload{}
+		workerLowW1 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits only in worker2
+		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
+
+		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+		managerLowWl2 := &kueue.Workload{}
+		workerLowW2 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits in both workers
+		highJob := testingjob.MakeJob("high-job", managerNs.Name).
+			WorkloadPriorityClass(managerHighWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
+
+		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+		managerHighWl := &kueue.Workload{}
+		workerHighWorkload := &kueue.Workload{}
+
+		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the high-priority workload is admitted in exactly one of the workers", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
+				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
+
+				g.Expect((worker1Error == nil) != (worker2Error == nil)).To(gomega.BeTrue())
+				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+				w1Requeued := apimeta.IsStatusConditionTrue(workerLowW1.Status.Conditions, kueue.WorkloadRequeued)
+				w2Requeued := apimeta.IsStatusConditionTrue(workerLowW2.Status.Conditions, kueue.WorkloadRequeued)
+				g.Expect(w1Requeued != w2Requeued).To(gomega.BeTrue())
+
+				g.Expect(workload.IsEvicted(workerLowW1) != workload.IsEvicted(workerLowW2)).To(gomega.BeTrue())
+				var evictedWorkload *kueue.Workload
+				if workload.IsEvicted(workerLowW1) {
+					evictedWorkload = workerLowW1
+				} else {
+					evictedWorkload = workerLowW2
+				}
+				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
```

  This is a fair point, but there was some reasoning behind checking "requeued".
  
  I wanted to have a condition which will always fail if **both** workers get preempted. Just checking evicted won't cut it, because one of the workers will get re-admitted. So it will be requeued, but will lose the evicted condition.
  
  I restructured this expectation a little bit - I now find one evicted workload and then check that it has the required conditions, while the other workload doesn't have them. Maybe that is clearer?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T23:01:26Z

- **test/integration/multikueue/scheduler/scheduler_test.go:651** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T23:01:26Z
```diff
@@ -523,4 +534,122 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 			util.ExpectWorkloadsWithWorkloadPriority(worker1TestCluster.ctx, worker1TestCluster.client, managerLowWPC.Name, managerLowWPC.Value, wlKey)
 		})
 	})
+
+	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
+
+		// Fits only in worker1
+		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
+
+		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+		managerLowWl1 := &kueue.Workload{}
+		workerLowW1 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits only in worker2
+		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+			WorkloadPriorityClass(managerLowWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
+
+		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+		managerLowWl2 := &kueue.Workload{}
+		workerLowW2 := &kueue.Workload{}
+
+		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		// Fits in both workers
+		highJob := testingjob.MakeJob("high-job", managerNs.Name).
+			WorkloadPriorityClass(managerHighWPC.Name).
+			Queue(kueue.LocalQueueName(managerLq.Name)).
+			RequestAndLimit(corev1.ResourceCPU, "0.5").
+			RequestAndLimit(corev1.ResourceMemory, "0.5G").
+			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+			Obj()
+		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
+
+		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+		managerHighWl := &kueue.Workload{}
+		workerHighWorkload := &kueue.Workload{}
+
+		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that the high-priority workload is admitted in exactly one of the workers", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
+				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
+
+				g.Expect((worker1Error == nil) != (worker2Error == nil)).To(gomega.BeTrue())
+				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+				w1Requeued := apimeta.IsStatusConditionTrue(workerLowW1.Status.Conditions, kueue.WorkloadRequeued)
+				w2Requeued := apimeta.IsStatusConditionTrue(workerLowW2.Status.Conditions, kueue.WorkloadRequeued)
+				g.Expect(w1Requeued != w2Requeued).To(gomega.BeTrue())
+
+				g.Expect(workload.IsEvicted(workerLowW1) != workload.IsEvicted(workerLowW2)).To(gomega.BeTrue())
+				var evictedWorkload *kueue.Workload
+				if workload.IsEvicted(workerLowW1) {
+					evictedWorkload = workerLowW1
+				} else {
+					evictedWorkload = workerLowW2
+				}
+				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
```

  Yes, this is what I hoped to see. Thanks!

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T23:10:53Z

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T23:10:53Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  Ah, indeed. 
  I think I just didn't expect copying of a full struct...
  Well, it's not _huge_: 7 fields, none of them of a sub-struct type.
  Looking quickly through our code, I think the standard is rather to take a pointer, though some exceptions already exist (e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/0d4b20498109b6cc3360f366db31e64c2a9e28c8/pkg/workloadslicing/workloadslicing.go#L361)).
  So I'm on the edge. No strong opinion.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-09T23:18:41Z

- **pkg/controller/admissionchecks/multikueue/workload.go:426** by [@olekzabl](https://github.com/olekzabl) — 2026-03-09T23:18:41Z
  Makes sense.
  (I somehow disliked the fact that we're introducing a special treatment for this very field, so I wanted to scope this "specialty" down as much as possible. But I now think you're right. This special treatment is sometimes needed, and given that, let's apply it broadly, for future safety).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-09T23:52:22Z

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@kshalot](https://github.com/kshalot) — 2026-03-09T23:52:22Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  Two relatively small structs allocated on the stack seem like a low price to pay to avoid unsetting/resetting in the logic or doing an explicit DeepCopy.
  
  But if it's confusing or goes against some convention, I'm not going to argue.
  
  Also it has the pod sets from what I can see, which are not the smallest thing. It's at most 8 though

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-10T08:43:58Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:1674** by [@kshalot](https://github.com/kshalot) — 2026-03-10T08:43:57Z
```diff
@@ -1521,6 +1523,243 @@ func TestWlReconcile(t *testing.T) {
 				},
 			},
 		},
+		"creating remote workloads with preemption gates": {
+			features: map[featuregate.Feature]bool{
+				features.MultiKueueOrchestratedPreemption: true,
+			},
+			reconcileFor: "wl1",
+
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+
+			useSecondWorker: true,
+
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					NominatedClusterNames("worker1", "worker2").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					Obj(),
+			},
+			wantWorker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					Obj(),
+			},
+		},
+		"opening preemption gate for first workload requiring preemption": {
+			features: map[featuregate.Feature]bool{
+				features.MultiKueueOrchestratedPreemption: true,
+			},
+			reconcileFor: "wl1",
+
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+
+			worker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Obj(),
+			},
+			useSecondWorker: true,
+			worker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(earlier),
+					}).
+					Obj(),
+			},
+
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{Name: "ac1", State: kueue.CheckStatePending}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuotaAt(utiltestingapi.MakeAdmission("q1").Obj(), now).
+					NominatedClusterNames("worker1", "worker2").
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobManagedByKueueBuilder.Clone().Obj(),
+			},
+			wantWorker1Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(kueue.PreemptionGateState{
+						Name:               constants.MultiKueuePreemptionGate,
+						State:              kueue.GateStateClosed,
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(now),
+					}).
+					Obj(),
+			},
+			wantWorker2Workloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					Label(kueue.MultiKueueOriginLabel, defaultOrigin).
+					PreemptionGates(kueue.PreemptionGate{Name: constants.MultiKueuePreemptionGate}).
+					PreemptionGateStates(
+						kueue.PreemptionGateState{
+							Name:               constants.MultiKueuePreemptionGate,
+							State:              kueue.GateStateOpen,
+							LastTransitionTime: metav1.NewTime(now),
+						},
+					).
+					Condition(metav1.Condition{
+						Type:               kueue.WorkloadPreemptionBlocked,
+						Status:             metav1.ConditionTrue,
+						Reason:             kueue.PreemptionGated,
+						Message:            "Preemption gate closed",
+						LastTransitionTime: metav1.NewTime(earlier),
+					}).
+					Obj(),
+			},
+		},
+		"not opening preemption gate if timeout did not elapse": {
```

  Yep. Good point. Added.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-10T08:46:25Z

- **pkg/controller/admissionchecks/multikueue/workload.go:808** by [@kshalot](https://github.com/kshalot) — 2026-03-10T08:46:25Z
```diff
@@ -781,21 +805,23 @@ func (w *wlReconciler) Generic(_ event.GenericEvent) bool {
 func newWlReconciler(c client.Client, helper *admissioncheck.MultiKueueStoreHelper, cRec *clustersReconciler, origin string,
 	recorder record.EventRecorder, workerLostTimeout, eventsBatchPeriod time.Duration,
 	adapters map[string]jobframework.MultiKueueAdapter, dispatcherName string, roleTracker *roletracker.RoleTracker,
+	singleClusterPreemptionTimeout time.Duration,
```

  Indeed, adding another argument here felt very bad when I was writing this, but I also saw that this is a private function that is called in just a handful of places I think. So I passed the arg and went on with my life.
  
  If you feel strongly about it, then I can consider refactoring this in this PR, but we can also revisit this later (to not extend the diff even further).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-10T10:30:52Z

- **pkg/controller/admissionchecks/multikueue/workload.go:518** by [@kshalot](https://github.com/kshalot) — 2026-03-10T10:30:52Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
```

  Indeed - PTAL @ https://github.com/kubernetes-sigs/kueue/pull/9721/commits/7ceb7a59a102d9d04bfa6678664ebb65844c26d7.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-10T15:51:27Z

- **pkg/controller/admissionchecks/multikueue/workload.go:532** by [@olekzabl](https://github.com/olekzabl) — 2026-03-10T15:51:24Z
```diff
@@ -503,7 +513,31 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
-	return w.nominateAndSynchronizeWorkers(ctx, group)
+	var requeueAfterSynchronize time.Duration
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+
+			// Ensure there is a reconcile that considers the next preemption gate.
+			requeueAfterSynchronize = w.singleClusterPreemptionTimeout
+		}
+	}
+
+	res, err := w.nominateAndSynchronizeWorkers(ctx, group)
+	if err == nil && requeueAfterSynchronize < res.RequeueAfter {
```

  AFAICS `nominateASW` can often return `Result{}`, in which `Requeue` will be false and `RequeueAfter` will be 0. Then, this condition will not match, so you'll not schedule a re-run. Is that WAI?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-10T16:07:43Z

- **pkg/controller/admissionchecks/multikueue/workload.go:532** by [@kshalot](https://github.com/kshalot) — 2026-03-10T16:07:43Z
```diff
@@ -503,7 +513,31 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
-	return w.nominateAndSynchronizeWorkers(ctx, group)
+	var requeueAfterSynchronize time.Duration
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+
+			// Ensure there is a reconcile that considers the next preemption gate.
+			requeueAfterSynchronize = w.singleClusterPreemptionTimeout
+		}
+	}
+
+	res, err := w.nominateAndSynchronizeWorkers(ctx, group)
+	if err == nil && requeueAfterSynchronize < res.RequeueAfter {
```

  Oops, good catch. I added a `res.RequeueAfter != 0` but it's gone, I must have removed it by mistake.
  
  I re-added it, thanks!

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-10T21:33:19Z

- **pkg/controller/admissionchecks/multikueue/workload.go:532** by [@olekzabl](https://github.com/olekzabl) — 2026-03-10T21:33:19Z
```diff
@@ -503,7 +513,31 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
-	return w.nominateAndSynchronizeWorkers(ctx, group)
+	var requeueAfterSynchronize time.Duration
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+
+			// Ensure there is a reconcile that considers the next preemption gate.
+			requeueAfterSynchronize = w.singleClusterPreemptionTimeout
+		}
+	}
+
+	res, err := w.nominateAndSynchronizeWorkers(ctx, group)
+	if err == nil && requeueAfterSynchronize < res.RequeueAfter {
```

  Wait... I think [your last commit](https://github.com/kubernetes-sigs/kueue/pull/9721/commits/29b0e76ec93cf22825cef971bc4d82421ca4cea1) doesn't change anything?
  
  If `res.RequeueAfter` is larger than the timeout, then certainly it's `!= 0`, so `&&`-ing this is a no-op.
  
  However, I'd like to schedule a re-run also in the case when `res.RequeueAfter == 0`.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-10T21:40:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@olekzabl](https://github.com/olekzabl) — 2026-03-10T21:40:01Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  > _But if it's confusing or goes against some convention, I'm not going to argue._
  
  In my feeling, it goes against the dominant trend, though exceptions exist.
  Leaving the choice to you.
  
  > _Also it has the pod sets from what I can see, which are not the smallest thing. It's at most 8 though_
  
  FWIW, pod sets are a slice, and my understanding of [this](https://go.dev/tour/moretypes/8) is that value-passing a slice will not lead to copying the actual elements. (If I'm wrong, I'll be happy to be educated).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-11T10:25:57Z

- **pkg/controller/admissionchecks/multikueue/workload.go:541** by [@kshalot](https://github.com/kshalot) — 2026-03-11T10:25:57Z
```diff
@@ -501,11 +511,25 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		} else {
 			return reconcile.Result{}, w.updateACS(ctx, group.local, acs, kueue.CheckStateRetry, "Reserving remote lost")
 		}
+	} else if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
 	}
 
 	return w.nominateAndSynchronizeWorkers(ctx, group)
 }
 
+func isRemoteSpecOutOfSync(local, remote kueue.WorkloadSpec) bool {
+	remote.PreemptionGates = nil
```

  > FWIW, pod sets are a slice, and my understanding of [this](https://go.dev/tour/moretypes/8) is that value-passing a slice will not lead to copying the actual elements. (If I'm wrong, I'll be happy to be educated).
  
  Yes, obviously only the header of the slice will be copied. I went too far there.
  
  > In my feeling, it goes against the dominant trend, though exceptions exist.
  Leaving the choice to you.
  
  I prefer leaving it as is, i.e. passing the struct and not the pointer. Mainly because:
  
  > I'd consider restoring the previous state before returning
  
  This doesn't feel clean to me. Another alternative is doing sth like `remote.DeepCopy()`, which beats the purpose completely.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-11T12:33:17Z

- **pkg/controller/admissionchecks/multikueue/workload.go:420** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-11T12:33:17Z
```diff
@@ -410,7 +411,13 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 	// - elastic workloads which have been scaled down
 	// - workloads for which workload priority has changed
 	for rem, remWl := range group.remotes {
-		if remWl != nil && !equality.Semantic.DeepEqual(group.local.Spec, remWl.Spec) {
+		if remWl == nil {
+			continue
+		}
+
+		if isRemoteSpecOutOfSync(group.local.Spec, remWl.Spec) {
+			remotePreemptionGates := remWl.Spec.PreemptionGates
+
```

  Why split it like this? If you want to keep the nested if you can do the if as before.
  If you would like to remove the indentation and use "continue" you could do it as "remWl == nil || isRemoteSpecOutOfSync(...)".
  No point in mixing both approaches as their benefits are not having the downsides of the other, while here we have downsides of both (readability wise).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-11T13:16:45Z

- **pkg/controller/admissionchecks/multikueue/workload.go:420** by [@kshalot](https://github.com/kshalot) — 2026-03-11T13:16:45Z
```diff
@@ -410,7 +411,13 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 	// - elastic workloads which have been scaled down
 	// - workloads for which workload priority has changed
 	for rem, remWl := range group.remotes {
-		if remWl != nil && !equality.Semantic.DeepEqual(group.local.Spec, remWl.Spec) {
+		if remWl == nil {
+			continue
+		}
+
+		if isRemoteSpecOutOfSync(group.local.Spec, remWl.Spec) {
+			remotePreemptionGates := remWl.Spec.PreemptionGates
+
```

  Likely leftover from some work-in-progress logic. I inlined it.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-11T18:53:56Z

- **pkg/scheduler/scheduler.go:312** by [@kshalot](https://github.com/kshalot) — 2026-03-11T18:53:56Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  @mimowo We discussed how the scheduling hashes impact this feature. AFAIR we wanted to make `HasClosedPreemptionGate` part of the hash and assign a `NoFit`. Now that I think about it, it might be a bit more complex than that.
  
  If we have two equivalent workloads A and B, both of them having a closed preemption gate, if we start scheduling A and we want to skip B, we'll have to update the `PreemptionBlocked` condition for A and B, otherwise B might not get ungated. But if B gets ungated, it's possible that it won't be scheduled in this cluster for a long time, while it could be scheduled in another one with a preemption. So the B workload could be prevented from making progress for at most `SingleClusterPreemptionTimeout` (because the gate was opened in a cluster where it won't become the head).
  
  We could keep it as is, where the mode is still `Preempt` instead of `NoFit`. I think in terms of semantics, this case is closer to `preemptionTargets == 0` than a regular `NoFit`.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-11T21:17:14Z

- **test/e2e/multikueue/e2e_test.go:2166** by [@olekzabl](https://github.com/olekzabl) — 2026-03-11T21:15:58Z
```diff
@@ -2104,24 +2113,152 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 	})
 
 	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
-		var defaultManagerKueueCfg *kueueconfig.Configuration
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
 
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
 				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
-				newCfg := defaultManagerKueueCfg.DeepCopy()
-				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, newCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
 					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
-				})
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
 			})
 		})
 		ginkgo.AfterAll(func() {
 			ginkgo.By("reverting the configuration", func() {
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
 			})
 		})
 
 		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.5").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
```

  Nit:
  AFAICS this generally fits on both workers.
  (In particular, it has the same requests as `highJob` below which you described as fitting both).
  Though I understand it fits only worker2 _right now_ because worker1's ephemeral storage is currently fully consumed.
  
  One easy way to make it unambiguous is to raise RAM request 0.5G -> 1.5G.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T08:04:31Z

- **pkg/scheduler/scheduler.go:312** by [@mimowo](https://github.com/mimowo) — 2026-03-12T08:04:31Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  Thank you for raising that. I will probably be able to look deeper into that only on Friday after the patch releases are out. 
  
  In the meanwhile - is it feasible to design an integration/e2e test which would confirm the behavior matches our desired semantics? That would be valuable for this implementation, and also in case we make a mistake and need to refactor.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-12T09:07:29Z

- **test/e2e/multikueue/e2e_test.go:2166** by [@kshalot](https://github.com/kshalot) — 2026-03-12T09:07:29Z
```diff
@@ -2104,24 +2113,152 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 	})
 
 	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
-		var defaultManagerKueueCfg *kueueconfig.Configuration
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
 
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
 				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
-				newCfg := defaultManagerKueueCfg.DeepCopy()
-				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, newCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
 					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
-				})
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
 			})
 		})
 		ginkgo.AfterAll(func() {
 			ginkgo.By("reverting the configuration", func() {
 				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
 			})
 		})
 
 		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.5").
+				RequestAndLimit(corev1.ResourceMemory, "0.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
```

  I changed it to 1.5G but also lowered the ram and CPU for other workloads so the preemption doesn't happen on the manager.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-12T10:48:18Z

- **pkg/scheduler/scheduler.go:312** by [@olekzabl](https://github.com/olekzabl) — 2026-03-12T10:48:18Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  +1 for keeping it as is. Then, it's rather clear that gated workloads are seen as `Preempt`, not `NoFit`, so they don't "participate in the hashing game", and various edge cases are avoided.
  
  We might reconsider it later, if we see that hashing brings significant gains which suggest "joining the game". But the current code feels like a safe start.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T11:22:26Z

- **pkg/scheduler/scheduler.go:312** by [@mimowo](https://github.com/mimowo) — 2026-03-12T11:22:26Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  I think the hashing is rather a distraction at this point, we can adjust it if needed 
  
  The fundamental question is: do we want the semantics of "Preempt" or "NoFit"? Preempt is actively blocking the head of the queue, while NoFit gives other workloads a chance to schedule. 
  
  At this point I'm not sure yet which semantics fits better the problem we have.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-12T13:05:52Z

- **pkg/scheduler/scheduler.go:312** by [@kshalot](https://github.com/kshalot) — 2026-03-12T13:05:52Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  I'm writing a test for this, but I think what's missing in this PR is adding a new requeue reason:
  ```go
  RequeueReasonPreemptionGated       RequeueReason = "PreemptionGated"
  ```
  
  This way the workload will be moved back to the inadmissible list, which IMO is the desired semantics here.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-12T16:17:01Z

- **pkg/scheduler/scheduler.go:312** by [@kshalot](https://github.com/kshalot) — 2026-03-12T16:17:01Z
```diff
@@ -279,21 +287,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
```

  FYI I added code that would make this work: https://github.com/kubernetes-sigs/kueue/pull/9721/commits/e5513fddb9421dfc24ea401745436422f03c8249.
  
  As far as tests go, I tried to come up with some MultiKueue-specific way of testing this, but I don't think it's easily doable. I opted for a singlecluster test for the "stickiness" issue: https://github.com/kubernetes-sigs/kueue/pull/9721/commits/a7050fe879d67169d917fabb24bd99b9af679cad
  
  Since the scheduler does not differentiate between a "multikueue gate" and any other gate, this test is even better than a MK-specific one.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-03-12T21:11:04Z

- **test/integration/multikueue/scheduler/scheduler_test.go:538** by [@olekzabl](https://github.com/olekzabl) — 2026-03-12T20:55:28Z
```diff
@@ -534,125 +534,137 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 		})
 	})
 
-	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
-
-		// Fits only in worker1
-		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
-
-		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
-
-		managerLowWl1 := &kueue.Workload{}
-		workerLowW1 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Fits only in worker2
-		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "1.5G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
-
-		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
-
-		managerLowWl2 := &kueue.Workload{}
-		workerLowW2 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Can fit in both workers after preemptions
-		highJob := testingjob.MakeJob("high-job", managerNs.Name).
-			WorkloadPriorityClass(managerHighWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
-
-		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
-
-		managerHighWl := &kueue.Workload{}
-		workerHighWorkload := &kueue.Workload{}
-
-		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
-				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
-				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
-
-				g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
-				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
-			gomega.Consistently(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-
-				var evictedWorkload *kueue.Workload
-				var otherWorkload *kueue.Workload
-				if workload.IsEvicted(workerLowW1) {
-					evictedWorkload = workerLowW1
-					otherWorkload = workerLowW2
-				} else {
-					evictedWorkload = workerLowW2
-					otherWorkload = workerLowW1
-				}
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", func() {
+		var (
+			lowWlKey1     types.NamespacedName
+			managerLowWl1 *kueue.Workload
+			workerLowWl1  *kueue.Workload
+
+			lowWlKey2     types.NamespacedName
+			managerLowWl2 *kueue.Workload
+			workerLowWl2  *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
```

  Let me just double-check: given how much of the "scenario" you pulled into this `BeforeEach`, is it really intended that you still have a single `It` inside this `When`?
  
  When reading this refactor, I assumed it's purposed for introducing some other test scenario (a similar one so that `BeforeEach` would capture "the common prep") - but this didn't happen.

- **pkg/controller/core/workload_controller.go:1055** by [@olekzabl](https://github.com/olekzabl) — 2026-03-12T21:10:53Z
```diff
@@ -1006,6 +1049,13 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		// and are not supposed to actually change anything.
 		r.cache.AddOrUpdateWorkload(log, wlCopy)
 	}
+
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		if workload.HasClosedPreemptionGate(e.ObjectOld) && !workload.HasClosedPreemptionGate(e.ObjectNew) {
+			r.queues.QueueAssociatedInadmissibleWorkloadsAfter(ctx, wlKey, nil)
```

  LGTM but I wonder why not queue only the workload under concern.
  (At the first glance, it sounds like achieving same effect but with less work on the scheduler side).
  
  However, I'm fine if you postpone considering this to after this PR.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-12T21:32:58Z

- **test/integration/multikueue/scheduler/scheduler_test.go:538** by [@kshalot](https://github.com/kshalot) — 2026-03-12T21:32:57Z
```diff
@@ -534,125 +534,137 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 		})
 	})
 
-	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
-
-		// Fits only in worker1
-		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
-
-		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
-
-		managerLowWl1 := &kueue.Workload{}
-		workerLowW1 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Fits only in worker2
-		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "1.5G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
-
-		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
-
-		managerLowWl2 := &kueue.Workload{}
-		workerLowW2 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Can fit in both workers after preemptions
-		highJob := testingjob.MakeJob("high-job", managerNs.Name).
-			WorkloadPriorityClass(managerHighWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
-
-		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
-
-		managerHighWl := &kueue.Workload{}
-		workerHighWorkload := &kueue.Workload{}
-
-		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
-				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
-				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
-
-				g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
-				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
-			gomega.Consistently(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-
-				var evictedWorkload *kueue.Workload
-				var otherWorkload *kueue.Workload
-				if workload.IsEvicted(workerLowW1) {
-					evictedWorkload = workerLowW1
-					otherWorkload = workerLowW2
-				} else {
-					evictedWorkload = workerLowW2
-					otherWorkload = workerLowW1
-				}
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", func() {
+		var (
+			lowWlKey1     types.NamespacedName
+			managerLowWl1 *kueue.Workload
+			workerLowWl1  *kueue.Workload
+
+			lowWlKey2     types.NamespacedName
+			managerLowWl2 *kueue.Workload
+			workerLowWl2  *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
```

  I was working on a new integration test but dropped it. I then remembered that I already committed an identical structure of the test (`When(is enabled)`) in the e2e test, so I figured I'll leave it for consistency. Since for now there is only one test, we can drop it in both cases.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T09:33:09Z

- **pkg/controller/core/workload_controller.go:1055** by [@kshalot](https://github.com/kshalot) — 2026-03-13T09:33:09Z
```diff
@@ -1006,6 +1049,13 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		// and are not supposed to actually change anything.
 		r.cache.AddOrUpdateWorkload(log, wlCopy)
 	}
+
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		if workload.HasClosedPreemptionGate(e.ObjectOld) && !workload.HasClosedPreemptionGate(e.ObjectNew) {
+			r.queues.QueueAssociatedInadmissibleWorkloadsAfter(ctx, wlKey, nil)
```

  Actually, this might not be needed at all... I'm triple checking.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-13T09:55:11Z

- **pkg/controller/admissionchecks/multikueue/workload.go:516** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-13T09:55:11Z
```diff
@@ -503,7 +509,30 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
```

  Similar to how other steps are numbered and have comments to describe them - this seems like a good place to add another one of those, e.g.:
  
  7. Open preemption gate if orchestrated preemptions are enabled.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-13T10:04:18Z

- **pkg/controller/admissionchecks/multikueue/workload.go:533** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-13T10:04:18Z
```diff
@@ -503,7 +509,30 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
-	return w.nominateAndSynchronizeWorkers(ctx, group)
+	var requeueAfterSynchronize time.Duration
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient, requeueIn := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
+		// Ensure there is a reconcile that considers the next preemption gate.
+		requeueAfterSynchronize = requeueIn
+	}
+
+	res, err := w.nominateAndSynchronizeWorkers(ctx, group)
```

  Does nominateAndSynchronizeWorkers ever return a non empty res? If not you can just make it return err and set requeueAfter without unnecessary ifs.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-13T10:16:57Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1062** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-13T10:16:57Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
+	var previousUngateTime *metav1.Time
+	var oldestPreemptionSignalTime *metav1.Time
+	var workloadToUngate *kueue.Workload
+	var remote *remoteClient
+
+	for name, wl := range group.remotes {
+		if wl == nil {
+			continue
+		}
+
+		openMkGateStateIdx := slices.IndexFunc(wl.Status.PreemptionGates, func(gate kueue.PreemptionGateState) bool {
+			return gate.Name == constants.MultiKueuePreemptionGate && gate.State == kueue.GateStateOpen
+		})
+		if openMkGateStateIdx == -1 {
+			preemptionSignalCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreemptionBlocked)
+			wlRequiresPreemption := preemptionSignalCond != nil && preemptionSignalCond.Status == metav1.ConditionTrue
+			wlHasOlderPreemptionSignal := oldestPreemptionSignalTime == nil || preemptionSignalCond.LastTransitionTime.Before(oldestPreemptionSignalTime)
+			if wlRequiresPreemption && wlHasOlderPreemptionSignal {
+				workloadToUngate = wl
+				remote = group.remoteClients[name]
+				oldestPreemptionSignalTime = &preemptionSignalCond.LastTransitionTime
+			}
+		} else {
+			openMkGateState := wl.Status.PreemptionGates[openMkGateStateIdx]
+			gateOpenedTime := openMkGateState.LastTransitionTime
+			if previousUngateTime == nil || gateOpenedTime.After(previousUngateTime.Time) {
+				previousUngateTime = &gateOpenedTime
+			}
+		}
+	}
+
+	var timeSinceUngate time.Duration
+	if previousUngateTime == nil {
+		timeSinceUngate = 0
+	} else {
+		timeSinceUngate = w.clock.Now().Sub(previousUngateTime.Time)
+	}
+	timeLeftInTimeout := w.singleClusterPreemptionTimeout - timeSinceUngate
```

  Nit: a little shorter (left timeSinceUngate in a separate line for readability - up to u if u prefer to roll it into the line below).
  ```suggestion
  	timeLeftInTimeout := w.singleClusterPreemptionTimeout
  	if previousUngateTime != nil {
  	    timeSinceUngate := w.clock.Now().Sub(previousUngateTime.Time)
  		timeLeftInTimeout -= timeSinceUngate
  	}
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T10:22:30Z

- **pkg/controller/admissionchecks/multikueue/workload.go:533** by [@kshalot](https://github.com/kshalot) — 2026-03-13T10:22:30Z
```diff
@@ -503,7 +509,30 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 		}
 	}
 
-	return w.nominateAndSynchronizeWorkers(ctx, group)
+	var requeueAfterSynchronize time.Duration
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		remWl, remClient, requeueIn := w.workloadToOpenPreemptionGate(group)
+
+		if remWl != nil && remClient != nil {
+			workload.SetPreemptionGateState(remWl, constants.MultiKueuePreemptionGate, kueue.GateStateOpen, metav1.NewTime(w.clock.Now()))
+			if err := remClient.client.Status().Update(ctx, remWl); err != nil {
+				return reconcile.Result{}, fmt.Errorf("failed to update remote workload: %w", err)
+			}
+		}
+		// Ensure there is a reconcile that considers the next preemption gate.
+		requeueAfterSynchronize = requeueIn
+	}
+
+	res, err := w.nominateAndSynchronizeWorkers(ctx, group)
```

  Right now I think it doesn't, but I wanted to be defensive about it and handle this according to the return type. I don't think it's a huge deal either way.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T10:24:30Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1062** by [@kshalot](https://github.com/kshalot) — 2026-03-13T10:24:30Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
+	var previousUngateTime *metav1.Time
+	var oldestPreemptionSignalTime *metav1.Time
+	var workloadToUngate *kueue.Workload
+	var remote *remoteClient
+
+	for name, wl := range group.remotes {
+		if wl == nil {
+			continue
+		}
+
+		openMkGateStateIdx := slices.IndexFunc(wl.Status.PreemptionGates, func(gate kueue.PreemptionGateState) bool {
+			return gate.Name == constants.MultiKueuePreemptionGate && gate.State == kueue.GateStateOpen
+		})
+		if openMkGateStateIdx == -1 {
+			preemptionSignalCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreemptionBlocked)
+			wlRequiresPreemption := preemptionSignalCond != nil && preemptionSignalCond.Status == metav1.ConditionTrue
+			wlHasOlderPreemptionSignal := oldestPreemptionSignalTime == nil || preemptionSignalCond.LastTransitionTime.Before(oldestPreemptionSignalTime)
+			if wlRequiresPreemption && wlHasOlderPreemptionSignal {
+				workloadToUngate = wl
+				remote = group.remoteClients[name]
+				oldestPreemptionSignalTime = &preemptionSignalCond.LastTransitionTime
+			}
+		} else {
+			openMkGateState := wl.Status.PreemptionGates[openMkGateStateIdx]
+			gateOpenedTime := openMkGateState.LastTransitionTime
+			if previousUngateTime == nil || gateOpenedTime.After(previousUngateTime.Time) {
+				previousUngateTime = &gateOpenedTime
+			}
+		}
+	}
+
+	var timeSinceUngate time.Duration
+	if previousUngateTime == nil {
+		timeSinceUngate = 0
+	} else {
+		timeSinceUngate = w.clock.Now().Sub(previousUngateTime.Time)
+	}
+	timeLeftInTimeout := w.singleClusterPreemptionTimeout - timeSinceUngate
```

  I'm not sure I prefer this actually. I like the explicit `timeSinceUngate = 0` in the original implementation. I like that it's not doing `-=`. Maybe someone else has an opinion, but this is a tiny implementation detail anyway.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-13T10:31:27Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1024** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-13T10:31:27Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
```

  Any point to returning both the workload and client here? You can just return the "name" and get the wl and client as needed outside of the "workloadToOpenPreemptionGate" function.
  It improves readability as you dont have to do the mental gymnastics of "what combination of nil and ptr can the result of this func have" - just return the name and leave the nil checking to the function calling this one.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-13T10:35:40Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1072** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-13T10:35:40Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
+	var previousUngateTime *metav1.Time
+	var oldestPreemptionSignalTime *metav1.Time
+	var workloadToUngate *kueue.Workload
+	var remote *remoteClient
+
+	for name, wl := range group.remotes {
+		if wl == nil {
+			continue
+		}
+
+		openMkGateStateIdx := slices.IndexFunc(wl.Status.PreemptionGates, func(gate kueue.PreemptionGateState) bool {
+			return gate.Name == constants.MultiKueuePreemptionGate && gate.State == kueue.GateStateOpen
+		})
+		if openMkGateStateIdx == -1 {
+			preemptionSignalCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreemptionBlocked)
+			wlRequiresPreemption := preemptionSignalCond != nil && preemptionSignalCond.Status == metav1.ConditionTrue
+			wlHasOlderPreemptionSignal := oldestPreemptionSignalTime == nil || preemptionSignalCond.LastTransitionTime.Before(oldestPreemptionSignalTime)
+			if wlRequiresPreemption && wlHasOlderPreemptionSignal {
+				workloadToUngate = wl
+				remote = group.remoteClients[name]
+				oldestPreemptionSignalTime = &preemptionSignalCond.LastTransitionTime
+			}
+		} else {
+			openMkGateState := wl.Status.PreemptionGates[openMkGateStateIdx]
+			gateOpenedTime := openMkGateState.LastTransitionTime
+			if previousUngateTime == nil || gateOpenedTime.After(previousUngateTime.Time) {
+				previousUngateTime = &gateOpenedTime
+			}
+		}
+	}
+
+	var timeSinceUngate time.Duration
+	if previousUngateTime == nil {
+		timeSinceUngate = 0
+	} else {
+		timeSinceUngate = w.clock.Now().Sub(previousUngateTime.Time)
+	}
+	timeLeftInTimeout := w.singleClusterPreemptionTimeout - timeSinceUngate
+	// If the timeout elapsed or no workload was ungated yet, return the workload with the oldest signal.
+	if previousUngateTime == nil || timeLeftInTimeout <= 0 {
+		var rescheduleIn time.Duration
+		if workloadToUngate != nil {
+			rescheduleIn = timeLeftInTimeout
+		}
+		return workloadToUngate, remote, rescheduleIn
+	}
+
+	return nil, nil, timeLeftInTimeout
```

  Nit: readability impr proposal.
  ```suggestion
  	if previousUngateTime != nil && timeLeftInTimeout > 0 {
  	    return nil, nil, timeLeftInTimeout
  	}
  	if workloadToUngate == nil {
  	    return nil, nil, 0
  	}
  	return workloadToUngate, remote, timeLeftInTimeout
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:22:53Z

- **apis/config/v1beta1/configuration_types.go:286** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:22:53Z
```diff
@@ -280,6 +280,11 @@ type MultiKueue struct {
 	// +optional
 	WorkerLostTimeout *metav1.Duration `json:"workerLostTimeout,omitempty"`
 
+	// The timeout after which another worker cluster replica can be ungated, measured since the previous time a replica was ungated.
+	// Defaults to 5 minutes.
+	// +optional
+	SingleClusterPreemptionTimeout *metav1.Duration `json:"singleClusterPreemptionTimeout,omitempty"`
```

  Maybe we could refrain from making it configuratble in Alpha. The reason is that I foresee when graduating to Beta we will need to introduce more preemption knobs, as discussed in https://github.com/kubernetes-sigs/kueue/pull/9721#issuecomment-4054172653
  
  So, I foresee in the long run we will want to have some grouping of the configuration options, like
  
  ```yaml
  multiKueue:
    preemptionControl:
      mode: Sequential # or Concurrent
      singleClusterTimeout: 5min
  ```
  wdyt? Do we foresee the need to move the timeout in alpha? I would prefer to avoid the need to restructure the API later on.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T11:38:06Z

- **apis/config/v1beta1/configuration_types.go:286** by [@kshalot](https://github.com/kshalot) — 2026-03-13T11:38:06Z
```diff
@@ -280,6 +280,11 @@ type MultiKueue struct {
 	// +optional
 	WorkerLostTimeout *metav1.Duration `json:"workerLostTimeout,omitempty"`
 
+	// The timeout after which another worker cluster replica can be ungated, measured since the previous time a replica was ungated.
+	// Defaults to 5 minutes.
+	// +optional
+	SingleClusterPreemptionTimeout *metav1.Duration `json:"singleClusterPreemptionTimeout,omitempty"`
```

  Yes, in the context of the beta graduation discussion, dropping this configuration option makes perfect sense.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:44:47Z

- **apis/config/v1beta1/configuration_types.go:286** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:44:47Z
```diff
@@ -280,6 +280,11 @@ type MultiKueue struct {
 	// +optional
 	WorkerLostTimeout *metav1.Duration `json:"workerLostTimeout,omitempty"`
 
+	// The timeout after which another worker cluster replica can be ungated, measured since the previous time a replica was ungated.
+	// Defaults to 5 minutes.
+	// +optional
+	SingleClusterPreemptionTimeout *metav1.Duration `json:"singleClusterPreemptionTimeout,omitempty"`
```

  ok, please submit a small KEP update then and also simplify the impl pr

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T13:52:22Z

- **pkg/controller/core/workload_controller.go:1055** by [@kshalot](https://github.com/kshalot) — 2026-03-13T13:52:22Z
```diff
@@ -1006,6 +1049,13 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		// and are not supposed to actually change anything.
 		r.cache.AddOrUpdateWorkload(log, wlCopy)
 	}
+
+	if features.Enabled(features.MultiKueueOrchestratedPreemption) {
+		if workload.HasClosedPreemptionGate(e.ObjectOld) && !workload.HasClosedPreemptionGate(e.ObjectNew) {
+			r.queues.QueueAssociatedInadmissibleWorkloadsAfter(ctx, wlKey, nil)
```

  https://github.com/kubernetes-sigs/kueue/pull/9721/commits/28fd78dd497fbe726e3bd174c98149f7c0d048b8 - I thought it wasn't working, that's why I queued the whole queue. But I found that I was just hitting a filter.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T14:31:21Z

- **test/integration/multikueue/scheduler/scheduler_test.go:538** by [@kshalot](https://github.com/kshalot) — 2026-03-13T14:31:21Z
```diff
@@ -534,125 +534,137 @@ var _ = ginkgo.Describe("MultiKueue with scheduler", ginkgo.Label("area:multikue
 		})
 	})
 
-	ginkgo.It("should not trigger concurrent preemptions when MultiKueueOrchestratedPreemption is enabled", func() {
-		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.MultiKueueOrchestratedPreemption, true)
-
-		// Fits only in worker1
-		lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob1)
-
-		lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
-
-		managerLowWl1 := &kueue.Workload{}
-		workerLowW1 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey1, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Fits only in worker2
-		lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
-			WorkloadPriorityClass(managerLowWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "1.5G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, lowJob2)
-
-		lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
-
-		managerLowWl2 := &kueue.Workload{}
-		workerLowW2 := &kueue.Workload{}
-
-		ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-				g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
-
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey2, &kueue.Workload{})).To(testing.BeNotFoundError())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		// Can fit in both workers after preemptions
-		highJob := testingjob.MakeJob("high-job", managerNs.Name).
-			WorkloadPriorityClass(managerHighWPC.Name).
-			Queue(kueue.LocalQueueName(managerLq.Name)).
-			RequestAndLimit(corev1.ResourceCPU, "0.1").
-			RequestAndLimit(corev1.ResourceMemory, "0.1G").
-			RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
-			Obj()
-		util.MustCreate(managerTestCluster.ctx, managerTestCluster.client, highJob)
-
-		highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
-
-		managerHighWl := &kueue.Workload{}
-		workerHighWorkload := &kueue.Workload{}
-
-		ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
-				g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
-			gomega.Eventually(func(g gomega.Gomega) {
-				worker1Error := worker1TestCluster.client.Get(worker1TestCluster.ctx, highWlKey, workerHighWorkload)
-				worker2Error := worker2TestCluster.client.Get(worker2TestCluster.ctx, highWlKey, workerHighWorkload)
-
-				g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
-				g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
-			}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		})
-
-		ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
-			gomega.Consistently(func(g gomega.Gomega) {
-				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
-				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
-
-				var evictedWorkload *kueue.Workload
-				var otherWorkload *kueue.Workload
-				if workload.IsEvicted(workerLowW1) {
-					evictedWorkload = workerLowW1
-					otherWorkload = workerLowW2
-				} else {
-					evictedWorkload = workerLowW2
-					otherWorkload = workerLowW1
-				}
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(evictedWorkload.Status.Conditions).To(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadEvicted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadPreempted))
-				g.Expect(otherWorkload.Status.Conditions).NotTo(testing.HaveConditionStatusTrue(kueue.WorkloadRequeued))
-			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", func() {
+		var (
+			lowWlKey1     types.NamespacedName
+			managerLowWl1 *kueue.Workload
+			workerLowWl1  *kueue.Workload
+
+			lowWlKey2     types.NamespacedName
+			managerLowWl2 *kueue.Workload
+			workerLowWl2  *kueue.Workload
+		)
+
+		ginkgo.BeforeEach(func() {
```

  Actually, I reverted this change. So this is addressed, thanks

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T14:57:03Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1024** by [@kshalot](https://github.com/kshalot) — 2026-03-13T14:57:03Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
```

  Possibly a leftover, but I don't remember exactly. I changed it to return the cluster name.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T14:57:11Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1072** by [@kshalot](https://github.com/kshalot) — 2026-03-13T14:57:11Z
```diff
@@ -990,13 +1021,70 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*kueue.Workload, *remoteClient, time.Duration) {
+	var previousUngateTime *metav1.Time
+	var oldestPreemptionSignalTime *metav1.Time
+	var workloadToUngate *kueue.Workload
+	var remote *remoteClient
+
+	for name, wl := range group.remotes {
+		if wl == nil {
+			continue
+		}
+
+		openMkGateStateIdx := slices.IndexFunc(wl.Status.PreemptionGates, func(gate kueue.PreemptionGateState) bool {
+			return gate.Name == constants.MultiKueuePreemptionGate && gate.State == kueue.GateStateOpen
+		})
+		if openMkGateStateIdx == -1 {
+			preemptionSignalCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreemptionBlocked)
+			wlRequiresPreemption := preemptionSignalCond != nil && preemptionSignalCond.Status == metav1.ConditionTrue
+			wlHasOlderPreemptionSignal := oldestPreemptionSignalTime == nil || preemptionSignalCond.LastTransitionTime.Before(oldestPreemptionSignalTime)
+			if wlRequiresPreemption && wlHasOlderPreemptionSignal {
+				workloadToUngate = wl
+				remote = group.remoteClients[name]
+				oldestPreemptionSignalTime = &preemptionSignalCond.LastTransitionTime
+			}
+		} else {
+			openMkGateState := wl.Status.PreemptionGates[openMkGateStateIdx]
+			gateOpenedTime := openMkGateState.LastTransitionTime
+			if previousUngateTime == nil || gateOpenedTime.After(previousUngateTime.Time) {
+				previousUngateTime = &gateOpenedTime
+			}
+		}
+	}
+
+	var timeSinceUngate time.Duration
+	if previousUngateTime == nil {
+		timeSinceUngate = 0
+	} else {
+		timeSinceUngate = w.clock.Now().Sub(previousUngateTime.Time)
+	}
+	timeLeftInTimeout := w.singleClusterPreemptionTimeout - timeSinceUngate
+	// If the timeout elapsed or no workload was ungated yet, return the workload with the oldest signal.
+	if previousUngateTime == nil || timeLeftInTimeout <= 0 {
+		var rescheduleIn time.Duration
+		if workloadToUngate != nil {
+			rescheduleIn = timeLeftInTimeout
+		}
+		return workloadToUngate, remote, rescheduleIn
+	}
+
+	return nil, nil, timeLeftInTimeout
```

  Done.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-16T09:04:23Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T07:43:30Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1024** by [@mimowo](https://github.com/mimowo) — 2026-03-17T07:43:31Z
```diff
@@ -990,13 +1021,71 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*string, time.Duration) {
```

  The logic here is tricky so please add some logging to at V4 to document the decision making. It will also serve as natural comments.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T07:58:24Z

- **pkg/scheduler/scheduler.go:334** by [@mimowo](https://github.com/mimowo) — 2026-03-17T07:58:24Z
```diff
@@ -300,21 +309,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
+			if len(e.preemptionTargets) == 0 {
+				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
+				// we reserve capacity if we are uncertain
+				// whether we can reclaim the capacity
+				// later. Otherwise, we allow other workloads
+				// in the Cohort to borrow this capacity,
+				// confident we can reclaim it later.
+				if !preemption.CanAlwaysReclaim(cq) {
+					// reserve capacity up to the
+					// borrowing limit, so that
+					// lower-priority workloads in another
+					// Cohort cannot admit before us.
+					cq.AddUsage(resourcesToReserve(e, cq))
+				}
+				continue
+			}
+
+			if features.Enabled(features.MultiKueueOrchestratedPreemption) && workload.HasClosedPreemptionGate(e.Obj) {
+				gatedMsg := "Workload requires preemption, but it's gated"
+				log.V(3).Info(gatedMsg)
+				setPreemptionGated(e, gatedMsg)
+				continue
```

  I like how short the solution is. And I see we only advertise opening the gate if actual preemption is possible. 
  
  Do we have a test case to show that the condition is not added if there are no preemption candidates?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T07:59:14Z

- **pkg/controller/admissionchecks/multikueue/workload.go:415** by [@mimowo](https://github.com/mimowo) — 2026-03-17T07:59:14Z
```diff
@@ -410,7 +411,9 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 	// - elastic workloads which have been scaled down
 	// - workloads for which workload priority has changed
 	for rem, remWl := range group.remotes {
-		if remWl != nil && !equality.Semantic.DeepEqual(group.local.Spec, remWl.Spec) {
+		if remWl != nil && isRemoteSpecOutOfSync(group.local.Spec, remWl.Spec) {
+			remotePreemptionGates := remWl.Spec.PreemptionGates
```

  nit but wrap with the FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T07:59:25Z

- **pkg/controller/admissionchecks/multikueue/workload.go:436** by [@mimowo](https://github.com/mimowo) — 2026-03-17T07:59:25Z
```diff
@@ -429,6 +432,9 @@ func (w *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) (reco
 			}
 
 			if updateRemote {
+				// Make sure to not overwrite preemption gates which are managed by the worker.
+				remWl.Spec.PreemptionGates = remotePreemptionGates
```

  nit, but wrap with FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T08:00:40Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1024** by [@mimowo](https://github.com/mimowo) — 2026-03-17T08:00:40Z
```diff
@@ -990,13 +1021,71 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*string, time.Duration) {
```

  Also, is this function covered 100% by unit tests? Please check with some golang coverage tool

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T08:02:04Z

LGTM, just minor comments to make sure the code is well isolated without side effects, double check test coverage, and to improve logging.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-17T12:07:34Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1024** by [@kshalot](https://github.com/kshalot) — 2026-03-17T12:07:34Z
```diff
@@ -990,13 +1021,71 @@ func updateDelayedTopologyRequest(local, remote *kueue.Workload) {
 	}
 }
 
-func cloneForCreate(orig *kueue.Workload, origin string) *kueue.Workload {
+func (w *wlReconciler) workloadToOpenPreemptionGate(group *wlGroup) (*string, time.Duration) {
```

  This function was 91% covered, I added another test to test in 95%:
  ```
  sigs.k8s.io/kueue/pkg/controller/admissionchecks/multikueue/workload.go:1029:           workloadToOpenPreemptionGate            95.3%
  ```
  
  I think we won't get to 100%, because the only line which is not "covered" is:
  ```go
  if !wlHasOlderPreemptionSignal { <- covered
      remoteWlLog.V(4).Info("Remote workload was not the first to require preemption") <- not covered
      continue <- not covered
  } <- covered
  ```
  We have a test for this (`opening preemption gate for first workload requiring preemption`), but AFAIK sometimes it just won't hit this if-statement because map iteration order is randomized.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-17T12:31:02Z

- **pkg/scheduler/scheduler.go:334** by [@kshalot](https://github.com/kshalot) — 2026-03-17T12:31:02Z
```diff
@@ -300,21 +309,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
+			if len(e.preemptionTargets) == 0 {
+				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
+				// we reserve capacity if we are uncertain
+				// whether we can reclaim the capacity
+				// later. Otherwise, we allow other workloads
+				// in the Cohort to borrow this capacity,
+				// confident we can reclaim it later.
+				if !preemption.CanAlwaysReclaim(cq) {
+					// reserve capacity up to the
+					// borrowing limit, so that
+					// lower-priority workloads in another
+					// Cohort cannot admit before us.
+					cq.AddUsage(resourcesToReserve(e, cq))
+				}
+				continue
+			}
+
+			if features.Enabled(features.MultiKueueOrchestratedPreemption) && workload.HasClosedPreemptionGate(e.Obj) {
+				gatedMsg := "Workload requires preemption, but it's gated"
+				log.V(3).Info(gatedMsg)
+				setPreemptionGated(e, gatedMsg)
+				continue
```

  Nice spot, I added more cases: https://github.com/kubernetes-sigs/kueue/pull/9721/commits/af344ea93cbd2d021eea86cfa804367931dfd2a4
  
  Specifically:
  1. That this does not signal if you fit without preemptions.
  2. That this does not signal if you do not fit.
  
  Do we need another case for when there are no preemption targets? So this line specifically:
  https://github.com/kubernetes-sigs/kueue/blob/755f97a6382127c3b6952798bdea68a2cdf57504/pkg/scheduler/scheduler.go#L303
  
  I don't know if we have the setup conditions in the unit tests already, but I'll look.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T12:44:43Z

- **pkg/scheduler/scheduler.go:334** by [@mimowo](https://github.com/mimowo) — 2026-03-17T12:44:43Z
```diff
@@ -300,21 +309,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
+			if len(e.preemptionTargets) == 0 {
+				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
+				// we reserve capacity if we are uncertain
+				// whether we can reclaim the capacity
+				// later. Otherwise, we allow other workloads
+				// in the Cohort to borrow this capacity,
+				// confident we can reclaim it later.
+				if !preemption.CanAlwaysReclaim(cq) {
+					// reserve capacity up to the
+					// borrowing limit, so that
+					// lower-priority workloads in another
+					// Cohort cannot admit before us.
+					cq.AddUsage(resourcesToReserve(e, cq))
+				}
+				continue
+			}
+
+			if features.Enabled(features.MultiKueueOrchestratedPreemption) && workload.HasClosedPreemptionGate(e.Obj) {
+				gatedMsg := "Workload requires preemption, but it's gated"
+				log.V(3).Info(gatedMsg)
+				setPreemptionGated(e, gatedMsg)
+				continue
```

  > Do we need another case for when there are no preemption targets? So this line specifically:
  
  Yes, I asked the comment to test this line specifically :) 
  
  > I don't know if we have the setup conditions in the unit tests already, but I'll look.
  
  I think it should be possible in scheduler_test.go as a unit test, but please double check

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-17T13:04:44Z

- **pkg/scheduler/scheduler.go:334** by [@kshalot](https://github.com/kshalot) — 2026-03-17T13:04:43Z
```diff
@@ -300,21 +309,30 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			continue
 		}
 
-		if mode == flavorassigner.Preempt && len(e.preemptionTargets) == 0 {
-			log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
-			// we reserve capacity if we are uncertain
-			// whether we can reclaim the capacity
-			// later. Otherwise, we allow other workloads
-			// in the Cohort to borrow this capacity,
-			// confident we can reclaim it later.
-			if !preemption.CanAlwaysReclaim(cq) {
-				// reserve capacity up to the
-				// borrowing limit, so that
-				// lower-priority workloads in another
-				// Cohort cannot admit before us.
-				cq.AddUsage(resourcesToReserve(e, cq))
+		if mode == flavorassigner.Preempt {
+			if len(e.preemptionTargets) == 0 {
+				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemption", cq.Preemption)
+				// we reserve capacity if we are uncertain
+				// whether we can reclaim the capacity
+				// later. Otherwise, we allow other workloads
+				// in the Cohort to borrow this capacity,
+				// confident we can reclaim it later.
+				if !preemption.CanAlwaysReclaim(cq) {
+					// reserve capacity up to the
+					// borrowing limit, so that
+					// lower-priority workloads in another
+					// Cohort cannot admit before us.
+					cq.AddUsage(resourcesToReserve(e, cq))
+				}
+				continue
+			}
+
+			if features.Enabled(features.MultiKueueOrchestratedPreemption) && workload.HasClosedPreemptionGate(e.Obj) {
+				gatedMsg := "Workload requires preemption, but it's gated"
+				log.V(3).Info(gatedMsg)
+				setPreemptionGated(e, gatedMsg)
+				continue
```

  Nevermind, for some reason at the moment of writing the previous comment I thought we'll need some more complex setup, but the `Preempt` + `len(preemptionTargets) == 0` is just is for example the case where the preemption target is higher priority.
  
  So `do not signal `BlockedOnPreemptionGates` when a preemption gate is present, but the workload had nothing to preempt` is already testing it 👍

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T13:15:45Z

- **test/e2e/multikueue/e2e_test.go:2244** by [@mimowo](https://github.com/mimowo) — 2026-03-17T13:15:46Z
```diff
@@ -2094,6 +2103,156 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 	})
+
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
+
+		ginkgo.BeforeAll(func() {
+			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
+				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
+					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			ginkgo.By("reverting the configuration", func() {
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
+			})
+		})
+
+		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
+			lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "1.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob2)
+
+			lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+			managerLowWl2 := &kueue.Workload{}
+			workerLowW2 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW2)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey2, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Can fit in both workers after preemptions
+			highJob := testingjob.MakeJob("high-job", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerHighWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, highJob)
+
+			highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+			managerHighWl := &kueue.Workload{}
+			workerHighWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+					g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					worker1Error := k8sWorker1Client.Get(ctx, highWlKey, workerHighWorkload)
+					worker2Error := k8sWorker2Client.Get(ctx, highWlKey, workerHighWorkload)
+
+					g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
+					g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+					var evictedWorkload *kueue.Workload
+					var otherWorkload *kueue.Workload
+					if workload.IsEvicted(workerLowW1) {
+						evictedWorkload = workerLowW1
+						otherWorkload = workerLowW2
+					} else {
+						evictedWorkload = workerLowW2
+						otherWorkload = workerLowW1
+					}
```

  just a question at this point, if we conclude we could clean up it can be a follow up: could we make the choice of the workload to evict deterministic to avoid that?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-17T13:27:12Z

- **test/e2e/multikueue/e2e_test.go:2244** by [@kshalot](https://github.com/kshalot) — 2026-03-17T13:27:12Z
```diff
@@ -2094,6 +2103,156 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 	})
+
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
+
+		ginkgo.BeforeAll(func() {
+			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
+				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
+					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			ginkgo.By("reverting the configuration", func() {
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
+			})
+		})
+
+		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
+			lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "1.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob2)
+
+			lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+			managerLowWl2 := &kueue.Workload{}
+			workerLowW2 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW2)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey2, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Can fit in both workers after preemptions
+			highJob := testingjob.MakeJob("high-job", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerHighWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, highJob)
+
+			highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+			managerHighWl := &kueue.Workload{}
+			workerHighWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+					g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					worker1Error := k8sWorker1Client.Get(ctx, highWlKey, workerHighWorkload)
+					worker2Error := k8sWorker2Client.Get(ctx, highWlKey, workerHighWorkload)
+
+					g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
+					g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+					var evictedWorkload *kueue.Workload
+					var otherWorkload *kueue.Workload
+					if workload.IsEvicted(workerLowW1) {
+						evictedWorkload = workerLowW1
+						otherWorkload = workerLowW2
+					} else {
+						evictedWorkload = workerLowW2
+						otherWorkload = workerLowW1
+					}
```

  I thought about it as well, but I did not come up with anything useful.
  
  The high priority workload has to fit in both clusters (so we can check that the preemption is not triggered in both of them). In order for this to be deterministic, we'd have to guarantee that the workload is scheduled, gated and ungated in one cluster before the other.
  
  One direction that we could investigate is changing the `Status.NominatedClusterNames` of the local workload to include only one cluster at first and then the second one. I'm not sure whether that will work cleanly though.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T13:30:32Z

- **test/e2e/multikueue/e2e_test.go:2244** by [@mimowo](https://github.com/mimowo) — 2026-03-17T13:30:31Z
```diff
@@ -2094,6 +2103,156 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 	})
+
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
+
+		ginkgo.BeforeAll(func() {
+			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
+				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
+					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			ginkgo.By("reverting the configuration", func() {
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
+			})
+		})
+
+		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
+			lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "1.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob2)
+
+			lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+			managerLowWl2 := &kueue.Workload{}
+			workerLowW2 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW2)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey2, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Can fit in both workers after preemptions
+			highJob := testingjob.MakeJob("high-job", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerHighWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, highJob)
+
+			highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+			managerHighWl := &kueue.Workload{}
+			workerHighWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+					g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					worker1Error := k8sWorker1Client.Get(ctx, highWlKey, workerHighWorkload)
+					worker2Error := k8sWorker2Client.Get(ctx, highWlKey, workerHighWorkload)
+
+					g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
+					g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+					var evictedWorkload *kueue.Workload
+					var otherWorkload *kueue.Workload
+					if workload.IsEvicted(workerLowW1) {
+						evictedWorkload = workerLowW1
+						otherWorkload = workerLowW2
+					} else {
+						evictedWorkload = workerLowW2
+						otherWorkload = workerLowW1
+					}
```

  I see, no idea for now, but I expect more tests on preemption gates to also have this non-determinism, so I would suggest introducing some helper which would allow to choose the evicted, and non-evicted workloads easier. 
  
  I'm not committed on that, but maybe two 2-fold:
  1. first helper to return the clusterName for the cluster on which the preemption happened
  2. select workload for the clusterName

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T14:57:21Z

- **test/e2e/multikueue/e2e_test.go:2244** by [@mimowo](https://github.com/mimowo) — 2026-03-17T14:57:21Z
```diff
@@ -2094,6 +2103,156 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 			})
 		})
 	})
+
+	ginkgo.When("MultiKueueOrchestratedPreemption is enabled", ginkgo.Ordered, func() {
+		var defaultManagerKueueCfg, defaultWorker1KueueCfg, defaultWorker2KueueCfg *kueueconfig.Configuration
+
+		ginkgo.BeforeAll(func() {
+			ginkgo.By("setting MultiKueueClusterProfile feature gate", func() {
+				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
+				defaultWorker1KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker1Client)
+				defaultWorker2KueueCfg = util.GetKueueConfiguration(ctx, k8sWorker2Client)
+
+				updateCfg := func(cfg *kueueconfig.Configuration) {
+					cfg.FeatureGates[string(features.MultiKueueOrchestratedPreemption)] = true
+				}
+
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg.DeepCopy(), managerClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg.DeepCopy(), worker1ClusterName, updateCfg)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg.DeepCopy(), worker2ClusterName, updateCfg)
+			})
+		})
+		ginkgo.AfterAll(func() {
+			ginkgo.By("reverting the configuration", func() {
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker1Client, defaultWorker1KueueCfg, worker1ClusterName)
+				util.UpdateKueueConfigurationAndRestart(ctx, k8sWorker2Client, defaultWorker2KueueCfg, worker2ClusterName)
+			})
+		})
+
+		ginkgo.It("should not trigger concurrent preemptions", func() {
+			// Fits only in worker1
+			lowJob1 := testingjob.MakeJob("low-job1", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "15G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob1)
+
+			lowWlKey1 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob1.Name, lowJob1.UID), Namespace: managerNs.Name}
+
+			managerLowWl1 := &kueue.Workload{}
+			workerLowW1 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the first low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey1, managerLowWl1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl1)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the first low-priority workload is created in worker1 and not in worker2", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW1)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey1, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Fits only in worker2
+			lowJob2 := testingjob.MakeJob("low-job2", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerLowWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "1.5G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, lowJob2)
+
+			lowWlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(lowJob2.Name, lowJob2.UID), Namespace: managerNs.Name}
+
+			managerLowWl2 := &kueue.Workload{}
+			workerLowW2 := &kueue.Workload{}
+
+			ginkgo.By("Checking that the second low-priority workload is created and admitted in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, lowWlKey2, managerLowWl2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(managerLowWl2)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the second low-priority workload is created in worker2 and not in worker1", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+					g.Expect(workload.IsAdmitted(workerLowW2)).To(gomega.BeTrue())
+
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey2, &kueue.Workload{})).To(utiltesting.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			// Can fit in both workers after preemptions
+			highJob := testingjob.MakeJob("high-job", managerNs.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				WorkloadPriorityClass(managerHighWPC.Name).
+				Queue(kueue.LocalQueueName(managerLq.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "0.1").
+				RequestAndLimit(corev1.ResourceMemory, "0.1G").
+				RequestAndLimit(corev1.ResourceEphemeralStorage, "5G").
+				Obj()
+			util.MustCreate(ctx, k8sManagerClient, highJob)
+
+			highWlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(highJob.Name, highJob.UID), Namespace: managerNs.Name}
+
+			managerHighWl := &kueue.Workload{}
+			workerHighWorkload := &kueue.Workload{}
+
+			ginkgo.By("Checking that the high-priority workload is created in the manager cluster", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sManagerClient.Get(ctx, highWlKey, managerHighWl)).To(gomega.Succeed())
+					g.Expect(managerHighWl.Spec.QueueName).To(gomega.BeEquivalentTo(managerLq.Name))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					worker1Error := k8sWorker1Client.Get(ctx, highWlKey, workerHighWorkload)
+					worker2Error := k8sWorker2Client.Get(ctx, highWlKey, workerHighWorkload)
+
+					g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
+					g.Expect(workload.IsAdmitted(workerHighWorkload)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that only one low-priority workload was preempted and requeued", func() {
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.Get(ctx, lowWlKey1, workerLowW1)).To(gomega.Succeed())
+					g.Expect(k8sWorker2Client.Get(ctx, lowWlKey2, workerLowW2)).To(gomega.Succeed())
+
+					var evictedWorkload *kueue.Workload
+					var otherWorkload *kueue.Workload
+					if workload.IsEvicted(workerLowW1) {
+						evictedWorkload = workerLowW1
+						otherWorkload = workerLowW2
+					} else {
+						evictedWorkload = workerLowW2
+						otherWorkload = workerLowW1
+					}
```

  Let's consider this as a follow up.
