# PR #9726: Add podset-replica-sizes annotation on RayJob

**Summary**: Add podset-replica-sizes annotation on RayJob

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9726

**Last updated**: 2026-03-16T19:17:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@hiboyang](https://github.com/hiboyang)
- **Branch**: `main-bo-podset-size-annotation` → `main`
- **Created**: 2026-03-06T18:36:19Z
- **Updated**: 2026-03-16T19:17:42Z
- **Merged**: 2026-03-16T19:17:39Z
- **Closed**: 2026-03-16T19:17:39Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 8   **+555 / -3**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This is from discussion in https://github.com/kubernetes-sigs/kueue/pull/9570, add a new annotation to serve following purpose:

1. Update RayJob resourceVersion, which can be used to compose workload name to avoid name conflict
2. For MultiKueue to sync the information back to Management cluster and handle autoscaling scenario

#### Which issue(s) this PR fixes:

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T18:36:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | feaa6b9a52bd660eef00045e1193ada3c4552145 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b84df71fc31a0008ea8233 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T18:36:30Z

Hi @hiboyang. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-06T18:43:12Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T16:45:30Z

> This is from discussion in https://github.com/kubernetes-sigs/kueue/pull/9570, add a new annotation to serve following purpose:

Are you going to use this PR as a preparation for the bugfix for https://github.com/kubernetes-sigs/kueue/issues/9523? I'm wondering how much work is needed. Maybe you could open a sibling PR with that bufix?

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T18:46:37Z

> > This is from discussion in #9570, add a new annotation to serve following purpose:
> 
> Are you going to use this PR as a preparation for the bugfix for #9523? I'm wondering how much work is needed. Maybe you could open a sibling PR with that bufix?

Yes, this PR is a preparation, might dig more into #9523 and deal with it in other PRs.

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-03-12T05:39:06Z

/retest

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-03-12T17:18:45Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T18:37:25Z

Thank you 👍 Looking forward to see the MultiKueue integration. Also, let me know if this is required for bugfixing the Autoscaling failures.
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T18:37:34Z

LGTM label has been added.  <details>Git tree hash: 942dba3cde3d428e3afe5270e192bf00029230f7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T18:37:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9726#" title="Author self-approved">hiboyang</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9726#issuecomment-4069789586" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T19:16:58Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T19:17:06Z

LGTM label has been added.  <details>Git tree hash: 88d7a992c0909e9e23753424a0ac1c007fd5f64d</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T17:28:56Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:223** by [@mimowo](https://github.com/mimowo) — 2026-03-09T17:28:56Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
+
 	rayClusterName := j.Status.RayClusterName
 	podSets, err = raycluster.UpdatePodSets(ctx, podSets, reconciler.client, j.Object(), j.Spec.RayClusterSpec.EnableInTreeAutoscaling, rayClusterName)
 	if err != nil {
 		return nil, err
 	}
 
+	// If any PodSet counts changed, record the updated PodSets as an annotation
+	var updatedPodSets []podSetReplicaSize
+	for _, ps := range podSets {
+		if original, originalExists := originalCounts[ps.Name]; !originalExists || ps.Count != original {
+			updatedPodSets = append(updatedPodSets, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
+		}
+	}
+	if len(updatedPodSets) > 0 {
+		// Only update if the annotation value has actually changed to avoid
+		// an infinite reconciliation loop: each Update() changes the
+		// resourceVersion, which triggers another reconciliation.
+		if !podSetReplicaSizesMatchAnnotation(j.Annotations, updatedPodSets) {
+			podSetsJSON, err := json.Marshal(updatedPodSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+			}
+
+			// Patch the job with the annotation, use a copy of the job object to avoid it being overwritten by the content returned by the Server.
+			objCopy := j.Object().DeepCopyObject().(client.Object)
+			patch := client.MergeFrom(objCopy.DeepCopyObject().(client.Object))
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			if err := reconciler.client.Patch(ctx, objCopy, patch); err != nil {
+				return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+			}
```

  Instead of that use our helper in pkg/util/client/client.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T17:29:49Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:249** by [@mimowo](https://github.com/mimowo) — 2026-03-09T17:29:50Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
+
 	rayClusterName := j.Status.RayClusterName
 	podSets, err = raycluster.UpdatePodSets(ctx, podSets, reconciler.client, j.Object(), j.Spec.RayClusterSpec.EnableInTreeAutoscaling, rayClusterName)
 	if err != nil {
 		return nil, err
 	}
 
+	// If any PodSet counts changed, record the updated PodSets as an annotation
+	var updatedPodSets []podSetReplicaSize
+	for _, ps := range podSets {
+		if original, originalExists := originalCounts[ps.Name]; !originalExists || ps.Count != original {
+			updatedPodSets = append(updatedPodSets, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
+		}
+	}
+	if len(updatedPodSets) > 0 {
+		// Only update if the annotation value has actually changed to avoid
+		// an infinite reconciliation loop: each Update() changes the
+		// resourceVersion, which triggers another reconciliation.
+		if !podSetReplicaSizesMatchAnnotation(j.Annotations, updatedPodSets) {
+			podSetsJSON, err := json.Marshal(updatedPodSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+			}
+
+			// Patch the job with the annotation, use a copy of the job object to avoid it being overwritten by the content returned by the Server.
+			objCopy := j.Object().DeepCopyObject().(client.Object)
+			patch := client.MergeFrom(objCopy.DeepCopyObject().(client.Object))
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			if err := reconciler.client.Patch(ctx, objCopy, patch); err != nil {
+				return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+			}
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// podSetReplicaSizesMatchAnnotation checks whether the existing annotation
+// already records the same PodSet replica sizes as the given updatedPodSets.
+func podSetReplicaSizesMatchAnnotation(annotations map[string]string, updatedPodSets []podSetReplicaSize) bool {
+	if annotations == nil {
+		return false
+	}
+	existing, ok := annotations[PodsetReplicaSizesAnnotation]
+	if !ok {
+		return false
+	}
+	var existingPodSets []podSetReplicaSize
+	if err := json.Unmarshal([]byte(existing), &existingPodSets); err != nil {
+		return false
```

  Don't silence parsing errors, I think we should propagate them up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T17:34:57Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@mimowo](https://github.com/mimowo) — 2026-03-09T17:34:57Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  Do we need the originalCounts? I think we could direclty compare the sizes from `podSets` with the annotation, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T17:36:52Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:60** by [@mimowo](https://github.com/mimowo) — 2026-03-09T17:36:53Z
```diff
@@ -52,6 +53,11 @@ const (
 	headGroupPodSetName    = "head"
 	submitterJobPodSetName = "submitter"
 	FrameworkName          = "ray.io/rayjob"
+
+	// PodsetReplicaSizesAnnotation is set on the RayJob when autoscaling causes
+	// PodSet replica sizes to differ from the original spec. The value is a JSON
+	// array compatible with []kueue.PodSet, containing only the changed PodSets.
+	PodsetReplicaSizesAnnotation = "kueue.x-k8s.io/podset-replica-sizes"
```

  We will also need some unit and (integration tests or e2e tests) for the annotation

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-09T18:32:02Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T18:32:02Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  We need originalCounts to remember the counts from RayCluster, compare them with counts from underlying RayCluster, and only add annotation when there is count change.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-09T18:44:43Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:223** by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T18:44:42Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
+
 	rayClusterName := j.Status.RayClusterName
 	podSets, err = raycluster.UpdatePodSets(ctx, podSets, reconciler.client, j.Object(), j.Spec.RayClusterSpec.EnableInTreeAutoscaling, rayClusterName)
 	if err != nil {
 		return nil, err
 	}
 
+	// If any PodSet counts changed, record the updated PodSets as an annotation
+	var updatedPodSets []podSetReplicaSize
+	for _, ps := range podSets {
+		if original, originalExists := originalCounts[ps.Name]; !originalExists || ps.Count != original {
+			updatedPodSets = append(updatedPodSets, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
+		}
+	}
+	if len(updatedPodSets) > 0 {
+		// Only update if the annotation value has actually changed to avoid
+		// an infinite reconciliation loop: each Update() changes the
+		// resourceVersion, which triggers another reconciliation.
+		if !podSetReplicaSizesMatchAnnotation(j.Annotations, updatedPodSets) {
+			podSetsJSON, err := json.Marshal(updatedPodSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+			}
+
+			// Patch the job with the annotation, use a copy of the job object to avoid it being overwritten by the content returned by the Server.
+			objCopy := j.Object().DeepCopyObject().(client.Object)
+			patch := client.MergeFrom(objCopy.DeepCopyObject().(client.Object))
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			if err := reconciler.client.Patch(ctx, objCopy, patch); err != nil {
+				return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+			}
```

  updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-09T18:44:55Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:249** by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T18:44:55Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
+
 	rayClusterName := j.Status.RayClusterName
 	podSets, err = raycluster.UpdatePodSets(ctx, podSets, reconciler.client, j.Object(), j.Spec.RayClusterSpec.EnableInTreeAutoscaling, rayClusterName)
 	if err != nil {
 		return nil, err
 	}
 
+	// If any PodSet counts changed, record the updated PodSets as an annotation
+	var updatedPodSets []podSetReplicaSize
+	for _, ps := range podSets {
+		if original, originalExists := originalCounts[ps.Name]; !originalExists || ps.Count != original {
+			updatedPodSets = append(updatedPodSets, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
+		}
+	}
+	if len(updatedPodSets) > 0 {
+		// Only update if the annotation value has actually changed to avoid
+		// an infinite reconciliation loop: each Update() changes the
+		// resourceVersion, which triggers another reconciliation.
+		if !podSetReplicaSizesMatchAnnotation(j.Annotations, updatedPodSets) {
+			podSetsJSON, err := json.Marshal(updatedPodSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+			}
+
+			// Patch the job with the annotation, use a copy of the job object to avoid it being overwritten by the content returned by the Server.
+			objCopy := j.Object().DeepCopyObject().(client.Object)
+			patch := client.MergeFrom(objCopy.DeepCopyObject().(client.Object))
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			if err := reconciler.client.Patch(ctx, objCopy, patch); err != nil {
+				return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+			}
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// podSetReplicaSizesMatchAnnotation checks whether the existing annotation
+// already records the same PodSet replica sizes as the given updatedPodSets.
+func podSetReplicaSizesMatchAnnotation(annotations map[string]string, updatedPodSets []podSetReplicaSize) bool {
+	if annotations == nil {
+		return false
+	}
+	existing, ok := annotations[PodsetReplicaSizesAnnotation]
+	if !ok {
+		return false
+	}
+	var existingPodSets []podSetReplicaSize
+	if err := json.Unmarshal([]byte(existing), &existingPodSets); err != nil {
+		return false
```

  good suggestion, updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-09T18:45:08Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:60** by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T18:45:08Z
```diff
@@ -52,6 +53,11 @@ const (
 	headGroupPodSetName    = "head"
 	submitterJobPodSetName = "submitter"
 	FrameworkName          = "ray.io/rayjob"
+
+	// PodsetReplicaSizesAnnotation is set on the RayJob when autoscaling causes
+	// PodSet replica sizes to differ from the original spec. The value is a JSON
+	// array compatible with []kueue.PodSet, containing only the changed PodSets.
+	PodsetReplicaSizesAnnotation = "kueue.x-k8s.io/podset-replica-sizes"
```

  Yes, added tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T18:57:09Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@mimowo](https://github.com/mimowo) — 2026-03-09T18:57:09Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  Hm, let's consider:
  1. RayJob size by user was 4, RayJobSpec=4, RayCluster=4, RayJobAnnotation (absent), 
  2. Autoscaler scaled up to 6, so RayJobSpec=4, RayCluster=6 -> RayJobAnnotation=6,
  3. Autoscaler scales down to 4, so  RayJobSpec=4, RayCluster=4  -> so no "update" so annotation is stale at 6
  
  I think we should instead compare the value of the annotation if changed or not.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-09T21:07:28Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@hiboyang](https://github.com/hiboyang) — 2026-03-09T21:07:28Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  Good example, you are right! We need to update annotation to handle this scenario, let me update the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T10:32:47Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@mimowo](https://github.com/mimowo) — 2026-03-10T10:32:47Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  The code still looks rather complex. I'm wondering that we could:
  Retrieve two []podSetReplicaSize - one from annotation (parsed), and another based on `podSets` - then compare. If different, then update the annotation. In this flow we don't need `originalCounts`

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T16:18:53Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T16:18:52Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  This means we always add the annotation, e.g. add the annotation initially before scale-up happening. I can do that. Let me update the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T16:30:40Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:184** by [@mimowo](https://github.com/mimowo) — 2026-03-10T16:30:40Z
```diff
@@ -172,15 +178,91 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	originalCounts := make(map[kueue.PodSetReference]int32, len(podSets))
+	for _, ps := range podSets {
+		originalCounts[ps.Name] = ps.Count
+	}
```

  Indeed, but this is ok, I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T16:58:52Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:198** by [@mimowo](https://github.com/mimowo) — 2026-03-10T16:58:52Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
```

  nit, Move it to a helper function to offload the main function, eg deserializePodSetCounts

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T16:59:48Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:203** by [@mimowo](https://github.com/mimowo) — 2026-03-10T16:59:48Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
```

  Why checking if `reconciler.client != nil`, this should never happen that the client is nil, afaik

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:01:29Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:248** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:01:29Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
+		allPodSets := make([]podSetReplicaSize, len(podSets))
+		for i, ps := range podSets {
+			allPodSets[i] = podSetReplicaSize{Name: ps.Name, Count: ps.Count}
+		}
+		podSetsJSON, err := json.Marshal(allPodSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			return true, nil
+		})
+		if err != nil {
+			return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// comparePodSetCounts returns the PodSets whose counts differ from referenceCounts.
+func comparePodSetCounts(podSets []kueue.PodSet, referenceCounts map[kueue.PodSetReference]int32) []podSetReplicaSize {
+	var changed []podSetReplicaSize
+	for _, ps := range podSets {
+		if refCount, ok := referenceCounts[ps.Name]; !ok || ps.Count != refCount {
+			changed = append(changed, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
```

  just return true, no need for the extra infomation - it is not used

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:02:05Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:211** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:02:05Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
+		allPodSets := make([]podSetReplicaSize, len(podSets))
+		for i, ps := range podSets {
+			allPodSets[i] = podSetReplicaSize{Name: ps.Name, Count: ps.Count}
+		}
+		podSetsJSON, err := json.Marshal(allPodSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
```

  Extract to helper, like serializePodSetCounts

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T17:12:30Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:211** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T17:12:30Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
+		allPodSets := make([]podSetReplicaSize, len(podSets))
+		for i, ps := range podSets {
+			allPodSets[i] = podSetReplicaSize{Name: ps.Name, Count: ps.Count}
+		}
+		podSetsJSON, err := json.Marshal(allPodSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
```

  updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T17:12:35Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:248** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T17:12:35Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
+		allPodSets := make([]podSetReplicaSize, len(podSets))
+		for i, ps := range podSets {
+			allPodSets[i] = podSetReplicaSize{Name: ps.Name, Count: ps.Count}
+		}
+		podSetsJSON, err := json.Marshal(allPodSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			return true, nil
+		})
+		if err != nil {
+			return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// comparePodSetCounts returns the PodSets whose counts differ from referenceCounts.
+func comparePodSetCounts(podSets []kueue.PodSet, referenceCounts map[kueue.PodSetReference]int32) []podSetReplicaSize {
+	var changed []podSetReplicaSize
+	for _, ps := range podSets {
+		if refCount, ok := referenceCounts[ps.Name]; !ok || ps.Count != refCount {
+			changed = append(changed, podSetReplicaSize{
+				Name:  ps.Name,
+				Count: ps.Count,
+			})
```

  updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T17:12:46Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:203** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T17:12:46Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	updatedPodSets := comparePodSetCounts(podSets, previousCounts)
+	if len(updatedPodSets) > 0 && reconciler.client != nil {
```

  removed nil check

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T17:13:04Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:198** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T17:13:04Z
```diff
@@ -178,9 +185,72 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	// Parse existing annotation counts; use empty counts if annotation is absent.
+	previousCounts := make(map[kueue.PodSetReference]int32)
+	if j.Annotations[PodsetReplicaSizesAnnotation] != "" {
+		var annotatedPodSets []podSetReplicaSize
+		if err := json.Unmarshal([]byte(j.Annotations[PodsetReplicaSizesAnnotation]), &annotatedPodSets); err != nil {
+			return nil, fmt.Errorf("failed to unmarshal existing %s annotation: %w", PodsetReplicaSizesAnnotation, err)
+		}
+		for _, ps := range annotatedPodSets {
+			previousCounts[ps.Name] = ps.Count
+		}
+	}
```

  updated to helper function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T18:26:29Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:205** by [@mimowo](https://github.com/mimowo) — 2026-03-10T18:26:29Z
```diff
@@ -178,9 +185,83 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	previousCounts, err := parsePodSetReplicaSizes(j.Annotations[PodsetReplicaSizesAnnotation])
+	if err != nil {
+		return nil, err
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	changed := comparePodSetCounts(podSets, previousCounts)
+	if changed {
+		podSetsJSON, err := serializePodSetCounts(podSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			return true, nil
+		})
+		if err != nil {
+			return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// comparePodSetCounts returns true if any PodSet count differs from referenceCounts.
+func comparePodSetCounts(podSets []kueue.PodSet, referenceCounts map[kueue.PodSetReference]int32) bool {
+	for _, ps := range podSets {
+		if refCount, ok := referenceCounts[ps.Name]; !ok || ps.Count != refCount {
+			return true
+		}
+	}
+	return false
+}
```

  I think there is a subtle bug that it may say not changed when recefrence PodSets have extra elements. To fix we could check also the len() is the same.
  
  However, I'm wondering - maybe the better approach is to use `[]podSetReplicaSize` for both. Then we could simply check `slices.Equal`. wdyt?

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-10T19:38:56Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:205** by [@hiboyang](https://github.com/hiboyang) — 2026-03-10T19:38:56Z
```diff
@@ -178,9 +185,83 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	previousCounts, err := parsePodSetReplicaSizes(j.Annotations[PodsetReplicaSizesAnnotation])
+	if err != nil {
+		return nil, err
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	changed := comparePodSetCounts(podSets, previousCounts)
+	if changed {
+		podSetsJSON, err := serializePodSetCounts(podSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
+			annotations := objCopy.GetAnnotations()
+			if annotations == nil {
+				annotations = make(map[string]string)
+			}
+			annotations[PodsetReplicaSizesAnnotation] = string(podSetsJSON)
+			objCopy.SetAnnotations(annotations)
+			return true, nil
+		})
+		if err != nil {
+			return nil, fmt.Errorf("failed to patch RayJob annotations: %w", err)
+		}
+	}
+
 	return podSets, nil
 }
 
+// podSetReplicaSize is a minimal representation of a PodSet for the
+// PodsetReplicaSizesAnnotation, containing only name and count.
+type podSetReplicaSize struct {
+	Name  kueue.PodSetReference `json:"name"`
+	Count int32                 `json:"count"`
+}
+
+// comparePodSetCounts returns true if any PodSet count differs from referenceCounts.
+func comparePodSetCounts(podSets []kueue.PodSet, referenceCounts map[kueue.PodSetReference]int32) bool {
+	for _, ps := range podSets {
+		if refCount, ok := referenceCounts[ps.Name]; !ok || ps.Count != refCount {
+			return true
+		}
+	}
+	return false
+}
```

  Let me add length check. If use `[]podSetReplicaSize`, then will need to consider element ordering, or sort the slice. Length check seems simpler change here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T17:30:13Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:583** by [@mimowo](https://github.com/mimowo) — 2026-03-11T17:30:14Z
```diff
@@ -574,6 +574,14 @@ func TestValidateCreate(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TopologyAwareScheduling, tc.topologyAwareScheduling)
 			features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, tc.elasticJobsViaWorkloadSlices)
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.job).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  Why do we do this? The unit tests for webhook should not depend on the reconciler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T17:30:35Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:686** by [@mimowo](https://github.com/mimowo) — 2026-03-11T17:30:35Z
```diff
@@ -669,6 +677,14 @@ func TestValidateUpdate(t *testing.T) {
 				manageJobsWithoutQueueName: tc.manageAll,
 			}
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.newJob).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  Same here, it looks weird that unit test for webhooks would depend on reconciler

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-11T17:47:49Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:583** by [@hiboyang](https://github.com/hiboyang) — 2026-03-11T17:47:49Z
```diff
@@ -574,6 +574,14 @@ func TestValidateCreate(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TopologyAwareScheduling, tc.topologyAwareScheduling)
 			features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, tc.elasticJobsViaWorkloadSlices)
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.job).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  It is because RayJob will need `reconciler.client` to update the annotation. The test code here needs to prepare that client object, otherwise, we will have `reconciler.client` nil error inside RayJob.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-11T17:48:23Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:686** by [@hiboyang](https://github.com/hiboyang) — 2026-03-11T17:48:23Z
```diff
@@ -669,6 +677,14 @@ func TestValidateUpdate(t *testing.T) {
 				manageJobsWithoutQueueName: tc.manageAll,
 			}
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.newJob).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  Similar like previous comment: It is because RayJob will need reconciler.client to update the annotation. The test code here needs to prepare that client object, otherwise, we will have reconciler.client nil error inside RayJob.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T17:55:56Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:583** by [@mimowo](https://github.com/mimowo) — 2026-03-11T17:55:56Z
```diff
@@ -574,6 +574,14 @@ func TestValidateCreate(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TopologyAwareScheduling, tc.topologyAwareScheduling)
 			features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, tc.elasticJobsViaWorkloadSlices)
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.job).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  Ahhh, I see, because the validation code calls PodSets, and this code sends update(), we need to change that. The update could be submitted only from the Reconciler. Webhooks cannot have side effects

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T17:57:11Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:206** by [@mimowo](https://github.com/mimowo) — 2026-03-11T17:57:12Z
```diff
@@ -178,9 +186,88 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	previousCounts, err := parsePodSetReplicaSizes(j.Annotations[PodsetReplicaSizesAnnotation])
+	if err != nil {
+		return nil, err
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	changed := comparePodSetCounts(podSets, previousCounts)
+	if changed {
+		podSetsJSON, err := serializePodSetCounts(podSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
```

  I somehow missed that, but we cannot call the Patch from within PodSets(), because PodSets() is called from webhooks for validation and defaulting. Those need to be super fast, so we cannot afford sending requests. Please move updating the annotation to the Reconcile code only.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-11T18:01:54Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:206** by [@hiboyang](https://github.com/hiboyang) — 2026-03-11T18:01:54Z
```diff
@@ -178,9 +186,88 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	previousCounts, err := parsePodSetReplicaSizes(j.Annotations[PodsetReplicaSizesAnnotation])
+	if err != nil {
+		return nil, err
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	changed := comparePodSetCounts(podSets, previousCounts)
+	if changed {
+		podSetsJSON, err := serializePodSetCounts(podSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
```

  Sounds good, this is good suggestion, let me update the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T18:08:28Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:686** by [@mimowo](https://github.com/mimowo) — 2026-03-11T18:08:28Z
```diff
@@ -669,6 +677,14 @@ func TestValidateUpdate(t *testing.T) {
 				manageJobsWithoutQueueName: tc.manageAll,
 			}
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.newJob).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  yes, feel free to mark this as resolved, it is the same underlying comment

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-12T05:07:02Z

- **pkg/controller/jobs/rayjob/rayjob_controller.go:206** by [@hiboyang](https://github.com/hiboyang) — 2026-03-12T05:07:01Z
```diff
@@ -178,9 +186,88 @@ func (j *RayJob) PodSets(ctx context.Context) ([]kueue.PodSet, error) {
 		return nil, err
 	}
 
+	previousCounts, err := parsePodSetReplicaSizes(j.Annotations[PodsetReplicaSizesAnnotation])
+	if err != nil {
+		return nil, err
+	}
+
+	// Compare current counts against previous annotation. If any differ, update the annotation
+	// with ALL current podSet counts (not just the changed ones) to avoid infinite patch loops.
+	changed := comparePodSetCounts(podSets, previousCounts)
+	if changed {
+		podSetsJSON, err := serializePodSetCounts(podSets)
+		if err != nil {
+			return nil, fmt.Errorf("failed to marshal updated podsets: %w", err)
+		}
+
+		// Patch the job with the annotation, use a copy of the job object to avoid
+		// it being overwritten by the content returned by the Server.
+		objCopy := j.Object().DeepCopyObject().(client.Object)
+		err = utilclient.Patch(ctx, reconciler.client, objCopy, func() (bool, error) {
```

  updated the code to move patch outside of PodSets()

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-12T05:39:21Z

- **pkg/controller/jobs/rayjob/rayjob_webhook_test.go:583** by [@hiboyang](https://github.com/hiboyang) — 2026-03-12T05:39:21Z
```diff
@@ -574,6 +574,14 @@ func TestValidateCreate(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TopologyAwareScheduling, tc.topologyAwareScheduling)
 			features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, tc.elasticJobsViaWorkloadSlices)
 			ctx, _ := utiltesting.ContextWithLog(t)
+
+			fakeClient := utiltesting.NewClientBuilder(rayv1.AddToScheme).
+				WithObjects(tc.job).
+				Build()
+			oldReconciler := reconciler
+			reconciler = rayJobReconciler{client: fakeClient}
+			t.Cleanup(func() { reconciler = oldReconciler })
```

  updated code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T17:25:13Z

- **pkg/controller/jobframework/reconciler.go:876** by [@mimowo](https://github.com/mimowo) — 2026-03-12T17:25:13Z
```diff
@@ -868,7 +873,7 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
-		podSets, err := JobPodSets(ctx, job)
+		podSets, err := GetJobPodSetsWithUpdateJob(ctx, job, r.client)
```

  I don't think this is good place to inject it. First this is only needed for RayJob, second PodSets() don't need to be 1:1 the same as JobSet necessarily anyway.
  
  Let me think. I would rather register a new controller for this functionality using NewAdditionalReconcilers. Otherwise, it would need to be opt-in by some dedicated interface like `UpdateAnnotations` maybe.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-13T05:27:24Z

- **pkg/controller/jobframework/reconciler.go:876** by [@hiboyang](https://github.com/hiboyang) — 2026-03-13T05:27:24Z
```diff
@@ -868,7 +873,7 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
-		podSets, err := JobPodSets(ctx, job)
+		podSets, err := GetJobPodSetsWithUpdateJob(ctx, job, r.client)
```

  Remove job patching code from here, added `JobWithCustomAnnotations` interface and RayJob implementation for that interface

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T06:49:11Z

- **pkg/controller/jobframework/reconciler.go:77** by [@mimowo](https://github.com/mimowo) — 2026-03-13T06:49:07Z
```diff
@@ -68,6 +70,11 @@ import (
 const (
 	FailedToStartFinishedReason = "FailedToStart"
 	managedOwnersChainLimit     = 10
+
+	// PodsetReplicaSizesAnnotation is set on the job when autoscaling causes
+	// PodSet replica sizes to differ from the original spec. The value is a JSON
+	// array compatible with []kueue.PodSet, containing only the changed PodSets.
+	PodsetReplicaSizesAnnotation = "kueue.x-k8s.io/podset-replica-sizes"
```

  Since this annotation is only needed for ElasticJobs for now. lets guard it by the feature gate. This way we dont need to commit to maintaining it lomg term. Please also extend the comment saying "This annotation is alpha-level enabled by the ElasticJobsViaWorkloadSlices", ir sonething along the lines

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:38:11Z

- **pkg/controller/jobframework/reconciler.go:893** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:38:11Z
```diff
@@ -868,11 +875,23 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
+		oldAnnotations := make(map[string]string, len(job.Object().GetAnnotations()))
+		maps.Copy(oldAnnotations, job.Object().GetAnnotations())
+
 		podSets, err := JobPodSets(ctx, job)
 		if err != nil {
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if ca, implements := job.(JobWithCustomAnnotations); implements {
+			if !apiequality.Semantic.DeepEqual(job.Object().GetAnnotations(), oldAnnotations) {
+				err = ca.UpdateAnnotations(ctx, r.client)
+				if err != nil {
+					return nil, fmt.Errorf("failed to update annotations on object %s: %w", object.GetName(), err)
+				}
+			}
+		}
```

  Since the interface name is generic I would expect the update to also be in the generic place: https://github.com/kubernetes-sigs/kueue/blob/2dc064137bf04449bfc25a14f5dec629dfcc21a5/pkg/controller/jobframework/reconciler.go#L419-L442
  
  Not under the `WorkloadSliceEnabled`. I think inside the helper we should check the feature gate enablement. 
  
  I would also prefer to follow the same pattern as for `CustomWorkloadConditions`, so make the function non-changing. Just informative. 
  
  So I expect something like:
  
  ```golang
  		if annotations := job.CustomAnnotations(); len(annotations) > 0 {
             // patch using clientutil.Patch()
  		}
  ```
  let me know what you think

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:38:41Z

- **pkg/controller/jobframework/reconciler.go:879** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:38:41Z
```diff
@@ -868,11 +875,23 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
+		oldAnnotations := make(map[string]string, len(job.Object().GetAnnotations()))
+		maps.Copy(oldAnnotations, job.Object().GetAnnotations())
```

  This takes copy of the annotation even though we may not need to update them, I would like to avoid that

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-13T16:16:35Z

- **pkg/controller/jobframework/reconciler.go:893** by [@hiboyang](https://github.com/hiboyang) — 2026-03-13T16:16:35Z
```diff
@@ -868,11 +875,23 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
+		oldAnnotations := make(map[string]string, len(job.Object().GetAnnotations()))
+		maps.Copy(oldAnnotations, job.Object().GetAnnotations())
+
 		podSets, err := JobPodSets(ctx, job)
 		if err != nil {
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if ca, implements := job.(JobWithCustomAnnotations); implements {
+			if !apiequality.Semantic.DeepEqual(job.Object().GetAnnotations(), oldAnnotations) {
+				err = ca.UpdateAnnotations(ctx, r.client)
+				if err != nil {
+					return nil, fmt.Errorf("failed to update annotations on object %s: %w", object.GetName(), err)
+				}
+			}
+		}
```

  It is doable, but will need to add some state job object (RayJob) to track the annotation change, guess such state will be used by `job.CustomAnnotations()` in your code example. Then there is chance that the job has this state (changed annotations), but the UpdateAnnotations fail and the state becomes stale. Another potential issue is the job is actually `rayv1.RayJob`, we need to figure out a proper way to add such state on `rayv1.RayJob`.

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-13T16:22:25Z

- **pkg/controller/jobframework/reconciler.go:879** by [@hiboyang](https://github.com/hiboyang) — 2026-03-13T16:22:25Z
```diff
@@ -868,11 +875,23 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
+		oldAnnotations := make(map[string]string, len(job.Object().GetAnnotations()))
+		maps.Copy(oldAnnotations, job.Object().GetAnnotations())
```

  Updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-13T16:22:43Z

- **pkg/controller/jobframework/reconciler.go:77** by [@hiboyang](https://github.com/hiboyang) — 2026-03-13T16:22:43Z
```diff
@@ -68,6 +70,11 @@ import (
 const (
 	FailedToStartFinishedReason = "FailedToStart"
 	managedOwnersChainLimit     = 10
+
+	// PodsetReplicaSizesAnnotation is set on the job when autoscaling causes
+	// PodSet replica sizes to differ from the original spec. The value is a JSON
+	// array compatible with []kueue.PodSet, containing only the changed PodSets.
+	PodsetReplicaSizesAnnotation = "kueue.x-k8s.io/podset-replica-sizes"
```

  Good suggestion, updated code

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-13T16:26:40Z

- **pkg/controller/jobframework/reconciler.go:893** by [@hiboyang](https://github.com/hiboyang) — 2026-03-13T16:26:39Z
```diff
@@ -868,11 +875,23 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 
 	// If workload slicing is enabled for this job, use the slice-based processing path.
 	if WorkloadSliceEnabled(job) {
+		oldAnnotations := make(map[string]string, len(job.Object().GetAnnotations()))
+		maps.Copy(oldAnnotations, job.Object().GetAnnotations())
+
 		podSets, err := JobPodSets(ctx, job)
 		if err != nil {
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if ca, implements := job.(JobWithCustomAnnotations); implements {
+			if !apiequality.Semantic.DeepEqual(job.Object().GetAnnotations(), oldAnnotations) {
+				err = ca.UpdateAnnotations(ctx, r.client)
+				if err != nil {
+					return nil, fmt.Errorf("failed to update annotations on object %s: %w", object.GetName(), err)
+				}
+			}
+		}
```

  Another alternative is that we keep a copy of job annotations in the beginning of `JobReconciler.ReconcileGenericJob` method, then later near the end of the method, check whether job annotations change, if change, call `UpdateAnnotations`, how do you like this option?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T10:10:01Z

- **pkg/controller/jobframework/reconciler.go:894** by [@mimowo](https://github.com/mimowo) — 2026-03-16T10:10:01Z
```diff
@@ -886,6 +880,26 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if jobWithCustomAnnotations, ok := job.(JobWithCustomAnnotations); ok {
+			customAnnotations, err := jobWithCustomAnnotations.GetCustomAnnotations(ctx, r.client, podSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to get custom annotations based on pod sets from job %s: %w", job.Object().GetName(), err)
+			}
+			if len(customAnnotations) > 0 {
+				if err := clientutil.Patch(ctx, r.client, object, func() (bool, error) {
+					annotations := job.Object().GetAnnotations()
+					if annotations == nil {
+						annotations = make(map[string]string)
+					}
+					maps.Copy(annotations, customAnnotations)
+					job.Object().SetAnnotations(annotations)
+					return true, nil
+				}); err != nil {
+					return nil, fmt.Errorf("failed to get update custom annotations on job %s: %w", job.Object().GetName(), err)
+				}
```

  I would suggest to extract this to a helper `mergeAnnotations` which returns if the update is needed, so something like:
  
  ```golang
  if newAnnotations, updated := mergeAnnotations(job, customAnnotations); updated {
    if err := clientutil.Patch(ctx, r.client, object, func() (bool, error) {
      job.Object().SetAnnotations(newAnnotations)
      return true, nil
     }); err != nil {
       return nil, fmt.Errorf("failed to get update custom annotations on job %s: %w", job.Object().GetName(), err)
    }
  } 
  ```
  I know you already added the check inside `GetCustomAnnotations`, but it will be easier to use for also other users of the mechanism, and safer that someone would forget, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T10:10:43Z

- **pkg/controller/jobframework/reconciler.go:883** by [@mimowo](https://github.com/mimowo) — 2026-03-16T10:10:43Z
```diff
@@ -886,6 +880,26 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if jobWithCustomAnnotations, ok := job.(JobWithCustomAnnotations); ok {
```

  Could you also add unit tests for reconciler.go in reconciler_test.go. I think this should be possible, based on the mocks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T10:11:27Z

LGTM overall, thank you 👍

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-16T15:02:02Z

- **pkg/controller/jobframework/reconciler.go:894** by [@hiboyang](https://github.com/hiboyang) — 2026-03-16T15:02:02Z
```diff
@@ -886,6 +880,26 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if jobWithCustomAnnotations, ok := job.(JobWithCustomAnnotations); ok {
+			customAnnotations, err := jobWithCustomAnnotations.GetCustomAnnotations(ctx, r.client, podSets)
+			if err != nil {
+				return nil, fmt.Errorf("failed to get custom annotations based on pod sets from job %s: %w", job.Object().GetName(), err)
+			}
+			if len(customAnnotations) > 0 {
+				if err := clientutil.Patch(ctx, r.client, object, func() (bool, error) {
+					annotations := job.Object().GetAnnotations()
+					if annotations == nil {
+						annotations = make(map[string]string)
+					}
+					maps.Copy(annotations, customAnnotations)
+					job.Object().SetAnnotations(annotations)
+					return true, nil
+				}); err != nil {
+					return nil, fmt.Errorf("failed to get update custom annotations on job %s: %w", job.Object().GetName(), err)
+				}
```

  sounds good, updated

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-16T15:02:55Z

- **pkg/controller/jobframework/reconciler.go:883** by [@hiboyang](https://github.com/hiboyang) — 2026-03-16T15:02:55Z
```diff
@@ -886,6 +880,26 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			return nil, fmt.Errorf("failed to retrieve pod sets from job: %w", err)
 		}
 
+		if jobWithCustomAnnotations, ok := job.(JobWithCustomAnnotations); ok {
```

  yes, added unit test

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T18:04:59Z

- **pkg/controller/jobframework/reconciler.go:1663** by [@mimowo](https://github.com/mimowo) — 2026-03-16T18:04:59Z
```diff
@@ -1648,3 +1656,17 @@ func WorkloadSliceEnabled(job GenericJob) bool {
 	}
 	return workloadslicing.Enabled(jobObject)
 }
+
+// mergeAnnotations merges customAnnotations into the job's existing annotations.
+// It returns a new merged annotation map and true if there are changes to apply.
+// The job's existing annotations are not modified.
+func mergeAnnotations(job GenericJob, customAnnotations map[string]string) (map[string]string, bool) {
```

  This looks mostly OK, but I have one concern here: mergeAnnotations() currently returns true whenever customAnnotations is non-empty, even if all of those annotations already match the existing values. That could lead to unnecessary update requests on reconcile loops. I think it would be better to return true only when there is an effective change to apply. Something like:
  
  ```golang
  func mergeAnnotations(job GenericJob, customAnnotations map[string]string) (map[string]string, bool) {
  	if len(customAnnotations) == 0 {
  		return nil, false
  	}
  
  	existing := job.Object().GetAnnotations()
  
  	var merged map[string]string
  	changed := false
  
  	for k, v := range customAnnotations {
  		if cur, ok := existing[k]; !ok || cur != v {
  			if !changed {
  				merged = maps.Clone(existing)
  				if merged == nil {
  					merged = make(map[string]string, len(customAnnotations))
  				}
  				changed = true
  			}
  			merged[k] = v
  		}
  	}
  	return merged, changed
  }
  ```
  please also make sure the core is covered 100% with the unit tests in reconciler_test.go

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-03-16T18:27:27Z

- **pkg/controller/jobframework/reconciler.go:1663** by [@hiboyang](https://github.com/hiboyang) — 2026-03-16T18:27:27Z
```diff
@@ -1648,3 +1656,17 @@ func WorkloadSliceEnabled(job GenericJob) bool {
 	}
 	return workloadslicing.Enabled(jobObject)
 }
+
+// mergeAnnotations merges customAnnotations into the job's existing annotations.
+// It returns a new merged annotation map and true if there are changes to apply.
+// The job's existing annotations are not modified.
+func mergeAnnotations(job GenericJob, customAnnotations map[string]string) (map[string]string, bool) {
```

  updated
