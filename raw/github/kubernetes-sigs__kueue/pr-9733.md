# PR #9733: Fix NodeHotSwap for multi-layer TAS topology constraints

**Summary**: Fix NodeHotSwap for multi-layer TAS topology constraints

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9733

**Last updated**: 2026-03-21T02:22:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@jzhaojieh](https://github.com/jzhaojieh)
- **Branch**: `fix-nodehotswap-multilayer-tas` → `main`
- **Created**: 2026-03-06T23:01:34Z
- **Updated**: 2026-03-21T02:22:37Z
- **Merged**: 2026-03-10T14:25:15Z
- **Closed**: 2026-03-10T14:25:15Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+210 / -33**

## Description



<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug 
/area tas

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:

This fixes two issues with NodeHotSwap replacement in multi-layer topology:

1. Domain Selection (requiredReplacementDomain): When multiple slice
   constraints are configured (e.g., [{rack: 8}, {switch: 4}, {hostname: 2}]),
   the code was using only the outermost constraint to find the replacement
   domain. This could place replacements in the wrong sub-domain.

   Fix: Iterate from innermost to outermost constraint and use the first
   (innermost) broken constraint's domain to confine the search.

2. Slice Size (findReplacementAssignment): After clearing
   PodsetSliceRequiredTopologyConstraints, setting PodSetSliceSize alone
   was a dead write because getSliceSizeWithSinglePodAsDefault() requires
   PodSetSliceRequiredTopology to be set. This caused sliceSize=1, scattering
   replacement pods across hosts and violating hostname grouping constraints.

   Fix: Find the innermost satisfied constraint and set both
   PodSetSliceRequiredTopology and PodSetSliceSize to preserve leaf-level
   grouping.

Added test cases:
- 3-layer topology: validates innermost broken domain selection
- 2-layer topology: validates sliceSize prevents scattered placement

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # 

Addresses the NodeHotSwap followup highlighted in #9506 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2026-03-06T23:01:41Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: jzhaojieh / name: Joshua Huang  (172ff018dc7ded7037047b14f6ab7852de2d0863)</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-06T23:01:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 172ff018dc7ded7037047b14f6ab7852de2d0863 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69af2ed886878200082a9ce3 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-06T23:01:45Z

Hi @jzhaojieh. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-07T05:33:33Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T10:28:36Z

/approve
I would like to leave lgtm to @PBundyra who worked closely on the NodeHotSwap

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T10:28:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9733#" title="Author self-approved">jzhaojieh</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9733#issuecomment-4030327072" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-03-10T13:58:23Z

/lgtm
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T13:58:31Z

LGTM label has been added.  <details>Git tree hash: 5c67ed28f0bd2cdee8060d206457307255a9ef64</details>

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-21T02:22:36Z

Part of #9046

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T16:56:23Z

- **pkg/cache/scheduler/tas_cache_test.go:6024** by [@mimowo](https://github.com/mimowo) — 2026-03-09T16:56:24Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
```

  What is the role of the Pods actually here? Maybe we could just drop them to simplify (same for the other test case). 
  
  IIRC the intention of the "pods" param is to account for non-TAS Pods. So I think we could cover the code equally well without them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T17:06:54Z

- **pkg/cache/scheduler/tas_cache_test.go:6050** by [@mimowo](https://github.com/mimowo) — 2026-03-09T17:06:54Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod-x1-a", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x1-b", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-a", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-b", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-a", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-b", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-a", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-b", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-a", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-b", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-a", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-b", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-a", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-b", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+			},
+			// 16 pods: 2 per host. r1: s1(x1+x2)=4 + s2(x3+x4)=4 = 8.
+			// r2: s3(x5+x6)=4 + s4(x7+x8)=4 = 8. All constraints satisfied.
+			existingTA: utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x1"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x2"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x3"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x4"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x5"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x6"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x7"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x8"}}).
+				Obj(),
+			admissionCount: 16,
+			unhealthyNode:  "x3",
+			topologyRequest: &kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+				PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+					{Topology: tasRackLabel, Size: 8},
+					{Topology: tasSwitchLabel, Size: 4},
+					{Topology: corev1.LabelHostname, Size: 2},
+				},
+			},
+			count: 16,
+			// With fix: domain = switch s2. Only x4 eligible. x4(4). ✓
+			wantAssignment: &tas.TopologyAssignment{
+				Levels: []string{corev1.LabelHostname},
+				Domains: []tas.TopologyDomainAssignment{
+					{Count: 2, Values: []string{"x1"}},
+					{Count: 2, Values: []string{"x2"}},
+					{Count: 4, Values: []string{"x4"}},
+					{Count: 2, Values: []string{"x5"}},
+					{Count: 2, Values: []string{"x6"}},
+					{Count: 2, Values: []string{"x7"}},
+					{Count: 2, Values: []string{"x8"}},
+				},
+			},
+		},
+		"2-layer: sliceSize=2 prevents scattered single-pod placement across hosts": {
+			// Topology: block → rack → hostname
+			//       b1
+			//   /        \
+			//  r1         r2
+			//  /  \    /  |  \
+			// x1  x2  x3 x4  x5
+			//          ^(NotReady)
```

  I think this is not something specific to the PR, but could you refresh my memory what happens when multiple Nodes are NotReady? 
  
  I know we don't support that, but maybe worth documenting the behavior with a test case. cc @PBundyra who likely has fresher memory here.

### Review by [@jzhaojieh](https://github.com/jzhaojieh) (commented) — 2026-03-09T17:48:34Z

- **pkg/cache/scheduler/tas_cache_test.go:6050** by [@jzhaojieh](https://github.com/jzhaojieh) — 2026-03-09T17:48:33Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod-x1-a", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x1-b", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-a", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-b", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-a", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-b", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-a", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-b", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-a", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-b", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-a", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-b", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-a", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-b", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+			},
+			// 16 pods: 2 per host. r1: s1(x1+x2)=4 + s2(x3+x4)=4 = 8.
+			// r2: s3(x5+x6)=4 + s4(x7+x8)=4 = 8. All constraints satisfied.
+			existingTA: utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x1"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x2"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x3"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x4"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x5"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x6"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x7"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x8"}}).
+				Obj(),
+			admissionCount: 16,
+			unhealthyNode:  "x3",
+			topologyRequest: &kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+				PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+					{Topology: tasRackLabel, Size: 8},
+					{Topology: tasSwitchLabel, Size: 4},
+					{Topology: corev1.LabelHostname, Size: 2},
+				},
+			},
+			count: 16,
+			// With fix: domain = switch s2. Only x4 eligible. x4(4). ✓
+			wantAssignment: &tas.TopologyAssignment{
+				Levels: []string{corev1.LabelHostname},
+				Domains: []tas.TopologyDomainAssignment{
+					{Count: 2, Values: []string{"x1"}},
+					{Count: 2, Values: []string{"x2"}},
+					{Count: 4, Values: []string{"x4"}},
+					{Count: 2, Values: []string{"x5"}},
+					{Count: 2, Values: []string{"x6"}},
+					{Count: 2, Values: []string{"x7"}},
+					{Count: 2, Values: []string{"x8"}},
+				},
+			},
+		},
+		"2-layer: sliceSize=2 prevents scattered single-pod placement across hosts": {
+			// Topology: block → rack → hostname
+			//       b1
+			//   /        \
+			//  r1         r2
+			//  /  \    /  |  \
+			// x1  x2  x3 x4  x5
+			//          ^(NotReady)
```

  It looks like the hot swap features only supports replacing 1 node and Tte node failure controller will evict the workload if it requires replacement for more than 1 node https://github.com/kubernetes-sigs/kueue/blob/f4e1c4c15560f8b8ba26dcd138325253b019628d/pkg/controller/tas/node_failure_controller.go#L299-L302
  
  I can update this with a test case for this as well

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-09T18:01:56Z

- **pkg/cache/scheduler/tas_cache_test.go:6050** by [@mimowo](https://github.com/mimowo) — 2026-03-09T18:01:56Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod-x1-a", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x1-b", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-a", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-b", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-a", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-b", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-a", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-b", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-a", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-b", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-a", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-b", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-a", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-b", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+			},
+			// 16 pods: 2 per host. r1: s1(x1+x2)=4 + s2(x3+x4)=4 = 8.
+			// r2: s3(x5+x6)=4 + s4(x7+x8)=4 = 8. All constraints satisfied.
+			existingTA: utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x1"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x2"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x3"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x4"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x5"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x6"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x7"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x8"}}).
+				Obj(),
+			admissionCount: 16,
+			unhealthyNode:  "x3",
+			topologyRequest: &kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+				PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+					{Topology: tasRackLabel, Size: 8},
+					{Topology: tasSwitchLabel, Size: 4},
+					{Topology: corev1.LabelHostname, Size: 2},
+				},
+			},
+			count: 16,
+			// With fix: domain = switch s2. Only x4 eligible. x4(4). ✓
+			wantAssignment: &tas.TopologyAssignment{
+				Levels: []string{corev1.LabelHostname},
+				Domains: []tas.TopologyDomainAssignment{
+					{Count: 2, Values: []string{"x1"}},
+					{Count: 2, Values: []string{"x2"}},
+					{Count: 4, Values: []string{"x4"}},
+					{Count: 2, Values: []string{"x5"}},
+					{Count: 2, Values: []string{"x6"}},
+					{Count: 2, Values: []string{"x7"}},
+					{Count: 2, Values: []string{"x8"}},
+				},
+			},
+		},
+		"2-layer: sliceSize=2 prevents scattered single-pod placement across hosts": {
+			// Topology: block → rack → hostname
+			//       b1
+			//   /        \
+			//  r1         r2
+			//  /  \    /  |  \
+			// x1  x2  x3 x4  x5
+			//          ^(NotReady)
```

  > It looks like the hot swap features only supports replacing 1 node and Tte node failure controller will evict the workload if it requires replacement for more than 1 node
  
  Yes, this matches my memory. Still, it would be good to cover that with a unit test. I don't recall if we have a unit test for the NHS for single-level slices, but we could add such a test case drive-by.

### Review by [@jzhaojieh](https://github.com/jzhaojieh) (commented) — 2026-03-09T20:28:21Z

- **pkg/cache/scheduler/tas_cache_test.go:6024** by [@jzhaojieh](https://github.com/jzhaojieh) — 2026-03-09T20:28:21Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
```

  The pods were just used to consume resources on the nodes and influence the placement, but it looks like this is adjustable via the allocatable values directly on the nodes. Updated these tests to remove pods and just rely on the allocatable values

### Review by [@jzhaojieh](https://github.com/jzhaojieh) (commented) — 2026-03-09T20:31:19Z

- **pkg/cache/scheduler/tas_cache_test.go:6050** by [@jzhaojieh](https://github.com/jzhaojieh) — 2026-03-09T20:31:19Z
```diff
@@ -5958,6 +5960,214 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			// x3 is NotReady, x4 is at capacity → no node available in r2 for replacement.
 			wantReason: `topology "default" doesn't allow to fit any of 1 pod(s). Total nodes: 3; excluded: resource "cpu": 1, topologyDomain: 2`,
 		},
+		"3-layer: innermost broken domain confines replacement to correct switch": {
+			// Topology: block → rack → switch → hostname
+			//                    b1
+			//              /            \
+			//            r1              r2
+			//          /    \          /    \
+			//        s1      s2      s3      s4
+			//       / \     / \     / \     / \
+			//      x1 x2  x3 x4  x5  x6  x7  x8
+			//              ^(NotReady)
+			//
+			// Constraints: [{rack: 8}, {switch: 4}, {hostname: 2}], Required: block
+			// Initial: 16 pods (2 per host). x3 dies → tr.Count = 2.
+			//   2 % 8 ≠ 0 → rack constraint broken (outermost)
+			//   2 % 4 ≠ 0 → switch constraint broken (innermost broken)
+			//   2 % 2 = 0 → hostname constraint satisfied
+			//
+			// BUG without fix: requiredReplacementDomain uses outermost constraint
+			// (rack, constraints[0]) → domain = rack r1 ("b1,r1").
+			// belongsToRequiredDomain admits all hosts in r1 (x1, x2, x4).
+			// x1 has tighter fit (allocatable=4, 2 free) than x4
+			// (allocatable=8, 6 free) → BestFit picks x1 (in switch s1!) over x4 (in s2).
+			// Result: x1(4) — replacement lands in WRONG switch. ✗
+			//
+			// With fix: uses innermost broken constraint (switch) → domain = switch s2
+			// ("b1,r1,s2"). Only x4 is eligible. Result: x4(4). ✓
+			levels: []string{tasBlockLabel, tasRackLabel, tasSwitchLabel, corev1.LabelHostname},
+			nodes: []corev1.Node{
+				*testingnode.MakeNode("b1-r1-s1-x1").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x1").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s1-x2").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s1").Label(corev1.LabelHostname, "x2").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x3").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x3").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					NotReady().Obj(),
+				*testingnode.MakeNode("b1-r1-s2-x4").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r1").Label(tasSwitchLabel, "s2").Label(corev1.LabelHostname, "x4").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("8"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x5").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x5").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s3-x6").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s3").Label(corev1.LabelHostname, "x6").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x7").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x7").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+				*testingnode.MakeNode("b1-r2-s4-x8").
+					Label(tasBlockLabel, "b1").Label(tasRackLabel, "r2").Label(tasSwitchLabel, "s4").Label(corev1.LabelHostname, "x8").
+					StatusAllocatable(corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("4"), corev1.ResourcePods: resource.MustParse("10")}).
+					Ready().Obj(),
+			},
+			pods: []corev1.Pod{
+				*testingpod.MakePod("pod-x1-a", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x1-b", "test-ns").NodeName("b1-r1-s1-x1").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-a", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x2-b", "test-ns").NodeName("b1-r1-s1-x2").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-a", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x4-b", "test-ns").NodeName("b1-r1-s2-x4").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-a", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x5-b", "test-ns").NodeName("b1-r2-s3-x5").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-a", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x6-b", "test-ns").NodeName("b1-r2-s3-x6").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-a", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x7-b", "test-ns").NodeName("b1-r2-s4-x7").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-a", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+				*testingpod.MakePod("pod-x8-b", "test-ns").NodeName("b1-r2-s4-x8").
+					Request(corev1.ResourceCPU, "1").Obj(),
+			},
+			// 16 pods: 2 per host. r1: s1(x1+x2)=4 + s2(x3+x4)=4 = 8.
+			// r2: s3(x5+x6)=4 + s4(x7+x8)=4 = 8. All constraints satisfied.
+			existingTA: utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x1"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x2"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x3"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x4"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x5"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x6"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x7"}}).
+				Domain(tas.TopologyDomainAssignment{Count: 2, Values: []string{"x8"}}).
+				Obj(),
+			admissionCount: 16,
+			unhealthyNode:  "x3",
+			topologyRequest: &kueue.PodSetTopologyRequest{
+				Required: ptr.To(tasBlockLabel),
+				PodsetSliceRequiredTopologyConstraints: []kueue.PodsetSliceRequiredTopologyConstraint{
+					{Topology: tasRackLabel, Size: 8},
+					{Topology: tasSwitchLabel, Size: 4},
+					{Topology: corev1.LabelHostname, Size: 2},
+				},
+			},
+			count: 16,
+			// With fix: domain = switch s2. Only x4 eligible. x4(4). ✓
+			wantAssignment: &tas.TopologyAssignment{
+				Levels: []string{corev1.LabelHostname},
+				Domains: []tas.TopologyDomainAssignment{
+					{Count: 2, Values: []string{"x1"}},
+					{Count: 2, Values: []string{"x2"}},
+					{Count: 4, Values: []string{"x4"}},
+					{Count: 2, Values: []string{"x5"}},
+					{Count: 2, Values: []string{"x6"}},
+					{Count: 2, Values: []string{"x7"}},
+					{Count: 2, Values: []string{"x8"}},
+				},
+			},
+		},
+		"2-layer: sliceSize=2 prevents scattered single-pod placement across hosts": {
+			// Topology: block → rack → hostname
+			//       b1
+			//   /        \
+			//  r1         r2
+			//  /  \    /  |  \
+			// x1  x2  x3 x4  x5
+			//          ^(NotReady)
```

  Taking another look it seems like there is a test already covering this at the controller layer which should cover this case, so the scheduler shouldnt be attempting to find replacement for more than one node. Let me know if theres any other gap that you think we should add additional unit tests for
  
  https://github.com/kubernetes-sigs/kueue/blob/0d4b20498109b6cc3360f366db31e64c2a9e28c8/pkg/controller/tas/node_failure_controller_test.go#L246-L247
