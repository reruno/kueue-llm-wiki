# PR #9742: Support CEL expressions in DRA ResourceClaimTemplates

**Summary**: Support CEL expressions in DRA ResourceClaimTemplates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9742

**Last updated**: 2026-04-20T17:57:25Z

---

## Metadata

- **State**: open
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `issue-7298-cel` → `main`
- **Created**: 2026-03-08T20:08:11Z
- **Updated**: 2026-04-20T17:57:25Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note`, `size/XXL`, `do-not-merge/hold`, `cncf-cla: yes`
- **Assignees**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@rphillips](https://github.com/rphillips), [@alaypatel07](https://github.com/alaypatel07), [@mimowo](https://github.com/mimowo), [@harche](https://github.com/harche), [@gabesaba](https://github.com/gabesaba), [@olekzabl](https://github.com/olekzabl), [@sohankunkerkar](https://github.com/sohankunkerkar), [@vladikkuzn](https://github.com/vladikkuzn)
- **Changed files**: 16   **+1607 / -15**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Removes the restriction that rejected CEL selectors in DRA `ResourceClaimTemplate` device requests. CEL selectors are filters that narrow which devices satisfy a request but don't change how many devices are requested. Kueue only needs the device count and class name for quota management, so CEL selectors can be safely accepted without evaluation. The Kubernetes scheduler handles actual device matching at scheduling time.

This enables use cases like:
- Partitionable devices (MIG GPUs) via [KEP-4815](https://github.com/kubernetes/enhancements/blob/master/keps/sig-scheduling/4815-dra-partitionable-devices/README.md)
- Network device filtering via [DRANET](https://dranet.dev/docs/user/nvidia-dranet/)
- Any DRA driver that publishes device attributes for CEL-based selection

#### Which issue(s) this PR fixes:

Fixes #7298

#### Special notes for your reviewer:

This PR was developed with the assistance of Claude Code (AI).

The upstream issue [kubernetes/kubernetes#137490](https://github.com/kubernetes/kubernetes/issues/137490) tracks improving error feedback when CEL selectors don't match any device at scheduling time — that is a separate scheduler-side concern.

#### Does this PR introduce a user-facing change?

```release-note
Support CEL expressions in DRA ResourceClaimTemplates. Kueue no longer rejects workloads that use CEL device selectors for filtering devices in resource claims.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-08T20:08:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4900da084dcb7c362a4bfcb60e6acd3a23bd32ed |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e65e6a7b02de0008279678 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk3NDItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.W_YEoiP6WVj9Ay39yE24DyiFwJfVLVXSN09miluuOaE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-08T20:08:21Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9742#" title="Author self-approved">kannon92</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mimowo](https://github.com/mimowo) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T07:38:49Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T07:38:57Z

LGTM label has been added.  <details>Git tree hash: 78f1e907166a95ca7b83bd59a220c6fcd4336b55</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T07:43:15Z

cc @sohankunkerkar @alaypatel07 ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T15:04:43Z

> We know Kueue shouldn't evaluate CEL.

Why is this a necessary assumption? I wouldn't make this as a "rule" if this steps in the way. I would assume there are some helpers which could help with that, and it is not rocket science . If this would solve the issue, then I think this is the right thing to do. At least I wouldn't do something just because of "code complexity" if the feature is broken otherwise. We may reject evaluation of CEL if there are other reasons, but I would like to undrestand those.

General ask: let's think which semantics is best to serve our users. Then we can think how to best achieve it.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T15:22:22Z

Yea this is sorta a problem with Kueue generally.

If a user requests a bogus gpu and sets ClusterQueue for it (or say they want a pod requiesting 256 gpus). I think Kueue will check against quota.

But it won't check the scheduler if it can actually be placed.

Kueue's TAS does incorporate scheduling capacity and becomes its own scheduler.
So this problem becomes critical to solve for DRA + TAS but we haven't yet designed or worked on that.

For kueue as a quota manager I guess I'm not sure if we need CEL expressions to be evaulated at quota time. We can certainly do it and its required for TAS. But I don't know of anyone working on Kueue + DRA + TAS and incorportating DRA scheduling into TAS.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T15:35:38Z

> But it won't check the scheduler if it can actually be placed.

Yeah, but would evaluating the CEL be equivalent to that? What are the resources Kueue would need to track to evaluate the CELs properly?

> For kueue as a quota manager I guess I'm not sure if we need CEL expressions to be evaulated at quota time. 

Yeah. The problem is that "as quota manager" Kueue works reasonably without TAS  - just need to account for node fragmentation and node failures. Still, there were too many admission mistakes to be usable for some of our users. However, DRA without CEL evaluation seems like many more admission mistakes. So, I would like to explore evaluation as a possibility.

> But I don't know of anyone working on Kueue + DRA + TAS and incorportating DRA scheduling into TAS.

We are planning to staff this effort, probably this year. The idea is to use the "Scheduling library" as presented on the last wg-batch by @44past4.

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-09T15:35:53Z

> > We know Kueue shouldn't evaluate CEL.
> 
> Why is this a necessary assumption? I wouldn't make this as a "rule" if this steps in the way. I would assume there are some helpers which could help with that, and it is not rocket science. If this would solve the issue, then I think this is the right thing to do.

I think you picked the first line :D I wasn't ruling out CEL evaluation as a hard constraint. The rest of that sentence listed ResourceSlice observation as one of the approaches, which is essentially what you're suggesting. Evaluating the user's CEL against ResourceSlice data would solve both the quota leak problem and the counter-based cost problem for partitionable devices. The TOCTOU gap is the same one Kueue already lives with for regular resources.

One thing to keep in mind from the extended resources [discussion](https://github.com/kubernetes-sigs/kueue/pull/8734#discussion_r2885721163), we'd want to avoid importing from `k8s.io/dynamic-resource-allocation` staging repo (per your earlier feedback). cel-go is already a transitive dependency in the vendor tree, so we can build a minimal device-matching environment on top of that directly if we go that route.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T16:33:19Z

At least from DRA folks, I was told that staging/dynamic-resource-allocation was the approach they recommend for autoscaling evaulation of CEL constraints.

I was thinking we'd have to go this route if we want the control-plane to evaulate the CEL expressions.

I did also notice that the UX for CEL is tricky from even core k8s perspective. I filled https://github.com/kubernetes/kubernetes/issues/137490 as if you have invalid CEL I didn't see any information actually bubbled up the user that the cel expression failed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T21:28:33Z

New changes are detected. LGTM label has been removed.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T21:40:59Z

Pushed up a potential solution for cel.

### Comment by [@harche](https://github.com/harche) — 2026-03-10T14:40:01Z

@kannon92 thanks, can you add e2e tests to cover the CEL support? 

xref : https://gist.github.com/harche/567b32cf471b9973b73ca04d52a2a330

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-11T00:15:57Z

I think this is ready for review.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-12T19:13:17Z

I would request KEP update for such new DRA capability support.
/hold

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-12T21:03:37Z

> I would request KEP update for such new DRA capability support. /hold

https://github.com/kubernetes-sigs/kueue/pull/9841

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-15T18:52:43Z

@mimowo @tenzen-y @alaypatel07 

PR changed a bit. Could you all review? 

@PannagaRao work on partionable devices is blocked by this due to the need to support CEL.

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-09T14:29:24Z

I spent some time looking at this from both sides i.e. the Kueue DRA pipeline and the upstream K8s allocator and I think there's more to this than removing the CEL rejection.
                                                                                                                                                                                                                                                                                                                                                                             
The problem is that `CEL expressions in DRA` covers two very different things. For simple filtering (DRANET, selecting by driver name), CEL just narrows which devices match and the cost is still "1 device" and removing the rejection works fine. But for partitionable devices (KEP-4815), which was the primary motivation for this issue, two workloads can request the same DeviceClass with count=1, but one picks a small MIG partition consuming ~5Gi of 80Gi GPU memory while the other picks a full partition consuming the entire 80Gi. The upstream scheduler handles this through SharedCounters/ConsumesCounters on ResourceSlices but if Kueue just counts devices, both get the same quota cost and the quota becomes meaningless.

We know Kueue shouldn't evaluate CEL. But we do need some way to determine the actual cost in terms of counter consumption rather than device count; whether through admin config that assigns cost per DeviceClass, user-declared consumption, or ResourceSlice observation.

~Removing the CEL rejection is a reasonable first step for simple filtering use cases~, but I don't think we should close [this](https://github.com/kubernetes-sigs/kueue/issues/7298) issue until we've addressed the counter-based quota problem for partitionable devices.

**Edit:** Beyond the partitionable devices problem, there's an even more immediate concern with simply removing the rejection. CEL evaluation happens in the scheduler's Filter phase against real ResourceSlice data well beyond Kueue's reach. By the time the scheduler runs, Kueue has already admitted and charged quota. If a user submits a workload with a CEL selector that no device actually matches (wrong attribute name, impossible filter, typo), Kueue charges quota, the scheduler can't place the pod, and that quota is permanently consumed. This blocks other legitimate workloads from getting through. @harche reproduced this on a Kind cluster with the DRA example driver. Without `WaitForPodsReady` configured (which is optional), there's no self-correction at all.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T15:40:11Z

- **pkg/dra/claims.go:296** by [@kannon92](https://github.com/kannon92) — 2026-03-10T15:40:11Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
```

  I think we may have to add rbac here for ResourceSliceList.

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2026-03-10T18:10:11Z

- **pkg/dra/claims.go:314** by [@rphillips](https://github.com/rphillips) — 2026-03-10T18:10:11Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
+		device dracel.Device
+	}
+	var clusterDevices []clusterDevice
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, clusterDevice{
+				driver: slice.Spec.Driver,
+				device: dracel.Device{
+					Driver:     slice.Spec.Driver,
+					Attributes: dev.Attributes,
+					Capacity:   dev.Capacity,
+				},
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				// Already validated by validateCELSelectors, skip.
```

  Capturing the error in AllErrs would be good here to catch invalid selectors.

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2026-03-10T18:12:01Z

- **pkg/dra/claims.go:234** by [@rphillips](https://github.com/rphillips) — 2026-03-10T18:12:01Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
```

  errors.Join() might be useful here so the function can return just `error`.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T22:42:55Z

- **pkg/dra/claims.go:314** by [@kannon92](https://github.com/kannon92) — 2026-03-10T22:42:55Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
+		device dracel.Device
+	}
+	var clusterDevices []clusterDevice
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, clusterDevice{
+				driver: slice.Spec.Driver,
+				device: dracel.Device{
+					Driver:     slice.Spec.Driver,
+					Attributes: dev.Attributes,
+					Capacity:   dev.Capacity,
+				},
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				// Already validated by validateCELSelectors, skip.
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T22:43:04Z

- **pkg/dra/claims.go:234** by [@kannon92](https://github.com/kannon92) — 2026-03-10T22:43:04Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
```

  done.

### Review by [@harche](https://github.com/harche) (commented) — 2026-03-11T19:42:19Z

Nice work addressing the quota leak issue with CEL selectors. The approach of fetching ResourceSlices and evaluating CEL via `DeviceMatches` is solid. Two minor comments below.

- **pkg/dra/claims.go:284** by [@harche](https://github.com/harche) — 2026-03-11T19:42:19Z
```diff
@@ -182,3 +208,147 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
```

  nit: Is the `driver` field used? I could only see it being set but not read anywhere. `device.Driver` seems to already carry the same value.

- **test/integration/singlecluster/controller/dra/dra_test.go:540** by [@harche](https://github.com/harche) — 2026-03-11T19:42:19Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  Minor: The old integration test covered the CEL rejection path through the controller. Now that it's replaced with the happy path, the rejection flow is only covered at unit and E2E level. Might be worth adding an integration test that creates a ResourceSlice with non-matching devices and verifies the inadmissible condition, but not a blocker since the existing coverage across unit + E2E is solid.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-11T21:06:17Z

- **pkg/dra/claims.go:284** by [@kannon92](https://github.com/kannon92) — 2026-03-11T21:06:17Z
```diff
@@ -182,3 +208,147 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-11T21:06:25Z

- **test/integration/singlecluster/controller/dra/dra_test.go:540** by [@kannon92](https://github.com/kannon92) — 2026-03-11T21:06:25Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  added.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-11T21:32:01Z

- **pkg/dra/claims.go:194** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:06:02Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  I liked this idea but per @mimowo's suggestion, we should evaluate CEL to avoid admitting workloads that can never be placed. My concern is the liveness of this check. This validation runs in the workload controller's Reconcile, before the scheduler ever sees the workload. When the scheduler marks a workload inadmissible (e.g., insufficient quota), `queueInadmissibleWorkloads` periodically re-evaluates it. But workloads rejected here sit with an error condition on the workload status indefinitely and nothing re-triggers reconciliation when ResourceSlices change.
  
  So if no devices match right now but a new GPU node joins or a DRA driver restarts, the workload stays stuck. Users would need to manually touch the workload to force re-evaluation.
  
    Two possible paths:
    1. A ResourceSlice informer that re-enqueues affected workloads when device inventory changes
    2. Move this check to the scheduler's admission cycle where the periodic re-evaluation already exists (similar to how TAS does topology checks during snapshot/nomination,
    not in the workload controller)

- **pkg/dra/claims.go:323** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:19:32Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
```

  The evaluation loop iterates all devices from all ResourceSlices regardless of DeviceClassName. If the request is for `nvidia.com/gpu` devices, the CEL still runs against NICs, FPGAs, and everything else in the cluster. I think the performance concern is real as evaluating CEL against a bunch of devices when only a few belong to the requested class wastes cycles.
                                                                                                                                                                            
  
  The request already carries `req.Exactly.DeviceClassName` which maps to a specific driver via the `DeviceClass` object. We could resolve that (one GET, reusable across requests) and pre-filter the device list by driver before running CEL. Cuts evaluation scope and makes the count accurate.

- **pkg/dra/claims.go:405** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:21:52Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  Errors from DeviceMatches are silently treated as `no match`. This makes sense for some errors (device doesn't have the attribute the CEL references) but not for others (context cancelled, CEL runtime type mismatch).
  
  If a CEL expression errors on every device, the user sees `CEL selectors match only 0 device(s)` which is misleading bc the real issue is evaluation failure. Could we log the first error at V(3), or surface it in the field error message?

- **pkg/dra/claims.go:296** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:24:38Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
```

  nit: the CEL expressions are compiled twice, once in `validateCELSelectors`  and again here for evaluation. Since the first check already passes before we reach this point, the compilation error handling at https://github.com/kubernetes-sigs/kueue/pull/9742/changes#diff-1817bbb9cab28ee6c2fb856446a0164731ee7fb63b3620c069d31c439750e2d1R306-R312 is dead code.

- **go.mod:37** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:28:01Z
```diff
@@ -34,6 +34,7 @@ require (
 	k8s.io/client-go v0.35.2
 	k8s.io/component-base v0.35.2
 	k8s.io/component-helpers v0.35.2
+	k8s.io/dynamic-resource-allocation v0.35.2
```

  Quick check: in PR #8734, @mimowo previously flagged concerns about importing from `k8s.io/dynamic-resource-allocation`. Just want to make sure we're aligned on taking this dependency.

- **test/integration/singlecluster/controller/dra/dra_test.go:540** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:29:06Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  +1

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-12T15:52:56Z

- **pkg/dra/claims.go:194** by [@kannon92](https://github.com/kannon92) — 2026-03-12T15:52:56Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  I'm not sure if this has to be solved here.
  
  I guess your point is valid with DeviceClasses also we should watch all deviceClasses and ResourceSlices for any changes. Both of these objects are usually added by an admin when setting up a cluster.
  
  2) Seems like we have to do this when we do DRA + TAS so I wonder if we can tackle this at that point.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T20:54:00Z

- **go.mod:37** by [@mimowo](https://github.com/mimowo) — 2026-03-12T20:54:00Z
```diff
@@ -34,6 +34,7 @@ require (
 	k8s.io/client-go v0.35.2
 	k8s.io/component-base v0.35.2
 	k8s.io/component-helpers v0.35.2
+	k8s.io/dynamic-resource-allocation v0.35.2
```

  I'm ok with the library, initially I thought this is part of the core k8s repo, but I see the library is extracted to dedicated repo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-12T21:09:18Z

- **pkg/dra/claims.go:323** by [@kannon92](https://github.com/kannon92) — 2026-03-12T21:09:18Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2026-03-16T05:03:35Z

- **pkg/dra/claims.go:420** by [@alaypatel07](https://github.com/alaypatel07) — 2026-03-16T05:03:35Z
```diff
@@ -182,3 +209,195 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	// Cache DeviceClass lookups so we resolve each class at most once.
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for _, cr := range celReqs {
+		// Resolve the DeviceClass and compile its selectors for pre-filtering.
+		var classSelectors []dracel.CompilationResult
+		if cr.deviceClassName != "" {
+			dc, ok := classCache[cr.deviceClassName]
+			if !ok {
+				dc = &resourcev1.DeviceClass{}
+				if err := cl.Get(ctx, client.ObjectKey{Name: cr.deviceClassName}, dc); err != nil {
+					allErrs = append(allErrs, field.InternalError(
+						basePath.Child("devices", "requests").Index(cr.index),
+						fmt.Errorf("failed to get DeviceClass %s: %w", cr.deviceClassName, err),
+					))
+					continue
+				}
+				classCache[cr.deviceClassName] = dc
+			}
+			for _, sel := range dc.Spec.Selectors {
+				if sel.CEL == nil {
+					continue
+				}
+				result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+				if result.Error != nil {
+					// DeviceClass has invalid CEL — no devices can match.
+					allErrs = append(allErrs, field.Invalid(
+						basePath.Child("devices", "requests").Index(cr.index),
+						cr.deviceClassName,
+						fmt.Sprintf("DeviceClass %s has invalid CEL selector: %s", cr.deviceClassName, result.Error.Detail),
+					))
+					continue
+				}
+				classSelectors = append(classSelectors, result)
+			}
+		}
+
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against cluster devices, pre-filtering by class selectors.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			// First check class selectors to skip devices that don't belong to this class.
+			classMatch := true
+			for _, comp := range classSelectors {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					classMatch = false
+					break
+				}
+			}
+			if !classMatch {
+				continue
+			}
+
+			// Then evaluate the request's own selectors.
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					allMatch = false
+					break
+				}
+			}
+			if allMatch {
+				matchCount++
+			}
+		}
+
+		if matchCount < cr.count {
```

  This if check can be inverted and put inside the loop, effectively, if the matchCount >= quota requested, exit the loop early, improving the performance from what is suggested here: https://github.com/kubernetes-sigs/kueue/pull/9742/changes#r2921055482

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T17:56:38Z

- **pkg/dra/claims.go:420** by [@kannon92](https://github.com/kannon92) — 2026-04-20T17:56:38Z
```diff
@@ -182,3 +209,195 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	// Cache DeviceClass lookups so we resolve each class at most once.
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for _, cr := range celReqs {
+		// Resolve the DeviceClass and compile its selectors for pre-filtering.
+		var classSelectors []dracel.CompilationResult
+		if cr.deviceClassName != "" {
+			dc, ok := classCache[cr.deviceClassName]
+			if !ok {
+				dc = &resourcev1.DeviceClass{}
+				if err := cl.Get(ctx, client.ObjectKey{Name: cr.deviceClassName}, dc); err != nil {
+					allErrs = append(allErrs, field.InternalError(
+						basePath.Child("devices", "requests").Index(cr.index),
+						fmt.Errorf("failed to get DeviceClass %s: %w", cr.deviceClassName, err),
+					))
+					continue
+				}
+				classCache[cr.deviceClassName] = dc
+			}
+			for _, sel := range dc.Spec.Selectors {
+				if sel.CEL == nil {
+					continue
+				}
+				result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+				if result.Error != nil {
+					// DeviceClass has invalid CEL — no devices can match.
+					allErrs = append(allErrs, field.Invalid(
+						basePath.Child("devices", "requests").Index(cr.index),
+						cr.deviceClassName,
+						fmt.Sprintf("DeviceClass %s has invalid CEL selector: %s", cr.deviceClassName, result.Error.Detail),
+					))
+					continue
+				}
+				classSelectors = append(classSelectors, result)
+			}
+		}
+
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against cluster devices, pre-filtering by class selectors.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			// First check class selectors to skip devices that don't belong to this class.
+			classMatch := true
+			for _, comp := range classSelectors {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					classMatch = false
+					break
+				}
+			}
+			if !classMatch {
+				continue
+			}
+
+			// Then evaluate the request's own selectors.
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					allMatch = false
+					break
+				}
+			}
+			if allMatch {
+				matchCount++
+			}
+		}
+
+		if matchCount < cr.count {
```

  I added something like that on line 415. Going to resolve.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T17:57:25Z

- **pkg/dra/claims.go:405** by [@kannon92](https://github.com/kannon92) — 2026-04-20T17:57:25Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  ```
  			allErrs = append(allErrs, field.Invalid(
  				basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
  				nil,
  				fmt.Sprintf("insufficient matching devices for CEL selector in DeviceClass %s: %d device(s) match in the cluster but %d requested",
  					cr.deviceClassName, matchCount, cr.count),
  			))
  ```
  
  I added this below which I think is sufficient.
