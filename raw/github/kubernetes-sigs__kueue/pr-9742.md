# PR #9742: Support CEL expressions in DRA ResourceClaimTemplates

**Summary**: Support CEL expressions in DRA ResourceClaimTemplates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9742

**Last updated**: 2026-05-12T13:51:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `issue-7298-cel` → `main`
- **Created**: 2026-03-08T20:08:11Z
- **Updated**: 2026-05-12T13:51:43Z
- **Merged**: 2026-05-12T07:35:33Z
- **Closed**: 2026-05-12T07:35:33Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@sohankunkerkar](https://github.com/sohankunkerkar), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@rphillips](https://github.com/rphillips), [@alaypatel07](https://github.com/alaypatel07), [@harche](https://github.com/harche), [@gabesaba](https://github.com/gabesaba), [@olekzabl](https://github.com/olekzabl), [@sohankunkerkar](https://github.com/sohankunkerkar), [@vladikkuzn](https://github.com/vladikkuzn), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 17   **+1718 / -28**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Removes the restriction that rejected CEL selectors in DRA `ResourceClaimTemplate` device requests. CEL selectors are filters that narrow which devices satisfy a request but don't change how many devices are requested. Kueue only needs the device count and class name for quota management, so CEL selectors can be safely accepted without evaluation. The Kubernetes scheduler handles actual device matching at scheduling time.

This enables use cases like:
- Partitionable devices (MIG GPUs) via [KEP-4815](https://github.com/kubernetes/enhancements/blob/master/keps/sig-scheduling/4815-dra-partitionable-devices/README.md)
- Network device filtering via [DRANET](https://dranet.dev/docs/user/nvidia-dranet/)
- Any DRA driver that publishes device attributes for CEL-based selection

#### Which issue(s) this PR fixes:

Fixes #7298

#### Special notes for your reviewer:

This PR was developed with the assistance of Claude Code (AI).

The upstream issue [kubernetes/kubernetes#137490](https://github.com/kubernetes/kubernetes/issues/137490) tracks improving error feedback when CEL selectors don't match any device at scheduling time — that is a separate scheduler-side concern.

#### Does this PR introduce a user-facing change?

```release-note
Support CEL expressions in DRA ResourceClaimTemplates. Kueue no longer rejects workloads that use CEL device selectors for filtering devices in resource claims.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-08T20:08:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9e6d62ce3ca02658703bafcb4f5fd42df7f955c3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0248d92fc589000806b2e6 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk3NDItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.W_YEoiP6WVj9Ay39yE24DyiFwJfVLVXSN09miluuOaE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9742--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-09T07:38:49Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-09T07:38:57Z

LGTM label has been added.  <details>Git tree hash: 78f1e907166a95ca7b83bd59a220c6fcd4336b55</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T07:43:15Z

cc @sohankunkerkar @alaypatel07 ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T15:04:43Z

> We know Kueue shouldn't evaluate CEL.

Why is this a necessary assumption? I wouldn't make this as a "rule" if this steps in the way. I would assume there are some helpers which could help with that, and it is not rocket science . If this would solve the issue, then I think this is the right thing to do. At least I wouldn't do something just because of "code complexity" if the feature is broken otherwise. We may reject evaluation of CEL if there are other reasons, but I would like to undrestand those.

General ask: let's think which semantics is best to serve our users. Then we can think how to best achieve it.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T15:22:22Z

Yea this is sorta a problem with Kueue generally.

If a user requests a bogus gpu and sets ClusterQueue for it (or say they want a pod requiesting 256 gpus). I think Kueue will check against quota.

But it won't check the scheduler if it can actually be placed.

Kueue's TAS does incorporate scheduling capacity and becomes its own scheduler.
So this problem becomes critical to solve for DRA + TAS but we haven't yet designed or worked on that.

For kueue as a quota manager I guess I'm not sure if we need CEL expressions to be evaulated at quota time. We can certainly do it and its required for TAS. But I don't know of anyone working on Kueue + DRA + TAS and incorportating DRA scheduling into TAS.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-09T15:35:38Z

> But it won't check the scheduler if it can actually be placed.

Yeah, but would evaluating the CEL be equivalent to that? What are the resources Kueue would need to track to evaluate the CELs properly?

> For kueue as a quota manager I guess I'm not sure if we need CEL expressions to be evaulated at quota time. 

Yeah. The problem is that "as quota manager" Kueue works reasonably without TAS  - just need to account for node fragmentation and node failures. Still, there were too many admission mistakes to be usable for some of our users. However, DRA without CEL evaluation seems like many more admission mistakes. So, I would like to explore evaluation as a possibility.

> But I don't know of anyone working on Kueue + DRA + TAS and incorportating DRA scheduling into TAS.

We are planning to staff this effort, probably this year. The idea is to use the "Scheduling library" as presented on the last wg-batch by @44past4.

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-09T15:35:53Z

> > We know Kueue shouldn't evaluate CEL.
> 
> Why is this a necessary assumption? I wouldn't make this as a "rule" if this steps in the way. I would assume there are some helpers which could help with that, and it is not rocket science. If this would solve the issue, then I think this is the right thing to do.

I think you picked the first line :D I wasn't ruling out CEL evaluation as a hard constraint. The rest of that sentence listed ResourceSlice observation as one of the approaches, which is essentially what you're suggesting. Evaluating the user's CEL against ResourceSlice data would solve both the quota leak problem and the counter-based cost problem for partitionable devices. The TOCTOU gap is the same one Kueue already lives with for regular resources.

One thing to keep in mind from the extended resources [discussion](https://github.com/kubernetes-sigs/kueue/pull/8734#discussion_r2885721163), we'd want to avoid importing from `k8s.io/dynamic-resource-allocation` staging repo (per your earlier feedback). cel-go is already a transitive dependency in the vendor tree, so we can build a minimal device-matching environment on top of that directly if we go that route.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T16:33:19Z

At least from DRA folks, I was told that staging/dynamic-resource-allocation was the approach they recommend for autoscaling evaulation of CEL constraints.

I was thinking we'd have to go this route if we want the control-plane to evaulate the CEL expressions.

I did also notice that the UX for CEL is tricky from even core k8s perspective. I filled https://github.com/kubernetes/kubernetes/issues/137490 as if you have invalid CEL I didn't see any information actually bubbled up the user that the cel expression failed.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-09T21:40:59Z

Pushed up a potential solution for cel.

### Comment by [@harche](https://github.com/harche) — 2026-03-10T14:40:01Z

@kannon92 thanks, can you add e2e tests to cover the CEL support? 

xref : https://gist.github.com/harche/567b32cf471b9973b73ca04d52a2a330

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-11T00:15:57Z

I think this is ready for review.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-12T19:13:17Z

I would request KEP update for such new DRA capability support.
/hold

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-12T21:03:37Z

> I would request KEP update for such new DRA capability support. /hold

https://github.com/kubernetes-sigs/kueue/pull/9841

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-15T18:52:43Z

@mimowo @tenzen-y @alaypatel07 

PR changed a bit. Could you all review? 

@PannagaRao work on partionable devices is blocked by this due to the need to support CEL.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-01T18:09:31Z

/hold cancel

KEP was merged.

/cc @tenzen-y @mimowo @sohankunkerkar @alaypatel07

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T09:16:49Z


Thank you @kannon92 for the update on the PR. I would love to have the first pass done by @sohankunkerkar or @alaypatel07, do you folks have cycles?

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-06T19:51:08Z

/assign @sohankunkerkar @mimowo

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-07T17:02:37Z

@sohankunkerkar @alaypatel07 
I believe I have addressed all your concerns.

Please take a look.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-11T13:22:08Z

@sohankunkerkar @mimowo 

anything outstanding here?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-12T07:02:53Z

LGTM label has been added.  <details>Git tree hash: 1eb4e281b3bb3eebbc8d253f31184e4c9258b107</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-12T07:02:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9742#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9742#pullrequestreview-4269887525" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-09T14:29:24Z

I spent some time looking at this from both sides i.e. the Kueue DRA pipeline and the upstream K8s allocator and I think there's more to this than removing the CEL rejection.
                                                                                                                                                                                                                                                                                                                                                                             
The problem is that `CEL expressions in DRA` covers two very different things. For simple filtering (DRANET, selecting by driver name), CEL just narrows which devices match and the cost is still "1 device" and removing the rejection works fine. But for partitionable devices (KEP-4815), which was the primary motivation for this issue, two workloads can request the same DeviceClass with count=1, but one picks a small MIG partition consuming ~5Gi of 80Gi GPU memory while the other picks a full partition consuming the entire 80Gi. The upstream scheduler handles this through SharedCounters/ConsumesCounters on ResourceSlices but if Kueue just counts devices, both get the same quota cost and the quota becomes meaningless.

We know Kueue shouldn't evaluate CEL. But we do need some way to determine the actual cost in terms of counter consumption rather than device count; whether through admin config that assigns cost per DeviceClass, user-declared consumption, or ResourceSlice observation.

~Removing the CEL rejection is a reasonable first step for simple filtering use cases~, but I don't think we should close [this](https://github.com/kubernetes-sigs/kueue/issues/7298) issue until we've addressed the counter-based quota problem for partitionable devices.

**Edit:** Beyond the partitionable devices problem, there's an even more immediate concern with simply removing the rejection. CEL evaluation happens in the scheduler's Filter phase against real ResourceSlice data well beyond Kueue's reach. By the time the scheduler runs, Kueue has already admitted and charged quota. If a user submits a workload with a CEL selector that no device actually matches (wrong attribute name, impossible filter, typo), Kueue charges quota, the scheduler can't place the pod, and that quota is permanently consumed. This blocks other legitimate workloads from getting through. @harche reproduced this on a Kind cluster with the DRA example driver. Without `WaitForPodsReady` configured (which is optional), there's no self-correction at all.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T15:40:11Z

- **pkg/dra/claims.go:410** by [@kannon92](https://github.com/kannon92) — 2026-03-10T15:40:11Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
```

  I think we may have to add rbac here for ResourceSliceList.

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2026-03-10T18:10:11Z

- **pkg/dra/claims.go:314** by [@rphillips](https://github.com/rphillips) — 2026-03-10T18:10:11Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
+		device dracel.Device
+	}
+	var clusterDevices []clusterDevice
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, clusterDevice{
+				driver: slice.Spec.Driver,
+				device: dracel.Device{
+					Driver:     slice.Spec.Driver,
+					Attributes: dev.Attributes,
+					Capacity:   dev.Capacity,
+				},
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				// Already validated by validateCELSelectors, skip.
```

  Capturing the error in AllErrs would be good here to catch invalid selectors.

### Review by [@rphillips](https://github.com/rphillips) (commented) — 2026-03-10T18:12:01Z

- **pkg/dra/claims.go:234** by [@rphillips](https://github.com/rphillips) — 2026-03-10T18:12:01Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
```

  errors.Join() might be useful here so the function can return just `error`.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T22:42:55Z

- **pkg/dra/claims.go:314** by [@kannon92](https://github.com/kannon92) — 2026-03-10T22:42:55Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
+		device dracel.Device
+	}
+	var clusterDevices []clusterDevice
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, clusterDevice{
+				driver: slice.Spec.Driver,
+				device: dracel.Device{
+					Driver:     slice.Spec.Driver,
+					Attributes: dev.Attributes,
+					Capacity:   dev.Capacity,
+				},
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				// Already validated by validateCELSelectors, skip.
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T22:43:04Z

- **pkg/dra/claims.go:234** by [@kannon92](https://github.com/kannon92) — 2026-03-10T22:43:04Z
```diff
@@ -182,3 +207,143 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) field.ErrorList {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return allErrs
```

  done.

### Review by [@harche](https://github.com/harche) (commented) — 2026-03-11T19:42:19Z

Nice work addressing the quota leak issue with CEL selectors. The approach of fetching ResourceSlices and evaluating CEL via `DeviceMatches` is solid. Two minor comments below.

- **pkg/dra/claims.go:284** by [@harche](https://github.com/harche) — 2026-03-11T19:42:19Z
```diff
@@ -182,3 +208,147 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
```

  nit: Is the `driver` field used? I could only see it being set but not read anywhere. `device.Driver` seems to already carry the same value.

- **test/integration/singlecluster/controller/dra/dra_test.go:545** by [@harche](https://github.com/harche) — 2026-03-11T19:42:19Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  Minor: The old integration test covered the CEL rejection path through the controller. Now that it's replaced with the happy path, the rejection flow is only covered at unit and E2E level. Might be worth adding an integration test that creates a ResourceSlice with non-matching devices and verifies the inadmissible condition, but not a blocker since the existing coverage across unit + E2E is solid.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-11T21:06:17Z

- **pkg/dra/claims.go:284** by [@kannon92](https://github.com/kannon92) — 2026-03-11T21:06:17Z
```diff
@@ -182,3 +208,147 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	type clusterDevice struct {
+		driver string
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-11T21:06:25Z

- **test/integration/singlecluster/controller/dra/dra_test.go:545** by [@kannon92](https://github.com/kannon92) — 2026-03-11T21:06:25Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  added.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-11T21:32:01Z

- **pkg/dra/claims.go:195** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:06:02Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  I liked this idea but per @mimowo's suggestion, we should evaluate CEL to avoid admitting workloads that can never be placed. My concern is the liveness of this check. This validation runs in the workload controller's Reconcile, before the scheduler ever sees the workload. When the scheduler marks a workload inadmissible (e.g., insufficient quota), `queueInadmissibleWorkloads` periodically re-evaluates it. But workloads rejected here sit with an error condition on the workload status indefinitely and nothing re-triggers reconciliation when ResourceSlices change.
  
  So if no devices match right now but a new GPU node joins or a DRA driver restarts, the workload stays stuck. Users would need to manually touch the workload to force re-evaluation.
  
    Two possible paths:
    1. A ResourceSlice informer that re-enqueues affected workloads when device inventory changes
    2. Move this check to the scheduler's admission cycle where the periodic re-evaluation already exists (similar to how TAS does topology checks during snapshot/nomination,
    not in the workload controller)

- **pkg/dra/claims.go:323** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:19:32Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
```

  The evaluation loop iterates all devices from all ResourceSlices regardless of DeviceClassName. If the request is for `nvidia.com/gpu` devices, the CEL still runs against NICs, FPGAs, and everything else in the cluster. I think the performance concern is real as evaluating CEL against a bunch of devices when only a few belong to the requested class wastes cycles.
                                                                                                                                                                            
  
  The request already carries `req.Exactly.DeviceClassName` which maps to a specific driver via the `DeviceClass` object. We could resolve that (one GET, reusable across requests) and pre-filter the device list by driver before running CEL. Cuts evaluation scope and makes the count accurate.

- **pkg/dra/claims.go:326** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:21:52Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  Errors from DeviceMatches are silently treated as `no match`. This makes sense for some errors (device doesn't have the attribute the CEL references) but not for others (context cancelled, CEL runtime type mismatch).
  
  If a CEL expression errors on every device, the user sees `CEL selectors match only 0 device(s)` which is misleading bc the real issue is evaluation failure. Could we log the first error at V(3), or surface it in the field error message?

- **pkg/dra/claims.go:296** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:24:38Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
```

  nit: the CEL expressions are compiled twice, once in `validateCELSelectors`  and again here for evaluation. Since the first check already passes before we reach this point, the compilation error handling at https://github.com/kubernetes-sigs/kueue/pull/9742/changes#diff-1817bbb9cab28ee6c2fb856446a0164731ee7fb63b3620c069d31c439750e2d1R306-R312 is dead code.

- **go.mod:37** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:28:01Z
```diff
@@ -34,6 +34,7 @@ require (
 	k8s.io/client-go v0.35.2
 	k8s.io/component-base v0.35.2
 	k8s.io/component-helpers v0.35.2
+	k8s.io/dynamic-resource-allocation v0.35.2
```

  Quick check: in PR #8734, @mimowo previously flagged concerns about importing from `k8s.io/dynamic-resource-allocation`. Just want to make sure we're aligned on taking this dependency.

- **test/integration/singlecluster/controller/dra/dra_test.go:545** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-11T21:29:06Z
```diff
@@ -526,7 +527,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
```

  +1

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-12T15:52:56Z

- **pkg/dra/claims.go:195** by [@kannon92](https://github.com/kannon92) — 2026-03-12T15:52:56Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  I'm not sure if this has to be solved here.
  
  I guess your point is valid with DeviceClasses also we should watch all deviceClasses and ResourceSlices for any changes. Both of these objects are usually added by an admin when setting up a cluster.
  
  2) Seems like we have to do this when we do DRA + TAS so I wonder if we can tackle this at that point.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T20:54:00Z

- **go.mod:37** by [@mimowo](https://github.com/mimowo) — 2026-03-12T20:54:00Z
```diff
@@ -34,6 +34,7 @@ require (
 	k8s.io/client-go v0.35.2
 	k8s.io/component-base v0.35.2
 	k8s.io/component-helpers v0.35.2
+	k8s.io/dynamic-resource-allocation v0.35.2
```

  I'm ok with the library, initially I thought this is part of the core k8s repo, but I see the library is extracted to dedicated repo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-12T21:09:18Z

- **pkg/dra/claims.go:323** by [@kannon92](https://github.com/kannon92) — 2026-03-12T21:09:18Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2026-03-16T05:03:35Z

- **pkg/dra/claims.go:454** by [@alaypatel07](https://github.com/alaypatel07) — 2026-03-16T05:03:35Z
```diff
@@ -182,3 +209,195 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	// Cache DeviceClass lookups so we resolve each class at most once.
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for _, cr := range celReqs {
+		// Resolve the DeviceClass and compile its selectors for pre-filtering.
+		var classSelectors []dracel.CompilationResult
+		if cr.deviceClassName != "" {
+			dc, ok := classCache[cr.deviceClassName]
+			if !ok {
+				dc = &resourcev1.DeviceClass{}
+				if err := cl.Get(ctx, client.ObjectKey{Name: cr.deviceClassName}, dc); err != nil {
+					allErrs = append(allErrs, field.InternalError(
+						basePath.Child("devices", "requests").Index(cr.index),
+						fmt.Errorf("failed to get DeviceClass %s: %w", cr.deviceClassName, err),
+					))
+					continue
+				}
+				classCache[cr.deviceClassName] = dc
+			}
+			for _, sel := range dc.Spec.Selectors {
+				if sel.CEL == nil {
+					continue
+				}
+				result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+				if result.Error != nil {
+					// DeviceClass has invalid CEL — no devices can match.
+					allErrs = append(allErrs, field.Invalid(
+						basePath.Child("devices", "requests").Index(cr.index),
+						cr.deviceClassName,
+						fmt.Sprintf("DeviceClass %s has invalid CEL selector: %s", cr.deviceClassName, result.Error.Detail),
+					))
+					continue
+				}
+				classSelectors = append(classSelectors, result)
+			}
+		}
+
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against cluster devices, pre-filtering by class selectors.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			// First check class selectors to skip devices that don't belong to this class.
+			classMatch := true
+			for _, comp := range classSelectors {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					classMatch = false
+					break
+				}
+			}
+			if !classMatch {
+				continue
+			}
+
+			// Then evaluate the request's own selectors.
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					allMatch = false
+					break
+				}
+			}
+			if allMatch {
+				matchCount++
+			}
+		}
+
+		if matchCount < cr.count {
```

  This if check can be inverted and put inside the loop, effectively, if the matchCount >= quota requested, exit the loop early, improving the performance from what is suggested here: https://github.com/kubernetes-sigs/kueue/pull/9742/changes#r2921055482

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T17:56:38Z

- **pkg/dra/claims.go:454** by [@kannon92](https://github.com/kannon92) — 2026-04-20T17:56:38Z
```diff
@@ -182,3 +209,195 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	// Cache DeviceClass lookups so we resolve each class at most once.
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for _, cr := range celReqs {
+		// Resolve the DeviceClass and compile its selectors for pre-filtering.
+		var classSelectors []dracel.CompilationResult
+		if cr.deviceClassName != "" {
+			dc, ok := classCache[cr.deviceClassName]
+			if !ok {
+				dc = &resourcev1.DeviceClass{}
+				if err := cl.Get(ctx, client.ObjectKey{Name: cr.deviceClassName}, dc); err != nil {
+					allErrs = append(allErrs, field.InternalError(
+						basePath.Child("devices", "requests").Index(cr.index),
+						fmt.Errorf("failed to get DeviceClass %s: %w", cr.deviceClassName, err),
+					))
+					continue
+				}
+				classCache[cr.deviceClassName] = dc
+			}
+			for _, sel := range dc.Spec.Selectors {
+				if sel.CEL == nil {
+					continue
+				}
+				result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+				if result.Error != nil {
+					// DeviceClass has invalid CEL — no devices can match.
+					allErrs = append(allErrs, field.Invalid(
+						basePath.Child("devices", "requests").Index(cr.index),
+						cr.deviceClassName,
+						fmt.Sprintf("DeviceClass %s has invalid CEL selector: %s", cr.deviceClassName, result.Error.Detail),
+					))
+					continue
+				}
+				classSelectors = append(classSelectors, result)
+			}
+		}
+
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against cluster devices, pre-filtering by class selectors.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			// First check class selectors to skip devices that don't belong to this class.
+			classMatch := true
+			for _, comp := range classSelectors {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					classMatch = false
+					break
+				}
+			}
+			if !classMatch {
+				continue
+			}
+
+			// Then evaluate the request's own selectors.
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
+				if err != nil || !matches {
+					allMatch = false
+					break
+				}
+			}
+			if allMatch {
+				matchCount++
+			}
+		}
+
+		if matchCount < cr.count {
```

  I added something like that on line 415. Going to resolve.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T17:57:25Z

- **pkg/dra/claims.go:326** by [@kannon92](https://github.com/kannon92) — 2026-04-20T17:57:25Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  ```
  			allErrs = append(allErrs, field.Invalid(
  				basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
  				nil,
  				fmt.Sprintf("insufficient matching devices for CEL selector in DeviceClass %s: %d device(s) match in the cluster but %d requested",
  					cr.deviceClassName, matchCount, cr.count),
  			))
  ```
  
  I added this below which I think is sufficient.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-05T04:16:14Z

- **pkg/dra/claims.go:195** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-05T03:49:28Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  The KEP says `queueInadmissibleWorkloads` re-evaluates these workloads, but they never enter the ClusterQueue as errors from here flow to https://github.com/kubernetes-sigs/kueue/pull/9742/changes#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR320, so re-evaluation is controller-runtime's error retry. Beta criteria list event-driven DeviceClass tracking and ResourceSlice indexing for performance, but not event-driven ResourceSlice tracking for re-evaluating stuck CEL workloads. Consider adding that as a Beta criterion and fixing the `queueInadmissibleWorkloads` reference.

- **pkg/dra/claims.go:326** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-05T03:53:54Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  The error message reports the count, but the concern was about distinguishing `evaluated false on all devices` from `evaluation failed on all devices.` Both produce `matchCount=0` but have different root causes. Could we log the first DeviceMatches error at V(3) so users can tell the difference in controller logs?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-05T14:24:26Z

- **pkg/dra/claims.go:195** by [@kannon92](https://github.com/kannon92) — 2026-05-05T14:24:26Z
```diff
@@ -162,6 +174,20 @@ func GetResourceRequestsForResourceClaimTemplates(
 				return nil, allErrs
 			}
 
+			// Validate CEL selectors against actual devices in the cluster.
+			celBasePath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j)
+			if celErrs := validateCELSelectorsAgainstDevices(ctx, cl, spec, celBasePath); len(celErrs) > 0 {
```

  https://github.com/kubernetes-sigs/kueue/pull/10961

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-06T22:44:06Z

- **pkg/dra/claims.go:326** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T22:44:06Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  @kannon92 any inputs here?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T13:16:58Z

- **pkg/dra/claims.go:326** by [@kannon92](https://github.com/kannon92) — 2026-05-07T13:16:57Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  sure. sorry I missed this. I'll get to this today.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T16:59:07Z

- **pkg/dra/claims.go:326** by [@kannon92](https://github.com/kannon92) — 2026-05-07T16:59:07Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		// Compile all CEL selectors for this request.
+		var compiled []dracel.CompilationResult
+		for _, sel := range cr.selectors {
+			if sel.CEL == nil {
+				continue
+			}
+			result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+			if result.Error != nil {
+				allErrs = append(allErrs, field.Invalid(
+					basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+					sel.CEL.Expression,
+					fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+				))
+				continue
+			}
+			compiled = append(compiled, result)
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		// Evaluate against all cluster devices.
+		var matchCount int64
+		for _, dev := range clusterDevices {
+			allMatch := true
+			for _, comp := range compiled {
+				matches, _, err := comp.DeviceMatches(ctx, dev)
```

  Addressed in 1897f743e — the first DeviceMatches evaluation error is now logged at V(3) so users can distinguish "no devices matched" from "CEL evaluation failed."

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T16:59:13Z

- **pkg/dra/claims.go:296** by [@kannon92](https://github.com/kannon92) — 2026-05-07T16:59:13Z
```diff
@@ -182,3 +208,140 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:     i,
+				count:     req.Exactly.Count,
+				selectors: req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	// Build CEL device representations from ResourceSlices.
+	var clusterDevices []dracel.Device
+	for i := range sliceList.Items {
+		slice := &sliceList.Items[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			clusterDevices = append(clusterDevices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
```

  Addressed in 1897f743e — removed the redundant compilation error handling since validateCELSelectors already catches these errors before validateCELSelectorsAgainstDevices is called.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T18:02:53Z

- **pkg/dra/claims.go:404** by [@mimowo](https://github.com/mimowo) — 2026-05-07T18:02:53Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  Please break this spaghetti into smaller functional pieces :) This seems to combine a couple of stages that would be better to reflect in helper functions, like getting the clusterDevices, then classSelectors, then compiling Cels, the actual validation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T18:04:16Z

- **pkg/dra/claims.go:269** by [@mimowo](https://github.com/mimowo) — 2026-05-07T18:04:16Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
```

  Please use built-in golang helpers for stuff like that, here slices.ContainsFunc seems to be enough

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T18:07:51Z

- **test/e2e/dra/dra_test.go:389** by [@mimowo](https://github.com/mimowo) — 2026-05-07T18:07:51Z
```diff
@@ -282,6 +282,117 @@ var _ = ginkgo.Describe("DRA", func() {
 			util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey3, util.LongTimeout)
 		})
 
+		ginkgo.It("Should admit and run a job with CEL selectors in resource claim template", func() {
+			ginkgo.By("Creating ResourceClaimTemplate with CEL selector filtering by driver name")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-gpu-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 1).
+				WithCELSelectors("device.driver == \"gpu.example.com\"").
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+
+			ginkgo.By("Creating Job with CEL-filtered ResourceClaimTemplate reference")
+			job := testingjob.MakeJob("cel-dra-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+				ResourceClaimTemplate("gpu", "cel-gpu-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, job)
+
+			wlLookupKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(job.Name, job.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying workload is created and has quota reservation with correct DRA resource usage")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+				g.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(createdWorkload.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := createdWorkload.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu")))
+				g.Expect(assignment.ResourceUsage["gpu"]).To(gomega.Equal(resource.MustParse("1")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying job is unsuspended")
+			util.ExpectJobUnsuspended(ctx, k8sClient, client.ObjectKeyFromObject(job))
+
+			ginkgo.By("Verifying job completes successfully")
+			util.ExpectJobToBeCompleted(ctx, k8sClient, job)
+
+			ginkgo.By("Verifying workload finished successfully")
+			util.ExpectWorkloadToFinish(ctx, k8sClient, wlLookupKey)
+		})
+
+		ginkgo.It("Should not admit a job with unsatisfiable CEL selectors to prevent quota leak", func() {
+			ginkgo.By("Creating ResourceClaimTemplate with CEL selector that matches no devices")
+			rctUnmatchable := utiltesting.MakeResourceClaimTemplate("unmatchable-cel-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver.example.com\"").
+				Obj()
+			util.MustCreate(ctx, k8sClient, rctUnmatchable)
+
+			ginkgo.By("Creating job with unmatchable CEL selector")
+			unmatchableJob := testingjob.MakeJob("unmatchable-cel-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				ResourceClaimTemplate("gpu", "unmatchable-cel-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, unmatchableJob)
+
+			unmatchableWlKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(unmatchableJob.Name, unmatchableJob.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying unmatchable workload is marked as inadmissible and does not consume quota")
+			unmatchableWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, unmatchableWlKey, unmatchableWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(unmatchableWl)).To(gomega.BeFalse())
+				g.Expect(unmatchableWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+					gomega.HaveField("Message", gomega.ContainSubstring("insufficient matching devices for CEL selector")),
+				)))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating a legitimate job that should still be admittable")
+			rctLegitimate := utiltesting.MakeResourceClaimTemplate("legitimate-gpu-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rctLegitimate)
+
+			legitimateJob := testingjob.MakeJob("legitimate-dra-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+				ResourceClaimTemplate("gpu", "legitimate-gpu-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, legitimateJob)
+
+			legitimateWlKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(legitimateJob.Name, legitimateJob.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying legitimate workload is admitted - quota was not leaked by unmatchable job")
+			legitimateWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, legitimateWlKey, legitimateWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(legitimateWl)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  We already have helpers for checking if a workload is admitted, eg ExpectWorkloadsToBeAdmitted

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T19:29:22Z

- **pkg/dra/claims.go:404** by [@kannon92](https://github.com/kannon92) — 2026-05-07T19:29:21Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  This is good feedback.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T20:34:34Z

- **pkg/dra/claims.go:404** by [@kannon92](https://github.com/kannon92) — 2026-05-07T20:34:34Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  Did some refactors. I think this satifies your ask.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T20:34:46Z

- **pkg/dra/claims.go:269** by [@kannon92](https://github.com/kannon92) — 2026-05-07T20:34:46Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-08T03:09:40Z

- **test/e2e/dra/dra_test.go:389** by [@kannon92](https://github.com/kannon92) — 2026-05-08T03:09:40Z
```diff
@@ -282,6 +282,117 @@ var _ = ginkgo.Describe("DRA", func() {
 			util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey3, util.LongTimeout)
 		})
 
+		ginkgo.It("Should admit and run a job with CEL selectors in resource claim template", func() {
+			ginkgo.By("Creating ResourceClaimTemplate with CEL selector filtering by driver name")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-gpu-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 1).
+				WithCELSelectors("device.driver == \"gpu.example.com\"").
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+
+			ginkgo.By("Creating Job with CEL-filtered ResourceClaimTemplate reference")
+			job := testingjob.MakeJob("cel-dra-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+				ResourceClaimTemplate("gpu", "cel-gpu-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, job)
+
+			wlLookupKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(job.Name, job.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying workload is created and has quota reservation with correct DRA resource usage")
+			createdWorkload := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(createdWorkload)).To(gomega.BeTrue())
+				g.Expect(createdWorkload.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(createdWorkload.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := createdWorkload.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu")))
+				g.Expect(assignment.ResourceUsage["gpu"]).To(gomega.Equal(resource.MustParse("1")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying job is unsuspended")
+			util.ExpectJobUnsuspended(ctx, k8sClient, client.ObjectKeyFromObject(job))
+
+			ginkgo.By("Verifying job completes successfully")
+			util.ExpectJobToBeCompleted(ctx, k8sClient, job)
+
+			ginkgo.By("Verifying workload finished successfully")
+			util.ExpectWorkloadToFinish(ctx, k8sClient, wlLookupKey)
+		})
+
+		ginkgo.It("Should not admit a job with unsatisfiable CEL selectors to prevent quota leak", func() {
+			ginkgo.By("Creating ResourceClaimTemplate with CEL selector that matches no devices")
+			rctUnmatchable := utiltesting.MakeResourceClaimTemplate("unmatchable-cel-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver.example.com\"").
+				Obj()
+			util.MustCreate(ctx, k8sClient, rctUnmatchable)
+
+			ginkgo.By("Creating job with unmatchable CEL selector")
+			unmatchableJob := testingjob.MakeJob("unmatchable-cel-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				ResourceClaimTemplate("gpu", "unmatchable-cel-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, unmatchableJob)
+
+			unmatchableWlKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(unmatchableJob.Name, unmatchableJob.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying unmatchable workload is marked as inadmissible and does not consume quota")
+			unmatchableWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, unmatchableWlKey, unmatchableWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(unmatchableWl)).To(gomega.BeFalse())
+				g.Expect(unmatchableWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+					gomega.HaveField("Message", gomega.ContainSubstring("insufficient matching devices for CEL selector")),
+				)))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating a legitimate job that should still be admittable")
+			rctLegitimate := utiltesting.MakeResourceClaimTemplate("legitimate-gpu-template", ns.Name).
+				DeviceRequest("gpu-request", draconsts.DriverName, 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rctLegitimate)
+
+			legitimateJob := testingjob.MakeJob("legitimate-dra-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+				ResourceClaimTemplate("gpu", "legitimate-gpu-template").
+				Obj()
+			util.MustCreate(ctx, k8sClient, legitimateJob)
+
+			legitimateWlKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(legitimateJob.Name, legitimateJob.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying legitimate workload is admitted - quota was not leaked by unmatchable job")
+			legitimateWl := &kueue.Workload{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, legitimateWlKey, legitimateWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(legitimateWl)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:39:24Z

- **pkg/dra/claims.go:251** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:39:24Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
```

  This code is duplicated with compileCELSelectors. I think we could just call `compileCELSelectors`, and return the errors, no?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:40:41Z

- **pkg/dra/claims.go:367** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:40:41Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
```

  I think the comment "// Return first error" is not needed - this is self explanatory. The useful comment would be to explain **why** we return the first item instead of aggregating the errors into one.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:42:11Z

- **pkg/dra/claims.go:375** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:42:11Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
```

  Why don't just return error here early?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:42:17Z

- **pkg/dra/claims.go:397** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:42:17Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
```

  Why don't just return error here early?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:43:25Z

- **pkg/dra/claims.go:461** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:43:25Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, firstEvalErr
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	clusterDevices := buildDeviceListFromSlices(sliceList.Items)
+	classCache := make(map[string]*resourcev1.DeviceClass)
+	consumed := make(map[int]bool)
+
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		classSelectors, err := resolveAndCompileDeviceClass(ctx, cl, cr.deviceClassName, classCache, basePath, cr.index)
+		if err != nil {
+			allErrs = append(allErrs, err)
+			continue
+		}
+
+		compiled, compErrs := compileCELSelectors(
+			cr.selectors,
+			basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+			"CEL compilation failed",
+		)
+		if len(compErrs) > 0 {
+			allErrs = append(allErrs, compErrs...)
+			continue
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		matchCount, firstEvalErr := countMatchingDevices(ctx, clusterDevices, classSelectors, compiled, consumed, cr.count)
+
+		if matchCount < cr.count {
+			if firstEvalErr != nil {
+				ctrl.LoggerFrom(ctx).V(3).Info("CEL evaluation error encountered while matching devices",
+					"deviceClassName", cr.deviceClassName, "error", firstEvalErr)
+			}
+			allErrs = append(allErrs, field.Invalid(
+				basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+				nil,
+				fmt.Sprintf("insufficient matching devices for CEL selector in DeviceClass %s: %d device(s) match in the cluster but %d requested",
+					cr.deviceClassName, matchCount, cr.count),
+			))
+		}
```

  Shouldn't we first fail if error, and only if not error, then inspect the results?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:45:23Z

- **test/integration/singlecluster/controller/dra/dra_test.go:636** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:45:23Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector that matches no devices")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-reject-template", ns.Name).
+				DeviceRequest("device-request", "foo.example.com", 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver\"").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with unsatisfiable CEL selectors")
+			wl := utiltestingapi.MakeWorkload("test-wl-cel-reject", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
```

  nit, use helpers like PodSets when constructing the workload, instead of mutating in place

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:47:13Z

- **test/integration/singlecluster/controller/dra/dra_test.go:560** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:47:13Z
```diff
@@ -528,7 +537,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices matching the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "test-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+						{Name: "dev-1"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
```

  use the helpers that we have: util.MustCreate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:47:19Z

- **test/integration/singlecluster/controller/dra/dra_test.go:620** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:47:19Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
```

  use the helpers that we have: util.MustCreate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:47:25Z

- **test/integration/singlecluster/controller/dra/dra_test.go:645** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:47:25Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector that matches no devices")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-reject-template", ns.Name).
+				DeviceRequest("device-request", "foo.example.com", 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver\"").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with unsatisfiable CEL selectors")
+			wl := utiltestingapi.MakeWorkload("test-wl-cel-reject", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{
+					Name:                      "device-template",
+					ResourceClaimTemplateName: new("cel-reject-template"),
+				},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "device-template"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
```

  use the helpers that we have: util.MustCreate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:49:28Z

- **pkg/dra/claims.go:384** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:49:28Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  What if the device is used by not fully? Couldn't a large device be able to accomodate more than one Pod?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:50:49Z

- **pkg/dra/claims.go:404** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:50:49Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, firstEvalErr
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  What is the test coverage of the new code? What are the lines which are not covered?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T13:51:41Z

- **test/integration/singlecluster/controller/dra/dra_test.go:562** by [@mimowo](https://github.com/mimowo) — 2026-05-11T13:51:42Z
```diff
@@ -528,7 +537,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices matching the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "test-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+						{Name: "dev-1"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
```

  Instead of defer, can we delete the slices in AfterEach / AfterAll as we normally do for other resources?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:14:36Z

- **pkg/dra/claims.go:367** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:14:36Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:14:59Z

- **pkg/dra/claims.go:375** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:14:59Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:15:07Z

- **pkg/dra/claims.go:397** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:15:07Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:15:51Z

- **test/integration/singlecluster/controller/dra/dra_test.go:636** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:15:51Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector that matches no devices")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-reject-template", ns.Name).
+				DeviceRequest("device-request", "foo.example.com", 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver\"").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with unsatisfiable CEL selectors")
+			wl := utiltestingapi.MakeWorkload("test-wl-cel-reject", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:15:57Z

- **test/integration/singlecluster/controller/dra/dra_test.go:560** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:15:57Z
```diff
@@ -528,7 +537,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices matching the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "test-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+						{Name: "dev-1"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:16:05Z

- **test/integration/singlecluster/controller/dra/dra_test.go:620** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:16:04Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:16:12Z

- **test/integration/singlecluster/controller/dra/dra_test.go:645** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:16:12Z
```diff
@@ -551,7 +584,67 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}
 			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
 
-			ginkgo.By("Verifying workload is marked as inadmissible")
+			ginkgo.By("Verifying workload is admitted with correct resource usage")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("foo")))
+				g.Expect(assignment.ResourceUsage["foo"]).To(gomega.Equal(resource.MustParse("2")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject workload with unsatisfiable CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices that won't match the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-reject-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "real-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector that matches no devices")
+			rct := utiltesting.MakeResourceClaimTemplate("cel-reject-template", ns.Name).
+				DeviceRequest("device-request", "foo.example.com", 2).
+				WithCELSelectors("device.driver == \"nonexistent-driver\"").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload with unsatisfiable CEL selectors")
+			wl := utiltestingapi.MakeWorkload("test-wl-cel-reject", ns.Name).
+				Queue("test-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{
+					Name:                      "device-template",
+					ResourceClaimTemplateName: new("cel-reject-template"),
+				},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "device-template"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:17:54Z

- **pkg/dra/claims.go:404** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:17:54Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, firstEvalErr
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  Did some updates. The only areas that are not covered are a few of the continue loops. In this function, the failure to list ResourceSlices, cel compilation failure, and the logging of the cel failure. The coverage of the dra folder is 84%.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:18:01Z

- **test/integration/singlecluster/controller/dra/dra_test.go:562** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:18:01Z
```diff
@@ -528,7 +537,31 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 		})
 
-		ginkgo.It("Should reject workload with CEL selectors", func() {
+		ginkgo.It("Should admit workload with CEL selectors", func() {
+			ginkgo.By("Creating a ResourceSlice with devices matching the CEL selector")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "cel-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "test-driver",
+					Pool: resourcev1.ResourcePool{
+						Name:               "test-pool",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{Name: "dev-0"},
+						{Name: "dev-1"},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
```

  updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:19:33Z

- **pkg/dra/claims.go:404** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:19:32Z
```diff
@@ -192,3 +224,213 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
```

  yes, looks great, left another round of comments, but this one is addressed

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:20:14Z

- **pkg/dra/claims.go:384** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:20:14Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  I'm not exactly following you here. MIG/Partiionable devices would be supported via https://github.com/kubernetes-sigs/kueue/pull/10283.
  
  Maybe the variable name is unfortunate because this is not consumable capacity.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:22:50Z

- **pkg/dra/claims.go:384** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:22:50Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  This CEL work is mainly about selecting the right device and I think more than 1 pod could bind to that device. 
  
  I'm not actually sure how DRA limits the number of users that can request a certain device. @johnbelamaric any idea here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:32:06Z

- **pkg/dra/claims.go:384** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:32:05Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  > I'm not exactly following you here. MIG/Partiionable devices would be supported via https://github.com/kubernetes-sigs/kueue/pull/10283.
  
  I'm not an expert on DRA so I might be messing some concepts here, sorry if this is the case here.
  
  My attention was drawn by the boolean map "consumed" which seems to assume the consumption of devices is binary, whereas IIUC some devices can be shared across multiple Pods or Workloads. IIUC this may happen even without Partiionable devices - if a single node is represented as a device it may still host multiple workloads.
  
  If this is possible, then it would be great to add a unit or integration test to confirm sharing devices works well. 
  
  Let me also cc @sohankunkerkar to review this bit who certainly understands it better.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T16:43:02Z

- **pkg/dra/claims.go:384** by [@kannon92](https://github.com/kannon92) — 2026-05-11T16:43:02Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  As of now I think each workload must use a ResourceClaimTemplate (so each pod would have a separate request). 
  
  Resource Sharing can be done via a ResourceClaim but kueue does not support this at the moment. This would be how multiple pods could share the same request.
  
  Right now, each RCT would be quoted separately. CEL basically evaluates the scheduling decision but I think the quota management would work fine. That is essentially what all of our integration tests are confirming.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-11T17:07:27Z

- **pkg/dra/claims.go:384** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-11T17:07:27Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  Adding to what @kannon92, the consumed map prevents two requests in the same claim from matching the same device entry. Devices in ResourceSlices are GPUs/accelerators, not nodes. For shared/partitionable device semantics, that's covered by #10283, which uses counter-based quota instead of binary device consumption

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T20:01:10Z

- **pkg/dra/claims.go:461** by [@kannon92](https://github.com/kannon92) — 2026-05-11T20:01:10Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, firstEvalErr
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	clusterDevices := buildDeviceListFromSlices(sliceList.Items)
+	classCache := make(map[string]*resourcev1.DeviceClass)
+	consumed := make(map[int]bool)
+
+	var allErrs field.ErrorList
+
+	for _, cr := range celReqs {
+		classSelectors, err := resolveAndCompileDeviceClass(ctx, cl, cr.deviceClassName, classCache, basePath, cr.index)
+		if err != nil {
+			allErrs = append(allErrs, err)
+			continue
+		}
+
+		compiled, compErrs := compileCELSelectors(
+			cr.selectors,
+			basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+			"CEL compilation failed",
+		)
+		if len(compErrs) > 0 {
+			allErrs = append(allErrs, compErrs...)
+			continue
+		}
+		if len(compiled) == 0 {
+			continue
+		}
+
+		matchCount, firstEvalErr := countMatchingDevices(ctx, clusterDevices, classSelectors, compiled, consumed, cr.count)
+
+		if matchCount < cr.count {
+			if firstEvalErr != nil {
+				ctrl.LoggerFrom(ctx).V(3).Info("CEL evaluation error encountered while matching devices",
+					"deviceClassName", cr.deviceClassName, "error", firstEvalErr)
+			}
+			allErrs = append(allErrs, field.Invalid(
+				basePath.Child("devices", "requests").Index(cr.index).Child("exactly", "selectors"),
+				nil,
+				fmt.Sprintf("insufficient matching devices for CEL selector in DeviceClass %s: %d device(s) match in the cluster but %d requested",
+					cr.deviceClassName, matchCount, cr.count),
+			))
+		}
```

  good call. fixed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-11T20:01:27Z

- **pkg/dra/claims.go:251** by [@kannon92](https://github.com/kannon92) — 2026-05-11T20:01:27Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
```

  fixed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T06:55:11Z

- **pkg/dra/claims.go:86** by [@mimowo](https://github.com/mimowo) — 2026-05-12T06:55:11Z
```diff
@@ -66,10 +82,12 @@ func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Re
 			return nil, allErrs
 		}
 
-		switch {
-		case len(req.Exactly.Selectors) > 0:
-			allErrs = append(allErrs, field.Invalid(field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors"), nil, "CEL selectors are not supported"))
+		if err := validateCELSelectors(req.Exactly.Selectors, field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors")); err != nil {
+			allErrs = append(allErrs, field.Invalid(field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors"), nil, err.Error()))
```

  Precompute the path `field.Invalid(field.NewPath("devices", "requests")` as var `devicesRequestsPath`
  
  Then compute `devicesRequestsPath.Index(i).Child("exactly", "selectors")`.` before calling validateCELSelectors avoid computing twice.
  
  This comment is not about performance, but readability by avoiding code duplication

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:01:13Z

- **pkg/dra/claims.go:417** by [@mimowo](https://github.com/mimowo) — 2026-05-12T07:01:13Z
```diff
@@ -192,3 +224,242 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+	_, compErrs := compileCELSelectors(selectors, fldPath, "CEL compilation failed")
+	return compErrs.ToAggregate()
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		// A single class selector error is enough to reject the request.
+		return nil, errs[0]
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil {
+			return matchCount, err
+		}
+		if !classMatch {
+			continue
+		}
+
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			return matchCount, err
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, nil
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	clusterDevices := buildDeviceListFromSlices(sliceList.Items)
+	classCache := make(map[string]*resourcev1.DeviceClass)
+	consumed := make(map[int]bool)
```

  nit, it I think using Set[int] would better capture the intention, as here the map[]->bool is basically as set.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:02:46Z

/lgtm
/approve
Thank you 👍 The two remaining comments from me are non-blocking and can be addressed post merge.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:04:58Z

- **pkg/dra/claims.go:384** by [@mimowo](https://github.com/mimowo) — 2026-05-12T07:04:58Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  Thanks for explaining. So iiuc currently we only support `ResourceClaimTemplate` and so we can assume the requests are separate.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T13:51:10Z

- **pkg/dra/claims.go:86** by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:51:10Z
```diff
@@ -66,10 +82,12 @@ func countDevicesPerClass(claimSpec *resourcev1.ResourceClaimSpec) (resources.Re
 			return nil, allErrs
 		}
 
-		switch {
-		case len(req.Exactly.Selectors) > 0:
-			allErrs = append(allErrs, field.Invalid(field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors"), nil, "CEL selectors are not supported"))
+		if err := validateCELSelectors(req.Exactly.Selectors, field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors")); err != nil {
+			allErrs = append(allErrs, field.Invalid(field.NewPath("devices", "requests").Index(i).Child("exactly", "selectors"), nil, err.Error()))
```

  https://github.com/kubernetes-sigs/kueue/pull/11142

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T13:51:14Z

- **pkg/dra/claims.go:417** by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:51:14Z
```diff
@@ -192,3 +224,242 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+	_, compErrs := compileCELSelectors(selectors, fldPath, "CEL compilation failed")
+	return compErrs.ToAggregate()
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		// A single class selector error is enough to reject the request.
+		return nil, errs[0]
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil {
+			return matchCount, err
+		}
+		if !classMatch {
+			continue
+		}
+
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			return matchCount, err
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
+			matchCount++
+			if matchCount >= needed {
+				break
+			}
+		}
+	}
+
+	return matchCount, nil
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
+// evaluates the CEL selectors from each request against the actual devices.
+// For each request, it resolves the DeviceClassName to its DeviceClass and uses
+// the class selectors to pre-filter devices before evaluating the request's own
+// CEL selectors. This avoids running CEL against devices that belong to
+// unrelated drivers or classes.
+// If fewer devices match than requested, it returns field errors indicating the
+// workload is unsatisfiable, preventing quota from being consumed by workloads
+// whose pods can never be scheduled.
+func validateCELSelectorsAgainstDevices(ctx context.Context, cl client.Client, claimSpec *resourcev1.ResourceClaimSpec, basePath *field.Path) field.ErrorList {
+	celReqs := extractCELRequests(claimSpec)
+	if len(celReqs) == 0 {
+		return nil
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList); err != nil {
+		return field.ErrorList{field.InternalError(basePath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+
+	clusterDevices := buildDeviceListFromSlices(sliceList.Items)
+	classCache := make(map[string]*resourcev1.DeviceClass)
+	consumed := make(map[int]bool)
```

  https://github.com/kubernetes-sigs/kueue/pull/11142

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T13:51:42Z

- **pkg/dra/claims.go:384** by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:51:42Z
```diff
@@ -192,3 +225,251 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		if slices.ContainsFunc(req.Exactly.Selectors, func(sel resourcev1.DeviceSelector) bool {
+			return sel.CEL != nil
+		}) {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// compileCELSelectors compiles CEL expressions from selectors, skipping non-CEL selectors.
+// Returns compiled results and any compilation errors encountered.
+func compileCELSelectors(selectors []resourcev1.DeviceSelector, errPath *field.Path, errContext string) ([]dracel.CompilationResult, field.ErrorList) {
+	var compiled []dracel.CompilationResult
+	var allErrs field.ErrorList
+
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := celCache.GetOrCompile(sel.CEL.Expression)
+		if result.Error != nil {
+			allErrs = append(allErrs, field.Invalid(
+				errPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("%s: %s", errContext, result.Error.Detail),
+			))
+		} else {
+			compiled = append(compiled, result)
+		}
+	}
+	return compiled, allErrs
+}
+
+// evaluateSelectorsOnDevice checks if all compiled CEL selectors match the given device.
+// Returns (allMatch bool, firstError error). If any selector fails to evaluate or returns
+// false, allMatch will be false. The first evaluation error encountered is returned.
+func evaluateSelectorsOnDevice(ctx context.Context, selectors []dracel.CompilationResult, dev dracel.Device) (bool, error) {
+	for _, comp := range selectors {
+		matches, _, err := comp.DeviceMatches(ctx, dev)
+		if err != nil {
+			return false, err
+		}
+		if !matches {
+			return false, nil
+		}
+	}
+	return true, nil
+}
+
+// buildDeviceListFromSlices extracts all devices from ResourceSlices into CEL device representations.
+func buildDeviceListFromSlices(slices []resourcev1.ResourceSlice) []dracel.Device {
+	var devices []dracel.Device
+	for i := range slices {
+		slice := &slices[i]
+		for j := range slice.Spec.Devices {
+			dev := &slice.Spec.Devices[j]
+			devices = append(devices, dracel.Device{
+				Driver:     slice.Spec.Driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			})
+		}
+	}
+	return devices
+}
+
+// resolveAndCompileDeviceClass fetches and compiles selectors for a DeviceClass, using cache.
+// Returns compiled selectors and a field error if the class cannot be resolved or has invalid CEL.
+func resolveAndCompileDeviceClass(
+	ctx context.Context,
+	cl client.Client,
+	className string,
+	cache map[string]*resourcev1.DeviceClass,
+	basePath *field.Path,
+	reqIndex int,
+) ([]dracel.CompilationResult, *field.Error) {
+	if className == "" {
+		return nil, nil
+	}
+
+	dc, ok := cache[className]
+	if !ok {
+		dc = &resourcev1.DeviceClass{}
+		if err := cl.Get(ctx, client.ObjectKey{Name: className}, dc); err != nil {
+			return nil, field.InternalError(
+				basePath.Child("devices", "requests").Index(reqIndex),
+				fmt.Errorf("failed to get DeviceClass %s: %w", className, err),
+			)
+		}
+		cache[className] = dc
+	}
+
+	compiled, errs := compileCELSelectors(
+		dc.Spec.Selectors,
+		basePath.Child("devices", "requests").Index(reqIndex),
+		fmt.Sprintf("DeviceClass %s has invalid CEL selector", className),
+	)
+	if len(errs) > 0 {
+		return nil, errs[0] // Return first error
+	}
+
+	return compiled, nil
+}
+
+// countMatchingDevices evaluates devices against class and request selectors,
+// marking consumed devices and returning the match count and first error encountered.
+// Devices already in the consumed map are skipped to prevent double-counting.
+func countMatchingDevices(ctx context.Context, devices []dracel.Device, classSelectors, requestSelectors []dracel.CompilationResult, consumed map[int]bool, needed int64) (int64, error) {
+	var matchCount int64
+	var firstEvalErr error
+
+	for devIdx, dev := range devices {
+		if consumed[devIdx] {
+			continue
+		}
+
+		// First check class selectors to skip devices that don't belong to this class.
+		classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, dev)
+		if err != nil || !classMatch {
+			continue
+		}
+
+		// Then evaluate the request's own selectors.
+		allMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, dev)
+		if err != nil {
+			if firstEvalErr == nil {
+				firstEvalErr = err
+			}
+			continue
+		}
+
+		if allMatch {
+			consumed[devIdx] = true
```

  The name choice of `consumed` was unforunate. I opened up https://github.com/kubernetes-sigs/kueue/pull/11142 to rename to make it more clear of the intention.
