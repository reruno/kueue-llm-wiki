# PR #977: kep: issue 487 support for kubectl plugin

**Summary**: kep: issue 487 support for kubectl plugin

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/977

**Last updated**: 2023-11-13T16:16:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vsoch](https://github.com/vsoch)
- **Branch**: `kep/issue-487` → `main`
- **Created**: 2023-07-12T12:47:09Z
- **Updated**: 2023-11-13T16:16:20Z
- **Merged**: 2023-11-13T16:16:18Z
- **Closed**: 2023-11-13T16:16:18Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+648 / -0**

## Description

Problem: It is challenging to see all details of a Job/Workload Solution: Provide a dual command-line tool and plugin to expose this information.

#### What type of PR is this?

KEP!

/kind feature

#### What this PR does / why we need it:

It's not currently possible to (easily) see information about jobs or queues.

#### Which issue(s) this PR fixes:

Part of #487 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-12T12:47:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 53a538f4031e79b5cd5ab12c5da69da0112e00ad |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64c95015a84fff000878340e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-977--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-977--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk3Ny0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.sB3ysxCCmYuCU94_p0ZQWIH1xGP5cy5yTPMWse6S14E)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-12T12:47:18Z

Hi @vsoch. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-12T14:28:12Z

/ok-to-test

### Comment by [@vsoch](https://github.com/vsoch) — 2023-07-13T00:01:58Z

> Not sure where to put this but it may be worth calling out that a goal could be to integrate this with a package manager for kubectl plugins.

Yep definitely can do that!

### Comment by [@vsoch](https://github.com/vsoch) — 2023-07-14T13:44:27Z

Thank you for the review @trasc ! I've redone the document to be oriented toward workloads, and taken your review into account.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T16:53:48Z

Since this is a kubernetes subproject, doing another repo doesn't fit very well.
In other words, we can't have "kubernetes-sigs/kueue/{core,cli,dashboard}".

Still, it's something we can consider in the future.

OTOH, I'm in the need for extra OWNERS, so we could definitely have a folder for cli/dashboard with their own OWNERS.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T15:53:40Z

Let's merge this as-is and we'll follow up with some updates.

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-13T15:53:47Z

LGTM label has been added.  <details>Git tree hash: 5fc04bdb580b0b8384708314f35f840613bcdafd</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-13T15:53:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/977#issuecomment-1808435044" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/977#" title="Author self-approved">vsoch</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-12T23:56:38Z

Not sure where to put this but it may be worth calling out that a goal could be to integrate this with a package manager for kubectl plugins.  

https://krew.sigs.k8s.io/plugins/

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-13T00:00:46Z

- **keps/487-kubectl-plugin/README.md:157** by [@kannon92](https://github.com/kannon92) — 2023-07-13T00:00:46Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
```

  From what I understand of Kueue, Kueue has a concept of a workload which is internal and hidden from the user.  I think that in order to answer the question of if a "workload" is running/finished you would actually want the plugin to reach out to the actual workload.  
  
  In the case of Job you can look at the status or something like that.  I don't know if a Kueue workload has these concepts.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-13T00:02:32Z

- **keps/487-kubectl-plugin/README.md:157** by [@vsoch](https://github.com/vsoch) — 2023-07-13T00:02:32Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
```

  I'm not familiar enough either - I've only run very basic examples using Kueue with jobs.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-13T00:04:13Z

- **keps/487-kubectl-plugin/README.md:157** by [@kannon92](https://github.com/kannon92) — 2023-07-13T00:04:13Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
```

  Ignore me!  https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/workload_types.go#L130
  
  Looks like it may be available as a condition.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-13T00:11:21Z

- **keps/487-kubectl-plugin/README.md:157** by [@vsoch](https://github.com/vsoch) — 2023-07-13T00:11:20Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
```

  @kannon92 I will take many requests but one thing I will not do is ignore you! :)

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-14T07:06:41Z

Overall we should not talk about `jobs`, kueue is scheduling `workloads` which could then provide some information about their owner.

- **keps/487-kubectl-plugin/README.md:229** by [@trasc](https://github.com/trasc) — 2023-07-14T06:38:30Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
```

  ```suggestion
  kuctl workloads <queue-name>
  ```

- **keps/487-kubectl-plugin/README.md:325** by [@trasc](https://github.com/trasc) — 2023-07-14T06:53:34Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl jobs
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl jobs --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific job. E.g., if I just submit a job and know the name, this would be intuitive to type.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl jobs --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Job
+
+Describe is intended to show more detailed information about one or more jobs. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about jobs. Right now I'm providing a generic template anticipating that. The above will include all metadata that the job offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Job
```

  In my opinion this is a tricky one,  because the same way that deleting a `workload` will only result in it to be recreated by the parent integration controller is very likely the same will happen if you delete a job that was originally created by another operator  (`flux` maybe) .  You will need walk up the ownership tree and let the user know what is about to happen, someting like.
  
  `In order to cancel xxx workload the flux job yyy will be deleted...`

- **keps/487-kubectl-plugin/README.md:352** by [@trasc](https://github.com/trasc) — 2023-07-14T06:56:28Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl jobs
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl jobs --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific job. E.g., if I just submit a job and know the name, this would be intuitive to type.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl jobs --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Job
+
+Describe is intended to show more detailed information about one or more jobs. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about jobs. Right now I'm providing a generic template anticipating that. The above will include all metadata that the job offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Job
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all jobs y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+This command (and others) will need to be designed with multi-tenancy in mind, because the requesting user should only be interacting with the jobs they own or control.
```

  For the `multi-tenancy` , having the option to specify filters for  `local-queue`, `cluster-queue` and `cohort` should be sufficient.

- **keps/487-kubectl-plugin/README.md:356** by [@trasc](https://github.com/trasc) — 2023-07-14T06:59:33Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl jobs
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl jobs --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific job. E.g., if I just submit a job and know the name, this would be intuitive to type.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl jobs --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Job
+
+Describe is intended to show more detailed information about one or more jobs. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about jobs. Right now I'm providing a generic template anticipating that. The above will include all metadata that the job offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Job
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all jobs y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+This command (and others) will need to be designed with multi-tenancy in mind, because the requesting user should only be interacting with the jobs they own or control.
+
+> Question for thought: is there a difference between what might be a cancel or a delete? Is there an abstraction for a group of jobs that could also be cancelled or deleted?
+
+### Watch Job
```

  Should be be a some kind live watch for the workloads events?

- **keps/487-kubectl-plugin/README.md:467** by [@trasc](https://github.com/trasc) — 2023-07-14T07:02:23Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl jobs
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl jobs --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific job. E.g., if I just submit a job and know the name, this would be intuitive to type.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl jobs --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Job
+
+Describe is intended to show more detailed information about one or more jobs. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about jobs. Right now I'm providing a generic template anticipating that. The above will include all metadata that the job offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Job
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all jobs y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+This command (and others) will need to be designed with multi-tenancy in mind, because the requesting user should only be interacting with the jobs they own or control.
+
+> Question for thought: is there a difference between what might be a cancel or a delete? Is there an abstraction for a group of jobs that could also be cancelled or deleted?
+
+### Watch Job
+
+After submitting a job, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing jobs, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
+
+In the future, if we can make this a full fledged client for interaction with jobs, we could consider the following commands.
+Note that these are not proposed to be in the first stage of this design document.
+
+#### Submit
+
+```
+# Submit, either a yaml as is
+kuctl submit jobfile.yaml
+
+# or a simpler abstraction that uses some kind of default or template
+# This would actually be really cool if we could map a community develoepd job spec (that works for other tools) into kueue
+kuctl submit <something else>
+```
+
+#### Update
```

  If we are talking about `workloads`, they should only be changed by their creator. and the kueue's internals ( workload controller + scheduler)

- **keps/487-kubectl-plugin/README.md:494** by [@trasc](https://github.com/trasc) — 2023-07-14T07:03:45Z
```diff
@@ -0,0 +1,545 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Jobs](#listing-jobs)
+  - [Describe Job](#describe-job)
+  - [Cancel Job](#cancel-job)
+  - [Watch Job](#watch-job)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line job management tool.
+It should be able to manage and list Jobs and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What jobs are there in the user-queue? (`jobs/describe`)
+1. What jobs are there in this specific namespace? (`jobs/describe`)
+2. What jobs are there in this specific state? (`jobs/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting jobs to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting jobs, I want to easily see the status of an entire Workload,
+or check on why my Job is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with jobs.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage jobs. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of job metadata and status, and move toward a tool that can further create / delete or otherwise interact with jobs. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Jobs
+
+The most likely thing a user wants to do is see "all" jobs. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl jobs <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl jobs
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl jobs --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific job. E.g., if I just submit a job and know the name, this would be intuitive to type.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl jobs <name>
+kuctl jobs taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl jobs --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Job
+
+Describe is intended to show more detailed information about one or more jobs. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about jobs. Right now I'm providing a generic template anticipating that. The above will include all metadata that the job offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Job
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all jobs y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+This command (and others) will need to be designed with multi-tenancy in mind, because the requesting user should only be interacting with the jobs they own or control.
+
+> Question for thought: is there a difference between what might be a cancel or a delete? Is there an abstraction for a group of jobs that could also be cancelled or deleted?
+
+### Watch Job
+
+After submitting a job, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing jobs, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
+
+In the future, if we can make this a full fledged client for interaction with jobs, we could consider the following commands.
+Note that these are not proposed to be in the first stage of this design document.
+
+#### Submit
+
+```
+# Submit, either a yaml as is
+kuctl submit jobfile.yaml
+
+# or a simpler abstraction that uses some kind of default or template
+# This would actually be really cool if we could map a community develoepd job spec (that works for other tools) into kueue
+kuctl submit <something else>
+```
+
+#### Update
+
+Is it possible to update a CRD for a kueue job? I suspect not, but actually, if it is to change the size or some policy or termination time, maybe this isn't a crazy idea.
+
+```bash
+# Note this format follows how helm sets variables
+kuctl update set path.to.attribute=thing
+
+# This does not, but it could be wanted to remove an attribute entirely (instead of trying to set the default null type)
+kuctl update remove path.to.attribute
+```
+
+### Resources
```

  This could be done, however at pod-set level. Something similar could /should be done `cluster-queue` and `cohort` , in terms of  `Total` vs `Used`.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-14T15:55:16Z

- **keps/487-kubectl-plugin/README.md:372** by [@kannon92](https://github.com/kannon92) — 2023-07-14T15:55:15Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  This may be require some thought on the CRD side.  
  
  If a Job has multiple pods, I guess you could add an option to stream those specific logs.  
  
  Events seem to make sense to me and then a separate call for logs where you allow for someone to drill into the particular pod.  
  
  In cases like MPIJob or JobSet, you could have loads of different pods with different jobs so not sure what that interface would look like.  Maybe its post MVP?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-14T15:57:25Z

@alculquicondor or @ahg-g do we want to continue with a monorepo pattern for Kueue?  I could see this as a good candidate to be put in a separate repo but I know there is a lot of overhead on that.  

I see the issue on dashboarding and this one as potentially separate repositories but its all up to you!

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-15T03:53:08Z

- **keps/487-kubectl-plugin/README.md:372** by [@vsoch](https://github.com/vsoch) — 2023-07-15T03:53:08Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  At least for the flux operator, we only care about the logs for one pod. I think for the MPI operator we'd care about a launcher pod? And JobSet is a total wild card! For those that are predictable, it might be possible to select the specific pod based on a label or similar. For others, we might need to step back and think of a larger strategy for handing logging (generally). It's definitely very important.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-07-15T14:28:09Z

- **keps/487-kubectl-plugin/README.md:372** by [@kannon92](https://github.com/kannon92) — 2023-07-15T14:28:09Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  I think that someone would want to see logs of a worker pod in MPI if they were debugging a pod.  
  
  I also see job could be tricky (for same reason as JobSet) as you could have thousands of replica pods.  With indexed job as a label that could work but still requires some clarification on API.   
  
  Maybe a kueue reviewer can weigh in on if logs should be included as a first round of the plugin.  I just don't see it as a trivial implementation (or even the API) and I think it could warrant discussion as a separate task.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-17T18:08:27Z

- **keps/487-kubectl-plugin/README.md:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-17T18:08:26Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
```

  I think it would be useful if this would work with the job name. And describe would make an aggregation of the job+workload objects, so that users can have a single view of everything that is happening with their job.
  
  With that in mind, I opened #992

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-17T20:06:47Z

- **keps/487-kubectl-plugin/README.md:372** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-17T20:06:47Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  `kubectl logs job/{name}` actually gives the output for every pod in the job (with a prefix for the pod name).
  
  In general, I think it will depend on the application and the user. Maybe we will have to have a command line flag to specify a name, index, or something else. But maybe default should be "everything"?
  
  I think I'll defer here to the people that have more experience being end-users.
  As an infrastructure developer, I'm biased to say that this is the application's problem, given how generic Kueue is.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-18T12:55:38Z

- **keps/487-kubectl-plugin/README.md:225** by [@trasc](https://github.com/trasc) — 2023-07-18T12:55:37Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
```

  Going with job-name will require to  either specify the job type also , or try to list all the supported types objects having that name.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-18T15:05:07Z

- **keps/487-kubectl-plugin/README.md:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T15:05:06Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
```

  yes, we need to include the API group and kind

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-07-18T17:48:42Z

- **keps/487-kubectl-plugin/README.md:372** by [@vsoch](https://github.com/vsoch) — 2023-07-18T17:48:42Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  I can probably give better feedback about this when I start working on something.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T15:00:35Z

- **keps/487-kubectl-plugin/README.md:98** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:12:17Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
```

  What are the things inside parenthesis?

- **keps/487-kubectl-plugin/README.md:372** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:15:09Z
```diff
@@ -0,0 +1,572 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | 
+| taco-123 | default | python | 2 |  | Pending | 
+| ant-123 | insects | echo | 2 | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name | Namespace | Command | Pods | Time | State | Queue |
+|------|-----------|---------|------|------|-------|-------|
+| taco-345 | default | python | 3 | 2.322s | Running | user-queue | 
+| taco-123 | default | python | 2 |  | Pending | user-queue |
+| ant-123 | insects | echo | 2 | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| ant-123 | insects | echo | 2 | 4.323s | Completed |
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-345 | default | python | 3 | 2.322s | Runningg |
+| taco-123 | default | python | 2 |  | Pending |
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name | Namespace | Command | Pods | Time | State |
+|------|-----------|---------|------|------|-------|
+| taco-123 | default | python | 2 |  | Pending |
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
```

  I just noticed that "streaming logs" is not listed in the goals. And I think that's ok. We should probably start with a more narrow set of features. You could add it to non-goals for now.

- **keps/487-kubectl-plugin/README.md:161** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:17:27Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
```

  nit: you can show here how the command looks like, for illustration purposes.

- **keps/487-kubectl-plugin/README.md:170** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:18:28Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
```

  Can you reference how the commands look like?

- **keps/487-kubectl-plugin/README.md:205** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:19:27Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
```

  I don't think this is major concern. They will live in the same repo.

- **keps/487-kubectl-plugin/README.md:204** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:20:42Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
```

  The suffix `ctl` is rather something common only in the k8s world. Maybe the CLI can just be named `kueue`?

- **keps/487-kubectl-plugin/README.md:227** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:24:20Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
```

  In kubectl, searches are limited to the `default` namespace, or they can point to a specific namespace `-n foo` or they can point to all namespaces `-A`. We should keep that the same.

- **keps/487-kubectl-plugin/README.md:227** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:24:44Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
```

  The queue should probably be optional.

- **keps/487-kubectl-plugin/README.md:233** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:25:28Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
```

  I would put namespace first

- **keps/487-kubectl-plugin/README.md:233** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:26:39Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
```

  What if there are multiple commands or the command is empty? Should it be the image instead?

- **keps/487-kubectl-plugin/README.md:236** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T13:27:02Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
```

  Since Kueue doesn't know about MiniCluster, this would only show as Job, wouldn't it?

- **keps/487-kubectl-plugin/README.md:253** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:39:54Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
```

  I disagree that it should be `all-namespaces` by default. Users should generally only have permissions to see their own jobs, not others'

- **keps/487-kubectl-plugin/README.md:253** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:41:00Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
```

  We could also a command like: `kueue set default-namespace my-namespace`. And we store the configuration in a file somewhere in $HOME. https://www.cloudytuts.com/tutorials/kubernetes/how-to-set-default-kubernetes-namespace/

- **keps/487-kubectl-plugin/README.md:268** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:44:06Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
```

  note that the name of the workload doesn't necessarily match the name of the job. So more likely the user knows the name of the job and we have to find the name of the workload.
  Then, users probably need to also provide the name of the API. It can be batch/Job by default.

- **keps/487-kubectl-plugin/README.md:275** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:49:22Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
```

  The only way to do this is to query the entire list of Jobs or Workloads and filter client-side. Here is where it can be specially important to only list per namespace, or the list could be too big.
  
  You also need to take into consideration that multiple users trying to use this command could accidentally cause denial-of-service.

- **keps/487-kubectl-plugin/README.md:329** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:51:19Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
```

  Where are you getting the states from? Is it Workload conditions?
  
  The design should include these details.

- **keps/487-kubectl-plugin/README.md:349** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:52:44Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
```

  What's a feature set?

- **keps/487-kubectl-plugin/README.md:330** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:54:15Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
```

  Would the command issue deletes? It's not clear from this paragraph.

- **keps/487-kubectl-plugin/README.md:407** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:56:06Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+Or a user may want to watch or stream events:
+
+kuctl watch taco-123 --events
+```
+```console
+<timestamp> <event1>
+<timestamp> <event2>
+...
+```
+
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing workloads, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
```

  ```suggestion
  ### Possible command extensions
  ```

- **keps/487-kubectl-plugin/README.md:467** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T14:58:36Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+Or a user may want to watch or stream events:
+
+kuctl watch taco-123 --events
+```
+```console
+<timestamp> <event1>
+<timestamp> <event2>
+...
+```
+
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing workloads, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
+
+In the future, if we can make this a full fledged client for interaction with workloads, we could consider the following commands.
+Note that these are not proposed to be in the first stage of this design document.
+
+#### Submit
+
+```
+# Submit, either a yaml as is
+kuctl submit workload.yaml
+
+# or a simpler abstraction that uses some kind of default or template
+# This would actually be really cool if we could map a community develoepd job or workload spec (that works for other tools) into kueue
+kuctl submit <something else>
+```
+
+#### Update
```

  I'm not too sure about this one. kubectl already provides these mechanics (`kubectl edit`). What would kueue add? Could it just be a wrapper around `kubectl edit` for the convenience of users that don't know about kubectl?

- **keps/487-kubectl-plugin/README.md:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T15:00:33Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  Throughout the examples, what is missing is a parameter for the apigroup/kind. We probably don't want the client to have to do multiple API calls (corresponding to different kinds) to find one that matches the provided name. Also the name could repeat among different Kinds.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T17:57:43Z

- **keps/487-kubectl-plugin/README.md:98** by [@vsoch](https://github.com/vsoch) — 2023-08-01T17:57:43Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
```

  Mention of which API interaction responds to the particular goal. I can remove them.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:05:04Z

- **keps/487-kubectl-plugin/README.md:204** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:05:04Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
```

  I like that!

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:19:28Z

- **keps/487-kubectl-plugin/README.md:233** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:19:27Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
```

  That's a good question - I'm inclined to take it out entirely, because what if there are multiple commands/images?

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:22:35Z

- **keps/487-kubectl-plugin/README.md:236** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:22:35Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
```

  It doesn't, but why couldn't it retrieve this metadata? it's fairly meaningful to know the kind / type.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T18:26:32Z

- **keps/487-kubectl-plugin/README.md:98** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T18:26:32Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
```

  I think it's distracting in the "goals"

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:27:32Z

- **keps/487-kubectl-plugin/README.md:275** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:27:32Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
```

  okay - we can nix pattern matching for now then.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:28:21Z

- **keps/487-kubectl-plugin/README.md:329** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:28:21Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
```

  I don't know, I haven't dug into any code yet. I assumed it's somewhere :)

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:28:43Z

- **keps/487-kubectl-plugin/README.md:349** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:28:42Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
```

  A filler term for until I actually look at what is there! :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T18:29:27Z

- **keps/487-kubectl-plugin/README.md:236** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T18:29:26Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
```

  I guess we could show the owner Kind from the owner reference.
  But while that is ok for MiniCluster, is that generally ok? Should we show both?

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:29:31Z

- **keps/487-kubectl-plugin/README.md:330** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:29:31Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
```

  I was thinking it was a natural addition - but if you think it's out of scope we can leave it for future discussion (add to non-goals for now).

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:31:25Z

- **keps/487-kubectl-plugin/README.md:467** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:31:25Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+Or a user may want to watch or stream events:
+
+kuctl watch taco-123 --events
+```
+```console
+<timestamp> <event1>
+<timestamp> <event2>
+...
+```
+
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing workloads, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
+
+In the future, if we can make this a full fledged client for interaction with workloads, we could consider the following commands.
+Note that these are not proposed to be in the first stage of this design document.
+
+#### Submit
+
+```
+# Submit, either a yaml as is
+kuctl submit workload.yaml
+
+# or a simpler abstraction that uses some kind of default or template
+# This would actually be really cool if we could map a community develoepd job or workload spec (that works for other tools) into kueue
+kuctl submit <something else>
+```
+
+#### Update
```

  When I've used edit, it typically opens the yaml in an editor. Update would be a more scoped set of commands to update key value pairs without the editor. It would be more akin to how helm install does set with keys and values.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:32:50Z

- **keps/487-kubectl-plugin/README.md:220** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:32:50Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  Do we want to do:
  
  ```
  kueue <command> --kind MPIJob
  kueue <command> -k MPIJob
  ```
  or
  
  ```
  kueue <command> --kind kubeflow.org/MPIJob
  kueue <command> -k kubeflow.org/MPIJob
  ```
  
  Or some combination of those (or something else)?

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:35:42Z

- **keps/487-kubectl-plugin/README.md:236** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:35:42Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
```

  I think Kind carries a lot of meaning - MiniCluster / MPIJob are meaningful to me. API Group is less so, at least for this use case was the first time I looked up `kubeflow.org` and I only knew the flux one because I develop the flux operator.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T18:38:23Z

- **keps/487-kubectl-plugin/README.md:467** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T18:38:23Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Summary
+
+Kueuectl is a command-line tool for interacting with Kueue, and can serve as a standalone tool or be renamed/installed to act as a kubectl plugin. Using `kueuectl` a user can manage workloads. An alternative (shorter and easier to type name) might also be `kuctl`, which I'll use for the remainder of this document. Thus, the main client takes the following format:
+
+```bash
+kuctl [subcommand] [flags]
+```
+
+As a kubectl plugin, we would install it named as `kubectl-kueue` and then the interaction would be:
+
+```bash
+kubectl kueue [subcommand] [flags]
+```
+
+We can start with basic query of workload metadata and status, and move toward a tool that can further create / delete or otherwise interact with workloads. This design document will proceed with proposed interactions and example tables, which would be printed in the terminal.
+
+
+### Version
+
+```bash
+kuctl version
+```
+
+### Listing Workloads
+
+The most likely thing a user wants to do is see "all" workloads. We will need to decide if "all" means all namespaces, or (akin to `kubectl` just those in default.  I think likely this set of attributes needs a queue too. I'm undecided if we should enforce the user to provide the queue as the first argument, e.g,
+
+```bash
+kuctl workloads <queue-name>
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | 
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | 
+
+Or allow it to be generic, and then include the Queue as a field:
+
+```bash
+kuctl workloads
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+We could likely support both, depending on how we think the average user interacts with queues (always using one, or wanting to interact with more than one). In HPC, people usually are submitting to a default queue and they don't ask for anything extra or special, but only ask for `--queue` to submit to another one.
+
+With respect to the question about namespaces - whether "default" should be the default or not, my gut likes the idea of using `--all-namespaces` as default, but only if it makes sense for the user case. I think it's annoying to have to add custom namespaces when you want to see everything, and filtering (to any namespace, including default) might only be done if it's asked for. But if most people are going to be using default, it wouldn't hurt to use the same convention as kubectl.
+
+```bash
+kuctl workloads --namespace insects
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| ant-123  |  kubeflow.org     | MPIJob      | insects   | echo    | 2    | 4.323s | Completed | user-queue |
+
+
+We can also ask for a specific workload. E.g., if I just submit a workload and know the name, this would be intuitive to type.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-123
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+I think pattern matching should work to. This is something I've always wanted to work in `kubectl` and the closest I can get is tab completion.
+
+```bash
+# kuctl workloads <name>
+kuctl workloads taco-*
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-345 | flux-framework.org| MiniCluster | default   | python  | 3    | 2.322s |   Running | user-queue |
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+We likely want to also filter by state (or other attributes, TBA which others?)
+
+```bash
+kuctl workloads --state Pending
+```
+
+| Name     | API Group         | Kind        | Namespace | Command | Pods | Time   |     State | Queue |
+|----------|-------------------|-------------|-----------|---------|------|--------|-----------|-------|
+| taco-123 | flux-framework.org| MiniCluster | default   | python  | 2    |        |   Pending | user-queue | 
+
+
+### Describe Workloads
+
+Describe is intended to show more detailed information about one or more workloads. Akin to kubectl describe, we would stack them on top of the other. Unlike kubectl, I think we should have the -o json/yaml options here (it never made sense to me that kubectl uses describe for more rich metadata, but those output variables are available with "get" !
+
+```bash
+kuctl describe taco-123
+kuctl describe taco-123 taco-345
+```
+```console
+Feature Set A:
+  Field A            Field B     Field C
+  --------           --------    ------
+  cpu                650m (8%)   0 (0%)
+  memory             100Mi (0%)  0 (0%)
+Events:
+  Field A    Field B              Field C    
+  ----       -------              ------- ... 
+  Normal     Starting             Pancakes.
+```
+
+Note that I likely will develop this when I dig into working on the tool itself, and get a sense of all the attributes available to see about workloads. Right now I'm providing a generic template anticipating that. The above will include all metadata that the workload offers, and the additional features requested in the original prompt for reasons for pending or misconfiguration.
+
+The above should also provide different output formats:
+
+```bash
+kuctl describe taco-123 -o json
+kuctl describe taco-123 -o yaml
+```
+
+### Cancel Workloads
+
+A request to cancel would be akin to deleting the CRD. A "cancel" is more intuitive / natural than a delete request for this use case.
+This implementation will be tricky because we need to make the request to the underlying controller.
+
+
+```bash
+kuctl cancel taco-123
+```
+
+It might also be useful to request a cancel all, limited to the permission that the user has, a namespace, or other filter. This likely needs a confirmation.
+
+```bash
+kuctl cancel --all
+> Are you sure you want to cancel all workloads y/n?
+```
+
+Or without the prompt:
+
+```bash
+kuctl cancel --all --force
+```
+
+Or within a specific filter:
+
+```bash
+kuctl cancel --all --namespace insects
+```
+
+To support the multi-tenancy use case, filters for each of local-queue, cluster-queue and cohort will be allowed.
+
+### Watch Workload
+
+After submitting a workload, it's nice to be able to watch / stream logs. That should be easy to do.
+
+```bash
+kuctl watch taco-123
+```
+```console
+... makin' tacos!
+... taco 1 is cooked!
+... taco 2 is cooking.
+```
+
+Or a user may want to watch or stream events:
+
+kuctl watch taco-123 --events
+```
+```console
+<timestamp> <event1>
+<timestamp> <event2>
+...
+```
+
+
+### Queues
+
+I haven't used queues extensively so this likely needs to be expanded, but akin to listing workloads, we probably want to list kueues. For all queues, this could be:
+
+```bash
+kuctl queues
+```
+
+| Name | Admitted | Pending | State |
+|------|-----------|---------|------|
+| user-queue | 1 | 1 | Operational |
+
+To filter to a specific queue:
+
+```bash
+kuctl queues user-queue
+```
+
+We likely want to be able to provide more metadata, and either we could add a `describe-queue` subcommand, or have the above second example show the more verbose information. I think I like the first idea. We also likely want to expand these subcommands to include details that the original prompt warranted. I'm not familiar enough with Kueue yet to add them. This command should also support yaml/json.
+
+```bash
+kuctl queues -o json
+```
+
+
+### Future Commands
+
+In the future, if we can make this a full fledged client for interaction with workloads, we could consider the following commands.
+Note that these are not proposed to be in the first stage of this design document.
+
+#### Submit
+
+```
+# Submit, either a yaml as is
+kuctl submit workload.yaml
+
+# or a simpler abstraction that uses some kind of default or template
+# This would actually be really cool if we could map a community develoepd job or workload spec (that works for other tools) into kueue
+kuctl submit <something else>
+```
+
+#### Update
```

  oh, it was not `edit`, it was `patch` https://jamesdefabia.github.io/docs/user-guide/kubectl/kubectl_patch/

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T18:39:55Z

- **keps/487-kubectl-plugin/README.md:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T18:39:54Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  In general, kinds can repeat across different API groups. But for the purpose of simplifying users' life, we could have some aliases for "well-known" APIs.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T18:42:09Z

- **keps/487-kubectl-plugin/README.md:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T18:42:09Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  I guess it can be similar to how `kubectl get` works: you just say `kubectl get mpijob`. I think this uses the discovery API to learn about the CRDs.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2023-08-01T18:42:18Z

- **keps/487-kubectl-plugin/README.md:220** by [@vsoch](https://github.com/vsoch) — 2023-08-01T18:42:18Z
```diff
@@ -0,0 +1,573 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Summary](#summary-1)
+  - [Version](#version)
+  - [Listing Workloads](#listing-workloads)
+  - [Describe Workloads](#describe-workloads)
+  - [Cancel Workloads](#cancel-workloads)
+  - [Watch Workload](#watch-workload)
+  - [Queues](#queues)
+  - [Future Commands](#future-commands)
+    - [Submit](#submit)
+    - [Update](#update)
+  - [Resources](#resources)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We would like to develop a Kubectl plugin for Kueue that serves as a command-line workload management tool.
+It should be able to manage and list workloads and ClusterQueues with comprehesive information.
+
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+The basic information provided by `kubectl get` does include details such as whether a workload is admitted or why it's pending. 
+As this information is available in compound objects, we need to perform different queries to the API to get it. 
+Similarly, `kubectl` cannot tell the status of a ClusterQueue that is misconfigured, nor does it
+provide details of an accompanying Workload listed alongside a job. We want to expose this information
+to the user, and in a way that feels comfortable and familiar.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+A successful plugin should be able to answer the following questions:
+
+1. What workloads are there in the user-queue? (`workloads/describe`)
+1. What workloads are there in this specific namespace? (`workloads/describe`)
+2. What workloads are there in this specific state? (`workloads/describe`)
+3. Why is my workload pending? (`describe`)
+4. Was there an error admitting my workload? (`describe`) 
+5. Is a ClusterQueue misconfigured or some other issue? (this is more of an admin command?) (`queues` or `describe-queues`)
+6. All of the above, but instead of a table I want json/yaml (add `-o yaml/json`)
+
+And for this scoped work, the plugin should be added to the [krew plugins package manager](https://krew.sigs.k8s.io/plugins/).
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- Serve as a duplicate implementation of information available via kubectl already
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+We propose creating a command-line tool that can serve as a Kubectl plugin that exposes this missing information. We also propose using a design strategy that mimics existing tools that are available for other workload managers across HPC and cloud that users are comforable with to ease adoption of both the tool and approach of submitting workloads to Kubernetes. The main command-line interactions will take the following shape:
+
+```bash
+# As a Kubectl plugin
+kubectl kueue <options> <command< <args>
+
+# As a standalone command-line tool
+kuctl <options> <command> <args>
+```
+
+For comparison, the Armada team developed a [general client](https://github.com/armadaproject/armada/tree/master/cmd/armadactl) that uses this strategy to be renamed and fold into kubectl. This is an ideal approach because it presents an interface to kueue to make it feel more like a standalone job manager (and not force folks to live in Kubernetes abstractions, at least entirely). 
+
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload,
+or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+#### Story 2
+
+As a user coming from High Performance Computing, I am not comfortable with using
+`kubectl` and don't want to learn an entirely new means to interact with workloads.
+The proposed plugin makes the transition much easier for me.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+Maintaining the plugin in-sync with the latest version of Kueue will add additional
+maintainer responsibilities.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  > In general, kinds can repeat across different API groups. But for the purpose of simplifying users' life, we could have some aliases for "well-known" APIs.
  
  How often does that happen? Let's say we have a kubeflow.org/MPIJob and dinosaur.org/MPIJob and I ask for MPIJob. I would get both. I would then likely see the API group, and I could further adjust my filter to include API group as well.
