# PR #9774: Add custom metadata labels for metrics

**Summary**: Add custom metadata labels for metrics

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9774

**Last updated**: 2026-03-13T15:52:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/additional-metadata-label-metrics` → `main`
- **Created**: 2026-03-09T21:34:11Z
- **Updated**: 2026-03-13T15:52:43Z
- **Merged**: 2026-03-13T15:51:36Z
- **Closed**: 2026-03-13T15:51:36Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 85   **+2443 / -305**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->
/kind feature

#### What this PR does / why we need it:

Implements KEP-7066 (#9225)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7066 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: Add custom Prometheus metric labels from CQ/LQ/Cohort metadata via metrics.customLabels config.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-09T21:34:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 18f76d237d508487abbb128a6e1d827d98c6bb6d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b3efca78e7240008d683e0 |

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-10T02:46:15Z

/assign

I'd like to take first pass at a review tomorrow.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T11:19:29Z

/test pull-kueue-test-e2e-multikueue-main

Looks like infra issue: 

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9774/pull-kueue-test-e2e-multikueue-main/2031316771612397568

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T12:19:58Z

@mbobrovskyi @mszadkow @mykysha PTAL

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-10T14:16:28Z

In the KEP we call out the following integration tests:

    - CQ and LQ with matching labels: custom labels appear at /metrics
    - Config with sourceLabelKey / sourceAnnotationKey reading correctly
    - Queue without configured labels produces empty values
    - Label value change: stale series cleanup
    - Cohort with custom labels on CohortWeightedShare
    - PreemptedWorkloadsTotal with custom labels
    - Empty customLabels backward compatibility
    - CustomMetricLabels enabled with LocalQueueMetrics disabled

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:46:19Z

> In the KEP we call out the following integration tests:
> 

I've updated the PR to add the integration tests mentioned in the KEP.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T14:53:36Z

/lgtm
/approve
Thank you for implementing the important feature 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-13T14:53:45Z

LGTM label has been added.  <details>Git tree hash: 67c236e40e1c924fdcdaa891c88414fb6e850964</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-13T14:53:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9774#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9774#issuecomment-4055751029" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T15:34:34Z

/test pull-kueue-verify-main
ch cling If flake

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T02:45:31Z

- **apis/config/v1beta2/configuration_types.go:181** by [@kannon92](https://github.com/kannon92) — 2026-03-10T02:45:32Z
```diff
@@ -176,6 +176,33 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CustomLabels is a list of entries whose values will be added as extra
+	// Prometheus labels (with a "custom_" prefix) on ClusterQueue, LocalQueue,
```

  These comments confuse me. Do you expect an admin to title the labels as custom_label_a? Or you will prepend custom_ to the labels?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T10:31:29Z

- **apis/config/v1beta2/configuration_types.go:181** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T10:31:29Z
```diff
@@ -176,6 +176,33 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CustomLabels is a list of entries whose values will be added as extra
+	// Prometheus labels (with a "custom_" prefix) on ClusterQueue, LocalQueue,
```

  Kueue will prepend `custom_` to the labels. I've updated the comment on the `Name` field to make it more explicit.

### Review by [@kannon92](https://github.com/kannon92) (changes_requested) — 2026-03-10T14:15:24Z

I see some testing gaps but high level it looks good.

I haven't done a deep review of the metrics code yet.

- **apis/config/v1beta2/configuration_types.go:190** by [@kannon92](https://github.com/kannon92) — 2026-03-10T13:44:26Z
```diff
@@ -176,6 +176,33 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CustomLabels is a list of entries whose values will be added as extra
+	// Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+	// Requires the CustomMetricLabels feature gate.
+	// +optional
+	// +kubebuilder:validation:MaxItems=8
+	CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+
+// ControllerMetricsCustomLabel defines a Kubernetes label or annotation to promote
+// as a Prometheus metric label with a "custom_" prefix.
+type ControllerMetricsCustomLabel struct {
```

  Can you add information about what requirements we have on these strings?
  
  Looks like it is a qualified name.

- **pkg/cache/scheduler/clusterqueue.go:100** by [@kannon92](https://github.com/kannon92) — 2026-03-10T13:45:14Z
```diff
@@ -96,6 +96,8 @@ type clusterQueue struct {
 	AdmissionScope *kueue.AdmissionScope
 
 	roleTracker *roletracker.RoleTracker
+
+	customLabelValues []string
```

  Our API supports annotations and labels. Is this just labels?

- **site/content/en/docs/reference/kueue-config.v1beta2.md:341** by [@kannon92](https://github.com/kannon92) — 2026-03-10T14:10:52Z
```diff
@@ -334,6 +334,61 @@ It can be set to &quot;0&quot; to disable the metrics serving.</p>
 metrics will be reported.</p>
 </td>
 </tr>
+<tr><td><code>customLabels</code><br/>
+<a href="#config-kueue-x-k8s-io-v1beta2-ControllerMetricsCustomLabel"><code>[]ControllerMetricsCustomLabel</code></a>
+</td>
+<td>
+   <p>CustomLabels is a list of entries whose values will be added as extra
```

  Can we also add some documentation for this for our site?
  
  Not sure where it should be yet but we should document this feature.

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:412** by [@kannon92](https://github.com/kannon92) — 2026-03-10T14:12:49Z
```diff
@@ -411,7 +411,8 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 
 			ginkgo.By("checking the weighted share metric")
```

  There needs to be a lot more testing of this feature.
  
  Can we add a custom config test?
  
  It would be good to have some negative testing of the feature gate at the integration level.

- **pkg/workload/workload.go:1618** by [@kannon92](https://github.com/kannon92) — 2026-03-10T14:13:57Z
```diff
@@ -1610,8 +1615,8 @@ func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cq
 	recorder.Event(wl, corev1.EventTypeNormal, eventReason, message)
 }
 
-func ReportPreemption(preemptingCqName kueue.ClusterQueueReference, preemptingReason string, targetCqName kueue.ClusterQueueReference, tracker *roletracker.RoleTracker) {
-	metrics.ReportPreemption(preemptingCqName, preemptingReason, targetCqName, tracker)
+func ReportPreemption(preemptingCqName kueue.ClusterQueueReference, preemptingReason string, tracker *roletracker.RoleTracker) {
```

  nit: can you do this cleanup separately?

- **pkg/scheduler/scheduler.go:893** by [@kannon92](https://github.com/kannon92) — 2026-03-10T14:14:52Z
```diff
@@ -878,15 +879,19 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 	s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s", admission.ClusterQueue)
 
 	priorityClassName := workload.PriorityClassName(newWorkload)
-	metrics.AdmittedWorkload(admission.ClusterQueue, priorityClassName, waitTime, s.roleTracker)
+	cqCustomLabels := metrics.GetCQCustomLabelValues(admission.ClusterQueue)
+	metrics.AdmittedWorkload(admission.ClusterQueue, priorityClassName, waitTime, cqCustomLabels, s.roleTracker)
 	if features.Enabled(features.LocalQueueMetrics) {
```

  Can we verify in testing that this also works with LocalQueueMetrics?
  
  Integration would be great here.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:23:01Z

- **apis/config/v1beta2/configuration_types.go:190** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:23:01Z
```diff
@@ -176,6 +176,33 @@ type ControllerMetrics struct {
 	// metrics will be reported.
 	// +optional
 	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
+
+	// CustomLabels is a list of entries whose values will be added as extra
+	// Prometheus labels on ClusterQueue, LocalQueue, and Cohort metrics.
+	// Requires the CustomMetricLabels feature gate.
+	// +optional
+	// +kubebuilder:validation:MaxItems=8
+	CustomLabels []ControllerMetricsCustomLabel `json:"customLabels,omitempty"`
+}
+
+// ControllerMetricsCustomLabel defines a Kubernetes label or annotation to promote
+// as a Prometheus metric label with a "custom_" prefix.
+type ControllerMetricsCustomLabel struct {
```

  Done. Added string requirements to all three fields.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:23:53Z

- **pkg/cache/scheduler/clusterqueue.go:100** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:23:53Z
```diff
@@ -96,6 +96,8 @@ type clusterQueue struct {
 	AdmissionScope *kueue.AdmissionScope
 
 	roleTracker *roletracker.RoleTracker
+
+	customLabelValues []string
```

  Renamed the field to `customMetricLabelValues` and added a comment clarifying it covers both.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:24:39Z

- **site/content/en/docs/reference/kueue-config.v1beta2.md:341** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:24:39Z
```diff
@@ -334,6 +334,61 @@ It can be set to &quot;0&quot; to disable the metrics serving.</p>
 metrics will be reported.</p>
 </td>
 </tr>
+<tr><td><code>customLabels</code><br/>
+<a href="#config-kueue-x-k8s-io-v1beta2-ControllerMetricsCustomLabel"><code>[]ControllerMetricsCustomLabel</code></a>
+</td>
+<td>
+   <p>CustomLabels is a list of entries whose values will be added as extra
```

  Done. Added a new page at `site/content/en/docs/tasks/manage/observability/configure_custom_metric_labels.md`.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:29:22Z

- **pkg/workload/workload.go:1618** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:29:22Z
```diff
@@ -1610,8 +1615,8 @@ func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cq
 	recorder.Event(wl, corev1.EventTypeNormal, eventReason, message)
 }
 
-func ReportPreemption(preemptingCqName kueue.ClusterQueueReference, preemptingReason string, targetCqName kueue.ClusterQueueReference, tracker *roletracker.RoleTracker) {
-	metrics.ReportPreemption(preemptingCqName, preemptingReason, targetCqName, tracker)
+func ReportPreemption(preemptingCqName kueue.ClusterQueueReference, preemptingReason string, tracker *roletracker.RoleTracker) {
```

  Done. Reverted.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:40:45Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:412** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:40:45Z
```diff
@@ -411,7 +411,8 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 
 			ginkgo.By("checking the weighted share metric")
```

  Done. Added integration tests at `test/integration/singlecluster/controller/core/custom_labels_test.go`.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-10T17:40:54Z

- **pkg/scheduler/scheduler.go:893** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-10T17:40:54Z
```diff
@@ -878,15 +879,19 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 	s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s", admission.ClusterQueue)
 
 	priorityClassName := workload.PriorityClassName(newWorkload)
-	metrics.AdmittedWorkload(admission.ClusterQueue, priorityClassName, waitTime, s.roleTracker)
+	cqCustomLabels := metrics.GetCQCustomLabelValues(admission.ClusterQueue)
+	metrics.AdmittedWorkload(admission.ClusterQueue, priorityClassName, waitTime, cqCustomLabels, s.roleTracker)
 	if features.Enabled(features.LocalQueueMetrics) {
```

  Done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T19:33:51Z

- **pkg/cache/queue/manager_test.go:202** by [@kannon92](https://github.com/kannon92) — 2026-03-10T19:33:51Z
```diff
@@ -197,7 +197,7 @@ func TestUpdateClusterQueue(t *testing.T) {
 
 	// Put cq2 in the same cohort as cq1.
 	clusterQueues[1].Spec.CohortName = clusterQueues[0].Spec.CohortName
-	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1], true); err != nil {
+	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1], true, false); err != nil {
```

  Can you add a test case for testing the true case for UpdateClusterQueue?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T20:06:49Z

- **pkg/metrics/reassign_vectors.go:25** by [@kannon92](https://github.com/kannon92) — 2026-03-10T20:06:49Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/prometheus/client_golang/prometheus"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+)
+
+// reassignMetricVectors recreates each metric variable with custom labels
```

  I really dislike this code.
  
  I worry that we will not remember to update both reassign_vectors and metrics.go everytime we update or add a new metric.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-10T20:07:09Z

- **pkg/metrics/reassign_vectors.go:25** by [@kannon92](https://github.com/kannon92) — 2026-03-10T20:07:09Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/prometheus/client_golang/prometheus"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+)
+
+// reassignMetricVectors recreates each metric variable with custom labels
```

  Cam we reuse the definitions in metrics.go somewhow?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-11T09:28:59Z

- **pkg/controller/core/localqueue_controller.go:267** by [@mykysha](https://github.com/mykysha) — 2026-03-11T09:28:59Z
```diff
@@ -253,6 +264,13 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	log := r.logger().WithValues("localQueue", klog.KObj(e.ObjectNew))
 	log.V(2).Info("Queue update event")
 
+	if metrics.LQCustomLabels.Store(string(utilqueue.Key(e.ObjectNew)), metrics.ExtractCustomLabelValues(e.ObjectNew.GetLabels(), e.ObjectNew.GetAnnotations())) {
```

  I wonder if it would be possible to commonize the label change check and clearing process of lq and cq metrics and call that function where needed

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-11T10:09:27Z

- **pkg/cache/queue/manager_test.go:202** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-11T10:09:26Z
```diff
@@ -197,7 +197,7 @@ func TestUpdateClusterQueue(t *testing.T) {
 
 	// Put cq2 in the same cohort as cq1.
 	clusterQueues[1].Spec.CohortName = clusterQueues[0].Spec.CohortName
-	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1], true); err != nil {
+	if err := manager.UpdateClusterQueue(ctx, clusterQueues[1], true, false); err != nil {
```

  Done. I've added `TestUpdateClusterQueueLabelsUpdated`.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-11T10:12:15Z

- **pkg/metrics/reassign_vectors.go:25** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-11T10:12:15Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/prometheus/client_golang/prometheus"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+)
+
+// reassignMetricVectors recreates each metric variable with custom labels
```

  The main constraint for extracting shared variables is that the metrics doc generator parses the metric definitions by looking for inline `prometheus.New*Vec(...)` declarations. If we refactor the metric definitions to avoid duplication, the generator will skip those metrics. We ran into the same constraint in this PR: https://github.com/kubernetes-sigs/kueue/pull/9487#discussion_r2859785784. 
  
  But I’ll check if we can extend the doc generator.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-11T11:47:44Z

- **pkg/metrics/reassign_vectors.go:25** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-11T11:47:43Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/prometheus/client_golang/prometheus"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+)
+
+// reassignMetricVectors recreates each metric variable with custom labels
```

  I've opened #9802, which extends the metricsdoc generator.
  
  With that PR, I think we can consolidate `metrics.go` and `reassign_vectors.go` into a single `initMetrics(extraLabels)` function, so that metrics are constructed once with the correct labels from the start, eliminating the duplication entirely.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T10:59:12Z

- **pkg/controller/core/localqueue_controller.go:267** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T10:59:12Z
```diff
@@ -253,6 +264,13 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	log := r.logger().WithValues("localQueue", klog.KObj(e.ObjectNew))
 	log.V(2).Info("Queue update event")
 
+	if metrics.LQCustomLabels.Store(string(utilqueue.Key(e.ObjectNew)), metrics.ExtractCustomLabelValues(e.ObjectNew.GetLabels(), e.ObjectNew.GetAnnotations())) {
```

  Done. I've added `StoreAndClear` method to commonize the pattern.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T13:32:38Z

- **pkg/cache/queue/manager.go:316** by [@mimowo](https://github.com/mimowo) — 2026-03-12T13:32:38Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  Could you explain me what is the concept of updatingLabels, and why this is needed? I thought the labels are in the global configuration and so they cannot change during runtime. Is it just for writing tests?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T14:43:17Z

- **pkg/cache/queue/manager.go:316** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T14:43:16Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  What is static (set once from the global config) is the list of label names to extract (e.g., extract `team` and `cost_center`). However, the actual values those labels resolve to are dynamic.
  
  `labelsUpdated` is needed because custom label values come from each ClusterQueue’s labels/annotations and can change. When that happens, the old metric series must be cleared and re-reported with the new values.
  
  This scenario is covered by the integration test `"should clean up stale series on label value change"`:
  https://github.com/epam/kubernetes-kueue/blob/2b720f0d70b6420a7f012b9d4339be8549a1ab41/test/integration/singlecluster/controller/core/custom_labels_test.go#L169
  
  and the unit test `TestUpdateClusterQueueLabelsUpdated`:
  https://github.com/epam/kubernetes-kueue/blob/2b720f0d70b6420a7f012b9d4339be8549a1ab41/pkg/cache/queue/manager_test.go#L239

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:08:36Z

- **pkg/cache/queue/manager.go:316** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:08:36Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  > labelsUpdated is needed because custom label values come from each ClusterQueue’s labels/annotations and can change. When that happens, the old metric series must be cleared and re-reported with the new values.
  
  Hm, but I still don't understand why we need to store the set of available label values. For example, we don't store the set of available label values for `priority_class`, and the set of values for "priority_class" is resolved at runtime, based on the classes created by an admin etc.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T15:19:57Z

- **pkg/metrics/reassign_vectors.go:25** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T15:19:57Z
```diff
@@ -0,0 +1,384 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"github.com/prometheus/client_golang/prometheus"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+)
+
+// reassignMetricVectors recreates each metric variable with custom labels
```

  I've eliminated `reassign_vectors.go` entirely by consolidating all metric initialization into a single `initMetricVectors(extraLabels)` function in `metrics.go`.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T15:24:48Z

- **pkg/cache/queue/manager.go:316** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T15:24:48Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  `priority_class` appears in counters, where having multiple series per CQ is expected, since different workloads have different priorities and all those series are valid.
  
  Custom labels appear in gauges like `PendingWorkloads`. A CQ only has one `team` value at a time. If it changes from `alpha` to `beta`, the old gauge `PendingWorkloads{custom_team="alpha"} = 5` is never updated again. It persists indefinitely with stale data because it is a different Prometheus series. We need to explicitly delete it, which is what `StoreAndClear` does.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:35:14Z

- **hack/tools/metricsdoc/main.go:253** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:35:14Z
```diff
@@ -243,15 +243,20 @@ func metricFromCall(expr ast.Expr) (Metric, bool) {
 }
 
 func parseLabels(arg ast.Expr) []string {
-	var labels []string
-	cl, ok := arg.(*ast.CompositeLit)
-	if !ok {
+	switch v := arg.(type) {
+	case *ast.CompositeLit:
+		var labels []string
+		for _, elt := range v.Elts {
+			labels = append(labels, stringLiteral(elt))
+		}
 		return labels
+	case *ast.CallExpr:
+		// Extract only the static labels from append(base, dynamic...) calls.
+		if ident, ok := v.Fun.(*ast.Ident); ok && ident.Name == "append" && len(v.Args) >= 1 {
+			return parseLabels(v.Args[0])
+		}
 	}
-	for _, elt := range cl.Elts {
-		labels = append(labels, stringLiteral(elt))
-	}
```

  Can you send a preparatory PR to offload it? It is already massive

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:40:58Z

- **pkg/metrics/custom_labels.go:68** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:40:58Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
+
+// ExtractCustomLabelValues reads custom label values from a K8s object's
+// metadata according to the configured entries.
+func ExtractCustomLabelValues(labels, annotations map[string]string) []string {
+	if len(customLabelEntries) == 0 {
+		return nil
+	}
+	vals := make([]string, len(customLabelEntries))
+	for i, e := range customLabelEntries {
+		switch {
+		case e.SourceAnnotationKey != "":
+			vals[i] = annotations[e.SourceAnnotationKey]
+		case e.SourceLabelKey != "":
+			vals[i] = labels[e.SourceLabelKey]
+		default:
+			vals[i] = labels[e.Name]
+		}
+	}
+	return vals
+}
+
+func CustomLabelNames() []string {
+	return customLabelNames
+}
+
+func appendCustomLabels(base []string, customLabelValues []string) []string {
+	if len(customLabelNames) == 0 {
+		return base
+	}
+	if len(customLabelValues) == 0 {
+		return append(base, make([]string, len(customLabelNames))...)
+	}
+	return append(base, customLabelValues...)
+}
+
+// CustomLabelStore is a type-safe wrapper around SyncMap for storing custom
+// label values per object, used for stale series cleanup.
+type CustomLabelStore[K ~string] struct {
+	m *utilmaps.SyncMap[string, []string]
```

  what is the key here? What if two objects of different types have the same name, ie. LocalQueue and ClusterQueue - that is a common naming pattern,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:42:05Z

- **pkg/metrics/custom_labels.go:68** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:42:04Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
+
+// ExtractCustomLabelValues reads custom label values from a K8s object's
+// metadata according to the configured entries.
+func ExtractCustomLabelValues(labels, annotations map[string]string) []string {
+	if len(customLabelEntries) == 0 {
+		return nil
+	}
+	vals := make([]string, len(customLabelEntries))
+	for i, e := range customLabelEntries {
+		switch {
+		case e.SourceAnnotationKey != "":
+			vals[i] = annotations[e.SourceAnnotationKey]
+		case e.SourceLabelKey != "":
+			vals[i] = labels[e.SourceLabelKey]
+		default:
+			vals[i] = labels[e.Name]
+		}
+	}
+	return vals
+}
+
+func CustomLabelNames() []string {
+	return customLabelNames
+}
+
+func appendCustomLabels(base []string, customLabelValues []string) []string {
+	if len(customLabelNames) == 0 {
+		return base
+	}
+	if len(customLabelValues) == 0 {
+		return append(base, make([]string, len(customLabelNames))...)
+	}
+	return append(base, customLabelValues...)
+}
+
+// CustomLabelStore is a type-safe wrapper around SyncMap for storing custom
+// label values per object, used for stale series cleanup.
+type CustomLabelStore[K ~string] struct {
+	m *utilmaps.SyncMap[string, []string]
```

  ah, I see you instantiate a separete store per predefined type, ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:42:40Z

- **pkg/metrics/custom_labels.go:103** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:42:40Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
+
+// ExtractCustomLabelValues reads custom label values from a K8s object's
+// metadata according to the configured entries.
+func ExtractCustomLabelValues(labels, annotations map[string]string) []string {
+	if len(customLabelEntries) == 0 {
+		return nil
+	}
+	vals := make([]string, len(customLabelEntries))
+	for i, e := range customLabelEntries {
+		switch {
+		case e.SourceAnnotationKey != "":
+			vals[i] = annotations[e.SourceAnnotationKey]
+		case e.SourceLabelKey != "":
+			vals[i] = labels[e.SourceLabelKey]
+		default:
+			vals[i] = labels[e.Name]
+		}
+	}
+	return vals
+}
+
+func CustomLabelNames() []string {
+	return customLabelNames
+}
+
+func appendCustomLabels(base []string, customLabelValues []string) []string {
+	if len(customLabelNames) == 0 {
+		return base
+	}
+	if len(customLabelValues) == 0 {
+		return append(base, make([]string, len(customLabelNames))...)
+	}
+	return append(base, customLabelValues...)
+}
+
+// CustomLabelStore is a type-safe wrapper around SyncMap for storing custom
+// label values per object, used for stale series cleanup.
+type CustomLabelStore[K ~string] struct {
+	m *utilmaps.SyncMap[string, []string]
+}
+
+func newCustomLabelStore[K ~string]() CustomLabelStore[K] {
+	return CustomLabelStore[K]{m: utilmaps.NewSyncMap[string, []string](0)}
+}
+
+// Store stores new values and returns true if they differ from previous values.
+func (s CustomLabelStore[K]) Store(key K, newVals []string) bool {
+	old, existed := s.m.Swap(string(key), newVals)
+	return existed && !slices.Equal(old, newVals)
+}
+
+// StoreAndClear is like Store but also calls clearFn when labels change.
+func (s CustomLabelStore[K]) StoreAndClear(key K, newVals []string, clearFn func()) bool {
+	changed := s.Store(key, newVals)
+	if changed && clearFn != nil {
+		clearFn()
+	}
+	return changed
+}
+
+// Get returns the stored custom label values for the given key.
+func (s CustomLabelStore[K]) Get(key K) []string {
+	vals, _ := s.m.Get(string(key))
+	return vals
+}
+
+// Delete removes the stored custom label values for the given key.
+func (s CustomLabelStore[K]) Delete(key K) {
+	s.m.Delete(string(key))
+}
+
+var (
+	CQCustomLabels     = newCustomLabelStore[kueue.ClusterQueueReference]()
+	LQCustomLabels     = newCustomLabelStore[string]()
```

  why not using LocalQueue reference here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T15:43:44Z

- **pkg/metrics/custom_labels.go:29** by [@mimowo](https://github.com/mimowo) — 2026-03-12T15:43:44Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
```

  Can we avoid the global state? THis is problematic for MultiKueue integration tests, but also makes the code tricky to evolve.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T16:28:34Z

- **pkg/cache/queue/manager.go:316** by [@mimowo](https://github.com/mimowo) — 2026-03-12T16:28:34Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  Interesting, so this mechanism we could also use instead of the ResyncGuageMetrics we did to fix the ReplicaRole tracking? Because in this case also the replica_role must have at most one value. I'm wondering if the mechanism should be generic then for all guages.
  
  Also, is it only useful / applied for guage metrics?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T16:30:43Z

- **hack/tools/metricsdoc/main.go:253** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T16:30:43Z
```diff
@@ -243,15 +243,20 @@ func metricFromCall(expr ast.Expr) (Metric, bool) {
 }
 
 func parseLabels(arg ast.Expr) []string {
-	var labels []string
-	cl, ok := arg.(*ast.CompositeLit)
-	if !ok {
+	switch v := arg.(type) {
+	case *ast.CompositeLit:
+		var labels []string
+		for _, elt := range v.Elts {
+			labels = append(labels, stringLiteral(elt))
+		}
 		return labels
+	case *ast.CallExpr:
+		// Extract only the static labels from append(base, dynamic...) calls.
+		if ident, ok := v.Fun.(*ast.Ident); ok && ident.Name == "append" && len(v.Args) >= 1 {
+			return parseLabels(v.Args[0])
+		}
 	}
-	for _, elt := range cl.Elts {
-		labels = append(labels, stringLiteral(elt))
-	}
```

  Opened PR: https://github.com/kubernetes-sigs/kueue/pull/9834

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-12T18:10:19Z

- **pkg/cache/scheduler/cache.go:579** by [@mimowo](https://github.com/mimowo) — 2026-03-12T18:10:20Z
```diff
@@ -564,6 +567,18 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
+				if features.Enabled(features.CustomMetricLabels) {
+					lq.customMetricLabelValues = metrics.ExtractCustomLabelValues(newQ.Labels, newQ.Annotations)
+				}
+				lq.Unlock()
```

  The lock take/release should be certainly just undre the FG, but probably the entire green block of code here, right? There is also no need taking `c.RLock()` if the FG is disabled.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T20:41:14Z

- **pkg/metrics/custom_labels.go:103** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T20:41:14Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
+
+// ExtractCustomLabelValues reads custom label values from a K8s object's
+// metadata according to the configured entries.
+func ExtractCustomLabelValues(labels, annotations map[string]string) []string {
+	if len(customLabelEntries) == 0 {
+		return nil
+	}
+	vals := make([]string, len(customLabelEntries))
+	for i, e := range customLabelEntries {
+		switch {
+		case e.SourceAnnotationKey != "":
+			vals[i] = annotations[e.SourceAnnotationKey]
+		case e.SourceLabelKey != "":
+			vals[i] = labels[e.SourceLabelKey]
+		default:
+			vals[i] = labels[e.Name]
+		}
+	}
+	return vals
+}
+
+func CustomLabelNames() []string {
+	return customLabelNames
+}
+
+func appendCustomLabels(base []string, customLabelValues []string) []string {
+	if len(customLabelNames) == 0 {
+		return base
+	}
+	if len(customLabelValues) == 0 {
+		return append(base, make([]string, len(customLabelNames))...)
+	}
+	return append(base, customLabelValues...)
+}
+
+// CustomLabelStore is a type-safe wrapper around SyncMap for storing custom
+// label values per object, used for stale series cleanup.
+type CustomLabelStore[K ~string] struct {
+	m *utilmaps.SyncMap[string, []string]
+}
+
+func newCustomLabelStore[K ~string]() CustomLabelStore[K] {
+	return CustomLabelStore[K]{m: utilmaps.NewSyncMap[string, []string](0)}
+}
+
+// Store stores new values and returns true if they differ from previous values.
+func (s CustomLabelStore[K]) Store(key K, newVals []string) bool {
+	old, existed := s.m.Swap(string(key), newVals)
+	return existed && !slices.Equal(old, newVals)
+}
+
+// StoreAndClear is like Store but also calls clearFn when labels change.
+func (s CustomLabelStore[K]) StoreAndClear(key K, newVals []string, clearFn func()) bool {
+	changed := s.Store(key, newVals)
+	if changed && clearFn != nil {
+		clearFn()
+	}
+	return changed
+}
+
+// Get returns the stored custom label values for the given key.
+func (s CustomLabelStore[K]) Get(key K) []string {
+	vals, _ := s.m.Get(string(key))
+	return vals
+}
+
+// Delete removes the stored custom label values for the given key.
+func (s CustomLabelStore[K]) Delete(key K) {
+	s.m.Delete(string(key))
+}
+
+var (
+	CQCustomLabels     = newCustomLabelStore[kueue.ClusterQueueReference]()
+	LQCustomLabels     = newCustomLabelStore[string]()
```

  Done. Changed to LocalQueueReference

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T20:41:59Z

- **pkg/cache/scheduler/cache.go:579** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T20:41:59Z
```diff
@@ -564,6 +567,18 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
+				if features.Enabled(features.CustomMetricLabels) {
+					lq.customMetricLabelValues = metrics.ExtractCustomLabelValues(newQ.Labels, newQ.Annotations)
+				}
+				lq.Unlock()
```

  Right, done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T21:18:56Z

- **pkg/metrics/custom_labels.go:29** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T21:18:56Z
```diff
@@ -0,0 +1,128 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+// Set once via InitCustomLabels, then read-only.
+var customLabelNames []string
+var customLabelEntries []configapi.ControllerMetricsCustomLabel
```

  Done. I've added a CustomLabels struct.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-12T21:50:13Z

- **pkg/cache/queue/manager.go:316** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-12T21:50:13Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  Yes, they only apply to gauges, and both solve the same problem but at different scopes: `ResyncGaugeMetrics` does it for all objects at once, and `StoreAndClear` does it per object. 
  
  But that’s a good point. If that’s not blocking, I can create an issue to explore how to commonize or align the patterns in a follow-up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T09:03:29Z

- **apis/config/v1beta1/configuration_conversion.go:96** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T09:03:29Z
```diff
@@ -92,6 +92,11 @@ func Convert_v1beta2_WaitForPodsReady_To_v1beta1_WaitForPodsReady(in *v1beta2.Wa
 	return nil
 }
 
+func Convert_v1beta2_ControllerMetrics_To_v1beta1_ControllerMetrics(in *v1beta2.ControllerMetrics, out *ControllerMetrics, s conversionapi.Scope) error {
+	// CustomLabels is dropped (not present in v1beta1)
```

  Can we add to v1beta1 as well?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T09:15:18Z

- **pkg/metrics/custom_labels.go:28** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T09:15:18Z
```diff
@@ -0,0 +1,223 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func appendCustomLabels(base, customLabelValues []string) []string {
```

  ```suggestion
  func appendCustomLabels(customLabelValues []string, baseLables ...string) []string {
  ```
  I'm wondering if we can swap this arguments.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-13T09:30:13Z

- **apis/config/v1beta1/configuration_conversion.go:96** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-13T09:30:13Z
```diff
@@ -92,6 +92,11 @@ func Convert_v1beta2_WaitForPodsReady_To_v1beta1_WaitForPodsReady(in *v1beta2.Wa
 	return nil
 }
 
+func Convert_v1beta2_ControllerMetrics_To_v1beta1_ControllerMetrics(in *v1beta2.ControllerMetrics, out *ControllerMetrics, s conversionapi.Scope) error {
+	// CustomLabels is dropped (not present in v1beta1)
```

  Done.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-13T11:09:17Z

- **pkg/metrics/custom_labels.go:28** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-13T11:09:17Z
```diff
@@ -0,0 +1,223 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package metrics
+
+import (
+	"slices"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func appendCustomLabels(base, customLabelValues []string) []string {
```

  I dropped the function entirely, as it was only doing a check `len() != 0` before the append and seemed unnecessary.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2026-03-13T11:31:36Z

- **pkg/cache/queue/manager.go:316** by [@IrvingMg](https://github.com/IrvingMg) — 2026-03-13T11:31:36Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  I've opened a new issue to explore consolidating this logic: https://github.com/kubernetes-sigs/kueue/issues/9854

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:40:29Z

- **pkg/cache/queue/manager.go:316** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:40:29Z
```diff
@@ -304,7 +304,7 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated bool) error {
+func (m *Manager) UpdateClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, specUpdated, labelsUpdated bool) error {
```

  Yes, non-blocking, just waned to clarify. We will revisit how to fix the problem long term.
