# PR #9788: Extract ExpectAdmissionCheckState and deduplicate admission check tes…

**Summary**: Extract ExpectAdmissionCheckState and deduplicate admission check tes…

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9788

**Last updated**: 2026-03-17T08:12:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@reruno](https://github.com/reruno)
- **Branch**: `cleanup/7021` → `main`
- **Created**: 2026-03-10T13:13:26Z
- **Updated**: 2026-03-17T08:12:25Z
- **Merged**: 2026-03-16T16:53:40Z
- **Closed**: 2026-03-16T16:53:40Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kshalot](https://github.com/kshalot), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 13   **+301 / -340**

## Description

…t assertions

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Extract ExpectAdmissionCheckState and deduplicate admission check test assertions
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7021 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-10T13:13:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0957abe519dad1012a5d3261c0c5536caf17ab1a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b7e51325906800086feb9f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9788--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9788--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk3ODgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.LoOmyCFxVjYYqRc7hRsajhbRj4_OwGft8zdUHEQlDzk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9788--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-10T13:13:37Z

Hi @reruno. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T13:23:35Z

/ok-to-test

### Comment by [@reruno](https://github.com/reruno) — 2026-03-11T12:29:31Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-11T13:18:03Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-11T14:29:51Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-11T15:06:32Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T11:21:41Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T12:04:51Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T14:05:16Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T14:43:07Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T15:16:15Z

/test all

### Comment by [@reruno](https://github.com/reruno) — 2026-03-12T17:36:00Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T16:21:44Z

LGTM label has been added.  <details>Git tree hash: bc39d498638b22572ea03c56240d6112817b0249</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T16:21:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9788#pullrequestreview-3955108663" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9788#" title="Author self-approved">reruno</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T16:40:36Z

please cherrypick
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T16:40:41Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9788#issuecomment-4069056223):

>please cherrypick
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T16:54:20Z

@mimowo: #9788 failed to apply on top of branch "release-0.16":
```
Applying: Extract ExpectAdmissionCheckState and deduplicate admission check test assertions
Using index info to reconstruct a base tree...
A	test/e2e/multikueue-dra/dra_test.go
M	test/e2e/multikueue/e2e_test.go
M	test/e2e/multikueue/tas_test.go
A	test/integration/multikueue/dra_test.go
M	test/integration/multikueue/jobs_test.go
M	test/integration/singlecluster/tas/tas_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/multikueue/jobs_test.go
CONFLICT (modify/delete): test/integration/multikueue/dra_test.go deleted in HEAD and modified in Extract ExpectAdmissionCheckState and deduplicate admission check test assertions. Version Extract ExpectAdmissionCheckState and deduplicate admission check test assertions of test/integration/multikueue/dra_test.go left in tree.
Auto-merging test/e2e/multikueue/tas_test.go
Auto-merging test/e2e/multikueue/e2e_test.go
CONFLICT (modify/delete): test/e2e/multikueue-dra/dra_test.go deleted in HEAD and modified in Extract ExpectAdmissionCheckState and deduplicate admission check test assertions. Version Extract ExpectAdmissionCheckState and deduplicate admission check test assertions of test/e2e/multikueue-dra/dra_test.go left in tree.
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Extract ExpectAdmissionCheckState and deduplicate admission check test assertions

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9788#issuecomment-4069056223):

>please cherrypick
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T16:54:56Z

@mimowo: #9788 failed to apply on top of branch "release-0.15":
```
Applying: Extract ExpectAdmissionCheckState and deduplicate admission check test assertions
Using index info to reconstruct a base tree...
A	test/e2e/multikueue-dra/dra_test.go
M	test/e2e/multikueue/e2e_test.go
M	test/e2e/multikueue/tas_test.go
M	test/integration/multikueue/cluster_role_sharing_test.go
M	test/integration/multikueue/dispatcher_test.go
A	test/integration/multikueue/dra_test.go
M	test/integration/multikueue/enabled_integration_test.go
M	test/integration/multikueue/jobs_test.go
M	test/integration/multikueue/no_gc_test.go
M	test/integration/multikueue/provisioning_test.go
M	test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go
M	test/integration/singlecluster/tas/tas_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
CONFLICT (content): Merge conflict in test/util/util.go
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go
Auto-merging test/integration/multikueue/provisioning_test.go
Auto-merging test/integration/multikueue/no_gc_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/no_gc_test.go
Auto-merging test/integration/multikueue/jobs_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/jobs_test.go
Auto-merging test/integration/multikueue/enabled_integration_test.go
CONFLICT (modify/delete): test/integration/multikueue/dra_test.go deleted in HEAD and modified in Extract ExpectAdmissionCheckState and deduplicate admission check test assertions. Version Extract ExpectAdmissionCheckState and deduplicate admission check test assertions of test/integration/multikueue/dra_test.go left in tree.
Auto-merging test/integration/multikueue/dispatcher_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/dispatcher_test.go
Auto-merging test/integration/multikueue/cluster_role_sharing_test.go
CONFLICT (content): Merge conflict in test/integration/multikueue/cluster_role_sharing_test.go
Auto-merging test/e2e/multikueue/tas_test.go
Auto-merging test/e2e/multikueue/e2e_test.go
CONFLICT (modify/delete): test/e2e/multikueue-dra/dra_test.go deleted in HEAD and modified in Extract ExpectAdmissionCheckState and deduplicate admission check test assertions. Version Extract ExpectAdmissionCheckState and deduplicate admission check test assertions of test/e2e/multikueue-dra/dra_test.go left in tree.
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Extract ExpectAdmissionCheckState and deduplicate admission check test assertions

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9788#issuecomment-4069056223):

>please cherrypick
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T13:28:16Z

- **test/util/metrics.go:325** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T13:28:17Z
```diff
@@ -307,3 +307,19 @@ func ExpectCohortNominalQuotaGaugeMetricCleaned(cohortName, flavor, resource str
 	ginkgo.GinkgoHelper()
 	ExpectCohortNominalQuotaGaugeMetric(cohortName, flavor, resource, 0)
 }
+
+var attemptStatuses = []metrics.AdmissionResult{metrics.AdmissionResultInadmissible, metrics.AdmissionResultSuccess}
+
+func ExpectAdmissionAttemptsMetric(pending, admitted int) {
+	ginkgo.GinkgoHelper()
+	vals := []int{pending, admitted}
+
+	for i, status := range attemptStatuses {
+		metric := metrics.AdmissionAttemptsTotal.WithLabelValues(string(status))
+		gomega.Eventually(func(g gomega.Gomega) {
+			v, err := testutil.GetCounterMetricValue(metric)
+			g.Expect(err).ToNot(gomega.HaveOccurred())
+			g.Expect(int(v)).Should(gomega.Equal(vals[i]), "pending_workloads with status=%s", status)
+		}, Timeout, Interval).Should(gomega.Succeed())
+	}
+}
```

  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T13:28:52Z

- **test/integration/multikueue/no_gc_test.go:218** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T13:28:52Z
```diff
@@ -216,9 +215,7 @@ var _ = ginkgo.Describe("MultiKueue no GC", ginkgo.Label("area:multikueue", "fea
 		ginkgo.By("setting workload reservation in worker1, AC state is updated in manager and worker2 wl is removed", func() {
 			gomega.Eventually(func(g gomega.Gomega) {
 				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-				acs := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAC.Name))
-				g.Expect(acs).NotTo(gomega.BeNil())
-				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStatePending))
+				util.ExpectAdmissionCheckState(g, createdWorkload, multiKueueAC.Name, kueue.CheckStatePending, "")
```

  Please move eventually to ExpectAdmissionCheckState() as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-10T13:29:31Z

- **test/util/util.go:81** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T13:29:31Z
```diff
@@ -78,6 +78,40 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+var DefaultTestPodSetUpdates1 = []kueue.PodSetUpdate{
```

  Why this?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-11T12:30:19Z

- **test/integration/multikueue/no_gc_test.go:218** by [@reruno](https://github.com/reruno) — 2026-03-11T12:30:19Z
```diff
@@ -216,9 +215,7 @@ var _ = ginkgo.Describe("MultiKueue no GC", ginkgo.Label("area:multikueue", "fea
 		ginkgo.By("setting workload reservation in worker1, AC state is updated in manager and worker2 wl is removed", func() {
 			gomega.Eventually(func(g gomega.Gomega) {
 				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-				acs := admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAC.Name))
-				g.Expect(acs).NotTo(gomega.BeNil())
-				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStatePending))
+				util.ExpectAdmissionCheckState(g, createdWorkload, multiKueueAC.Name, kueue.CheckStatePending, "")
```

  Done

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-11T12:30:44Z

- **test/util/metrics.go:325** by [@reruno](https://github.com/reruno) — 2026-03-11T12:30:44Z
```diff
@@ -307,3 +307,19 @@ func ExpectCohortNominalQuotaGaugeMetricCleaned(cohortName, flavor, resource str
 	ginkgo.GinkgoHelper()
 	ExpectCohortNominalQuotaGaugeMetric(cohortName, flavor, resource, 0)
 }
+
+var attemptStatuses = []metrics.AdmissionResult{metrics.AdmissionResultInadmissible, metrics.AdmissionResultSuccess}
+
+func ExpectAdmissionAttemptsMetric(pending, admitted int) {
+	ginkgo.GinkgoHelper()
+	vals := []int{pending, admitted}
+
+	for i, status := range attemptStatuses {
+		metric := metrics.AdmissionAttemptsTotal.WithLabelValues(string(status))
+		gomega.Eventually(func(g gomega.Gomega) {
+			v, err := testutil.GetCounterMetricValue(metric)
+			g.Expect(err).ToNot(gomega.HaveOccurred())
+			g.Expect(int(v)).Should(gomega.Equal(vals[i]), "pending_workloads with status=%s", status)
+		}, Timeout, Interval).Should(gomega.Succeed())
+	}
+}
```

  Removed unused code from metrics.go

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-11T12:33:24Z

- **test/util/util.go:81** by [@reruno](https://github.com/reruno) — 2026-03-11T12:33:24Z
```diff
@@ -78,6 +78,40 @@ import (
 	"sigs.k8s.io/kueue/pkg/workloadslicing"
 )
 
+var DefaultTestPodSetUpdates1 = []kueue.PodSetUpdate{
```

  Moved these variables to provisioning_test.go file 
  
  These variables are used by tests inside provisioning_test.go

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T12:39:37Z

- **test/e2e/multikueue/e2e_test.go:260** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T12:39:38Z
```diff
@@ -253,19 +253,11 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdLeaderWorkload := &kueue.Workload{}
 			wlLookupKey := types.NamespacedName{Name: workloadpod.GetWorkloadNameForPod(pod.Name, pod.UID), Namespace: managerNs.Name}
 
 			// the execution should be given to worker2
 			ginkgo.By("Waiting to be admitted in worker2", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sManagerClient.Get(ctx, wlLookupKey, createdLeaderWorkload)).To(gomega.Succeed())
-					g.Expect(admissioncheck.FindAdmissionCheck(createdLeaderWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAc.Name))).To(gomega.BeComparableTo(&kueue.AdmissionCheckState{
-						Name:    kueue.AdmissionCheckReference(multiKueueAc.Name),
-						State:   kueue.CheckStateReady,
-						Message: `The workload got reservation on "worker2"`,
-					}, cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime")))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckStateWithMessage(ctx, k8sManagerClient, wlLookupKey, multiKueueAc.Name, kueue.CheckStatePending, `The workload got reservation on "worker2"`)
```

  ```suggestion
  				util.ExpectAdmissionCheckStateWithMessage(ctx, k8sManagerClient, wlLookupKey, multiKueueAc.Name, kueue.CheckStateReady, `The workload got reservation on "worker2"`)
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T12:41:44Z

- **test/e2e/multikueue/e2e_test.go:663** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T12:41:44Z
```diff
@@ -664,19 +656,11 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdLeaderWorkload := &kueue.Workload{}
 			wlLookupKey := types.NamespacedName{Name: groupName, Namespace: managerNs.Name}
 
 			// the execution should be given to worker2
 			ginkgo.By("Waiting to be admitted in worker2", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sManagerClient.Get(ctx, wlLookupKey, createdLeaderWorkload)).To(gomega.Succeed())
-					g.Expect(admissioncheck.FindAdmissionCheck(createdLeaderWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(multiKueueAc.Name))).To(gomega.BeComparableTo(&kueue.AdmissionCheckState{
-						Name:    kueue.AdmissionCheckReference(multiKueueAc.Name),
-						State:   kueue.CheckStateReady,
-						Message: `The workload got reservation on "worker2"`,
-					}, cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime")))
-				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckStateWithMessage(ctx, k8sManagerClient, wlLookupKey, multiKueueAc.Name, kueue.CheckStatePending, `The workload got reservation on "worker2"`)
```

  ```suggestion
  				util.ExpectAdmissionCheckStateWithMessage(ctx, k8sManagerClient, wlLookupKey, multiKueueAc.Name, kueue.CheckStateReady, `The workload got reservation on "worker2"`)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T12:49:56Z

- **test/e2e/multikueue/e2e_test.go:2095** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T12:49:56Z
```diff
@@ -2115,11 +2092,7 @@ func waitForJobAdmitted(wlLookupKey types.NamespacedName, acName, workerName str
 			Reason:  "Admitted",
 			Message: "The workload is admitted",
 		}, util.IgnoreConditionTimestampsAndObservedGeneration))
-		g.Expect(admissioncheck.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))).To(gomega.BeComparableTo(&kueue.AdmissionCheckState{
-			Name:    kueue.AdmissionCheckReference(acName),
-			State:   kueue.CheckStateReady,
-			Message: fmt.Sprintf(`The workload got reservation on "%s"`, workerName),
-		}, cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime")))
+		util.MustAdmissionCheckStateWithMessage(g, createdWorkload, acName, kueue.CheckStateReady, fmt.Sprintf(`The workload got reservation on "%s"`, workerName))
```

  I think it's much better to split it and use util.ExpectAdmissionCheckState() separately to keep the same pattern and avoid confusing contributors in the future.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T12:51:00Z

- **test/util/util.go:571** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T12:51:00Z
```diff
@@ -568,6 +568,50 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func MustAdmissionCheckStateWithMessage(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  I propose to do not export this function and just use as helper for ExpectAdmissionCheckState.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T12:51:32Z

- **test/util/util.go:585** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T12:51:32Z
```diff
@@ -568,6 +568,50 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func MustAdmissionCheckStateWithMessage(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	g.Expect(check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+}
+
+func MustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	MustAdmissionCheckStateWithMessage(g, updatedWl, admissionCheckName, expectedState, "", podSetUpdates...)
+}
```

  If we are using it internally I think we don't need it.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-11T14:28:58Z

- **test/util/util.go:585** by [@reruno](https://github.com/reruno) — 2026-03-11T14:28:58Z
```diff
@@ -568,6 +568,50 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func MustAdmissionCheckStateWithMessage(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	g.Expect(check.PodSetUpdates).To(gomega.Equal(podSetUpdates))
+}
+
+func MustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	MustAdmissionCheckStateWithMessage(g, updatedWl, admissionCheckName, expectedState, "", podSetUpdates...)
+}
```

  fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-11T14:29:37Z

- **test/util/util.go:571** by [@reruno](https://github.com/reruno) — 2026-03-11T14:29:37Z
```diff
@@ -568,6 +568,50 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func MustAdmissionCheckStateWithMessage(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
```

  fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T16:15:41Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T16:15:42Z
```diff
@@ -50,6 +50,40 @@ const (
 	customResourceOne = "example.org/res1"
 )
 
+var defaultTestPodSetUpdates1 = []kueue.PodSetUpdate{
```

  I actually can't find this in the original. Do we really need it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:14:31Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:852** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:14:31Z
```diff
@@ -917,13 +844,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Probably we need to put in the middle. I would propose to split it for 3 ginkgo.By() blocks:
  
  1. Checking is evicted workload.
  2. Checking AdmissionChecks reset.
  3. Checking retry info preserved.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:15:34Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:656** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:15:34Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
```

  ```suggestion
  				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady,
  					kueue.PodSetUpdate{
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:15:49Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:663** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:15:49Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "ps1",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-						{
-							Name: "ps2",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+					},
+					{
```

  ```suggestion
  					kueue.PodSetUpdate{
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:16:04Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:670** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:16:04Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "ps1",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-						{
-							Name: "ps2",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+					},
+					{
+						Name: "ps2",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-					}))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+					},
+				}...)
```

  ```suggestion
  					},
  				)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:17:03Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:948** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:17:03Z
```diff
@@ -1019,13 +940,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Here is the same. Split for 3 blocks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:17:36Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1028** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:17:36Z
```diff
@@ -1098,7 +1018,16 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(wantState,
 						cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime", "PodSetUpdates", "RequeueAfterSeconds"))))
 					g.Expect(workload.IsActive(&updatedWl)).To(gomega.BeFalse())
+					g.Expect(workload.IsActive(&updatedWl)).To(gomega.BeFalse())
+					ok, err := utiltesting.HasEventAppeared(ctx, k8sClient, corev1.Event{
+						Reason:  "AdmissionCheckRejected",
+						Type:    corev1.EventTypeWarning,
+						Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", ac.Name),
+					})
+					g.Expect(err).NotTo(gomega.HaveOccurred())
+					g.Expect(ok).To(gomega.BeTrue())
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:18:06Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1078** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:18:06Z
```diff
@@ -1141,13 +1070,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Same here, split for 3 blocks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:18:40Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1284** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:18:40Z
```diff
@@ -1354,13 +1276,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(2)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Here also.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:19:12Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1811** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:19:12Z
```diff
@@ -1879,11 +1800,15 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Label("controller
 			})
 
 			ginkgo.By("await for wl1 to have status Ready for AdmissionCheck2", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wl1Key, &wlObj)).Should(gomega.Succeed())
-					acs := admissioncheck.FindAdmissionCheck(wlObj.Status.AdmissionChecks, ac2Ref)
-					g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wl1Key, ac2Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "main",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey2.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
+						},
+					},
+				}...)
```

  ```suggestion
  				util.ExpectAdmissionCheckState(ctx, k8sClient, wl1Key, ac2Name, kueue.CheckStateReady)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:19:59Z

- **test/integration/singlecluster/tas/tas_test.go:3545** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:19:59Z
```diff
@@ -3535,12 +3534,15 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 
 				ginkgo.By("await for the check to be ready", func() {
-					gomega.Eventually(func(g gomega.Gomega) {
-						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-						state := admissioncheck.FindAdmissionCheck(wl1.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-						g.Expect(state).NotTo(gomega.BeNil())
-						g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+					util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+						{
+							Name: "main",
+							Annotations: map[string]string{
+								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
+							},
+						},
+					}...)
```

  ```suggestion
  					util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-12T18:20:50Z

- **test/util/util.go:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-12T18:20:50Z
```diff
@@ -572,6 +572,47 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if podSetUpdates != nil {
```

  ```suggestion
  	if len(podSetUpdates) > 0 {
  ```

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:09:37Z

- **test/util/util.go:583** by [@reruno](https://github.com/reruno) — 2026-03-13T09:09:37Z
```diff
@@ -572,6 +572,47 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if podSetUpdates != nil {
```

  Done

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:11:03Z

- **test/integration/singlecluster/tas/tas_test.go:3545** by [@reruno](https://github.com/reruno) — 2026-03-13T09:11:03Z
```diff
@@ -3535,12 +3534,15 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 
 				ginkgo.By("await for the check to be ready", func() {
-					gomega.Eventually(func(g gomega.Gomega) {
-						g.Expect(k8sClient.Get(ctx, wlKey, wl1)).To(gomega.Succeed())
-						state := admissioncheck.FindAdmissionCheck(wl1.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-						g.Expect(state).NotTo(gomega.BeNil())
-						g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+					util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+						{
+							Name: "main",
+							Annotations: map[string]string{
+								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
+							},
+						},
+					}...)
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:12:07Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1811** by [@reruno](https://github.com/reruno) — 2026-03-13T09:12:07Z
```diff
@@ -1879,11 +1800,15 @@ var _ = ginkgo.Describe("Provisioning with scheduling", ginkgo.Label("controller
 			})
 
 			ginkgo.By("await for wl1 to have status Ready for AdmissionCheck2", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wl1Key, &wlObj)).Should(gomega.Succeed())
-					acs := admissioncheck.FindAdmissionCheck(wlObj.Status.AdmissionChecks, ac2Ref)
-					g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wl1Key, ac2Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "main",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey2.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
+						},
+					},
+				}...)
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:13:08Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:948** by [@reruno](https://github.com/reruno) — 2026-03-13T09:13:08Z
```diff
@@ -1019,13 +940,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Done

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:15:51Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:670** by [@reruno](https://github.com/reruno) — 2026-03-13T09:15:51Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "ps1",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-						{
-							Name: "ps2",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+					},
+					{
+						Name: "ps2",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-					}))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+					},
+				}...)
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:16:11Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:663** by [@reruno](https://github.com/reruno) — 2026-03-13T09:16:11Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
+						Name: "ps1",
+						Annotations: map[string]string{
+							autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
+							autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
 						},
-						{
-							Name: "ps2",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+					},
+					{
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:16:30Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:656** by [@reruno](https://github.com/reruno) — 2026-03-13T09:16:30Z
```diff
@@ -709,28 +652,22 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
-						{
-							Name: "ps1",
-							Annotations: map[string]string{
-								autoscaling.ProvisioningRequestPodAnnotationKey: provReqKey.Name,
-								autoscaling.ProvisioningClassPodAnnotationKey:   prc.Spec.ProvisioningClassName,
-							},
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, []kueue.PodSetUpdate{
+					{
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:16:58Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:852** by [@reruno](https://github.com/reruno) — 2026-03-13T09:16:58Z
```diff
@@ -917,13 +844,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Done

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:17:36Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1284** by [@reruno](https://github.com/reruno) — 2026-03-13T09:17:36Z
```diff
@@ -1354,13 +1276,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(2)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T09:17:52Z

- **test/util/util.go:587** by [@mimowo](https://github.com/mimowo) — 2026-03-13T09:17:53Z
```diff
@@ -572,6 +572,47 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.Expect(check.PodSetUpdates).To(gomega.BeComparableTo(podSetUpdates))
+	}
```

  I would suggest to attach the entire workload object if one of the assert fails. We recenly introduced the toJSON helper for that. Please look it up.
  Debugging admission checks is very tricky in isolation, and the state of the entire workload is often useful.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T09:18:37Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1078** by [@reruno](https://github.com/reruno) — 2026-03-13T09:18:37Z
```diff
@@ -1141,13 +1070,12 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
 					g.Expect(evicted).To(gomega.BeTrue())
 					check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(check).NotTo(gomega.BeNil())
-					g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending))
 					// Retry info is preserved across eviction
 					g.Expect(check.RequeueAfterSeconds).ToNot(gomega.BeNil())
 					g.Expect(check.RetryCount).ToNot(gomega.BeNil())
 					g.Expect(*check.RetryCount).To(gomega.Equal(int32(1)))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStatePending)
```

  Fixed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T10:54:29Z

- **test/util/util.go:587** by [@reruno](https://github.com/reruno) — 2026-03-13T10:54:29Z
```diff
@@ -572,6 +572,47 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil())
+	g.Expect(check.State).To(gomega.Equal(expectedState))
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage))
+	}
+	if len(podSetUpdates) > 0 {
+		g.Expect(check.PodSetUpdates).To(gomega.BeComparableTo(podSetUpdates))
+	}
```

  I have added jsonified workload object with concise message to asserts on fail

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-13T11:02:06Z

- **test/util/util.go:577** by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:02:01Z
```diff
@@ -572,6 +572,48 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	wlJSON := toJSON(updatedWl)
```

  actually, it would be better for performance to avoid serializing on every loop, and only if needed. I think we might need to improve the current approach...

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-13T12:01:08Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1028** by [@reruno](https://github.com/reruno) — 2026-03-13T12:01:08Z
```diff
@@ -1098,7 +1018,16 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 					g.Expect(updatedWl.Status.AdmissionChecks).To(gomega.ContainElement(gomega.BeComparableTo(wantState,
 						cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime", "PodSetUpdates", "RequeueAfterSeconds"))))
 					g.Expect(workload.IsActive(&updatedWl)).To(gomega.BeFalse())
+					g.Expect(workload.IsActive(&updatedWl)).To(gomega.BeFalse())
+					ok, err := utiltesting.HasEventAppeared(ctx, k8sClient, corev1.Event{
+						Reason:  "AdmissionCheckRejected",
+						Type:    corev1.EventTypeWarning,
+						Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", ac.Name),
+					})
+					g.Expect(err).NotTo(gomega.HaveOccurred())
+					g.Expect(ok).To(gomega.BeTrue())
```

  Removed duplicate code

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:30:37Z

- **test/util/util.go:598** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T12:30:37Z
```diff
@@ -572,6 +572,51 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	getWLJSON := func() string {
+		wlJSON := toJSON(updatedWl)
+		return wlJSON
+	}
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil(), getWLJSON)
+	g.Expect(check.State).To(gomega.Equal(expectedState), getWLJSON)
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage), getWLJSON)
+	}
+	if len(podSetUpdates) > 0 {
+		g.Expect(check.PodSetUpdates).To(gomega.BeComparableTo(podSetUpdates), getWLJSON)
+	}
+}
+
+func ExpectAdmissionCheckStateWithMessage(ctx context.Context, c client.Client, wlKey client.ObjectKey, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	updatedWl := &kueue.Workload{}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(c.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+		mustAdmissionCheckState(g, updatedWl, admissionCheckName, expectedState, expectedMessage, podSetUpdates...)
+	}, LongTimeout, Interval).Should(gomega.Succeed())
```

  ```suggestion
  	}, MediumTimeout, Interval).Should(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:32:41Z

- **test/util/util.go:580** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T12:32:41Z
```diff
@@ -572,6 +572,51 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	getWLJSON := func() string {
+		wlJSON := toJSON(updatedWl)
+		return wlJSON
+	}
```

  Please move it to Eventually() and Consistently() instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:33:28Z

- **test/util/util.go:578** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T12:33:28Z
```diff
@@ -572,6 +572,51 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	getWLJSON := func() string {
+		wlJSON := toJSON(updatedWl)
```

  I'm wondering if we can update toJSON to :
  
  ```
  func toJSON(obj any) func() string {
      return func() string {
         format.Object(obj, 1)
      }
  }
  ```
  
  To execute it only on failure.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:53:57Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:553** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-13T12:53:57Z
```diff
@@ -579,14 +552,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			ginkgo.By("Checking if the admission check is still ready and workload is admitted", func() {
 				gomega.Consistently(func(g gomega.Gomega) {
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) {
  ```
  
  Now we have two Consistently, which is not good. I think we can use Eventually here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-13T12:54:25Z

Overall LGTM

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T11:05:29Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:553** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-16T11:05:29Z
```diff
@@ -579,14 +552,9 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Label("controller:provisioning",
 			ginkgo.By("Checking if the admission check is still ready and workload is admitted", func() {
 				gomega.Consistently(func(g gomega.Gomega) {
```

  @mimowo WDYT?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-16T11:11:09Z

- **test/util/util.go:598** by [@reruno](https://github.com/reruno) — 2026-03-16T11:11:09Z
```diff
@@ -572,6 +572,51 @@ func ExpectWorkloadToBeAdmittedAs(ctx context.Context, k8sClient client.Client,
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
 
+func mustAdmissionCheckState(g gomega.Gomega, updatedWl *kueue.Workload, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	getWLJSON := func() string {
+		wlJSON := toJSON(updatedWl)
+		return wlJSON
+	}
+	check := admissioncheck.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(admissionCheckName))
+	g.Expect(check).NotTo(gomega.BeNil(), getWLJSON)
+	g.Expect(check.State).To(gomega.Equal(expectedState), getWLJSON)
+	if expectedMessage != "" {
+		g.Expect(check.Message).To(gomega.Equal(expectedMessage), getWLJSON)
+	}
+	if len(podSetUpdates) > 0 {
+		g.Expect(check.PodSetUpdates).To(gomega.BeComparableTo(podSetUpdates), getWLJSON)
+	}
+}
+
+func ExpectAdmissionCheckStateWithMessage(ctx context.Context, c client.Client, wlKey client.ObjectKey, admissionCheckName string, expectedState kueue.CheckState, expectedMessage string, podSetUpdates ...kueue.PodSetUpdate) {
+	ginkgo.GinkgoHelper()
+	updatedWl := &kueue.Workload{}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(c.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+		mustAdmissionCheckState(g, updatedWl, admissionCheckName, expectedState, expectedMessage, podSetUpdates...)
+	}, LongTimeout, Interval).Should(gomega.Succeed())
```

  done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-16T16:21:36Z

Thank you!

/lgtm
/approve
