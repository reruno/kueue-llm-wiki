# PR #9791: TAS: improve performance of the addNode function.

**Summary**: TAS: improve performance of the addNode function.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9791

**Last updated**: 2026-03-11T08:14:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/improve-performance-of-the-addNode-function` → `main`
- **Created**: 2026-03-10T16:42:21Z
- **Updated**: 2026-03-11T08:14:14Z
- **Merged**: 2026-03-11T08:09:35Z
- **Closed**: 2026-03-11T08:09:35Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Changed files**: 1   **+36 / -24**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
/area tas

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
TAS: improve performance of the addNode function.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9782

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-10T16:42:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5aa88d0b8c6205a9f691d4f25bb91349d10c98bc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b11c8481004e0008db4edf |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-10T16:42:35Z

/area tas

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:04:36Z

@mbobrovskyi please reference https://github.com/kubernetes-sigs/kueue/issues/9782

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T05:13:05Z

> Thanks for this refactor. I was curious if you could share some benchmark numbers showing the improvement

I used [60k nodes configuration](https://gist.github.com/j-skiba/6b397714dc445ecb3ef99157dc7230f1):

- cpu:

<img width="1487" height="545" alt="image" src="https://github.com/user-attachments/assets/cd832cdd-d92d-4bbc-bdff-8dfab49a07b8" />

- heap:

<img width="2950" height="846" alt="image" src="https://github.com/user-attachments/assets/aa3012de-4128-4beb-a6c1-30bc70861333" />

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T08:07:26Z

LGTM label has been added.  <details>Git tree hash: e8d40ef7e7a596ecc195bc333d4d535baf12d1c0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T08:07:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9791#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9791#pullrequestreview-3927664227" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T08:12:29Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-11T08:13:14Z

@mbobrovskyi: #9791 failed to apply on top of branch "release-0.15":
```
Applying: TAS: improve performance of the addNode function.
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_flavor_snapshot.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_flavor_snapshot.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 TAS: improve performance of the addNode function.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9791#issuecomment-4037312082):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-11T08:13:49Z

@mbobrovskyi: #9791 failed to apply on top of branch "release-0.16":
```
Applying: TAS: improve performance of the addNode function.
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_flavor_snapshot.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_flavor_snapshot.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 TAS: improve performance of the addNode function.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9791#issuecomment-4037312082):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-11T08:14:14Z

@mbobrovskyi please prepare the cherrypicks manually

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-10T17:02:17Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:162** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-10T16:59:53Z
```diff
@@ -155,9 +155,15 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
 	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
+
+	var domainID utiltas.TopologyDomainID
```

  Thanks for this refactor. I was curious if you could share some benchmark numbers showing the improvement

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-10T17:06:51Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:162** by [@mimowo](https://github.com/mimowo) — 2026-03-10T17:06:51Z
```diff
@@ -155,9 +155,15 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
 	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
+
+	var domainID utiltas.TopologyDomainID
```

  +1 on benchmarking, see the issue: https://github.com/kubernetes-sigs/kueue/issues/9782
  The contribution from addNode shows up consistently on pprof charts:
  - https://github.com/kubernetes-sigs/kueue/pull/9712#issuecomment-4023895532
  - https://github.com/kubernetes-sigs/kueue/issues/9337
  
  but indeed, it would be great to repeat the experiment to also show **after**

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T05:41:25Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:162** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T05:41:25Z
```diff
@@ -155,9 +155,15 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
 	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
+
+	var domainID utiltas.TopologyDomainID
```

  https://github.com/kubernetes-sigs/kueue/pull/9791#issuecomment-4036493743

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T07:34:43Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:163** by [@mimowo](https://github.com/mimowo) — 2026-03-11T07:34:43Z
```diff
@@ -141,32 +144,47 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 	}
 
 	snapshot := &TASFlavorSnapshot{
-		log:             log,
-		topologyName:    topologyName,
-		levelKeys:       slices.Clone(levels),
-		leaves:          make(leafDomainByID),
-		tolerations:     slices.Clone(tolerations),
-		domains:         make(domainByID),
-		roots:           make(domainByID),
-		domainsPerLevel: domainsPerLevel,
+		log:                   log,
+		topologyName:          topologyName,
+		levelKeys:             slices.Clone(levels),
+		leaves:                make(leafDomainByID),
+		tolerations:           slices.Clone(tolerations),
+		domains:               make(domainByID),
+		roots:                 make(domainByID),
+		domainsPerLevel:       domainsPerLevel,
+		hostnameIsLowestLevel: len(levels) > 0 && levels[len(levels)-1] == corev1.LabelHostname,
 	}
 	return snapshot
 }
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
-	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
-	if s.isLowestLevelNode() {
-		domainID = utiltas.DomainID(levelValues[len(levelValues)-1:])
+	var levelValues []string
+	var domainID utiltas.TopologyDomainID
+	var foundLeave bool
```

  ```suggestion
  	var leafFound bool
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T07:38:44Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:163** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T07:38:44Z
```diff
@@ -141,32 +144,47 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 	}
 
 	snapshot := &TASFlavorSnapshot{
-		log:             log,
-		topologyName:    topologyName,
-		levelKeys:       slices.Clone(levels),
-		leaves:          make(leafDomainByID),
-		tolerations:     slices.Clone(tolerations),
-		domains:         make(domainByID),
-		roots:           make(domainByID),
-		domainsPerLevel: domainsPerLevel,
+		log:                   log,
+		topologyName:          topologyName,
+		levelKeys:             slices.Clone(levels),
+		leaves:                make(leafDomainByID),
+		tolerations:           slices.Clone(tolerations),
+		domains:               make(domainByID),
+		roots:                 make(domainByID),
+		domainsPerLevel:       domainsPerLevel,
+		hostnameIsLowestLevel: len(levels) > 0 && levels[len(levels)-1] == corev1.LabelHostname,
 	}
 	return snapshot
 }
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
-	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
-	if s.isLowestLevelNode() {
-		domainID = utiltas.DomainID(levelValues[len(levelValues)-1:])
+	var levelValues []string
+	var domainID utiltas.TopologyDomainID
+	var foundLeave bool
```

  Thank you!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T07:38:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:136** by [@mimowo](https://github.com/mimowo) — 2026-03-11T07:38:53Z
```diff
@@ -131,6 +131,9 @@ type TASFlavorSnapshot struct {
 
 	// tolerations represents the list of tolerations defined for the resource flavor
 	tolerations []corev1.Toleration
+
+	// hostnameIsLowestLevel indicates if kubernetes.io/hostname is the lowest topology level
+	hostnameIsLowestLevel bool
```

  ```suggestion
  	// isLowestLevelNode indicates if kubernetes.io/hostname is the lowest topology level
  	isLowestLevelNode bool
  ```
  I think it makes the core more readbable. We care about Nodes, because only they have taints, labels etc. 
  
  Yes, we detect that by the `kubernetes.io/hostname` currently, but this should probably be configurable, because the label can be overriten by kubelet. We don't support it currently.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T07:41:01Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:136** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T07:41:01Z
```diff
@@ -131,6 +131,9 @@ type TASFlavorSnapshot struct {
 
 	// tolerations represents the list of tolerations defined for the resource flavor
 	tolerations []corev1.Toleration
+
+	// hostnameIsLowestLevel indicates if kubernetes.io/hostname is the lowest topology level
+	hostnameIsLowestLevel bool
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T07:41:34Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:178** by [@mimowo](https://github.com/mimowo) — 2026-03-11T07:41:34Z
```diff
@@ -141,32 +144,47 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 	}
 
 	snapshot := &TASFlavorSnapshot{
-		log:             log,
-		topologyName:    topologyName,
-		levelKeys:       slices.Clone(levels),
-		leaves:          make(leafDomainByID),
-		tolerations:     slices.Clone(tolerations),
-		domains:         make(domainByID),
-		roots:           make(domainByID),
-		domainsPerLevel: domainsPerLevel,
+		log:                   log,
+		topologyName:          topologyName,
+		levelKeys:             slices.Clone(levels),
+		leaves:                make(leafDomainByID),
+		tolerations:           slices.Clone(tolerations),
+		domains:               make(domainByID),
+		roots:                 make(domainByID),
+		domainsPerLevel:       domainsPerLevel,
+		hostnameIsLowestLevel: len(levels) > 0 && levels[len(levels)-1] == corev1.LabelHostname,
 	}
 	return snapshot
 }
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
-	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
-	if s.isLowestLevelNode() {
-		domainID = utiltas.DomainID(levelValues[len(levelValues)-1:])
+	var levelValues []string
+	var domainID utiltas.TopologyDomainID
+	var foundLeave bool
+
+	if s.hostnameIsLowestLevel {
+		// When the lowest level is kubernetes.io/hostname, directly extract hostname.
+		hostname := node.Labels[corev1.LabelHostname]
+		domainID = utiltas.TopologyDomainID(hostname)
+		_, foundLeave = s.leaves[domainID]
+		// Only compute levelValues when we actually need to create a new leafDomain.
+		if !foundLeave {
+			levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		}
+	} else {
+		// Compute full level values and domain ID.
+		levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		domainID = utiltas.DomainID(levelValues)
+		_, foundLeave = s.leaves[domainID]
```

  it is not that important, but here we could also compute `levelValues`, only if creating a new node, righ? I l wondering computation of the `levelValues` can be delayed in both cases.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-11T07:48:24Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:178** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-11T07:48:24Z
```diff
@@ -141,32 +144,47 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 	}
 
 	snapshot := &TASFlavorSnapshot{
-		log:             log,
-		topologyName:    topologyName,
-		levelKeys:       slices.Clone(levels),
-		leaves:          make(leafDomainByID),
-		tolerations:     slices.Clone(tolerations),
-		domains:         make(domainByID),
-		roots:           make(domainByID),
-		domainsPerLevel: domainsPerLevel,
+		log:                   log,
+		topologyName:          topologyName,
+		levelKeys:             slices.Clone(levels),
+		leaves:                make(leafDomainByID),
+		tolerations:           slices.Clone(tolerations),
+		domains:               make(domainByID),
+		roots:                 make(domainByID),
+		domainsPerLevel:       domainsPerLevel,
+		hostnameIsLowestLevel: len(levels) > 0 && levels[len(levels)-1] == corev1.LabelHostname,
 	}
 	return snapshot
 }
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
-	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
-	if s.isLowestLevelNode() {
-		domainID = utiltas.DomainID(levelValues[len(levelValues)-1:])
+	var levelValues []string
+	var domainID utiltas.TopologyDomainID
+	var foundLeave bool
+
+	if s.hostnameIsLowestLevel {
+		// When the lowest level is kubernetes.io/hostname, directly extract hostname.
+		hostname := node.Labels[corev1.LabelHostname]
+		domainID = utiltas.TopologyDomainID(hostname)
+		_, foundLeave = s.leaves[domainID]
+		// Only compute levelValues when we actually need to create a new leafDomain.
+		if !foundLeave {
+			levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		}
+	} else {
+		// Compute full level values and domain ID.
+		levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		domainID = utiltas.DomainID(levelValues)
+		_, foundLeave = s.leaves[domainID]
```

  Yeah, but we need `levelValues` to calculate `domainID`, which we return and use to add capacity.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T08:06:39Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:178** by [@mimowo](https://github.com/mimowo) — 2026-03-11T08:06:39Z
```diff
@@ -141,32 +144,47 @@ func newTASFlavorSnapshot(log logr.Logger, topologyName kueue.TopologyReference,
 	}
 
 	snapshot := &TASFlavorSnapshot{
-		log:             log,
-		topologyName:    topologyName,
-		levelKeys:       slices.Clone(levels),
-		leaves:          make(leafDomainByID),
-		tolerations:     slices.Clone(tolerations),
-		domains:         make(domainByID),
-		roots:           make(domainByID),
-		domainsPerLevel: domainsPerLevel,
+		log:                   log,
+		topologyName:          topologyName,
+		levelKeys:             slices.Clone(levels),
+		leaves:                make(leafDomainByID),
+		tolerations:           slices.Clone(tolerations),
+		domains:               make(domainByID),
+		roots:                 make(domainByID),
+		domainsPerLevel:       domainsPerLevel,
+		hostnameIsLowestLevel: len(levels) > 0 && levels[len(levels)-1] == corev1.LabelHostname,
 	}
 	return snapshot
 }
 
 func (s *TASFlavorSnapshot) addNode(node *nodeInfo) utiltas.TopologyDomainID {
-	levelValues := utiltas.LevelValues(s.levelKeys, node.Labels)
-	domainID := utiltas.DomainID(levelValues)
-	if s.isLowestLevelNode() {
-		domainID = utiltas.DomainID(levelValues[len(levelValues)-1:])
+	var levelValues []string
+	var domainID utiltas.TopologyDomainID
+	var foundLeave bool
+
+	if s.hostnameIsLowestLevel {
+		// When the lowest level is kubernetes.io/hostname, directly extract hostname.
+		hostname := node.Labels[corev1.LabelHostname]
+		domainID = utiltas.TopologyDomainID(hostname)
+		_, foundLeave = s.leaves[domainID]
+		// Only compute levelValues when we actually need to create a new leafDomain.
+		if !foundLeave {
+			levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		}
+	} else {
+		// Compute full level values and domain ID.
+		levelValues = utiltas.LevelValues(s.levelKeys, node.Labels)
+		domainID = utiltas.DomainID(levelValues)
+		_, foundLeave = s.leaves[domainID]
```

  I see, makes sense, I missed that

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-11T08:07:19Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15
