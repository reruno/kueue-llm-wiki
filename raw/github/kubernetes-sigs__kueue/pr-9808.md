# PR #9808: Add support for quotaCheckStrategy

**Summary**: Add support for quotaCheckStrategy

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9808

**Last updated**: 2026-05-06T19:02:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@MaysaMacedo](https://github.com/MaysaMacedo)
- **Branch**: `add-support-quota-check-strategy` → `main`
- **Created**: 2026-03-11T15:30:56Z
- **Updated**: 2026-05-06T19:02:48Z
- **Merged**: 2026-05-06T08:24:24Z
- **Closed**: 2026-05-06T08:24:24Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@olekzabl](https://github.com/olekzabl), [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 22   **+839 / -36**

## Description

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:

Introduces a new configuration API which allows to restrict the
quota check to only resources which are declared in the `ClusterQueue`.
Other resources requested by the Pod are skipped from the quota checks.

#### Which issue(s) this PR fixes:
Fixes #7513

#### Special notes for your reviewer:
This PR was developed with the assistance of Claude.

#### Does this PR introduce a user-facing change?
```release-note
Add a new configuration named `quotaCheckStrategy` to be able to restrict the quota check to only resources which are declared in the `ClusterQueue`.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-11T15:30:59Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-11T15:31:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5327a599654242ad27591e49a24a4938c7eac541 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fa65ec1f6ec500081cbb29 |

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-12T19:53:20Z

/hold

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-13T22:25:51Z

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-17T00:41:07Z

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-17T23:37:44Z

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-24T01:15:28Z

/test pull-kueue-test-integration-baseline-main

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-24T18:42:32Z

/hold cancel

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-24T19:27:46Z

@mimowo @tenzen-y
This is ready for a look, can you check it whenever possible?
The approach I've taken is by skipping the flavor assignment. Let me know if you're aware of  any impact/testing scenario that I might not be experimenting with already.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-25T11:42:55Z

@sohankunkerkar hey, I see you recommended integration tests, is there any scenario you're looking for?
I added a couple of singlecluster e2e already.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-30T12:48:22Z

cc @sohankunkerkar ptal

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-08T00:49:16Z

Based on https://github.com/kubernetes-sigs/kueue/pull/9808#issuecomment-4120578353 it seems that this PR is not WIP anymore.

/retitle Add support for quotaCheckStrategy

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T05:33:51Z

@MaysaMacedo what is the status here? 

I'm also wondering if we should re-consider putting this quotaCheckStrategy at the CQ level. Maybe it makes sense for alpha to skip the API update, and just rely on the FG. Then we would introduce the API, either global or CQ-scoped before Beta, wdyt? This would also simplify that PR, and allow us to validate the idea without API cc @tenzen-y @kannon92 @sohankunkerkar , wdyt?

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-10T12:13:12Z

@mimowo I still need to address @sohankunkerkar 's review points on the PR.
About the API, just my 2 cents, given it's already implemented in the PR, maybe we could keep it?

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-10T13:41:07Z

> I'm also wondering if we should re-consider putting this quotaCheckStrategy at the CQ level. Maybe it makes sense for alpha to skip the API update, and just rely on the FG. Then we would introduce the API, either global or CQ-scoped before Beta, wdyt? This would also simplify that PR, and allow us to validate the idea without API cc @tenzen-y @kannon92 @sohankunkerkar , wdyt?

cc @amy 

I discussed the original feature with Amy. Based on what we thought I think a configuration makes more sense than at the CQ level. Our goal is to limit the operational burden of admins to remember to update `excludeResourcePrefixes` so I worry putting this information at the CQ level still makes it an operational issue to remember to add this information to each CQ.

Putting it as an alpha feature gate makes it difficult for us to offer this to customers. I'd like alpha to match what we eventually want to offer customers so I also prefer the PR as is. (minus the pending updates to address reviews).

We also agreed on the design on the [KEP](https://github.com/kubernetes-sigs/kueue/tree/main/keps/7513-quota-check-strategy).

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T14:25:52Z

> I discussed the original feature with Amy. Based on what we thought I think a configuration makes more sense than at the CQ level. Our goal is to limit the operational burden of admins to remember to update excludeResourcePrefixes so I worry putting this information at the CQ level still makes it an operational issue to remember to add this information to each CQ.

Good point, making it global makes it easier for admins. OTOH, maybe for some clusters a CQ requires another setting.
Still, moving this to CQ level could be another extension of the feature later on, rather than replacement.

> Putting it as an alpha feature gate makes it difficult for us to offer this to customers. I'd like alpha to match what we eventually want to offer customers so I also prefer the PR as is. (minus the pending updates to address reviews).

That makes sense, I just want to clarify that this is going to be behind an alpha feature for the first release anyway. 

> We also agreed on the design on the KEP.

Sure, I'm just exploring your view on that. It is not unheard of that some designs change during the implementation phase. Still, if you think global is better for your use case I'm all for it, we can revisit the CQ-level override for that setting later on.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-14T01:01:14Z

The PR is ready for another look.

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-17T13:09:18Z

@tenzen-y @mimowo This PR is ready for a review whenever you can. :pray:

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-20T08:43:05Z

cc @sohankunkerkar could you also give it a pass?

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-30T17:21:08Z

/hold
I'll unhold when  I address all the comments from the review

### Comment by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T15:34:22Z

/hold cancel

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:50:39Z

Minor items.

/lgtm

/assign @sohankunkerkar @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T16:50:48Z

LGTM label has been added.  <details>Git tree hash: b1b9a9ee675bf730d261536e222d2717a9eb646b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T19:08:49Z

LGTM label has been added.  <details>Git tree hash: b389ec74764e248e67843ad5b6153667b7258a08</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T17:55:51Z

LGTM label has been added.  <details>Git tree hash: 394ab73eda62efd0a8c2a2a288aa9e73f1619ac0</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T07:55:28Z

/lgtm
/approve
Thank you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-06T07:55:35Z

LGTM label has been added.  <details>Git tree hash: ae7e4c5e45227a5e990e342e9dfb942d3087b23c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-06T07:55:38Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9808#" title="Author self-approved">MaysaMacedo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9808#issuecomment-4386088777" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T08:52:49Z

/kind feature

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-24T21:50:15Z

Thanks! I think it would be helpful if you add an integration test for this.

- **pkg/config/validation.go:554** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-24T21:31:13Z
```diff
@@ -535,6 +551,12 @@ func ValidateFeatureGates(featureGateCLI string, featureGateMap map[string]bool)
 		}
 	}
 
+	if !strings.Contains(featureGateCLI, string(features.QuotaCheckStrategy)+"=true") && !featureGateMap[string(features.QuotaCheckStrategy)] {
```

  The KEP [says](https://github.com/kubernetes-sigs/kueue/tree/main/keps/7513-quota-check-strategy#feature-gate) when the gate is disabled, `quotaCheckStrategy` should be ignored (fallback to BlockUndeclared), but this rejects the config and prevents Kueue from starting. Remove this block and instead add `features.Enabled(features.QuotaCheckStrategy)` to `quotaCheckStrategy()` in main.go, so the field is silently ignored when the gate is off. Also strings.Contains on the CLI string is fragile.

- **pkg/scheduler/flavorassigner/flavorassigner.go:653** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-24T21:41:42Z
```diff
@@ -637,10 +644,18 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 		var groupStatus Status
 		for resName, quantity := range requests {
-			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079).
-			if quantity == 0 && a.cq.RGByResource(resName) == nil {
-				continue
+			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079) or
+			// If quotaCheckStrategy is IgnoreUndeclared, skip resources not declared in the ClusterQueue.
+			if a.cq.RGByResource(resName) == nil {
+				if quantity == 0 {
+					continue
+				}
+				if a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared {
```

  I guess undeclared resources are correctly skipped from flavor assignment here, but check how AFS entry penalties are computed. The penalty calculation still sees the full unfiltered resource set from the workload spec, not the filtered admission. This means undeclared resources influence admission ordering even though quota ignores them.

- **pkg/scheduler/flavorassigner/flavorassigner.go:215** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-24T21:43:36Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
```

  Use `log.V(3).Info`

- **pkg/scheduler/flavorassigner/flavorassigner.go:654** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-24T21:43:54Z
```diff
@@ -637,10 +644,18 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 		var groupStatus Status
 		for resName, quantity := range requests {
-			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079).
-			if quantity == 0 && a.cq.RGByResource(resName) == nil {
-				continue
+			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079) or
+			// If quotaCheckStrategy is IgnoreUndeclared, skip resources not declared in the ClusterQueue.
+			if a.cq.RGByResource(resName) == nil {
+				if quantity == 0 {
+					continue
+				}
+				if a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared {
+					log.Info("Skipping resource not declared in the ClusterQueue", "res", resName)
```

  same: https://github.com/kubernetes-sigs/kueue/pull/9808/changes#r2984447414

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:131** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-24T21:47:34Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package customconfigse2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("QuotaCheckStrategy", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+		clusterQueue   *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			cfg.FeatureGates = map[string]bool{string(features.QuotaCheckStrategy): true}
+			cfg.Resources = &configapi.Resources{
+				QuotaCheckStrategy: ptr.To(configapi.QuotaCheckIgnoreUndeclared),
+			}
+			cfg.Metrics = configapi.ControllerMetrics{
+				EnableClusterQueueResources: true,
+			}
+		})
+
+		resourceFlavor = utiltestingapi.MakeResourceFlavor("test-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("").
+			GeneratedName("test-cq-ignoreundeclared-").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+					Resource(corev1.ResourceCPU, "100").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+	})
+
+	ginkgo.AfterAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.MediumTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, resourceFlavor, true, util.MediumTimeout)
+	})
+
+	ginkgo.When("Metrics is available", func() {
+		var (
+			localQueue                      *kueue.LocalQueue
+			createdJob                      *batchv1.Job
+			curlContainerName               string
+			curlPod                         *corev1.Pod
+			metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-quota-check-strategy-")
+			metricsReaderClusterRoleBinding = &rbacv1.ClusterRoleBinding{
+				ObjectMeta: metav1.ObjectMeta{Name: "metrics-reader-rolebinding-" + ns.Name},
+				Subjects: []rbacv1.Subject{
+					{
+						Kind:      "ServiceAccount",
+						Name:      serviceAccountName,
+						Namespace: kueueNS,
+					},
+				},
+				RoleRef: rbacv1.RoleRef{
+					APIGroup: rbacv1.GroupName,
+					Kind:     "ClusterRole",
+					Name:     metricsReaderClusterRoleName,
+				},
+			}
+			util.MustCreate(ctx, k8sClient, metricsReaderClusterRoleBinding)
+
+			curlPod = testingjobspod.MakePod("curl-metrics-"+ns.Name, kueueNS).
+				ServiceAccountName(serviceAccountName).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				TerminationGracePeriod(1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, curlPod)
+
+			ginkgo.By("Waiting for the curl-metrics pod to run.", func() {
+				util.WaitForPodRunning(ctx, k8sClient, curlPod)
+			})
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-ignoreundeclared-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
```

  You might need to add `metricsReaderClusterRoleBinding` and `curlpod` are created in kueue-system, not in ns. AfterEach only deletes ns, so these leak across test runs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T09:40:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@mimowo](https://github.com/mimowo) — 2026-03-25T09:40:17Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  This code should be under feature gate and the check for quota strategy?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T09:42:03Z

- **pkg/config/validation.go:558** by [@mimowo](https://github.com/mimowo) — 2026-03-25T09:42:03Z
```diff
@@ -535,6 +551,12 @@ func ValidateFeatureGates(featureGateCLI string, featureGateMap map[string]bool)
 		}
 	}
 
+	if !strings.Contains(featureGateCLI, string(features.QuotaCheckStrategy)+"=true") && !featureGateMap[string(features.QuotaCheckStrategy)] {
+		if cfg.Resources != nil && cfg.Resources.QuotaCheckStrategy != nil {
+			allErrs = append(allErrs, field.Invalid(resourceQuotaCheckStrategyPath, featureGateMap[string(features.QuotaCheckStrategy)], "quotaCheckStrategy is not allowed when feature gate QuotaCheckStrategy is disabled"))
+		}
+	}
```

  Move this validation to later stage when the feature gates are loaded so that you can just check if the FG is enabled or not without this strings.Contains. Note that we also have two ways of setting feature gates - cli and configuration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T09:43:02Z

- **pkg/config/validation.go:558** by [@mimowo](https://github.com/mimowo) — 2026-03-25T09:43:02Z
```diff
@@ -535,6 +551,12 @@ func ValidateFeatureGates(featureGateCLI string, featureGateMap map[string]bool)
 		}
 	}
 
+	if !strings.Contains(featureGateCLI, string(features.QuotaCheckStrategy)+"=true") && !featureGateMap[string(features.QuotaCheckStrategy)] {
+		if cfg.Resources != nil && cfg.Resources.QuotaCheckStrategy != nil {
+			allErrs = append(allErrs, field.Invalid(resourceQuotaCheckStrategyPath, featureGateMap[string(features.QuotaCheckStrategy)], "quotaCheckStrategy is not allowed when feature gate QuotaCheckStrategy is disabled"))
+		}
+	}
```

  Actually, I would probably allow to set the ignoreUndeclared just ignore iff FG is disabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T09:44:42Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:653** by [@mimowo](https://github.com/mimowo) — 2026-03-25T09:44:42Z
```diff
@@ -637,10 +644,18 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 		var groupStatus Status
 		for resName, quantity := range requests {
-			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079).
-			if quantity == 0 && a.cq.RGByResource(resName) == nil {
-				continue
+			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079) or
+			// If quotaCheckStrategy is IgnoreUndeclared, skip resources not declared in the ClusterQueue.
+			if a.cq.RGByResource(resName) == nil {
+				if quantity == 0 {
+					continue
+				}
+				if a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared {
```

  Check also the FG here

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-25T11:41:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-25T11:41:17Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  @mimowo I was actually blocking the startup of Kueue at the validation stage, if the featureGate is not set, but the config is. That is why I didn't add it here. We prefer the featuregate check to be actually done at the stage the config is used? Maybe that makes more sense to allow other features to run.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T11:44:55Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@mimowo](https://github.com/mimowo) — 2026-03-25T11:44:55Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  This code runs on flavor assignment stage, so I see no relationship with the startup.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-25T13:06:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-25T13:06:35Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  It would block Kueue from starting when validating the featuregates here https://github.com/MaysaMacedo/kueue/blob/add-support-quota-check-strategy/cmd/kueue/main.go#L155-L158

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-26T15:30:18Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:653** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-26T15:30:18Z
```diff
@@ -637,10 +644,18 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 		var groupStatus Status
 		for resName, quantity := range requests {
-			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079).
-			if quantity == 0 && a.cq.RGByResource(resName) == nil {
-				continue
+			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079) or
+			// If quotaCheckStrategy is IgnoreUndeclared, skip resources not declared in the ClusterQueue.
+			if a.cq.RGByResource(resName) == nil {
+				if quantity == 0 {
+					continue
+				}
+				if a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared {
```

  Ok, I will check this scenario.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-27T19:17:40Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:653** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-27T19:17:40Z
```diff
@@ -637,10 +644,18 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		}
 		var groupStatus Status
 		for resName, quantity := range requests {
-			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079).
-			if quantity == 0 && a.cq.RGByResource(resName) == nil {
-				continue
+			// Skip zero-quantity requests for resources not defined in the ClusterQueue (#8079) or
+			// If quotaCheckStrategy is IgnoreUndeclared, skip resources not declared in the ClusterQueue.
+			if a.cq.RGByResource(resName) == nil {
+				if quantity == 0 {
+					continue
+				}
+				if a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared {
```

  FYI I could check that all resources are being used and not just the filtered ones, so I worked on the fix. Now, I'm trying to figure out how to test admission ordering depending on the entry penalties.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T19:22:23Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@mimowo](https://github.com/mimowo) — 2026-03-27T19:22:23Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  Maybe I wasn't clear, what I'm saying is that the new code should be under the feature gate
  
  ```
  if a.PodSets[i].Flavors[res] == nil {
    log.Info("Skipping usage count for resource with undefined flavor", "res", res)
     continue
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T19:23:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@mimowo](https://github.com/mimowo) — 2026-03-27T19:23:35Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  > It would block Kueue from starting when validating the featuregates here https://github.com/MaysaMacedo/kueue/blob/add-support-quota-check-strategy/cmd/kueue/main.go#L155-L158
  
  This code fails when feature gates are set both by CLI and configMap, I don't see how this is related to the code

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-03-30T12:31:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:217** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-03-30T12:31:04Z
```diff
@@ -210,6 +211,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if a.PodSets[i].Flavors[res] == nil {
+				log.Info("Skipping usage count for resource with undefined flavor", "res", res)
+				continue
+			}
```

  This is fixed now:
  https://github.com/kubernetes-sigs/kueue/pull/9808/changes#diff-0870ec2c0ea330a7d087a6ef599905bf9b7164a0dd889d1b00ba17c31a1bfd05R214

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-04T05:10:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:229** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T04:49:48Z
```diff
@@ -225,6 +226,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if features.Enabled(features.QuotaCheckStrategy) && a.PodSets[i].Flavors[res] == nil {
```

  Right now this skips nil flavors whenever the feature gate is on, regardless of which strategy is set. That means under `BlockUndeclared` a nil flavor gets quietly ignored instead of being caught as a bug. Add `&& a.quotaCheckStrategy == configapi.QuotaCheckIgnoreUndeclared` so it only skips when that's actually the intended behavior.

- **pkg/controller/core/workload_controller.go:1181** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T04:53:27Z
```diff
@@ -1168,7 +1177,13 @@ func (r *WorkloadReconciler) Generic(e event.TypedGenericEvent[*kueue.Workload])
 
 func (r *WorkloadReconciler) updateAfsConsumedUsage(log logr.Logger, wl *kueue.Workload) {
 	lqKey := qutil.KeyFromWorkload(wl)
-	penalty := afs.CalculateEntryPenalty(workload.NewInfo(wl).SumTotalRequests(), r.admissionFSConfig)
+	totalRequests := workload.NewInfo(wl).SumTotalRequests()
+	if features.Enabled(features.QuotaCheckStrategy) && r.quotaCheckStrategy == config.QuotaCheckIgnoreUndeclared {
```

  Same: https://github.com/kubernetes-sigs/kueue/pull/9808/changes#r3035133993

- **pkg/scheduler/scheduler.go:989** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T04:54:19Z
```diff
@@ -963,10 +973,22 @@ const (
 	subtract
 )
 
+func allCoveredResources(resourceGroups []schdcache.ResourceGroup) sets.Set[corev1.ResourceName] {
+	covered := sets.New[corev1.ResourceName]()
+	for _, rg := range resourceGroups {
+		covered = covered.Union(rg.CoveredResources)
+	}
+	return covered
+}
+
 func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	lqKey := utilqueue.NewLocalQueueReference(e.Obj.Namespace, e.Obj.Spec.QueueName)
 	lqObjRef := klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName))
-	penalty := afs.CalculateEntryPenalty(e.SumTotalRequests(), s.admissionFairSharing)
+	totalRequests := e.SumTotalRequests()
+	if features.Enabled(features.QuotaCheckStrategy) && s.quotaCheckStrategy == config.QuotaCheckIgnoreUndeclared {
+		totalRequests = utilresource.FilterByNames(totalRequests, allCoveredResources(e.clusterQueueSnapshot.ResourceGroups))
```

  hmm...The penalty is computed independently at push and sub time using the CQ's current covered resources. If the CQ changes in between, the two won't cancel out cleanly. A safer approach would be to store the exact penalty that was pushed and subtract that stored value later, instead of recomputing from mutable CQ state. Not blocking for Alpha but worth considering for Beta.

- **site/content/en/docs/tasks/manage/administer_cluster_quotas.md:472** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T04:57:22Z
```diff
@@ -465,3 +465,21 @@ The Workload obtains an effective resource requests for quota purposes:
         example.com/gpu-memory: 30Gi
         example.com/credits: 61
 ```
+
+## Change quota check strategy for admission
+
+By default, administrators must specify all resources required by workloads in the ClusterQueues `.spec.resourceGroups[*]`.
+If you want relax that requirement and make resources declared on the workload, but not declared on the ClusterQueues to be skipped from quota management and admission process, 
```

  ```suggestion
  If you want to relax that requirement and make resources declared on the workload, but not declared on the ClusterQueues to be skipped from quota management and admission process, 
  ```

- **site/content/en/docs/tasks/manage/administer_cluster_quotas.md:471** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T04:58:07Z
```diff
@@ -465,3 +465,21 @@ The Workload obtains an effective resource requests for quota purposes:
         example.com/gpu-memory: 30Gi
         example.com/credits: 61
 ```
+
+## Change quota check strategy for admission
+
+By default, administrators must specify all resources required by workloads in the ClusterQueues `.spec.resourceGroups[*]`.
```

  `s/ClusterQueues/ClusterQueue's/g`

- **test/e2e/customconfigs/quota_check_strategy_test.go:49** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T05:07:06Z
```diff
@@ -0,0 +1,231 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package customconfigse2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("QuotaCheckStrategy", ginkgo.Label("feature:quota-check-strategy"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+		clusterQueue   *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			cfg.FeatureGates = map[string]bool{string(features.QuotaCheckStrategy): true}
```

  I think this will replace the other feature gates set in the default config. Should this be additive instead?

- **pkg/config/validation_test.go:1008** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-04T05:10:06Z
```diff
@@ -1005,6 +1005,22 @@ func TestValidate(t *testing.T) {
 				},
 			},
 		},
+		"quotaCheckStrategy with value ignoreundeclared not allowed with excludeResourcePrefixes": {
```

  It would be good if you could also cover the following scenarios here:
  - IgnoreUndeclared without prefixes: should pass
  - BlockUndeclared with prefixes: should pass
  - Invalid enum value: should fail with NotSupported

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-04-04T21:34:06Z

- **pkg/scheduler/scheduler.go:989** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-04T21:34:06Z
```diff
@@ -963,10 +973,22 @@ const (
 	subtract
 )
 
+func allCoveredResources(resourceGroups []schdcache.ResourceGroup) sets.Set[corev1.ResourceName] {
+	covered := sets.New[corev1.ResourceName]()
+	for _, rg := range resourceGroups {
+		covered = covered.Union(rg.CoveredResources)
+	}
+	return covered
+}
+
 func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	lqKey := utilqueue.NewLocalQueueReference(e.Obj.Namespace, e.Obj.Spec.QueueName)
 	lqObjRef := klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName))
-	penalty := afs.CalculateEntryPenalty(e.SumTotalRequests(), s.admissionFairSharing)
+	totalRequests := e.SumTotalRequests()
+	if features.Enabled(features.QuotaCheckStrategy) && s.quotaCheckStrategy == config.QuotaCheckIgnoreUndeclared {
+		totalRequests = utilresource.FilterByNames(totalRequests, allCoveredResources(e.clusterQueueSnapshot.ResourceGroups))
```

  So, your thinking is that when the second CalculateEntryPenalty happens we could just drop the additional resource that might exist on the AfsEntryPenalties, given the output from the first round of SumTotalRequests() would be different from the second given in the second the workload is already admitted?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-04-04T21:51:42Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:229** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-04T21:51:42Z
```diff
@@ -225,6 +226,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if features.Enabled(features.QuotaCheckStrategy) && a.PodSets[i].Flavors[res] == nil {
```

  I skip the flavor assignment only when the `IgnoreUndeclared` is configured here https://github.com/kubernetes-sigs/kueue/pull/9808/changes#diff-0870ec2c0ea330a7d087a6ef599905bf9b7164a0dd889d1b00ba17c31a1bfd05R668
  That is why I thought it wouldn't be necessary to add this check for `QuotaCheckIgnoreUndeclared`. Let me know if you still would like me to add it and I will need to add a quotaCheckStrategy to the Assignment object, so I can check its value here.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-05T04:12:04Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:229** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T04:12:04Z
```diff
@@ -225,6 +226,10 @@ func (a *Assignment) TotalRequestsFor(wl *workload.Info) resources.FlavorResourc
 			if q == 0 {
 				continue
 			}
+			if features.Enabled(features.QuotaCheckStrategy) && a.PodSets[i].Flavors[res] == nil {
```

  Yup, you are right there. But it's still worth adding as defensive code. If someone later adds a new caller to               `TotalRequestsFor` or changes the assignment flow, the nil flavor would silently undercount preemption usage instead of being caught.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-05T04:32:40Z

- **pkg/scheduler/scheduler.go:989** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T04:32:40Z
```diff
@@ -963,10 +973,22 @@ const (
 	subtract
 )
 
+func allCoveredResources(resourceGroups []schdcache.ResourceGroup) sets.Set[corev1.ResourceName] {
+	covered := sets.New[corev1.ResourceName]()
+	for _, rg := range resourceGroups {
+		covered = covered.Union(rg.CoveredResources)
+	}
+	return covered
+}
+
 func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	lqKey := utilqueue.NewLocalQueueReference(e.Obj.Namespace, e.Obj.Spec.QueueName)
 	lqObjRef := klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName))
-	penalty := afs.CalculateEntryPenalty(e.SumTotalRequests(), s.admissionFairSharing)
+	totalRequests := e.SumTotalRequests()
+	if features.Enabled(features.QuotaCheckStrategy) && s.quotaCheckStrategy == config.QuotaCheckIgnoreUndeclared {
+		totalRequests = utilresource.FilterByNames(totalRequests, allCoveredResources(e.clusterQueueSnapshot.ResourceGroups))
```

  Not exactly. `SumTotalRequests()` on the sub side already excludes undeclared resources because the workload is admitted and NewInfo reads from the admission status, not the pod spec. The real risk is the `FilterByNames` call that re-filters that result against the current CQ, so if the CQ changed, you get a mismatch. Since the admission status already handles the filtering, you can just remove the FilterByNames block from `updateAfsConsumedUsage` IMO.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T14:46:33Z

- **pkg/features/kube_features.go:553** by [@kannon92](https://github.com/kannon92) — 2026-04-10T14:46:33Z
```diff
@@ -543,6 +549,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	QuotaCheckStrategy: {
+		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-19T18:37:09Z

- **pkg/config/validation.go:96** by [@kannon92](https://github.com/kannon92) — 2026-04-19T18:37:09Z
```diff
@@ -87,6 +88,32 @@ func validate(c *configapi.Configuration, scheme *runtime.Scheme) field.ErrorLis
 	allErrs = append(allErrs, validateTLS(c)...)
 	allErrs = append(allErrs, validateVisibilityServer(c)...)
 	allErrs = append(allErrs, validateCustomLabels(c)...)
+	allErrs = append(allErrs, validateQuotaCheckStrategy(c)...)
+	return allErrs
+}
+
+func validateQuotaCheckStrategy(c *configapi.Configuration) field.ErrorList {
```

  We should check feature gate here.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-19T18:42:05Z

- **pkg/util/resource/resource.go:106** by [@kannon92](https://github.com/kannon92) — 2026-04-19T18:42:05Z
```diff
@@ -100,6 +101,18 @@ func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
 	return ret
 }
 
+// FilterByNames returns a new ResourceList containing only resources whose names
+// are in the allowed set.
+func FilterByNames(requests corev1.ResourceList, allowed sets.Set[corev1.ResourceName]) corev1.ResourceList {
```

  nit: If we are exporting this we should have some unit tests.

### Review by [@kannon92](https://github.com/kannon92) (changes_requested) — 2026-04-19T18:43:55Z

At a high level the code looks good to me.

We should validate the feature gate in the configuration also.

I also think we need some integration tests verifying this feature gate on/off in the scheduler.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-04-23T18:51:33Z

- **pkg/config/validation.go:96** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-23T18:51:32Z
```diff
@@ -87,6 +88,32 @@ func validate(c *configapi.Configuration, scheme *runtime.Scheme) field.ErrorLis
 	allErrs = append(allErrs, validateTLS(c)...)
 	allErrs = append(allErrs, validateVisibilityServer(c)...)
 	allErrs = append(allErrs, validateCustomLabels(c)...)
+	allErrs = append(allErrs, validateQuotaCheckStrategy(c)...)
+	return allErrs
+}
+
+func validateQuotaCheckStrategy(c *configapi.Configuration) field.ErrorList {
```

  @kannon92 I removed the validation I had here for feature gates because @sohankunkerkar brought up an interesting point https://github.com/kubernetes-sigs/kueue/pull/9808#discussion_r2984396041
  But I just noticed I'm missing checking the featuregates at quotaCheckStrategy() as he recommended, I will add it.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-04-23T19:10:02Z

- **pkg/config/validation.go:554** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-04-23T19:10:02Z
```diff
@@ -535,6 +551,12 @@ func ValidateFeatureGates(featureGateCLI string, featureGateMap map[string]bool)
 		}
 	}
 
+	if !strings.Contains(featureGateCLI, string(features.QuotaCheckStrategy)+"=true") && !featureGateMap[string(features.QuotaCheckStrategy)] {
```

  @kannon92 FYI

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-23T19:11:17Z

- **pkg/config/validation.go:96** by [@kannon92](https://github.com/kannon92) — 2026-04-23T19:11:17Z
```diff
@@ -87,6 +88,32 @@ func validate(c *configapi.Configuration, scheme *runtime.Scheme) field.ErrorLis
 	allErrs = append(allErrs, validateTLS(c)...)
 	allErrs = append(allErrs, validateVisibilityServer(c)...)
 	allErrs = append(allErrs, validateCustomLabels(c)...)
+	allErrs = append(allErrs, validateQuotaCheckStrategy(c)...)
+	return allErrs
+}
+
+func validateQuotaCheckStrategy(c *configapi.Configuration) field.ErrorList {
```

  Sounds good. Yea the main point of feature gates is to not have any impacts of this feature if you turn off the gate. In this case, you are still validating quota check strategy even if its disabled.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-05-04T15:26:38Z

- **pkg/util/resource/resource.go:106** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T15:26:38Z
```diff
@@ -100,6 +101,18 @@ func MulByFloat(q corev1.ResourceList, f float64) corev1.ResourceList {
 	return ret
 }
 
+// FilterByNames returns a new ResourceList containing only resources whose names
+// are in the allowed set.
+func FilterByNames(requests corev1.ResourceList, allowed sets.Set[corev1.ResourceName]) corev1.ResourceList {
```

  I updated it to not export given it's not needed anymore.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-05-04T15:26:51Z

- **pkg/config/validation.go:96** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T15:26:51Z
```diff
@@ -87,6 +88,32 @@ func validate(c *configapi.Configuration, scheme *runtime.Scheme) field.ErrorLis
 	allErrs = append(allErrs, validateTLS(c)...)
 	allErrs = append(allErrs, validateVisibilityServer(c)...)
 	allErrs = append(allErrs, validateCustomLabels(c)...)
+	allErrs = append(allErrs, validateQuotaCheckStrategy(c)...)
+	return allErrs
+}
+
+func validateQuotaCheckStrategy(c *configapi.Configuration) field.ErrorList {
```

  added the check.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:30:23Z

- **cmd/kueue/main.go:174** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:30:23Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  I don't really see how this is related to this feature.
  
  Can you do this as a separate PR?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:33:44Z

- **pkg/config/validation_test.go:1009** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:33:44Z
```diff
@@ -1005,11 +1006,76 @@ func TestValidate(t *testing.T) {
 				},
 			},
 		},
+		"quotaCheckStrategy with value ignoreundeclared not allowed with excludeResourcePrefixes": {
```

  ```suggestion
  		"quotaCheckStrategy with value ignoreUndeclared not allowed with excludeResourcePrefixes": {
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:45:21Z

- **site/content/en/docs/tasks/manage/administer_cluster_quotas.md:469** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:45:21Z
```diff
@@ -465,3 +465,21 @@ The Workload obtains an effective resource requests for quota purposes:
         example.com/gpu-memory: 30Gi
         example.com/credits: 61
 ```
+
+## Change quota check strategy for admission
```

  Can you also mention that this an alpha level feature?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:47:41Z

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:194** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:47:41Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package baseline
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	rbacv1 "k8s.io/api/rbac/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingjob "sigs.k8s.io/kueue/pkg/util/testingjobs/job"
+	testingjobspod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("QuotaCheckStrategy", ginkgo.Label("feature:quotacheckstrategy"), ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns             *corev1.Namespace
+		resourceFlavor *kueue.ResourceFlavor
+		clusterQueue   *kueue.ClusterQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName, func(cfg *configapi.Configuration) {
+			if cfg.FeatureGates == nil {
+				cfg.FeatureGates = make(map[string]bool)
+			}
+			cfg.FeatureGates[string(features.QuotaCheckStrategy)] = true
+			cfg.Resources = &configapi.Resources{
+				QuotaCheckStrategy: ptr.To(configapi.QuotaCheckIgnoreUndeclared),
+			}
+			cfg.Metrics = configapi.ControllerMetrics{
+				EnableClusterQueueResources: true,
+			}
+		})
+
+		resourceFlavor = utiltestingapi.MakeResourceFlavor("test-flavor").Obj()
+		util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+		clusterQueue = utiltestingapi.MakeClusterQueue("").
+			GeneratedName("test-cq-ignoreundeclared-").
+			ResourceGroup(
+				*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+					Resource(corev1.ResourceCPU, "100").
+					Obj(),
+			).
+			Preemption(kueue.ClusterQueuePreemption{
+				WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+			}).
+			Obj()
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+	})
+
+	ginkgo.AfterAll(func() {
+		util.UpdateKueueConfigurationAndRestart(ctx, k8sClient, defaultKueueCfg, kindClusterName)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, clusterQueue, true, util.MediumTimeout)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, resourceFlavor, true, util.MediumTimeout)
+	})
+
+	ginkgo.When("Metrics is available", func() {
+		var (
+			localQueue                      *kueue.LocalQueue
+			createdJob                      *batchv1.Job
+			curlContainerName               string
+			curlPod                         *corev1.Pod
+			metricsReaderClusterRoleBinding *rbacv1.ClusterRoleBinding
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-quota-check-strategy-")
+			metricsReaderClusterRoleBinding = &rbacv1.ClusterRoleBinding{
+				ObjectMeta: metav1.ObjectMeta{Name: "metrics-reader-rolebinding-" + ns.Name},
+				Subjects: []rbacv1.Subject{
+					{
+						Kind:      "ServiceAccount",
+						Name:      serviceAccountName,
+						Namespace: kueueNS,
+					},
+				},
+				RoleRef: rbacv1.RoleRef{
+					APIGroup: rbacv1.GroupName,
+					Kind:     "ClusterRole",
+					Name:     metricsReaderClusterRoleName,
+				},
+			}
+			util.MustCreate(ctx, k8sClient, metricsReaderClusterRoleBinding)
+
+			curlPod = testingjobspod.MakePod("curl-metrics-"+ns.Name, kueueNS).
+				ServiceAccountName(serviceAccountName).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				TerminationGracePeriod(1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, curlPod)
+
+			ginkgo.By("Waiting for the curl-metrics pod to run.", func() {
+				util.WaitForPodRunning(ctx, k8sClient, curlPod)
+			})
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-ignoreundeclared-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, metricsReaderClusterRoleBinding, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, curlPod, true)
+		})
+
+		ginkgo.It("should not report metrics for resource usage for undeclared resources", func() {
+			ginkgo.By("Create a job with undeclared resource", func() {
+				createdJob = testingjob.MakeJob("ignoreundeclared-job", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).
+					Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+					RequestAndLimit(corev1.ResourceCPU, "1").
+					RequestAndLimit(corev1.ResourceMemory, "1Gi").
+					Obj()
+				util.MustCreate(ctx, k8sClient, createdJob)
+				createdJobWLName := job.GetWorkloadNameForJob(createdJob.Name, createdJob.UID)
+				workloadKey := types.NamespacedName{
+					Name:      createdJobWLName,
+					Namespace: ns.Name,
+				}
+				util.ExpectWorkloadsToBeAdmittedByKeys(ctx, k8sClient, workloadKey)
+			})
+
+			availableMetrics := [][]string{
+				{"kueue_cluster_queue_resource_usage", clusterQueue.Name, "cpu"},
+			}
+			curlContainerName = curlPod.Spec.Containers[0].Name
+			ginkgo.By("checking that resource usage metrics for declared resources are available", func() {
+				util.ExpectMetricsToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, availableMetrics)
+			})
+
+			unavailableMetrics := [][]string{
+				{"kueue_cluster_queue_resource_usage", clusterQueue.Name, "memory"},
+			}
+			ginkgo.By("checking that resource usage metrics for undeclared resources are not available", func() {
+				util.ExpectMetricsNotToBeAvailable(ctx, cfg, restClient, curlPod.Name, curlContainerName, unavailableMetrics)
+			})
+		})
+	})
+
+	ginkgo.When("Preemption is enabled", func() {
+		var (
+			localQueue *kueue.LocalQueue
+			highWPC    *kueue.WorkloadPriorityClass
+			lowWPC     *kueue.WorkloadPriorityClass
+			lowJobKey  types.NamespacedName
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "e2e-preemption-")
+
+			localQueue = utiltestingapi.MakeLocalQueue("", ns.Name).
+				GeneratedName("test-lq-preemption-").
+				ClusterQueue(clusterQueue.Name).
+				Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, highWPC, true, util.MediumTimeout)
+			util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sClient, lowWPC, true, util.MediumTimeout)
+		})
+
+		ginkgo.It("should preempt workloads that have lower priority", func() {
```

  Could you include that this is testing undeclared resource in the It block?
  
  That way we can see what feature this is testing in the junit.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:48:39Z

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:2** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:48:39Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright The Kubernetes Authors.
```

  @mimowo 
  
  I'm not entirely sure what defines baseline versus extended.
  
  Should we move alpha features to extended?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-05-04T16:55:06Z

- **cmd/kueue/main.go:174** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T16:55:06Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  This is needed in this PR because when validating the quotacheckstrategy configs we also verify if the feature gate is enabled. And it's only possible to verify if the feature gate is enabled when they are actually set, so I moved the config validation.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T16:59:41Z

- **cmd/kueue/main.go:174** by [@kannon92](https://github.com/kannon92) — 2026-05-04T16:59:41Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  Is this the bug behind https://github.com/kubernetes-sigs/kueue/issues/10866?
  
  I'm just wondering if we need to cherry-pick this change separate from this feature?

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-05-04T17:31:36Z

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:2** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T17:31:35Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright The Kubernetes Authors.
```

  I'm also not sure. I saw another feature which is alpha and is on baseline package, the `FailureRecoveryPolicy`.

### Review by [@MaysaMacedo](https://github.com/MaysaMacedo) (commented) — 2026-05-04T17:36:58Z

- **cmd/kueue/main.go:174** by [@MaysaMacedo](https://github.com/MaysaMacedo) — 2026-05-04T17:36:58Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  No, the bug you linked is for validation of feature gates.
  The work I did here is for validation of configMap, before this PR there was no check of feature gates within the validate of config Map, but now there is and that is why we started needing it.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-04T19:08:43Z

/lgtm

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-04T19:14:25Z

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:2** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-04T19:14:25Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright The Kubernetes Authors.
```

  IIRC, the baseline/extended split is about external operator dependencies (JobSet, LWS, Spark etc.), not feature maturity. This test only needs core Kueue + batch Jobs. If future tests need integration with external operators, those would go in extended.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T19:17:41Z

- **test/e2e/sequential/baseline/quota_check_strategy_test.go:2** by [@mimowo](https://github.com/mimowo) — 2026-05-04T19:17:41Z
```diff
@@ -0,0 +1,234 @@
+/*
+Copyright The Kubernetes Authors.
```

  Yes, as @sohankunkerkar mentioned they are currently split on the use of external integrations, not feature maturity. I think this feature could be tested using built-in integrations like Jobs or Pods, so it fits baseline.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-04T19:21:21Z

- **cmd/kueue/main.go:174** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-04T19:21:21Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  nit(non-blocking): The `config.Validate()` move breaks Load()'s previous invariant of returning a validated config. Even with one caller today, I'd prefer either keeping Load() semantics unchanged with a small post-gate validation pass or splitting the contract change into a separate PR.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-04T20:05:38Z

Two minor nits but overall LGTM. Thanks!

- **pkg/config/validation.go:104** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-04T19:53:28Z
```diff
@@ -87,6 +89,35 @@ func validate(c *configapi.Configuration, scheme *runtime.Scheme) field.ErrorLis
 	allErrs = append(allErrs, validateTLS(c)...)
 	allErrs = append(allErrs, validateVisibilityServer(c)...)
 	allErrs = append(allErrs, validateCustomLabels(c)...)
+	allErrs = append(allErrs, validateQuotaCheckStrategy(c)...)
+	return allErrs
+}
+
+func validateQuotaCheckStrategy(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if !features.Enabled(features.QuotaCheckStrategy) {
+		return allErrs
+	}
+	if c.Resources != nil && c.Resources.QuotaCheckStrategy != nil {
+		strategy := *c.Resources.QuotaCheckStrategy
+		if len(c.Resources.ExcludeResourcePrefixes) > 0 && strategy == configapi.QuotaCheckIgnoreUndeclared {
+			allErrs = append(allErrs, field.Invalid(
```

  nit: `field.Invalid()` here passes the pointer `c.Resources.QuotaCheckStrategy` as the bad value, while `field.NotSupported()` below correctly uses the dereferenced strategy. Should be strategy in both places? otherwise the error message renders a pointer instead of the enum string.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-05T17:23:23Z

- **cmd/kueue/main.go:174** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-05T17:23:22Z
```diff
@@ -171,6 +171,12 @@ func main() {
 		}
 	}
 
+	// Validates the configuration after it has been loaded and feature gates have been set.
```

  We discussed this offline and agreed to stick with the current approach to avoid unnecessary complications. With this PR, Load() no longer performs validation, so the config is loaded but not validated at that stage. Validation now happens later in main(), after the feature gates are set, so [this](https://github.com/kubernetes-sigs/kueue/pull/9808/changes#diff-64a1e0bd9f87bdadd12179b8e48ace63c6aa3ace3f7694375a8a0dd0e566e25dR657) change makes it clear.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-05T17:55:43Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T19:29:07Z

- **test/integration/singlecluster/scheduler/quotacheckstrategy/quota_check_strategy_test.go:192** by [@mimowo](https://github.com/mimowo) — 2026-05-05T19:29:07Z
```diff
@@ -0,0 +1,195 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package quotacheckstrategy
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/features"
+	utilqueue "sigs.k8s.io/kueue/pkg/util/queue"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Quota check strategy", ginkgo.Ordered, ginkgo.ContinueOnFailure, ginkgo.Label("feature:quotacheckstrategy"), func() {
+	ginkgo.When("quota check strategy and admission fair sharing are enabled", func() {
+		var (
+			ns            *corev1.Namespace
+			defaultFlavor *kueue.ResourceFlavor
+			cq            *kueue.ClusterQueue
+			lq            *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeAll(func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.QuotaCheckStrategy, true)
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				configapi.QuotaCheckIgnoreUndeclared,
+				&configapi.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 100 * time.Millisecond},
+					UsageHalfLifeTime:     metav1.Duration{Duration: time.Hour},
+				},
+			))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "quota-check-strategy-")
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+			cq = utiltestingapi.MakeClusterQueue("test-cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("default").
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			util.ExpectLocalQueuesToBeActive(ctx, k8sClient, lq)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("should ignore undeclared resources and not use them to calculate entry penalty", func() {
+			ginkgo.By("Creating a workload that requests a declared and an undeclared resource")
+			wl := utiltestingapi.MakeWorkload("undeclared-penalty-wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Request("example.com/gpu", "100").
+				Obj()
+			util.MustCreate(ctx, k8sClient, wl)
+
+			wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+			ginkgo.By("Verifying the workload is admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(wl)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the undeclared resource is not in admission resource usage")
+			gomega.Expect(wl.Status.Admission).NotTo(gomega.BeNil())
+			gomega.Expect(wl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+			resourceUsage := wl.Status.Admission.PodSetAssignments[0].ResourceUsage
+			gomega.Expect(resourceUsage).To(gomega.HaveKey(corev1.ResourceCPU))
+			gomega.Expect(resourceUsage).NotTo(gomega.HaveKey(corev1.ResourceName("example.com/gpu")))
+
+			ginkgo.By("Verifying the ClusterQueue usage only accounts for declared resources")
+			var updatedCQ kueue.ClusterQueue
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cq), &updatedCQ)).To(gomega.Succeed())
+				g.Expect(updatedCQ.Status.ReservingWorkloads).To(gomega.Equal(int32(1)))
+				g.Expect(updatedCQ.Status.FlavorsReservation).Should(gomega.BeComparableTo([]kueue.FlavorUsage{{
+					Name: "default",
+					Resources: []kueue.ResourceUsage{
+						{
+							Name:  corev1.ResourceCPU,
+							Total: resource.MustParse("2"),
+						},
+					},
+				}}))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying the entry penalty does not include the undeclared resource")
+			lqKey := utilqueue.NewLocalQueueReference(ns.Name, kueue.LocalQueueName(lq.Name))
+			gomega.Eventually(func(g gomega.Gomega) {
+				penalty := qManager.AfsEntryPenalties.Peek(lqKey)
+				g.Expect(penalty).NotTo(gomega.HaveKey(corev1.ResourceName("example.com/gpu")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+	ginkgo.When("quota check strategy is set to IgnoreUndeclared and feature gate is disabled", func() {
+		var (
+			ns            *corev1.Namespace
+			defaultFlavor *kueue.ResourceFlavor
+			cq            *kueue.ClusterQueue
+			lq            *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(configapi.QuotaCheckIgnoreUndeclared, nil))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "quota-check-gate-off-")
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+
+			cq = utiltestingapi.MakeClusterQueue("test-cq-gate-off").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("default").
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("test-lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			util.ExpectLocalQueuesToBeActive(ctx, k8sClient, lq)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+		})
+
+		ginkgo.It("should not admit workload with undeclared resources", func() {
+			wl := utiltestingapi.MakeWorkload("gate-off-wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				Request(corev1.ResourceCPU, "2").
+				Request("example.com/gpu", "100").
+				Obj()
+			util.MustCreate(ctx, k8sClient, wl)
+
+			wlKey := types.NamespacedName{Name: wl.Name, Namespace: ns.Name}
+
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).To(gomega.Succeed())
+				g.Expect(workload.IsAdmitted(wl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  nit, but use util.ExpectPendingWorkloadsMetric to avoid waiting time on consistency. We know that when the metric is bumped, then the workload is already considered by scheduler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T19:30:57Z

LGTM, one nit from me  👍
