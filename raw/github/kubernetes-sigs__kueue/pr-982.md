# PR #982: Cluster queue resource metrics.

**Summary**: Cluster queue resource metrics.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/982

**Last updated**: 2023-10-25T17:32:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `cq_metrics` → `main`
- **Created**: 2023-07-13T11:42:53Z
- **Updated**: 2023-10-25T17:32:59Z
- **Merged**: 2023-07-19T14:36:06Z
- **Closed**: 2023-07-19T14:36:06Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 19   **+975 / -28**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add optional cluster queue resource quota and  usage metrics.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #972

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add optional cluster queue resource quota and usage metrics.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-13T11:42:55Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-13T11:43:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 59bdc9aa3569b47e3cd288829169a8e24ba14aa2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64b79578293df90008fef222 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-982--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-982--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk4Mi0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.RDkSjKrXsA5s_v2CU4qCX8VUOzHPKEsPi12rIZZfLP8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@trasc](https://github.com/trasc) — 2023-07-13T11:43:17Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-19T14:35:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/982#pullrequestreview-1537255236" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/982#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:32:57Z

/kind feature

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-13T13:42:04Z

- **apis/kueue/v1beta1/clusterqueue_types.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-13T13:42:03Z
```diff
@@ -88,6 +88,11 @@ type ClusterQueueSpec struct {
 	// preempt to accomomdate the pending Workload, preempting Workloads with
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
+
+	// resourceMetrics enables the reporting of resource usage and quotas
+	//
+	// +optional
+	ResourceMetrics *bool `json:"resourceMetrics,omitempty"`
```

  This configuration should be global and live in the `Configuration` object.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-13T13:45:40Z

- **pkg/metrics/metrics.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-13T13:45:39Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
```

  What's the rational for recording this is metrics, if it's something that the users input in the spec?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-13T14:02:41Z

- **apis/kueue/v1beta1/clusterqueue_types.go:95** by [@trasc](https://github.com/trasc) — 2023-07-13T14:02:41Z
```diff
@@ -88,6 +88,11 @@ type ClusterQueueSpec struct {
 	// preempt to accomomdate the pending Workload, preempting Workloads with
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
+
+	// resourceMetrics enables the reporting of resource usage and quotas
+	//
+	// +optional
+	ResourceMetrics *bool `json:"resourceMetrics,omitempty"`
```

  In my opinion this provides better control of the metrics.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-13T14:03:53Z

- **pkg/metrics/metrics.go:137** by [@trasc](https://github.com/trasc) — 2023-07-13T14:03:53Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
```

  The values could be used in dashboards to compute percentage  of  usage for each resource (for instance).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-13T14:12:15Z

- **pkg/metrics/metrics.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-13T14:12:15Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
```

  That could also be done from the borrowing metric.
  Fewer metrics is preferred for lower memory usage.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-13T14:12:48Z

- **apis/kueue/v1beta1/clusterqueue_types.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-13T14:12:47Z
```diff
@@ -88,6 +88,11 @@ type ClusterQueueSpec struct {
 	// preempt to accomomdate the pending Workload, preempting Workloads with
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
+
+	// resourceMetrics enables the reporting of resource usage and quotas
+	//
+	// +optional
+	ResourceMetrics *bool `json:"resourceMetrics,omitempty"`
```

  let's start simple, just one global check.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-13T14:23:49Z

- **site/content/en/docs/reference/metrics.md:33** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-13T14:23:48Z
```diff
@@ -29,3 +29,13 @@ Use the following metrics to monitor the status of your ClusterQueues:
 | `kueue_admission_wait_time_seconds` | Histogram | The time between a Workload was created until it was admitted. | `cluster_queue`: the name of the ClusterQueue |
 | `kueue_admitted_active_workloads` | Gauge | The number of admitted Workloads that are active (unsuspended and not finished) | `cluster_queue`: the name of the ClusterQueue |
 | `kueue_cluster_queue_status` | Gauge | Reports the status of the ClusterQueue | `cluster_queue`: The name of the ClusterQueue<br> `status`: Possible values are `pending`, `active` or `terminated`. For a ClusterQueue, the metric only reports a value of 1 for one of the statuses. |
+
+### Optional metrics
```

  I just created a separate branch `website-release-0.5`

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-14T10:34:30Z

Using the managers config , docs removed to be added in a PR targeting the website branch.

- **pkg/metrics/metrics.go:137** by [@trasc](https://github.com/trasc) — 2023-07-14T10:33:08Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
```

  not 100%, you can extract the nominal quota if the resource has a borrowing limit and the usage is more than that. So you can only say if you are using all the nominal quota or not. I think that being able to know the % of usage out of nominal quota and borrowing is important from an  UX POV.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-14T10:34:50Z

- **apis/kueue/v1beta1/clusterqueue_types.go:95** by [@trasc](https://github.com/trasc) — 2023-07-14T10:34:50Z
```diff
@@ -88,6 +88,11 @@ type ClusterQueueSpec struct {
 	// preempt to accomomdate the pending Workload, preempting Workloads with
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
+
+	// resourceMetrics enables the reporting of resource usage and quotas
+	//
+	// +optional
+	ResourceMetrics *bool `json:"resourceMetrics,omitempty"`
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-14T15:04:33Z

- **apis/config/v1beta1/configuration_types.go:130** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:27:49Z
```diff
@@ -123,6 +123,11 @@ type ControllerMetrics struct {
 	// It can be set to "0" to disable the metrics serving.
 	// +optional
 	BindAddress string `json:"bindAddress,omitempty"`
+
+	// ClusterQueueResourceUsage, if true the cluster queue resource usage and quotas
+	// metrics will be reported.
+	// +optional
+	ClusterQueueResourceUsage bool `json:"clusterQueueResourceUsage,omitempty"`
```

  how about this?
  ```suggestion
  	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
  ```

- **pkg/controller/core/clusterqueue_controller.go:255** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:36:05Z
```diff
@@ -236,6 +250,10 @@ func (r *ClusterQueueReconciler) Update(e event.UpdateEvent) bool {
 	if err := r.qManager.UpdateClusterQueue(context.Background(), newCq); err != nil {
 		log.Error(err, "Failed to update clusterQueue in queue manager")
 	}
+
+	if r.reportResourceMetrics {
```

  If the CQ removes a flavor or resource, we should clear those metrics.

- **pkg/metrics/metrics.go:143** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:38:34Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
+			Help:      `Reports the cluster_queue's resource nominal quota within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceBorrowingQuota = prometheus.NewGaugeVec(
```

  ```suggestion
  	ClusterQueueResourceBorrowingLimit= prometheus.NewGaugeVec(
  ```

- **pkg/metrics/metrics.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:38:45Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
+			Help:      `Reports the cluster_queue's resource nominal quota within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceBorrowingQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_borrowing_quota",
```

  ```suggestion
  			Name:      "cluster_queue_borrowing_limit",
  ```

- **pkg/metrics/metrics.go:209** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:41:37Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
+	lbls := prometheus.Labels{
+		"cluster_queue": queue,
+	}
+	ClusterQueueResourceNominalQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceBorrowingQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceUsage.DeletePartialMatch(lbls)
+}
+
+func CollectGaugeVec(v *prometheus.GaugeVec, labels prometheus.Labels) []prometheus.Metric {
```

  should be part of a testing package.

- **pkg/metrics/metrics.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:42:30Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
+	lbls := prometheus.Labels{
+		"cluster_queue": queue,
+	}
+	ClusterQueueResourceNominalQuota.DeletePartialMatch(lbls)
```

  ohhhh they finally added this, great!

- **pkg/metrics/metrics.go:200** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:42:46Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
```

  ```suggestion
  func ClearClusterQueueResourceMetrics(cq string) {
  ```

- **pkg/metrics/metrics.go:229** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T14:45:41Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
+	lbls := prometheus.Labels{
+		"cluster_queue": queue,
+	}
+	ClusterQueueResourceNominalQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceBorrowingQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceUsage.DeletePartialMatch(lbls)
+}
+
+func CollectGaugeVec(v *prometheus.GaugeVec, labels prometheus.Labels) []prometheus.Metric {
+	ch := make(chan prometheus.Metric)
+	ret := []prometheus.Metric{}
+	wg := &sync.WaitGroup{}
+	wg.Add(2)
+	go func() {
+		for m := range ch {
+			ret = append(ret, m)
+		}
+		wg.Done()
+	}()
+
+	go func() {
+		f, err := v.CurryWith(labels)
+		if err == nil {
+			f.Collect(ch)
+		}
+		close(ch)
+		wg.Done()
+	}()
+	wg.Wait()
```

  ```suggestion
  	go func() {
  		f, err := v.CurryWith(labels)
  		if err == nil {
  			f.Collect(ch)
  		}
  		close(ch)
  	}()
  	for m := range ch {
  			ret = append(ret, m)
  	}
  ```

- **pkg/util/resource/resource.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-14T15:03:44Z
```diff
@@ -95,3 +95,13 @@ func GetGreaterKeys(a, b corev1.ResourceList) []string {
 	}
 	return ret
 }
+
+func QuantityToFloat(q *resource.Quantity) float64 {
```

  add a small unit test for this

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-18T06:20:37Z

- **apis/config/v1beta1/configuration_types.go:130** by [@trasc](https://github.com/trasc) — 2023-07-17T06:02:35Z
```diff
@@ -123,6 +123,11 @@ type ControllerMetrics struct {
 	// It can be set to "0" to disable the metrics serving.
 	// +optional
 	BindAddress string `json:"bindAddress,omitempty"`
+
+	// ClusterQueueResourceUsage, if true the cluster queue resource usage and quotas
+	// metrics will be reported.
+	// +optional
+	ClusterQueueResourceUsage bool `json:"clusterQueueResourceUsage,omitempty"`
```

  done

- **pkg/controller/core/clusterqueue_controller.go:255** by [@trasc](https://github.com/trasc) — 2023-07-17T07:30:30Z
```diff
@@ -236,6 +250,10 @@ func (r *ClusterQueueReconciler) Update(e event.UpdateEvent) bool {
 	if err := r.qManager.UpdateClusterQueue(context.Background(), newCq); err != nil {
 		log.Error(err, "Failed to update clusterQueue in queue manager")
 	}
+
+	if r.reportResourceMetrics {
```

  done

- **pkg/metrics/metrics.go:143** by [@trasc](https://github.com/trasc) — 2023-07-17T07:35:58Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
+			Help:      `Reports the cluster_queue's resource nominal quota within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceBorrowingQuota = prometheus.NewGaugeVec(
```

  done

- **pkg/metrics/metrics.go:146** by [@trasc](https://github.com/trasc) — 2023-07-17T07:36:04Z
```diff
@@ -121,6 +122,31 @@ The label 'result' can have the following values:
 For a ClusterQueue, the metric only reports a value of 1 for one of the statuses.`,
 		}, []string{"cluster_queue", "status"},
 	)
+
+	// Optional cluster queue metrics
+	ClusterQueueResourceUsage = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_resource_usage",
+			Help:      `Reports the cluster_queue's total resource usage within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceNominalQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_nominal_quota",
+			Help:      `Reports the cluster_queue's resource nominal quota within all the flavors`,
+		}, []string{"cohort", "cluster_queue", "flavor", "resource"},
+	)
+
+	ClusterQueueResourceBorrowingQuota = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cluster_queue_borrowing_quota",
```

  done

- **pkg/metrics/metrics.go:200** by [@trasc](https://github.com/trasc) — 2023-07-17T07:39:07Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
```

  done

- **pkg/metrics/metrics.go:229** by [@trasc](https://github.com/trasc) — 2023-07-17T09:50:52Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
+	lbls := prometheus.Labels{
+		"cluster_queue": queue,
+	}
+	ClusterQueueResourceNominalQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceBorrowingQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceUsage.DeletePartialMatch(lbls)
+}
+
+func CollectGaugeVec(v *prometheus.GaugeVec, labels prometheus.Labels) []prometheus.Metric {
+	ch := make(chan prometheus.Metric)
+	ret := []prometheus.Metric{}
+	wg := &sync.WaitGroup{}
+	wg.Add(2)
+	go func() {
+		for m := range ch {
+			ret = append(ret, m)
+		}
+		wg.Done()
+	}()
+
+	go func() {
+		f, err := v.CurryWith(labels)
+		if err == nil {
+			f.Collect(ch)
+		}
+		close(ch)
+		wg.Done()
+	}()
+	wg.Wait()
```

  done

- **pkg/metrics/metrics.go:209** by [@trasc](https://github.com/trasc) — 2023-07-17T09:51:02Z
```diff
@@ -162,6 +188,48 @@ func ClearCacheMetrics(cqName string) {
 	}
 }
 
+func ReportClusterQueueQuotas(cohort, queue, flavor, resource string, nominal, borrowing float64) {
+	ClusterQueueResourceNominalQuota.WithLabelValues(cohort, queue, flavor, resource).Set(nominal)
+	ClusterQueueResourceBorrowingQuota.WithLabelValues(cohort, queue, flavor, resource).Set(borrowing)
+}
+
+func ReportClusterQueueResourceUsage(cohort, queue, flavor, resource string, usage float64) {
+	ClusterQueueResourceUsage.WithLabelValues(cohort, queue, flavor, resource).Set(usage)
+}
+
+func ClearClusterQueueResourceMetrics(queue string) {
+	lbls := prometheus.Labels{
+		"cluster_queue": queue,
+	}
+	ClusterQueueResourceNominalQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceBorrowingQuota.DeletePartialMatch(lbls)
+	ClusterQueueResourceUsage.DeletePartialMatch(lbls)
+}
+
+func CollectGaugeVec(v *prometheus.GaugeVec, labels prometheus.Labels) []prometheus.Metric {
```

  done

- **pkg/util/resource/resource.go:99** by [@trasc](https://github.com/trasc) — 2023-07-17T10:24:47Z
```diff
@@ -95,3 +95,13 @@ func GetGreaterKeys(a, b corev1.ResourceList) []string {
 	}
 	return ret
 }
+
+func QuantityToFloat(q *resource.Quantity) float64 {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-18T18:49:50Z

- **apis/config/v1beta1/configuration_types.go:130** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:09:40Z
```diff
@@ -123,6 +123,11 @@ type ControllerMetrics struct {
 	// It can be set to "0" to disable the metrics serving.
 	// +optional
 	BindAddress string `json:"bindAddress,omitempty"`
+
+	// EnableClusterQueueResources, if true the cluster queue resource usage and quotas
+	// metrics will be reported.
+	// +optional
+	EnableClusterQueueResources bool `json:"enambleClusterQueueResources,omitempty"`
```

  ```suggestion
  	// enableClusterQueueResources, if true the cluster queue resource usage and quotas
  	// metrics will be reported.
  	// +optional
  	EnableClusterQueueResources bool `json:"enableClusterQueueResources,omitempty"`
  ```

- **pkg/controller/core/clusterqueue_controller.go:222** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:13:47Z
```diff
@@ -207,6 +218,10 @@ func (r *ClusterQueueReconciler) Delete(e event.DeleteEvent) bool {
 	r.log.V(2).Info("ClusterQueue delete event", "clusterQueue", klog.KObj(cq))
 	r.cache.DeleteClusterQueue(cq)
 	r.qManager.DeleteClusterQueue(cq)
+
+	r.log.V(2).Info("Cleanup metrics.", "clusterQueue", klog.KObj(cq))
```

  ```suggestion
  	r.log.V(2).Info("Cleared resource metrics for deleted ClusterQueue", "clusterQueue", klog.KObj(cq))
  ```
  Also, move it after the metrics have been updated.

- **pkg/controller/core/clusterqueue_controller.go:266** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:15:40Z
```diff
@@ -244,6 +263,88 @@ func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	return true
 }
 
+func recordResourceMetics(cq *kueue.ClusterQueue) {
```

  ```suggestion
  func recordResourceMetrics(cq *kueue.ClusterQueue) {
  ```

- **pkg/controller/core/clusterqueue_controller.go:290** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:17:33Z
```diff
@@ -244,6 +263,88 @@ func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	return true
 }
 
+func recordResourceMetics(cq *kueue.ClusterQueue) {
+	for rgi := range cq.Spec.ResourceGroups {
+		rg := &cq.Spec.ResourceGroups[rgi]
+		for fqi := range rg.Flavors {
+			fq := &rg.Flavors[fqi]
+			for ri := range fq.Resources {
+				r := &fq.Resources[ri]
+				nominal := resource.QuantityToFloat(&r.NominalQuota)
+				borrow := resource.QuantityToFloat(r.BorrowingLimit)
+				metrics.ReportClusterQueueQuotas(cq.Spec.Cohort, cq.Name, string(fq.Name), string(r.Name), nominal, borrow)
+			}
+		}
+	}
+
+	for fui := range cq.Status.FlavorsUsage {
+		fu := &cq.Status.FlavorsUsage[fui]
+		for ri := range fu.Resources {
+			r := &fu.Resources[ri]
+			metrics.ReportClusterQueueResourceUsage(cq.Spec.Cohort, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total))
+		}
+	}
+}
+
+func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue) {
+	// if the cohort is changed, drop all the old metrics
```

  ```suggestion
  	// if the cohort changed, drop all the old metrics
  ```

- **pkg/util/resource/resource_test.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:38:47Z
```diff
@@ -259,3 +259,44 @@ func TestGetGraterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestContains(t *testing.T) {
```

  ```suggestion
  func TestQuantityToFloat(t *testing.T) {
  ```

- **pkg/util/maps/maps.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:42:46Z
```diff
@@ -57,3 +57,26 @@ func Merge[K comparable, V any, S ~map[K]V](a, b S, resolveConflict func(a, b V)
 func MergeKeepFirst[K comparable, V any, S ~map[K]V](a, b S) S {
 	return Merge(a, b, func(v, _ V) V { return v })
 }
+
+// Contains returns true if a contains all the keys in b with the same value
+func Contains[K, V comparable, A ~map[K]V, B ~map[K]V](a A, b B) bool {
+	for k, bv := range b {
+		if av, found := a[k]; !found || av != bv {
+			return false
+		}
+	}
+	return true
+}
+
+// Keys returns a a slice containing the m keys
+func Keys[K comparable, V any, M ~map[K]V](m M) []K {
```

  Is this being used?

- **pkg/util/slices/slices.go:19** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:43:49Z
```diff
@@ -16,12 +16,12 @@ limitations under the License.
 
 package slices
 
-// ToMap creates a map[K]V out of the provided slice s using key() to create the map keys and
-// val() to create the values.
+// ToMap creates a map[K]V out of the provided slice s using mk() to get the key and value
```

  ```suggestion
  // ToMap creates a map[K]V out of the provided slice s using mf() to get the key and value
  ```

- **pkg/controller/core/clusterqueue_controller_test.go:281** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:47:14Z
```diff
@@ -203,3 +208,273 @@ func TestUpdateCqStatusIfChanged(t *testing.T) {
 		})
 	}
 }
+
+type cqMetrics struct {
+	NominalDPs   []testingmetrics.GaugeDataPoint
+	BorrowingDPs []testingmetrics.GaugeDataPoint
+	UsageDPs     []testingmetrics.GaugeDataPoint
+}
+
+func allMetricsForQueue(name string) cqMetrics {
+	return cqMetrics{
+		NominalDPs:   testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceNominalQuota, map[string]string{"cluster_queue": name}),
+		BorrowingDPs: testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceBorrowingLimit, map[string]string{"cluster_queue": name}),
+		UsageDPs:     testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceUsage, map[string]string{"cluster_queue": name}),
+	}
+}
+
+func testDataPoint(cohort, name, flavor, res string, v float64) testingmetrics.GaugeDataPoint {
+	return testingmetrics.GaugeDataPoint{
+		Labels: map[string]string{
+			"cohort":        cohort,
+			"cluster_queue": name,
+			"flavor":        flavor,
+			"resource":      res,
+		},
+		Value: v,
+	}
+}
+
+func TestRecordResourceMetrics(t *testing.T) {
+	baseQueue := &kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "name",
+		},
+		Spec: kueue.ClusterQueueSpec{
+			Cohort: "cohort",
+			ResourceGroups: []kueue.ResourceGroup{
+				{
+					CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+					Flavors: []kueue.FlavorQuotas{
+						{
+							Name: "flavor",
+							Resources: []kueue.ResourceQuota{
+								{
+									Name:           corev1.ResourceCPU,
+									NominalQuota:   resource.MustParse("1"),
+									BorrowingLimit: pointer.Quantity(resource.MustParse("2")),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
+		Status: kueue.ClusterQueueStatus{
+			FlavorsUsage: []kueue.FlavorUsage{
+				{
+					Name: "flavor",
+					Resources: []kueue.ResourceUsage{
+						{
+							Name:     corev1.ResourceCPU,
+							Total:    resource.MustParse("2"),
+							Borrowed: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+		},
+	}
+
+	testCases := map[string]struct {
+		queue          *kueue.ClusterQueue
+		metrics        cqMetrics
```

  Always prefix with `want` things that we check
  
  ```suggestion
  		wantMetrics        cqMetrics
  ```

- **pkg/controller/core/clusterqueue_controller_test.go:226** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-18T18:48:20Z
```diff
@@ -203,3 +208,273 @@ func TestUpdateCqStatusIfChanged(t *testing.T) {
 		})
 	}
 }
+
+type cqMetrics struct {
+	NominalDPs   []testingmetrics.GaugeDataPoint
+	BorrowingDPs []testingmetrics.GaugeDataPoint
+	UsageDPs     []testingmetrics.GaugeDataPoint
+}
+
+func allMetricsForQueue(name string) cqMetrics {
+	return cqMetrics{
+		NominalDPs:   testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceNominalQuota, map[string]string{"cluster_queue": name}),
+		BorrowingDPs: testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceBorrowingLimit, map[string]string{"cluster_queue": name}),
+		UsageDPs:     testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceUsage, map[string]string{"cluster_queue": name}),
+	}
+}
+
+func testDataPoint(cohort, name, flavor, res string, v float64) testingmetrics.GaugeDataPoint {
```

  ```suggestion
  func resourceDataPoint(cohort, name, flavor, res string, v float64) testingmetrics.GaugeDataPoint {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-19T07:49:51Z

- **apis/config/v1beta1/configuration_types.go:130** by [@trasc](https://github.com/trasc) — 2023-07-19T07:23:27Z
```diff
@@ -123,6 +123,11 @@ type ControllerMetrics struct {
 	// It can be set to "0" to disable the metrics serving.
 	// +optional
 	BindAddress string `json:"bindAddress,omitempty"`
+
+	// EnableClusterQueueResources, if true the cluster queue resource usage and quotas
+	// metrics will be reported.
+	// +optional
+	EnableClusterQueueResources bool `json:"enambleClusterQueueResources,omitempty"`
```

  changed the json tag, the member comments are using the name it the full config

- **pkg/controller/core/clusterqueue_controller.go:222** by [@trasc](https://github.com/trasc) — 2023-07-19T07:25:54Z
```diff
@@ -207,6 +218,10 @@ func (r *ClusterQueueReconciler) Delete(e event.DeleteEvent) bool {
 	r.log.V(2).Info("ClusterQueue delete event", "clusterQueue", klog.KObj(cq))
 	r.cache.DeleteClusterQueue(cq)
 	r.qManager.DeleteClusterQueue(cq)
+
+	r.log.V(2).Info("Cleanup metrics.", "clusterQueue", klog.KObj(cq))
```

  done

- **pkg/controller/core/clusterqueue_controller.go:266** by [@trasc](https://github.com/trasc) — 2023-07-19T07:26:50Z
```diff
@@ -244,6 +263,88 @@ func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	return true
 }
 
+func recordResourceMetics(cq *kueue.ClusterQueue) {
```

  done

- **pkg/controller/core/clusterqueue_controller.go:290** by [@trasc](https://github.com/trasc) — 2023-07-19T07:27:23Z
```diff
@@ -244,6 +263,88 @@ func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	return true
 }
 
+func recordResourceMetics(cq *kueue.ClusterQueue) {
+	for rgi := range cq.Spec.ResourceGroups {
+		rg := &cq.Spec.ResourceGroups[rgi]
+		for fqi := range rg.Flavors {
+			fq := &rg.Flavors[fqi]
+			for ri := range fq.Resources {
+				r := &fq.Resources[ri]
+				nominal := resource.QuantityToFloat(&r.NominalQuota)
+				borrow := resource.QuantityToFloat(r.BorrowingLimit)
+				metrics.ReportClusterQueueQuotas(cq.Spec.Cohort, cq.Name, string(fq.Name), string(r.Name), nominal, borrow)
+			}
+		}
+	}
+
+	for fui := range cq.Status.FlavorsUsage {
+		fu := &cq.Status.FlavorsUsage[fui]
+		for ri := range fu.Resources {
+			r := &fu.Resources[ri]
+			metrics.ReportClusterQueueResourceUsage(cq.Spec.Cohort, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total))
+		}
+	}
+}
+
+func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue) {
+	// if the cohort is changed, drop all the old metrics
```

  done

- **pkg/controller/core/clusterqueue_controller_test.go:226** by [@trasc](https://github.com/trasc) — 2023-07-19T07:28:36Z
```diff
@@ -203,3 +208,273 @@ func TestUpdateCqStatusIfChanged(t *testing.T) {
 		})
 	}
 }
+
+type cqMetrics struct {
+	NominalDPs   []testingmetrics.GaugeDataPoint
+	BorrowingDPs []testingmetrics.GaugeDataPoint
+	UsageDPs     []testingmetrics.GaugeDataPoint
+}
+
+func allMetricsForQueue(name string) cqMetrics {
+	return cqMetrics{
+		NominalDPs:   testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceNominalQuota, map[string]string{"cluster_queue": name}),
+		BorrowingDPs: testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceBorrowingLimit, map[string]string{"cluster_queue": name}),
+		UsageDPs:     testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceUsage, map[string]string{"cluster_queue": name}),
+	}
+}
+
+func testDataPoint(cohort, name, flavor, res string, v float64) testingmetrics.GaugeDataPoint {
```

  done

- **pkg/controller/core/clusterqueue_controller_test.go:281** by [@trasc](https://github.com/trasc) — 2023-07-19T07:29:57Z
```diff
@@ -203,3 +208,273 @@ func TestUpdateCqStatusIfChanged(t *testing.T) {
 		})
 	}
 }
+
+type cqMetrics struct {
+	NominalDPs   []testingmetrics.GaugeDataPoint
+	BorrowingDPs []testingmetrics.GaugeDataPoint
+	UsageDPs     []testingmetrics.GaugeDataPoint
+}
+
+func allMetricsForQueue(name string) cqMetrics {
+	return cqMetrics{
+		NominalDPs:   testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceNominalQuota, map[string]string{"cluster_queue": name}),
+		BorrowingDPs: testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceBorrowingLimit, map[string]string{"cluster_queue": name}),
+		UsageDPs:     testingmetrics.CollectFilteredGaugeVec(metrics.ClusterQueueResourceUsage, map[string]string{"cluster_queue": name}),
+	}
+}
+
+func testDataPoint(cohort, name, flavor, res string, v float64) testingmetrics.GaugeDataPoint {
+	return testingmetrics.GaugeDataPoint{
+		Labels: map[string]string{
+			"cohort":        cohort,
+			"cluster_queue": name,
+			"flavor":        flavor,
+			"resource":      res,
+		},
+		Value: v,
+	}
+}
+
+func TestRecordResourceMetrics(t *testing.T) {
+	baseQueue := &kueue.ClusterQueue{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "name",
+		},
+		Spec: kueue.ClusterQueueSpec{
+			Cohort: "cohort",
+			ResourceGroups: []kueue.ResourceGroup{
+				{
+					CoveredResources: []corev1.ResourceName{corev1.ResourceCPU},
+					Flavors: []kueue.FlavorQuotas{
+						{
+							Name: "flavor",
+							Resources: []kueue.ResourceQuota{
+								{
+									Name:           corev1.ResourceCPU,
+									NominalQuota:   resource.MustParse("1"),
+									BorrowingLimit: pointer.Quantity(resource.MustParse("2")),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
+		Status: kueue.ClusterQueueStatus{
+			FlavorsUsage: []kueue.FlavorUsage{
+				{
+					Name: "flavor",
+					Resources: []kueue.ResourceUsage{
+						{
+							Name:     corev1.ResourceCPU,
+							Total:    resource.MustParse("2"),
+							Borrowed: resource.MustParse("1"),
+						},
+					},
+				},
+			},
+		},
+	}
+
+	testCases := map[string]struct {
+		queue          *kueue.ClusterQueue
+		metrics        cqMetrics
```

  done

- **pkg/util/maps/maps.go:72** by [@trasc](https://github.com/trasc) — 2023-07-19T07:32:15Z
```diff
@@ -57,3 +57,26 @@ func Merge[K comparable, V any, S ~map[K]V](a, b S, resolveConflict func(a, b V)
 func MergeKeepFirst[K comparable, V any, S ~map[K]V](a, b S) S {
 	return Merge(a, b, func(v, _ V) V { return v })
 }
+
+// Contains returns true if a contains all the keys in b with the same value
+func Contains[K, V comparable, A ~map[K]V, B ~map[K]V](a A, b B) bool {
+	for k, bv := range b {
+		if av, found := a[k]; !found || av != bv {
+			return false
+		}
+	}
+	return true
+}
+
+// Keys returns a a slice containing the m keys
+func Keys[K comparable, V any, M ~map[K]V](m M) []K {
```

  yes, currently only in testing metrics test , but it could be useful in the future

- **pkg/util/resource/resource_test.go:263** by [@trasc](https://github.com/trasc) — 2023-07-19T07:33:59Z
```diff
@@ -259,3 +259,44 @@ func TestGetGraterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestContains(t *testing.T) {
```

  done

- **pkg/util/slices/slices.go:19** by [@trasc](https://github.com/trasc) — 2023-07-19T07:35:49Z
```diff
@@ -16,12 +16,12 @@ limitations under the License.
 
 package slices
 
-// ToMap creates a map[K]V out of the provided slice s using key() to create the map keys and
-// val() to create the values.
+// ToMap creates a map[K]V out of the provided slice s using mk() to get the key and value
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-19T14:35:36Z

/lgtm
/approve
