# PR #9833: Provide a metric for cohort resource reservations

**Summary**: Provide a metric for cohort resource reservations

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9833

**Last updated**: 2026-03-24T03:42:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/7539-cohort-metrics-usage` → `main`
- **Created**: 2026-03-12T16:11:43Z
- **Updated**: 2026-03-24T03:42:13Z
- **Merged**: 2026-03-23T19:10:17Z
- **Closed**: 2026-03-23T19:10:17Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@vladikkuzn](https://github.com/vladikkuzn), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 9   **+756 / -43**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Add metrics for cohort's resource reservations.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #7539

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability:  Introduce the cohort resource reservations metric.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-12T16:12:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 87267c811fe30fbc6c06ff1d497fa92dc4824382 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c188d5a6c077000867ae8a |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T17:48:02Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T17:48:12Z

LGTM label has been added.  <details>Git tree hash: 63f91d381ca482809071ea08ba34d1800d818d03</details>

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-18T08:29:05Z

@pajakd ptal

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T18:08:47Z

@mbobrovskyi please check again

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T15:19:24Z

> @mszadkow: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:
> 
> Test name	Commit	Details	Required	Rerun command
> pull-kueue-test-integration-baseline-main	[817a364](https://github.com/kubernetes-sigs/kueue/commit/817a364cdaea5e14aba31e1fd4b8d9c1a6fd7e5f)	[link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9833/pull-kueue-test-integration-baseline-main/2036094216022528000)	true	`/test pull-kueue-test-integration-baseline-main`
> [Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=9833). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Amszadkow). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.
> 
> Details

Known flake: https://github.com/kubernetes-sigs/kueue/issues/10057

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T18:24:50Z

@mbobrovskyi @mimowo ptal
I think I resolved all the comments for now

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T19:08:42Z

LGTM label has been added.  <details>Git tree hash: f1ed1f908efff284dd9553606ae5d2eda5f6d309</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T19:08:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9833#pullrequestreview-3994021532" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9833#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-16T06:51:33Z

- **pkg/cache/scheduler/cache.go:136** by [@pajakd](https://github.com/pajakd) — 2026-03-16T06:51:34Z
```diff
@@ -133,6 +133,20 @@ type Cache struct {
 	roleTracker *roletracker.RoleTracker
 }
 
+func (c *Cache) GetCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
```

  perhaps this should be called `GetDirectCohortNameForWorkload`, to make it clear what does it return for multi-level hierarchies?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-16T07:37:11Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:455** by [@pajakd](https://github.com/pajakd) — 2026-03-16T07:37:11Z
```diff
@@ -425,5 +443,187 @@ var _ = ginkgo.Describe("Cohorts", func() {
 				util.ExpectCohortSubtreeQuotaGaugeMetric("ch1", flavor1.Name, "nvidia.com/gpu", 1)
 			})
 		})
+
+		ginkgo.It("reports CohortSubtreeResourceUsage across child-parent propagation, reduced spill, release, and topology recompute", func() {
+			var (
+				wlCh1a *kueue.Workload
+				wlCh1b *kueue.Workload
+				wlCh1c *kueue.Workload
+				wlCh2  *kueue.Workload
+			)
+
+			ginkgo.By("Create root and children cohorts with explicit quotas/lending limits to reduce spillage", func() {
```

  I'm not familiar with term "spill/spillage". In resource_node.go https://github.com/kubernetes-sigs/kueue/blob/baf8a94160fe65077c17a1387c57055c6309ac01/pkg/cache/scheduler/resource_node.go#L95-L99 the capacity is "stored" at the parent. Is "spill/spillage" only used here? Please verify.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-16T08:21:47Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:479** by [@pajakd](https://github.com/pajakd) — 2026-03-16T08:21:47Z
```diff
@@ -425,5 +443,187 @@ var _ = ginkgo.Describe("Cohorts", func() {
 				util.ExpectCohortSubtreeQuotaGaugeMetric("ch1", flavor1.Name, "nvidia.com/gpu", 1)
 			})
 		})
+
+		ginkgo.It("reports CohortSubtreeResourceUsage across child-parent propagation, reduced spill, release, and topology recompute", func() {
+			var (
+				wlCh1a *kueue.Workload
+				wlCh1b *kueue.Workload
+				wlCh1c *kueue.Workload
+				wlCh2  *kueue.Workload
+			)
+
+			ginkgo.By("Create root and children cohorts with explicit quotas/lending limits to reduce spillage", func() {
+				createCohort(utiltestingapi.MakeCohort("root").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+							Resource(corev1.ResourceCPU, "30").
+							Obj(),
+					).Obj())
+
+				// ch1 local quota before spilling to parent is 8-3=5 CPU
+				createCohort(utiltestingapi.MakeCohort("ch1").
+					Parent("root").
+					ResourceGroup(
+						withLending(kueue.ResourceFlavorReference(defaultFlavor.Name), "8", "3"),
+					).Obj())
+
+				// ch2 local quota before spilling to parent is 6-2=4 CPU.
+				createCohort(utiltestingapi.MakeCohort("ch2").
+					Parent("root").
+					ResourceGroup(
+						withLending(kueue.ResourceFlavorReference(defaultFlavor.Name), "6", "2"),
+					).Obj())
+
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("ch1", defaultFlavor.Name, corev1.ResourceCPU.String())
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("ch2", defaultFlavor.Name, corev1.ResourceCPU.String())
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("root", defaultFlavor.Name, corev1.ResourceCPU.String())
```

  we don't check `SubtreeQuotaGaugeMetric` -- is it intentional?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-16T08:48:41Z

- **pkg/cache/scheduler/cohort_metrics.go:82** by [@pajakd](https://github.com/pajakd) — 2026-03-16T08:48:41Z
```diff
@@ -73,21 +74,38 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+	removedUsage := ch.resourceNode.Usage
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
 		quotas := ancestor.resourceNode.SubtreeQuota
+		usages := ancestor.resourceNode.Usage
```

  Perhaps a dumb question but `resourceNode.Usage` is the usage "visible" in this node (exceeding the nominal quotas, restricted by lending limits). The objective of this metric isn't to track the total usage? Looking at this comment https://github.com/kubernetes-sigs/kueue/issues/7539#issuecomment-3819010176 it seems like we want to surface the usage including the nominalQuota consumption (which might not be included in resourceNode.Usage).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-16T11:10:33Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:455** by [@mszadkow](https://github.com/mszadkow) — 2026-03-16T11:10:33Z
```diff
@@ -425,5 +443,187 @@ var _ = ginkgo.Describe("Cohorts", func() {
 				util.ExpectCohortSubtreeQuotaGaugeMetric("ch1", flavor1.Name, "nvidia.com/gpu", 1)
 			})
 		})
+
+		ginkgo.It("reports CohortSubtreeResourceUsage across child-parent propagation, reduced spill, release, and topology recompute", func() {
+			var (
+				wlCh1a *kueue.Workload
+				wlCh1b *kueue.Workload
+				wlCh1c *kueue.Workload
+				wlCh2  *kueue.Workload
+			)
+
+			ginkgo.By("Create root and children cohorts with explicit quotas/lending limits to reduce spillage", func() {
```

  Yes, it's the same, I might accidentally introduce the new word (it was more convincing metaphor to me), I will make sure it's "storing" or "using".

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-16T11:15:05Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:479** by [@mszadkow](https://github.com/mszadkow) — 2026-03-16T11:15:04Z
```diff
@@ -425,5 +443,187 @@ var _ = ginkgo.Describe("Cohorts", func() {
 				util.ExpectCohortSubtreeQuotaGaugeMetric("ch1", flavor1.Name, "nvidia.com/gpu", 1)
 			})
 		})
+
+		ginkgo.It("reports CohortSubtreeResourceUsage across child-parent propagation, reduced spill, release, and topology recompute", func() {
+			var (
+				wlCh1a *kueue.Workload
+				wlCh1b *kueue.Workload
+				wlCh1c *kueue.Workload
+				wlCh2  *kueue.Workload
+			)
+
+			ginkgo.By("Create root and children cohorts with explicit quotas/lending limits to reduce spillage", func() {
+				createCohort(utiltestingapi.MakeCohort("root").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+							Resource(corev1.ResourceCPU, "30").
+							Obj(),
+					).Obj())
+
+				// ch1 local quota before spilling to parent is 8-3=5 CPU
+				createCohort(utiltestingapi.MakeCohort("ch1").
+					Parent("root").
+					ResourceGroup(
+						withLending(kueue.ResourceFlavorReference(defaultFlavor.Name), "8", "3"),
+					).Obj())
+
+				// ch2 local quota before spilling to parent is 6-2=4 CPU.
+				createCohort(utiltestingapi.MakeCohort("ch2").
+					Parent("root").
+					ResourceGroup(
+						withLending(kueue.ResourceFlavorReference(defaultFlavor.Name), "6", "2"),
+					).Obj())
+
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("ch1", defaultFlavor.Name, corev1.ResourceCPU.String())
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("ch2", defaultFlavor.Name, corev1.ResourceCPU.String())
+				util.ExpectCohortSubtreeResourceUsageGaugeMetricCleaned("root", defaultFlavor.Name, corev1.ResourceCPU.String())
```

  Yes, we check it once, when all the objects are present.
  This usage check could also land one step below, maybe it has more sense to keep it in one step to check initial state.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-16T13:21:58Z

- **pkg/cache/scheduler/cohort_metrics.go:82** by [@mszadkow](https://github.com/mszadkow) — 2026-03-16T13:21:58Z
```diff
@@ -73,21 +74,38 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+	removedUsage := ch.resourceNode.Usage
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
 		quotas := ancestor.resourceNode.SubtreeQuota
+		usages := ancestor.resourceNode.Usage
```

  No dumb questions.
  I think you are correct, I have missed that, due to focusing on nominal quota first.
  Thanks!
  I will correct that

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-17T17:45:33Z

- **pkg/cache/scheduler/cache_test.go:3757** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T17:45:33Z
```diff
@@ -3753,3 +3753,64 @@ func TestGetWorkloadFromCache(t *testing.T) {
 		})
 	}
 }
+
+func TestGetCohortNameForWorkload(t *testing.T) {
```

  NIT:
  ```suggestion
  func TestGetDirectCohortNameForWorkload(t *testing.T) {
  ```
  And update the error message:
  
  ```
  			if diff := cmp.Diff(tc.wantCohort, got); diff != "" {
  				t.Errorf("GetDirectCohortNameForWorkload() mismatch (-want,+got):\n%s", diff)
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-03-17T17:47:46Z

- **pkg/metrics/metrics_test.go:310** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-17T17:47:46Z
```diff
@@ -298,9 +298,25 @@ func TestCohortMetrics(t *testing.T) {
 	ReportCohortSubtreeQuota("cohort_two", "flavor", "res", 5, leaderTracker)
 	expectFilteredMetricsCount(t, CohortSubtreeQuota, 1, "cohort", "cohort_two", "replica_role", "leader")
 
+	ReportCohortSubtreeResourceUsage("cohort", "flavor", "res", 5, leaderTracker)
+	expectFilteredMetricsCount(t, CohortSubtreeResourceUsage, 1, "cohort", "cohort", "replica_role", "leader")
+
+	ReportCohortSubtreeResourceUsage("cohort", "flavor", "res", 3, followerTracker)
+	expectFilteredMetricsCount(t, CohortSubtreeResourceUsage, 1, "cohort", "cohort", "replica_role", "follower")
+
+	ReportCohortSubtreeResourceUsage("cohort_two", "flavor", "res", 3, leaderTracker)
+	expectFilteredMetricsCount(t, CohortSubtreeResourceUsage, 1, "cohort", "cohort_two", "replica_role", "leader")
+
+	ClearCohortSubtreeResourceUsage("cohort", "", "")
```

  What about cohort?
  NIT:
  ```suggestion
  	ClearCohortSubtreeResourceUsage("cohort", "", "")
  	expectFilteredMetricsCount(t, CohortSubtreeResourceUsage, 0, "cohort", "cohort")
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-18T11:49:44Z

- **pkg/cache/scheduler/cache_test.go:3758** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-18T11:49:44Z
```diff
@@ -3753,3 +3753,64 @@ func TestGetWorkloadFromCache(t *testing.T) {
 		})
 	}
 }
+
+func TestGetDirectCohortNameForWorkload(t *testing.T) {
+	ctx, _ := utiltesting.ContextWithLog(t)
```

  Please move it inside t.Run().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-18T11:57:44Z

- **pkg/controller/core/workload_controller.go:1460** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-18T11:57:44Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
```

  Can we move this inside AddOrUpdateWorkload()?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-18T11:58:19Z

- **pkg/controller/core/workload_controller.go:1473** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-18T11:58:19Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	if cohortName == "" {
+		cohortName = r.cache.GetDirectCohortNameForWorkload(oldWl)
+	}
+	err := r.cache.DeleteWorkload(log, wlKey)
+	r.cache.RecordCohortMetrics(log, cohortName)
```

  And this inside r.cache.DeleteWorkload()?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-18T12:00:15Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:125** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-18T12:00:16Z
```diff
@@ -106,8 +122,8 @@ var _ = ginkgo.Describe("Cohorts", func() {
 		})
 
 		ginkgo.It("follows reporting correct metrics", func() {
-			ginkgo.By("Creating initial ClusterQueue cqa, with no cohort", func() {
-				createQueue(utiltestingapi.MakeClusterQueue("cqa").
+			ginkgo.By("Creating initial ClusterQueue cq1, with no cohort", func() {
```

  Why this renaming?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-18T12:51:40Z

- **pkg/controller/core/workload_controller.go:1460** by [@mszadkow](https://github.com/mszadkow) — 2026-03-18T12:51:40Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
```

  We could, the thing that stopped me from doing this was to be consistent on which "level" we record the metrics.
  I wanted it to be on the controller level, it was achieved by adding this wrapper.
  I think it's a matter of preference

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-18T12:56:39Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:125** by [@mszadkow](https://github.com/mszadkow) — 2026-03-18T12:56:39Z
```diff
@@ -106,8 +122,8 @@ var _ = ginkgo.Describe("Cohorts", func() {
 		})
 
 		ginkgo.It("follows reporting correct metrics", func() {
-			ginkgo.By("Creating initial ClusterQueue cqa, with no cohort", func() {
-				createQueue(utiltestingapi.MakeClusterQueue("cqa").
+			ginkgo.By("Creating initial ClusterQueue cq1, with no cohort", func() {
```

  reverted

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-18T12:59:32Z

- **pkg/controller/core/workload_controller.go:1460** by [@mszadkow](https://github.com/mszadkow) — 2026-03-18T12:59:32Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
```

  I mean it's this type of metrics, the usage, because it is recorded with the same compute function -  RecordCohortMetrics
  But sure it can be done the other way

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T14:11:22Z

- **pkg/controller/core/workload_controller.go:1460** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T14:11:22Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
```

  Also another point we avoid double locking.
  One lock in the `AddOrUpdateWorkload` and the other in the `RecordCohortMetrics -> collectCohortMetricPoints`.
  
  But obviously this also can be overcome.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:09:19Z

- **pkg/cache/scheduler/cache.go:144** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:09:19Z
```diff
@@ -141,6 +141,20 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
```

  ```suggestion
  func (c *Cache) CohortForWorkload(wl *kueue.Workload) kueue.CohortReference {
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:09:49Z

- **pkg/cache/scheduler/cache.go:151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:09:49Z
```diff
@@ -141,6 +141,20 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
+	if wl == nil || wl.Status.Admission == nil {
+		return ""
+	}
+	c.RLock()
+	defer c.RUnlock()
+	cqName := wl.Status.Admission.ClusterQueue
+	cq := c.hm.ClusterQueue(cqName)
```

  ```suggestion
  	cq := c.hm.ClusterQueue(cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue))
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:18:10Z

- **pkg/cache/scheduler/cohort_metrics.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:18:10Z
```diff
@@ -99,19 +130,38 @@ func (c *Cache) withCohortLogger(log logr.Logger, cohortName kueue.CohortReferen
 }
 
 func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
-	if p.qty <= 0 {
-		metrics.ClearCohortSubtreeQuota(
-			p.cohortName,
-			string(p.flavorResource.Flavor),
-			string(p.flavorResource.Resource),
-		)
-		return
+	flavor := string(p.flavorResource.Flavor)
+	resource := string(p.flavorResource.Resource)
```

  I’m wondering if we can send it as is and convert it to a string inside the report function.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T16:19:37Z

- **pkg/cache/scheduler/cohort_metrics.go:134** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T16:19:37Z
```diff
@@ -99,19 +130,38 @@ func (c *Cache) withCohortLogger(log logr.Logger, cohortName kueue.CohortReferen
 }
 
 func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
-	if p.qty <= 0 {
-		metrics.ClearCohortSubtreeQuota(
-			p.cohortName,
-			string(p.flavorResource.Flavor),
-			string(p.flavorResource.Resource),
-		)
-		return
+	flavor := string(p.flavorResource.Flavor)
+	resource := string(p.flavorResource.Resource)
```

  Yes, I have this thing done already in the other PR: https://github.com/kubernetes-sigs/kueue/pull/9347/changes#diff-4bc91a74bc47b8467cdcd25a7e3fe1651dbe44907f478377eca31b982f444f23
  I will apply it here aswell

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:22:52Z

- **pkg/cache/scheduler/cohort_metrics.go:156** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:22:52Z
```diff
@@ -99,19 +130,38 @@ func (c *Cache) withCohortLogger(log logr.Logger, cohortName kueue.CohortReferen
 }
 
 func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
-	if p.qty <= 0 {
-		metrics.ClearCohortSubtreeQuota(
-			p.cohortName,
-			string(p.flavorResource.Flavor),
-			string(p.flavorResource.Resource),
-		)
-		return
+	flavor := string(p.flavorResource.Flavor)
+	resource := string(p.flavorResource.Resource)
+
+	if p.quotaQty <= 0 {
+		metrics.ClearCohortSubtreeQuota(p.cohortName, flavor, resource)
+	} else {
+		metrics.ReportCohortSubtreeQuota(p.cohortName, flavor, resource, float64(p.quotaQty), c.roleTracker)
 	}
-	metrics.ReportCohortSubtreeQuota(
-		p.cohortName,
-		string(p.flavorResource.Flavor),
-		string(p.flavorResource.Resource),
-		float64(p.qty),
-		c.roleTracker,
-	)
+
+	if p.usageQty <= 0 {
+		metrics.ClearCohortSubtreeResourceUsage(p.cohortName, flavor, resource)
+	} else {
+		metrics.ReportCohortSubtreeResourceUsage(p.cohortName, flavor, resource, float64(p.usageQty), c.roleTracker)
+	}
+}
+
+// totalSubtreeUsage returns the sum of *actual* resource usage in the subtree rooted at given cohort
+func totalSubtreeUsage(ch *cohort) resources.FlavorResourceQuantities {
+	total := make(resources.FlavorResourceQuantities)
+
+	for _, cq := range ch.ChildCQs() {
+		for fr, qty := range cq.getResourceNode().Usage {
+			total[fr] += qty
+		}
```

  Looks like duplication, right? Can we make a helper?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T16:28:29Z

- **pkg/cache/scheduler/cache.go:144** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T16:28:29Z
```diff
@@ -141,6 +141,20 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
```

  I think that this name reflects the intent, also mentioned here: https://github.com/kubernetes-sigs/kueue/pull/9833#discussion_r2938482239

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:42:23Z

- **pkg/cache/scheduler/cohort_metrics.go:160** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:42:23Z
```diff
@@ -99,19 +130,38 @@ func (c *Cache) withCohortLogger(log logr.Logger, cohortName kueue.CohortReferen
 }
 
 func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
-	if p.qty <= 0 {
-		metrics.ClearCohortSubtreeQuota(
-			p.cohortName,
-			string(p.flavorResource.Flavor),
-			string(p.flavorResource.Resource),
-		)
-		return
+	flavor := string(p.flavorResource.Flavor)
+	resource := string(p.flavorResource.Resource)
+
+	if p.quotaQty <= 0 {
+		metrics.ClearCohortSubtreeQuota(p.cohortName, flavor, resource)
+	} else {
+		metrics.ReportCohortSubtreeQuota(p.cohortName, flavor, resource, float64(p.quotaQty), c.roleTracker)
 	}
-	metrics.ReportCohortSubtreeQuota(
-		p.cohortName,
-		string(p.flavorResource.Flavor),
-		string(p.flavorResource.Resource),
-		float64(p.qty),
-		c.roleTracker,
-	)
+
+	if p.usageQty <= 0 {
+		metrics.ClearCohortSubtreeResourceUsage(p.cohortName, flavor, resource)
+	} else {
+		metrics.ReportCohortSubtreeResourceUsage(p.cohortName, flavor, resource, float64(p.usageQty), c.roleTracker)
+	}
+}
+
+// totalSubtreeUsage returns the sum of *actual* resource usage in the subtree rooted at given cohort
+func totalSubtreeUsage(ch *cohort) resources.FlavorResourceQuantities {
+	total := make(resources.FlavorResourceQuantities)
+
+	for _, cq := range ch.ChildCQs() {
+		for fr, qty := range cq.getResourceNode().Usage {
+			total[fr] += qty
+		}
+	}
+
+	for _, child := range ch.ChildCohorts() {
+		childTotal := totalSubtreeUsage(child)
```

  I’m wondering if we can convert it to an iterative approach.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:47:35Z

- **pkg/cache/scheduler/cohort_metrics.go:97** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:46:58Z
```diff
@@ -73,21 +74,46 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache total subtree usage calls during this run
+	usageCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	getTotalUsage := func(co *cohort) resources.FlavorResourceQuantities {
+		return totalSubtreeUsageWithCache(co, usageCache)
+	}
+	removedUsage := getTotalUsage(ch)
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
 		quotas := ancestor.resourceNode.SubtreeQuota
+		usages := getTotalUsage(ancestor)
+
+		var baselineUsage resources.FlavorResourceQuantities
 		if simulateRemoval {
 			quotas = removedQuota
+			usages = removedUsage
+			baselineUsage = getTotalUsage(ancestor)
 		}
 
-		for flr, qty := range quotas {
+		keys := make(map[resources.FlavorResource]struct{}, len(quotas)+len(usages))
```

  Maybe use set?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T16:52:09Z

- **pkg/cache/scheduler/cohort_metrics.go:97** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T16:52:09Z
```diff
@@ -73,21 +74,46 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache total subtree usage calls during this run
+	usageCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	getTotalUsage := func(co *cohort) resources.FlavorResourceQuantities {
+		return totalSubtreeUsageWithCache(co, usageCache)
+	}
+	removedUsage := getTotalUsage(ch)
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
 		quotas := ancestor.resourceNode.SubtreeQuota
+		usages := getTotalUsage(ancestor)
+
+		var baselineUsage resources.FlavorResourceQuantities
 		if simulateRemoval {
 			quotas = removedQuota
+			usages = removedUsage
+			baselineUsage = getTotalUsage(ancestor)
 		}
 
-		for flr, qty := range quotas {
+		keys := make(map[resources.FlavorResource]struct{}, len(quotas)+len(usages))
```

  ```suggestion
  		keys := sets.New[resources.FlavorResource]()
  		keys.Insert(slices.Collect(maps.Keys(quotas))...)
  		keys.Insert(slices.Collect(maps.Keys(usages))...)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T17:06:10Z

- **pkg/util/metrics/metrics.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T17:06:10Z
  Actually, maybe we can keep this helper in pkg/metrics? It looks like it is used only in this package, so there is no need to expose it.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T17:07:32Z

- **pkg/util/metrics/metrics.go:1** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T17:07:32Z
  Yes, we can do that too

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T17:09:43Z

- **pkg/controller/core/workload_controller.go:1467** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T17:09:43Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
```

  Can we propagate newWl? In that case we don't need to get it from cache.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-19T17:18:03Z

- **pkg/controller/core/workload_controller.go:1470** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T17:18:03Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	if cohortName == "" {
+		cohortName = r.cache.GetDirectCohortNameForWorkload(oldWl)
```

  Is it possible this case?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T17:51:48Z

- **pkg/controller/core/workload_controller.go:1470** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T17:51:48Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	if cohortName == "" {
+		cohortName = r.cache.GetDirectCohortNameForWorkload(oldWl)
```

  you mean that the cohort name couldn't be retrieved from the "wlKey" workload but from the "oldWl"?
  it should not be possible, correct

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T17:55:32Z

- **pkg/controller/core/workload_controller.go:1470** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T17:55:32Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	if cohortName == "" {
+		cohortName = r.cache.GetDirectCohortNameForWorkload(oldWl)
```

  no need to pass OldWl or repeat `GetDirectCohortNameForWorkload`, because it's the same wl

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T18:01:02Z

- **pkg/controller/core/workload_controller.go:1467** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T18:01:02Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
```

  It make sense to pass only WlKey and get wl from the cache here.
  
  if wl is not found in the cache, wl will be nil, then cohort will be empty,
  then no metric will be recorded 
  also `r.cache.DeleteWorkload` handles situations when no wl of wlKey is found

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T07:37:34Z

- **pkg/cache/scheduler/cohort_metrics.go:86** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T07:37:34Z
```diff
@@ -73,21 +78,42 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache total subtree usage calls during this run
+	usageCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	getTotalUsage := func(co *cohort) resources.FlavorResourceQuantities {
+		return totalSubtreeUsageWithCache(co, usageCache)
+	}
```

  Do we need this function? It looks like we can use totalSubtreeUsageWithCache(co, usageCache).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T07:41:03Z

- **pkg/controller/core/workload_controller.go:603** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T07:41:03Z
```diff
@@ -599,15 +599,16 @@ func (r *WorkloadReconciler) deleteWorkloadFromCaches(ctx context.Context, names
 
 	// If workload cached by scheduler points to a different queue:
 	// notify watchers to ensure all identified queues remain up-to-date.
-	if wlInSchedulerCache := r.cache.GetWorkloadFromCache(wlRef); workload.GetLocalQueue(wlInSchedulerCache) != workload.GetLocalQueue(wlInQueuesCache) {
+	wlInSchedulerCache := r.cache.GetWorkloadFromCache(wlRef)
+	if workload.GetLocalQueue(wlInSchedulerCache) != workload.GetLocalQueue(wlInQueuesCache) {
```

  Please revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T07:44:14Z

- **pkg/controller/core/workload_controller.go:1470** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T07:44:14Z
```diff
@@ -1452,3 +1453,20 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	err := r.cache.DeleteWorkload(log, wlKey)
+	r.cache.RecordCohortMetrics(log, cohortName)
```

  Do we need to record on error?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T07:53:37Z

- **pkg/controller/core/workload_controller.go:1467** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T07:53:37Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
```

  OK. Can we propagate wlKey inside GetDirectCohortNameForWorkload() then?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T07:55:22Z

- **pkg/controller/core/workload_controller.go:1467** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T07:55:22Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
```

  In that case we don't need to double lock/unlock cache.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T07:56:34Z

- **pkg/controller/core/workload_controller.go:1470** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T07:56:34Z
```diff
@@ -1452,3 +1453,20 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
+	err := r.cache.DeleteWorkload(log, wlKey)
+	r.cache.RecordCohortMetrics(log, cohortName)
```

  Yes, correct, we shouldn't confuse user if cq can not be found and wl deleted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:02:29Z

- **pkg/metrics/metrics.go:741** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:02:29Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  Is this usage for QuotaReserved worklaods, or Admitted? Let's clarify please also double check which usage is counted by the analogous metrics for CLusterQueue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:03:25Z

- **pkg/metrics/metrics.go:741** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:03:25Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  I see this is also not clarified in Help for `cluster_queue_resource_usage`, so please investigate by code and align. Please also make it clear in the description.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:06:48Z

- **pkg/cache/scheduler/cache.go:147** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:06:48Z
```diff
@@ -141,6 +141,19 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
+	if wl == nil || wl.Status.Admission == nil {
+		return ""
+	}
```

  This check makes the function confusing, because it only restricts the workload set to those with reservation (admission), the function name does not reflect that at all. 
  
  So, we need to either:
  1. make the function generic by lookup via LocalQueue -> ClusterQueue-> Cohort, I thought we already have a helper LocalQueue -> ClusterQueue
  2. reflect in the name that it only works for Worklaods with amission
  
  I would probably be leaning towards (1.), iirc we already have such helpers in cache, but let me know if I'm missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:08:26Z

- **pkg/cache/scheduler/cohort_metrics.go:87** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:08:27Z
```diff
@@ -73,21 +78,42 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache total subtree usage calls during this run
+	usageCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	getTotalUsage := func(co *cohort) resources.FlavorResourceQuantities {
+		return totalSubtreeUsageWithCache(co, usageCache)
+	}
+	removedUsage := getTotalUsage(ch)
```

  What is removedUsage? I'm very confused by the variable name computed by `getTotalUsage`. Please explain what it is doing, let's add some comments to the functions and variables, because they are named very counter-intuitive imo - I cannot deduce by anemes what is going on.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:09:38Z

- **pkg/controller/core/workload_controller.go:1468** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:09:38Z
```diff
@@ -1452,3 +1453,20 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
```

  Are we sure wl cannot be nil at this point?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T08:26:14Z

- **pkg/metrics/metrics.go:741** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T08:26:14Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  Well, yes there is a difference here.
  ```
  CohortSubtreeResourceUsage -> reservation-side aggregate (not admitted-only).
  ClusterQueueResourceReservations -> reservation-side.
  ClusterQueueResourceUsage -> admitted-only.
  ```
  That means we need some correction here.
  Let me add `kueue_cohort_resource_reservation` already here and I will do this:
  ```
  CohortSubtreeResourceUsage -> aggregation of admitted-only childCQs.
  CohortSubtreeResourceReservation -> reservation-side aggregate (not admitted-only).
  ClusterQueueResourceReservations -> reservation-side.
  ClusterQueueResourceUsage -> admitted-only.
  ```
  And yes it will be reflected in the metrics' help.
  
  wdyt?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T08:29:33Z

- **pkg/controller/core/workload_controller.go:1468** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T08:29:33Z
```diff
@@ -1452,3 +1453,20 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
+	cohortName := r.cache.GetDirectCohortNameForWorkload(wl)
```

  This goes like this:
  if wl is nil, there will be empty cohortName.
  for nil wl delete won't happen and for empty cohortName metrics recording won't happen.
  This is ensured as first step in those functions, so I didn't want to repeat it here.
  I hope it's ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:32:21Z

- **pkg/metrics/metrics.go:741** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:32:21Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  This sgtm, let's align the metrics, but TBH I don't like ClusterQueueResourceUsage and CohortSubtreeResourceUsage to be admitted-only. 
  
  I think it would be better to have `ClusterQueueResourceAdmitted` and `ClusterQueueResourceReservations`. Unfortunately we already have the confusing "Usage". 
  
  I propose to revisit this later, for now I would just suggest to introduce the most helpful  `ClusterQueueResourceReservations`  as in `ClusterQueueResourceReservations`
  
  Let me revisit the topic of `ClusterQueueResourceUsage` on another PR (less priority IMO).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T08:58:49Z

- **pkg/metrics/metrics.go:741** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T08:58:48Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  Great, thanks for explaining!

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T09:15:51Z

- **pkg/controller/core/workload_controller.go:1467** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T09:15:51Z
```diff
@@ -1452,3 +1453,23 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(wl))
+	}
+
+	return updated
+}
+
+func (r *WorkloadReconciler) deleteWorkload(log logr.Logger, wlKey workload.Reference, oldWl *kueue.Workload) error {
+	wl := r.cache.GetWorkloadFromCache(wlKey)
```

  can be achieved, yes
  anyway inside GetDirectCohortNameForWorkload we will have to call `GetWorkloadFromCache`, but thar's ok

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T10:35:26Z

- **pkg/metrics/metrics.go:741** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T10:35:25Z
```diff
@@ -728,6 +733,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeResourceUsage = trackGaugeVec(prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_resource_usage",
+			Help:      `Reports the cohort's resource usage aggregated within the cohort's subtree. The values are reported per resource and flavor`,
```

  Done, the metric is now called `CohortSubtreeResourceReservations` and description updated

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T10:35:42Z

- **pkg/cache/scheduler/cache.go:147** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T10:35:42Z
```diff
@@ -141,6 +141,19 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wl *kueue.Workload) kueue.CohortReference {
+	if wl == nil || wl.Status.Admission == nil {
+		return ""
+	}
```

  Updated

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T10:35:52Z

- **pkg/cache/scheduler/cohort_metrics.go:87** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T10:35:52Z
```diff
@@ -73,21 +78,42 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 	}
 
 	removedQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache total subtree usage calls during this run
+	usageCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	getTotalUsage := func(co *cohort) resources.FlavorResourceQuantities {
+		return totalSubtreeUsageWithCache(co, usageCache)
+	}
+	removedUsage := getTotalUsage(ch)
```

  There is now more comments and names were updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T11:01:11Z

- **pkg/controller/core/workload_controller.go:1459** by [@mimowo](https://github.com/mimowo) — 2026-03-20T11:01:11Z
```diff
@@ -1452,3 +1452,22 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(workload.Key(wl)))
```

  I'm actually concerned about this change because it calls the heavy cohort metrics computation on the event handlers which should be very fast. 
  
  I don't understand why do we need to react to workload events to update resource usage for cohort metrics, I would assume we could do it from the places where we update the usage for "nominal" subtree quota, I'm really tripped this injection point

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T11:02:13Z

- **pkg/controller/core/workload_controller.go:1459** by [@mimowo](https://github.com/mimowo) — 2026-03-20T11:02:13Z
```diff
@@ -1452,3 +1452,22 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(workload.Key(wl)))
```

  If we really need to react to workloads (but again I have no idea why we would) maybe we could at least move the code to the Reconcile. However, let's first settle the main conundrum.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T11:54:56Z

- **pkg/controller/core/workload_controller.go:1459** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T11:54:56Z
```diff
@@ -1452,3 +1452,22 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+func (r *WorkloadReconciler) addOrUpdateWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	updated := r.cache.AddOrUpdateWorkload(log, wl)
+	if wl != nil && workload.HasQuotaReservation(wl) {
+		r.cache.RecordCohortMetrics(log, r.cache.GetDirectCohortNameForWorkload(workload.Key(wl)))
```

  Ok, after talk with @mimowo.
  We checked that `NotifyWorkloadUpdate` triggers Reconcile in ClusterQueue where computation is usually done.
  Then no change in workload_controller is needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T12:04:17Z

- **pkg/cache/scheduler/cache.go:144** by [@mimowo](https://github.com/mimowo) — 2026-03-20T12:04:17Z
```diff
@@ -141,6 +141,21 @@ type Cache struct {
 	customLabels *metrics.CustomLabels
 }
 
+func (c *Cache) GetDirectCohortNameForWorkload(wlKey workload.Reference) kueue.CohortReference {
```

  So this funcion is unused now, right? Let's drop

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T12:46:45Z

- **pkg/cache/scheduler/cohort_metrics.go:70** by [@mimowo](https://github.com/mimowo) — 2026-03-20T12:46:46Z
```diff
@@ -60,6 +65,9 @@ func (c *Cache) ClearCohortMetrics(log logr.Logger, cohortName kueue.CohortRefer
 	}
 }
 
+// collectCohortMetricPoints prepares metric values for the target cohort and each ancestor.
+// When simulateRemoval=true, it computes the remaining ancestor subtree values after
+// subtracting the target cohort subtree quotas and reservations.
```

  Is it the intention?
  
  // collectCohortMetricPoints prepares subtree metric points for the target cohort
  // and all cohorts on the path from the target to the root.
  // When simulateRemoval=true, it computes post-removal subtree values by subtracting
  // the target cohort's subtree contribution from each cohort's current subtree totals.
  // This is used when clearing metrics so ancestor subtree gauges are updated too,
  // rather than left with stale values after the target cohort is removed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T13:13:15Z

- **pkg/cache/scheduler/cohort_metrics.go:120** by [@mimowo](https://github.com/mimowo) — 2026-03-20T13:13:15Z
```diff
@@ -72,22 +80,43 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 		return nil
 	}
 
-	removedQuota := ch.resourceNode.SubtreeQuota
+	removedSubtreeQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache subtree reservation aggregations once per cohort during this run to avoid
+	// repeated recursion when walking ancestor paths.
+	reservationsCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	removedSubtreeReservations := totalSubtreeReservationsWithCache(ch, reservationsCache)
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
-		quotas := ancestor.resourceNode.SubtreeQuota
+		cohortSubtreeQuota := ancestor.resourceNode.SubtreeQuota
+		cohortSubtreeReservations := totalSubtreeReservationsWithCache(ancestor, reservationsCache)
+
+		var ancestorCurrentSubtreeReservations resources.FlavorResourceQuantities
 		if simulateRemoval {
-			quotas = removedQuota
+			// In removal mode, subtract target subtree values from the ancestor current values
+			// to obtain the post-removal snapshot used for clearing metrics.
+			cohortSubtreeQuota = removedSubtreeQuota
+			cohortSubtreeReservations = removedSubtreeReservations
+			ancestorCurrentSubtreeReservations = totalSubtreeReservationsWithCache(ancestor, reservationsCache)
 		}
 
-		for flr, qty := range quotas {
+		flavorResourceKeys := sets.New[resources.FlavorResource]()
+		flavorResourceKeys.Insert(slices.Collect(maps.Keys(cohortSubtreeQuota))...)
+		flavorResourceKeys.Insert(slices.Collect(maps.Keys(cohortSubtreeReservations))...)
+
+		for fr := range flavorResourceKeys {
+			quotaQty := cohortSubtreeQuota[fr]
+			reservationsQty := cohortSubtreeReservations[fr]
 			if simulateRemoval {
-				qty = max(ancestor.resourceNode.SubtreeQuota[flr]-qty, 0)
+				quotaQty = max(ancestor.resourceNode.SubtreeQuota[fr]-quotaQty, 0)
+				reservationsQty = max(ancestorCurrentSubtreeReservations[fr]-reservationsQty, 0)
 			}
 			points = append(points, cohortMetricPoint{
-				cohortName:     ancestor.Name,
-				flavorResource: flr,
-				qty:            qty,
+				cohortName:      ancestor.Name,
+				flavorResource:  fr,
+				quotaQty:        quotaQty,
+				reservationsQty: reservationsQty,
 			})
```

  The code is quite complex I would say. Maybe worth considering a helper to Subtract the usage maps represtented as ResourceFlavorQuantity maps to hide technicalities behind an abstraction and avoid some weird tricks with pinning / unpining the cohort variables.
  
  I imagine the flow could be overall somthing like :
  
  ```golang
  currentCohortQuota := ch.resourceNode.SubtreeQuota
  for ancestor := range ch.PathSelfToRoot() {
    ancestorCohortQuota := ancestor.resourceNode.SubtreeQuota
    if simulateRemoval {
      currentCohortQuota.Subtract(ancestorCohortQuota) // or currentCohortQuotaWithRemove = currentCohortQuota.Sub(ancestorCohortQuota) if we need to also keep previous value
    }
  ```
  This would iiuc allow us later to skip the `simulateRemoval` handling in the flow.
  
  Let me know if I missing something or your encounter some obstacles.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T13:14:03Z

- **pkg/cache/scheduler/cohort_metrics.go:83** by [@mimowo](https://github.com/mimowo) — 2026-03-20T13:14:04Z
```diff
@@ -72,22 +80,43 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 		return nil
 	}
 
-	removedQuota := ch.resourceNode.SubtreeQuota
+	removedSubtreeQuota := ch.resourceNode.SubtreeQuota
```

  Let's also add some unit tests for the function which would allow quick verification of correctness with debugger without running the heavy machinery of integration tests.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-20T14:14:36Z

- **pkg/cache/scheduler/cohort_metrics.go:70** by [@mszadkow](https://github.com/mszadkow) — 2026-03-20T14:14:36Z
```diff
@@ -60,6 +65,9 @@ func (c *Cache) ClearCohortMetrics(log logr.Logger, cohortName kueue.CohortRefer
 	}
 }
 
+// collectCohortMetricPoints prepares metric values for the target cohort and each ancestor.
+// When simulateRemoval=true, it computes the remaining ancestor subtree values after
+// subtracting the target cohort subtree quotas and reservations.
```

  Yes, that's the point of this function

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-23T06:34:30Z

- **pkg/cache/scheduler/cohort_metrics.go:83** by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T06:34:30Z
```diff
@@ -72,22 +80,43 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 		return nil
 	}
 
-	removedQuota := ch.resourceNode.SubtreeQuota
+	removedSubtreeQuota := ch.resourceNode.SubtreeQuota
```

  Done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-23T06:34:47Z

- **pkg/cache/scheduler/cohort_metrics.go:120** by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T06:34:47Z
```diff
@@ -72,22 +80,43 @@ func (c *Cache) collectCohortMetricPoints(cohortName kueue.CohortReference, simu
 		return nil
 	}
 
-	removedQuota := ch.resourceNode.SubtreeQuota
+	removedSubtreeQuota := ch.resourceNode.SubtreeQuota
+
+	// Cache subtree reservation aggregations once per cohort during this run to avoid
+	// repeated recursion when walking ancestor paths.
+	reservationsCache := make(map[*cohort]resources.FlavorResourceQuantities)
+	removedSubtreeReservations := totalSubtreeReservationsWithCache(ch, reservationsCache)
+
 	var points []cohortMetricPoint
 	for ancestor := range ch.PathSelfToRoot() {
-		quotas := ancestor.resourceNode.SubtreeQuota
+		cohortSubtreeQuota := ancestor.resourceNode.SubtreeQuota
+		cohortSubtreeReservations := totalSubtreeReservationsWithCache(ancestor, reservationsCache)
+
+		var ancestorCurrentSubtreeReservations resources.FlavorResourceQuantities
 		if simulateRemoval {
-			quotas = removedQuota
+			// In removal mode, subtract target subtree values from the ancestor current values
+			// to obtain the post-removal snapshot used for clearing metrics.
+			cohortSubtreeQuota = removedSubtreeQuota
+			cohortSubtreeReservations = removedSubtreeReservations
+			ancestorCurrentSubtreeReservations = totalSubtreeReservationsWithCache(ancestor, reservationsCache)
 		}
 
-		for flr, qty := range quotas {
+		flavorResourceKeys := sets.New[resources.FlavorResource]()
+		flavorResourceKeys.Insert(slices.Collect(maps.Keys(cohortSubtreeQuota))...)
+		flavorResourceKeys.Insert(slices.Collect(maps.Keys(cohortSubtreeReservations))...)
+
+		for fr := range flavorResourceKeys {
+			quotaQty := cohortSubtreeQuota[fr]
+			reservationsQty := cohortSubtreeReservations[fr]
 			if simulateRemoval {
-				qty = max(ancestor.resourceNode.SubtreeQuota[flr]-qty, 0)
+				quotaQty = max(ancestor.resourceNode.SubtreeQuota[fr]-quotaQty, 0)
+				reservationsQty = max(ancestorCurrentSubtreeReservations[fr]-reservationsQty, 0)
 			}
 			points = append(points, cohortMetricPoint{
-				cohortName:     ancestor.Name,
-				flavorResource: flr,
-				qty:            qty,
+				cohortName:      ancestor.Name,
+				flavorResource:  fr,
+				quotaQty:        quotaQty,
+				reservationsQty: reservationsQty,
 			})
```

  I have simplified it significantly

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-03-23T11:11:27Z

- **pkg/cache/scheduler/cohort_metrics_test.go:114** by [@pajakd](https://github.com/pajakd) — 2026-03-23T11:11:28Z
```diff
@@ -0,0 +1,468 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"context"
+	"testing"
+
+	"github.com/go-logr/logr"
+	"github.com/prometheus/client_golang/prometheus"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	kueuemetrics "sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingmetrics "sigs.k8s.io/kueue/pkg/util/testing/metrics"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+func TestRecordCohortMetrics_Guards(t *testing.T) {
+	type wantPoint struct {
+		cohort       kueue.CohortReference
+		fr           resources.FlavorResource
+		quota        float64
+		reservations float64
+	}
+
+	cohortLeft := kueue.CohortReference("record-left")
+	cohortRight := kueue.CohortReference("record-right")
+	cohortRoot := kueue.CohortReference("record-root")
+	fr := resources.FlavorResource{Flavor: "flavor1", Resource: corev1.ResourceCPU}
+
+	cases := []struct {
+		name           string
+		cohortToRecord kueue.CohortReference
+		setup          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint
+		wantNoMetrics  []kueue.CohortReference
+	}{
+		{
+			name:           "Empty cohort name skips recording",
+			cohortToRecord: "",
+			setup:          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint { return nil },
+			wantNoMetrics:  []kueue.CohortReference{"empty-sentinel"},
+		},
+		{
+			name:           "Unknown cohort skips recording",
+			cohortToRecord: "missing-cohort",
+			setup:          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint { return nil },
+			wantNoMetrics:  []kueue.CohortReference{"missing-cohort"},
+		},
+		{
+			name:           "Records target cohort and ancestors only",
+			cohortToRecord: "record-left",
+			setup: func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint {
+				t.Helper()
+
+				setupRecordMetricsHierarchy(
+					ctx, t, log, cache,
+					[]*kueue.ResourceFlavor{
+						utiltestingapi.MakeResourceFlavor("flavor1").Obj(),
+					},
+					[]*kueue.Cohort{
+						utiltestingapi.MakeCohort(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "30").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeCohort(cohortLeft).
+							Parent(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "20").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeCohort(cohortRight).
+							Parent(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "10").
+								Obj()).
+							Obj(),
+					},
+					[]*kueue.ClusterQueue{
+						utiltestingapi.MakeClusterQueue("record-left-cq").
+							Cohort(cohortLeft).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "10").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeClusterQueue("record-right-cq").
+							Cohort(cohortRight).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "5").
+								Obj()).
+							Obj(),
+					},
+				)
+
+				// reservationMemo := subtreeReservationMemo{cache: make(map[*cohort]resources.FlavorResourceQuantities)}
+				// leftReservations := reservationMemo.total(cache.hm.Cohort(cohortLeft))[fr]
+				// rootReservations := reservationMemo.total(cache.hm.Cohort(cohortRoot))[fr]
```

  This seems to be unfinished.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-23T14:53:28Z

- **pkg/cache/scheduler/cohort_metrics_test.go:114** by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T14:53:28Z
```diff
@@ -0,0 +1,468 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"context"
+	"testing"
+
+	"github.com/go-logr/logr"
+	"github.com/prometheus/client_golang/prometheus"
+	corev1 "k8s.io/api/core/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	kueuemetrics "sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingmetrics "sigs.k8s.io/kueue/pkg/util/testing/metrics"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+func TestRecordCohortMetrics_Guards(t *testing.T) {
+	type wantPoint struct {
+		cohort       kueue.CohortReference
+		fr           resources.FlavorResource
+		quota        float64
+		reservations float64
+	}
+
+	cohortLeft := kueue.CohortReference("record-left")
+	cohortRight := kueue.CohortReference("record-right")
+	cohortRoot := kueue.CohortReference("record-root")
+	fr := resources.FlavorResource{Flavor: "flavor1", Resource: corev1.ResourceCPU}
+
+	cases := []struct {
+		name           string
+		cohortToRecord kueue.CohortReference
+		setup          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint
+		wantNoMetrics  []kueue.CohortReference
+	}{
+		{
+			name:           "Empty cohort name skips recording",
+			cohortToRecord: "",
+			setup:          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint { return nil },
+			wantNoMetrics:  []kueue.CohortReference{"empty-sentinel"},
+		},
+		{
+			name:           "Unknown cohort skips recording",
+			cohortToRecord: "missing-cohort",
+			setup:          func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint { return nil },
+			wantNoMetrics:  []kueue.CohortReference{"missing-cohort"},
+		},
+		{
+			name:           "Records target cohort and ancestors only",
+			cohortToRecord: "record-left",
+			setup: func(t *testing.T, ctx context.Context, log logr.Logger, cache *Cache) []wantPoint {
+				t.Helper()
+
+				setupRecordMetricsHierarchy(
+					ctx, t, log, cache,
+					[]*kueue.ResourceFlavor{
+						utiltestingapi.MakeResourceFlavor("flavor1").Obj(),
+					},
+					[]*kueue.Cohort{
+						utiltestingapi.MakeCohort(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "30").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeCohort(cohortLeft).
+							Parent(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "20").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeCohort(cohortRight).
+							Parent(cohortRoot).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "10").
+								Obj()).
+							Obj(),
+					},
+					[]*kueue.ClusterQueue{
+						utiltestingapi.MakeClusterQueue("record-left-cq").
+							Cohort(cohortLeft).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "10").
+								Obj()).
+							Obj(),
+						utiltestingapi.MakeClusterQueue("record-right-cq").
+							Cohort(cohortRight).
+							ResourceGroup(*utiltestingapi.MakeFlavorQuotas("flavor1").
+								Resource(corev1.ResourceCPU, "5").
+								Obj()).
+							Obj(),
+					},
+				)
+
+				// reservationMemo := subtreeReservationMemo{cache: make(map[*cohort]resources.FlavorResourceQuantities)}
+				// leftReservations := reservationMemo.total(cache.hm.Cohort(cohortLeft))[fr]
+				// rootReservations := reservationMemo.total(cache.hm.Cohort(cohortRoot))[fr]
```

  thanks, I should have removed it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T18:31:09Z

- **pkg/resources/resource.go:56** by [@mimowo](https://github.com/mimowo) — 2026-03-23T18:31:09Z
```diff
@@ -51,3 +51,12 @@ func (frq FlavorResourceQuantities) FlattenFlavors() Requests {
 	}
 	return result
 }
+
+// SubNonNegative returns max(frq-other, 0) for keys present in frq.
+func (frq FlavorResourceQuantities) SubNonNegative(other FlavorResourceQuantities) FlavorResourceQuantities {
```

  can we do just Sub() ? It is simpler semantic and later I see `quotaQty <= 0` so it seems we are handling negative values as 0 anyway

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-23T18:34:35Z

- **pkg/resources/resource.go:56** by [@mszadkow](https://github.com/mszadkow) — 2026-03-23T18:34:34Z
```diff
@@ -51,3 +51,12 @@ func (frq FlavorResourceQuantities) FlattenFlavors() Requests {
 	}
 	return result
 }
+
+// SubNonNegative returns max(frq-other, 0) for keys present in frq.
+func (frq FlavorResourceQuantities) SubNonNegative(other FlavorResourceQuantities) FlavorResourceQuantities {
```

  sure

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T19:08:34Z

/lgtm
/approve
