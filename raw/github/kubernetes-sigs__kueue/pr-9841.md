# PR #9841: Update DRA KEP to explain CEL handling

**Summary**: Update DRA KEP to explain CEL handling

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9841

**Last updated**: 2026-04-30T18:43:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `issue-7291-cel-kep` → `main`
- **Created**: 2026-03-12T21:00:50Z
- **Updated**: 2026-04-30T18:43:28Z
- **Merged**: 2026-04-30T18:43:25Z
- **Closed**: 2026-04-30T18:43:26Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@alaypatel07](https://github.com/alaypatel07), [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alaypatel07](https://github.com/alaypatel07), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+81 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Describe design of #7298 
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-12T21:01:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a08e96f3715a8ab553a8fd2f0479c64b4c00e942 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e92f18d9b9750008eca04c |

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-12T21:03:06Z

/cc @tenzen-y @sohankunkerkar @alaypatel07 @mimowo

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-13T06:55:23Z

/remove-kind feature
/kind kep

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-13T06:55:27Z

@tenzen-y: Those labels are not set on the issue: `kind/feature`

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9841#issuecomment-4053178549):

>/remove-kind feature
>/kind kep


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-30T13:21:24Z

@mimowo @tenzen-y can one of you take a look at this?

### Comment by [@kannon92](https://github.com/kannon92) — 2026-03-31T19:47:13Z

/assign @alaypatel07 @sohankunkerkar

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-15T18:51:32Z

@alaypatel07 @tenzen-y could you take a look at this?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T17:10:12Z

@kannon92 can you also resolve the conflicts?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T17:16:29Z

/remove-kind documentation

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-20T20:52:23Z

@tenzen-y 
this is ready for a review again. I believe I addressed your concerns.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T07:05:30Z

Also, I'm wondering if we should introduce a dedicated FeatureGate, something like DRACELExpressions.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-21T15:48:06Z

> Also, I'm wondering if we should introduce a dedicated FeatureGate, something like DRACELExpressions.

CEL is critical for DRA so I think we should not go with a feature gate. We have the DRA feature gate which is alpha and CEL is a GA feature of DRA so I think its fine to guard this under the existing feature gate.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-22T17:08:59Z

> > Also, I'm wondering if we should introduce a dedicated FeatureGate, something like DRACELExpressions.
> 
> CEL is critical for DRA so I think we should not go with a feature gate. We have the DRA feature gate which is alpha and CEL is a GA feature of DRA so I think its fine to guard this under the existing feature gate.

I will decide FG based on the final https://github.com/kubernetes-sigs/kueue/pull/9841#discussion_r3112398693 decision.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-30T16:19:02Z

LGTM label has been added.  <details>Git tree hash: 7800411696bafc85f70e16c910200300c24ea689</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-30T16:19:05Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9841#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9841#pullrequestreview-4206759083" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T18:25:32Z

/unhold
I'm good to give it a go as Alpha, thank you 👍

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-05T22:19:31Z

Thanks for the write up! I have added a few comments.

- **keps/2941-DRA/README.md:495** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T21:35:50Z
```diff
@@ -481,6 +486,9 @@ rules:
 - apiGroups: ["resource.k8s.io"]
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
+- apiGroups: ["resource.k8s.io"]
```

  nit: I think you can combine both `resourceslices` + `deviceclasses` into one.

- **keps/2941-DRA/README.md:528** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T21:38:09Z
```diff
@@ -489,13 +497,60 @@ rules:
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error, preventing quota consumption for unsatisfiable requests.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.attributes["memory"].compareTo(quantity("80Gi")) >= 0`
```

  I think memory is a capacity and not an attribute. Per the upstream k8s docs, it should be
  ```sh
  device.capacity["driver.example.com"].memory.compareTo(quantity("80Gi")) >= 0 
  ```

- **keps/2941-DRA/README.md:269** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T21:40:16Z
```diff
@@ -265,6 +266,10 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
```

  This reads like a guarantee but the validation is best-effort. Devices can be allocated between Kueue's check and scheduling. Also the reverse: new ResourceSlices published after validation can make previously-unsatisfiable workloads satisfiable, but nothing triggers re-evaluation until the next periodic queueInadmissibleWorkloads cycle. Worth adding a sentence about `WaitForPodsReady` being the safety net and the liveness gap for newly added devices

- **keps/2941-DRA/README.md:517** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-05T22:10:09Z
```diff
@@ -489,13 +497,60 @@ rules:
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
```

  Just a note: before this change, inadmissible meant "not enough quota" and users knew to wait. Now it can also mean "no matching devices exist in the cluster" which is a fundamentally different problem. Users need to change their CEL or an admin needs to add hardware. The condition message should clearly distinguish between quota-based and device-based inadmissibility so users know what action to take.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T21:01:37Z

- **keps/2941-DRA/README.md:495** by [@kannon92](https://github.com/kannon92) — 2026-04-06T21:01:37Z
```diff
@@ -481,6 +486,9 @@ rules:
 - apiGroups: ["resource.k8s.io"]
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
+- apiGroups: ["resource.k8s.io"]
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T21:01:44Z

- **keps/2941-DRA/README.md:528** by [@kannon92](https://github.com/kannon92) — 2026-04-06T21:01:44Z
```diff
@@ -489,13 +497,60 @@ rules:
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error, preventing quota consumption for unsatisfiable requests.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.attributes["memory"].compareTo(quantity("80Gi")) >= 0`
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T21:01:59Z

- **keps/2941-DRA/README.md:269** by [@kannon92](https://github.com/kannon92) — 2026-04-06T21:01:59Z
```diff
@@ -265,6 +266,10 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
```

  thanks for that. updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T21:02:11Z

- **keps/2941-DRA/README.md:517** by [@kannon92](https://github.com/kannon92) — 2026-04-06T21:02:11Z
```diff
@@ -489,13 +497,60 @@ rules:
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
```

  good idea. I added a note here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T17:09:52Z

Thank you for updating this KEP with CEL support!
I left some comments.

- **keps/2941-DRA/README.md:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:40:10Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
+
+**Dependencies**: This feature imports `k8s.io/dynamic-resource-allocation/cel` for CEL compilation and
```

  ```suggestion
  **Dependencies**: This feature imports [`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel) for CEL compilation and
  ```
  
  This link could help to understand what is this library.

- **keps/2941-DRA/README.md:270** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:46:54Z
```diff
@@ -265,6 +266,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
+  time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
```

  ```suggestion
  - CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at qtuota reservation
    time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
  ```
  
  Does this mean `QuotaReservation` phase?

- **keps/2941-DRA/README.md:271** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:47:23Z
```diff
@@ -265,6 +266,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
+  time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
+  to prevent quota leaks. This validation uses the upstream DRA CEL compiler from `k8s.io/dynamic-resource-allocation/cel`.
```

  ```suggestion
    to prevent quota leaks. This validation uses the upstream DRA CEL compiler from [`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel).
  ```

- **keps/2941-DRA/README.md:272** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:48:06Z
```diff
@@ -265,6 +266,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
+  time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
+  to prevent quota leaks. This validation uses the upstream DRA CEL compiler from `k8s.io/dynamic-resource-allocation/cel`.
+  Note that devices can be allocated between Kueue's check and scheduling, and new ResourceSlices published after
```

  ```suggestion
    On the other hand, devices can be allocated between Kueue's check and scheduling, and new ResourceSlices published after
  ```
  
  This is the NOTE section.

- **keps/2941-DRA/README.md:518** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:49:39Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
```

  ```suggestion
     compiler ([`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel)). This catches syntax errors, type errors, and other
  ```

- **keps/2941-DRA/README.md:519** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:50:09Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
```

  ```suggestion
     compilation issues before quota reservation.
  ```
  
  I'd like to distinguish between admission and reservation.

- **keps/2941-DRA/README.md:531** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:51:36Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
```

  Which Workload condition type does this indicate? QuotaReserved?

- **keps/2941-DRA/README.md:601** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:52:10Z
```diff
@@ -530,12 +590,16 @@ When a user submits a workload and DynamicResourceAllocation feature gate is ena
    - Read the ResourceClaimTemplate from the same namespace as the workload
    - Extract deviceClassName from each request in the template spec
    - Look up the corresponding resource name using the device class mappings from the Configuration API
-5. Resource Preprocessing:
+5. CEL Selector Validation: For requests with CEL selectors:
+   - Compile CEL expressions and reject workloads with invalid syntax
+   - Evaluate CEL selectors against actual devices from ResourceSlices, pre-filtering by DeviceClass
+   - Reject workloads where fewer devices match than requested count
+6. Resource Preprocessing:
    - Calculate total device count per device class across all containers and init containers
    - Map device classes to resource names using the configuration
    - Generate preprocessed resource requests for queue admission
-6. Queue Admission: Add the workload to the queue with preprocessed DRA resources.
-7. Status Update: Once admitted, the workload status reflects the assigned flavors and resource usage including DRA resources.
+7. Queue Admission: Add the workload to the queue with preprocessed DRA resources.
```

  ```suggestion
  7. Queue Reservation: Add the workload to the queue with preprocessed DRA resources.
  ```

- **keps/2941-DRA/README.md:277** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:53:39Z
```diff
@@ -265,6 +266,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
+  time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
+  to prevent quota leaks. This validation uses the upstream DRA CEL compiler from `k8s.io/dynamic-resource-allocation/cel`.
+  Note that devices can be allocated between Kueue's check and scheduling, and new ResourceSlices published after
+  validation can make previously-unsatisfiable workloads satisfiable without triggering re-evaluation until the
+  next periodic `queueInadmissibleWorkloads` cycle. `WaitForPodsReady` serves as the safety net for cases where
+  the validation state diverges from actual device availability at scheduling time.
```

  Yes, correct. We can strengthen validations by TAS in the future.
  But, it's out of scope at this time.

- **keps/2941-DRA/README.md:552** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:57:12Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
```

  ```suggestion
                expression: 'device.capacity["gpu.example.com"].memory.compareTo(quantity("80Gi")) >= 0'
  ```
  
  Is this example really correct? As I can see https://github.com/kubernetes/kubernetes/blob/03779bbd00da25c7fcd03711ddfe466a3322e1d7/pkg/apis/resource/types.go#L1241, my suggestion is correct.

- **keps/2941-DRA/README.md:533** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:57:37Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
```

  ```suggestion
  **Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["gpu.example.com"].memory.compareTo(quantity("80Gi")) >= 0`
  ```
  
  ditto

- **keps/2941-DRA/README.md:997** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:58:14Z
```diff
@@ -928,6 +994,7 @@ Use existing dra-example-driver or Kubernetes test driver for e2e testing.
 - Alpha implementation completed: December 2024
 - KEP updated to reflect actual implementation: September 2025 by @alaypatel07
 - Extended Resources implementation: January 2026
+- CEL expression validation support added: March 2026 by @kannon92
```

  ```suggestion
  - CEL expression validation support added: April 2026 by @kannon92
  ```
  
  Let's update this after v0.17 release.

- **keps/2941-DRA/README.md:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T17:09:37Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  In this example, how many quotas will be consumed in ClusterQueue?
  Let's say that we have the following ClusterQueue, then all the ResourceSlices have only GPUs with 100Gi VRAM. In that case, will ClusterQueue consume only `example.com/gpu: 1` even if it takes larger GPU resources compared with the minimum request size (80Gi)?
  
  ```yaml
  apiVersion: config.kueue.x-k8s.io/v1beta2
  kind: Configuration
  ...
  resources:
    deviceClassMappings:
    - name: example.com/gpu
      deviceClassNames:
      - gpu.example.com
  ---
  apiVersion: kueue.x-k8s.io/v1beta2
  kind: ClusterQueue
  ...
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "example.com/gpu"]
      flavors:
      - name: "default-flavor"
        resources:
        - name: "cpu"
          nominalQuota: 40
        - name: "memory"
          nominalQuota: 200Gi
        - name: "example.com/gpu"
          nominalQuota: 8
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T17:15:09Z

- **keps/2941-DRA/README.md:555** by [@kannon92](https://github.com/kannon92) — 2026-04-20T17:15:09Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  
  
  This can be somewhat related to @PannagaRao and @sohankunkerkar work on partionable devices.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-20T17:43:30Z

- **keps/2941-DRA/README.md:555** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-20T17:43:30Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  With deviceClassMappings alone, Kueue charges device count — `example.com/gpu: 2` in this example. 
                                                                                                                                                                                                                      
  With counterMappings from [#10320](https://github.com/kubernetes-sigs/kueue/pull/10320), Kueue reads consumesCounters from matched devices and charges based on actual counter values. The charge is MAX(counter across all matched devices) × count. In this example, if all GPUs have 100Gi memory, the charge would be gpu.memory: 100Gi × 2 = 200Gi

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T19:02:51Z

- **keps/2941-DRA/README.md:555** by [@kannon92](https://github.com/kannon92) — 2026-04-20T19:02:51Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  For Kueue could we even do quota on VRAM for GPU? 
  
  I think the resources has to point to the deviceClass.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T20:00:33Z

- **keps/2941-DRA/README.md:531** by [@kannon92](https://github.com/kannon92) — 2026-04-20T20:00:33Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
```

  Yes, eventually this ends up in QuotaReserved.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T20:51:39Z

- **keps/2941-DRA/README.md:555** by [@kannon92](https://github.com/kannon92) — 2026-04-20T20:51:39Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
+
+**Dependencies**: This feature imports `k8s.io/dynamic-resource-allocation/cel` for CEL compilation and
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T20:51:51Z

- **keps/2941-DRA/README.md:270** by [@kannon92](https://github.com/kannon92) — 2026-04-20T20:51:51Z
```diff
@@ -265,6 +266,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at admission
+  time on a best-effort basis. Workloads with CEL selectors that match fewer devices than requested are rejected
```

  yes.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-21T06:52:32Z

Thank you for updating that.

- **keps/2941-DRA/README.md:270** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T06:34:22Z
```diff
@@ -266,6 +267,14 @@ administrator configures DeviceClasses with `extendedResourceName`.
   `extendedResourceName`, Kueue unifies quota** using the `deviceClassMappings` logical
   name as the quota key for both paths, preventing over-allocation.
 
+- CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at qtuota reservation
```

  ```suggestion
  - CEL selectors in ResourceClaimTemplates are validated against cluster devices (ResourceSlices) at quota reservation
  ```
  
  nit: typo

- **keps/2941-DRA/README.md:603** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T06:35:51Z
```diff
@@ -531,12 +591,16 @@ When a user submits a workload and DynamicResourceAllocation feature gate is ena
    - Read the ResourceClaimTemplate from the same namespace as the workload
    - Extract deviceClassName from each request in the template spec
    - Look up the corresponding resource name using the device class mappings from the Configuration API
-5. Resource Preprocessing:
+5. CEL Selector Validation: For requests with CEL selectors:
+   - Compile CEL expressions and reject workloads with invalid syntax
+   - Evaluate CEL selectors against actual devices from ResourceSlices, pre-filtering by DeviceClass
+   - Reject workloads where fewer devices match than requested count
+6. Resource Preprocessing:
    - Calculate total device count per device class across all containers and init containers
    - Map device classes to resource names using the configuration
    - Generate preprocessed resource requests for queue admission
-6. Queue Admission: Add the workload to the queue with preprocessed DRA resources.
-7. Status Update: Once admitted, the workload status reflects the assigned flavors and resource usage including DRA resources.
+7. Queue Reservation: Add the workload to the queue with preprocessed DRA resources.
+8. Status Update: Once admitted, the workload status reflects the assigned flavors and resource usage including DRA resources.
```

  ```suggestion
  8. Status Update: Once the quota is reserved, the workload status reflects the assigned flavors and resource usage, including DRA resources.
  ```
  
  nit: admission -> quota reservation

- **keps/2941-DRA/README.md:531** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T06:42:08Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
```

  In that case, can you mention the `QuotaReserved` in this message like the following:
  
  ```suggestion
       unsatisfiable requests. The `QuotaReserved` condition message clearly distinguishes between device-based inadmissibility
       (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
       know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
       quota to become available.
  ```
  
  (I just aded `QuotaReseerved` term)

- **keps/2941-DRA/README.md:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T06:50:30Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  @PannagaRao @kannon92 Thank you for describing that. I understand what happens here.
  I'm wondering if we should `counterMappings` somehow mechanism as part of this CEL evaluation mechanism? Otherwise, there will be large gaps between actual consuming device capacities and consumed quotas.

- **keps/2941-DRA/README.md:524** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T06:52:16Z
```diff
@@ -483,20 +492,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler ([`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel)). This catches syntax errors, type errors, and other
+   compilation issues before quota reservation.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
```

  I'm just wondering if we can filter out the obtaining ResourceSlices instead of obtaining all ResourceSlices to improve the Kueue performance.
  
  @kannon92 Are we able that?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-21T14:10:48Z

- **keps/2941-DRA/README.md:555** by [@kannon92](https://github.com/kannon92) — 2026-04-21T14:10:48Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  I think there is plans to use this PR for the PartionableDevices work.
  
  There is a separate KEP for that. I think we should keep this separate for now.
  
  CounterMappings is only relevant if you are using PartitionableDevices but the cel expression is a GA feature for structured parameters.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-21T15:47:10Z

- **keps/2941-DRA/README.md:524** by [@kannon92](https://github.com/kannon92) — 2026-04-21T15:47:09Z
```diff
@@ -483,20 +492,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler ([`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel)). This catches syntax errors, type errors, and other
+   compilation issues before quota reservation.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
```

  Maybe. It is indexed by driverName but resource slices can be at the node level so I'm not sure how much that would gain us.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-22T17:01:31Z

- **keps/2941-DRA/README.md:524** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-22T17:01:31Z
```diff
@@ -483,20 +492,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler ([`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel)). This catches syntax errors, type errors, and other
+   compilation issues before quota reservation.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
```

  My motivation for the filtering of ResourceSlice is optimizing performance both for Kueue cache and the kube-apiserver load. I'm ok with postponing the optimizations to Beta or GA graduation.
  
  Can you add such optimization to the graduation criteria?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-22T17:08:19Z

- **keps/2941-DRA/README.md:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-22T17:08:19Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  > I think there is plans to use this PR for the PartionableDevices work.
  > 
  > There is a separate KEP for that. I think we should keep this separate for now.
  > 
  > CounterMappings is only relevant if you are using PartitionableDevices but the cel expression is a GA feature for structured parameters.
  
  The key point is `Otherwise, there will be large gaps between actual consuming device capacities and consumed quotas.`.
  
  So, if we don't introduce the `counterMappings` somehow mechanism for DRA CEL impls, we should restrict DRA CEL usage only when they use DRA CEL with DRA PartitionableDevices.
  
  Otherwise, this DRA CEL introduces critical Quota violations because, as I mentioned, DRA CEL w/o `counterMappings` can not guarantee how many actual resources are consumed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-23T01:28:02Z

- **keps/2941-DRA/README.md:524** by [@kannon92](https://github.com/kannon92) — 2026-04-23T01:28:02Z
```diff
@@ -483,20 +492,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler ([`k8s.io/dynamic-resource-allocation/cel`](https://github.com/kubernetes/dynamic-resource-allocation/tree/master/cel)). This catches syntax errors, type errors, and other
+   compilation issues before quota reservation.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
```

  Done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-23T01:35:25Z

- **keps/2941-DRA/README.md:555** by [@kannon92](https://github.com/kannon92) — 2026-04-23T01:35:25Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  I'm not really following you here.
  
  If I have a request with CEL asking for > 80 GB of memory, it will get the device and then count the request to that device.
  
  CEL is more about getting the right device class and not about enforcing GPU memory. With GPUs there isn't really a way to enforce memory as you would with cgroups. 
  
  Maybe I focused too much on CEL for memory in this update but CEL is basically a way to get any kind of attribute. You could use it to say "I want a specific device" ex: https://github.com/kubernetes-sigs/dra-example-driver/blob/main/demo/gpu-test8.yaml.
  
  For DRANET, you can see other examples: https://dranet.dev/docs/user/mpi-operator/.
  
  In these examples we are grabbing a certain device with specific attributes. The hope of adding CEL is to support things all the ways CEL can be used with DRA.
  
  I don't think CEL evaulation needs to tie into Partionable devices. Happy to discuss tomorrow if you have more questions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-24T17:10:49Z

- **keps/2941-DRA/README.md:555** by [@mimowo](https://github.com/mimowo) — 2026-04-24T17:10:49Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  Hm, I'm also not following where is the concern, but I'm maybe missing some of the DRA context. 
  
  Let me ask about this specific scenario - in my ResourceFlavor, I have 
  - 2 devices with 1 GPU, 80Gi mem 
  - 2 devices with 1 GPU, 40Gi mem
  
  Now, with the configurations as above I have workloads 
  - (1.) matching >=80Gi devices, and requesting 1 device, so it consumes `example.com/gpu: 1` - admitted by quota, binds to one of the machines; 3 GPU of quota left 
  - (2.) matching >=80Gi devices, and requesting 1 device, so it consumes `example.com/gpu: 1` - admitted by quota, binds to one of the machines; 2 GPU of quota left
  - (3.) matching >=80Gi devices, and requesting 1 device, so it consumes `example.com/gpu: 1` - admitted by quota, but cannot find the machines because the only two 80Gi machines are already taken; It cannot schedule using 2 machines with 40Gi each
  
  So (3.) means the workload remains pending, which is a right risk to call out. However:
  1. I think this risk always existed in quota-only Kueue, also even without DRA Kueue could admit workload requesting 8GPU in one Pod, even though there all machines have only 1GPU
  2. In my mind this is where Kueue needs to know "infra", and for that we will have the DRA-TAS integration via scheduler-library which will fix such issues
  3. for now this would be the risk we need to document, but I don't think this should prevent the CEL support completely (unless I'm missing something)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-24T17:13:50Z

- **keps/2941-DRA/README.md:555** by [@mimowo](https://github.com/mimowo) — 2026-04-24T17:13:50Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  In other words, I think the lack of machine / infra awareness is indeed a risk, but it is not new, and in Kueue the right layer to fix problems like this is TAS. So, we would still consume the quota the same, but block admission of workload (3.) based on TAS+DRA constraints rather than quota.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-30T16:16:16Z

- **keps/2941-DRA/README.md:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-30T16:16:16Z
```diff
@@ -482,20 +491,71 @@ rules:
   resources: ["resourceclaimtemplates"]
   verbs: ["get", "list", "watch"]
 - apiGroups: ["resource.k8s.io"]
-  resources: ["deviceclasses"]
+  resources: ["resourceslices", "deviceclasses"]
   verbs: ["get", "list", "watch"]
 ```
 
 **Required Permissions:**
 - `resourceclaims`: Read access to validate ResourceClaim references (though not supported for quota)
 - `resourceclaimtemplates`: Read access to process ResourceClaimTemplates and extract device class information
-- `deviceclasses`: Read access to look up `extendedResourceName` for Extended Resources
+- `resourceslices`: Read access to list cluster devices for CEL selector validation
+- `deviceclasses`: Read access to resolve DeviceClass selectors for device pre-filtering during CEL evaluation
 
 **Security Considerations:**
 - Kueue only requires read permissions - no create, update, or delete access to DRA resources
 - Permissions are cluster-scoped to allow processing workloads across all namespaces
 - No elevated privileges required beyond standard Kueue controller permissions
 
+### CEL Expression Validation
+
+ResourceClaimTemplates may include CEL (Common Expression Language) selectors that constrain which devices
+can satisfy a request. Kueue validates these CEL selectors before admitting a workload to prevent quota from
+being consumed by workloads whose pods can never be scheduled.
+
+The validation has two stages:
+
+1. **CEL Compilation**: Each CEL expression in the request's selectors is compiled using the upstream DRA CEL
+   compiler (`k8s.io/dynamic-resource-allocation/cel`). This catches syntax errors, type errors, and other
+   compilation issues before quota admission.
+
+2. **CEL Evaluation Against Cluster Devices**: Kueue lists all ResourceSlices in the cluster and evaluates
+   the compiled CEL selectors against actual devices. For each request:
+   - The DeviceClass is resolved and its selectors are compiled to pre-filter devices by class, avoiding
+     CEL evaluation against unrelated devices (e.g., NICs when requesting GPUs).
+   - The request's CEL selectors are evaluated against matching devices.
+   - If fewer devices match than the requested count, the workload is marked inadmissible with a descriptive
+     error indicating that no matching devices exist in the cluster, preventing quota consumption for
+     unsatisfiable requests. The condition message clearly distinguishes between device-based inadmissibility
+     (e.g., "insufficient matching devices for CEL selector") and quota-based inadmissibility so that users
+     know whether they need to adjust their CEL selectors / request admin hardware changes, or wait for
+     quota to become available.
+
+**Example**: A ResourceClaimTemplate requesting 2 GPUs with `device.capacity["memory"].compareTo(quantity("80Gi")) >= 0`
+will be checked against actual devices in the cluster. If only 1 device matches, the workload is rejected
+before consuming quota.
+
+```yaml
+apiVersion: resource.k8s.io/v1
+kind: ResourceClaimTemplate
+metadata:
+  name: large-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        exactly:
+          deviceClassName: gpu.example.com
+          count: 2
+          selectors:
+          - cel:
+              expression: 'device.capacity["memory"].compareTo(quantity("80Gi")) >= 0'
+```
```

  @mimowo Thank you for getting back comments here.
  
  As I chatted with @mimowo in Slack, I almost agreed with this fragmentation between Node-level resources and quota. Certainly, we have some risks when cluster admins misconfigure too flexible CEL parameters like `'device.capacity["memory"].compareTo(quantity("16Gi")) >= 0'`. In this scenario, if the cluster has only accelerators with over 141 GiB VRAM, it looks like a very soft restriction.
  
  But, the same as before TAS, we had similar problems about fragmentations in non-DRA environments as well. We can recommend not specifying too flexible CEL parameters to avoid much fragmentation between Node allocatable resources and nominalQuota in our user documentation.
  
  And, in the future, we can mitigate these risks in TAS+DRA functionalities.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-30T16:18:54Z

Based on https://github.com/kubernetes-sigs/kueue/pull/9841#discussion_r3112398693 discussion, I decided not to request adding mitigation for fragmentation between Node allocatable resources with DRA CEL and ClusterQueue quotas.

And, I don't request additional FG for that.

@kannon92 Thank you!
/lgtm
/approve

/hold
for @mimowo
