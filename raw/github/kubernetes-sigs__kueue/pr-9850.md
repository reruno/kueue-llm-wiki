# PR #9850: feat: Enable configuration of visibility server bind address and port…

**Summary**: feat: Enable configuration of visibility server bind address and port…

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9850

**Last updated**: 2026-03-23T17:46:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Nilsachy](https://github.com/Nilsachy)
- **Branch**: `feat--add-visibility-to-configuration-api` → `main`
- **Created**: 2026-03-13T10:38:57Z
- **Updated**: 2026-03-23T17:46:03Z
- **Merged**: 2026-03-23T17:42:25Z
- **Closed**: 2026-03-23T17:42:25Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@kshalot](https://github.com/kshalot), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 17   **+274 / -22**

## Description

What type of PR is this?
/kind feature

What this PR does / why we need it:
Enable configuration of visibility server bind address and port through the Configuration API

Which issue(s) this PR fixes:
Fixes #

Does this PR introduce a user-facing change?
```release-note
VisibilityOnDemand: Introduce a new configuration type: "VisibilityServer" which allows passing custom "VisibilityServerConfiguration" limited to BindAddress and BindPort as a first iteration. Removing the deprecated "visibility-server-port" flag.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-13T10:39:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e9bfc599ef84f153c0ce618cf920d46d07939d91 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c15e7c2a836500087b812c |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-13T10:39:08Z

Hi @Nilsachy. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T10:50:15Z

/ok-to-test

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T11:10:34Z

I am not sure what the KEP should define here? Is this a revision of the current KEP for pending-workloads-visibility or that's a separate KEP? @mimowo @kshalot

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-13T11:19:32Z

> I am not sure what the KEP should define here? Is this a revision of the current KEP for pending-workloads-visibility or that's a separate KEP? @mimowo @kshalot

Good question. My gut feeling is that this would be a separate KEP to not couple it with "pending workloads visibility".

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:25:51Z

> I am not sure what the KEP should define here? Is this a revision of the current KEP for pending-workloads-visibility or that's a separate KEP? @mimowo @kshalot

I don't have a strong view here, but I would rather be on the update to the existing KEP, it feels like less work overall.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T11:27:06Z

In that case I would probably just expand with a new "Story" and "Technical Details" sections, any optinon @tenzen-y ?

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T13:23:57Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T13:44:54Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-20T13:53:05Z

I think we can update in this PR. iiuc this is just commentary addition

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T14:04:26Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T10:42:00Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T11:09:17Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T13:54:32Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T15:26:26Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T16:03:31Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-23T17:40:19Z

/lgtm
/approve
Thank you 👍 We can follow up with the docs update

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T17:40:27Z

LGTM label has been added.  <details>Git tree hash: 00291ab249b1572bf22dfa4fda59524edff448e9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T17:40:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9850#pullrequestreview-3991528398" title="Approved">dkaluza</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9850#issuecomment-4112506425" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9850#" title="Author self-approved">Nilsachy</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T11:02:26Z

- **pkg/visibility/server.go:107** by [@kshalot](https://github.com/kshalot) — 2026-03-13T11:00:44Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
```

  Maybe this could become an explicit default value in the API instead of being hard-coded here?

- **pkg/visibility/server.go:115** by [@kshalot](https://github.com/kshalot) — 2026-03-13T11:02:24Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
-	if enableInternalCertManagement {
+
+	// Apply visibility overrides from Configuration API if present
+	if cfg.Visibility != nil {
+		if cfg.Visibility.BindPort != nil {
+			o.SecureServing.BindPort = int(*cfg.Visibility.BindPort)
+		}
+		if cfg.Visibility.BindAddress != nil && *cfg.Visibility.BindAddress != "" {
+			o.SecureServing.BindAddress = net.ParseIP(*cfg.Visibility.BindAddress)
```

  I'm not 100% sure the other `BindAddress` fields in our config are validated, but if the address is not parseable, we'll end up with a no-op (I think since it will assign `nil`, but you can verify the impact of that).
  
  Maybe we could use `net.ParseIP` to validate the addresses when the config is being loaded.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-13T11:05:52Z

- **pkg/visibility/server.go:107** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T11:05:52Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
```

  Good point, done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-13T11:37:58Z

- **pkg/visibility/server.go:115** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T11:37:58Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
-	if enableInternalCertManagement {
+
+	// Apply visibility overrides from Configuration API if present
+	if cfg.Visibility != nil {
+		if cfg.Visibility.BindPort != nil {
+			o.SecureServing.BindPort = int(*cfg.Visibility.BindPort)
+		}
+		if cfg.Visibility.BindAddress != nil && *cfg.Visibility.BindAddress != "" {
+			o.SecureServing.BindAddress = net.ParseIP(*cfg.Visibility.BindAddress)
```

  Do you mean creating a new `func validateBindAddresses(c *configapi.Configuration)` for all TCP and IP address fields defined in the config?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-13T11:43:02Z

- **pkg/visibility/server.go:115** by [@kshalot](https://github.com/kshalot) — 2026-03-13T11:43:01Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
-	if enableInternalCertManagement {
+
+	// Apply visibility overrides from Configuration API if present
+	if cfg.Visibility != nil {
+		if cfg.Visibility.BindPort != nil {
+			o.SecureServing.BindPort = int(*cfg.Visibility.BindPort)
+		}
+		if cfg.Visibility.BindAddress != nil && *cfg.Visibility.BindAddress != "" {
+			o.SecureServing.BindAddress = net.ParseIP(*cfg.Visibility.BindAddress)
```

  I think I'd start by validating only the visibility bind address, to keep the scope of this PR tighter. `validateVisibility` or something along those lines.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-13T13:01:16Z

- **pkg/visibility/server.go:115** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T13:01:16Z
```diff
@@ -97,14 +98,25 @@ func CreateAndStartVisibilityServer(ctx context.Context, kueueMgr *qcache.Manage
 	return nil
 }
 
-func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, enableInternalCertManagement bool, tlsOpts *tlsconfig.TLS) error {
+func applyVisibilityServerOptions(config *genericapiserver.RecommendedConfig, cfg *configapi.Configuration, tlsOpts *tlsconfig.TLS) error {
 	o := genericoptions.NewRecommendedOptions("", codecs.LegacyCodec(
 		visibilityv1beta2.SchemeGroupVersion,
 		visibilityv1beta1.SchemeGroupVersion,
 	))
 	o.Etcd = nil
 	o.SecureServing.BindPort = 8082
-	if enableInternalCertManagement {
+
+	// Apply visibility overrides from Configuration API if present
+	if cfg.Visibility != nil {
+		if cfg.Visibility.BindPort != nil {
+			o.SecureServing.BindPort = int(*cfg.Visibility.BindPort)
+		}
+		if cfg.Visibility.BindAddress != nil && *cfg.Visibility.BindAddress != "" {
+			o.SecureServing.BindAddress = net.ParseIP(*cfg.Visibility.BindAddress)
```

  Done.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-16T10:37:27Z

- **apis/config/v1beta2/configuration_types.go:597** by [@kshalot](https://github.com/kshalot) — 2026-03-16T10:32:19Z
```diff
@@ -588,3 +592,14 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type VisibilityServerConfiguration struct {
+	// BindAddress is the IP address on which to listen for the visibility server.
```

  Nit: "listen for" sounds a bit backwards IMO, because it's the visibility server that is doing the listening. I'd propose:
  ```suggestion
  	// BindAddress is the IP address the visibility server listens on.
  ```
  
  Same for the port.

- **apis/config/v1beta2/configuration_types.go:111** by [@kshalot](https://github.com/kshalot) — 2026-03-16T10:33:15Z
```diff
@@ -105,6 +105,10 @@ type Configuration struct {
 	// of Kueue-managed objects. A nil value disables all automatic deletions.
 	// +optional
 	ObjectRetentionPolicies *ObjectRetentionPolicies `json:"objectRetentionPolicies,omitempty"`
+
+	// Visibility configures the visibility server.
+	// +optional
+	Visibility *VisibilityServerConfiguration `json:"visibility,omitempty"`
```

  Nit: I'd consider naming this `visibilityServer`. I feel like it's a bit less ambiguous? Because we are configuring the server specifically, not some general notion of "visibility" in Kueue.

- **keps/168-2-pending-workloads-visibility/README.md:240** by [@kshalot](https://github.com/kshalot) — 2026-03-16T10:34:51Z
```diff
@@ -227,6 +232,13 @@ The introduced API uses mechanism of subresources. It means, that in the future
 GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
 ```
 
+### Technical Details
+
+The Visibility server can be configured in the `kueueconfigurations` API under `visibility`. The following options are available:
+
+- `bindAddress`: The IP address on which to listen for the visibility server. If not specified, the server will listen on all available network interfaces.
+- `bindPort`: The port on which to listen for the visibility server. Defaults to `8082`.
```

  Similarly to my comment about the docstring, I'd reword this "listen for" to "listen on"

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T11:38:21Z

- **keps/168-2-pending-workloads-visibility/README.md:240** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T11:38:21Z
```diff
@@ -227,6 +232,13 @@ The introduced API uses mechanism of subresources. It means, that in the future
 GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
 ```
 
+### Technical Details
+
+The Visibility server can be configured in the `kueueconfigurations` API under `visibility`. The following options are available:
+
+- `bindAddress`: The IP address on which to listen for the visibility server. If not specified, the server will listen on all available network interfaces.
+- `bindPort`: The port on which to listen for the visibility server. Defaults to `8082`.
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T11:38:28Z

- **apis/config/v1beta2/configuration_types.go:111** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T11:38:28Z
```diff
@@ -105,6 +105,10 @@ type Configuration struct {
 	// of Kueue-managed objects. A nil value disables all automatic deletions.
 	// +optional
 	ObjectRetentionPolicies *ObjectRetentionPolicies `json:"objectRetentionPolicies,omitempty"`
+
+	// Visibility configures the visibility server.
+	// +optional
+	Visibility *VisibilityServerConfiguration `json:"visibility,omitempty"`
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T11:38:37Z

- **apis/config/v1beta2/configuration_types.go:597** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T11:38:36Z
```diff
@@ -588,3 +592,14 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type VisibilityServerConfiguration struct {
+	// BindAddress is the IP address on which to listen for the visibility server.
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T14:11:05Z

- **pkg/config/validation.go:576** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T14:10:53Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.VisibilityServer == nil || c.VisibilityServer.BindAddress == nil || *c.VisibilityServer.BindAddress == "" {
```

  I am not sure if an empty string for BindAddress is a valid case

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:44:28Z

- **pkg/config/validation.go:576** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:44:28Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.VisibilityServer == nil || c.VisibilityServer.BindAddress == nil || *c.VisibilityServer.BindAddress == "" {
```

  No strong view, but I would reject against such a value. Otherwise user may be thinking hey did something useful but Kueue is not reacting. It is always easier to relax validation later (than to add validation)

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-03-20T14:46:16Z

- **apis/config/v1beta2/configuration_types.go:627** by [@dkaluza](https://github.com/dkaluza) — 2026-03-20T14:15:22Z
```diff
@@ -618,3 +622,14 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type VisibilityServerConfiguration struct {
+	// BindAddress is the IP address the visibility server listens on.
```

  It would be good to specify what is the default behavior here also.
  
  Is this all network interfaces? Or just localhost? Or something else?

- **pkg/config/config_test.go:1125** by [@dkaluza](https://github.com/dkaluza) — 2026-03-20T14:32:18Z
```diff
@@ -1104,7 +1122,7 @@ webhook:
 
 			// Compare the loaded configuration
 			configCmpOpts := cmp.Options{
-				cmpopts.IgnoreFields(configapi.Configuration{}, "ControllerManager"),
+				cmpopts.IgnoreFields(configapi.Configuration{}, "ControllerManager", "VisibilityServer"),
```

  Why are we adding the "VisibilityServer" entries above if we are exludeing them from comparison anyway?
  
  For me cleanest solution would be to have actually just a separate test for it, but I know test tables are the preferred solution in golang. The disadvantage is that we are doing here like 20 modifications and I do not have a feeling those actually test anything more than the first addition in this table (even if we enable cmp of those).

- **pkg/config/validation.go:575** by [@dkaluza](https://github.com/dkaluza) — 2026-03-20T14:38:36Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
```

  What about port validation?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:46:50Z

- **test/e2e/customconfigs/visibility_server_test.go:242** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:46:50Z
```diff
@@ -229,17 +241,6 @@ var _ = ginkgo.Describe("Visibility Server", func() {
 		}
 		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
 
-		ginkgo.By("Updating the visibility-server deployment's port")
-		patchedDeployment := originalDeployment.DeepCopy()
-		for i, c := range patchedDeployment.Spec.Template.Spec.Containers {
-			if c.Name == "manager" {
-				container := &patchedDeployment.Spec.Template.Spec.Containers[i]
-				container.Args = append(c.Args, fmt.Sprintf("--visibility-server-port=%d", customVisibilityPort))
-			}
-		}
-		gomega.Expect(k8sClient.Update(ctx, patchedDeployment)).To(gomega.Succeed())
-		util.WaitForKueueAvailabilityNoRestartCountCheck(ctx, k8sClient)
-
```

  Why removing the test? Let's keep both tests for as long both mechanisms are supported

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T14:49:53Z

- **test/e2e/customconfigs/visibility_server_test.go:242** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T14:49:52Z
```diff
@@ -229,17 +241,6 @@ var _ = ginkgo.Describe("Visibility Server", func() {
 		}
 		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
 
-		ginkgo.By("Updating the visibility-server deployment's port")
-		patchedDeployment := originalDeployment.DeepCopy()
-		for i, c := range patchedDeployment.Spec.Template.Spec.Containers {
-			if c.Name == "manager" {
-				container := &patchedDeployment.Spec.Template.Spec.Containers[i]
-				container.Args = append(c.Args, fmt.Sprintf("--visibility-server-port=%d", customVisibilityPort))
-			}
-		}
-		gomega.Expect(k8sClient.Update(ctx, patchedDeployment)).To(gomega.Succeed())
-		util.WaitForKueueAvailabilityNoRestartCountCheck(ctx, k8sClient)
-
```

  Oh I understood your previous comment wrongly. We want to keep both mechanisms for now? So we should still allow users to define the visibility port through a flag?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:50:01Z

- **cmd/kueue/main.go:133** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:50:01Z
```diff
@@ -129,9 +129,6 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
-	var visibilityServerPort int
-	flag.IntVar(&visibilityServerPort, "visibility-server-port", 8082, "The port the visibility server binds to.")
```

  Oh I thought we are just deprecating - not removing completely in this release, but I'm ok to drop it actually if we don't have a use case. The only use case I see is to make the migration smooth - admin can upgrade Kueue first, and only then change configuration. If we just remove in 0.17 then user needs to upgrade and change the configuration as a single step.
  
  However, if you don't see this a problem, let's drop for simplicity.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T14:52:22Z

- **cmd/kueue/main.go:133** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T14:52:22Z
```diff
@@ -129,9 +129,6 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
-	var visibilityServerPort int
-	flag.IntVar(&visibilityServerPort, "visibility-server-port", 8082, "The port the visibility server binds to.")
```

  I see. So you would prefer keeping both options for now? And if that is the case, which one takes precedence on the other? I assume configuration API should override the flag?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:53:33Z

- **test/e2e/customconfigs/visibility_server_test.go:242** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:53:33Z
```diff
@@ -229,17 +241,6 @@ var _ = ginkgo.Describe("Visibility Server", func() {
 		}
 		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
 
-		ginkgo.By("Updating the visibility-server deployment's port")
-		patchedDeployment := originalDeployment.DeepCopy()
-		for i, c := range patchedDeployment.Spec.Template.Spec.Containers {
-			if c.Name == "manager" {
-				container := &patchedDeployment.Spec.Template.Spec.Containers[i]
-				container.Args = append(c.Args, fmt.Sprintf("--visibility-server-port=%d", customVisibilityPort))
-			}
-		}
-		gomega.Expect(k8sClient.Update(ctx, patchedDeployment)).To(gomega.Succeed())
-		util.WaitForKueueAvailabilityNoRestartCountCheck(ctx, k8sClient)
-
```

  Yeah this was the comment by @tenzen-y: https://github.com/kubernetes-sigs/kueue/pull/9863#discussion_r2947704051 "Let's try to expand Configuration field for the 0.17 release, then remove this flag in the future releaase (might be 0.18 or 0.19)"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:54:23Z

- **test/e2e/customconfigs/visibility_server_test.go:242** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:54:23Z
```diff
@@ -229,17 +241,6 @@ var _ = ginkgo.Describe("Visibility Server", func() {
 		}
 		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
 
-		ginkgo.By("Updating the visibility-server deployment's port")
-		patchedDeployment := originalDeployment.DeepCopy()
-		for i, c := range patchedDeployment.Spec.Template.Spec.Containers {
-			if c.Name == "manager" {
-				container := &patchedDeployment.Spec.Template.Spec.Containers[i]
-				container.Args = append(c.Args, fmt.Sprintf("--visibility-server-port=%d", customVisibilityPort))
-			}
-		}
-		gomega.Expect(k8sClient.Update(ctx, patchedDeployment)).To(gomega.Succeed())
-		util.WaitForKueueAvailabilityNoRestartCountCheck(ctx, k8sClient)
-
```

  So if not too much work I would keep both for 0.17. Configuration wins if specified. The flag is only respected if the configuration is absent.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:55:44Z

- **cmd/kueue/main.go:133** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:55:43Z
```diff
@@ -129,9 +129,6 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
-	var visibilityServerPort int
-	flag.IntVar(&visibilityServerPort, "visibility-server-port", 8082, "The port the visibility server binds to.")
```

  > I see. So you would prefer keeping both options for now?
  
  Yeah, again, no strong view, tbut I think this is more aligned with the deprecation policy - we keep both options supported for at least one release for the ease of migration.
  
  >  And if that is the case, which one takes precedence on the other? I assume configuration API should override the flag?
  
  I would say configuration wins, so that user can do the following upgrade steps:
  1. set the flag in 0.16
  2. upgrade to 0.17.0 - all continues to work
  3. modify configuration - all continues to work
  4. drop feature gate - all continues to work

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T14:58:19Z

- **pkg/config/validation.go:575** by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:58:18Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
```

  +1

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:08:05Z

- **pkg/config/config_test.go:1125** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:08:05Z
```diff
@@ -1104,7 +1122,7 @@ webhook:
 
 			// Compare the loaded configuration
 			configCmpOpts := cmp.Options{
-				cmpopts.IgnoreFields(configapi.Configuration{}, "ControllerManager"),
+				cmpopts.IgnoreFields(configapi.Configuration{}, "ControllerManager", "VisibilityServer"),
```

  If you look closely, the VisibilityServer entries are actually not being added to the wantConfiguration structs inside `TestTLSOptionsFeatureGate`. They are only added for the structs in `TestLoad` which tests that the configuration file is parsed correctly across all possible fields, including defaults.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:30:52Z

- **pkg/config/validation.go:575** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:30:52Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:31:18Z

- **apis/config/v1beta2/configuration_types.go:627** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:31:17Z
```diff
@@ -618,3 +622,14 @@ type WorkloadRetentionPolicy struct {
 	// +optional
 	AfterDeactivatedByKueue *metav1.Duration `json:"afterDeactivatedByKueue,omitempty"`
 }
+
+type VisibilityServerConfiguration struct {
+	// BindAddress is the IP address the visibility server listens on.
```

  This defaults to all network interfaces: https://github.com/kubernetes/apiserver/blob/master/pkg/server/options/serving.go#L120. Added the comment.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:31:30Z

- **pkg/config/validation.go:576** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:31:30Z
```diff
@@ -568,6 +571,17 @@ func validateTLS(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateVisibilityServer(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if c.VisibilityServer == nil || c.VisibilityServer.BindAddress == nil || *c.VisibilityServer.BindAddress == "" {
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:38:58Z

- **cmd/kueue/main.go:133** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:38:58Z
```diff
@@ -129,9 +129,6 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
-	var visibilityServerPort int
-	flag.IntVar(&visibilityServerPort, "visibility-server-port", 8082, "The port the visibility server binds to.")
```

  Done. Please let me know if the documentation is clear enough.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-20T15:39:06Z

- **test/e2e/customconfigs/visibility_server_test.go:242** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-20T15:39:06Z
```diff
@@ -229,17 +241,6 @@ var _ = ginkgo.Describe("Visibility Server", func() {
 		}
 		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
 
-		ginkgo.By("Updating the visibility-server deployment's port")
-		patchedDeployment := originalDeployment.DeepCopy()
-		for i, c := range patchedDeployment.Spec.Template.Spec.Containers {
-			if c.Name == "manager" {
-				container := &patchedDeployment.Spec.Template.Spec.Containers[i]
-				container.Args = append(c.Args, fmt.Sprintf("--visibility-server-port=%d", customVisibilityPort))
-			}
-		}
-		gomega.Expect(k8sClient.Update(ctx, patchedDeployment)).To(gomega.Succeed())
-		util.WaitForKueueAvailabilityNoRestartCountCheck(ctx, k8sClient)
-
```

  Done.

### Review by [@dkaluza](https://github.com/dkaluza) (approved) — 2026-03-23T12:53:49Z

Generally looks good to me, you probably have to regenerate some docs to fix the CI issues.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T17:38:26Z

- **site/content/en/docs/tasks/manage/monitor_pending_workloads/pending_workloads_on_demand.md:79** by [@mimowo](https://github.com/mimowo) — 2026-03-23T17:38:26Z
```diff
@@ -74,7 +74,9 @@ You can disable it by setting the `VisibilityOnDemand` feature gate. Check the [
 
 If you disable the feature, you also need to remove the associated `APIService` from your cluster by doing `kubectl delete APIService v1beta2.visibility.kueue.x-k8s.io`.
 
-If you need the embedded visibility server to run on a different port, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`.
+If you need the embedded visibility server to run on a different port, you can use the Configuration API `visibilityServer.bindPort`.
+
+Alternatively, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`. Keep in mind, this option will be deprecated soon.
```

  I think we should make it deprecated day one, so that we can remove in 0.18 or 0.19

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T17:39:42Z

- **site/content/en/docs/tasks/manage/monitor_pending_workloads/pending_workloads_on_demand.md:79** by [@mimowo](https://github.com/mimowo) — 2026-03-23T17:39:42Z
```diff
@@ -74,7 +74,9 @@ You can disable it by setting the `VisibilityOnDemand` feature gate. Check the [
 
 If you disable the feature, you also need to remove the associated `APIService` from your cluster by doing `kubectl delete APIService v1beta2.visibility.kueue.x-k8s.io`.
 
-If you need the embedded visibility server to run on a different port, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`.
+If you need the embedded visibility server to run on a different port, you can use the Configuration API `visibilityServer.bindPort`.
+
+Alternatively, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`. Keep in mind, this option will be deprecated soon.
```

  So I would say Alternatively, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`.However, this option is deprecated as of 0.17 and will be removed in the future.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-23T17:46:03Z

- **site/content/en/docs/tasks/manage/monitor_pending_workloads/pending_workloads_on_demand.md:79** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-23T17:46:03Z
```diff
@@ -74,7 +74,9 @@ You can disable it by setting the `VisibilityOnDemand` feature gate. Check the [
 
 If you disable the feature, you also need to remove the associated `APIService` from your cluster by doing `kubectl delete APIService v1beta2.visibility.kueue.x-k8s.io`.
 
-If you need the embedded visibility server to run on a different port, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`.
+If you need the embedded visibility server to run on a different port, you can use the Configuration API `visibilityServer.bindPort`.
+
+Alternatively, you can use the `--visibility-server-port` flag when running the Kueue controller manager. For example: `--visibility-server-port=8443`. Keep in mind, this option will be deprecated soon.
```

  Thanks, I will open a PR for the updates of the docs.
