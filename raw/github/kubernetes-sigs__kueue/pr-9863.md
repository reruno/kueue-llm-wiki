# PR #9863: Allow custom port for visibility server

**Summary**: Allow custom port for visibility server

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9863

**Last updated**: 2026-03-18T09:57:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Nilsachy](https://github.com/Nilsachy)
- **Branch**: `allow-custom-port-for-visibility-server` → `main`
- **Created**: 2026-03-13T13:16:50Z
- **Updated**: 2026-03-18T09:57:31Z
- **Merged**: 2026-03-17T16:23:45Z
- **Closed**: 2026-03-17T16:23:45Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@kshalot](https://github.com/kshalot), [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 5   **+105 / -45**

## Description

**What type of PR is this?**
/kind feature

**What this PR does / why we need it:**
Allow to pass custom port to the visibility server

**Which issue(s) this PR fixes:**
Fixes https://github.com/kubernetes-sigs/kueue/issues/9650

**Does this PR introduce a user-facing change?**

```release-note
VisibilityOnDemand: Introduce a new Kueue deployment argument, --visibility-server-port, which allows passing custom port when starting the visibility server.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-13T13:16:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f5be7aa7a283068423e826b5ee5ce6855176d391 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b95feab650e60008c22b43 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-13T13:17:01Z

Hi @Nilsachy. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-13T13:25:57Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-13T13:45:00Z

nit, added `release-note` to follow the 3 quotes in the PR description so that the robot stops complaining about the missing release note

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T14:17:10Z

/retest

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-13T15:02:17Z

QQ: What is our strategy with this and https://github.com/kubernetes-sigs/kueue/pull/9850? Do we want both? One for cherry-pick, other for full release?

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-13T15:20:35Z

> QQ: What is our strategy with this and #9850? Do we want both? One for cherry-pick, other for full release?

That's right, this is the strategy.

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-15T17:58:20Z

/retest

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T11:32:36Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T11:42:24Z

LGTM overall, thanks 👍 Once merged please follow up with the Configuration API extension, and we will deprecate the secure-port going forward.

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-16T11:45:43Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T11:45:51Z

LGTM label has been added.  <details>Git tree hash: 84b7986ee46d69030b7f5c98116220a362790f2a</details>

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-16T13:04:29Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T13:04:36Z

LGTM label has been added.  <details>Git tree hash: 2705a27703f156f1971155911177d15929b7fa32</details>

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T14:34:47Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-17T14:52:04Z

> /hold

why on hold?

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T15:11:19Z

> > /hold
> 
> why on hold?

I tested a negative case where `visibility-server-port` is missing from the command args and the test still passes which shouldn't be the case. I'd like to investigate that first.

### Comment by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T15:30:04Z

False alarm. Test works as expected.

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-17T15:33:47Z

Thank you 👍 
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-17T15:33:51Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9863#issuecomment-4075897171):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T15:33:55Z

LGTM label has been added.  <details>Git tree hash: 49c1d5c774f496c20d3ada02762fbf0843427a58</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T15:39:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9863#pullrequestreview-3953170760" title="Approved">dkaluza</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9863#issuecomment-4075897171" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9863#" title="Author self-approved">Nilsachy</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9863#pullrequestreview-3961782776" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-17T16:24:26Z

@mimowo: #9863 failed to apply on top of branch "release-0.16":
```
Applying: feat(visibility): add custom port option for visibility server
Using index info to reconstruct a base tree...
M	cmd/kueue/main.go
M	pkg/visibility/server.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/visibility/server.go
Auto-merging cmd/kueue/main.go
CONFLICT (content): Merge conflict in cmd/kueue/main.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 feat(visibility): add custom port option for visibility server

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9863#issuecomment-4075897171):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-17T16:25:01Z

@mimowo: #9863 failed to apply on top of branch "release-0.15":
```
Applying: feat(visibility): add custom port option for visibility server
Using index info to reconstruct a base tree...
M	cmd/kueue/main.go
M	pkg/visibility/server.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/visibility/server.go
CONFLICT (content): Merge conflict in pkg/visibility/server.go
Auto-merging cmd/kueue/main.go
CONFLICT (content): Merge conflict in cmd/kueue/main.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 feat(visibility): add custom port option for visibility server

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9863#issuecomment-4075897171):

>Thank you 👍 
>/lgtm
>/approve
>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T17:56:24Z

@Nilsachy could you manually cherry pick this PR to the released branches?

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (changes_requested) — 2026-03-13T15:25:49Z

- **test/e2e/customconfigs/visibility_kubeconfig_test.go:51** by [@kannon92](https://github.com/kannon92) — 2026-03-13T15:25:37Z
```diff
@@ -35,26 +37,32 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	visibilityServerPort      = 9444
```

  I would like to see us keep the default and maybe have this change under a separate test case.
  
  We now are no longer testing the default case with this change.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-15T17:57:45Z

- **test/e2e/customconfigs/visibility_kubeconfig_test.go:51** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-15T17:57:45Z
```diff
@@ -35,26 +37,32 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	visibilityServerPort      = 9444
```

  Refactored current test setup and add separate test.

### Review by [@dkaluza](https://github.com/dkaluza) (approved) — 2026-03-16T11:23:58Z

LGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T11:39:26Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@mimowo](https://github.com/mimowo) — 2026-03-16T11:39:26Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  Why DeferCleanup for BeforeEach, I think it is more idiomatic to cleanup in AfterEach for symmetry

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T11:40:57Z

- **test/e2e/customconfigs/visibility_server_test.go:233** by [@mimowo](https://github.com/mimowo) — 2026-03-16T11:40:57Z
```diff
@@ -195,6 +217,35 @@ var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
 			g.Expect(pw).NotTo(gomega.BeNil())
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should use the custom port from the --visibility-secure-port flag", func() {
+		ginkgo.By("Updating the visibility-server service's targetPort")
+		patchedService := originalService.DeepCopy()
+		for i, p := range patchedService.Spec.Ports {
+			if p.Name == "https" {
+				patchedService.Spec.Ports[i].TargetPort = intstr.FromInt32(customVisibilityPort)
+			}
+		}
+		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
+
+		patchedDeployment := originalDeployment.DeepCopy()
```

  Add ginkgo.By for the step of patching the Deployment

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-16T11:45:35Z

- **cmd/kueue/main.go:132** by [@kshalot](https://github.com/kshalot) — 2026-03-16T11:39:22Z
```diff
@@ -129,6 +129,9 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
+	var visibilitySecurePort int
```

  Nit: Maybe `visibilityServerSecurePort`? Also maybe you could drop "secure", if we want to be specific then it would be `HttpsPort`. But this is super nitty. Or just `visibilityServerPort` (because there is no such thing as a "secure" port).

- **site/content/en/docs/tasks/manage/monitor_pending_workloads/pending_workloads_on_demand.md:77** by [@kshalot](https://github.com/kshalot) — 2026-03-16T11:41:03Z
```diff
@@ -74,6 +74,8 @@ You can disable it by setting the `VisibilityOnDemand` feature gate. Check the [
 
 If you disable the feature, you also need to remove the associated `APIService` from your cluster by doing `kubectl delete APIService v1beta2.visibility.kueue.x-k8s.io`.
 
+If you need the embedded visibility server to run on a different port, you can use the `--visibility-secure-port` flag when running the Kueue controller manager. For example: `--visibility-secure-port=8443`.
```

  QQ: Since the dedicated API is coming (as far as I understand), do we want to "advertise" the flag in the documentation? In terms of reduction of potential integrations using this temporary solution.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T11:49:23Z

- **site/content/en/docs/tasks/manage/monitor_pending_workloads/pending_workloads_on_demand.md:77** by [@mimowo](https://github.com/mimowo) — 2026-03-16T11:49:23Z
```diff
@@ -74,6 +74,8 @@ You can disable it by setting the `VisibilityOnDemand` feature gate. Check the [
 
 If you disable the feature, you also need to remove the associated `APIService` from your cluster by doing `kubectl delete APIService v1beta2.visibility.kueue.x-k8s.io`.
 
+If you need the embedded visibility server to run on a different port, you can use the `--visibility-secure-port` flag when running the Kueue controller manager. For example: `--visibility-secure-port=8443`.
```

  I'm ok to mention it, people who need it, they will find it anyway. With the docs we can already have a spot to update when the Configuration APi is ready.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T12:52:41Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T12:52:40Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  `AfterEach` does provide visual symmetry, but `DeferCleanup` seems to be a better choice here:
  
  **Scoping & Encapsulation:** It allows us to keep variables (like cq, sa, etc.) locally scoped within the block. `AfterEach` would require us to move these to the outer Describe scope, defining variables used only for cleanup.
  
  **Registration Safety:** `DeferCleanup` is only registered if the code execution actually reaches that line. This prevents the test from attempting to clean up resources that were never created due to a mid-setup failure.
  
  **Automatic LIFO:** It naturally follows Last-In-First-Out (LIFO) order, which IMO is safer for handling dependent resources (e.g., deleting a Pod before its Namespace).
  
  Does that make sense or do you still prefer the use of `AfterEach` block instead?

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T12:55:06Z

- **test/e2e/customconfigs/visibility_server_test.go:233** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T12:55:06Z
```diff
@@ -195,6 +217,35 @@ var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
 			g.Expect(pw).NotTo(gomega.BeNil())
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should use the custom port from the --visibility-secure-port flag", func() {
+		ginkgo.By("Updating the visibility-server service's targetPort")
+		patchedService := originalService.DeepCopy()
+		for i, p := range patchedService.Spec.Ports {
+			if p.Name == "https" {
+				patchedService.Spec.Ports[i].TargetPort = intstr.FromInt32(customVisibilityPort)
+			}
+		}
+		gomega.Expect(k8sClient.Update(ctx, patchedService)).To(gomega.Succeed())
+
+		patchedDeployment := originalDeployment.DeepCopy()
```

  Done.

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-16T13:05:12Z

- **cmd/kueue/main.go:132** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-16T13:05:12Z
```diff
@@ -129,6 +129,9 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
+	var visibilitySecurePort int
```

  Used visibilityServerPort instead

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T13:20:58Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@mimowo](https://github.com/mimowo) — 2026-03-16T13:20:57Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  > Scoping & Encapsulation: It allows us to keep variables (like cq, sa, etc.) locally scoped within the block. AfterEach would require us to move these to the outer Describe scope, defining variables used only for cleanup.
  
  That is a good point, nd I'm all for the encapsulation, but I feel some disconnect as the code declares many consts in the global scope, eg `customVisibilityPort`, or `kueueVisibilityServerName`. So it feels like a half-done effort. Let me know if I'm missing something.
  
  AI: please double check all global consts if you can restrict their scope after the change.
  
  > Registration Safety: DeferCleanup is only registered if the code execution actually reaches that line. This prevents the test from attempting to clean up resources that were never created due to a mid-setup failure.
  
  I don't buy this. AfterEach also executes regardless of failure / success.
  
  > Automatic LIFO: It naturally follows Last-In-First-Out (LIFO) order, which IMO is safer for handling dependent resources (e.g., deleting a Pod before its Namespace).
  
  Sort of, but it rarely makes a difference. If the order of deletion makes a difference it is usually a sign of some bugs. For Deleting Pods and Namespaces it makes a difference, but we have a helper for that `util.DeleteNamespace` (which is probably badly named).

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-17T10:59:35Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T10:59:34Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  But IIUC correctly, `AfterEach` is always executed (unlike `DeferCleanup` which is only executed if we reach that line). So would you then prefer the use of `AfterEach` with non-nil checks?
  
  Something like this:
  ```
          if cq != nil {
  			ginkgo.By("Cleaning up cluster queue")
  			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
  			cq = nil
  		}
  		if sa != nil {
  			ginkgo.By("Cleaning up custom SA")
  			util.ExpectObjectToBeDeleted(ctx, k8sClient, sa, true)
  			sa = nil
  		}
  		if secret != nil {
  			ginkgo.By("Cleaning up secret")
  			util.ExpectObjectToBeDeleted(ctx, k8sClient, secret, true)
  			secret = nil
  		}
  		if cm != nil {
  			ginkgo.By("Cleaning up config map")
  			util.ExpectObjectToBeDeleted(ctx, k8sClient, cm, true)
  			cm = nil
  		}
  ```
  
  IMO it is less readable, but let me know and of course we can update this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T11:01:08Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@mimowo](https://github.com/mimowo) — 2026-03-17T11:01:07Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  I don't like the checks, but I was pretty sure the checks are done internally https://github.com/kubernetes-sigs/kueue/blob/612c05549532ebe17d1b7b9c098f798064a497a3/test/util/util.go#L145-L147

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-17T14:06:58Z

- **test/e2e/customconfigs/visibility_server_test.go:72** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T14:06:58Z
```diff
@@ -35,29 +37,65 @@ import (
 )
 
 const (
-	kubeSystemNamespace = "kube-system"
-	kueueManagerName    = "kueue-controller-manager"
-	configMapName       = "visibility-kubeconfig-test"
-	customSAName        = "visibility-test-sa"
-	customSecretName    = "visibility-test-sa-token"
-	testLabelKey        = "visibility-test-rbac"
-	testLabelValue      = "true"
-	kubeconfigVolName   = "kubeconfig-vol"
-	customSAVolName     = "custom-sa-vol"
-	customSAMountPath   = "/etc/custom-sa"
-	cqName              = "test-kubeconfig-cq"
+	kubeSystemNamespace       = "kube-system"
+	kueueManagerName          = "kueue-controller-manager"
+	kueueVisibilityServerName = "kueue-visibility-server"
+	configMapName             = "visibility-kubeconfig-test"
+	customSAName              = "visibility-test-sa"
+	customSecretName          = "visibility-test-sa-token"
+	testLabelKey              = "visibility-test-rbac"
+	testLabelValue            = "true"
+	kubeconfigVolName         = "kubeconfig-vol"
+	customSAVolName           = "custom-sa-vol"
+	customSAMountPath         = "/etc/custom-sa"
+	cqName                    = "test-kubeconfig-cq"
+	customVisibilityPort      = 9444
 )
 
-var _ = ginkgo.Describe("Visibility Server KubeConfig flag with RBAC", func() {
+var _ = ginkgo.Describe("Visibility Server", func() {
 	var originalDeployment appsv1.Deployment
+	var originalService corev1.Service
 
 	ginkgo.BeforeEach(func() {
 		err := k8sClient.Get(ctx, types.NamespacedName{Name: kueueManagerName, Namespace: kueueNS}, &originalDeployment)
 		gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
+		err = k8sClient.Get(ctx, types.NamespacedName{Name: kueueVisibilityServerName, Namespace: kueueNS}, &originalService)
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		ginkgo.By("Creating a ClusterQueue")
+		cq := &kueue.ClusterQueue{
+			ObjectMeta: metav1.ObjectMeta{Name: cqName},
+		}
+		util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
+
+		ginkgo.DeferCleanup(func() {
```

  Done. In fact custom SA, secret, and configmaps are only specific to the RBAC (previous) test so this can be cleaned up separately.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-03-17T15:39:02Z

LGTM, thank you!
/lgtm
/approve

- **cmd/kueue/main.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-17T15:38:44Z
```diff
@@ -129,6 +129,9 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
+	var visibilityServerPort int
```

  Port-specific flag sounds good to me as well.
  Let's try to expand Configuration field for the 0.17 release, then remove this flag in the future releaase (might be 0.18 or 0.19)

### Review by [@Nilsachy](https://github.com/Nilsachy) (commented) — 2026-03-17T15:39:59Z

- **cmd/kueue/main.go:132** by [@Nilsachy](https://github.com/Nilsachy) — 2026-03-17T15:39:58Z
```diff
@@ -129,6 +129,9 @@ func main() {
 	var featureGates string
 	flag.StringVar(&featureGates, "feature-gates", "", "A set of key=value pairs that describe feature gates for alpha/experimental features.")
 
+	var visibilityServerPort int
```

  Once this is merged I will pick up #9850

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-18T09:57:31Z

/lgtm
