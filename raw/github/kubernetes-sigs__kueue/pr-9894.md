# PR #9894: Fix limits only issue code

**Summary**: Fix limits only issue code

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9894

**Last updated**: 2026-03-16T21:16:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-limits-only-issue-code` → `main`
- **Created**: 2026-03-16T11:57:21Z
- **Updated**: 2026-03-16T21:16:59Z
- **Merged**: 2026-03-16T21:15:39Z
- **Closed**: 2026-03-16T21:15:39Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+34 / -3**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9889

#### Special notes for your reviewer:

See PR reproducing the problem: https://github.com/kubernetes-sigs/kueue/pull/9893

Example failure: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9893/pull-kueue-test-integration-baseline-main/2033626544118697984

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scheduling: fix the issue that scheduler could indefinitely try re-queueing a workload which was once 
inadmissible, but is admissible after an update. The issue affected workloads which don't specify 
resource requests explicitly, but rely on defaulting based on limits.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-16T11:57:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e9fbc3987fcfb38b603e4991388fa5d37342b624 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b8654993e24400086ecd17 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T19:31:43Z

/retitle Fix limits only issue code

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-16T19:37:52Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T19:37:56Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9894#issuecomment-4070110730):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T21:14:22Z

LGTM label has been added.  <details>Git tree hash: 2a84e6de610e92ea119e04ddb65749c6bcc6e115</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-16T21:14:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9894#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9894#pullrequestreview-3956696911" title="LGTM">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T21:16:22Z

@mimowo: new pull request created: #9912

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9894#issuecomment-4070110730):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-16T21:16:59Z

@mimowo: new pull request created: #9913

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/9894#issuecomment-4070110730):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-03-16T20:13:13Z

Nice fix! Thanks for handling that.
two nits but overall LGTM

- **test/integration/singlecluster/tas/tas_test.go:590** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-16T20:09:51Z
```diff
@@ -576,6 +575,37 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("Should admit a previously inadmissible limits-only workload after another workload finishes", func() {
+				ginkgo.By("Creating a blocker workload and waiting until it reserves most of the available GPU quota")
+				blocker := utiltestingapi.MakeWorkload("blocker", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 4).
+					PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Obj()).Request(corev1.ResourceCPU, "1").Obj()
+				util.MustCreate(ctx, k8sClient, blocker)
+
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, blocker)
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+
+				ginkgo.By("Creating a second workload with only GPU limits so it becomes inadmissible while the blocker holds quota")
+				limitsOnly := utiltestingapi.MakeWorkload("limits-pnly", ns.Name).
```

  `s/limits-pnly/limits-only`

- **test/integration/singlecluster/tas/tas_test.go:589** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-03-16T20:12:26Z
```diff
@@ -576,6 +575,37 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("Should admit a previously inadmissible limits-only workload after another workload finishes", func() {
+				ginkgo.By("Creating a blocker workload and waiting until it reserves most of the available GPU quota")
+				blocker := utiltestingapi.MakeWorkload("blocker", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 4).
+					PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Obj()).Request(corev1.ResourceCPU, "1").Obj()
+				util.MustCreate(ctx, k8sClient, blocker)
+
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, blocker)
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+
+				ginkgo.By("Creating a second workload with only GPU limits so it becomes inadmissible while the blocker holds quota")
```

  Not a blocker but it shouldn't use GPU in the description as it uses `corev1.ResourceCPU`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T20:15:45Z

- **test/integration/singlecluster/tas/tas_test.go:589** by [@mimowo](https://github.com/mimowo) — 2026-03-16T20:15:45Z
```diff
@@ -576,6 +575,37 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("Should admit a previously inadmissible limits-only workload after another workload finishes", func() {
+				ginkgo.By("Creating a blocker workload and waiting until it reserves most of the available GPU quota")
+				blocker := utiltestingapi.MakeWorkload("blocker", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 4).
+					PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Obj()).Request(corev1.ResourceCPU, "1").Obj()
+				util.MustCreate(ctx, k8sClient, blocker)
+
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, blocker)
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+
+				ginkgo.By("Creating a second workload with only GPU limits so it becomes inadmissible while the blocker holds quota")
```

  Yeah, I was thinking about that, but the mechanics is the same, so I went for reusing the pre-existing `ginkgo.When`. I'm happy to update if you recommend so, just explaining my reasoning.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T20:16:21Z

- **test/integration/singlecluster/tas/tas_test.go:590** by [@mimowo](https://github.com/mimowo) — 2026-03-16T20:16:20Z
```diff
@@ -576,6 +575,37 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("Should admit a previously inadmissible limits-only workload after another workload finishes", func() {
+				ginkgo.By("Creating a blocker workload and waiting until it reserves most of the available GPU quota")
+				blocker := utiltestingapi.MakeWorkload("blocker", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 4).
+					PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Obj()).Request(corev1.ResourceCPU, "1").Obj()
+				util.MustCreate(ctx, k8sClient, blocker)
+
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, blocker)
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+
+				ginkgo.By("Creating a second workload with only GPU limits so it becomes inadmissible while the blocker holds quota")
+				limitsOnly := utiltestingapi.MakeWorkload("limits-pnly", ns.Name).
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-16T20:17:26Z

- **test/integration/singlecluster/tas/tas_test.go:589** by [@mimowo](https://github.com/mimowo) — 2026-03-16T20:17:26Z
```diff
@@ -576,6 +575,37 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				}
 			})
 
+			ginkgo.It("Should admit a previously inadmissible limits-only workload after another workload finishes", func() {
+				ginkgo.By("Creating a blocker workload and waiting until it reserves most of the available GPU quota")
+				blocker := utiltestingapi.MakeWorkload("blocker", ns.Name).
+					Queue(kueue.LocalQueueName(localQueue.Name)).PodSets(*utiltestingapi.MakePodSet("worker", 4).
+					PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+					Obj()).Request(corev1.ResourceCPU, "1").Obj()
+				util.MustCreate(ctx, k8sClient, blocker)
+
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, clusterQueue.Name, blocker)
+				util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
+
+				ginkgo.By("Creating a second workload with only GPU limits so it becomes inadmissible while the blocker holds quota")
```

  For now I just removed "GPU" from the ginkgo.By

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (approved) — 2026-03-16T21:14:14Z

/lgtm
