# PR #9935: cleanup: move metric reporting from workload finish function

**Summary**: cleanup: move metric reporting from workload finish function

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9935

**Last updated**: 2026-03-17T11:27:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `cleanup/move-lq-metrics-from-evict-finish` → `main`
- **Created**: 2026-03-17T08:50:03Z
- **Updated**: 2026-03-17T11:27:34Z
- **Merged**: 2026-03-17T11:11:39Z
- **Closed**: 2026-03-17T11:11:39Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow)
- **Changed files**: 10   **+29 / -23**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Move metrics reporting from Evict and Finish functions to simplify the configuration introduction in #9371

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8830

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-17T08:50:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1ec6a3ab3f92d13582dd05d7fe9198c727787e94 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69b91db8edb9c1000885eced |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-03-17T09:05:57Z

Looks like you covered all the places for Finish and Evict.
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T09:06:05Z

LGTM label has been added.  <details>Git tree hash: e29fde733aa303ed1e4871b00f7e6f4b7e5e0fcd</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T09:12:17Z

/hold

As I mentioned here https://github.com/kubernetes-sigs/kueue/pull/9701#issuecomment-4073407348. I'm not sure about this changes.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T09:37:53Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T09:38:02Z

LGTM label has been added.  <details>Git tree hash: f72fbb7be29fb9ff9d86daf88f47e48c1b2a3ede</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T09:38:23Z

/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-17T11:09:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9935#pullrequestreview-3959960694" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9935#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T09:13:42Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T09:13:42Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  I'm not sure I understand the approach, I was expecting changing the metrics on the event handlers for workloads, rather than post every Evict call. 
  
  wdyt @mbobrovskyi ?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-17T09:15:22Z

- **pkg/controller/core/workload_controller.go:714** by [@mykysha](https://github.com/mykysha) — 2026-03-17T09:15:22Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  For the Evict metrics it was not possible to do that way, as we have underlyingCause which is not easily retrieved in the handler. However, the approach was possible for the Finish function. Readjusting the PR now

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-17T09:29:36Z

- **pkg/controller/core/workload_controller.go:944** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-17T09:29:36Z
```diff
@@ -938,6 +938,11 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 		if !active {
 			log.V(2).Info("Workload will not be queued because the workload is not active")
 		}
+
+		if status == workload.StatusFinished && prevStatus != workload.StatusFinished {
+			r.reportFinishedWorkload(wlCopy)
+		}
```

  Yeah, this approach is much better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T09:38:58Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T09:38:58Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  hm, I thought we could read the underlying cause feom the secondary condition, no? 
  
  For example I thought we would have the PodsReady condition where we can find details

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-17T10:09:12Z

- **pkg/controller/core/workload_controller.go:714** by [@mykysha](https://github.com/mykysha) — 2026-03-17T10:09:12Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  We can, but the reason in condition which contains underlying cause stores it in the form of ReasonDueToUnderlyingCause. So it is possible to obtain, but we would need to split the condition in question. Would that be ok?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T10:14:02Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T10:14:02Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Can you provide an example (yaml snippet) of the relevant conditions? I thought no parsing would be involved.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-17T10:27:13Z

- **pkg/controller/core/workload_controller.go:714** by [@mykysha](https://github.com/mykysha) — 2026-03-17T10:27:13Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Not yaml, but from the test, as an example:
  ```go
  metav1.Condition{
  	Type:    kueue.WorkloadEvicted,
  	Status:  metav1.ConditionTrue,
  	Reason:  "DeactivatedDueToRequeuingLimitExceeded",
  	Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
  }
  ```
  
  Here the Reason would be actual reason (Deactivated) and underlying cause (RequeuingLimitExceeded) merged together

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T10:39:44Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T10:39:44Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Is there no "PodsReady" condition wit the info?
  give me all conditions

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-17T10:48:54Z

- **pkg/controller/core/workload_controller.go:714** by [@mykysha](https://github.com/mykysha) — 2026-03-17T10:48:54Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Surely, again, conditions from the same test:
  ```go
  metav1.Condition{
  	Type:    kueue.WorkloadPodsReady,
  	Status:  metav1.ConditionFalse,
  	Reason:  kueue.WorkloadWaitForStart,
  	Message: "Not all pods are ready or succeeded",
  },
  metav1.Condition{
  	Type:    kueue.WorkloadQuotaReserved,
  	Status:  metav1.ConditionFalse,
  	Reason:  "Pending",
  	Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
  },
  metav1.Condition{
  	Type:    kueue.WorkloadEvicted,
  	Status:  metav1.ConditionTrue,
  	Reason:  "DeactivatedDueToRequeuingLimitExceeded",
  	Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
  },
  metav1.Condition{
  	Type:    kueue.WorkloadAdmitted,
  	Status:  metav1.ConditionFalse,
  	Reason:  "NoReservation",
  	Message: "The workload has no reservation",
  },
  metav1.Condition{
  	Type:    podcontroller.WorkloadWaitingForReplacementPods,
  	Status:  metav1.ConditionTrue,
  	Reason:  "DeactivatedDueToRequeuingLimitExceeded",
  	Message: "The workload is deactivated due to exceeding the maximum number of re-queuing retries",
  }
  ```
  Here, underlyingCause being RequeuingLimitExceeded is not present separately

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T10:54:55Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T10:54:55Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Thank you, ok, so I see in that case the underlyingCause is `RequeuingLimitExceeded`, not `WaitForStart` as I initially thought based on metrics: https://github.com/kubernetes-sigs/kueue/blob/612c05549532ebe17d1b7b9c098f798064a497a3/pkg/metrics/metrics.go#L405

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T10:57:42Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T10:57:42Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Ahhh, I see, this is because we don't have the "Deactivated" condition. I think we should have it unlimately.
  
  However, as we don't have it currently I think it is ok to strip the prefix `DeactivatedDueTo` and use the suffix as the underlying cause in this case. Not very pretty, but I think it's better than duplicating the code.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-17T11:03:24Z

- **pkg/controller/core/workload_controller.go:714** by [@mykysha](https://github.com/mykysha) — 2026-03-17T11:03:24Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  I suppose it might be better to split by DueTo once, with the first part being the reason and the second underlying cause, not to hard-fix on the Deactivated field

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T11:06:31Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T11:06:31Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  sgtm, just split by the first DueTo, to make sure not to split if the reason has DueTo inside

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T11:07:49Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T11:07:49Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  Ok, let's move refactor of Evict to separete PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T11:07:57Z

- **pkg/controller/core/workload_controller.go:714** by [@mimowo](https://github.com/mimowo) — 2026-03-17T11:07:57Z
```diff
@@ -705,7 +708,11 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		}
 		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
 		err := workload.Evict(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.clock, r.roleTracker, r.customLabels)
-		return true, err
+		if err != nil {
+			return true, err
+		}
+		workload.ReportEvictionMetrics(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped", "", r.roleTracker, r.customLabels)
```

  This PR already looks good

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-17T11:09:22Z

/approve
Thank you 👍
