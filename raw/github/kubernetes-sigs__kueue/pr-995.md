# PR #995: Support kubeflow.org/pytorchjob

**Summary**: Support kubeflow.org/pytorchjob

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/995

**Last updated**: 2023-08-16T12:31:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `support-kubeflow-pytorchjob` → `main`
- **Created**: 2023-07-18T06:47:37Z
- **Updated**: 2023-08-16T12:31:33Z
- **Merged**: 2023-08-16T12:28:26Z
- **Closed**: 2023-08-16T12:28:26Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 27   **+1776 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:
Support kubeflow.org/pytorchjob.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part-of #297

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support kubeflow.org/pytorchjob
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-18T06:47:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 775ca92cf9db57bda02aa77fb46b7dbdfe654b59 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64db11c388c0cb00082002b3 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-18T16:02:37Z

Blocked on https://github.com/kubeflow/training-operator/pull/1859.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-18T16:08:44Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-18T16:14:49Z

/assign @trasc

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T06:49:25Z

> The generic approach around pkg/controller/jobs/kubeflow/kubeflowjob makes the implementation a bit hard to follow. Are we planing to support other similar kubeflow jobs in the near future?

@trasc Yes. We will support TFJob, MXJob, XGboostJob, and PaddleJob.
cc: @alculquicondor

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-07-19T06:53:52Z

/cc

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T15:14:32Z

I'll get back to this after @trasc gives an LGTM.
I think he's out this week.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-01T17:18:02Z

Sorry, I've been away from this PR for a while.
I will move this forward in the next week since I'm busy now due to Kubeflow code freeze.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T07:31:25Z

@trasc This PR is ready for review. PTAL.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T11:02:16Z

/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-14T17:58:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/995#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T19:01:50Z

The kueue-controller-manager seems suddenly can not access the kube-apiserver:
https://storage.googleapis.com/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/995/pull-kueue-test-e2e-main-1-24/1691153180097777664/artifacts/run-test-e2e-1.24.15/kueue-controller-manager.log
/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-15T05:49:25Z

Squashed.

### Comment by [@trasc](https://github.com/trasc) — 2023-08-16T06:04:11Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T12:16:10Z

/release-note-edit
```release-note
Support kubeflow.org/pytorchjob
```
/hold cancel

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-19T06:46:51Z

The generic approach around `pkg/controller/jobs/kubeflow/kubeflowjob` makes the implementation a bit hard to follow. Are we planing to support other similar kubeflow jobs in the near future?

- **config/components/manager/controller_manager_config.yaml:37** by [@trasc](https://github.com/trasc) — 2023-07-19T05:25:21Z
```diff
@@ -32,3 +32,4 @@ integrations:
   - "kubeflow.org/mpijob"
   - "ray.io/rayjob"
   - "jobset.x-k8s.io/jobset"
+  - "kubeflow.org/pytorchjob"
```

  The same should be done in `charts/kueue/values.yaml`
  
  https://github.com/kubernetes-sigs/kueue/blob/f215a43a7be9b3c2788e00ec03c130b8fbc053b5/charts/kueue/values.yaml#L84-L88

- **config/components/rbac/role.yaml:134** by [@trasc](https://github.com/trasc) — 2023-07-19T05:32:03Z
```diff
@@ -122,6 +122,23 @@ rules:
   verbs:
   - get
   - update
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs
+  verbs:
+  - get
+  - list
+  - patch
+  - update
+  - watch
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs/status
+  verbs:
+  - get
+  - update
```

  two extra role files should be created `config/components/rbac/pytorchjob_editor_role.yaml` and  `config/components/rbac/pytorchjob_viewer_role.yaml` (check the content of `config/components/rbac/jobset_XXX_role.yaml`  ) and referenced in `config/components/rbac/kustomization.yaml`

- **go.mod:89** by [@trasc](https://github.com/trasc) — 2023-07-19T05:43:24Z
```diff
@@ -83,3 +85,5 @@ require (
 	sigs.k8s.io/json v0.0.0-20221116044647-bc3834ca7abd // indirect
 	sigs.k8s.io/yaml v1.3.0 // indirect
 )
+
+replace github.com/kubeflow/training-operator v1.6.0 => github.com/tenzen-y/training-operator v1.3.0-rc.1.0.20230717233919-1ed3e8e55322
```

  we should drop the replace and either:
  
  a. Hold this PR until kubeflow/training-operator#1859 is merged and a new kubeflow/training-operator is available containing it.
  b.  Hold this PR until kubeflow/training-operator#1859 is merged, use a non-release version of `github.com/kubeflow/training-operator v1.7.0-xxx-xxxx`  and create a Followup pr to switch to a release version when available.

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-19T06:11:47Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  If you make `Object()` part of the `KFJobControl` you could remove the `kftraining.PyTorchJob` member,  and potentially change this  to :
  
  ```suggestion
  type PyTorchJob kubeflowjob.KubeflowJob
  ```

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:108** by [@trasc](https://github.com/trasc) — 2023-07-19T06:21:33Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
+
+var _ jobframework.GenericJob = (*PyTorchJob)(nil)
+
+func fromObject(o runtime.Object) *PyTorchJob {
+	pytorchJobObj := o.(*kftraining.PyTorchJob)
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}
+
+func (j *PyTorchJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *PyTorchJob) Object() client.Object {
+	return j.PyTorchJob
+}
+
+// PriorityClass calculates the priorityClass name needed for workload according to the following priorities:
+//  1. .spec.runPolicy.schedulingPolicy.priorityClass
+//  2. .spec.replicaSpecs[Master].template.spec.priorityClassName
+//  3. .spec.replicaSpecs[Worker].template.spec.priorityClassName
+//
+// This function is inspired by an analogous one in mpi-controller:
+// https://github.com/kubeflow/mpi-operator/blob/5946ef4157599a474ab82ff80e780d5c2546c9ee/pkg/controller/podgroup.go#L69-L72
+func (j *PyTorchJob) PriorityClass() string {
+	if j.Spec.RunPolicy.SchedulingPolicy != nil && len(j.Spec.RunPolicy.SchedulingPolicy.PriorityClass) != 0 {
+		return j.Spec.RunPolicy.SchedulingPolicy.PriorityClass
+	} else if l := j.Spec.PyTorchReplicaSpecs[kftraining.PyTorchJobReplicaTypeMaster]; l != nil && len(l.Template.Spec.PriorityClassName) != 0 {
+		return l.Template.Spec.PriorityClassName
+	} else if w := j.Spec.PyTorchReplicaSpecs[kftraining.PyTorchJobReplicaTypeWorker]; w != nil && len(w.Template.Spec.PriorityClassName) != 0 {
+		return w.Template.Spec.PriorityClassName
+	}
+	return ""
+}
+
+type JobControl kftraining.PyTorchJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
+
+func (c *JobControl) JobStatus() kftraining.JobStatus {
+	return c.Status
+}
+
+func (c *JobControl) ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec {
+	return c.Spec.PyTorchReplicaSpecs
+}
+
+func (c *JobControl) RunPolicy() *kftraining.RunPolicy {
+	return &c.Spec.RunPolicy
+}
+
+func (c *JobControl) OrderedReplicaTypes(replicaSpecs map[kftraining.ReplicaType]*kftraining.ReplicaSpec) []kftraining.ReplicaType {
+	var result []kftraining.ReplicaType
```

  ```suggestion
  	result := make([]kftraining.ReplicaType, 0, 2)
  ```

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:1** by [@trasc](https://github.com/trasc) — 2023-07-19T06:24:31Z
  Missing header

- **pkg/controller/jobs/kubeflow/jobs/jobs.go:1** by [@trasc](https://github.com/trasc) — 2023-07-19T06:26:22Z
  Missing header
  
  also I think we could just import 
  `"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/pytorchjob"`
  in 
  `pkg/controller/jobs/jobs.go`

- **pkg/controller/jobs/kubeflow/kubeflowjob/interface.go:1** by [@trasc](https://github.com/trasc) — 2023-07-19T06:28:22Z
  missing header

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob.go:1** by [@trasc](https://github.com/trasc) — 2023-07-19T06:29:55Z
  missing header

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T06:51:35Z

- **config/components/rbac/role.yaml:134** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T06:51:35Z
```diff
@@ -122,6 +122,23 @@ rules:
   verbs:
   - get
   - update
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs
+  verbs:
+  - get
+  - list
+  - patch
+  - update
+  - watch
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs/status
+  verbs:
+  - get
+  - update
```

  It's a good point. Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T06:52:45Z

- **go.mod:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T06:52:45Z
```diff
@@ -83,3 +85,5 @@ require (
 	sigs.k8s.io/json v0.0.0-20221116044647-bc3834ca7abd // indirect
 	sigs.k8s.io/yaml v1.3.0 // indirect
 )
+
+replace github.com/kubeflow/training-operator v1.6.0 => github.com/tenzen-y/training-operator v1.3.0-rc.1.0.20230717233919-1ed3e8e55322
```

  Yes, I will do `b`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T07:03:29Z

- **pkg/controller/jobs/kubeflow/jobs/jobs.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T07:03:29Z
  In the future, we will add more framework support. So, I would like to put the linking for kubeflow here.
  
  ```go
  // Reference the job framework integration packages to ensure linking.
  import (
  	_ "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/pytorchjob"
  	_ "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/tfjob"
  	_ "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/paddlejob"
  	_ "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/xgboostjob"
  	_ "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/mxjob"
  )
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T07:10:02Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T07:10:02Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T07:56:21Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T07:56:21Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  For Composition, we need to use an embedded struct. So I will modify this in the following:
  
  ```go
  type PyTorchJob struct {
  	*kubeflowjob.KubeflowJob
  }
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-19T09:44:41Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-19T09:44:41Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  I think it should work the same way, it is used like this to "make" the generic job out of all integrations we currently have.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T09:55:21Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T09:55:20Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  @trasc  Does that mean we should use the type alias instead of the embedded struct?
  
  If doing so, it means that we remove `kubeflowjob.KubeflowJob`, and then in all kubeflow integrations (TFJob, PaddleJob, ...), we declare the same processes such as `RunWithPodSetsInfo`, `RestorePodSetsInfo`  and so on.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T09:58:47Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T09:58:46Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  I'd like to use the embedded struct instead of the type alias because I'd like to use method override and then avoid redeclaration of the same processes.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T10:36:43Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:108** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T10:36:42Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
+
+var _ jobframework.GenericJob = (*PyTorchJob)(nil)
+
+func fromObject(o runtime.Object) *PyTorchJob {
+	pytorchJobObj := o.(*kftraining.PyTorchJob)
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}
+
+func (j *PyTorchJob) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *PyTorchJob) Object() client.Object {
+	return j.PyTorchJob
+}
+
+// PriorityClass calculates the priorityClass name needed for workload according to the following priorities:
+//  1. .spec.runPolicy.schedulingPolicy.priorityClass
+//  2. .spec.replicaSpecs[Master].template.spec.priorityClassName
+//  3. .spec.replicaSpecs[Worker].template.spec.priorityClassName
+//
+// This function is inspired by an analogous one in mpi-controller:
+// https://github.com/kubeflow/mpi-operator/blob/5946ef4157599a474ab82ff80e780d5c2546c9ee/pkg/controller/podgroup.go#L69-L72
+func (j *PyTorchJob) PriorityClass() string {
+	if j.Spec.RunPolicy.SchedulingPolicy != nil && len(j.Spec.RunPolicy.SchedulingPolicy.PriorityClass) != 0 {
+		return j.Spec.RunPolicy.SchedulingPolicy.PriorityClass
+	} else if l := j.Spec.PyTorchReplicaSpecs[kftraining.PyTorchJobReplicaTypeMaster]; l != nil && len(l.Template.Spec.PriorityClassName) != 0 {
+		return l.Template.Spec.PriorityClassName
+	} else if w := j.Spec.PyTorchReplicaSpecs[kftraining.PyTorchJobReplicaTypeWorker]; w != nil && len(w.Template.Spec.PriorityClassName) != 0 {
+		return w.Template.Spec.PriorityClassName
+	}
+	return ""
+}
+
+type JobControl kftraining.PyTorchJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
+
+func (c *JobControl) JobStatus() kftraining.JobStatus {
+	return c.Status
+}
+
+func (c *JobControl) ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec {
+	return c.Spec.PyTorchReplicaSpecs
+}
+
+func (c *JobControl) RunPolicy() *kftraining.RunPolicy {
+	return &c.Spec.RunPolicy
+}
+
+func (c *JobControl) OrderedReplicaTypes(replicaSpecs map[kftraining.ReplicaType]*kftraining.ReplicaSpec) []kftraining.ReplicaType {
+	var result []kftraining.ReplicaType
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T10:39:51Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T10:39:51Z
  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T10:40:15Z

- **pkg/controller/jobs/kubeflow/jobs/jobs.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T10:40:15Z
  > Missing header
  
  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T10:40:31Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/interface.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T10:40:31Z
  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-19T10:40:36Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-19T10:40:36Z
  Done.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T11:31:48Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-20T11:31:47Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  The embed approach comes with an extra  allocation every time is passed by value.  Method overwrite should work the same way.  (it is not type alias)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-20T11:45:26Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-20T11:45:26Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  Hm. My understanding might not be correct. Could you clarify more about `If you make Object() mart of the KFJobControl`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T12:49:08Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-20T12:49:07Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  If Object is part of `KFJobControl`, then you can  change 
  
  ```diff
  func (j *KubeflowJob) Object() client.Object {
  -	return nil
  +	return j.KFJobControl.Object()
  }
  ```
  
  Then you don't need the `*kftraining.PyTorchJob` memeber. 
  
  Then you can simplify the struct, and also make it a type "redefinition"  `type PyTorchJob kubeflowjob.KubeflowJob`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T13:06:27Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-20T13:06:27Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  thinking about it , you could do the same for `GetGVK() ` and might be able to make `KubeflowJob` generic and
  
  ```diff
  var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
  -	pytorchJobObj := &kftraining.PyTorchJob{}
  -	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
  +	return &kftraining.PyTorchJob{}
  }, nil)
  
  ...
  
  -type PyTorchJob struct {
  -	*kftraining.PyTorchJob
  -	*kubeflowjob.KubeflowJob
  -}
  + type PyTorchJob  KubeflowJob[JobControl]
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-20T13:19:08Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-20T13:19:07Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  > If Object is part of KFJobControl, then you can change
  > 
  > func (j *KubeflowJob) Object() client.Object {
  > -	return nil
  > +	return j.KFJobControl.Object()
  > }
  > Then you don't need the *kftraining.PyTorchJob memeber.
  > 
  > Then you can simplify the struct, and also make it a type "redefinition" type PyTorchJob kubeflowjob.KubeflowJob.
  
  Thanks for the clarifications.
  If doing so, I think `type PyTorchJob kubeflowjob.KubeflowJob` doesn't implement the `GenericJob` interface, right?
  
  Although `type JobControl kftraining.PyTorchJob` implement the `GenericJob`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-20T13:52:03Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@trasc](https://github.com/trasc) — 2023-07-20T13:52:03Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  Tooo many interfaces :)... ,  
  
  To keep it simple I think `kubeflowjob.KubeflowJob` should have all the common code for kubeflow(kftraining)) Jobs implementing `GenericJob`. and use `KFJobControl` to get it's job  (eg. `kftraining.PyTorchJob`) specific bits.
  
  To avoid confusion,  `KFJobControl` should be a named member, don't embed.
  
  ```go
  type KubeflowJob struct {
  	c KFJobControl
  }
  ```
  
  The reconciler setup could look  like:
  
  ```go
  var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
  	return &kubeflowjob.KubeflowJob{c:  &JobControl{}}
  }, nil)
  
  type JobControl kftraining.PyTorchJob
  
  var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
  ....
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-24T08:50:06Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T08:50:06Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  Uhm. Actually, I adopted the same pattern you suggested before submitting this PR. Then, I switched implementation to now one since this one is more reusable.
  
  However, I will roll back my implementation to your suggested one since if @trasc,  who has much understanding of the kueue, is confused by this implementation, then almost everyone will be confused.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T07:02:56Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T07:02:55Z
```diff
@@ -0,0 +1,116 @@
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PytorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	pytorchJobObj := &kftraining.PyTorchJob{}
+	return &PyTorchJob{PyTorchJob: pytorchJobObj, KubeflowJob: kubeflowjob.NewKubeflowJob((*JobControl)(pytorchJobObj))}
+}, nil)
+
+func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
+	return jobframework.SetupWorkloadOwnerIndex(ctx, indexer, gvk)
+}
+
+func GetWorkloadNameForPyTorchJob(jobName string) string {
+	return jobframework.GetWorkloadNameForOwnerWithGVK(jobName, gvk)
+}
+
+type PyTorchJob struct {
+	*kftraining.PyTorchJob
+	*kubeflowjob.KubeflowJob
+}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T07:03:02Z

- **go.mod:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T07:03:01Z
```diff
@@ -83,3 +85,5 @@ require (
 	sigs.k8s.io/json v0.0.0-20221116044647-bc3834ca7abd // indirect
 	sigs.k8s.io/yaml v1.3.0 // indirect
 )
+
+replace github.com/kubeflow/training-operator v1.6.0 => github.com/tenzen-y/training-operator v1.3.0-rc.1.0.20230717233919-1ed3e8e55322
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T07:10:56Z

- **config/components/rbac/role.yaml:134** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T07:10:56Z
```diff
@@ -122,6 +122,23 @@ rules:
   verbs:
   - get
   - update
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs
+  verbs:
+  - get
+  - list
+  - patch
+  - update
+  - watch
+- apiGroups:
+  - kubeflow.org
+  resources:
+  - pytorchjobs/status
+  verbs:
+  - get
+  - update
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T07:12:33Z

- **config/components/manager/controller_manager_config.yaml:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T07:12:33Z
```diff
@@ -32,3 +32,4 @@ integrations:
   - "kubeflow.org/mpijob"
   - "ray.io/rayjob"
   - "jobset.x-k8s.io/jobset"
+  - "kubeflow.org/pytorchjob"
```

  Done.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-11T10:44:54Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T15:27:09Z

- **Makefile:156** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:02:05Z
```diff
@@ -153,7 +153,7 @@ test: generate gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd ## Run tests.
+test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd training-operator-crd ## Run tests.
```

  "training-operator" sounds a bit generic, in a way that can be confusing for future readers.
  Maybe "tf-training-operator"?

- **Makefile:323** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:03:02Z
```diff
@@ -320,6 +320,12 @@ mpi-operator-crd:
 	mkdir -p $(PROJECT_DIR)/dep-crds/mpi-operator/
 	cp -f $(MPIROOT)/manifests/base/* $(PROJECT_DIR)/dep-crds/mpi-operator/
 
+TRAININGROOT = $(shell $(GO_CMD) list -m -f "{{.Dir}}" github.com/kubeflow/training-operator)
```

  ```suggestion
  TFTRAININGROOT = $(shell $(GO_CMD) list -m -f "{{.Dir}}" github.com/kubeflow/training-operator)
  ```

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:18:03Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/pointer"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type KubeflowJob struct {
+	KFJobControl KFJobControl
+}
+
+var _ jobframework.GenericJob = (*KubeflowJob)(nil)
+var _ jobframework.JobWithPriorityClass = (*KubeflowJob)(nil)
+
+func (j *KubeflowJob) Object() client.Object {
+	return j.KFJobControl.Object()
+}
+
+func (j *KubeflowJob) IsSuspended() bool {
+	return j.KFJobControl.RunPolicy().Suspend != nil && *j.KFJobControl.RunPolicy().Suspend
+}
+
+func (j *KubeflowJob) Suspend() {
```

  Can RunPolicy be nil? Should we add it here if it's missing?

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:20:17Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/pointer"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type KubeflowJob struct {
+	KFJobControl KFJobControl
+}
+
+var _ jobframework.GenericJob = (*KubeflowJob)(nil)
+var _ jobframework.JobWithPriorityClass = (*KubeflowJob)(nil)
+
+func (j *KubeflowJob) Object() client.Object {
+	return j.KFJobControl.Object()
+}
+
+func (j *KubeflowJob) IsSuspended() bool {
+	return j.KFJobControl.RunPolicy().Suspend != nil && *j.KFJobControl.RunPolicy().Suspend
+}
+
+func (j *KubeflowJob) Suspend() {
+	j.KFJobControl.RunPolicy().Suspend = pointer.Bool(true)
+}
+
+func (j *KubeflowJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
+	j.KFJobControl.RunPolicy().Suspend = pointer.Bool(false)
```

  Same note about nil RunPolicy

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:22:52Z
```diff
@@ -0,0 +1,123 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(&kftraining.PyTorchJob{})}
+}, nil)
+
+type JobControl kftraining.PyTorchJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
+
+func (j *JobControl) Object() client.Object {
+	return (*kftraining.PyTorchJob)(j)
+}
+
+func fromObject(o runtime.Object) *kubeflowjob.KubeflowJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(o.(*kftraining.PyTorchJob))}
+}
+
+func (j *JobControl) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *JobControl) RunPolicy() *kftraining.RunPolicy {
+	return &j.Spec.RunPolicy
+}
+
+func (j *JobControl) ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec {
+	return j.Spec.PyTorchReplicaSpecs
+}
+
+func (j *JobControl) JobStatus() kftraining.JobStatus {
+	return j.Status
+}
+
+func (j *JobControl) OrderedReplicaTypes(replicaSpecs map[kftraining.ReplicaType]*kftraining.ReplicaSpec) []kftraining.ReplicaType {
```

  What if you make this function just return the list of ReplicaTypes? Wouldn't the KFJob be able to obtain the rest?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-14T15:45:52Z

- **Makefile:156** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T15:45:52Z
```diff
@@ -153,7 +153,7 @@ test: generate gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd ## Run tests.
+test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd training-operator-crd ## Run tests.
```

  How about "kf-training-operator"?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T15:47:33Z

- **Makefile:156** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T15:47:32Z
```diff
@@ -153,7 +153,7 @@ test: generate gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd ## Run tests.
+test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd training-operator-crd ## Run tests.
```

  oops, that's what I wanted to say, sorry

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-14T17:28:08Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:50** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T17:28:08Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/pointer"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type KubeflowJob struct {
+	KFJobControl KFJobControl
+}
+
+var _ jobframework.GenericJob = (*KubeflowJob)(nil)
+var _ jobframework.JobWithPriorityClass = (*KubeflowJob)(nil)
+
+func (j *KubeflowJob) Object() client.Object {
+	return j.KFJobControl.Object()
+}
+
+func (j *KubeflowJob) IsSuspended() bool {
+	return j.KFJobControl.RunPolicy().Suspend != nil && *j.KFJobControl.RunPolicy().Suspend
+}
+
+func (j *KubeflowJob) Suspend() {
```

  RunPolicy isn't a pointer. So we wouldn't be concerned about missing Runpolicy.
  
  https://github.com/kubeflow/training-operator/blob/48dbbf0a8e90e52c55ec05d0f689fcbf83c6b441/pkg/apis/kubeflow.org/v1/pytorch_types.go#L76-L80
  
  @alculquicondor WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-14T17:33:21Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T17:33:21Z
```diff
@@ -0,0 +1,123 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(&kftraining.PyTorchJob{})}
+}, nil)
+
+type JobControl kftraining.PyTorchJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
+
+func (j *JobControl) Object() client.Object {
+	return (*kftraining.PyTorchJob)(j)
+}
+
+func fromObject(o runtime.Object) *kubeflowjob.KubeflowJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(o.(*kftraining.PyTorchJob))}
+}
+
+func (j *JobControl) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *JobControl) RunPolicy() *kftraining.RunPolicy {
+	return &j.Spec.RunPolicy
+}
+
+func (j *JobControl) ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec {
+	return j.Spec.PyTorchReplicaSpecs
+}
+
+func (j *JobControl) JobStatus() kftraining.JobStatus {
+	return j.Status
+}
+
+func (j *JobControl) OrderedReplicaTypes(replicaSpecs map[kftraining.ReplicaType]*kftraining.ReplicaSpec) []kftraining.ReplicaType {
```

  IIUC, it wouldn't work since every kubeflow framework has different replicaRoles.
  For example:
  
  TFJob: https://github.com/kubeflow/training-operator/blob/48dbbf0a8e90e52c55ec05d0f689fcbf83c6b441/pkg/apis/kubeflow.org/v1/tensorflow_types.go#L104-L123
  PyTorchJob: https://github.com/kubeflow/training-operator/blob/48dbbf0a8e90e52c55ec05d0f689fcbf83c6b441/pkg/apis/kubeflow.org/v1/pytorch_types.go#L40-L43
  MXJob: https://github.com/kubeflow/training-operator/blob/48dbbf0a8e90e52c55ec05d0f689fcbf83c6b441/pkg/apis/kubeflow.org/v1/mxnet_types.go#L39-L58

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T17:49:31Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T17:49:30Z
```diff
@@ -0,0 +1,123 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pytorchjob
+
+import (
+	"context"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	utilruntime "k8s.io/apimachinery/pkg/util/runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+)
+
+var (
+	gvk           = kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind)
+	FrameworkName = "kubeflow.org/pytorchjob"
+)
+
+func init() {
+	utilruntime.Must(jobframework.RegisterIntegration(FrameworkName, jobframework.IntegrationCallbacks{
+		SetupIndexes:  SetupIndexes,
+		NewReconciler: NewReconciler,
+		SetupWebhook:  SetupPyTorchJobWebhook,
+		JobType:       &kftraining.PyTorchJob{},
+		AddToScheme:   kftraining.AddToScheme,
+	}))
+}
+
+// +kubebuilder:rbac:groups=scheduling.k8s.io,resources=priorityclasses,verbs=list;get;watch
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kubeflow.org,resources=pytorchjobs/status,verbs=get;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+
+var NewReconciler = jobframework.NewGenericReconciler(func() jobframework.GenericJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(&kftraining.PyTorchJob{})}
+}, nil)
+
+type JobControl kftraining.PyTorchJob
+
+var _ kubeflowjob.KFJobControl = (*JobControl)(nil)
+
+func (j *JobControl) Object() client.Object {
+	return (*kftraining.PyTorchJob)(j)
+}
+
+func fromObject(o runtime.Object) *kubeflowjob.KubeflowJob {
+	return &kubeflowjob.KubeflowJob{KFJobControl: (*JobControl)(o.(*kftraining.PyTorchJob))}
+}
+
+func (j *JobControl) GetGVK() schema.GroupVersionKind {
+	return gvk
+}
+
+func (j *JobControl) RunPolicy() *kftraining.RunPolicy {
+	return &j.Spec.RunPolicy
+}
+
+func (j *JobControl) ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec {
+	return j.Spec.PyTorchReplicaSpecs
+}
+
+func (j *JobControl) JobStatus() kftraining.JobStatus {
+	return j.Status
+}
+
+func (j *JobControl) OrderedReplicaTypes(replicaSpecs map[kftraining.ReplicaType]*kftraining.ReplicaSpec) []kftraining.ReplicaType {
```

  Nvm. This function is doing exactly what I said. I read it wrong.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-14T17:58:11Z

- **Makefile:156** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T17:58:10Z
```diff
@@ -153,7 +153,7 @@ test: generate gotestsum ## Run tests.
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) $(shell $(GO_CMD) list ./... | grep -v '/test/') -coverpkg=./... -coverprofile $(ARTIFACTS)/cover.out
 
 .PHONY: test-integration
-test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd ## Run tests.
+test-integration: manifests generate envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd training-operator-crd ## Run tests.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-14T17:58:14Z

- **Makefile:323** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-14T17:58:13Z
```diff
@@ -320,6 +320,12 @@ mpi-operator-crd:
 	mkdir -p $(PROJECT_DIR)/dep-crds/mpi-operator/
 	cp -f $(MPIROOT)/manifests/base/* $(PROJECT_DIR)/dep-crds/mpi-operator/
 
+TRAININGROOT = $(shell $(GO_CMD) list -m -f "{{.Dir}}" github.com/kubeflow/training-operator)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-16T06:05:55Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:50** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T06:05:55Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/pointer"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type KubeflowJob struct {
+	KFJobControl KFJobControl
+}
+
+var _ jobframework.GenericJob = (*KubeflowJob)(nil)
+var _ jobframework.JobWithPriorityClass = (*KubeflowJob)(nil)
+
+func (j *KubeflowJob) Object() client.Object {
+	return j.KFJobControl.Object()
+}
+
+func (j *KubeflowJob) IsSuspended() bool {
+	return j.KFJobControl.RunPolicy().Suspend != nil && *j.KFJobControl.RunPolicy().Suspend
+}
+
+func (j *KubeflowJob) Suspend() {
```

  @alculquicondor friendly ping :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-16T12:14:54Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T12:14:53Z
```diff
@@ -0,0 +1,150 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflowjob
+
+import (
+	"strings"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/pointer"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type KubeflowJob struct {
+	KFJobControl KFJobControl
+}
+
+var _ jobframework.GenericJob = (*KubeflowJob)(nil)
+var _ jobframework.JobWithPriorityClass = (*KubeflowJob)(nil)
+
+func (j *KubeflowJob) Object() client.Object {
+	return j.KFJobControl.Object()
+}
+
+func (j *KubeflowJob) IsSuspended() bool {
+	return j.KFJobControl.RunPolicy().Suspend != nil && *j.KFJobControl.RunPolicy().Suspend
+}
+
+func (j *KubeflowJob) Suspend() {
```

  sounds good :)
