# PR #9985: Commonize gauge metric reporting, create clusterqueue_metrics in …

**Summary**: Commonize gauge metric reporting, create clusterqueue_metrics in …

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9985

**Last updated**: 2026-04-14T15:01:17Z

---

## Metadata

- **State**: open
- **Author**: [@reruno](https://github.com/reruno)
- **Branch**: `cleanup/i9793` → `main`
- **Created**: 2026-03-18T16:09:55Z
- **Updated**: 2026-04-14T15:01:17Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 5   **+174 / -132**

## Description

…pkg/cache/scheduler/ package

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Commonize gauge metric reporting so that the cache becomes the single source of truth.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9793 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-18T16:10:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9cac815a6a4e3b820d5f4d017f779121e3890390 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69de516ec3004b00086a9a50 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-9985--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-9985--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTk5ODUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.nY6Jee0jZkEp96ELyk1GfhJyb99I7F6tt5X1kuX7QUA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-9985--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-18T16:10:06Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9985#" title="Author self-approved">reruno</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba), [pbundyra](https://github.com/pbundyra) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba","pbundyra"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-18T16:10:07Z

Hi @reruno. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T05:51:46Z

/ok-to-test

### Comment by [@reruno](https://github.com/reruno) — 2026-03-23T07:46:28Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-03-23T08:55:55Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-03-23T09:02:23Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-03-23T09:47:23Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-03-23T15:40:12Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-04-01T13:59:08Z

/retest

### Comment by [@reruno](https://github.com/reruno) — 2026-04-02T12:56:51Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:55:49Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-02T14:55:57Z

LGTM label has been added.  <details>Git tree hash: 8cd60dcc9294a76d611039df21cebf11d72947cf</details>

### Comment by [@reruno](https://github.com/reruno) — 2026-04-14T08:23:34Z

@mimowo I received LGTM from the team, do you have any additional comments for the PR?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-14T14:38:41Z

New changes are detected. LGTM label has been removed.

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-24T11:37:11Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:47** by [@mszadkow](https://github.com/mszadkow) — 2026-03-24T11:37:11Z
```diff
@@ -0,0 +1,105 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference) {
```

  does it make sense to keep this separate?
  Maybe we could clear before we record and just have one method?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-24T11:50:34Z

- **pkg/controller/core/localqueue_controller.go:296** by [@mszadkow](https://github.com/mszadkow) — 2026-03-24T11:50:34Z
```diff
@@ -293,7 +293,7 @@ func (r *LocalQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.LocalQueue
 	}
 
 	if features.Enabled(features.LocalQueueMetrics) {
-		updateLocalQueueResourceMetrics(e.ObjectNew, r.roleTracker, r.customLabels)
+		updateLocalQueueResourceMetrics(r.logger(), r.cache, e.ObjectNew)
```

  the log is already here in line 281

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-24T11:52:58Z

- **pkg/controller/core/clusterqueue_controller.go:412** by [@mszadkow](https://github.com/mszadkow) — 2026-03-24T11:52:58Z
```diff
@@ -411,7 +409,7 @@ func (r *ClusterQueueReconciler) Update(e event.TypedUpdateEvent[*kueue.ClusterQ
 	}
 
 	if r.reportResourceMetrics {
-		updateResourceMetrics(e.ObjectOld, e.ObjectNew, r.roleTracker, r.customLabels)
+		updateResourceMetrics(r.logger(), r.cache, e.ObjectOld, e.ObjectNew, r.fairSharingEnabled)
```

  log is already here up in line 375

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-03-24T14:44:17Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:47** by [@reruno](https://github.com/reruno) — 2026-03-24T14:44:17Z
```diff
@@ -0,0 +1,105 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference) {
```

  I created method for cache to replace metrics.ClearClusterQueueResourceMetrics call here 
  pkg/controller/core/clusterqueue_controller.go:425
  
  On the second thought I think I should revert this change and keep metrics call as it was. 
  
  > Maybe we could clear before we record and just have one method?
  
  Why should we clear metrics before record? I think these metrics are rewritten by reportResourceMetrics anyway

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-30T12:26:17Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:68** by [@mszadkow](https://github.com/mszadkow) — 2026-03-30T12:26:17Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	newCq := c.hm.ClusterQueue(kueue.ClusterQueueReference(oldCq.Name))
+	if newCq == nil {
+		return
+	}
+
+	for _, rg := range oldCq.Spec.ResourceGroups {
+		for fi := range rg.Flavors {
+			flavor := &rg.Flavors[fi]
+			for ri := range flavor.Resources {
+				fr := resources.FlavorResource{Flavor: flavor.Name, Resource: flavor.Resources[ri].Name}
+				if _, found := newCq.resourceNode.Quotas[fr]; !found {
+					metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+				}
+			}
+		}
+	}
```

  This logic seems to be "simplified", what happened to the comparison between old and new values.
  We don't need to do that anymore?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-01T13:17:02Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:68** by [@reruno](https://github.com/reruno) — 2026-04-01T13:17:02Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	newCq := c.hm.ClusterQueue(kueue.ClusterQueueReference(oldCq.Name))
+	if newCq == nil {
+		return
+	}
+
+	for _, rg := range oldCq.Spec.ResourceGroups {
+		for fi := range rg.Flavors {
+			flavor := &rg.Flavors[fi]
+			for ri := range flavor.Resources {
+				fr := resources.FlavorResource{Flavor: flavor.Name, Resource: flavor.Resources[ri].Name}
+				if _, found := newCq.resourceNode.Quotas[fr]; !found {
+					metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+				}
+			}
+		}
+	}
```

  If I understand it correctly `oldCq.Status.FlavorsUsage` and `oldCq.Status.FlavorReservation` are derived from `oldCq.Spec.ResourceGroups`, and there is no case, I think, when there is quota metric, but no reservation and usage metrics at the same time. 
  
  Also cache clusterqueue doesn't have `.Status.FlavorReservation` and `.Spec.ResourceGroups`, so we can check only `Spec.ResourceGroups` from cache clusterqueue

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-01T13:18:36Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:68** by [@reruno](https://github.com/reruno) — 2026-04-01T13:18:36Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	newCq := c.hm.ClusterQueue(kueue.ClusterQueueReference(oldCq.Name))
+	if newCq == nil {
+		return
+	}
+
+	for _, rg := range oldCq.Spec.ResourceGroups {
+		for fi := range rg.Flavors {
+			flavor := &rg.Flavors[fi]
+			for ri := range flavor.Resources {
+				fr := resources.FlavorResource{Flavor: flavor.Name, Resource: flavor.Resources[ri].Name}
+				if _, found := newCq.resourceNode.Quotas[fr]; !found {
+					metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+				}
+			}
+		}
+	}
```

  @mbobrovskyi what do you think?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T10:35:09Z

- **pkg/cache/scheduler/localqueue_metrics.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T10:35:09Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withLocalQueueLogger(log, lqKey)
+	log.V(4).Info("Recording resource metrics for LocalQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	lq, ok := cq.localQueues[lqKey]
+	if !ok {
+		return
+	}
+
+	lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+}
+
+func (c *Cache) withLocalQueueLogger(log logr.Logger, lqKey queue.LocalQueueReference) logr.Logger {
+	return log.WithValues("localQueue", lqKey)
+}
```

  What the idea of this method? Why not use log.WithValues("localQueue", lqKey) directly?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T10:36:10Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:86** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T10:36:10Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withClusterQueueLogger(log, cqName)
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	newCq := c.hm.ClusterQueue(kueue.ClusterQueueReference(oldCq.Name))
+	if newCq == nil {
+		return
+	}
+
+	for rgi := range oldCq.Spec.ResourceGroups {
+		oldRg := &oldCq.Spec.ResourceGroups[rgi]
+		newFlavorSet := sets.New[kueue.ResourceFlavorReference]()
+		if rgi < len(newCq.ResourceGroups) && len(newCq.ResourceGroups[rgi].Flavors) > 0 {
+			newFlavorSet.Insert(newCq.ResourceGroups[rgi].Flavors...)
+		}
+
+		for fi := range oldRg.Flavors {
+			flavor := &oldRg.Flavors[fi]
+			if !newFlavorSet.Has(flavor.Name) {
+				metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(flavor.Name), "")
+			} else {
+				for ri := range flavor.Resources {
+					fr := resources.FlavorResource{Flavor: flavor.Name, Resource: flavor.Resources[ri].Name}
+					if _, found := newCq.resourceNode.Quotas[fr]; !found {
+						metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+						metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+						metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(fr.Flavor), string(fr.Resource))
+					}
+				}
+			}
+		}
+	}
+}
+
+func (c *Cache) withClusterQueueLogger(log logr.Logger, cqName kueue.ClusterQueueReference) logr.Logger {
+	return log.WithValues("clusterQueue", cqName)
+}
```

  Also here.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T10:51:07Z

- **pkg/cache/scheduler/localqueue_metrics.go:53** by [@reruno](https://github.com/reruno) — 2026-04-02T10:51:07Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withLocalQueueLogger(log, lqKey)
+	log.V(4).Info("Recording resource metrics for LocalQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	lq, ok := cq.localQueues[lqKey]
+	if !ok {
+		return
+	}
+
+	lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+}
+
+func (c *Cache) withLocalQueueLogger(log logr.Logger, lqKey queue.LocalQueueReference) logr.Logger {
+	return log.WithValues("localQueue", lqKey)
+}
```

  I copied this function from file, to make new code similar to existing code
  ./pkg/cache/scheduler/cohort_metrics.go:115
  ```
  func (c *Cache) withCohortLogger(log logr.Logger, cohortName kueue.CohortReference) logr.Logger {
  	return log.WithValues("cohort", cohortName)
  }
  ```
  
  I can add log to ClearClusterQueueOldResourceQuotas, that will reuse withClusterQueueLogger, and about LocalQueue I can remove it because it is used only once, wdyt?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T10:53:03Z

- **pkg/cache/scheduler/localqueue_metrics.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T10:53:03Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withLocalQueueLogger(log, lqKey)
+	log.V(4).Info("Recording resource metrics for LocalQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	lq, ok := cq.localQueues[lqKey]
+	if !ok {
+		return
+	}
+
+	lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+}
+
+func (c *Cache) withLocalQueueLogger(log logr.Logger, lqKey queue.LocalQueueReference) logr.Logger {
+	return log.WithValues("localQueue", lqKey)
+}
```

  I don't think so we need this redundant method.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T10:53:41Z

- **pkg/cache/scheduler/localqueue_metrics.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T10:53:40Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withLocalQueueLogger(log, lqKey)
+	log.V(4).Info("Recording resource metrics for LocalQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	lq, ok := cq.localQueues[lqKey]
+	if !ok {
+		return
+	}
+
+	lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+}
+
+func (c *Cache) withLocalQueueLogger(log logr.Logger, lqKey queue.LocalQueueReference) logr.Logger {
+	return log.WithValues("localQueue", lqKey)
+}
```

  I think log.V(4).Info("Recording resource metrics for ClusterQueue", "clusterQueue", cqName) is much easier.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:01:29Z

- **pkg/cache/scheduler/localqueue_metrics.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:01:29Z
```diff
@@ -0,0 +1,53 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
+
+	log = c.withLocalQueueLogger(log, lqKey)
+	log.V(4).Info("Recording resource metrics for LocalQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	lq, ok := cq.localQueues[lqKey]
+	if !ok {
+		return
+	}
+
+	lq.reportResourceMetrics(cq.resourceNode.Quotas, c.roleTracker)
+}
+
+func (c *Cache) withLocalQueueLogger(log logr.Logger, lqKey queue.LocalQueueReference) logr.Logger {
+	return log.WithValues("localQueue", lqKey)
+}
```

  Actually, we already set clusterQueue outside this function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:07:20Z

- **pkg/controller/core/clusterqueue_controller.go:422** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:07:20Z
```diff
@@ -421,118 +419,15 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
-func recordResourceMetrics(cq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
-	cqCustomLabels := customLabels.CQGet(kueue.ClusterQueueReference(cq.Name))
-	for rgi := range cq.Spec.ResourceGroups {
-		rg := &cq.Spec.ResourceGroups[rgi]
-		for fqi := range rg.Flavors {
-			fq := &rg.Flavors[fqi]
-			for ri := range fq.Resources {
-				r := &fq.Resources[ri]
-				nominal := resource.QuantityToFloat(&r.NominalQuota)
-				borrow := resource.QuantityToFloat(r.BorrowingLimit)
-				lend := resource.QuantityToFloat(r.LendingLimit)
-				metrics.ReportClusterQueueQuotas(cq.Spec.CohortName, cq.Name, string(fq.Name), string(r.Name), nominal, borrow, lend, cqCustomLabels, tracker)
-			}
-		}
-	}
-
-	for fri := range cq.Status.FlavorsReservation {
-		fr := &cq.Status.FlavorsReservation[fri]
-		for ri := range fr.Resources {
-			r := &fr.Resources[ri]
-			metrics.ReportClusterQueueResourceReservations(cq.Spec.CohortName, cq.Name, string(fr.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-
-	for fui := range cq.Status.FlavorsUsage {
-		fu := &cq.Status.FlavorsUsage[fui]
-		for ri := range fu.Resources {
-			r := &fu.Resources[ri]
-			metrics.ReportClusterQueueResourceUsage(cq.Spec.CohortName, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-}
-
-func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
+func updateResourceMetrics(log logr.Logger, cache *schdcache.Cache, oldCq, newCq *kueue.ClusterQueue, fairSharingEnabled bool) {
```

  ```suggestion
  func (r *ClusterQueueReconciler) updateResourceMetrics(log logr.Logger, oldCq, newCq *kueue.ClusterQueue) {
  ```
  Can we make `updateResourceMetrics` part of `ClusterQueueReconciler`?
  
  Also, we already have the `fairSharingEnabled` parameter in the cache.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T11:12:14Z

- **pkg/controller/core/clusterqueue_controller.go:422** by [@reruno](https://github.com/reruno) — 2026-04-02T11:12:14Z
```diff
@@ -421,118 +419,15 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
-func recordResourceMetrics(cq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
-	cqCustomLabels := customLabels.CQGet(kueue.ClusterQueueReference(cq.Name))
-	for rgi := range cq.Spec.ResourceGroups {
-		rg := &cq.Spec.ResourceGroups[rgi]
-		for fqi := range rg.Flavors {
-			fq := &rg.Flavors[fqi]
-			for ri := range fq.Resources {
-				r := &fq.Resources[ri]
-				nominal := resource.QuantityToFloat(&r.NominalQuota)
-				borrow := resource.QuantityToFloat(r.BorrowingLimit)
-				lend := resource.QuantityToFloat(r.LendingLimit)
-				metrics.ReportClusterQueueQuotas(cq.Spec.CohortName, cq.Name, string(fq.Name), string(r.Name), nominal, borrow, lend, cqCustomLabels, tracker)
-			}
-		}
-	}
-
-	for fri := range cq.Status.FlavorsReservation {
-		fr := &cq.Status.FlavorsReservation[fri]
-		for ri := range fr.Resources {
-			r := &fr.Resources[ri]
-			metrics.ReportClusterQueueResourceReservations(cq.Spec.CohortName, cq.Name, string(fr.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-
-	for fui := range cq.Status.FlavorsUsage {
-		fu := &cq.Status.FlavorsUsage[fui]
-		for ri := range fu.Resources {
-			r := &fu.Resources[ri]
-			metrics.ReportClusterQueueResourceUsage(cq.Spec.CohortName, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-}
-
-func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
+func updateResourceMetrics(log logr.Logger, cache *schdcache.Cache, oldCq, newCq *kueue.ClusterQueue, fairSharingEnabled bool) {
```

  https://github.com/kubernetes-sigs/kueue/pull/9985/changes#diff-e7aa8a319ba9b1411da177aa70d1fb4d1a62e4172c84a443850090d9daf0e525R603
  
  We would need to add ClusterQueueReconciler in the unit test, if we change it from function to method

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:13:29Z

- **pkg/controller/core/clusterqueue_controller.go:422** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:13:28Z
```diff
@@ -421,118 +419,15 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
-func recordResourceMetrics(cq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
-	cqCustomLabels := customLabels.CQGet(kueue.ClusterQueueReference(cq.Name))
-	for rgi := range cq.Spec.ResourceGroups {
-		rg := &cq.Spec.ResourceGroups[rgi]
-		for fqi := range rg.Flavors {
-			fq := &rg.Flavors[fqi]
-			for ri := range fq.Resources {
-				r := &fq.Resources[ri]
-				nominal := resource.QuantityToFloat(&r.NominalQuota)
-				borrow := resource.QuantityToFloat(r.BorrowingLimit)
-				lend := resource.QuantityToFloat(r.LendingLimit)
-				metrics.ReportClusterQueueQuotas(cq.Spec.CohortName, cq.Name, string(fq.Name), string(r.Name), nominal, borrow, lend, cqCustomLabels, tracker)
-			}
-		}
-	}
-
-	for fri := range cq.Status.FlavorsReservation {
-		fr := &cq.Status.FlavorsReservation[fri]
-		for ri := range fr.Resources {
-			r := &fr.Resources[ri]
-			metrics.ReportClusterQueueResourceReservations(cq.Spec.CohortName, cq.Name, string(fr.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-
-	for fui := range cq.Status.FlavorsUsage {
-		fu := &cq.Status.FlavorsUsage[fui]
-		for ri := range fu.Resources {
-			r := &fu.Resources[ri]
-			metrics.ReportClusterQueueResourceUsage(cq.Spec.CohortName, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-}
-
-func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
+func updateResourceMetrics(log logr.Logger, cache *schdcache.Cache, oldCq, newCq *kueue.ClusterQueue, fairSharingEnabled bool) {
```

  Is it problematic?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T11:14:14Z

- **pkg/controller/core/clusterqueue_controller.go:422** by [@reruno](https://github.com/reruno) — 2026-04-02T11:14:14Z
```diff
@@ -421,118 +419,15 @@ func (r *ClusterQueueReconciler) Generic(e event.TypedGenericEvent[*kueue.Cluste
 	return true
 }
 
-func recordResourceMetrics(cq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
-	cqCustomLabels := customLabels.CQGet(kueue.ClusterQueueReference(cq.Name))
-	for rgi := range cq.Spec.ResourceGroups {
-		rg := &cq.Spec.ResourceGroups[rgi]
-		for fqi := range rg.Flavors {
-			fq := &rg.Flavors[fqi]
-			for ri := range fq.Resources {
-				r := &fq.Resources[ri]
-				nominal := resource.QuantityToFloat(&r.NominalQuota)
-				borrow := resource.QuantityToFloat(r.BorrowingLimit)
-				lend := resource.QuantityToFloat(r.LendingLimit)
-				metrics.ReportClusterQueueQuotas(cq.Spec.CohortName, cq.Name, string(fq.Name), string(r.Name), nominal, borrow, lend, cqCustomLabels, tracker)
-			}
-		}
-	}
-
-	for fri := range cq.Status.FlavorsReservation {
-		fr := &cq.Status.FlavorsReservation[fri]
-		for ri := range fr.Resources {
-			r := &fr.Resources[ri]
-			metrics.ReportClusterQueueResourceReservations(cq.Spec.CohortName, cq.Name, string(fr.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-
-	for fui := range cq.Status.FlavorsUsage {
-		fu := &cq.Status.FlavorsUsage[fui]
-		for ri := range fu.Resources {
-			r := &fu.Resources[ri]
-			metrics.ReportClusterQueueResourceUsage(cq.Spec.CohortName, cq.Name, string(fu.Name), string(r.Name), resource.QuantityToFloat(&r.Total), cqCustomLabels, tracker)
-		}
-	}
-}
-
-func updateResourceMetrics(oldCq, newCq *kueue.ClusterQueue, tracker *roletracker.RoleTracker, customLabels *metrics.CustomLabels) {
+func updateResourceMetrics(log logr.Logger, cache *schdcache.Cache, oldCq, newCq *kueue.ClusterQueue, fairSharingEnabled bool) {
```

  No just wanted to point that out

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:19:39Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:19:39Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
```

  Is it possible this case?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:21:47Z

- **pkg/cache/scheduler/localqueue_metrics.go:30** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:21:47Z
```diff
@@ -0,0 +1,48 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
```

  The same question here.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T11:25:10Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:32** by [@reruno](https://github.com/reruno) — 2026-04-02T11:25:10Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
```

  I think it is not possible, we extract clusterQueue name from objects that reconciler receives. It acts as a guard, if bad arg comes, we log it and exit. 
  
  https://github.com/kubernetes-sigs/kueue/pull/9985/changes#diff-54fb2e1e38c1d823a077207b574c6dc9f21d0b354584becd6940b26282aec41fR430
  
  https://github.com/kubernetes-sigs/kueue/pull/9985/changes#diff-54fb2e1e38c1d823a077207b574c6dc9f21d0b354584becd6940b26282aec41fR350

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T11:28:54Z

- **pkg/cache/scheduler/localqueue_metrics.go:30** by [@reruno](https://github.com/reruno) — 2026-04-02T11:28:54Z
```diff
@@ -0,0 +1,48 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/util/queue"
+)
+
+func (c *Cache) RecordLocalQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) {
+	if lqKey == "" {
+		log.V(4).Info("LocalQueue key is empty, skipping resource metrics recording")
+		return
+	}
```

  Same guard, I will add the second one for cqName arg
  
  https://github.com/kubernetes-sigs/kueue/pull/9985/changes#diff-9177ca87118e92b78007b8e08ffc92f1a18afa6fd6b4168b464e497e09ec53ecR424
  https://github.com/kubernetes-sigs/kueue/pull/9985/changes#diff-9177ca87118e92b78007b8e08ffc92f1a18afa6fd6b4168b464e497e09ec53ecR261

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:29:50Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:29:50Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
```

  If it’s not possible, I suggest removing it. We don’t need this guard – even if it’s empty, the next steps will handle it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:46:35Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:47** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:46:35Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(log logr.Logger, oldCq *kueue.ClusterQueue) {
```

  ```suggestion
  func (c *Cache) ClearClusterQueueResourceMetrics(log logr.Logger, oldCq *kueue.ClusterQueue) {
  ```

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T11:49:42Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:47** by [@reruno](https://github.com/reruno) — 2026-04-02T11:49:42Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(log logr.Logger, oldCq *kueue.ClusterQueue) {
```

  I would name it `ClearClusterQueueOldResourceMetrics`
  
  Because the method clears old resource metrics that are not present in current  clusterqueue (newCq)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T11:53:13Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:66** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T11:53:13Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(log logr.Logger, oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cqName := kueue.ClusterQueueReference(oldCq.Name)
+	log.V(4).Info("Clearing old resource metrics for ClusterQueue")
+
+	newCq := c.hm.ClusterQueue(cqName)
+	if newCq == nil {
+		return
+	}
+
+	for rgi := range oldCq.Spec.ResourceGroups {
+		oldRg := &oldCq.Spec.ResourceGroups[rgi]
+		newFlavorSet := sets.New[kueue.ResourceFlavorReference]()
+		if rgi < len(newCq.ResourceGroups) && len(newCq.ResourceGroups[rgi].Flavors) > 0 {
+			newFlavorSet.Insert(newCq.ResourceGroups[rgi].Flavors...)
+		}
+
+		for fi := range oldRg.Flavors {
+			flavor := &oldRg.Flavors[fi]
+			if !newFlavorSet.Has(flavor.Name) {
+				metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(flavor.Name), "")
```

  I’m wondering if we can keep only this.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-02T12:28:39Z

- **pkg/cache/scheduler/clusterqueue_metrics.go:66** by [@reruno](https://github.com/reruno) — 2026-04-02T12:28:39Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scheduler
+
+import (
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/resources"
+)
+
+func (c *Cache) RecordClusterQueueResourceMetrics(log logr.Logger, cqName kueue.ClusterQueueReference, fairSharingEnabled bool) {
+	if cqName == "" {
+		log.V(4).Info("ClusterQueue name is empty, skipping resource metrics recording")
+		return
+	}
+
+	log.V(4).Info("Recording resource metrics for ClusterQueue")
+
+	c.RLock()
+	defer c.RUnlock()
+
+	cq := c.hm.ClusterQueue(cqName)
+	if cq == nil {
+		return
+	}
+
+	cq.reportResourceMetrics(fairSharingEnabled)
+}
+
+func (c *Cache) ClearClusterQueueOldResourceQuotas(log logr.Logger, oldCq *kueue.ClusterQueue) {
+	c.RLock()
+	defer c.RUnlock()
+
+	cqName := kueue.ClusterQueueReference(oldCq.Name)
+	log.V(4).Info("Clearing old resource metrics for ClusterQueue")
+
+	newCq := c.hm.ClusterQueue(cqName)
+	if newCq == nil {
+		return
+	}
+
+	for rgi := range oldCq.Spec.ResourceGroups {
+		oldRg := &oldCq.Spec.ResourceGroups[rgi]
+		newFlavorSet := sets.New[kueue.ResourceFlavorReference]()
+		if rgi < len(newCq.ResourceGroups) && len(newCq.ResourceGroups[rgi].Flavors) > 0 {
+			newFlavorSet.Insert(newCq.ResourceGroups[rgi].Flavors...)
+		}
+
+		for fi := range oldRg.Flavors {
+			flavor := &oldRg.Flavors[fi]
+			if !newFlavorSet.Has(flavor.Name) {
+				metrics.ClearClusterQueueResourceQuotas(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceReservations(oldCq.Name, string(flavor.Name), "")
+				metrics.ClearClusterQueueResourceUsage(oldCq.Name, string(flavor.Name), "")
```

  No, when flavor is present in new cq and old cq, we need to check if old resource in existing flavor is present in new resources (`newCq.resourceNode.Quotas[fr]`), if old resource is not present it will be cleared.
  
  We can clear all flavors regardless if it is present in new cq or not, but it will be a little bit less efficient

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:21:03Z

- **pkg/controller/core/clusterqueue_controller_test.go:604** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:21:04Z
```diff
@@ -556,14 +576,32 @@ func TestRecordResourceMetrics(t *testing.T) {
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			recordResourceMetrics(tc.queue, nil, nil)
+			ctx, log := utiltesting.ContextWithLog(t)
+
+			cl := utiltesting.NewClientBuilder().Build()
+			cqCache := schdcache.New(cl)
+			r := &ClusterQueueReconciler{cache: cqCache}
+			err := cqCache.AddClusterQueue(ctx, tc.queue.DeepCopy())
+			if err != nil {
+				t.Errorf("Unexpected error err=%s", err.Error())
+			}
+
+			wl := workloadForReservation("name", tc.queue.Status.FlavorsReservation, time.Now())
+			cqCache.AddOrUpdateWorkload(log, wl)
+
+			cqCache.RecordClusterQueueResourceMetrics(log, kueue.ClusterQueueReference(tc.queue.Name))
 			gotMetrics := allMetricsForQueue(tc.queue.Name)
 			if diff := cmp.Diff(tc.wantMetrics, gotMetrics, opts...); len(diff) != 0 {
 				t.Errorf("Unexpected metrics (-want,+got):\n%s", diff)
 			}
 
 			if tc.updatedQueue != nil {
-				updateResourceMetrics(tc.queue, tc.updatedQueue, nil, nil)
+				wl := workloadForReservation("name", tc.updatedQueue.Status.FlavorsReservation, time.Now())
+				cqCache.AddOrUpdateWorkload(log, wl)
+				if err := cqCache.UpdateClusterQueue(log, tc.updatedQueue); err != nil {
+					t.Fatalf("Updating clusterQueue in cache: %v", err)
+				}
+				r.updateResourceMetrics(ctrl.Log, tc.queue, tc.updatedQueue)
```

  ```suggestion
  				r.updateResourceMetrics(log, tc.queue, tc.updatedQueue)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:23:31Z

- **pkg/controller/core/clusterqueue_controller_test.go:339** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:23:31Z
```diff
@@ -335,6 +335,23 @@ func resourceDataPoint(cohort, name, flavor, res string, v float64) testingmetri
 	}
 }
 
+func workloadForReservation(cqName string, reservation []kueue.FlavorUsage, t time.Time) *kueue.Workload {
+	psa := utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName)
```

  ```suggestion
  func workloadForReservation(cqName string, reservation []kueue.FlavorUsage) *kueue.Workload {
  	now := time.Now()
  	psa := utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName)
  ```
  IIUC, we don’t need to propagate time – we can just use now inside function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:26:42Z

- **pkg/controller/core/clusterqueue_controller_test.go:586** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:26:42Z
```diff
@@ -556,14 +576,32 @@ func TestRecordResourceMetrics(t *testing.T) {
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			recordResourceMetrics(tc.queue, nil, nil)
+			ctx, log := utiltesting.ContextWithLog(t)
+
+			cl := utiltesting.NewClientBuilder().Build()
+			cqCache := schdcache.New(cl)
+			r := &ClusterQueueReconciler{cache: cqCache}
+			err := cqCache.AddClusterQueue(ctx, tc.queue.DeepCopy())
+			if err != nil {
+				t.Errorf("Unexpected error err=%s", err.Error())
```

  ```suggestion
  				t.Fatalf("Unexpected error: %v", err)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:28:15Z

- **pkg/controller/core/clusterqueue_controller_test.go:584** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:28:15Z
```diff
@@ -556,14 +576,32 @@ func TestRecordResourceMetrics(t *testing.T) {
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			recordResourceMetrics(tc.queue, nil, nil)
+			ctx, log := utiltesting.ContextWithLog(t)
+
+			cl := utiltesting.NewClientBuilder().Build()
+			cqCache := schdcache.New(cl)
+			r := &ClusterQueueReconciler{cache: cqCache}
+			err := cqCache.AddClusterQueue(ctx, tc.queue.DeepCopy())
```

  ```suggestion
  			err := cqCache.AddClusterQueue(ctx, tc.queue)
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:30:31Z

- **pkg/controller/core/localqueue_controller.go:269** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:30:31Z
```diff
@@ -259,13 +258,16 @@ func (r *LocalQueueReconciler) Create(e event.TypedCreateEvent[*kueue.LocalQueue
 	}
 
 	if r.lqMetrics.ShouldExposeLocalQueueMetrics(e.Object.GetLabels()) {
-		recordLocalQueueUsageMetrics(e.Object, r.roleTracker, r.customLabels)
+		r.cache.RecordLocalQueueResourceMetrics(log, e.Object.Spec.ClusterQueue, utilqueue.Key(e.Object))
 	}
 
 	return true
 }
 
 func (r *LocalQueueReconciler) Delete(e event.TypedDeleteEvent[*kueue.LocalQueue]) bool {
+	log := r.logger().WithValues("localQueue", klog.KObj(e.Object))
+	log.V(2).Info("LocalQueue delete event")
```

  Is this required changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:50:19Z

- **pkg/controller/core/clusterqueue_controller_test.go:587** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:50:19Z
```diff
@@ -556,14 +577,32 @@ func TestRecordResourceMetrics(t *testing.T) {
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
-			recordResourceMetrics(tc.queue, nil, nil)
+			ctx, log := utiltesting.ContextWithLog(t)
+
+			cl := utiltesting.NewClientBuilder().Build()
+			cqCache := schdcache.New(cl)
+			r := &ClusterQueueReconciler{cache: cqCache}
+			err := cqCache.AddClusterQueue(ctx, tc.queue)
+			if err != nil {
+				t.Fatalf("Unexpected error err=%v", err)
```

  ```suggestion
  				t.Fatalf("Unexpected error: %v", err)
  ```
  For consistency with other places.
