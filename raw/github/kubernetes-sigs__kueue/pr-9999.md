# PR #9999: Introduce kueue_cohort_admitted_workloads_total metric.

**Summary**: Introduce kueue_cohort_admitted_workloads_total metric.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/9999

**Last updated**: 2026-03-20T16:01:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/cohort_admitted_workloads_total-metric` → `main`
- **Created**: 2026-03-19T08:49:48Z
- **Updated**: 2026-03-20T16:01:25Z
- **Merged**: 2026-03-20T15:58:21Z
- **Closed**: 2026-03-20T15:58:21Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 9   **+504 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Introduce kueue_cohort_admitted_workloads_total metric.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #7539

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: Introduce kueue_cohort_admitted_workloads_total metric
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-19T08:50:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 37531c6c59758db0d01d1d57b8cc1e33c618cc9c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69bd5fbf40846b0009073665 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T10:27:09Z

/cc @mszadkow

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-19T10:57:37Z

/retest 

Due to #9523

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-20T14:41:29Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9999/pull-kueue-test-integration-baseline-main/2034998068872482816
@mbobrovskyi ptal, the test failures are for metrics, so likely related

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T14:55:58Z

> https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/9999/pull-kueue-test-integration-baseline-main/2034998068872482816
@mbobrovskyi ptal, the test failures are for metrics, so likely related

Fixed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T15:57:48Z

LGTM label has been added.  <details>Git tree hash: ee74787302eab40c88c7b4e21f2e193ed4024a3a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T15:57:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/9999#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/9999#pullrequestreview-3982474177" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T14:18:19Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:371** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T14:18:19Z
```diff
@@ -368,6 +368,7 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 			ginkgo.By("Creating two workloads in cqA")
 			wlA1 := createWorkloadWithPriority(cqA.Name, "4", 9001)
 			wlA2 := createWorkloadWithPriority(cqA.Name, "4", 100)
+			util.ExpectCohortSubtreeAdmittedWorkloadsTotalMetric("top-cohort", "", 2)
```

  We should try to keep all metrics relevant tests in `test/integration/singlecluster/scheduler/fairsharing/metrics_test.go`

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-19T14:28:39Z

- **pkg/cache/scheduler/cohort_metrics.go:132** by [@mszadkow](https://github.com/mszadkow) — 2026-03-19T14:28:39Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
+		log.Error(err, "Failed getting ancestors for workload", "workload", wl.Name)
+		return
+	}
+
+	for _, ancestor := range ancestors {
+		metrics.ReportCohortSubtreeAdmittedWorkload(
```

  So, if I get this right.
  For the workload in the given clusterqueue, we check the chain up to the ancestor and we increment counter for each ancestor.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T07:51:10Z

- **pkg/cache/scheduler/cache.go:804** by [@mimowo](https://github.com/mimowo) — 2026-03-20T07:51:10Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  I think this is checking more that admitted - also quota reserved and evicted before the Admission is deleted. Please also double check what workloads are we counting in admitted_workloads_total.
  
  I think for reserving workloads we should have `cohort_subtree_reserving_workloads`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T07:52:57Z

- **pkg/cache/scheduler/cache.go:795** by [@mimowo](https://github.com/mimowo) — 2026-03-20T07:52:57Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  Actually, why excluding the root cohort?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T07:58:26Z

- **pkg/cache/scheduler/cohort_metrics.go:126** by [@mimowo](https://github.com/mimowo) — 2026-03-20T07:58:26Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
```

  ErrCohortHasCycle is the only possible error here. I would suggest not to return the error at all, and log it when we detect the cycle, because now we have the errors propagated unnecessarily. We don't support cycles anyway,.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T08:07:21Z

- **pkg/cache/scheduler/cache.go:795** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:07:21Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  Yeah, we take a decision to skip this in https://github.com/kubernetes-sigs/kueue/pull/4561#discussion_r1993495434.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:16:07Z

- **pkg/cache/scheduler/cache.go:795** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:16:07Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  I see, the decision certainly makes sense for fair sharing, because the root cohort has no other cohort to share resources with.
  
  Here for the root cohort I'm less sure. Let's say you have two root cohorts (independent trees) and you want to know the number of workloads in each of them, then the user would need to aggregate non-trivialy.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T08:32:29Z

- **pkg/cache/scheduler/cache.go:795** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:32:29Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  I created a separate helper that returns a list of ancestors for workloads, including the root, to avoid duplicating code. But to keep the existing logic, we need to skip the root cohort in ClusterQueueAncestors(). That’s why I added this if. I’m not using ClusterQueueAncestors() in the report.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:34:21Z

- **pkg/cache/scheduler/cache.go:795** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:34:21Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  >  But to keep the existing logic, we need to skip the root cohort in ClusterQueueAncestors().
  
  What do you mean by the existing logic, can you provide a code pointer?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T08:36:20Z

- **pkg/cache/scheduler/cache.go:804** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:36:19Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  We have an admitted check in workload_controller where we execute the report function. But here, I don’t think we need to add this logic, since this is just a helper used in ReportCohortSubtreeAdmittedWorkload().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T08:40:38Z

- **pkg/cache/scheduler/cache.go:795** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:40:38Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  Yes, here is one:
  
  https://github.com/kubernetes-sigs/kueue/blob/2bef19904ac7426af01d74a5d439052c7bff9a1e/pkg/cache/scheduler/cache.go#L805

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:42:02Z

- **pkg/cache/scheduler/cache.go:804** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:42:02Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  Let me maybe clarify / rephrase the question:
  1. is the pre-existing metric AdmittedWorkloadsTotal (for ClusterQueue) counting workloads which have QuotaReserved=true, but Admitted=False
  2. is the new metric `CohortSubtreeAdmittedWorkload` counting workloads which have QuotaReserved=true, but Admitted=False
  
  Do we have tests which demonstrate if such workloads are included or excluded from the metric?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T08:48:04Z

- **pkg/cache/scheduler/cache.go:804** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:48:04Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  > is the pre-existing metric AdmittedWorkloadsTotal (for ClusterQueue) counting workloads which have QuotaReserved=true, but Admitted=False
  
  Now we report AdmittedWorkloadsTotal only if Admitted=True.
  
  https://github.com/epam/kubernetes-kueue/blob/4b862c0e50080e7eaa5e004e69e636b43c9186be/pkg/controller/core/workload_controller.go#L494-L500
  
  https://github.com/epam/kubernetes-kueue/blob/4b862c0e50080e7eaa5e004e69e636b43c9186be/pkg/scheduler/scheduler.go#L907-L915
  
  > is the new metric CohortSubtreeAdmittedWorkload counting workloads which have QuotaReserved=true, but Admitted=False
  
  No, we report CohortSubtreeAdmittedWorkload in the same place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:51:46Z

- **pkg/cache/scheduler/cache.go:804** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:51:46Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  ok do we ha e an integration test to confiem that? lets make sure we have

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-03-20T12:17:30Z

- **pkg/metrics/metrics.go:739** by [@mwielgus](https://github.com/mwielgus) — 2026-03-20T12:12:59Z
```diff
@@ -728,6 +732,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeAdmittedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_admitted_workloads_total ",
```

  Remove trailing space.

- **site/content/en/docs/reference/metrics.md:85** by [@mwielgus](https://github.com/mwielgus) — 2026-03-20T12:13:27Z
```diff
@@ -82,6 +82,7 @@ The following metrics are available only if `LocalQueueMetrics` feature gate is
 <!-- BEGIN GENERATED TABLE: cohort -->
 | Metric name | Type | Description | Labels |
 | --- | --- | --- | --- |
+| `kueue_cohort_subtree_admitted_workloads_total ` | Counter | The total number of admitted workloads per cohort's subtree | `cohort`: the name of the Cohort<br> `priority_class`: the priority class name<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
```

  Trailing space.

- **pkg/cache/scheduler/cohort_metrics.go:126** by [@mwielgus](https://github.com/mwielgus) — 2026-03-20T12:14:19Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
+		log.Error(err, "Failed getting ancestors for workload", "workload", wl.Name)
```

  Use kobj(w) so that the namespace is included as well.

- **pkg/cache/scheduler/cache.go:803** by [@mwielgus](https://github.com/mwielgus) — 2026-03-20T12:15:35Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
```

  What is the locking contract here?

- **pkg/cache/scheduler/cache.go:819** by [@mwielgus](https://github.com/mwielgus) — 2026-03-20T12:16:19Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
 		return nil, nil
 	}
 
-	cohort := c.hm.Cohort(cqObj.Spec.CohortName)
+	cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if cq == nil || !cq.HasParent() {
+		return nil, nil
+	}
+
+	return c.ancestors(cq.Parent().Name)
+}
+
+func (c *Cache) ancestors(cohortName kueue.CohortReference) ([]kueue.CohortReference, error) {
```

  Document the locking contract here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T12:56:02Z

- **pkg/metrics/metrics.go:739** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T12:56:02Z
```diff
@@ -728,6 +732,14 @@ If the Cohort has a weight of zero and is borrowing, this will return NaN.`,
 			Help:      `Reports the cohort's nominal quota aggregated within the cohort's subtree. The values are reported per resource and flavor`,
 		}, []string{"cohort", "flavor", "resource", "replica_role"},
 	))
+
+	CohortSubtreeAdmittedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "cohort_subtree_admitted_workloads_total ",
```

  Thank you!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T12:56:08Z

- **pkg/cache/scheduler/cohort_metrics.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T12:56:08Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
+		log.Error(err, "Failed getting ancestors for workload", "workload", wl.Name)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T12:56:17Z

- **pkg/cache/scheduler/cache.go:803** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T12:56:17Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
```

  Added.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T12:56:22Z

- **pkg/cache/scheduler/cache.go:819** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T12:56:22Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
 		return nil, nil
 	}
 
-	cohort := c.hm.Cohort(cqObj.Spec.CohortName)
+	cq := c.hm.ClusterQueue(wl.Status.Admission.ClusterQueue)
+	if cq == nil || !cq.HasParent() {
+		return nil, nil
+	}
+
+	return c.ancestors(cq.Parent().Name)
+}
+
+func (c *Cache) ancestors(cohortName kueue.CohortReference) ([]kueue.CohortReference, error) {
```

  Added.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T13:08:48Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:371** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T13:08:48Z
```diff
@@ -368,6 +368,7 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 			ginkgo.By("Creating two workloads in cqA")
 			wlA1 := createWorkloadWithPriority(cqA.Name, "4", 9001)
 			wlA2 := createWorkloadWithPriority(cqA.Name, "4", 100)
+			util.ExpectCohortSubtreeAdmittedWorkloadsTotalMetric("top-cohort", "", 2)
```

  I’m actually thinking that for this metric it’s better to keep it here instead of creating more tests, to keep it close to `ExpectAdmittedWorkloadsTotalMetric()`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T13:09:53Z

- **pkg/cache/scheduler/cohort_metrics.go:132** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T13:09:53Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
+		log.Error(err, "Failed getting ancestors for workload", "workload", wl.Name)
+		return
+	}
+
+	for _, ancestor := range ancestors {
+		metrics.ReportCohortSubtreeAdmittedWorkload(
```

  Yes, exactly.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T13:11:58Z

- **pkg/cache/scheduler/cohort_metrics.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T13:11:58Z
```diff
@@ -115,3 +116,23 @@ func (c *Cache) applyCohortMetricPoint(p cohortMetricPoint) {
 		c.roleTracker,
 	)
 }
+
+func (c *Cache) ReportCohortSubtreeAdmittedWorkload(log logr.Logger, wl *kueue.Workload) {
+	c.RLock()
+	defer c.RUnlock()
+
+	ancestors, err := c.workloadAncestors(wl)
+	if err != nil {
```

  Do you propose propagating the logger to the ancestors() function or just to workloadAncestors()? Because if it’s ancestors(), we would also need to change the current logic.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T13:57:19Z

- **pkg/cache/scheduler/cache.go:804** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T13:57:19Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
+
+	return ancestors, nil
+}
+
+func (c *Cache) workloadAncestors(wl *kueue.Workload) ([]kueue.CohortReference, error) {
+	if wl == nil || wl.Status.Admission == nil {
```

  Added test case.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T14:06:20Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:371** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T14:06:20Z
```diff
@@ -368,6 +368,7 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 			ginkgo.By("Creating two workloads in cqA")
 			wlA1 := createWorkloadWithPriority(cqA.Name, "4", 9001)
 			wlA2 := createWorkloadWithPriority(cqA.Name, "4", 100)
+			util.ExpectCohortSubtreeAdmittedWorkloadsTotalMetric("top-cohort", "", 2)
```

  Also added separate test case with admission check.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-20T14:56:20Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:371** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T14:56:19Z
```diff
@@ -368,6 +368,7 @@ var _ = ginkgo.Describe("Scheduler", ginkgo.Label("feature:fairsharing"), func()
 			ginkgo.By("Creating two workloads in cqA")
 			wlA1 := createWorkloadWithPriority(cqA.Name, "4", 9001)
 			wlA2 := createWorkloadWithPriority(cqA.Name, "4", 100)
+			util.ExpectCohortSubtreeAdmittedWorkloadsTotalMetric("top-cohort", "", 2)
```

  OK. Moved all tests to metrics.go.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T15:56:33Z

- **pkg/cache/scheduler/cache.go:795** by [@mimowo](https://github.com/mimowo) — 2026-03-20T15:56:33Z
```diff
@@ -784,14 +784,42 @@ func (c *Cache) ClusterQueueAncestors(cqObj *kueue.ClusterQueue) ([]kueue.Cohort
 	c.RLock()
 	defer c.RUnlock()
 
-	if cqObj.Spec.CohortName == "" {
+	ancestors, err := c.ancestors(cqObj.Spec.CohortName)
+	if err != nil {
+		return nil, err
+	}
+
+	// Exclude the root cohort
+	if len(ancestors) > 0 {
+		ancestors = ancestors[:len(ancestors)-1]
+	}
```

  sgtm, I see the metric is generated for the roots: https://github.com/kubernetes-sigs/kueue/pull/9999/changes#diff-119d4450e4fb8bfe8786ea5c0ff81ee57e31eea554356d79a7a16dd4e1e83d9fR617-R618
  
  Yeah, I don't want to change the logic for the other metric, it is good that we skip the root there. 
  
  For this metric knowing the number of workloads in the top cohort makes sense.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T15:57:40Z

Thank you 👍 
/lgtm
/approve
